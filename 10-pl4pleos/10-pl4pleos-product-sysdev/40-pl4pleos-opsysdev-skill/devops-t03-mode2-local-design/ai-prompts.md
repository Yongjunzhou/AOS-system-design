# DevOps 流程第3步 AI 提示词
**DevOps Step 3: AI Prompt Templates — Mode 2: Local Design**

**文档版本**：v1.0
**创建日期**：2026-05-28
**对应场景**：04-devops · 第3步 · 模式2
**对应操作规程**：`task-definition.md`（同目录）

---

## 一、跨步骤上下文管理

### 1.1 上下文传递方式

DevOps 模式2流程中，本步的输入来自 t01，产出直接发布。AI 在执行时应确保完整继承了 t01 的关键决策：

| 继承内容 | 说明 | 保存位置 |
|---------|------|---------|
| 问题定位 | 受影响的 SysReq 和 PA 节点 | t01 决策记录 |
| 模式确认 | 模式2（SysReq + PA） | t01 决策记录 |
| 待确认项 | t01 标记的 ⚠️ 项 | t01 决策记录 |

### 1.2 决策记录机制

本步完成后，AI 将本步的关键决策记录在**决策记录模板**（见第四章）中。

### 1.3 回溯修正

如果在发布前发现需要修改 t01 的评估：
1. 评估是否影响 t01 的模式选择
2. 如模式选择需变更，返回 t01 重新评估
3. 标注变更记录

---

## 二、AI 经济原则

本步操作以下列顺序执行，AI 需注意：

1. **执行顺序不可逆**：SysReq 定位 → 影响分析 → SysReq 增量定义 → PA 增量定义及映射更新 → 回归验证 → 审查
2. **快速响应优先**：DevOps 场景追求最短时间完成修复，1 天内完成
3. **最小化变更原则**：只修改受影响的 SysReq 和 PA 节点及映射关系

## 三、本步提示词模板

### 3.1 本步提示词

具体提示词模板请参考以下文件中的第3步（模式2部分）：

`../../../../30-eos-pipeline-system-design-ai-support/04-devops-eos-pipeline-system-design/workflow-prompts.md`

```
请在开始前确认上下文状态：
- 当前处理的 DevOps 模式2：局部设计
- 输入：t01 影响范围评估报告（模式2）
- 参考基线：现有 SysReq 文档 + PA 文档

影响范围评估摘要：
[粘贴 t01 评估结果的受影响 SysReq 和 PA 节点]

请根据 task-definition.md 的阶段定义依次执行，注意保持 SysReq → PA 映射的一致性。
```

## 四、决策记录模板

```
本步关键决策记录：
├─ 阶段状态：
│  ├─ SysReq定位：[受影响的 SysReq 末级节点清单]
│  ├─ PA定位：[受影响的 PA 末级节点清单]
│  ├─ 影响分析：[符合性分析结果]
│  ├─ SysReq 增量：[~修改 N 处 / +新增 N 处]
│  ├─ PA 增量：[~修改 N 处 / +新增 N 处]
│  ├─ 映射更新：[SysReq→PA映射更新内容]
│  └─ 回归验证：[通过/未通过]
├─ 变更清单：
│  ├─ ~ 修改：[修改内容 + 变更说明]
│  └─ + 新增：[新增内容 + 新增原因]
├─ 升级判断：[是否需要升级为模式3？是/否 + 理由]
├─ ⚠️ 待确认项：[汇总需要人工确认的项]
└─ 执行模式：[全自动/半自动]
```

---

## 五、审查修复流程

### 5.1 审查输入

AI 完成本步后，输出产物供 rule-conformance-review 审查。审查报告包含：
- 通过/未通过的项目清单
- 问题定位（涉及的具体文件、章节、编号）
- 问题类型（结构性/内容性/格式性）

### 5.2 AI 修复流程

收到审查报告后，AI 按以下优先级处理：

1. **格式性问题**（增量标记不规范）→ 直接修复，不需要人工确认
2. **内容缺失**（SysReq/PA 定义不完整）→ 补充内容，标注修复说明
3. **结构性问题**（映射关系不一致、修改范围过大）→ 提出修复方案，标注 ⚠️ 待确认
4. **跨步骤一致性问题** → 评估是否需升级为模式3

### 5.3 影响范围评估

| 问题定位 | 影响范围 | 修复策略 |
|---------|---------|---------|
| SysReq 节点定位错误 | 下游 PA 映射 | 重新定位并更新 PA 映射 |
| 修改超出 SysReq/PA 层 | 违反模式2条件 | 升级为模式3 |
| 映射关系不一致 | SysReq → PA 链路 | 重新验证并同步更新映射 |
| 回归验证失败 | 关联功能 | 扩展修复范围或升级模式 |

---

## 变更日志

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-05-28 | v1.0 | 首次构建 |
