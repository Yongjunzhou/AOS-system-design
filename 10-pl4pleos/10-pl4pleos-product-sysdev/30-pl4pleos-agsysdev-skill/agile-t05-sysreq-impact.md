# 系统需求影响分析
**Skill ID**: agile-t05 · **Step 5: System Requirements Impact Analysis**

> **概述**：分析新需求对系统需求的影响，输出 SysReq 增量定义（同时处理功能和非功能分支）。
>
> **输入 → 输出**：BA 增量定义 + BA→SysReq 骨架映射 → SysReq 增量定义 + SysReq→PA 骨架映射
>
> **适用场景**：敏捷式第5步。基于 BA 增量对 SysReq 层做增量更新。
>
> **版本**：v1.0 | **修订**：2026-06-03

---

## 一、设计角色

本步在设计链中承担的角色：
- **输入**：BA 增量定义 + BA → SysReq 骨架映射（task-04 产出），现有 SysReq 文档基线
- **产出1**：SysReq 增量定义（功能 0-9 级 + 非功能 0-6 级）
- **产出2**：SysReq → PA 骨架映射

### 与三条工程化设计原则的关系

| 原则 | 在本步中的体现 |
|------|--------------|
| **详概分离** | 本步仅产出 SysReq 层的增量定义（功能 + 非功能），不涉及未受影响的 SysReq 节点 |
| **文档结对** | BA IPO（需求方）↔ SysReq 节点（方案方）。功能和非功能分支分别结对 |
| **基于资产** | 基于现有 SysReq 文档判断承接方式。遵循复用→改进→新增的资产优先规则 |

---

## 二、前置条件

| 条件 | 说明 |
|------|------|
| 输入 | BA 增量定义 + BA→SysReq 骨架映射（task-04 产出），现有 SysReq 文档基线（`05-*.md` + `06-*.md`） |
| 参考 | 通用设计准则 §七.5（系统功能架构定义）；元流水线设计准则 |

### 文档结对关系

功能分支（t03→t04→t05）与非功能分支（t03 NFR→t05 非功能部分）在此处合并处理。

| 结对 | 需求方 | 方案方 | 说明 |
|------|--------|--------|------|
| 增量结对4a（功能分支） | BA IPO（增量） | SysReq 功能节点 | 功能分支影响分析 → 增量定义 |
| 增量结对4b（非功能分支） | SR NFR（增量） | SysReq 非功能节点 | 非分支影响分析 → 增量定义 |
| 骨架产出 | SysReq 增量 | PA 组件骨架 | 同步建立映射 |

---

## 三、操作流程

五阶段：SysReq 影响分析 → 功能增量定义 → 非功能增量定义 → 同步建骨架 → 审查。

### 启动方式

> 开始敏捷式 t05（或 agile-t05-sysreq-impact）

---

## 四~九

同 EOS 版本 agile-t05（路径调整为 `80-pl4pleos-opsdata/`，不涉及双设计链概念）。

---

## 变更日志

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-06-03 | v1.0 | 首次构建 |
