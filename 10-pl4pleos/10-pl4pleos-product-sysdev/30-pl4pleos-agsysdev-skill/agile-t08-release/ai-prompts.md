# 敏捷式流程第8步 AI 提示词
**Agile Step 8: AI Prompt Templates — Document Completion and Release**

**文档版本**：v1.0
**创建日期**：2026-05-28
**对应场景**：03-agile · 第8步
**对应操作规程**：`task-definition.md`（同目录）

---

## 一、跨步骤上下文管理

### 1.1 上下文传递方式

8 步流程中，每一步的产出是下一步的核心输入。AI 在执行每一步前，应确保完整继承了上一步的关键决策：

| 继承内容 | 说明 | 保存位置 |
|---------|------|---------|
| 架构决策 | 合并/拆分、分类方式、层级结构 | 上一步的决策记录 |
| 编号体系 | 所有已分配的 ID 和编号规则 | 各产品数据文件 |
| 待确认项 | 上一步标记的 ⚠️ 项 | 汇总清单 |
| AI推断记录 | AI 自行判断补齐的内容 | 决策记录文件的 AI推断项 |

### 1.2 决策记录机制

每步完成后，AI 将本步的关键决策记录在**本步决策记录模板**（见本章末尾）中，供后续步骤引用。

### 1.3 回溯修正

如果在第 N 步发现需要修改第 M 步（M < N）的产出：
1. 评估影响范围（见 AI决策框架 §7.2 跨步骤影响范围表）
2. 定位到受影响的具体步骤和节点
3. 修改后检查下游映射是否需要同步调整
4. 标注变更记录

---

## 二、AI 经济原则

本步操作以下列顺序执行，AI 需注意：

1. **执行顺序不可逆**：审查修复 → 变更汇总 → 生成日志 → 发布准备
   核心驱动力：先处理验证报告的问题，再汇总变更并生成发布物。确保所有问题闭环后再发布
2. **基于资产优先**：本次迭代的完整设计文档集为发布资产。v2 增量区与 v1 基线区合并，生成新的 v2 基线（冻结·只读），作为下一次迭代的资产
3. **增量设计特点**：本步是 8 步敏捷流程的最后一步，不涉及新设计工作。核心工作是：处理验证问题闭环、合并增量和基线、生成变更日志和版本发布说明。变更日志按层分类（OR/SR/BA/SysReq/PA），清晰记录本次迭代的范围

---

## 三、本步提示词模板

### 3.1 本步提示词

详细 AI 提示词模板见：
`30-eos-pipeline-system-design-ai-support/03-agile-eos-pipeline-system-design/workflow-prompts.md` 第8步

请在开始前确认上下文状态：

- 是否已加载 task-01 ~ task-06 的全部增量产出？
- 是否已加载 task-07 产出的验证报告？
- 是否已加载本次迭代前的完整基线文档？
- 验证报告中的问题是否已全部理解（哪些是格式性问题可直接修复，哪些是结构性问题需提方案）？
- 变更日志的按层分类汇总是否已有初步框架？

具体操作按 task-definition.md 五个阶段执行：审查修复 → 变更汇总 → 生成日志 → 发布准备 → 终验。

---

## 四、决策记录模板

```
本步关键决策记录：
├─ 阶段状态：
│  ├─ 审查修复：[已完成] — [格式性问题 N 条 / 内容缺失 N 条 / 结构性问题 N 条]
│  ├─ 变更汇总：[已完成] — [OR 层 N 条 / SR 层 N 条 / BA 层 N 条 / SysReq 层 N 条 / PA 层 N 条]
│  ├─ 变更日志：[已完成] — [按层分类变更明细]
│  └─ 版本发布说明：[已完成] — [新版本号]
├─ 修复记录：
│  ├─ 已直接修复：[N 项 — 格式性问题/内容缺失]
│  ├─ 标记 ⚠️ 待确认：[N 项 — 结构性问题，提了方案等确认]
│  └─ 未修复（遗留）：[N 项 — 说明原因]
├─ 版本信息：
│  ├─ 新版本号：[vX.Y]
│  └─ 发布日期：[日期]
├─ ⚠️ 终验待确认项：[汇总需要人工终验确认的项]
└─ 执行模式：[全自动/半自动]
```

---

## 五、审查修复流程

### 5.1 审查输入

AI 完成指定步骤后，输出产物供 rule-conformance-review 审查。审查报告包含：
- 通过/未通过的项目清单
- 问题定位（涉及的具体文件、章节、编号）
- 问题类型（结构性/内容性/格式性）

### 5.2 AI 修复流程

收到审查报告后，AI 按以下优先级处理：

1. **格式性问题**（编号错误、格式不规范）→ 直接修复，不需要人工确认
2. **内容缺失**（某节为空、映射不完整）→ 补充内容，标注修复说明
3. **结构性问题**（粒度不匹配、分类错误）→ 提出修复方案，标注 ⚠️ 待确认
4. **跨步骤一致性问题** → 参考AI决策框架的跨步骤影响范围表进行影响范围评估，列出所有受影响的下游节点

### 5.3 影响范围评估

| 问题定位 | 影响范围 | 修复策略 |
|---------|---------|---------|
| 某步的架构定义 | 本层 + 下游已建立映射的节点 | 修改后同步更新映射 |
| 详细定义 | 下层方案 | 修改后检查下层是否受影响 |
| 映射关系 | 所有关联节点 | 需重新运行追溯验证 |
| 编号变更 | 全链路 | 需逐层更新引用，重新验证 |

---

## 变更日志

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-05-28 | v1.0 | 首次构建 |
