# 产品架构影响分析
**Skill ID**: agile-t06 · **Step 6: Product Architecture Impact Analysis**

> **概述**：分析新需求对产品架构的影响，输出 PA 增量定义。
>
> **输入 → 输出**：SysReq 增量定义 + SysReq→PA 骨架映射 → PA 增量定义（更新 `07-*.md`）
>
> **适用场景**：敏捷式第6步。设计链增量更新的最后一层（t01→t06 完成全链路增量更新）。
>
> **版本**：v1.1 | **修订**：2026-06-03

---

## 一、设计角色

本步在设计链中承担的角色：
- **输入**：SysReq 增量定义 + SysReq→PA 骨架映射（task-05 产出），现有 PA 文档基线
- **产出**：更新后的 PA 文档（增量更新 `07-*.md`）

### 与三条工程化设计原则的关系

| 原则 | 在本步中的体现 |
|------|--------------|
| **详概分离** | 本步仅产出 PA 层的增量定义（架构+详细），不涉及未受影响的 PA 组件 |
| **文档结对** | SysReq 详细末级（需求方）↔ PA 组件（方案方） |
| **基于资产** | 基于现有 PA 文档和通用设计准则 §六判断构件合并/分离 |

---

## 二、前置条件

| 条件 | 说明 |
|------|------|
| 输入 | SysReq 增量定义 + SysReq→PA 骨架映射（task-05 产出），现有 PA 文档基线（`07-*.md`） |
| 参考 | 通用设计准则 §五.6 SysReq→PA、§2.4 产品架构分解原则；元流水线设计准则 §4.1 第6步 |

### 文档结对关系

本步执行 SysReq 层到 PA 层的增量映射：

| 结对 | 需求方 | 方案方 | 本步角色 | 工作方式 |
|------|--------|--------|---------|---------|
| 增量结对5 | SysReq 详细末级（增量） | PA 组件（架构+详细） | 需求方 + 方案方 | PA 影响分析 → 构件判断 → 增量定义 → 更新映射 |
| 与 t07 衔接 | PA 增量定义 | 验证报告 | t07 执行 | t07 验证全部增量的一致性 |

---

## 三、操作流程

本步按五个阶段的顺序执行：PA 影响分析 → PA 增量定义 → 补充详细定义 → 更新映射 → 审查。

核心驱动逻辑：从 SysReq 增量 + 骨架映射出发，按 §2.4 三因素判断构件合并/分离，使用增量标记更新 PA 文档。AI 无法判断时标记 ❓ 留给人类。

操作流程概览：

```
[阶段一] PA 影响分析            [阶段二] PA 增量定义             [阶段三] 补充详细定义
  需求说明符合性分析              遵循 §2.4 三因素                 因产品类型而异
  需求性能符合性分析              构件合并/分离判断                 [阶段四] 更新映射
                                                                  SysReq→PA 映射表
[阶段五] 审查
  结果审核（人类审）
```

### 阶段一：PA 影响分析

| # | 操作 | 执行者 | 说明 |
|---|------|--------|------|
| 1 | PA 影响分析 | AI | 需求说明符合性分析（SysReq 增量是否能由现有 PA 组件承接）+ 需求性能符合性分析（修改是否影响性能指标范围） |

### 阶段二：PA 增量定义

| # | 操作 | 执行者 | 说明 |
|---|------|--------|------|
| 2 | PA 增量条目定义 | AI | 遵循 §2.4 产品架构分解原则（三因素）判断构件合并/分离，使用增量标记（`+`/`~`） |

### 阶段三：补充详细定义

| # | 操作 | 执行者 | 说明 |
|---|------|--------|------|
| 3 | 补充组件详细定义 | AI | 因产品类型而异（软件/方法论），补充增量组件的详细规格 |

### 阶段四：更新映射

| # | 操作 | 执行者 | 说明 |
|---|------|--------|------|
| 4 | 更新映射关系表 | AI | SysReq → PA 映射 |

### 阶段五：审查

| # | 操作 | 执行者 | 说明 |
|---|------|--------|------|
| 5 | 结果审核 | 人类 | 确认 PA 更新 |

### 启动方式

人类向 AI 发出以下指令即可启动：

> 开始敏捷式 t06（或 agile-t06-pa-impact）

AI 动作序列：读取本目录下 task-definition.md + ai-prompts.md → 读取 t05 产出的 SysReq 增量定义 + SysReq→PA 骨架映射 → 加载现有 PA 文档基线（`07-*.md`）→ 按上述五阶段流程执行 → 全部完成后通知人类审核。

---

## 四、AI执行指南

### 4.1 跨步骤上下文管理

#### 上下文传递方式

8 步流程中，每一步的产出是下一步的核心输入。AI 在执行每一步前，应确保完整继承了上一步的关键决策：

| 继承内容 | 说明 | 保存位置 |
|---------|------|---------|
| 架构决策 | 合并/拆分、分类方式、层级结构 | 上一步的决策记录 |
| 编号体系 | 所有已分配的 ID 和编号规则 | 各产品数据文件 |
| 待确认项 | 上一步标记的 ⚠️ 项 | 汇总清单 |
| AI推断记录 | AI 自行判断补齐的内容 | 决策记录文件的 AI推断项 |

#### 决策记录机制

每步完成后，AI 将本步的关键决策记录在**本步决策记录模板**（见§六）中，供后续步骤引用。

#### 回溯修正

如果在第 N 步发现需要修改第 M 步（M < N）的产出：
1. 评估影响范围（见§七.2 跨步骤影响范围表）
2. 定位到受影响的具体步骤和节点
3. 修改后检查下游映射是否需要同步调整
4. 标注变更记录

### 4.2 AI经济原则

本步操作以下列顺序执行，AI 需注意：

1. **执行顺序不可逆**：PA 影响分析 → PA 增量条目定义 → 补充组件详细定义 → 更新映射关系表
   核心驱动力：从 SysReq 增量 + 骨架映射出发，按 §2.4 三因素判断构件合并/分离，使用增量标记更新 PA 文档
2. **基于资产优先**：基于现有 PA 文档（`07-*.md` 架构定义 + 详细定义）和通用设计准则 §2.4 产品架构分解原则判断承接方式
3. **增量设计特点**：本步是设计链增量更新的最后一层（t01→t06 完成 OR→SR→BA→SysReq→PA 的全链路增量更新）。PA 组件增量定义需根据产品类型（软件/方法论）有所不同。组件详细定义的补充因产品类型而异

---

## 五、AI提示词模板

### 5.1 本步提示词

详细 AI 提示词模板见：
`30-eos-pipeline-system-design-ai-support/03-agile-eos-pipeline-system-design/workflow-prompts.md` 第6步

请在开始前确认上下文状态：

- 是否已加载 t05 产出的 SysReq 增量定义 + SysReq→PA 骨架映射？
- 是否已加载现有 PA 文档基线（`07-*.md` 架构定义 + 详细定义）？
- 是否已参考通用设计准则 §2.4 产品架构分解原则（三因素）？
- 目标产品类型是软件产品还是方法论产品？（影响组件详细定义的补充方式）
- §2.4 三因素（职责内聚性/变更隔离性/部署独立性）的判断标准是否明确？

具体操作按 §三 五个阶段执行：PA 影响分析 → 增量定义 → 补充详细定义 → 更新映射 → 审查。

---

## 六、决策记录模板

```
本步关键决策记录：
├─ 阶段状态：
│  ├─ PA 影响分析：[已完成] — [通过/未通过项]
│  ├─ PA 增量条目：[已完成] — [+ 新增 N 条 / ~ 修改 N 条]
│  ├─ 组件详细定义：[已完成] — [软件/方法论类型，详细定义补充 N 条]
│  └─ 映射关系表：[已完成] — [N 条 SysReq→PA 映射]
├─ 资产使用记录：
│  ├─ 资产来源：[07-*.md（现有 PA 基线）+ §2.4 分解原则]
│  ├─ 复用节点：[N 个]
│  ├─ 改进（扩展）节点：[N 个，记录每个的扩展内容]
│  └─ 新增节点：[N 个，记录新增原因]
├─ §2.4 三因素决策记录：
│  ├─ 职责内聚性：[判断依据]
│  ├─ 变更隔离性：[判断依据]
│  └─ 部署独立性：[判断依据]
├─ 编号体系：[新 PA 节点编号范围]
├─ ⚠️ 待确认项：[汇总需要人工确认的项]
├─ AI推断记录：[AI自行判断补齐的模糊术语/缺失内容]
├─ 待传递的约束：[后续步骤需要注意的决策/约束]
└─ 执行模式：[全自动/半自动]
```

---

## 七、审查流程

### 7.1 审核要点

- [ ] 组件划分合理性（§2.4 三因素应用）
- [ ] 技术决策正确性
- [ ] PA 分类体系一致性
- [ ] 详细定义同步更新
- [ ] 映射关系完整性

### 7.2 审查修复流程

#### 审查输入

AI 完成指定步骤后，输出产物供 rule-conformance-review 审查。审查报告包含：
- 通过/未通过的项目清单
- 问题定位（涉及的具体文件、章节、编号）
- 问题类型（结构性/内容性/格式性）

#### AI 修复流程

收到审查报告后，AI 按以下优先级处理：

1. **格式性问题**（编号错误、格式不规范）→ 直接修复，不需要人工确认
2. **内容缺失**（某节为空、映射不完整）→ 补充内容，标注修复说明
3. **结构性问题**（粒度不匹配、分类错误）→ 提出修复方案，标注 ⚠️ 待确认
4. **跨步骤一致性问题** → 参考AI决策框架的跨步骤影响范围表进行影响范围评估，列出所有受影响的下游节点

#### 影响范围评估

| 问题定位 | 影响范围 | 修复策略 |
|---------|---------|---------|
| 某步的架构定义 | 本层 + 下游已建立映射的节点 | 修改后同步更新映射 |
| 详细定义 | 下层方案 | 修改后检查下层是否受影响 |
| 映射关系 | 所有关联节点 | 需重新运行追溯验证 |
| 编号变更 | 全链路 | 需逐层更新引用，重新验证 |

---

## 八、质量门禁

- [ ] PA 影响分析完成（需求说明 + 性能符合性分析）
- [ ] PA 增量条目已定义（遵循 §2.4 三因素）
- [ ] 组件详细定义已补充（因产品类型而异）
- [ ] SysReq → PA 映射关系表已更新
- [ ] SysReq → PA 映射正确（1:1 约束满足）
- [ ] 增量部分与基线已正确区隔（`+`/`~` 标记）
- [ ] 运行 rule-conformance-review 进行符合性审查，按审查修复流程处理问题

---

## 九、产出物

| 文件 | 层内角色 | 说明 | 路径 |
|------|---------|------|------|
| 产品架构定义（更新） | PA 层 | 增量更新后的 PA 架构定义 + 详细定义 | `80-pl4pleos-opsdata/07-pl4pleos-product-architecture.md` |

---

## 变更日志

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-06-03 | v1.1 | 全量展开 §四~§九，补充 AI 执行指南、提示词模板、决策记录模板、审查流程、质量门禁及产出物 |
| 2026-06-03 | v1.0 | 首次构建 |
