# 瀑布式流程第1步 AI 提示词
**Waterfall Step 1: AI Prompt Templates — Original Requirements → Stakeholder Requirements Architecture**

**文档版本**：v1.3
**创建日期**：2026-05-28
**修订日期**：2026-06-03
**对应场景**：01-waterfall · 第1步
**对应操作规程**：`task-definition.md`（同目录）

---

## 一、跨步骤上下文管理

### 1.1 上下文传递方式

6 步流程中，每一步的产出是下一步的核心输入。AI 在执行每一步前，应确保完整继承了上一步的关键决策：

| 继承内容 | 说明 | 保存位置 |
|---------|------|---------|
| 架构决策 | 合并/拆分、分类方式、层级结构 | 上一步的决策记录 |
| 编号体系 | 所有已分配的 ID 和编号规则 | 各产品数据文件 |
| 待确认项 | 上一步标记的 ⚠️ 项 | 汇总清单 |
| AI推断记录 | AI 自行判断补齐的内容 | 决策记录文件的 AI推断项 |

### 1.2 决策记录机制

每步完成后，AI 将本步的关键决策记录在**本步决策记录模板**（见本章末尾）中，供后续步骤引用。

### 1.3 回溯修正

如果在第 N 步发现需要修改第 M 步（M < N）的产出：
1. 评估影响范围（见 AI决策框架 §7.2 跨步骤影响范围表）
2. 定位到受影响的具体步骤和节点
3. 修改后检查下游映射是否需要同步调整
4. 标注变更记录

---

## 二、AI 经济原则

本步操作以下列顺序执行，AI 需注意：

1. **执行顺序不可逆**：OR 资产加载 → OR 分解与 SR 承接（同步）→ SR 节点完善 → 闭环检查
   核心驱动力：需求从源头（OR）出发，逐条向 SR 架构找宿主。SR 架构的调整（扩展/新增）由 OR 需求驱动，不是预设的
2. **基于资产优先**：SR 架构调整决策应先检查已有资产（复用），确实需要修改时再改进，最后才新增
3. **资产路径已知**：本步的资产是 `80-pl4pleos-opsdata/02-pl4pleos-stakeholder-requirements-architecture.md`，其中已存在 v1 基线内容（只读状态），是 SR 架构决策的首要资产来源。`01-pl4pleos-original-requirements.md` 是本步的输入数据（00-or-preprocessing 产出的 OR 条目），不是"资产"

## 三、本步提示词模板

### 3.1 加载 OR 资产（加载 00-or-preprocessing 产出）

```
请加载 00-or-preprocessing 产出的 OR 条目清单，作为本步 OR→SR 映射的输入。

OR 条目清单：[粘贴 00-or-preprocessing 产出的 OR 条目]

请确认以下内容：
1. 所有 OR 条目已完成碎片整理、规范化和冲突检测
2. 每个 OR 条目包含：标题、规范化描述、类型（功能/非功能）
3. 每个 OR 条目保留了原始出处（提出方、日期）
4. 冲突/重复已标注

输出：确认可用的 OR 条目清单
```

### 3.2 OR 分解到末级 + 同步找 SR 承接节点

> 先了解资产再动手——分解前加载现有SR架构，理解每个节点的职责边界。然后面对每条OR条目时，已经知道"哪个方向有节点、哪个方向还空着"，有方向地去分解。

```
请先加载现有SR架构资产，理解其分类结构和各节点职责边界后，再逐条处理OR条目。

OR 条目清单：[粘贴3.1的输出]

—— 第一步：加载并理解 SR 架构资产（先做到心中有数）——

资产文件：80-pl4pleos-opsdata/02-pl4pleos-stakeholder-requirements-architecture.md

加载所有架构末级节点，对每个节点理解：
├─ 节点名称和定义 — 这个节点"管什么"？
├─ 职责边界 — 它的覆盖范围到哪里为止？相邻节点之间的分界在哪里？
├─ 已承接的OR — 这个节点目前已经承接了哪些类别的OR？

整体理解：
├─ 功能需求（SR-F）按什么逻辑分类？（按相关方？按功能域？）
├─ 非功能需求（SR-NF）分了哪几类？（质量特性/环境适应性/可实现性）
└─ 哪个分类下节点密集（已覆盖充分），哪个分类下节点稀疏（可能需要补充）

—— 第二步：逐条 OR 条目分解 + 同步找 SR 宿主 ——

对每条 OR 条目（OR-XXX），执行以下操作：

0. 【前置判断】这条OR条目是否涉及多个SR职责范围？
├─ 例如：OR 描述中同时涉及"规范化"和"架构定义" → 这两个分属不同SR节点
├─ 如果是 → 先按涉及的职责范围分拆，再分别执行下面的分解步骤
└─ 如果不是 → 直接进入步骤1

1. 【分解】原子化分解：
├─ 原则：原子性、语义完整性、粒度一致性
├─ 触发分解条件：
│  ├─ 包含"和/并且/同时"连接多个功能 → 分解
│  ├─ 涉及多个角色或相关方 → 分解
│  └─ 涉及多个SR职责范围 → 分解（由前置判断的结果决定）
├─ 停止准则："在不依赖SR知识的情况下，这也是一条不可再分的单一诉求吗？"
│  （先独立判断原子性，再用SR知识验证）
│  - 是 → 得到末级 OR-XXX.X
│  - 否 → 继续分解
└─ 输出：OR-XXX.X [原子功能描述]

2. 【找宿主】对每个 OR-XXX.X，在已理解的 SR 知识中找承接节点：

   判断维度（用三个问题来决策）：
   ├─ Q1 职责方向匹配：此OR的核心意图，与哪个SR节点的职责方向一致？
   │  例："定义第1步流程" — SR-F-006（规范化）? SR-F-007（分解）? SR-F-001（准则体系）?
   │  判断依据：SR节点定义中描述的主语和核心动作
   │
   ├─ Q2 覆盖范围偏差：此OR描述的具体内容，是否在SR节点定义的边界之内？
   │  例：SR-F-006定义是"需求规范化规则和分类方法" → 
   │  "第1步流程" > "规范化规则"，超出了SR-F-006的边界
   │
   └─ Q3 同类型承接情况：此SR节点是否已经在承接同类OR？
      例：SR-F-001 已承接 1.1(链路结构), 10.1(SysReq层级), 14.1(占位符), 15.1(版本管理), 16.1(质量标准)
      → 它已经在承接各类"准则标准"类OR，覆盖范围较广

   根据三个问题的答案做决策：
   ├─ 条件A — 直接复用：
   │  Q1命中 + Q2在边界内 + Q3同类
   │  → OR-XXX.X → SR-F-YYY（复用）
   │  例："定义需求规范化规则" → SR-F-006（已在定义范围内）
   │
   ├─ 条件B — 扩展后承接：
   │  Q1命中 + Q2略超边界 + Q3同类
   │  → 标记 SR-F-YYY（改进-扩展），记录扩展理由
   │  例："定义第1步全流程"略超SR-F-006的"规范化"范围
   │       → 扩展SR-F-006定义：从"规范化规则"扩展为"第1步操作规程"
   │
   ├─ 条件C — 新增：
   │  Q1无命中 + Q2不在任何边界内 + Q3无同类
   │  → 记录需在哪个父节点下新增，写明新增原因
   │
   └─ 条件D — ❓ 无法判断：
       Q1命中了多个方向 / Q2边界完全模糊 / Q3信息不足以对比
       → 标记❓，说明卡在哪个维度上，**不要强行推测**，留给人类

3. 【记录】同步记录本步决策：
├─ 承接的SR节点：[SR-F-YYY / 新增]
├─ 资产决策：[复用 / 改进-扩展 / 新增]
└─ 决策理由：引用以上三个判断维度的结果

⚠️ 关键判断原则：
- "职责方向一致"的含义：OR的核心意图与SR节点的定义动词匹配
  （如"定义...规则"→准则类节点、"提供...工具"→工具类节点）
- "超边界"的含义：OR具体内容超出了SR节点定义中列举的范围
  （如SR节点定义写"规范化"但OR描述"流程定义"）
- 不是"感觉上接近"，而是用三个维度可验证地判断
- 同一个SR节点可承接多条OR末级（N:1）
- ⚠️ **扩展和新增的强内聚性检查**：
  ├─ 扩展前问"扩展后这个节点还描述一个单一职责吗？"
  │  例：SR-F-006负责"规范化规则"，扩展为"第1步操作规程"
  │  → 操作规程 ≠ 规范化规则，扩展后会变"杂"，应新增节点而非扩展
  ├─ 新增前问"这个新节点和同级兄弟节点的颗粒度一致吗？"
  │  例：相邻节点都是"某个具体设计能力"的粒度，新节点不能是一个"大类"
  └─ 颗粒度标准：该节点应可自然分解为2-4条详细末级（t02可操作）

—— 输出 ——

OR→SR映射清单：
| OR末级 | 描述 | 承接的SR架构末级 | 资产决策 | 决策理由（Q1/Q2/Q3） |
|--------|------|-----------------|---------|--------------------|
| OR-XXX.X | [描述] | SR-F-YYY | 复用 ✅ | Q1命中[职责]，Q2在边界内，Q3已有同类 |
| OR-YYY.Y | [描述] | SR-F-ZZZ | 改进-扩展 ⚠️ | Q1命中，Q2略超[范围]，需扩展定义 |
| OR-ZZZ.Z | [描述] | [新增] | 新增 ⚠️ | Q1无命中，无SR节点职责方向匹配 |
| OR-WWW.W | [描述] | — | ❓ 无法判断 | Q1命中多个[SR-F-AAA][SR-F-BBB]，无法确定主方向 |

资产决策汇总：
├─ 复用节点 ✅：[N个] — SR-F-XXX, SR-F-YYY, ...
├─ 改进（扩展）节点 ⚠️：[N个] — SR-F-ZZZ（扩展[内容]）
├─ 新增节点 ⚠️：[N个] — 在[父节点]下新增，用于承接[OR清单]
├─ ❓ 待人类裁定：[N个] — [OR编号] + [卡在哪个维度]
└─ 未承接的OR末级：[清单]（理论上应为空）
```

### 3.3 SR 节点完善 + 闭环检查

> 本步产出 SR 架构定义子文档（02-*.md），遵循**详概分离**原则：每个架构末级节点只写四要素（定义/映射/符合性分析/设计指导），不写入详细分解内容（那是 t02 的工作）。

```
请在OR→SR映射结果基础上，完善所有SR节点的完整定义。

OR→SR映射结果：[粘贴3.2的输出（OR→SR映射清单）]

⚠️ 资产文件分区规则：
- v1 基线区（已冻结·只读）— **禁止修改**。复用节点直接引用 v1 定义
- v2 新增区（活跃·当前操作）— 扩展定义和新增节点写在此区
  ├─ 改进（扩展）节点 → v2 区补充扩展后的定义，标注"基于v1的SR-F-YYY扩展"
  ├─ 新增节点 → v2 区创建新节点条目
  └─ 复用节点 → 不写入新内容

—— 执行 ——

1. 完善所有SR架构末级节点四要素

   SR-F-XXX: [名称]（架构末级节点）
   ├─ 定义：[该节点的具体定义]
   ├─ 承接的OR末级：OR-XXX.X, OR-YYY.Y（N:1）
   ├─ 符合性分析：[如何定性和定量满足所承接的OR需求]
   ├─ 设计指导：[对t02业务架构设计的指导]
   ├─ 性能指标：[继承自OR]
   └─ 资产决策：[复用/改进-扩展/新增]

   SR-NFR-XXX: [名称]（架构末级节点）
   ├─ 定义：[非功能需求定义]
   ├─ 需求类型：[性能/安全/可用性/可维护性等]
   ├─ 承接的OR末级：OR-XXX.X
   ├─ 符合性分析：[如何满足]
   ├─ 量化指标：[数值+单位+条件]
   ├─ 测量方法：[如何测试验证]
   └─ 资产决策：[复用/改进-扩展/新增]

2. 分配三项自检
   - 全覆盖：所有OR末级均已分配到SR节点？[通过/未通过]
   - 1:1约束：每条OR末级分配到唯一SR节点？[通过/未通过]
   - N:1合理性：每个SR节点承接2-5条OR末级？[通过/未通过]
     - 1条→考虑合并；6+条→考虑拆分

3. 粒度评估与t02结对预留检查
   ⚠️ 文档结对检查：每个SR架构末级节点应满足以下条件：
   - 范围适度：可自然分解为2-4条详细定义末级（t02可操作）
   - 边界清晰：节点之间的职责不重叠
   - 指导明确：设计指导中需包含对t02详细分解的要求

4. 性能指标继承检查
   - 所有OR的性能指标是否已被SR节点承接？
   - 指标数值是否不降级？

—— 输出 ——

第一部分：架构设计结果（树形结构）
相关方需求（0级·根）
├─ 功能需求（1级·分类）
│  └─ [分类名称]（2级·分类）
│     └─ SR-F-XXX: [名称]（架构末级节点）
│          ├─ 定义：[需求的定义]
│          ├─ 承接的OR末级：OR-XXX.X, OR-YYY.Y
│          ├─ 符合性分析：[如何满足]
│          ├─ 设计指导：[对业务架构的指导]
│          ├─ 性能指标：[继承自原始需求]
│          └─ 资产决策：[复用/改进-扩展/新增]
└─ 非功能需求（1级·分类）
   └─ SR-NFR-XXX: [名称]（架构末级节点）
        ├─ 定义：[非功能需求的定义]
        ├─ 需求类型：[性能/安全/可用性/可维护性等]
        ├─ 需求类别：[质量特性/环境适应性/可实现性]
        ├─ 承接的OR末级：[OR-XXX]
        ├─ 符合性分析：[如何满足]
        ├─ 量化指标：[具体数值+单位+条件]
        ├─ 测量方法：[如何测试和验证]
        └─ 资产决策：[复用/改进-扩展/新增]

第二部分：需求映射视图（映射关系表）
| OR末级 | 承接的SR架构末级 | 资产决策 | 关联说明 | 超链接 |
|--------|-----------------|---------|---------|--------|
| OR-001.1 | SR-F-XXX | 复用 | [说明] | [→] |

粒度评估（供t02参考）：
| SR架构末级节点 | 预估详细末级数 | 分解方向建议 |
|---------------|--------------|-------------|
| SR-F-XXX | 2-3条 | [对t02的分解指引] |
```

## 四、决策记录模板

```
本步关键决策记录：
├─ 阶段状态：
│  ├─ OR 资产加载：[已完成] — [加载 OR 条目数]
│  └─ OR分解到末级：[已完成] — [OR条目数→末级条目数]
├─ 资产使用记录：
│  ├─ 资产来源：[02-pl4pleos-stakeholder-requirements-architecture.md（v1基线）]
│  ├─ 复用节点：[N个]
│  ├─ 改进（扩展）节点：[N个，记录每个的扩展内容]
│  └─ 新增节点：[N个，记录新增原因和父节点]
├─ SR节点承接数：[每个节点承接了几条OR末级，是否有合并/拆分]
├─ 编号体系：[OR-XXX ~ OR-YYY]，[SR-F-XXX ~ SR-F-YYY]，[SR-NFR-XXX ~ SR-NFR-YYY]
├─ ⚠️ 待确认项：[汇总需要人工确认的项]
├─ AI推断记录：[AI自行判断补齐的模糊术语/缺失内容]
├─ 待传递的约束：[后续步骤需要注意的决策/约束]
└─ 执行模式：[全自动/半自动]
```

---

## 五、审查修复流程

### 5.1 审查输入

AI 完成指定步骤后，输出产物供 rule-conformance-review 审查。审查报告包含：
- 通过/未通过的项目清单
- 问题定位（涉及的具体文件、章节、编号）
- 问题类型（结构性/内容性/格式性）

### 5.2 AI 修复流程

收到审查报告后，AI 按以下优先级处理：

1. **格式性问题**（编号错误、格式不规范）→ 直接修复，不需要人工确认
2. **内容缺失**（某节为空、映射不完整）→ 补充内容，标注修复说明
3. **结构性问题**（粒度不匹配、分类错误）→ 提出修复方案，标注 ⚠️ 待确认
4. **跨步骤一致性问题** → 参考AI决策框架的跨步骤影响范围表进行影响范围评估，列出所有受影响的下游节点

### 5.3 影响范围评估

| 问题定位 | 影响范围 | 修复策略 |
|---------|---------|---------|
| 某步的架构定义 | 本层 + 下游已建立映射的节点 | 修改后同步更新映射 |
| 详细定义 | 下层方案 | 修改后检查下层是否受影响 |
| 映射关系 | 所有关联节点 | 需重新运行追溯验证 |
| 编号变更 | 全链路 | 需逐层更新引用，重新验证 |

---

## 变更日志

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-05-28 | v1.0 | 首次构建 |
| 2026-05-28 | v1.1 | 重构为OR驱动流程（OR预处理→OR分解→OR→SR找承接→SR完善），取消"先建骨架再填充"，改为"OR末级逐条在现有SR资产中找宿主" |
| 2026-05-29 | v1.2 | §3.1 替换为加载 OR 资产；§二/§四 相应调整 |
| 2026-06-03 | v1.3 | 修正路径引用：90-eos-pipeline-system-product-data/ → 80-pl4pleos-opsdata/，文件名 eos-pipeline- → pl4pleos- |
