# 逆向工程流程第2步 AI 提示词
**Reverse Engineering Step 2: AI Prompt Templates — Product Architecture Definition**

**文档版本**：v1.0
**创建日期**：2026-05-28
**对应场景**：02-reverse-engineering · 第2步
**对应操作规程**：`task-definition.md`（同目录）

---

## 一、跨步骤上下文管理

### 1.1 上下文传递方式

8 步流程中，每一步的产出是下一步的核心输入。AI 在执行每一步前，应确保完整继承了上一步的关键决策：

| 继承内容 | 说明 | 保存位置 |
|---------|------|---------|
| 架构决策 | 合并/拆分、分类方式、层级结构 | 上一步的决策记录 |
| 编号体系 | 所有已分配的 ID 和编号规则 | 各产品数据文件 |
| 待确认项 | 上一步标记的 ⚠️ 项 | 汇总清单 |
| AI推断记录 | AI 自行判断补齐的内容 | 决策记录文件的 AI推断项 |

### 1.2 决策记录机制

每步完成后，AI 将本步的关键决策记录在**本步决策记录模板**（见本章末尾）中，供后续步骤引用。

### 1.3 回溯修正

如果在第 N 步发现需要修改第 M 步（M < N）的产出：
1. 评估影响范围（见 AI决策框架 §7.2 跨步骤影响范围表）
2. 定位到受影响的具体步骤和节点
3. 修改后检查下游映射是否需要同步调整
4. 标注变更记录

---

## 二、AI 经济原则

本步操作以下列顺序执行，AI 需注意：

1. **执行顺序不可逆**：识别产品类型 → 产物分组定组件 → 建树形结构 → 组件定义 → 分析活动 → 结果审核
   核心驱动力：从产物分组出发，按分类体系组织 PA 树形结构。PA 组件由产物的实际组织方式决定
2. **基于资产优先**：产物分组方式本身就是资产分析的结果。三因素判断（职责方向/边界偏差/同类承接）优先考虑已有分组是否可作为独立 PA 组件
3. **逆向推导特点**：不同于瀑布式从需求推导 PA，逆向工程从产物目录结构"归纳"PA。关注产物之间的自然组织方式，而非预设的理想结构

## 三、本步提示词模板

### 3.1 本步提示词

```
请在开始前确认上下文状态：
1. 是否已读取 task-definition.md 了解本步操作流程？
2. 是否已获取 t01 产出的产物总览清单和分类结果？
3. 是否已理解产品架构分解原则（三因素）？
4. 是否准备按阶段一→阶段六的顺序执行？

具体操作按 task-definition.md 六个阶段执行。
```

## 四、决策记录模板

```
本步关键决策记录：
├─ 阶段状态：
│  ├─ 产品类型识别：[已完成] — [分类体系]
│  ├─ 产物分组定组件：[已完成] — [N个PA组件]
│  ├─ 树形结构：[已完成] — [N层级]
│  ├─ 组件定义：[已完成] — [所有组件定义完成]
│  └─ 分析活动：[已完成] — [覆盖度，粒度评估]
├─ 资产使用记录：
│  ├─ 资产来源：[t01产物总览清单]
│  └─ 组件划分依据：[三因素判断结果摘要]
├─ PA 组件清单：[组件名称，所属分类]
├─ ⚠️ 待确认项：[需要人工确认的组件划分争议]
├─ AI推断记录：[AI 自行判断的组件分类/定义]
├─ 待传递的约束：[后续步骤需要注意的 PA 结构特征]
└─ 执行模式：[全自动/半自动]
```

---

## 五、审查修复流程

### 5.1 审查输入

AI 完成指定步骤后，输出产物供 rule-conformance-review 审查。审查报告包含：
- 通过/未通过的项目清单
- 问题定位（涉及的具体文件、章节、编号）
- 问题类型（结构性/内容性/格式性）

### 5.2 AI 修复流程

收到审查报告后，AI 按以下优先级处理：

1. **格式性问题**（编号错误、格式不规范）→ 直接修复，不需要人工确认
2. **内容缺失**（某节为空、映射不完整）→ 补充内容，标注修复说明
3. **结构性问题**（粒度不匹配、分类错误）→ 提出修复方案，标注 ⚠️ 待确认
4. **跨步骤一致性问题** → 参考AI决策框架的跨步骤影响范围表进行影响范围评估，列出所有受影响的下游节点

### 5.3 影响范围评估

| 问题定位 | 影响范围 | 修复策略 |
|---------|---------|---------|
| 某步的架构定义 | 本层 + 下游已建立映射的节点 | 修改后同步更新映射 |
| 详细定义 | 下层方案 | 修改后检查下层是否受影响 |
| 映射关系 | 所有关联节点 | 需重新运行追溯验证 |
| 编号变更 | 全链路 | 需逐层更新引用，重新验证 |

---

## 变更日志

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-05-28 | v1.0 | 首次构建 |
