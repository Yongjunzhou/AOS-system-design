# 敏捷式流程第6步 AI 提示词
**Agile Step 6: AI Prompt Templates — Product Architecture Impact Analysis**

**文档版本**：v1.0
**创建日期**：2026-05-28
**对应场景**：03-agile · 第6步
**对应操作规程**：`task-definition.md`（同目录）

---

## 一、跨步骤上下文管理

### 1.1 上下文传递方式

8 步流程中，每一步的产出是下一步的核心输入。AI 在执行每一步前，应确保完整继承了上一步的关键决策：

| 继承内容 | 说明 | 保存位置 |
|---------|------|---------|
| 架构决策 | 合并/拆分、分类方式、层级结构 | 上一步的决策记录 |
| 编号体系 | 所有已分配的 ID 和编号规则 | 各产品数据文件 |
| 待确认项 | 上一步标记的 ⚠️ 项 | 汇总清单 |
| AI推断记录 | AI 自行判断补齐的内容 | 决策记录文件的 AI推断项 |

### 1.2 决策记录机制

每步完成后，AI 将本步的关键决策记录在**本步决策记录模板**（见本章末尾）中，供后续步骤引用。

### 1.3 回溯修正

如果在第 N 步发现需要修改第 M 步（M < N）的产出：
1. 评估影响范围（见 AI决策框架 §7.2 跨步骤影响范围表）
2. 定位到受影响的具体步骤和节点
3. 修改后检查下游映射是否需要同步调整
4. 标注变更记录

---

## 二、AI 经济原则

本步操作以下列顺序执行，AI 需注意：

1. **执行顺序不可逆**：PA 影响分析 → PA 增量条目定义 → 补充组件详细定义 → 更新映射关系表
   核心驱动力：从 SysReq 增量 + 骨架映射出发，按 §2.4 三因素判断构件合并/分离，使用增量标记更新 PA 文档
2. **基于资产优先**：基于现有 PA 文档（`07-*.md` 架构定义 + 详细定义）和通用设计准则 §2.4 产品架构分解原则判断承接方式
3. **增量设计特点**：本步是设计链增量更新的最后一层（t01→t06 完成 OR→SR→BA→SysReq→PA 的全链路增量更新）。PA 组件增量定义需根据产品类型（软件/方法论）有所不同。组件详细定义的补充因产品类型而异

---

## 三、本步提示词模板

### 3.1 本步提示词

详细 AI 提示词模板见：
`30-eos-pipeline-system-design-ai-support/03-agile-eos-pipeline-system-design/workflow-prompts.md` 第6步

请在开始前确认上下文状态：

- 是否已加载 t05 产出的 SysReq 增量定义 + SysReq→PA 骨架映射？
- 是否已加载现有 PA 文档基线（`07-*.md` 架构定义 + 详细定义）？
- 是否已参考通用设计准则 §2.4 产品架构分解原则（三因素）？
- 目标产品类型是软件产品还是方法论产品？（影响组件详细定义的补充方式）
- §2.4 三因素（职责内聚性/变更隔离性/部署独立性）的判断标准是否明确？

具体操作按 task-definition.md 五个阶段执行：PA 影响分析 → 增量定义 → 补充详细定义 → 更新映射 → 审查。

---

## 四、决策记录模板

```
本步关键决策记录：
├─ 阶段状态：
│  ├─ PA 影响分析：[已完成] — [通过/未通过项]
│  ├─ PA 增量条目：[已完成] — [+ 新增 N 条 / ~ 修改 N 条]
│  ├─ 组件详细定义：[已完成] — [软件/方法论类型，详细定义补充 N 条]
│  └─ 映射关系表：[已完成] — [N 条 SysReq→PA 映射]
├─ 资产使用记录：
│  ├─ 资产来源：[07-*.md（现有 PA 基线）+ §2.4 分解原则]
│  ├─ 复用节点：[N 个]
│  ├─ 改进（扩展）节点：[N 个，记录每个的扩展内容]
│  └─ 新增节点：[N 个，记录新增原因]
├─ §2.4 三因素决策记录：
│  ├─ 职责内聚性：[判断依据]
│  ├─ 变更隔离性：[判断依据]
│  └─ 部署独立性：[判断依据]
├─ 编号体系：[新 PA 节点编号范围]
├─ ⚠️ 待确认项：[汇总需要人工确认的项]
├─ AI推断记录：[AI自行判断补齐的模糊术语/缺失内容]
├─ 待传递的约束：[后续步骤需要注意的决策/约束]
└─ 执行模式：[全自动/半自动]
```

---

## 五、审查修复流程

### 5.1 审查输入

AI 完成指定步骤后，输出产物供 rule-conformance-review 审查。审查报告包含：
- 通过/未通过的项目清单
- 问题定位（涉及的具体文件、章节、编号）
- 问题类型（结构性/内容性/格式性）

### 5.2 AI 修复流程

收到审查报告后，AI 按以下优先级处理：

1. **格式性问题**（编号错误、格式不规范）→ 直接修复，不需要人工确认
2. **内容缺失**（某节为空、映射不完整）→ 补充内容，标注修复说明
3. **结构性问题**（粒度不匹配、分类错误）→ 提出修复方案，标注 ⚠️ 待确认
4. **跨步骤一致性问题** → 参考AI决策框架的跨步骤影响范围表进行影响范围评估，列出所有受影响的下游节点

### 5.3 影响范围评估

| 问题定位 | 影响范围 | 修复策略 |
|---------|---------|---------|
| 某步的架构定义 | 本层 + 下游已建立映射的节点 | 修改后同步更新映射 |
| 详细定义 | 下层方案 | 修改后检查下层是否受影响 |
| 映射关系 | 所有关联节点 | 需重新运行追溯验证 |
| 编号变更 | 全链路 | 需逐层更新引用，重新验证 |

---

## 变更日志

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-05-28 | v1.0 | 首次构建 |
