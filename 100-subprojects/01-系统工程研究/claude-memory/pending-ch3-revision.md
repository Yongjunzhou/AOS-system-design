---
name: pending-ch3-revision
description: 第3章《三基座：需求、设计与质量》修订 · 会话入口——底本状态、已勘明的活、开工前待裁事项；照《逐章修订手册》执行
metadata:
  type: pending
---

# 第3章《三基座：需求、设计与质量》修订 · 会话入口

> **状态**：**待开工**（2026-09-26 建，随第2章收工一并备好）。用户定：**每次对话只修订一章**。
> **开工话术**：「读 `claude-memory/MEMORY.md` 与 `claude-memory/pending-ch3-revision.md`，修订第3章。」
> **执行依据**：[`03-架构师是个怎样的物种/00-逐章修订手册.md`](../03-架构师是个怎样的物种/00-逐章修订手册.md)——**十一步全表在 §七**（第一步＝基于新版报告与标准重新编制内容 … **第七步＝把案例对话提升到专业水平** … **第八步＝处置比喻** … **第十一步＝整理本章各节标题，使其更优雅、一致性更好、对人类更友好**）；顺序纪律一句话：**内容先对齐 → 结构照第一章 → 逐项裁定 → 减重 → 对话专业化 → 比喻处置 → 一致性 → 收口 → 标题最后整理**。本文件 §二 列的九件活，就是这十一步在第3章的具体化。**第3章另有两笔已量出的欠账**：**对话**（空句 59.1%、术语 36.0/千字、出处 1.46/千字——四判据里三条不达标）与**比喻**（命中 164 次／**8.33 次每千字**，正好压在"触发逐条复审"的 8 线上；在用 9 套，超"≤4 套"探测线）。
> **★ 第2章那一轮是最新的样例**——它的六批（零风险件／判据去／反转归节／加粗瘦身／章前件删／改名）就在 [`pending-ch2-revision.md`](pending-ch2-revision.md)，含**四处脚本静默失效**与**"去标记留空格"**两个教训，开工前先扫一眼。
> **收工三件**：①复算（§四 脚本）②记忆写回 ③提交＋推送（**只含本目录**）。

---

## 一、底本（§〇 第〇步）

| 项 | 值 |
|---|---|
| 本章对应报告 | 框架 §三 第3章"来源"列＝**`01`**（01-设计开发概念研究报告-从需求到质量基座） |
| 要核的标准正本 | 库内 `00-系统工程相关研究报告/`：**ISO 9000:2026**（中英文对照版在库）、**ISO 9001**（**库内无正本**，属库外来源·不可核）；按章节实际引用取 |
| **是否已做过核对** | **点状核对已做（批次 4，2026-09-25）**——`03` 与 `04`／`06`／`07`／`08`／`18` 同批。**★ 但按 2026-09-26 新口径，第一步仍是"重写"、不是"复核"**（用户令：「第一步不要说修订，建议用重写这个字眼」）：**拿 01 号 v1.8 与库内 ISO 9000:2026 正本，把本章内容从头重做一遍**——批次 4 那点状核对只算已备好的底账，不能当第一步已做完。当时动作：**ISO 9001 正文裸写补版本 4 处**（一律 `:2015`）＋**`相关方`→利益相关方 13 处**＋**`生命周期`→生存周期 3 处**；01 号 v1.8 的三项（artefact／work product 纪律、15288 条款号结清 §3.47、错串 `sufficient to enable`）**本章均无落点**，按先例只记不动 |
| 复核要看什么 | ① 章末参考文献的 `R-02 ISO 9000:2015` 与附录 D **R-103 `ISO 9000:2026`** 是否双轨并存、口径是否要统一（ch7 已登记"标准版本双轨待裁"）；② §3.2.x 引 ISO 9000 条款号是否与 R-02 列的一致；③ 反向检索 `全生命周期`／`全生存周期` 之类批量替换的错产物 |

## 二、本章已勘明的活（照第2章那一轮）

现结构（`##` 级）：`3.1 误解现场` → `这一章要回答什么` → `这一章的骨架` → `3.2 第一幕 三兄弟是什么（定界）`（3.2.1~3.2.5）→ `案例进展①` → `3.3 第二幕 三条边：兄弟之间磨边界（磨边界弧）`（3.3.1~3.3.3）→ `案例进展②` → `3.4 第三幕 读标尺 → 冲突 → 咬合（运用）`（3.4.1~3.4.4，中间夹 `案例进展③`／`案例进展④`）→ `认知反转` → `小结` → `本章概念总图` → `本章判据` → `自查` → `练习` → `参考文献`

| # | 活 | 说明 |
|---|---|---|
| 1 | **章首引言化** | `## 3.1 误解现场` → `## 3.1 引言` ＋ 引言段 ＋ `### 3.1.1 误解现场`／`### 3.1.2 这一章要回答什么`／`### 3.1.3 这一章的骨架`（与第1、2章同形） |
| 2 | **删「第N幕」** | ★ **本章是全书幕指称最多的一章：正文共 21 处**（"第一幕／第二幕／第三幕／这一幕"——ch2 只有 3 处）。三个幕标题（`3.2 第一幕 …`／`3.3 第二幕 …`／`3.4 第三幕 …`）去标签，**21 处指称逐条改 §号**（骨架段、幕首导语、正文回指都要扫）；这 21 处正是框架 §九 记的"18 章正文共 102 处幕指称"里 ch3 那一份 |
| 3 | **反转归末节** | `## 认知反转` → **`### 3.4.5 认知反转`**（§3.4 现为 3.4.1~3.4.4，排在其后；其前的 `---` 去掉）；**反转 830 汉字——已超提示线 800、未超硬线 1000**，归节时顺带看要不要压到线内 |
| 4 | **章末「附录」组** | `## 附录` 落在小结之后＋组内各节降 `###` 给连续字母（**字母排法取决于 §三 第 1 条裁定**） |
| 5 | **清点块新写** | `### B 本章的能力级`——四行点名（表征／图式／心智模型／解释框架，缺级如实留白）＋`>` 收束句；**要按本章内容写**（本章主词是需求／设计开发／质量三张概念卡，"定标尺→造标尺→读标尺"是它的图式） |
| 6 | **刻度行量词「层」→「级」** | 1 行 3 处：`第 ① 层（表征）`→`第 ① 级（表征）`、`第 ② 层`→`第 ② 级`（§1.3.5 那个"§1.3.5"引用本身不动） |
| 7 | **加粗瘦身** | 现 **288 块／占正文 13.9%／每千字 14.6**（红线：≤6 块每千字、占比 ≤7%、散文中整句加粗 0 处）。手法照 `00-风格参考.md` §6.5 施工记（白名单＋去标记，脚本 `.work-style/slim_bold_ch2.py` 可复制改章号）；**★ 做完必跑"多余空格"一查**（`(?<=[CJK][。；：，、）】》]) (?=[CJK])`），那是"只删标记"唯一会留痕的地方 |
| 8 | **白话清扫** | 命中 **13 处**（提示线 10）：`口径` 5／`落点` 4／`抓手` 2／`收束` 1／`触发` 1——逐条换日常说法（判据见 `00-风格参考.md` §6.6） |
| 9 | **文件名** | `03-第3章-三基座-需求设计与质量.md` **已合约定**（＝章题 `三基座：需求、设计与质量` 的 `：`→`-`），**无需改** |

> **本章不在本轮范围内的两项**：比喻（本章 164 命中／8.33 每千字／9 套，密度偏高——属全书"比喻处置"批次）；ISO 9001 版本双轨（属标准版本裁定批次）。

## 三、开工前需用户裁定

1. **「本章判据」块去留**（**7 条**；**决定附录组字母排法**）——第1章（11 条）与第2章（5 条）**均已去掉**，理由都是"与正文逐条重复＋§指针 0 个＋与自查逐条对位"；框架 §五 第 5 条载"**其余 16 章待裁**"。**连带训侧**：`-从教材到问题分析` 按"判据 1~N"映射题目，去掉会失锚（ch1／ch2 已按"只落书侧、训侧登记挂账"办理）。
2. **章前件三行去留**（本章现有：本章概念 3 词／英文来源／主案例·镜照）——第1、2章**已删**，其余 16 章暂留（框架 §五 第 1 条）。
3. **★ 跨章挂账（用户尚未点头）**：`## 小结` 与 `## 附录` **要不要也编号**——用户曾令「**确保除了小节之外各标题都有编号**」；我查后（第1、2章同形）报：真正无号的只有这两处，另 3 个 `## 案例进展①②③` 用圈码。**我的建议＝只把小结编为 `## N.5 小结`，附录作为分组标题不编号、案例进展保留圈码**。此裁是**全书性**的（18 章 × 2 处＋目录＋框架 §九 无编号对象清单＋3 个按块名认块的脚本），定了就一次改齐。

## 四、复算（收工前）

```
python .work-style/ch2_layout.py                 # 逐块字数（复制改章号为 ch3_layout.py）
python "03-架构师是个怎样的物种/00-风格参考-量尺.py"   # 配比红线（单一量尺）
python .work-style/check_tail_order.py           # 章末件顺序＋「附录」定位
python .work-style/check_glued_paragraphs.py     # 粘段
python .work-style/audit_plainwords.py           # 白话体检（每章提示线 ≤10）
python .work-style/audit_chapter_openings.py     # 章首块名与旧叫法
python .work-style/audit_verbosity.py            # 加粗密度（瘦身前后各跑一次）
python .work-style/scan_block_matchers.py        # 扫按块名认块的脚本（防静默失效）
python .work-style/slim_bold_ch3.py              # 加粗瘦身（复制 ch2 版改章号；预览，加 --write 落盘）
python "03-架构师是个怎样的物种/00-三条线索-核对.py"   # 收束句 18/18＋刻度行 17/17
```

**目录↔正文对账**：复制 `.work-style/_check_ch2_toc.py` 改章号（第2章当场是 27 对 27、0 差异）。

**目录外未触碰。**

---

## 附：第2章的样例入口

第2章那一轮（已完成，六批）的完整记录在 [`pending-ch2-revision.md`](pending-ch2-revision.md)——含底本核对结论、五批落地的逐条清单（判据去／反转归节／加粗瘦身／章前件删／改名）、**四处脚本静默失效**（`audit_chapter_openings` 节号写死／`audit_plainwords` 参考文献锚点／`check_tail_order` 的 ORDER 少「本章的能力级」／`00-三条线索-核对.py` 收束句只查反转块）与**"去标记留空格"**、**`chN_layout.py` 把章末件算进末块**两个量尺教训。本章开工若遇同类问题，回那份查。
