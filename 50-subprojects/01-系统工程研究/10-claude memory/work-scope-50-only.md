---
name: work-scope-50-only
description: 工作范围限制——只处理 50-subprojects/01-系统工程研究 目录内容，目录外任何文件不调查不提交不触碰，除非用户明确指示
metadata:
  type: feedback
---

用户指令（2026-08-22）：**只处理 `50-subprojects/01-系统工程研究/`（教材子项目）目录中的内容，工作范围限制在该目录内；除非用户给出明确指示，否则不触碰目录外的任何文件。**

**Why:** 仓库是多项目单仓（AOS 主线 + 教材子项目等）。此前会话中，目录外文件（如 `20-pl4eos/` 的 EOS 文件）的改动会被误认为与本工作相关，甚至带来混提风险。用户要求严格按目录隔离——本会话只负责 50-subprojects/01-系统工程研究。

**How to apply:**
- 处理教材/研习笔记时，工作目录 = `50-subprojects/01-系统工程研究/`，只读写该目录内文件。
- 会话中若 `git status` 出现目录外改动（如 `20-pl4eos/`）：**不调查、不提交、不触碰**——那不在本工作范围，留给用户或其他会话处理。
- 提交时只纳入 `50-subprojects/01-系统工程研究/` 内的改动；目录外改动绝不混入教材提交。
- 除非用户明确点名要处理某目录外文件，一律守此边界。
- **例外（2026-09-13，用户授权）**：**共用工具 `tools/md2pdf/md2pdf.js` 的改动，经用户明确授权后可以提交**——当时为让 ISO 9000:2026 中英对照版 PDF 的内部锚点（术语交叉引用）可跳转，为该工具加了"标题补 id"后处理。**程序要求**：此类目录外改动**须先向用户说明"这会越界"并取得授权**，再提交；不得默认为工作范围，也不得先改后报。
