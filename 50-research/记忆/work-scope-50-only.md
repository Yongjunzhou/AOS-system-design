---
name: work-scope-50-only
description: 工作范围限制——只处理 50-research 目录内容，目录外任何文件不调查不提交不触碰，除非用户明确指示
metadata:
  type: feedback
---

用户指令（2026-08-22）：**只处理 `50-research/`（教材子项目）目录中的内容，工作范围限制在该目录内；除非用户给出明确指示，否则不触碰目录外的任何文件。**

**Why:** 仓库是多项目单仓（AOS 主线 + 教材子项目等）。此前会话中，目录外文件（如 `20-pl4eos/` 的 EOS 文件）的改动会被误认为与本工作相关，甚至带来混提风险。用户要求严格按目录隔离——本会话只负责 50-research。

**How to apply:**
- 处理教材/研习笔记时，工作目录 = `50-research/`，只读写该目录内文件。
- 会话中若 `git status` 出现目录外改动（如 `20-pl4eos/`）：**不调查、不提交、不触碰**——那不在本工作范围，留给用户或其他会话处理。
- 提交时只纳入 `50-research/` 内的改动；目录外改动绝不混入教材提交。
- 除非用户明确点名要处理某目录外文件，一律守此边界。
