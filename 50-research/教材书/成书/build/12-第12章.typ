= 第12章 架构决策：对不可逆的提前投资
#quote(block: true)[
本章概念：架构决策 / 不可逆性 英文来源：architecture decision /
irreversibility 主案例：恒信集团合同审批系统（IT侧）｜
镜照：冰链科技冷链温控箱（产品侧）
]

#divider()

#heading(level: 2, outlined: false)[12.1 章首 · 误解现场]
恒信合同审批系统，黄程提了一个”小需求”。

黄程推开万辉工位边的转椅，把手机屏幕转过来："万辉，合同编号得改。电子签章平台那边的规则是'合同类型-年份-流水号'，跟咱们库里对不上。帮我们把编号规则改了？"

万辉瞄了一眼："改个编号格式的事，小事。今天就能上。"

路过的产品开发经理满海听了半句，也没停下脚步："改个字段而已，测试跑一遍就上，别拖了。"

当天下午，万辉改了合同主表的编号字段，顺手把合同状态枚举加了两项------"作废""草稿"------方便法务标状态。测试环境跑了一遍，正常。上线。

三个月后，连环三爆。

#strong[第一爆，财务对账。]
合同编号按新规则重排后，与财务已归档的纸质合同对不上。审计抽查时，黄程查了三天，查不出”去年
12 月那批 500 万合同”对应的电子记录。

#strong[第二爆，台账。]
下游台账系统按旧编号规则解析，全部乱掉------同一份合同在台账里识别成了三条。

#strong[第三爆，追溯。]
法务要查”半年前那份作废合同当初是谁批的”，历史审批记录的关联键断了，审批链断在中间。

会议室里，万辉还觉得冤："我就是改了个字段。字段嘛，不是想改就改？"

徐漾看着他，说了一句让整间会议室安静下来的话：

"万辉，你改的不是一个字段。你改的是这套系统的#strong[数据模型]------它是这套系统里最不可逆的决策之一。你却把它当成了'改个变量名'。"

如果你坐在这间会议室里，你大概也会站在万辉那边。改个编号规则、加个状态枚举------听起来就是”改个字段”。大多数人对”架构决策”的直觉正是如此：架构是画图的事，改字段是码农的事，两码事。

这一章要推翻的，正是这个直觉。#strong[架构决策不是某次评审会上”画架构”才发生的；它藏在每一个”改个字段”里。]

合同的数据模型怎么组织、编号规则定成什么样、状态枚举分几类------这些选择，选错了要花大代价才能改对。而”想改就改”，恰恰是最危险的一种对待方式------#strong[把高不可逆的决策，当成低可逆的决策来处理。]

这一章我们做三件事------识别它、磨掉三种错觉、把尺子用起来；三幕的具体骨架，见下面「开篇
· 本章枢纽」。

你会发现两件反直觉的事：#strong[架构决策不可避免------不是做不做，是早晚；而不可逆的决策，必须在成本低的时候提前做。]

#divider()

#heading(level: 2, outlined: false)[本章问题]
读完本章，你应该能回答：

+ 什么算”架构决策”？为什么”加一个字段”也可能是架构决策？
+ 为什么说架构决策不可避免？"我们没做架构决策"到底是什么意思？
+ 怎么判断一个决策的不可逆程度？"不可逆"是技术属性还是经济属性？三层判断框架是什么？代价判据和目的判据是什么关系？
+ 数据所有权/数据模型为什么几乎最不可逆？改它要按什么待遇对待？
+ 高不可逆的决策应该什么时候做？"推迟决策"怎么反而是一种架构操作？
+ 一个决策怎么被记下来？决策记录六问是什么？"不可逆性标注"标在哪？
+ 审查深度该按什么分级？给低不可逆的决策开三场评审会值得吗？
+ 从需求到实现，每一层的架构决策为什么不同？需求有架构吗？

#divider()

#heading(level: 2, outlined: false)[开篇 · 本章枢纽（骨架先行）]
先把本章要钉的两个词、和它们牵出的一条判断链立起来------后面三幕，都是给这层骨架添血肉。

#quote(block: true)[
#strong[架构决策 = 选错了要花大代价才能改对的选择。]
它藏在每一个”改个字段”里；而判断它值不值得慎重，靠的不是”这是不是技术难题”，而是一把经济学的尺子------#strong[不可逆性]。
]

这一章的骨架是#strong[一个词 + 一把尺子 +
一条判断链]：一个词（架构决策------用三标志辨认：影响半径大 / 持续时间长
\/ 修改成本高）；一把尺子（不可逆性------三层判断框架：影响半径 /
信息成本 / 修改风险）；一条判断链（#strong[识别 → 判断 → 抉择 → 记录 →
分级 → 定位]，识别属第一幕、分级并入第三幕）。

第一幕把”架构决策”这个词钉死------识别它，别让它在”改个字段”里蒙混过关；第二幕把三种错觉磨掉------它是”不是什么”的边界；第三幕把尺子用起来------判断、抉择、记录、分级、定位，一条链走到底。

#divider()

== 12.2 第一幕 架构决策是什么（识别）
#quote(block: true)[
这一幕把”架构决策”看清：先看病（"改个字段"为什么炸了三个月），再钉定义（选错了要花大代价才能改对）+
三标志判据，然后用三个思想实验让三标志长进直觉里------最后落在识别它的第一道动作：改字段前三问。
]

=== 12.2.1 先看病："改个字段"为什么炸了三个月
万辉的病，不是”改错了字段”，而是把”数据模型”当成”字段”------把一件架构决策，当成一件普通实现。两个东西长得像（都要改代码、都能改）、其实不同层（一个是架构决策、一个是实现决策），糊在一起用，就出事了。

先看万辉到底改了什么。合同审批系统的数据模型里，"一份合同"是三个实体：合同主表（编号、类型、状态、金额、签署方）、审批记录（审批链、各节点意见）、附件（合同文件）。这三个实体之间的关联、编号怎么编码、状态枚举怎么划分------这套数据模型，决定了：

- #strong[财务怎么对账]：台账按合同编号解析，编号规则变了，解析全乱；
- #strong[法务怎么追溯]：审批记录挂在合同主表上，关联键变了，历史审批链断掉；
- #strong[下游怎么对接]：台账、报表、审计，全都依赖这套编号和状态口径。

换句话说，万辉改的”一个字段”，牵一发动全身。这正是架构决策的第一特征------#strong[影响半径大]。它不像”改一个函数内部的排序算法”，改了只影响一个点；它像地基里的一根桩，动了它，整栋楼的承重都要重新算。

但这里有个更隐蔽的陷阱，值得单独挑出来------#strong[万辉不是”改错了”，他甚至没意识到自己在做一个决策。]
他以为自己在”执行”一个字段修改，实际上他在”决定”数据模型的结构。

这个”没意识到”，才是最危险的：#strong[没有被识别出来的架构决策，不会得到它应有的审查，却依然承担架构决策的全部代价。]
认不出它是架构决策，就不会有人认真论证、认真评审、认真记录------但它改错的后果，一样都不少。

=== 12.2.2 权威定义：选错了，要花大代价才能改对
"架构决策"这个词，在架构文献里有一个非常直白的定义，它不做任何修饰：

#quote(block: true)[
#strong[架构决策 =
你在设计系统时做出的某个选择，这个选择选错了的话，后来要花很大代价才能改对。]
]

反过来就是它的判据：如果你做了一个选择，后来发现错了，改一下只要几分钟------这不叫架构决策，这就是普通的实现选择。

"花很大代价"这个说法，听起来有点虚。用什么度量？架构领域给出了三个标志，也就是三把尺子：

#figure(
  align(center)[#table(
    columns: (33.33%, 33.33%, 33.33%),
    align: (auto,auto,auto,),
    table.header([标志], [含义], [反面（非架构决策）],),
    table.hline(),
    [#strong[影响半径大]], [改了它，不止改一处，要联动改很多东西], [改它只影响一个文件、一个函数、一个模块内部],
    [#strong[持续时间长]], [一旦选定，后续无数个其他决策都建立在这个决策之上], [今天做了，下周就可以换掉，不影响其他东西],
    [#strong[修改成本高]], [选错了要改对，代价大到你会选择”算了，就忍着吧”], [选错了，改对只需要一次提交],
  )]
  , kind: table
  )

#quote(block: true)[
与第10章对账：§10.2.3
的”不可还原”三判据（去掉/缺少/换掉）识别的是系统最核心的架构------去掉就不是它的那层；这里的三标志识别的是”够格的架构决策”------凡改起来贵的选择都算，范围更大。两把尺子不冲突：后者包含前者。
]

用三把尺子量量万辉改的”编号规则”：

- #strong[影响半径]：编号规则一变，财务、台账、审计、电子签章全部要跟着改------#strong[大]；
- #strong[持续时间]：编号一旦定下来，三年后归档的合同还按它编码------#strong[长]；
- #strong[修改成本]：改了之后才发现要重排历史数据、重对审计账------#strong[高到想回到改之前]。

三把尺子全中。所以尽管万辉只敲了几行代码，他做的仍然是一个架构决策。

这里已经能看到第1章示范的判据主义------不给没有判据的定义。三标志，就是”架构决策”的判据------有了它，"这算不算架构决策"就不再是感觉，而是可以当场对表的三问。

这个定义不是凭空冒出来的。"架构决策"这个词，近年才成为工程界的高频词，它的兴起和两件事有关。

一是 #strong[ISO/IEC/IEEE 42010]（2011
年的架构描述标准）把”架构决策”列为架构的核心概念之一------架构不只是元素和关系，它还是”由一系列决策所塑造的”。

二是#strong[架构决策记录]（Architecture Decision
Record，ADR）这种做法的流行------2011 年，软件架构师 Michael Nygard
提议用”一页卡、回答几个固定问题”的方式记录架构决策，后来被大量团队采纳。

从那时起，"架构决策"从一种模糊的说法，变成了一个#strong[可记录、可管理、可审查的工程对象]。这一章讲的，就是这个对象：怎么识别它、怎么判断它、怎么做出它、怎么记下它。

=== 12.2.3 三个让人”哦”的例子：书架、厨房、订单服务
"影响半径大、持续时间长、修改成本高"------这三个词还是抽象。架构文献里流传着三个经典例子，每一个都是三标志的完美演示，也每一个都能让你拿身边的事套一遍。

#strong[例子一：书架怎么分类。]
你有一千本书，按颜色、按作者首字母、还是按主题？选了按颜色------以后每次找”Python
相关的书”，都要把所有颜色层翻一遍；选了按主题------"计算机"那一层秒找到。选完再想从按颜色改成按主题，五百本书全部重新摆------这就是修改成本。"书架按什么规则分类"，就是一个架构决策。

#strong[例子二：商用厨房的布局。]
灶台和冰箱之间的距离。灶台紧挨冰箱：效率高，但散热互相影响；灶台和冰箱在对角线：各自独立，但每次拿食材走八步。不管选哪个，一旦灶台接好燃气、冰箱接好水电，再想挪位置，就得拆墙、重铺管线、停工几天------这就是不可逆。厨房布局，也是一个架构决策。

#strong[例子三：软件里订单服务的边界。]
"库存扣减"放在订单服务里，还是库存服务里？放订单服务：前期开发快，但三年后采购入库、盘点调整都要改订单服务，订单服务开始管库存的事，职责膨胀；放库存服务：多一次远程调用，但库存的所有操作（扣减、入库、盘点、调拨）都在一个地方，职责干净。"库存扣减放在哪个服务里"，还是一个架构决策。

注意这三个例子的共同点：#strong[它们都不是”技术难题”，而是”选择难题”。]
书架分类不需要任何高深知识，厨房布局也不是物理难题，订单服务边界更是没有标准答案。

架构决策的难，不在”怎么做出来”，而在”选哪个、为什么选、选了之后要承受什么”。这也是为什么它常常被当成”反正怎么选都行，那就随手选一个”------随手选，就是不做决策地做决策，代价迟早会来收账。

把三标志套一下日常常见决策，你会看到一条光谱------不是所有决策都同样”架构”：

#figure(
  align(center)[#table(
    columns: (20%, 20%, 20%, 20%, 20%),
    align: (auto,auto,auto,auto,auto,),
    table.header([决策], [影响半径], [持续时间], [修改成本], [是不是架构决策？],),
    table.hline(),
    [系统拆几个服务，每个服务管什么], [大（所有后续开发都在这个框里做）], [系统整个生命周期], [极高（数据迁移+团队重组+接口重定）], [✅
    是],
    [订单服务的数据库选 MySQL 还是
    PostgreSQL], [中（影响一个服务的表结构设计+运维）], [系统生命周期（迁移成本高）], [中-高], [✅
    是],
    [订单服务内部用哪个 ORM
    框架], [小（只影响内部实现）], [中（换框架要重写数据访问层但接口不变）], [中（机械劳动，风险可控）], [⚠️
    边界],
    [订单列表排序用冒泡还是快排], [极小（只影响一个查询的性能）], [短（随时可以优化）], [极低（换算法一次提交）], [❌
    不是],
    [变量名叫 orderList 还是
    orders], [极小], [中（命名规范一旦确立会持续使用）], [低（全局替换即可）], [❌
    不是],
  )]
  , kind: table
  )

光谱的意义在于：#strong["架构决策"和”实现决策”之间没有一条自动分界线，分界线靠三标志划。]
划错了------把架构决策当实现决策------就是万辉的翻车现场。

#quote(block: true)[
#strong[概念卡片 · 架构决策]（四问为纲，九格为目；格号=九格尺子）

#figure(
  align(center)[#table(
    columns: (33.33%, 33.33%, 33.33%),
    align: (auto,auto,auto,),
    table.header([四问], [格], [内容],),
    table.hline(),
    [#strong[它是什么]], [①], [你在设计系统时做出的某个选择，这个选择选错了的话，后来要花很大代价才能改对------用三标志辨认：影响半径大
    \/ 持续时间长 / 修改成本高],
    [#strong[它不是什么]], [⑥], [对立：实现决策------架构决策改不起（三标志），实现决策改得起（一次提交、局部影响）],
    [#strong[它不是什么]], [⑦], [辨析：架构决策 ≠
    架构风格------微服务/事件驱动是决策的”套路”，具体系统选了哪些决策才是架构决策本身；也不等于”画架构图时做的决定”------它藏在每个”改个字段”里],
    [#strong[它从哪来]], [②③⑨], [ISO/IEC/IEEE
    42010（2011）把”架构决策”列为架构的核心概念之一------架构不只是元素和关系，还是”由一系列决策所塑造的”；2011
    年 Michael Nygard 提出
    ADR（一页卡、几个固定问题）记录做法，使它从模糊说法变成可记录、可管理、可审查的工程对象；英文
    decision ← 拉丁
    decidere（切断、裁断）------把多个选项”切断”成唯一选择],
    [#strong[它往哪去]], [④⑤⑧], [它回答”改它要花多大代价”；在动手改字段前、评审前、排期前用；识别它→判断不可逆→抉择时机→记录留档→分级审查→定位层级；误区
    ✗ "改字段是小事"（数据模型可能正是最不可逆的架构决策）✗
    "架构决策是评审会上才有的"（没被识别出来的架构决策依然承担全部代价）],
    [#strong[判据]], [三标志], [改它影响半径大吗？持续时间长吗？修改成本高到不想改吗？------两问以上答”是”，就该按架构决策对待],
  )]
  , kind: table
  )
]

#heading(level: 2, outlined: false)[案例进展①｜恒信：一次”改个字段”，三次撞墙]
#label("案例进展①恒信一次改个字段三次撞墙")
万辉那天晚上把编号变更回滚了一半，没完全回滚------因为回滚也要成本。他把徐漾拉进会议室，第一次认真复盘”改个字段”这件事。

玉杰那晚负责执行回滚，只回滚了一半就停了。他把运维记录摊在桌上："代码能回滚，可台账、财务已经按新编号开始解析了，它们不跟代码一起回滚------要全回滚，得把已经按新编号解析过的数据迁回去，账重对一遍。"

"我到现在也没想明白，"万辉说，"我改编号规则之前，查过所有引用这个字段的地方，十二处，全改了。测试也过了。为什么还会炸？"

徐漾没有直接回答，反问："你改之前，有没有问过------'合同编号'这个字段，除了代码里十二处引用，还有谁在依赖它？"

万辉："还有谁？台账系统、财务对账、电子签章……它们都是对接方。"

"对。它们不在你的代码里，但它们是这份数据模型的'下游'。"徐漾说，"你说的十二处引用，是'字段'层面的依赖；台账、财务、审计，是'数据模型'层面的依赖。你只排查了字段层面，就把一个数据模型级的决策，当成字段级的操作给做了。"

小白负责对接方接口，她插了一句："合同编号在我们接口文档里是定义得最死的字段，台账、财务对接时都拿它当关联键------这字段从来不只是我们系统自己在用。改之前要是有人问我一声，我第一句就让他先问对接方。"

万辉沉默了一会儿："那我怎么知道，一个改动是'字段级'还是'数据模型级'？"

"你问自己三个问题，一分钟就能判断------#strong[改了它，要联动改多少东西？它定了之后，多少别的决定要建立在它上面？改错了，改回来的代价有多大？]
三个问题里有两个答'大'，你就该停下来，把这次改动当成一个架构决策来处理------先论证、先评审、先记录。而不是'今天就能上'。"

万辉把这三个问题记在笔记本上。他后来管这个叫”改字段前的三问”------而他每次问完都会发现，真正值得动手改的'字段'，比想象中少得多。

把方法论的那个瞬间点破：改字段前三问，就是第1章示范的判据主义在落地------给一个改动下判断之前，先给它一把可检验的判据。万辉这一章做的，也是一次概念校准：把”改个字段”，从”实现的细节”，修正为”架构的决策”。

#divider()

== 12.3 第二幕 架构决策不是什么（磨边界）
#quote(block: true)[
这一幕把”架构决策”和三种错觉分开------判据主义在这里正式展开：概念靠正例活、靠反例定型。三条磨过去：不可逆不是技术属性（是经济属性）、不可避免不是可做可不做、被动做不是没得选。
]

=== 12.3.1 错觉一：不可逆性是经济属性，不是技术属性
先说最容易被技术直觉带偏的一个错觉。前面反复说”修改成本高”"不可逆"，这两个词容易让人误解成”技术上改不了”。这句话必须澄清：

#quote(block: true)[
#strong[不可逆性不是技术属性，是经济属性。]
一个决策是否不可逆，取决于修改它需要多少人、多少时间、冒多大风险------而不是”技术上能不能改”。
]

#quote(block: true)[
术语注：'不可逆性是经济属性而非技术属性'是本书的#strong[凝练]------经济学”撤销成本随时间上升”的观察是共识，把不可逆归结为”经济属性”、用”能改
vs 值得改”来表述，是本书的说法，不是任何标准的条文；欢迎检验、欢迎校准。
]

几乎任何软件决策，技术上都能改。数据库能迁移、接口能重新设计、数据结构能重构------没有改不了的。但”能改”和”值得改”是两回事。#strong["不可逆"的真正含义是：修改成本已经高到一个临界点，以至于理性的决策者会选择”忍受现有架构的不完美”，而不是”掏出修改成本”。]

万辉的编号规则，技术上当然能改回去------但改回去的代价是：三个月的账重新对、历史数据重新排、台账重新解析、审计重新自查。当这个代价摆上桌，理性的选择往往是”算了，将就着用吧”。于是”能改”变成了”没人愿意改”------这才叫不可逆。

这解释了为什么”不可逆”是架构决策的核心属性，而不是普通决策的属性：#strong[普通决策改得起，所以选错了问题不大；架构决策改不起，所以选错的那一刻，代价就开始复利了。]

"不可逆性"这个词，是从经济学和决策理论里借来的。经济学很早就发现：有些决策一旦做出，撤销的成本会随时间上升------你越晚发现选错了，改错的成本越高。

而金融学里”期权的价值”这个概念（为”保留选择权”定价），正是理解”一个决策应该什么时候做”的关键。这两个概念，会在第三幕
§12.4.7
再次出现------到那时你会看到，"提前投资"不是一句口号，它有完整的经济学逻辑。

补一句位次，别让这把经济学的尺子喧宾夺主：#strong[不可逆性这把代价尺子，是从属的经济校验。]
它不是与目的判据并列的”另一半”------什么是
fundamental（对系统在其环境中的目的而言必要且充分，IEV 831-01-01 Note
2，见第10章 §10.2.3、第11章
§11.3.6）由#strong[目的]单独裁决；代价判据只在这组根本#strong[之内]决定哪些要提前锁死、锁到什么程度、哪些保留自由度------做优先级与时机的排序，#strong[不产生新的架构成员]。顺序不可颠倒：#strong[先过目的关]（是不是根本的），#strong[再过代价关]（值不值得现在定死）。两种错位都是病------用代价判据越过目的判据，把难改但无关紧要的东西误当架构；跳过代价校验，把根本的东西不分轻重全锁死，演化窒息。

=== 12.3.2 错觉二：架构决策不可避免------不是可做可不做
如果万辉提前知道”编号规则”是个架构决策，他会怎么做？他可能会说："那我们不做了。"------这是另一个流行的误读，值得单独用一节来说。

想象你要盖一栋三十层的办公楼。有两种做法：

#strong[做法 A：直接开工。]
找几个施工队就干。地基挖到一半发现下面是软土层，得加深------返工。墙砌到五层发现电梯井位置和管线打架------拆墙重来。封顶后发现消防通道宽度不达标------改不了。

#strong[做法 B：先出结构图、管线图、消防图。]
各专业对图协调，确认无误后按图施工。

直觉上，做法 A 是”没有架构”，做法 B
是”有架构”。这一节要说的反直觉结论是：#strong[做法 A
不是”没有架构”，它只是把架构决策推迟到了施工过程中。]

地基要多深、承重墙在哪、管线怎么走、电梯井在哪------这些决策在做法 A
里一个也没少做。只是它们发生在”挖到软土层之后”"砌到五层之后""封顶之后"------晚到修改成本已经爆炸。#strong[做法
A 和做法 B
的区别，从来不是”有没有架构”，而是”架构决策发生在成本低的时候，还是成本高的时候”。]

#figure(
  align(center)[#table(
    columns: 2,
    align: (auto,auto,),
    table.header([盖楼], [做系统],),
    table.hline(),
    [地基], [数据模型------改一张核心表的字段类型，成本可能波及几十个服务],
    [承重结构], [服务边界------拆分或合并服务，比改内部逻辑贵一个数量级],
    [管线], [接口契约------改了上游接口，所有下游全得适配],
    [消防规范], [安全合规------事后改造安全模型，往往意味着架构级推翻重做],
  )]
  , kind: table
  )

这也是第1章反事实检验的一次使用：把”画架构图”这个动作去掉，架构决策一个也没少------它们只是换了个时间，在更贵的时刻重新发生。

"做法 A 不是没有架构"这个结论，推出一条这一章最硬的判断：

#quote(block: true)[
#strong[架构决策不是”做不做”的问题，而是”什么时候做”的问题。你只有两个选择------在成本低的时候主动做，或在成本高的时候被动做。]
]

注意这句话的两个部分。前半句，"不是做不做"------很多人以为，我们跳过架构、直接开写代码，就等于”没有架构决策”。错。#strong[写第一行代码时，架构决策就已经在发生了]：数据存在哪个库、功能拆成几个模块、接口怎么定义、状态枚举分几类------这些决策一个也跑不掉。

不画架构图，不等于没有架构决策，只是做这些决策的人没意识到它们的不可逆性。

后半句，"主动做 or
被动做"------这就是全部的选择空间。#strong["我们不做架构决策"是一个伪选项：你只是选择在哪个时刻做它们。]
主动做，是在成本低、信息充分、还有回旋余地的时候做；被动做，是等代码写完了、数据存满了、团队定形了，被事故逼着做。

万辉的编号规则，就是”被动做”的教科书：不是他没有做数据模型的决策，是他把决策推迟到了”三个月后、历史数据都进去了、下游都对接了”的时候------然后用一场事故的价格，把决策重新做了一遍。

=== 12.3.3 错觉三：被动做不是没得选
"成本低的时候做"和”成本高的时候做”，差别有多大？这张修改成本曲线图，是整个架构决策思想的核心图景：

```
修改成本
  │                                       ╱
  │                                   ╱╱
  │                               ╱╱
  │                           ╱╱
  │                       ╱╱
  │                   ╱╱          ← 不做架构决策：前期零成本，后期修改成本爆炸
  │               ╱╱
  │           ╱╱
  │       ╱╱
  │   ╱╱
  │ ╱  ← 做架构决策：前期一笔小投资，后期修改成本保持平缓
  └──────────────────────────────────────────→ 时间
     需求    设计    开发    测试    上线    运维
```

曲线上方那条陡峭的线，是”不做架构决策”的人生：前期一分钱不花（不用开评审会、不用论证、不用写决策记录），后期每一次改动都越来越贵------因为改动要重算的东西越来越多。

曲线下方那条平缓的线，是”做了架构决策”的人生：前期多花了一点分析和决策的时间（那是一次性的小投入），后期每次改动都在一个被划定的解空间里进行，成本可控。

图下方那行字，就是全部经济学：#strong[前期的一小时决策，买的是后期的一个月返工。]
这还不是最关键的------最关键的，是那些”改不了”的决策。曲线再陡，至少还能改；有些决策错过窗口期之后，不是贵，是#strong[几乎没法改]------数据已经迁移完了、对外接口已经发布了、历史合同已经归档了。

这也是为什么”架构决策”要和”普通决策”分开对待：普通决策错了可以改，架构决策错了，是拿着整个系统的生命周期去还。

既然主动做这么划算，为什么还有那么多团队像万辉一样被动做？这个问题值得停下来------因为答案不是”他们傻”，而是四个非常人性化的原因，每个人都中过招。

#strong[第一，乐观偏差。]
人天然低估未来的改动频率和成本。改编号规则的时候，万辉脑子里想的是”改一下就完事”，不会去想”三年后档案里还躺着十万份按旧规则归档的合同”。#strong[未来被想象得太便宜，于是现在的决策被做得太随便。]

#strong[第二，责任分散。]
没有人被指定为”架构决策的所有者”。数据模型是谁的？前端说归后端，后端说归数据库，数据库说归业务------于是没有人对”编号规则”这个决策负责，它默认成了”顺手就能改的实现”。#strong[没有所有者的决策，不会得到它应有的重视。]

而”编号规则”这类架构决策的所有者，正是#strong[系统架构师]的专责：可以委托给模块架构师/模块工程师，但责任仍在自己（第16章委托---协商---验收）。

#strong[第三，反馈滞后。]
架构决策的代价，往往在几年后才显现------而且那时做决策的人可能已经转岗、升职、离开公司。"今天的决定，三年后的代价"------代价没有主人，决定就没有压力。

#strong[第四，也是最隐蔽的，"隐形的决策"无处显形。]
大多数架构决策不是在一次正式的”架构评审”上做的，而是散落在无数个”改个字段”"加个接口"里。没有人把它们收集起来、命名出来，它们就永远是一堆”顺手的改动”，永远得不到架构决策该有的待遇。

这四条合在一起，指向同一个结论：#strong[被动做不是没有架构，是”没有显式的架构”。]
而让架构决策显式化的第一步，就是认出它------给它一个名字、把它从”顺手改动”里挑出来。

这正是第一幕三标志的用途：不是让你把每件事都当成架构决策隆重处理，而是让你#strong[先把那些高不可逆的挑出来，不让它们混在低可逆的堆里被随手处理掉。]

#heading(level: 2, outlined: false)[案例进展②｜冰链镜照：温度记录的数据所有权，被”随手一改”]
#label("案例进展②冰链镜照温度记录的数据所有权被随手一改")
几乎在恒信翻车的同时，冰链也出了一档子事------数据所有权。

第10章讲过，M3
的架构里有一条铁规矩："温度数据归属追溯域，传感器只管采、云端只管传、追溯平台管存管管看------数据所有权边界谁都不许动。"这条规矩，是水忠当晚写下、复盘时当众过了一遍的。

然后来了个客户。一家大型连锁药店，订购了 2000 台
M3，提了一个要求："我们要自己导出原始温度数据，接入我们的质管系统。"

董勐的第一反应是："简单。给他们的质管系统开个直连接口，把原始温度数据直接推过去。"

水忠听到这句话，拦住了："董勐，你知道这个'简单'，改的是 M3 的什么吗？"

"不就是加个数据导出接口吗？"

"数据从哪来？"水忠问。

"追溯平台啊。"

"那'直连'的这条通道，绕过谁？"

董勐顿了一下："绕过……追溯域。传感器→云端→客户质管系统，不经过追溯平台。"

"对。你这一'简单'，把第10章我们当众定下的'M3
最不可逆的决定'------数据所有权归追溯域------给改了。传感器的原始数据开始直连客户系统，追溯平台对这批数据失去控制。三年后追溯出问题，谁负责？追溯平台看不见、管不着、审计对不上。"

董勐后背一凉。他忽然意识到，自己刚才准备动手的”加个接口”，是冰链这套系统里不可逆性最高的一档决策------"数据所有权归属追溯域"，正是第10章列为
M3
最不可逆的决定之一。他差点把它当成了”随手一改”。#strong[把高不可逆的决策，当成低可逆的决策随手处理------这正是第二幕要磨掉的第三种错觉。]（怎么量出”极高不可逆”，第三幕
§12.4.5 那张矩阵会给全。）

"所以这不是'接口问题'，是'架构决策'。"董勐把”直连”方案划掉，"客户要导出数据，可以。但要走追溯域的受控通道，每条导出记录留痕。这么一来，导出还是能导出，但数据所有权这条线，谁都没动。"

水忠点头："对。#strong[不可逆的决策，不是不能做------是要在看清代价之后，用对的方式做。]
客户需求可以满足，但满足的方式，不能是悄悄改掉系统里最不可逆的决定。"

#divider()

== 12.4 第三幕 架构决策往哪去（运用）
#quote(block: true)[
这一幕把尺子用起来：先给不可逆性一把三层判断框架（影响半径 / 信息成本 /
修改风险），再回答什么时候做（抉择）、怎么留下来（记录）、审查资源怎么分（分级）、放在哪一层做（定位）。
]

=== 12.4.1 怎么判断不可逆性：三层判断框架
前两幕把”架构决策”和”不可避免”讲清楚了。现在进入最实操的一步：#strong[怎么判断一个具体决策的不可逆程度？]

先回到第二幕 §12.3.1
那个澄清："不可逆"不是”技术上改不了”，而是”修改成本高到理性的人选择忍受”。这把判断不可逆性的任务，从”能不能”变成了”多贵”。而这个”多贵”，可以从三个维度分别评估------这就是架构领域常说的#strong[不可逆性的三层判断框架]。

#quote(block: true)[
术语注：'三层判断框架'（影响半径 / 信息成本 /
修改风险）是本书对架构领域常见判断维度的#strong[凝练与命名]，不是任何标准给出的固定条款------不同团队可能用不同的维度组合；本书据此组织本章的判断尺子，欢迎检验、欢迎校准。
]

判断一个架构决策的不可逆性，看三层：#strong[修改的影响半径、修改的信息成本、修改的风险。]
对照第一幕的三标志：三标志是识别用的快筛，三层是判断不可逆程度的细化------首项同名”影响半径”，三标志的”修改成本”对应三层的”修改风险”。下面三节各讲一层，§12.4.5
用一张矩阵把它们叠起来，§12.4.6 用万辉的案例完整走一遍。

给”不可逆性”一把判据主义的尺子：三层框架，就是不可逆性的可检验判据------拿它量任何决策，都能当场落一个”高
\/ 中 / 低”。

=== 12.4.2 第一层：修改的影响半径
改这个决策，需要联动修改多少东西？影响半径决定修改成本的下限------半径越大，成本的下限越高。

影响半径有三个来源，分别对应三种”耦合”：

#figure(
  align(center)[#table(
    columns: (33.33%, 33.33%, 33.33%),
    align: (auto,auto,auto,),
    table.header([耦合维度], [低耦合（易修改）], [高耦合（难修改）],),
    table.hline(),
    [#strong[结构耦合]], [数据归属明确，改一个域不影响其他域；内部接口，单一消费者], [数据跨多个服务共享，改所有权涉及数据迁移；公开接口，几十个外部消费者],
    [#strong[时间耦合]], [可渐进式迁移------v1
    接口保留三个月，下游逐步迁到
    v2], [必须一次性同步上线------如数据库表结构变更，所有读写方必须同步升级],
    [#strong[组织耦合]], [修改只需一个团队内部协调], [修改需要跨团队对齐、同步排期------技术耦合可以用工具解决，组织耦合只能开会],
  )]
  , kind: table
  )

万辉的编号规则，三样全占：结构上它跨财务、台账、审计（结构耦合高）；改的时候不可能让历史数据渐进迁移，只能一次性同步（时间耦合高）；牵扯三个部门排期（组织耦合高）。所以它的影响半径，注定很大。

=== 12.4.3 第二层：修改的信息成本
有些决策修改的成本，不在”动手改”，而在”重新搞清楚应该改成什么样”。

这一层有两个来源：

#strong[第一，决策的知识深度。]
当初这个决策是靠一条长推演链做出来的------跨多层、多域分析权衡后才得出的结论。修改它，意味着要#strong[重走一遍推演链]，重新确认所有下游依赖仍然成立。追溯链越长，修改的信息成本越高。

合同的数据模型，当初是法务、财务、业务三方反复对齐后定的：哪几个字段是主键、编号怎么编码、状态枚举怎么划分------每条后面都站着一段业务理由。万辉改的时候，这些理由没有一条被重新审视过。

#strong[第二，决策的隐含上下文。]
当初做决策的背景信息没有被记录下来，只存在于当事人的脑子里。当这些人离开、忘记或转岗后，修改一个决策的真实成本是：

#quote(block: true)[
#strong[修改一个决策的真正成本 = 重新发现当初为什么做这个决策的成本 +
实际修改的成本]
]

大多数时候，真正贵的是前者。万辉不是不知道编号规则改了会乱，他是不知道#strong[当初为什么这么定]------而”不知道为什么要这么定”，才是他敢”今天就能上”的原因。

把决策记下来，压低的正是一个决策的#strong[信息恢复成本]------写下来，后来人才不用靠猜”当初为什么这么定”。（第三幕
§12.4.8 会讲具体怎么记。）

=== 12.4.4 第三层：修改的风险
有些决策不是不能改，而是一旦改错了，后果严重到无法接受。

风险也有两个来源：

#strong[第一，爆炸半径。]
被修改的东西，是否处于业务关键路径、数据关键路径、安全关键路径的交汇点？

```
修改内部管理后台的按钮颜色 → 错了 → 难看，不急
修改核心支付流程的状态机   → 错了 → 资金损失，紧急
修改用户认证的鉴权模型     → 错了 → 安全漏洞，灾难
```

#strong[第二，数据完整性风险。]
涉及历史数据迁移的修改，自动升级一个风险等级：

```
修改新写入数据的表结构   → 低风险（新的按新结构写）
修改已有历史数据的表结构 → 中风险（迁移脚本可能出错、可能丢数据）
修改多系统共享的核心数据 → 极高风险（数据迁移 + 多系统同步上线 + 回滚策略复杂）
```

到这里，一个规律已经浮出水面------#strong[为什么数据所有权/数据模型，几乎是架构里最不可逆的决策？]
因为它在三层框架里全都占满：影响半径大（数据被多个系统依赖）、信息成本高（当初为什么这么分域，只有少数人知道）、修改风险极高（历史数据迁移，一步错全盘错）。

这就是为什么第10章把”数据所有权”列为 M3
最不可逆的决定之一------这一章给那个判断补上了完整的论证。

=== 12.4.5 综合判断：一张矩阵，一眼定级
把三层叠起来，一张判断矩阵就可以落地了：

#figure(
  align(center)[#table(
    columns: (20%, 20%, 20%, 20%, 20%),
    align: (auto,auto,auto,auto,auto,),
    table.header([决策示例], [影响半径], [信息成本], [修改风险], [综合判定],),
    table.hline(),
    [服务边界划分], [大（影响团队组织+接口契约）], [高（涉及业务域理解）], [高（数据迁移）], [#strong[高不可逆]],
    [对外接口契约], [大（外部消费者不可控）], [中], [高（破坏外部集成）], [#strong[高不可逆]],
    [数据所有权归属], [大], [高], [极高（数据迁移）], [#strong[极高不可逆]],
    [架构模式（微服务 vs
    单体）], [极大], [高], [极大], [#strong[几乎不可逆]],
    [数据库选型], [大], [中], [中（有迁移工具但成本高）], [#strong[中-高不可逆]],
    [编程语言选择], [大（全代码库）], [低（社区标准明确）], [中（重写成本高但可渐进）], [#strong[中不可逆]],
    [内部工具库选型], [中], [低], [低（可逐步替换）], [#strong[低不可逆]],
    [日志格式], [小], [低], [低], [#strong[高度可逆]],
  )]
  , kind: table
  )

这张矩阵的用法，不是把每个决策都拿去做完整评估------那是浪费。它是一把#strong[分诊尺]：先粗判一个决策落在哪一档，再决定给它什么待遇。高不可逆的，认真做、多人审、记录在案；低不可逆的，快速决定、错了再改、不设关卡。

#quote(block: true)[
#strong[概念卡片 · 不可逆性]（四问为纲，九格为目；格号=九格尺子）

#figure(
  align(center)[#table(
    columns: (33.33%, 33.33%, 33.33%),
    align: (auto,auto,auto,),
    table.header([四问], [格], [内容],),
    table.hline(),
    [#strong[它是什么]], [①], [修改成本高到理性的决策者宁愿忍受现状、也不掏钱去改的程度],
    [#strong[它不是什么]], [⑥⑦], [对立：可逆性------可逆决策改得起（一次提交、局部影响），不可逆决策改不起（数据迁移、接口重定、团队重组）；辨析：不可逆
    ≠
    技术上不能改------几乎任何软件决策技术上都能改；"不可逆"是经济属性，不是技术属性],
    [#strong[它从哪来]], [②③⑨], [从经济学与决策理论借来------经济学很早就发现：有些决策一旦做出，撤销成本随时间上升，越晚发现选错、改错成本越高；金融学的”期权价值”（为保留选择权定价）是理解”什么时候做”的关键；英文
    irreversibility ← in-（不）+
    reverse（倒转）------不能倒转回去的状态],
    [#strong[它往哪去]], [④⑤⑧], [它回答”一个决策什么时候做、给什么待遇”；在判断时机、分级审查、分配分析资源时用；误区
    ✗ "技术上能改，所以可逆"（能改和值得改是两回事）✗
    "不可逆就是没办法了"（不可逆决策恰恰更要早做、多做分析）],
    [#strong[判据]], [三层], [影响半径大吗（结构/时间/组织耦合）？信息成本高吗（知识深度/隐含上下文）？风险大吗（爆炸半径/数据完整性）？],
  )]
  , kind: table
  )
]

=== 12.4.6 完整演练：把三层框架套到”编号规则”上
理论说完了，用万辉的案例完整走一遍，让三层框架不再是抽象名词。

#strong[第一层，影响半径。]
万辉改”编号规则”，需要联动修改的东西清单：代码里引用编号的 12
处（字段层面）；台账系统的编号解析（结构耦合）；财务对账口径、审计归档口径（结构耦合）；三个部门同步排期（组织耦合）；历史数据必须一次性重排，无法渐进（时间耦合）。#strong[半径：大。]

#strong[第二层，信息成本。]
当初编号为什么这么编码？------法务、财务、业务三方协商的结果，没人写过文档（隐含上下文）；这
12
处引用各自承载什么业务语义，没人能一次说清（知识深度）。#strong[信息成本：高。]

#strong[第三层，修改风险。]
合同编号处于对账关键路径和审计关键路径的交汇点（爆炸半径大）；要动历史合同数据，重排即可能破坏审计链（数据完整性风险极高）。#strong[风险：极高。]

三层合计，综合判定落在”#strong[高-极高不可逆]”。对照第三幕 §12.4.9
的待遇表：应该”完整审查 + 至少一人独立审查 +
决策记录”。而万辉实际给它的待遇是：零审查、零记录、"今天就能上"。#strong[差距，就是那场三个月的连环爆炸。]

这一节想说明的不只是”万辉错了”，而是这把尺子的可操作性：任何一个人，拿分诊三问（联动改多少？信息成本高吗？风险多大？）过一遍，一分钟就能给一个决策分诊。#strong[分诊不需要完美------只要能把”高不可逆”从”低可逆”里挑出来，就已经赢了九成。]（分诊三问与改字段前三问共享第一个筛选问句”联动改多少”------一个筛”是不是架构决策”，一个量”不可逆到什么程度”。）

注意别把三组问句混起来：#strong[改字段前三问]（第一幕）回答”这是不是架构决策”，#strong[分诊三问]回答”这个决策多不可逆”，#strong[决策记录六问]（§12.4.8）回答”这个决策怎么留档”------识别、判断、记录，各管一段。

=== 12.4.7 抉择：什么时候做------定节奏与显式推迟
前面给了”不可逆性”这把尺子。现在用它来回答那个最实际的问题：#strong[面对一个决策，到底什么时候做？]

直觉上，"越早做越好"------架构决策早做，不是成本低吗？这个直觉对了一半，另一半是反的。架构决策的时间策略，不是一刀切”早做”，而是#strong[按不可逆性分级]：

#figure(
  align(center)[#table(
    columns: (33.33%, 33.33%, 33.33%),
    align: (auto,auto,auto,),
    table.header([决策类型], [特征], [操作],),
    table.hline(),
    [#strong[高不可逆]], [影响半径大 + 信息成本高 +
    修改风险高], [#strong[早做]，充分分析，多人审查],
    [#strong[中不可逆]], [影响半径中等], [做初步分析，预留调整空间],
    [#strong[低不可逆]], [修改成本低，可随时改对], [#strong[晚做]，快速决定，错了再改],
  )]
  , kind: table
  )

注意最后一行的”晚做”------它不是偷懒，是一条有名字的策略。架构经济学里有一个概念叫#strong[期权的价值]（option
value）：#strong[一个决策如果现在信息不够，推迟它本身就是一种价值------等到信息充分时再做，比现在瞎猜然后返工要便宜得多。]

```
提前决定：服务边界（因为改这个很贵）
推迟决定：具体缓存策略（因为现在信息不够，改这个也不贵）
必须早做：数据所有权归哪个域（因为一旦错了，数据迁移成本极高）
```

这就是”提前投资”这四个字的准确含义。它不是说”所有架构决策都要尽早做死”------恰恰相反，#strong[好的架构投资，是把有限的深度分析资源，集中投到真正不可逆的决策上]------上面三类决策，各自一种节奏。

每个架构决策，操作上都先过两关：#strong[先过目的关]------它守住的是不是对目的必要的不变量（判别问句见第11章
§11.3.6；不是，就不配被锁进架构）；#strong[再过代价关]------按不可逆性排优先级与时机（上面这张表）。目的关决定”值不值得当架构”，代价关决定”什么时候做、做多重”------两关的次序，正是
§12.3.1 那句”先过目的关、再过代价关”。

把”推迟”从一种无奈，变成一种操作，是架构决策里最反直觉的一招。

多数时候，"推迟"给人的印象是坏的------拖延、不做决定、踢皮球。但架构语境里的”推迟”，是#strong[显式地、有意识地、记录在案地”决定现在不做决定”]。两者的差别在于：

- 被动的拖延：#strong[不做]决策，也不记录，等事故来逼；
- 显式的推迟：#strong[做]一个”现在不做”的决策，记录下”等什么信息、什么时候再评估”，然后主动去收集那个信息。

这在高频迭代的场景里尤其重要。假设团队收到一个小变更，标准流是”快速分析
→ 实施”。嵌入不可逆性预判之后，变成：

```
收到一个变更 → 30 秒不可逆性预判 →
    低不可逆 → 快速实施
    高不可逆但信息不足 → 显式推迟：标记"需完整设计推演"，升级到更重的分析路径
```

30
秒的预判，能把一个”高不可逆但被当成小变更”的改动拦下来------就像董勐拦下那个”直连接口”一样。而”显式推迟”之所以是决策而不是逃避，因为它回答了三个问题：#strong[现在为什么不做？等什么信息？什么时候再评估？]
回答不了这三个问题，才是真拖延。

§10.4.7
说过一句话：#strong[架构师的核心能力，不是做出正确的决策，而是识别哪些决策如果不正确，后果很严重。]
这一章给它补上后半句：识别出”后果严重”之后，#strong[对信息不足的高风险决策主动推迟、对低风险决策不投入、对高风险决策足额投入------这本身就是最好的架构决策。]

这条能力，正是第16章架构师能力清单的第四项增量------判断不可逆性。

=== 12.4.8 记录：决策记录六问，把决策变成资产
讲了半天”主动做”"显式推迟""看清代价"，可这些东西做没做、怎么验证？答案是一个极其朴素的动作------#strong[把决策记下来。]

架构决策的记录，有一套公认的格式，回答六个问题：

#figure(
  align(center)[#table(
    columns: 2,
    align: (auto,auto,),
    table.header([问题], [回答什么],),
    table.hline(),
    [#strong[决策名称]], [这是一次什么决策？],
    [#strong[背景]], [当初为什么必须做这个决策？面对什么问题？],
    [#strong[选项]], [考虑过哪几个方案？各有什么好处和代价？],
    [#strong[选择]], [选了哪个？为什么？],
    [#strong[影响]], [选完之后，哪些后续工作被它决定了？],
    [#strong[不可逆性]], [这个决策现在反悔，要付出多大代价？],
  )]
  , kind: table
  )

#quote(block: true)[
术语注：'决策记录六问'是本书在
ADR（架构决策记录）惯例基础上的#strong[凝练]------Nygard 原版字段为
标题/状态/背景/决定/后果；本书省去”状态”、补入”选项”与”不可逆性”两问，把权衡过程与可逆性对账直接写进记录。六问不是
ADR 原版字段，是本书的格式，欢迎检验、欢迎校准。
]

记”不可逆性”之前，先过目的判据：这个决策守住的是不是对目的必要的不变量（判别问句：这东西变了，系统还能不能达成它的目的，见第11章
§11.3.6）------不是，则不配被锁进架构；是，再用三层框架量代价。六问的”不可逆性”栏，记的是代价关那一档。

用恒信”合同编号规则”这个决策来演示一遍------如果万辉当初写下了这么一段，三个月后的连环爆炸，至少会提前被识别：

```
决策名称：合同编号规则与电子签章平台对齐

背景：电子签章平台要求编号为"合同类型-年份-流水号"，
  与系统现有编号规则不一致，法务提出对齐需求。

选项：
  A. 全局重排编号，历史合同同步迁移
     好处：编号统一，下游口径一致
     代价：历史数据迁移，与纸质归档对照会乱，审计风险高
  B. 新旧编号双轨，历史合同保留原编号 ← 选了它
     好处：不动历史数据，审计可追溯，下游渐进适配
     代价：一段时间内两套编号并存，报表需兼容

选择：B。因为历史合同已与纸质归档绑定，全局重排的
  审计与追溯风险，远大于双轨并存的一段过渡成本。

影响：台账/报表需兼容两套编号；电子签章对接按新编号；
  历史合同的查询与追溯维持原口径；三个月后评估是否并轨。

不可逆性：中-高。已归档历史数据的口径不可逆（重排即破坏
  审计链）；新编号规则一旦被下游长期依赖，再改成本上升。
```

注意这段记录的价值------它不在当下。当下万辉当然知道为什么选
B；它的价值在一年后：换个人接手，看到”不可逆性：中-高”和那句”重排即破坏审计链”，就不会再犯”全局重排”的错。

#strong[决策记录是把一次决策，从”当事人脑子里的知识”变成”组织的资产”。]
§10.4.1
说架构必须被表达出来，才能被共享、被讨论、被检查、被传承；决策记录，就是架构决策的”表达”------被写下来的决策，才可能被共享、被检查、被传承。

记住它和识别用的改字段前三问不同------前三问答”是不是”，六问答”怎么留档”。别撞名，也别忘了各管一段。

六问记录之外，还有一个配套动作------#strong[在架构的关键节点上标注不可逆性]：

```
合同数据模型（合同主表 / 审批记录 / 附件）：
  不可逆性：极高
  理由：数据所有权归合同域，财务 / 台账 / 审计 / 电子签章均依赖
  修改代价：历史数据迁移 + 4 个下游系统适配 + 审计口径重建
```

标注的价值，和上面六问记录是同一件事------价值都在#strong[一年后换人接手]时兑现（论证以六问那段为母本，这里不再展开）；落到具体节点上的作用，是把第10章案例进展②里水忠定下的”老规矩”变成一块自带警示牌：换人看到”不可逆性：极高”和”理由”，就知道不要轻易动。

ADR
里还应记两样，常被漏：#strong[被否掉的备选死因]------它回答”为什么不是它”，是评审对质三问的第三问（第8章）；#strong[复审条件]------写下”什么情况出现就必须重审这条决策”（如新增数据源、权限模型变更），过复审条件不重审，这条
ADR 就过期了。复审条件里最高的一条是#strong[目的变更 /
目的漂移]------系统目的变了，fundamental
的判据本身换了，架构必须重审：#strong[目的变更]（战略调整、产品定位改变，显式事件）是强制触发器；#strong[目的漂移]（目的悄然变化而架构未跟随）靠以目的为参照的定期检视（两类扳机的分工见第14章）。两者都与架构漂移方向相反（架构漂移=实现偏离架构，架构对、执行歪；目的漂移=目的偏离架构，执行对、判据过期）。示例”新增数据源、权限模型变更”之外，第一条就写”目的变更”。复审条件建议作六问之外的可选第七项，不破坏六问框架。

=== 12.4.9 分级：把审查资源花在刀刃上
记录之外，还有一个更硬的落地机制------#strong[审查深度按不可逆性分级。]

不是所有架构决策都值得同样的审查投入。给一个低不可逆的决策开三场评审会，是浪费；给一个高不可逆的决策走”自查”，是灾难。分级原则是：

#figure(
  align(center)[#table(
    columns: 3,
    align: (auto,auto,auto,),
    table.header([不可逆性], [最低审查深度], [审查参与者],),
    table.hline(),
    [#strong[极高]], [完整审查（语义 + 影响）], [多人交叉审查],
    [#strong[高]], [完整审查], [至少一人独立审查],
    [#strong[中]], [语义符合性], [自查 + 同行确认],
    [#strong[低]], [快速自检], [自查即可],
  )]
  , kind: table
  )

这条机制和一个更朴素的原则是同一件事：#strong[把有限的审查资源，集中在真正不可逆的决策上，而不是均匀撒在所有决策上。]
万辉最大的问题，不是没审查------是他根本没意识到编号规则属于”高不可逆”，于是它连”自查”都没走，直接”今天就能上”。

回想第8章的三个看门人：评审、验证、确认是把关的动作；这一节的按级审查，是把关动作在”架构决策”上的具体配置------先分诊，再派尺子。

#heading(level: 2, outlined: false)[案例进展③｜恒信：决策被写下来之后]
#label("案例进展③恒信决策被写下来之后")
编号风波过去一个月，恒信来了一个新需求：合同要支持”电子签章 +
线下签章”混合模式，法务要求在合同上加一个”签章方式”字段。

这次，万辉没有直接动手。他先做了一分钟的三问："改了它，要联动改多少东西？------财务对账、台账、审计、电子签章对接，四五处。定了之后，多少决定建立在它上面？------合同状态流转、审批链、归档规则。改错了改回来代价大吗？------涉及已签合同的历史数据。"三问里两问半答”大”。

他停下来，填了一份六问决策记录，把”签章方式”放进了”选项”栏里讨论，而不是直接当作”字段”加进去。评审会上，他把”背景、选项、选择、影响、不可逆性”讲了一遍，黄程当场提出："混合模式下，'电子签章的合同'和'线下签章的合同'，归档口径得分开，审计才好查。"------万辉把这条补进了”影响”栏。

玉杰在旁边听了全程，补了一句："以前你们改字段，我负责的是最难看的那段------数据不跟代码一起回滚，炸了也只能硬扛。这次'影响'栏写得清楚，归档口径、对账兼容都点名了，我照着部署，心里有数。"

会后，徐漾路过万辉的工位，看到那份记录，说了句："你现在把'改字段'当成'做决策'了。三个月前你要是有这一步，编号那次炸不了。"

万辉："那次之后我才明白------#strong[不是所有改动都值得这么隆重，但你不先判断它值不值得，就没有资格说'它不值得'。]"

=== 12.4.10 定位：设计链各层的架构决策
讲到这里，说的都是”一个决策”------怎么判断、怎么做、怎么记录。现在把镜头拉远：#strong[从一条完整的设计链看，每一层的架构决策为什么长得不一样？]

#quote(block: true)[
与第11章对账：§11.3.3
从需求粒度把链切成”需求工程→架构设计→详细设计→实现”；这里从承接对象切成”需求组织→业务流程→系统功能→产品构件”。两种切法互为投影------一个看每层”吃什么”（粒度），一个看每层”谁承接、做什么决策”（对象）。
]

第4章讲过需求怎么被开发出来，第6章讲过设计怎么落地成一棵”树”。把它们连起来看：从”零散的需求”到”一个能用的系统”，中间隔着几层工作。每层的工作，都是在把上一层的东西，翻译成下一层能用的东西。而翻译的过程中，必然要做决策。一个做医药系统的朋友曾经这样类比，记忆深刻：

#figure(
  align(center)[#table(
    columns: (33.33%, 33.33%, 33.33%),
    align: (auto,auto,auto,),
    table.header([层], [类比], [在做什么决策],),
    table.hline(),
    [需求原始表述（第0层·输入边界，不决策）], [患者说”我头疼、吃不下饭、睡不好”], [不决策，只是表述],
    [需求组织], [医生分诊：头疼→神经内科，吃不下饭→消化内科], [把散乱的自述归类到科室],
    [业务流程], [神经内科医生：偏头痛还是紧张性头痛？持续多久？], [把笼统主诉拆成可诊断的症状单元],
    [系统功能], [确诊并开检查单：偏头痛，做脑电图+MRI], [把症状转化为可检验的医学判断],
    [产品构件], [开药方：普萘洛尔 40mg
    每天两次], [把判断转化为可执行的处方],
  )]
  , kind: table
  )

各层的决策所以不同，是因为#strong[每一层面对的需求抽象层次不同、向下传递的对象不同]。

有一个贯穿各层的共性，值得先钉死：#strong[每一层的架构决策，本质上都在回答同一个问题------"这部分需求，由谁来承接、以什么形式承接、和其他承接者是什么关系？"]
只不过每一层的”承接者”不同：

#figure(
  align(center)[#table(
    columns: 3,
    align: (auto,auto,auto,),
    table.header([层], [承接者], [决策问题],),
    table.hline(),
    [需求组织], [需求分类节点], [需求怎么组织？按什么维度分类？],
    [业务流程], [业务行为单元], [业务流程怎么组织？每个节点拆到什么颗粒度？],
    [系统功能], [系统功能点], [系统功能怎么组织？功能之间怎么归并？],
    [产品构件], [产品构件], [构件边界划在哪？构件之间怎么交互？],
  )]
  , kind: table
  )

这张表还可以换一种读法------#strong[判据传导链]。每一层的”目的”，由上一层为它定义（本层要承接的使命）；本层判定”什么是根本”（fundamental
\= 对目的必要且充分，IEV 831-01-01 Note
2）的标尺，就是这一层的这个目的。于是每一层的架构决策，都是拿”上一层的目的”当
fundamental
标尺做出来的。#strong[断链处------本层做决策时够不到上一层的目的是什么------就是过度工程（为不服务于上层目的的能力提前建能力）与欠工程（上层目的要求的能力本层没接住）的发病点。]

判据传导链不只在这一条链里成立------它向上还能接到战略对齐（业务战略→产品战略→架构战略→技术实现，见第16章
§16.4.5）：每一层的目的，同样是下一层的 fundamental 标尺。

判据传导链还带出第10章 §10.2.3
三重过滤的#strong[层级关]：判定在当前承接对象（EoI）的粒度上进行------本层只在本层被架对象的粒度上裁
fundamental，元素内部的细节由下一层去裁，本层不越级。

用恒信的合同审批系统把这张表填满，你会看到每一层的架构决策都活生生：

- #strong[需求组织层]------"合同审批"的需求怎么分类（按部门？按合同类型？按审批角色？）
- #strong[业务流程层]------审批流从”发起→初审→校验→终审→归档”拆到什么颗粒度（每个节点是不是一个完整的业务动作）
- #strong[系统功能层]------审批引擎提供哪些功能、功能之间怎么归并（审批、会签、抄送、台账是几个功能）
- #strong[产品构件层]------审批引擎、电子签章对接、台账是几个构件、边界划在哪

=== 12.4.11 需求有架构吗？
一个经常把工程师绕晕的问题：#strong[需求不是回答”要什么”吗？架构不是回答”怎么做”吗？需求有架构吗？]

答案是：#strong[需求本身没有架构，但需求的方案有架构。]

把”零散的需求”和”组织好的需求”分开看。刚收集上来的需求，是散乱的、可能重叠甚至矛盾的表述------这个阶段，不需要架构，也不应该有架构，它就是一张待整理的清单。

但当你面对这堆零散需求，要把它们组织成一套结构化的、可验证的、可以往下传递的需求体系时------#strong[这个组织工作本身就是一次设计，它的输出是”需求的方案”，而方案就需要架构。]
就像盖楼：一车砖（零散需求）不需要结构设计；但一车砖怎么砌成承重墙、怎么分布成房间（需求怎么组织），需要结构设计。

这个区分有个实际的推论：#strong["需求架构"这个词，正确的意思不是”需求有架构”，而是”需求的方案有架构”。]
它里面的架构决策------按什么维度分类、每条需求写到什么粒度、冲突的需求怎么处理------

#strong[不决定”系统怎么做”，但决定了”系统设计从哪里出发、按什么逻辑组织、怎么验证说全了没有”。]
这些决策的影响贯穿整条设计链，不可逆性极高。

这一章不止一次提到”需求怎么组织”的决策。它是设计链上第一个架构决策，也是几乎所有下游决策的起点------需求分类分错了，后面每一层都在错的框架里打转。第4章说”需求是开发出来的”，这一章给它加一个注脚：#strong[需求不只是被开发出来的，还是被”架构”出来的------组织需求的方式，本身就是一次架构决策。]

#quote(block: true)[
与第10章对账：§10.3.2
把”需求架构”列为最抽象的架构描述层次，说的正是”需求的方案”的架构描述------这里给出更精确的说法，两者不矛盾。
]

=== 12.4.12 链的两端，承受最高风险
把各层决策摆在一起，你会看到一张非常对称的图景------#strong[这条链的两端，不可逆性都极高；而中间两层，反而是修改成本最低、最能吸收不确定性的地方。]

#figure(
  align(center)[#table(
    columns: (25%, 25%, 25%, 25%),
    align: (auto,auto,auto,auto,),
    table.header([层], [在做什么决策], [改错后果], [不可逆性],),
    table.hline(),
    [需求组织], [需求怎么分类、怎么组织], [整条链从这里出发------根歪了，整棵树歪], [#strong[极高]],
    [业务流程], [业务怎么组织、产出什么业务行为], [流程结构不合理、颗粒度失当], [中-高],
    [系统功能], [系统功能怎么组织], [功能混乱、质量关注点遗漏], [中],
    [产品构件], [构件边界和契约], [系统无法稳定演化------数据迁移、接口重定、团队重组], [#strong[极高]],
  )]
  , kind: table
  )

为什么两端最高、中间缓冲？逻辑很直白：

- #strong[需求组织层（链的起点）]：它定义了整条链的出发点和完备性判据------根歪了，后面全歪。改它虽然不碰代码，但要重做下游所有层；
- #strong[产品构件层（链的终点）]：它是最终落地的东西------改构件边界，不是改文档，是改代码
  \+ 数据 +
  团队。数据库要拆、接口要重定义、团队要重新划分。这是量级的差异；
- #strong[中间两层]：改它们的成本在”文档层面”，它们的作用正是#strong[缓冲和吸收不确定性]------在文档层面把问题充分暴露和解决，避免带到代码层面。

这个结构说明了一件事：#strong[设计链上的分析工作，不是为分析而分析。它的价值，就是在”产品构件”这最后动刀之前，把问题在便宜的层面充分暴露和解决。]
需求组织的决策贵（影响全链）、产品构件的决策更贵（落地即资产）；中间的每一层分析，都是在用”文档的代价”换”代码的代价”。

万辉的编号规则之所以炸，换到这条链上说，是他跳过了前面所有层的分析，直接在”产品构件”这个最贵的层面动了刀------连”需求组织”这一层都没走。#strong[他把一条本应该在链条上游便宜地解决的决策，拖到了链条下游最贵的地方才被迫解决。]

#heading(level: 2, outlined: false)[案例进展④｜恒信：把决策放回它该在的层]
#label("案例进展④恒信把决策放回它该在的层")
万辉后来养成了一个习惯：任何一个变更进来，先问一句”这个变更，落在哪一层？”

有一次，业务方提了一个需求："合同审批要支持'多人会签'，三个人以上联合审批。"万辉没有再直接改审批流代码，而是先看它落在哪一层：

- 落在#strong[需求组织层]吗？------不是。这不是”需求怎么分类”的问题，是需求内容变了；
- 落在#strong[业务流程层]吗？------是。审批流从”单人审批”变成”多人会签”，是业务流程结构的改变，是业务流程层的架构决策；
- 落在#strong[系统功能层]吗？------一半。审批引擎要支持会签节点，是功能能力的变化；
- 落在#strong[产品构件层]吗？------不是。不需要动构件边界，审批引擎还是那个引擎。

"多人会签"于是先在业务流程层被认真设计------流程怎么定义会签节点、会签和单人审批怎么共存、历史单人审批记录怎么兼容。分析完了，才落到功能层和构件层去实施。

徐漾在旁边看着，评价了一句："你以前改一个需求，直接从'需求'跳到'改代码'，中间两层你根本没走。现在你知道先把决策放到它该在的层------#strong[在便宜的层做对的决策，贵的层才不用付返工的学费。]"

万辉把这句话记了下来。他后来在团队里立了个规矩：每个变更，先填一行”这个变更落在哪一层、要动的是哪一类决策”------不是走形式，是逼自己在动手前，先把决策分诊清楚。

#divider()

#heading(level: 2, outlined: false)[本章概念总图]
```
架构决策 = 选错了要花大代价才能改对的选择
  三标志：影响半径大 · 持续时间长 · 修改成本高
  识别：改字段前三问（联动多少？定后多少决定建立在它上？改错代价多大？）
  反例：改个变量名（可逆、局部、改得起）——不是架构决策

三种错觉（磨边界弧）：
  错觉一：不可逆不是技术属性，是经济属性——"能改"和"值得改"是两回事（代价判据从属于目的判据：先过目的关再过代价关）
  错觉二：不可避免不是可做可不做——盖楼思想实验：不是做不做，是早晚
  错觉三：被动做不是没得选——被动做 = 没有显式的架构；曲线：前期一小时买后期一个月

不可逆性：三层判断框架
  影响半径（结构/时间/组织耦合）→ 信息成本（知识深度/隐含上下文）→ 修改风险（爆炸半径/数据完整性）
  最不可逆：数据所有权 / 数据模型 / 服务边界 / 对外接口契约
  矩阵分诊 → 完整演练；分诊三问 = 判断不可逆程度（区别于识别的改字段前三问）

判断链：识别 → 判断 → 抉择 → 记录 → 分级 → 定位（识别属第一幕、分级并入第三幕）
  抉择：先过目的关（是不是根本）再按不可逆性定节奏（高不可逆早做 · 低不可逆晚做 · 期权价值 · 显式推迟 = 记录"现在不做"）
  记录：决策记录六问（名称/背景/选项/选择/影响/不可逆性）+ 不可逆性标注 + 复审条件（目的变更=重审触发器）
  分级：审查深度按不可逆性分级，资源集中在刀刃上
  定位：设计链各层——需求组织（极高）→ 业务流程（中-高）→ 系统功能（中）→ 产品构件（极高）；判据传导链：每层目的=下一层 fundamental 标尺
        需求本身没有架构，需求的方案有架构
```

#heading(level: 2, outlined: false)[章末 · 认知反转]
回到章首。万辉把”合同编号规则”当成”改个字段”，当天改完当天上线，三个月后连环爆炸。现在我们知道了，那场爆炸，不是一次”改字段失误”，它连错了四层，一层比一层深。

#strong[第一层，他把”架构决策”当成了”实现决策”。]
数据模型------合同编号怎么编码、状态枚举怎么划分------是这套系统里不可逆性最高的决策之一，三标志全中：影响半径大、持续时间长、修改成本高。他却用”改个字段”的方式处理了它：不识别、不评审、不记录，直接”今天就能上”。#strong[没有被识别出来的架构决策，不会得到它应有的审查，却依然承担架构决策的全部代价。]

#strong[第二层，他不知道架构决策不可避免。]
他以为”这次改动不需要做架构”是一个选项------错了。数据模型的决策他早晚要做：不是在那次”改字段”时做，就是在三个月后那场事故里做。#strong["我们没做架构决策"从来不是一个选项，你只是在选择”在成本低的时候做”还是”在成本高的时候做”。]
他选择了后者，于是多付了三个月的账。

#strong[第三层，他没有一把判断不可逆性的尺子。]
三层框架------影响半径、信息成本、修改风险------没有一样是他改字段前会想的。如果他先问分诊三问："联动改多少东西？信息成本高吗？风险多大？"------他会发现答案全是”高”，然后停下来，把这次改动当成架构决策来对待。

#strong[第四层，也是最深的一层，他错过的不是一次决策，是一整条链。]
从需求组织到产品构件，每一层都有它该做的架构决策；而万辉的做法，是跳过中间所有层，直接在”产品构件”这个最贵的层面动刀。#strong[在便宜的层做对的决策，贵的层才不用付返工的学费]------这句话，是这一章所有论证的收束。

这一章的四次反转，其实是同一个反转的四种说法：

#quote(block: true)[
#strong[架构决策不是评审会上的事，它藏在每一个”改个字段”里；它不可避免------不是做不做，是早晚；而不可逆的决策，必须在成本低的时候、用对的方式提前做。]
]

用第1章的话收一次束：对”架构决策”，你原来只是”见过”------背得出定义、知道”提前投资”这个说法；这一章把它推到”拥有”------能答全四问（它是什么、不是什么、从哪来、往哪去），能用改字段前三问识别它、用三层框架判断它、用决策记录六问留下它。#strong[能答全四问才算拥有，这把尺子现在装进你手里了。]

回看第10章的结尾。第10章说：架构，是对不可逆决策的提前投资。这一章把这句话拆成了可操作的动作------识别哪些决策不可逆（三标志）、判断不可逆到什么程度（三层框架）、在正确的时机用正确的方式做（按不可逆性分级、显式推迟、决策记录六问）、放到正确的层面做（设计链各层）。

#strong[第10章回答”什么算架构”，这一章回答”架构怎么被决定”。]

而”提前投资”这个词，给下一章埋下了钩子。投资一旦做出，就需要被管理：一个决策做出之后，系统一直在变，谁来保证”当初那个决定”不被悄悄改掉、不被版本搞乱、不会被遗忘？

这就要说到#strong[配置管理]------基线把”已经批准的决定”冻结成参照，版本记录每个决策的演进，变更控制守住基线的门。第10章把”不可还原的涵义”找了出来，这一章把它变成了”不可逆的决策”，下一章，我们要让它在变化中”说得清”。

#heading(level: 2, outlined: false)[本章判据]
#quote(block: true)[
十条判据是本书的主张，不是任何标准的条文（判据3的”目的判据锚”出自 ISO
42010 所引 IEV 831-01-01 Note
2，"代价从属"关系为本书凝练）------它们都当场可检验：欢迎检验、欢迎校准。
]

+ #strong[架构决策三标志]：这个决定”影响半径大吗 / 持续时间长吗 /
  修改成本高吗”------两问以上答”是”，就该按架构决策对待；三问全否的，是实现决策。识别用改字段前三问。#strong[违反即崩：万辉把”合同编号规则”当”改个字段”------三标志全中，却按实现决策”今天就能上”，三个月后连环三爆。]
+ #strong[架构决策不可避免]："我们没做架构决策"是伪选项------架构决策不是做不做的问题，是”成本低时主动做”还是”成本高时被动做”的问题。#strong[违反即崩：盖楼”做法
  A”看起来没有架构------地基、承重、管线一个也没少做，只是都拖到成本爆炸时才做。]
+ #strong[不可逆性是经济属性（从属判据）]：不可逆 ≠
  技术上不能改；它的含义是修改成本高到理性的人选择忍受。"能改"和”值得改”是两回事。#strong[代价判据从属于目的判据]------什么是
  fundamental
  由目的单独裁决（第10/11章），代价只在这组根本之内排优先级与时机；先过目的关、再过代价关，两种错位都是病（§12.3.1）。#strong[违反即崩：拿”技术上能改回去”就说可逆------编号规则技术上当然能改，代价是三个月的账重对、历史数据重排，于是”能改”变成了”没人愿意改”。]
+ #strong[三层判断框架]：改一个决策，从”影响半径（结构/时间/组织耦合）→
  信息成本（知识深度/隐含上下文）→
  修改风险（爆炸半径/数据完整性）“三个维度评估不可逆性；一分钟版用分诊三问。#strong[违反即崩：只量”改起来多贵”一个维度------万辉只看到十二处引用改完，没量信息成本与修改风险，三爆一样不少。]
+ #strong[数据所有权几乎最不可逆]：数据模型/所有权在三层框架里全占满------影响半径大、信息成本高、修改风险极高。改动它，要按最高不可逆对待。#strong[违反即崩：把数据所有权当”加个导出接口”------董勐差点直连客户系统绕过追溯域，三年后追溯出问题没人负责。]
+ #strong[按不可逆性定节奏]：高不可逆→早做、充分分析、多人审查；中不可逆→初步分析、预留调整空间；低不可逆→晚做、快速决定、错了再改（期权的价值）。#strong[违反即崩：一刀切”越早做越好”------把低不可逆的缓存策略也拉去开三场评审会，分析资源被均匀撒掉，真不可逆的决策反而没有分析。]
+ #strong[显式推迟是决策]：信息不足的高风险决策，"决定现在不做、记录等什么信息、何时再评估"------是架构操作，不是拖延。#strong[违反即崩：回答不了”现在为什么不做？等什么信息？何时再评估？"的"再等等”，就是被动拖延------等事故来逼。]
+ #strong[决策记录六问]：名称 / 背景 / 选项 / 选择 / 影响 /
  不可逆性------把决策从个人知识变成组织资产，降低未来的信息恢复成本（与识别用的改字段前三问各管一段）。#strong[违反即崩：写不出”背景”和”选项”的记录------当初为什么这么定没人知道，换人接手只能靠猜，万辉”今天就能上”的事故重演。]
+ #strong[审查资源分级]：审查深度按不可逆性分级，把资源集中在真正不可逆的决策上，而不是均匀撒在所有决策上。#strong[违反即崩：给低不可逆的决策开三场评审会是浪费；给高不可逆的决策走”自查”是灾难------万辉连自查都没走，直接”今天就能上”。]
+ #strong[设计链两端最高风险]：需求组织（链起点，极高）和产品构件（链终点，极高）承受最高不可逆性；中间层的作用是缓冲和吸收不确定性------在便宜的层解决，别拖到贵的层。#strong[违反即崩：跳过中间所有层，直接在”产品构件”这个最贵的层面动刀------万辉连”需求组织”都没走，把本该上游便宜解决的决策拖到下游最贵处才被迫解决。]

#heading(level: 2, outlined: false)[自查 · 这些说法对吗？]
+ "改一个字段，就是改一个字段，跟架构没关系。"------#strong[错]。数据模型、编号规则、状态枚举往往是系统里不可逆性最高的架构决策；"改个字段"可能是改架构。
+ "我们直接写代码，不做架构决策。"------#strong[错]。架构决策不可避免------写第一行代码时它们就在发生；"不做"只是选择在成本高的时候被动做。
+ "这个决策技术上能改回去，所以它是可逆的。"------#strong[错]。不可逆性是经济属性不是技术属性；"能改"和”值得改”是两回事------修改成本高到不想改，就是不可逆。
+ "架构决策越早定越好。"------#strong[不准确]。按不可逆性分级：高不可逆早做，低不可逆晚做（保留期权价值）。一刀切”早做”会浪费分析资源。
+ "推迟决策就是拖延，是坏事。"------#strong[错]。显式的推迟------记录”现在不做、等什么信息、何时再评估”------是架构操作；被动的拖延才是问题。
+ "不可逆的决策就不能动，一动就是灾难。"------#strong[错]。不可逆决策不是不能做，是要在看清代价后用对的方式做（像董勐的受控导出）。
+ "写决策记录是浪费时间，当下我知道为什么这么选。"------#strong[错]。记录的价值在一年后------降低”重新发现为什么”的信息恢复成本，把决策变成组织资产。
+ "需求只是'要什么'，需求没有架构。"------#strong[半对]。需求本身没有架构，但”需求的方案”（需求怎么组织）有架构，而且是整条链的起点。
+ "所有架构决策都应该走完整评审。"------#strong[错]。审查深度按不可逆性分级------低不可逆的快速自检即可，把审查资源集中在高不可逆的决策上。
+ "判断一个决策的不可逆性，看'改起来多贵'一个维度就够了。"------#strong[错]。三层框架------影响半径（结构/时间/组织耦合）、信息成本（知识深度/隐含上下文）、修改风险（爆炸半径/数据完整性）------三个维度各打高/中/低，综合判定。
+ "数据所有权是 DBA
  的事，不是架构决策。"------#strong[错]。数据模型/所有权在三层框架里全占满------影响半径大、信息成本高、修改风险极高------几乎最不可逆，改动它要按最高不可逆对待。
+ "决策记录写了六问就行，不用在关键节点标注不可逆性。"------#strong[错]。标注=等级+理由+修改代价，让”老规矩”落到具体节点时自带警示牌------换人接手看到”不可逆性：极高”就知道不要轻易动它。
+ "越难改的决策越重要，贵的东西就该优先锁死。"------#strong[错]。难改 ≠
  重要：什么算 fundamental
  由目的判据裁决，代价只在这组根本之内排优先级与时机------难改但无关紧要的不配当架构；先过目的关、再过代价关（§12.3.1）。
+ "复审条件写'新增数据源、权限模型变更'就够了。"------#strong[错]。第一条该写”目的变更”------系统目的变了（含目的漂移的悄然变化），都是架构重审的触发器（§12.4.8、第14章：变更=显式事件强制触发，漂移=以目的为参照定期检视）。

#heading(level: 2, outlined: false)[练习]
+ 挑一个你最近做过的改动，用”三标志”检验：它影响半径大吗？持续时间长吗？修改成本高吗？它是个架构决策，还是个实现决策？
+ 找出你们系统里一个”数据模型级”的决定（主键怎么定、状态枚举怎么划分、字段口径），思考：如果重做，要联动改多少东西？
+ 用”三层判断框架”评估你们系统里一个具体的决策------影响半径、信息成本、修改风险，各打一个高/中/低，得出综合判定。
+ 找出一个”当初不知道为什么要这么定、现在也不敢改”的决定------它大概率是信息成本极高的不可逆决策。把它写下来，补上”背景”。
+ 用六问格式，为你们最近一次重要的架构决策写一份记录。如果你找不到”背景”和”选项”------说明这个决策从没被认真记录过，这本身就是发现。
+ 列出你们系统里五个决策，按不可逆性排序。检查一下：你们投入的审查资源，是跟着这个排序走的吗？
+ "推迟决策"实操：找一个”信息不足但不可逆性高”的决策，写三句话------现在为什么不做？等什么信息？什么时候再评估？
+ 把一个”高不可逆”的决策（数据所有权、服务边界、接口契约），对照审查分级表：你们当前对它投入的审查深度，匹配吗？
+ 从需求到实现，画出你们这条链（不需要四层，三层也行），标出每一层”不可逆性最高”的决策，看它们是不是集中在两端。
+ "需求的方案有架构"------你们的需求是怎么组织的（按什么分类、什么粒度）？这个组织方式本身，是被认真决策过的，还是顺手定的？
+ 复盘一次”被动做架构决策”的经历（像万辉那样，被事故逼着改）：如果能回到当时，你会在哪个时刻、用哪把尺子拦住它？
+ 挑一个高不可逆的决策，走两关：先过目的关（它守住的是对目的必要的不变量吗？），再过代价关（按三层框架排优先级与时机）------两关的次序错了吗？

（答案要点见书末附录，与训练教材题卡同源）

#heading(level: 2, outlined: false)[小结]
这一章把”架构决策”这个词钉死，并给了它一把可操作的尺子。#strong[架构决策]是”选错了要花大代价才能改对”的选择，用三标志辨认------影响半径大、持续时间长、修改成本高；它藏在每一个”改个字段”里，识别它（改字段前三问），是正确对待它的第一步。

第二幕磨掉三种错觉：#strong[不可逆性是经济属性]不是技术属性；#strong[架构决策不可避免]------不是做不做的问题，是”成本低时主动做”还是”成本高时被动做”的问题，"我们没做架构决策"是伪选项；#strong[被动做不是没得选]------它只是没有显式的架构。

第三幕把尺子用起来。#strong[不可逆性]用三层框架判断------影响半径（结构/时间/组织耦合）、信息成本（知识深度/隐含上下文）、修改风险（爆炸半径/数据完整性）；数据所有权/数据模型在其中几乎最不可逆。

"提前投资"的意思不是”所有决策都早做”，而是#strong[先过目的关、再过代价关]：什么是
fundamental
由目的判据裁决（第10/11章），代价判据只在这组根本之内按不可逆性决定节奏------高不可逆早做、低不可逆晚做（保留期权价值）、信息不足的高风险决策显式推迟。决策要用六问记录下来（名称/背景/选项/选择/影响/不可逆性），复审条件第一条写”目的变更”，审查深度按不可逆性分级。

最后，在整条设计链上，每一层的架构决策都不同------需求组织（链起点）和产品构件（链终点）承受最高不可逆性，中间层的作用是缓冲和吸收不确定性。#strong[需求本身没有架构，需求的方案有架构。]

对”架构决策”这四个字，你从”见过”走到了”拥有”：能答全四问，能用一条判断链（识别→判断→抉择→记录→分级→定位）走完任何一次改动。

到这里，把一条需求变成决定的路，走完了------从一棵树，到一次架构决策，每一站都有它的尺子。可决定做出来，不是终点：系统还在变，得有人守着它。

下一章，我们讲”决定做出来之后怎么被守护”。架构决策一旦做出，系统还在变，谁来保证”当初那个决定”不被悄悄改掉？#strong[配置管理]------用基线冻结、用版本追踪、用变更控制守护。第10章找到”不可还原的涵义”，这一章把它变成”不可逆的决策”，下一章，让它”说得清”。

散会时，董勐把”受控导出”写进了追溯平台的接口清单。以前他以为数据所有权就是”归对域、不许改字段”------归位就完事；这一章之后他知道，所有权是一条线，导出外给也动它------数据所有权不可逆，接口不是想开就开。

#divider()

#heading(level: 2, outlined: false)[参考文献]
#strong[标准] - R-04 ISO/IEC/IEEE 42010:2011（"基本"判据见所引 IEV
831-01-01 Note 2）------ 架构决策作为架构定义的一部分

#strong[经典文献] - R-36 Nygard, "Documenting Architecture
Decisions"（2011）------
架构决策记录（ADR）的起源（六问为本书适配；被否备选死因与复审条件为本书补充）

#quote(block: true)[
对照阅读：本章”决策记录与不可逆性”呼应第6章”需求与方案结对”、第8章”评审对目标”、§10.2.3”不可还原三判据”与
§10.4.1”架构必须被表达出来”、第11章”五块设计”。
]
