# EOS 运营体系系统设计准则
**System Design Standards for EOS Development**

**文档版本**：v3.0（基于通用设计准则 v2.3——方法引用 + EOS 专有扩展）
**创建日期**：2026-05-25
**最近修订**：2026-05-25
**目标受众**：EOS 开发者（需求提出者、业务流程开发者、信息系统开发者）

---

> **重要**：本准则不重复通用方法论核心。阅读前建议先了解[通用设计准则](../../../00-general/10-general-system-design-standards/01-general-system-design-standards.md)的基本概念（五层结构、核心规则、分解/分配/分析）。

## 快速导航

本准则是 EOS 运营体系的系统设计规范。如果你是：

- **首次接触**：从 §一（产品语境）开始，理解 EOS 是什么以及它和流水线工具的关系
- **需要理解三层工作层面**：直接看 §2.1
- **需要做 PA 分解决策**：直接看 §2.3 PA 节点定义原则 + §2.4 信息处理模式
- **需要了解端到端业务框架**：看 §2.2
- **需要设计某层的具体内容**：看 §四（各层设计指引）对应章节
- **需要检查设计质量**：看 §五（验收标准）+ 通用准则 §七（检查清单）
- **需要了解文档间关系**：看 §6.3（规范关系表）

---

## 零、前言

### 本文档的定位

本文档是 **EOS 运营体系的系统设计准则**。EOS（Enterprise Operation System）是企业运营体系——覆盖市场、研发、生产、售后和管理等业务的 IT/AI 化运营系统。本准则定义在系统设计阶段（阶段1）如何对 EOS 进行系统设计。

### 与通用准则的关系

通用系统设计准则（`00-general/`）是产品无关的方法论核心，本文档是该方法论在 **EOS 运营体系**这个软件类产品上的实例化。两者分工如下：

| 方法论层次 | 定义位置 | 本文档的处理 |
|-----------|---------|------------|
| **通用方法论核心**（五层结构、分解/分配/分析、三条核心规则、验收标准、检查清单） | [通用设计准则](../../../00-general/10-general-system-design-standards/01-general-system-design-standards.md) | **直接引用**，本文不重复 |
| **EOS 产品特有内容**（产品语境、三层工作层面、端到端业务框架、PA 分解三因素、信息处理模式、各层场景化指导） | 本文档 | **完整定义** |

### 阅读路径

| 如果你是 | 建议的阅读路径 |
|---------|--------------|
| 首次接触 | 先读[通用准则 §一~§二](../../../00-general/10-general-system-design-standards/01-general-system-design-standards.md)建立方法论基础，再读本文 §一、§二 |
| 需要做 EOS 的具体设计 | 直接看 §四（各层设计指引），对照通用准则 §五（六步法） |
| 需要理解流水线与 EOS 的关系 | 看 §二（特有设计框架），特别是 §2.1 三层工作层面 |
| 需要检查设计质量 | 看 §五（验收标准）+ 通用准则 §七（检查清单） |

### 章节映射

| 本文章节 | 对应的通用准则 |
|---------|--------------|
| §一 EOS 的产品语境 | 无直接对应（产品特有） |
| §二 EOS 的特有设计框架 | 无直接对应（产品特有） |
| §三 通用方法论精要 | §一~§七（摘要引用） |
| §四 各层设计指引 | §五（方法在 EOS 场景的应用指导） |
| §五 EOS 验收标准 | §六（仅保留 EOS 特有项） |

---

## 一、EOS 的产品语境

### 1.1 EOS 产品概述

#### 产品类型

EOS 是**软件类产品**。其 PA 构件（交付物）的形态以软件组件为主（前端、后端、数据、基础设施），而非文档或实物组件。

#### 什么是 EOS

EOS（Enterprise Operation System）是企业运营体系的简称。本准则关注的企业范围涵盖市场、研发、生产、售后和管理等业务。

#### 产品开发四阶段

产品开发一般分为四个阶段：

| 阶段 | 名称 | 说明 |
|------|------|------|
| 阶段1 | **系统设计** | 从原始需求出发，经过五层结构（OR → SR → BA → SysReq → PA），开发出产品架构，定义每个构件的需求和实现策略 |
| 阶段2 | **构件开发** | 对产品架构中所有需要新开发或改进的构件进行开发 |
| 阶段3 | **系统集成与交付** | 沿产品架构的树形层级逐级集成，形成满足系统需求的完整产品 |
| 阶段4 | **系统运维** | 产品交付后的持续运行、监控、维护和优化 |

**本准则的范围**：严格限定在**阶段1（系统设计）**。

#### EOS 开发者的三个角色

| 角色 | 职责 | 关注点 |
|------|------|--------|
| **需求提出者** | 提出运营体系建设、优化或改进需求 | 需求的完整性、清晰性、可追溯性 |
| **业务流程开发者** | 设计和实现业务流程 | 流程的合理性、可执行性、效率 |
| **信息系统开发者** | 设计和实现信息系统 | 系统的可实现性、性能、可维护性 |

> 本准则同时服务于上述三类 EOS 开发者。

#### 流水线工具组件

系统设计流水线（设计线）的产品架构构件，就是 EOS 开发者在系统设计阶段所使用的工具和规范：

| 工具组件 | 说明 | 位置 |
|---------|------|------|
| **设计准则**（本文档） | 定义系统设计的通用方法论和规则 | `01-eos-system-design-specification/` |
| **设计指南** | 为不同设计场景提供操作指导 | `02-eos-system-design-guidelines/` |
| **AI 辅助文档** | 辅助 AI 参与 EOS 系统设计的提示词、检查清单和范例 | `03-eos-system-design-ai-support/` |
| **任务定义** | 四场景中各步骤的操作规程和输入输出定义 | `04-eos-system-design-tasks/` |

### 1.2 设计场景

EOS 的系统设计可按以下四种设计方法场景推进。设计方向取决于 EOS 产品当前所处的阶段：

| 方法 | 设计方向 | 起点 | 终点 |
|------|---------|------|------|
| **瀑布式** | 正向完整：OR → SR → BA → SysReq → PA | 原始需求 | 产品架构 |
| **敏捷式** | 正向增量：分析影响 → 增量更新 | 现有系统设计 | 更新的系统设计 |
| **逆向工程** | 反向推导：PA 及构件 → 反向推导至 OR | 已有构件 | 完整系统设计 |
| **DevOps** | 最小正向变更：定位 → 修复 → 验证 | 生产环境问题 | 快速修复/发布 |

**选择决策**：

```
EOS 产品处于什么阶段？

┌─ 已开发完成，但文档不全？
│  └─ 方法：逆向工程
│     └─ 从已有构件反向推导出完整系统设计文档
│
├─ 只有原始需求，需要从零开始？
│  └─ 方法：瀑布式
│     └─ 逐层正向推导（OR → SR → BA → SysReq → PA）
│
├─ 已有完整系统设计，需要增加新需求或修改现有构件？
│  └─ 方法：敏捷式
│     └─ 分析影响范围 → 增量更新受影响的文档
│
└─ 产品已上线运行，需要快速修复？
   └─ 方法：DevOps
      └─ 快速评估影响 → 最小化变更 → 快速验证
```

**各方法风险特征**：

| 方法 | 主要风险 | 应对策略 |
|------|---------|---------|
| **瀑布式** | 需求不清晰导致后期返工 | 在 OR 层做充分的需求分析和冲突检测 |
| **敏捷式** | 增量范围扩散超出迭代边界 | 严格控制每次迭代的影响范围分析 |
| **逆向工程** | 反向推导中对原始意图的理解偏差 | 多方确认推导结果，保留推导依据 |
| **DevOps** | 快速修复引入副作用 | 最小化变更范围，变更前后做回归验证 |

> 通用方法框架详见[通用设计指南](../../../00-general/10-general-system-design-standards/02-general-system-design-4modes-guide.md)。

---

## 二、EOS 的特有设计框架

本章定义 EOS 系统设计的核心框架——这些内容是通用准则中未覆盖的、EOS 产品特有的设计规则。

### 2.1 三层工作层面

系统设计流水线（设计线）和 EOS 之间的关系由 **三个工作层面** 构成：

| 层面 | 内容 | 负责方 | 仓库位置 |
|------|------|--------|---------|
| **第1层：流水线设计** | 基于通用方法论，使用元流水线对 EOS 流水线的原始需求进行系统设计，产出其产品架构 | 流水线开发者 | `10-meta-pipeline/` |
| **第2层：构件开发** | 将 EOS 流水线的 PA 构件（准则、指南、AI 辅助、任务定义）开发出来，交付至 EOS 设计空间中供使用 | 流水线开发者 | `20-eos-design/.../01~04-*` |
| **第3层：使用工具设计 EOS** | EOS 开发者使用第2层产出的工具对 EOS 进行系统设计，产出 EOS 的产品数据 | EOS 开发者 | `20-eos-design/.../10-product-data/` |

**EOS 开发者视角**：你的工作对应第3层——使用流水线交付的工具（准则、指南、AI 辅助文档、任务定义）来设计 EOS。你的产出是产品数据文档（`10-product-data/`），而非工具本身。

#### 内嵌关系

运营体系涵盖市场、研发、生产、售后等业务，同时也包含"设计运营体系本身"这项业务。系统设计流水线就是支撑这项业务的方法和工具集。

```
运营体系
├── 市场
├── 研发
├── 生产
├── 售后
└── 设计运营体系本身 ← 系统设计流水线提供方法支撑
      ├── 系统设计方法论
      └── 设计准则、指南、AI辅助、任务定义（流水线的 PA 构件）
```

流水线的四类构件被放置在 `20-eos-design/10-eos-system-design/` 目录中，作为 EOS 开发者设计 EOS 时的操作规程。

#### 构件交付目录映射

| 流水线构件类型 | → 交付为 EOS 系统设计目录下的 | 说明 |
|----------------|---------------------------|------|
| **设计准则** | `01-eos-system-design-specification/` | 交付给 EOS 开发者使用的设计规范 |
| **设计指南** | `02-eos-system-design-guidelines/` | 交付给 EOS 开发者使用的操作指南 |
| **AI 辅助文档** | `03-eos-system-design-ai-support/` | 交付给 EOS 开发者使用的设计辅助工具 |
| **任务定义** | `04-eos-system-design-tasks/` | 交付给 EOS 开发者使用的操作规程 |
| **（非构件）** | `10-eos-system-product-data/` | **输出物**——EOS 开发者使用上述工具后产出的设计内容 |

> **一句话理解**：流水线建造了"设计工具箱"交付给 EOS 团队（目录 01/02/03/04），EOS 团队用这些工具去设计 EOS，产出的设计文档放入 `10-product-data`。

---

### 2.2 EOS 运营体系中的端到端业务框架

#### 2.2.1 端到端的定义

**从需求提出到需求满足**。

#### 2.2.2 三类客户与三个业务维度

企业有三类客户，每类客户的需求构成一个业务维度：

| 客户类型 | 提出的需求 | 对应的业务维度 |
|---------|-----------|--------------|
| 产品用户/客户 | 产品订货合同、产品研发需求、售后服务需求 | **主价值链** |
| 上级/监管方 | 经营管理目标、合规及资质取证、年度审核 | **管理监管** |
| 内部员工 | 对人、资金、物料、设备、设施、工具、数据、知识资产等需求 | **支持服务** |

三个维度不是层级拆分，而是同一企业业务的三个视角。同一项目同时受三个维度作用——归属于某个维度，被其他维度管理，向其他维度提需求。

#### 2.2.3 项目间接口原则

需求在端到端项目A的具体业务运行中产生，接收方是端到端项目B整体（BA 2级），B接收后自主决定内部如何分解响应，A不需要了解B的内部结构。三种接口关系：

| 关系 | 本质 | 适用范围 |
|-----|------|---------|
| 上下游 | 链式传递 | 主价值链内部 |
| 管理/被管理 | 下达要求、监督考核 | 管理监管 → 其他维度 |
| 支持/被支持 | 提出需求、统筹满足 | 支持服务 → 其他维度 |

#### 2.2.4 端到端项目业务分解方法

端到端项目的业务分解从**被实现对象的系统架构**中工程化推导而来：

```
端到端项目（BA 2级）
├── 价值业务 ← 由项目的"产品"决定
│     └── 项目产出什么（广义产品/服务）
│           └── 该产品的系统架构 → 架构节点类型
│                 └── 每个节点类型 → 一个从需求提出到需求满足的端到端业务
└── 管理业务 ← 项目本身的管控活动（目标、进度、成本、质量等）
```

##### BA 层级结构

| 层级 | 名称 | 说明 |
|------|------|------|
| 0级 | 架构根节点 | 整个系统的顶层根节点（唯一） |
| 1级 | 业务域 | 按业务领域划分 |
| 2级 | 端到端项目 | 从需求提出到需求满足的端到端项目 |
| 3级 | 项目业务对象的架构节点类型端到端业务 | 由产品架构节点类型推导得出 |
| 4级 | 工作包（WBS） | 业务对象的工作分解结构 |
| **5级** | **业务组件（承接层）** | **末级节点，使用 IPO 模型完整定义** |

##### 域间需求原则

端到端业务天然跨越多个业务域，但每个域只对自己域内的业务负责。跨域协作通过**需求—响应契约**来定义：

```
域A（端到端拥有者）
  ├── 定义：域A内部协调自己的端到端流程
  ├── 当需要域B支持时：向域B提出明确需求
  │     ↓
  └── 域B在自己的BA中响应该需求
        ├── 在自己的架构中安排能力（IPO）来满足域A的需求
        └── 定义服务范围、SLA和接口契约
```

#### 2.2.5 产品开发流水线的设计逻辑

**上下文**：EOS 的产品开发业务域中包含一类特殊的端到端项目——**产品开发流水线**。它不是运营体系中的一条业务执行流程，而是一条**用于工程化开发目标产品**的业务流水线。该流水线以通用准则 §4.1 定义的产品流水线开发逻辑为方法论基础，按五层系统设计方法进行设计。

**与目标产品的关系**：

```
目标产品（如机载设备）
  ↑ 开发产生
产品开发流水线（BA 2级端到端项目，属于产品开发业务域）
  ↑ 系统设计产生
EOS 运营体系
```

**产品流水线的开发逻辑（实例化——以机载设备为例）**：

通用准则 [§4.1](../../../00-general/10-general-system-design-standards/01-general-system-design-standards.md#41-产品流水线的开发逻辑) 定义的 8 步链路，实例化到机载设备产品开发流水线的设计过程如下：

| 步 | 通用准则 §4.1 | 实例化：机载设备产品开发流水线 |
|----|---------------|------------------------------|
| **①** | 识别目标产品的 PA 及节点类型 | 识别**机载设备**的产品架构——飞控系统、导航系统、通信系统、显示系统、任务系统等 |
| **②** | 对每类节点，定义使用流水线开发该节点类型的业务过程——即流水线的 BA（作业指南） | 对机载设备的每个子系统类型，定义使用产品开发流水线进行开发的过程——即**机载设备产品开发流水线的 BA**，以 IPO 描述每个开发步骤：需求分析 IPO、概要设计 IPO、详细设计 IPO、实现与验证 IPO 等 |
| **③** | BA IPO = 流水线的功能需求 | 这些 IPO 描述了产品开发流水线接收输入（如机载设备需求）、处理（如设计、仿真、验证）、产出（如设计方案、验证报告）——它们就是**产品开发流水线的功能需求** |
| **④** | 功能需求 → 流水线的 SysReq | 产品开发流水线的 SysReq 以"接收→转换→生成"格式描述流水线的信息处理功能 |
| **⑤** | SysReq → 流水线的 PA 构件 | 产品开发流水线的 SysReq 分配给其 PA 构件（准则/指南/AI辅助/任务定义——知识/方法类产物） |
| **⑥** | 各 PA 构件承接构件需求 | 例如"机载设备设计准则"承接对应的 SysReq 条目 |
| **⑦** | 流水线的 PA 构件被开发出来 | 机载设备产品开发流水线的全部工具就绪 |
| **⑧** | 流水线可供使用 | 机载设备开发团队可使用该流水线工程化地开发机载设备 |

**流程简图**：

```
机载设备产品架构节点类型（飞控/导航/通信/显示/任务等）
  → (前置) 产品开发流水线的 OR → 产品开发流水线的 SR
    → 产品开发流水线的 BA（作业指南：如何使用流水线开发该类节点）
      → BA IPO = 产品开发流水线的功能需求
        → 产品开发流水线的 SysReq
          → 产品开发流水线的 PA 构件（准则/指南/AI辅助/任务定义）
            → PA 构件被开发出来
              → 产品开发流水线可供使用
                → 机载设备开发团队使用它工程化地开发机载设备
```

> **端到端项目分类**：产品开发流水线属于项目业务对象为"方法/工具"的端到端项目（产出为方法工具集），区别于项目业务对象为"产品或服务"的端到端项目（产出为具体的业务成果）。两者的业务分解方法均遵循 §2.2.4 的定义，但"产品"的性质不同决定了 BA IPO 的形态不同。

---

### 2.3 PA 节点定义原则

**上下文与问题**：在系统设计链路中，SysReq 条目需要分配到 PA 架构节点，形成各构件的构件需求。分配决策不仅要确定每条 SysReq 属于哪类构件，还需判断同类内的多条 SysReq 是否应合并或拆分。

决策按以下次序执行：首先进行资产优先检查（§2.3.1），再基于三因素（§2.3.2~§2.3.4）判断。

#### 2.3.1 资产优先检查

**先检查组织已有组件资产**，遵循复用→改进→新增优先级（详见通用准则 §2.4.2）。EOS 的资产形态以已有软件模块、平台服务、可复用组件为主：

| 优先级 | 策略 | 适用条件 | 操作 |
|--------|------|---------|------|
| 最高 | **复用** | 现有构件完全满足需要 | 直接使用，无需新分配 |
| 次之 | **改进** | 现有构件可扩展/拆分/合并后满足 | 调整现有构件后分配 |
| 最低 | **新增** | 无合适现有构件 | 创建新构件，用三因素判定边界 |

#### 2.3.2 因素一：信息模型内聚性

两个业务活动如果**操作同一组核心实体**（共享主数据、流程天然连贯），倾向合并为同一构件：

```
强内聚 → 可共享：
  客户信息 ←→ 销售订单 ←→ 售后服务
  → 共享客户主数据，流程天然连贯

弱内聚 → 倾向分离：
  设计 BOM ←→ 生产 BOM ←→ 维修 BOM
  → 虽都叫"BOM"，管理目的、变更节奏、精度要求不同
  → 强行合并导致数据模型被最低公分母拖累
```

**原则**：操作同一核心实体 → 合并；实体不同但有关联 → 通过接口集成。

#### 2.3.3 因素二：NFR Profile 相容性

不同业务活动对非功能需求的要求可能截然不同。当 NFR Profile 冲突时，它们**不应共享同一构件**：

| 维度 | 生产执行（MES） | 经营分析（BI） |
|------|---------------|---------------|
| 响应时间 | 毫秒级（产线节拍） | 分钟级可接受 |
| 可用性 | 99.99%（停机即停产） | 99.9% |
| 数据量 | 实时高频小数据 | 批量大吞吐 |
| 安全要求 | 操作权限精细 | 只读+脱敏 |

这两个活动的 NFR Profile 相互冲突——一个要求实时低延迟，一个要求批量大吞吐。强行合并会导致两头不靠。

> **NFR 走平行路径（SR-NFR → SysReq-NFR → PA）的根本意义在此**：NFR 差异本身就是 PA 分解的依据，而非仅在 SysReq 层做约束分析。

#### 2.3.4 因素三：信息处理模式一致性

不同业务活动消费和生产信息的方式存在本质差异（详见 §2.4）。在此作为 PA 分解的判断依据：

| IPO 组合 | 可否共享？ | 原因 |
|---------|-----------|------|
| 同模式（前9类） | 倾向共享 | 跨领域通用，NFR 通常相容 |
| 同模式（后4类）+ 同领域 | 倾向共享 | 同一专业领域 |
| 同模式（后4类）+ 不同领域 | 分离 | 领域不同则数据模型不同 |
| 不同模式 | 倾向分离 | 信息处理逻辑不同 |

#### 2.3.5 三因素决策矩阵

| 信息模型 | NFR Profile | 信息模式 | 结论 |
|---------|------------|---------|------|
| 相同 | 相容 | 相同 | **合并**为同一构件 |
| 相同 | 冲突 | — | **分离**（NFR 优先） |
| 不同 | 相容 | 相同 | 看耦合度，接口集成 |
| 不同 | 冲突 | 不同 | **必然分离** |

> **核心认知**：PA 层的构件分解是对 BA 层业务边界的读取而非发明。架构设计的质量取决于能否读出这些自然边界。

---

### 2.4 参考：业务信息属性

#### 2.4.1 产品类型与产品架构形态

同一套五层方法论可应用于不同产品类型，但 PA 层的产物形态取决于**产品类型**：

| 产品类型 | 特点 | PA 构件的本质 | 示例 |
|---------|------|-------------|------|
| **知识/方法类** | 用户是人+AI，消费方式是阅读和理解 | 知识制品（准则、指南、指令） | 元流水线 |
| **软件类** | 用户是人+机器，消费方式是操作和运行 | 软件模块（前端、后端、数据） | EOS |
| **实物类** | 用户是使用者，消费方式是物理交互 | 物理组件（零件、装配件） | 硬件产品 |

**EOS 为软件类产品**，其 PA 构件为软件模块。

#### 2.4.2 业务活动的信息处理模式

企业运营体系包含多种业务活动，其本质差异在于处理信息的方式不同。这些差异不是技术选择，而是业务自身的信息属性决定的。

**信息处理模式十三类**：

前9类为**跨领域通用**的处理模式：

| 模式 | 核心特征 | 典型系统 |
|------|---------|---------|
| **计划事务** | 基于约束条件生成资源/时间分配方案 | 计划系统 |
| **工单事务** | 创建→派发→执行→反馈→关闭的闭环处理 | 工单系统 |
| **审批流程** | 按规则链路由、汇集决策意见、形成审批结论 | 流程引擎 |
| **合规事务** | 对照规则库检查行为/数据、判定合规性、记录证据链 | 合规系统 |
| **规范知识** | 结构化编撰准则/规程/指南，规范人员操作或AI执行 | 文档/AI辅助系统 |
| **指标看板** | 从多源数据聚合指标并以可视化形式呈现 | 看板/BI系统 |
| **进展追踪** | 跟踪状态变更、比对计划与实际、标识偏差 | 追踪系统 |
| **信息台账** | 对信息进行登记、维护、检索和状态管理 | 台账系统 |
| **xBOM** | 管理产品结构及配置关系，追踪物料清单的版本和变更 | BOM管理系统 |

后4类为**领域特有**的处理模式：

| 模式 | 核心特征 | 典型系统 |
|------|---------|---------|
| **设备IO** | 与物理设备进行实时数据交换，采集和控制IO信号 | 设备控制/SCADA |
| **领域分析** | 特定领域公式/模型驱动的计算分析 | 专业分析工具 |
| **领域建模** | 构建领域模型、运行仿真场景、输出预测结果 | 建模仿真平台 |
| **领域算法** | 封装特定领域逻辑的算法执行 | 算法引擎 |

**核心规律**：判断两个业务活动是否共享系统——**不同模式 → 分离；同模式 → 可共享**。后4类即使模式相同，如果领域不同，仍然需要分离。

---

## 三、通用方法论精要

本章以极简篇幅摘要列出通用准则的核心方法论，并标注其在 EOS 语境下的关注点。完整定义请直接查阅通用准则对应章节。

> **重要**：以下各节均非通用准则的替代品，而是帮助你快速定位到所需的通用方法内容。初次接触 EOS 系统设计前，**建议先通读通用准则 §一~§七**。

### 3.1 五层结构（通用 §四）

EOS 的系统设计遵循标准的五层设计链路，各层在 EOS 语境下的关注点如下：

| 层级 | 在 EOS 中的关注点 |
|------|----------------|
| **OR → SR** | 原始需求从哪些渠道来（运营管理部门、业务部门、信息系统部门等）、如何规范化为相关方需求 |
| **SR → BA** | 从 EOS 的功能需求中识别业务活动边界，按三类客户/三个业务维度组织 BA IPO |
| **BA → SysReq** | 将 BA IPO 去重后按系统功能分类重组——注意 EOS 软件产品的功能模块划分 |
| **SysReq → PA** | 按 PA 节点定义原则（§2.3）进行构件划分，得到软件构件的完整清单（前端/后端/数据/基础设施） |

> 完整五层结构定义见[通用准则 §四](../../../00-general/10-general-system-design-standards/01-general-system-design-standards.md#四系统设计链路)，三层核心规则见[通用准则 §三](../../../00-general/10-general-system-design-standards/01-general-system-design-standards.md#三文档结对设计)。

### 3.2 文档结对设计（通用 §三）

EOS 的系统设计过程包含四个依次衔接的文档结对：

| 序号 | 结对 | 在 EOS 中的映射 |
|------|------|---------------|
| 1 | OR 条目级需求 ↔ SR 架构定义 | EOS 原始需求 → 相关方需求架构 |
| 2 | SR 详细定义末级 ↔ BA 架构（IPO） | 相关方需求 → 业务组件（IPO清单） |
| 3 | BA IPO（去重后）↔ SysReq 架构定义 | IPO → 系统功能模块 |
| 4 | SysReq 详细定义末级 ↔ PA 架构定义 | 系统功能 → 软件构件 |

**结对设计原则**：上层详细定义与下层架构定义应同步设计而非串行执行——先快速约定下层架构的结构骨架，再并行填充内容。

> 需求分解/分配/分析三个核心设计活动的完整操作规则见[通用准则 §三](../../../00-general/10-general-system-design-standards/01-general-system-design-standards.md#三文档结对设计)。编号规则见[通用准则 §3.4](../../../00-general/10-general-system-design-standards/01-general-system-design-standards.md#34-编号规则)。

### 3.3 组织资产优先（通用 §2.4）

EOS 各层的架构节点不是凭空产生的，而是基于组织现有资产按 **复用 → 改进 → 新增** 优先级生成。

在 EOS 语境下，各层的资产形态：

| 层级 | 资产形态 |
|------|---------|
| **SR 层** | 需求分类体系、领域知识结构、行业标准目录、法律法规框架 |
| **BA 层** | 已有业务过程、岗位职责分工、跨系统交互流程 |
| **SysReq 层** | 系统边界划分、接口协议、技术域约定、技术标准规范 |
| **PA 层** | 已有软件模块、平台服务、可复用组件、中间件选型 |

> 资产优先级决策规则及操作细则见[通用准则 §2.4](../../../00-general/10-general-system-design-standards/01-general-system-design-standards.md#24-基于资产定义架构)。

### 3.4 系统设计过程（通用 §五）

EOS 的系统设计遵循通用准则定义的**六步法**：

| 步骤 | 名称 | 在 EOS 中的映射 |
|------|------|---------------|
| Step 1 | 原始需求分析及相关方需求架构定义 | 收集 EOS 原始需求，建立 SR 架构 |
| Step 2 | 相关方需求详细定义及业务架构定义 | 展开 SR 详细定义，识别 EOS 业务活动为 IPO |
| Step 3 | 系统需求功能部分架构定义 | BA IPO 去重后按 EOS 功能模块组织 |
| Step 4 | 非功能需求详细定义及系统需求非功能架构 | SR-NFR 平行路径，不经过 BA |
| Step 5 | 系统需求详细定义及产品架构定义 | 按 PA 节点定义原则划分 EOS 软件构件 |
| Step 6 | 双向追溯验证 | 验证 OR → SR → BA → SysReq → PA 完整追溯 |

> 六步法的完整定义、每步的检查要点和验收标准见[通用准则 §五](../../../00-general/10-general-system-design-standards/01-general-system-design-standards.md#五系统设计过程)。

### 3.5 验收标准与检查清单（通用 §六、§七）

通用准则定义了六个维度的质量验收标准（§六）和对应的逐项检查方法（§七），是 EOS 系统设计质量评估的通用框架。**本文档 §五 仅补充 EOS 特有的验收项，通用项不再重复**。

> 完整验收标准见[通用准则 §六](../../../00-general/10-general-system-design-standards/01-general-system-design-standards.md#六设计验收标准)，完整检查清单见[通用准则 §七](../../../00-general/10-general-system-design-standards/01-general-system-design-standards.md#七设计检查清单)。

---

## 四、各层设计指引

本章为 EOS 的五层设计提供场景化指导。每层聚焦"在 EOS 语境下如何进行设计活动"，通用方法步骤不再复述。

### 4.1 第1层 OR：原始需求及分析

**EOS 的 OR 特征**：EOS 的原始需求主要来自以下渠道：
- 运营管理部门提出的运营体系建设需求
- 业务部门提出的流程优化和改进需求
- 信息系统部门提出的系统升级和集成需求
- 合规和监管要求触发的适配需求

**设计指引**：

1. **收集**阶段关注企业运营体系的全覆盖——市场、研发、生产、售后、管理等各业务域的需求覆盖面
2. **初步整理**时使用四种方法（血缘归并、角色聚合、流程串联、父子补全）对零散碎片需求进行归并
3. **规范化**时确认每条需求都包含"需求描述 + 性能指标"（不可衡量则不成为需求）
4. **分解到末级**时遵循 1:1 约束——每条子需求须能分配到 SR 唯一一个架构末级节点

**输出**：原始需求详细定义（末级条目清单）

> 通用方法：OR 预处理（碎片整理/规范化/冲突检测）见[通用准则 §5.2 阶段一](../../../00-general/10-general-system-design-standards/01-general-system-design-standards.md#52-第1步or--sr)。

### 4.2 第2层 SR：相关方需求

**SR 架构分类**：EOS 的相关方需求按功能和非功能两个分支组织。

**功能部分**的典型分类：

```
EOS 相关方需求（功能部分）
├── 市场业务域
├── 研发业务域
├── 生产业务域
├── 售后业务域
└── 管理业务域
```

**非功能部分**按[通用准则附录B](../../../00-general/10-general-system-design-standards/01-general-system-design-standards.md#附录b-非功能需求分类体系)的三大类（质量特性/环境适应性/可实现性）组织。

**设计指引**：

1. 功能需求和非功能需求分别标识（`SR-F-` / `SR-NFR-`）
2. 每条 SR 架构末级节点承接 2-5 条 OR 末级条目（N:1），检查合并的合理性
3. 详细定义分解到每条末级条目只需映射到 BA 的一个 IPO

> 通用方法：SR 详细定义分解和 IPO 映射见[通用准则 §5.3](../../../00-general/10-general-system-design-standards/01-general-system-design-standards.md#53-第2步sr功能--ba)。

### 4.3 第3层 BA：业务架构

**EOS 的 BA 层级结构**：

| 层级 | 名称 | 说明 |
|------|------|------|
| 0级 | 架构根节点 | 运营体系 |
| 1级 | 业务域 | 市场、研发、生产、售后、管理等 |
| 2级 | 端到端项目 | 从需求提出到需求满足的端到端项目 |
| 3级 | 项目业务对象的架构节点类型端到端业务 | 由产品架构节点类型推导得出 |
| 4级 | 工作包（WBS） | 业务对象的工作分解结构 |
| **5级** | **业务组件（承接层）** | **使用 IPO 模型完整定义** |

**BA 的本质**：EOS 的业务架构是 EOS 的运行概念（Operational Concept）——描述用户如何使用 EOS 产品开展业务。BA 中的每个 IPO 描述的是 EOS 上的一个业务处理步骤。

**IPO 识别方法**：从 SR 详细定义集合中识别业务活动边界，以"系统接收→转换→产出"为框架描述每个 IPO。例如，从 SR 中的"销售订单处理"需求推导出 BA IPO：

```
BA-001: 销售订单处理（IPO名称）
├─ 系统接收：客户订单信息（含产品、数量、交期、价格）
├─ 系统转换：订单校验 → 库存分配 → 价格计算 → 订单确认
└─ 系统产出：已确认的销售订单（含交期承诺）
```

**IPO 去重**：同一 IPO 可能被多条 SR 详细定义映射——将重复出现的 IPO 归并为唯一条目，形成去重后的 IPO 清单。

> 通用方法：BA 的独立存在特性和 IPO 映射规则见[通用准则 §5.3](../../../00-general/10-general-system-design-standards/01-general-system-design-standards.md#53-第2步sr功能--ba)。

### 4.4 第4层 SysReq：系统需求

**SysReq 的性质**：EOS 作为软件类产品，其系统需求描述的是**系统的信息处理功能**——每项 SysReq 是分配给某个 PA 构件的一条功能需求。

**SysReq 架构层级**（功能需求 0-9 级）：

```
5级：组件实例 ←── 承接 BA 5级（N:1，架构末级）
6级：功能模块（Tab/页面）
7级：系统用例（按钮/菜单）
8级：用例场景（正常/异常/替代）
9级：场景活动 ──→ 分配到 PA 末级（1:1，详细定义末级）
```

**非功能需求**使用**0-6级**平行架构：根节点 → 需求类别（三大类）→ 需求类型 → 需求子类型 → 需求细分 → **5级承接层（架构末级）** → 6级验证标准。

**双重来源**：

| 来源 | 说明 | 处理方式 |
|------|------|---------|
| **功能需求** | BA IPO 去重清单 | IPO 去重后按功能模块组织，每项 IPO 对应一条 SysReq 功能条目 |
| **非功能需求** | SR-NFR（平行路径，不经过 BA） | 按质量特性/环境适应性/可实现性三大类组织，形成 0-6 级架构 |

**SysReq 功能部分示例**：

| PA 构件类型 | 所承接的 SysReq 条目 |
|------------|---------------------|
| 前端组件 | 应接收用户输入的操作指令，经过界面交互逻辑处理（表单校验/状态更新/路由跳转），生成对应的界面展示或请求数据 |
| 后端组件 | 应接收 API 请求和业务数据，经过业务规则引擎处理（校验/计算/编排/持久化），生成处理结果或响应数据 |
| 数据层 | 应接收数据读写请求，经过数据存取处理（查询/写入/事务/缓存），生成查询结果或写入确认 |
| 基础设施 | 应接收系统运行监控请求，经过监控指标处理（采集/聚合/告警/日志），生成监控视图或告警通知 |

**设计指引**：

1. 功能部分从 BA IPO 去重清单出发，每项 IPO 编写一条 SysReq
2. 每条 SysReq 对应于一个 PA 构件类型，功能需求的完整定义即决定了该构件应包含的功能
3. 非功能部分直接从 SR-NFR 承接，不经过 BA 层

> 通用方法：SysReq 的分解过程和功能/非功能分配路径见[通用准则 §5.4~§5.5](../../../00-general/10-general-system-design-standards/01-general-system-design-standards.md#54-第3步ba--sysreq功能架构)。

### 4.5 第5层 PA：产品架构

**EOS 的构件分类体系**（软件类产品的 PA 构件）：

| 类型 | 子类型 | 说明 |
|------|--------|------|
| **前端组件** | 页面组件、功能组件、交互组件 | 用户界面 |
| **后端组件** | API服务、业务引擎、数据处理、消息处理 | 业务逻辑 |
| **数据层** | 关系型数据库、NoSQL、文件存储、消息队列 | 数据存储 |
| **基础设施** | API网关、认证授权、监控告警、日志管理、CI/CD | 支撑能力 |

**构件划分原则**：每个构件应有明确的职责边界。对 EOS（软件产品），粒度标准为"一个团队可在 1-2 周内完成开发"且"可独立部署和测试"。

**SysReq → PA 的映射**：每个 PA 架构节点承接若干 SysReq 条目。对于 EOS，这些 SysReq 条目描述系统的信息处理功能（见 §4.4）。

**PA 节点定义方法**：

1. 从 SysReq 9 级场景活动出发，按 §2.3 的 PA 节点定义原则判断哪些活动应合并为同一构件
2. 按复用→改进→新增的资产优先顺序（通用准则 §2.4）处理已有构件
3. 每个构件标注四要素：定义、所映射的 SysReq 末级、符合性分析、实现策略

**举例**：EOS 设计线 PA 构件的划分（四类各一个示例节点）：

```
PA-001: 客户管理页面（前端组件）
├─ 定义：客户信息的增删改查界面
├─ 交付物形态：前端页面组件
├─ 承接的 SysReq 末级：SysReq-001.01 ~ SysReq-001.03
├─ 符合性分析：[如何满足所承接的需求]
└─ 实现策略：基于 React 实现，复用现有 UI 组件库

PA-002: 订单服务（后端组件）
├─ 定义：订单创建、修改、查询、状态管理的业务逻辑
├─ 交付物形态：API 服务
├─ 承接的 SysReq 末级：SysReq-002.01 ~ SysReq-002.04
├─ 符合性分析：[如何满足所承接的需求]
└─ 实现策略：微服务架构，与库存服务通过消息队列集成

PA-003: 生产数据库（数据层）
├─ 定义：生产订单、BOM、工艺路线等核心数据的持久化存储
├─ 交付物形态：关系型数据库实例
├─ 承接的 SysReq 末级：SysReq-003.01 ~ SysReq-003.02
├─ 符合性分析：[如何满足所承接的需求]
└─ 实现策略：PostgreSQL，按业务域分库设计

PA-004: API 网关（基础设施）
├─ 定义：统一 API 入口，提供路由、认证、限流等功能
├─ 交付物形态：网关服务
├─ 承接的 SysReq 末级：SysReq-004.01 ~ SysReq-004.02
├─ 符合性分析：[如何满足所承接的需求]
└─ 实现策略：基于 Kong 实现，与认证服务集成
```

> 通用方法：PA 的完整设计和 SysReq→PA 的映射规则见[通用准则 §5.6](../../../00-general/10-general-system-design-standards/01-general-system-design-standards.md#56-第5步sysreq--pa)。

---

## 五、EOS 验收标准

以下仅列出 EOS 特有的验收项。通用验收标准（架构定义完整率、详细定义完成率、1:1 约束满足率、映射覆盖率、追溯完整性等）见[通用准则 §六](../../../00-general/10-general-system-design-standards/01-general-system-design-standards.md#六设计验收标准)。

### 5.1 软件构件验收

| 检查项 | 标准 | 说明 |
|--------|------|------|
| **构件分类完整率** | 100% | PA 构件按四类（前端/后端/数据/基础设施）完整划分，无遗漏 |
| **构件交付物明确率** | 100% | 每个构件标注了交付物形态（页面组件/API服务/数据库实例/网关服务等） |
| **构件独立可验收率** | 100% | 每个构件可独立部署和测试，不依赖其他构件的交付物 |

### 5.2 层级结构验收

| 检查项 | 标准 | 说明 |
|--------|------|------|
| **BA 层级完整率** | 100% | BA 从 0 级到 5 级（业务组件/承接层）的完整树形结构 |
| **SysReq 功能层级完整率** | 100% | SysReq 功能部分从 5 级到 9 级（场景活动）的完整分解链路 |
| **SysReq NFR 层级完整率** | 100% | SysReq 非功能部分从 0 级到 6 级（验证标准）的完整分解链路 |

### 5.3 多产品关系验收

| 检查项 | 标准 | 说明 |
|--------|------|------|
| **三层工作层面标记** | 100% | 每个 PA 构件标注服务于第几层工作层面 |
| **端到端框架应用** | 通过评审 | BA 2级端到端项目按三类客户维度（主价值链/管理监管/支持服务）正确划分 |
| **域间接口记录率** | 100% | 跨域协作关系的接口契约已记录在案 |

### 5.4 设计场景匹配

| 检查项 | 标准 | 说明 |
|--------|------|------|
| **场景-方法一致性** | 100% | 当前所采用的设计方法场景与实际设计方向一致（参考 §1.2 的选择决策） |
| **风险记录完整率** | 100% | 所选场景的已知风险已在设计计划中记录应对策略 |

---

## 六、常见错误、FAQ 与规范关系

### 6.1 常见错误

**错误1：混淆架构末级节点和详细定义末级节点的角色**

```
错误做法：
在相关方需求层，将"需求收集"直接作为 SR 架构末级节点，
但"需求收集"其实是 SR 某个功能分类下的一个详细定义条目。

正确做法：
架构末级节点：SR-F-001（需求规范化功能）—— 完整的业务能力
详细定义末级：SR-F-001.01（需求收集）—— 功能下的具体环节
```

**错误2：同步设计退化为串行设计**

```
错误做法：
先完成第2层全部详细定义，再开始第3层架构定义
→ 第2层详细定义完成后才发现 BA 没有对应的 IPO 可承接

正确做法：
第一轮：快速约定各层架构骨架（占位符 + 映射关系）
第二轮：并行填充各层详细内容
```

**错误3：验收标准和检查清单混为一谈**

```
错误做法：
验收标准和检查清单放在一起，列出的全是"是否已检查"类项，
没有量化指标衡量设计质量。

正确做法：
验收标准（量化指标）："映射覆盖率100%，1:1约束满足率100%"
检查清单（操作验证）："[ ] 检查所有需求是否已分解到末级"
```

**错误4：分析活动被遗漏或延后**

```
错误做法：
需求分解、分配和符合性分析在全部设计完成后才做，或者根本没做。

正确做法：
分析活动贯穿设计全过程——每完成一次分配立即执行对应的符合性分析。
```

**错误5：忽略资产优先原则，动辄新增架构节点**

```
错误做法：
新需求到达时，直接在架构中新增架构末级节点来承接，
而不检查现有节点是否能复用或改进。

正确做法：
按复用→改进→新增的优先级依次检查现有节点。
```

**错误6：PA 分解跨越业务边界**

```
错误做法：
将信息处理模式不同的两个业务活动强行合并到同一构件，
或将 NFR Profile 冲突的需求分配到同一构件。

正确做法：
按三因素决策矩阵（§2.3.5）判断构件边界——信息模型不同或 NFR 冲突的需求应分离。
```

### 6.2 FAQ

**Q1：本文档与各层规范（02-06）是什么关系？**

本文档是 EOS 系统设计的**总纲**，定义"为什么这样做"和"整体结构是什么"。各层规范定义"具体怎么做"。在阅读时，建议先读本文档了解全局，再查阅特定层规范。

**Q2：六步法中的步骤是否可以并行执行？**

可以。第2步（功能分支）和第4步（非功能分支）是独立并行的。第2步完成后可立即开始第3步和第5步的功能部分，无需等待第4步。两阶段工作流程是实现并行化的具体方法。

**Q3：通用准则、本准则（EOS 系统设计）、EOS 流水线准则三者的关系？**

三者构成"方法→二次实例→一次实例"的递进链：
- **通用准则**（`00-general/`）：产品无关的方法论核心
- **EOS 流水线准则**（`10-meta-pipeline/.../01-eos-pipeline-system-design-standards.md`）：通用方法论在 EOS 流水线上的实例化
- **本准则**（`20-eos-design/.../01-eos-system-design-standards.md`）：通用方法论在 EOS 产品上的实例化（参考 EOS 流水线准则的结构，内容按 EOS 软件产品定制）

三者共享同一套方法论核心（五层结构、核心规则），各有自己的产品特有规则。

> **理解要点**：本准则与 EOS 流水线准则在方法论层面引用的是同一份通用准则（§一~§七）。之所以参考后者而不是前者的结构，是因为**产品同构性**——本准则和 EOS 流水线准则都是"将通用方法论实例化到具体产品"的产物，只不过实例化目标不同（EOS vs EOS 流水线）。

**Q4：当详细定义无法 1:1 映射到下层架构时，应该调整哪一方？**

先检查详细定义是否还能进一步分解。如果详细定义已到原子级仍然无法 1:1，则说明下层架构设计不合理，需要补充或调整下层架构末级节点。经验法则：先怀疑分解不充分，再怀疑架构不完整。

**Q5：两阶段工作流程和同步设计是什么关系？**

两阶段工作流程是**实现方式**，同步设计是**设计原则**。同步设计要求上下游在设计时对齐，两阶段工作流程提供了具体的操作方法：第一阶段快速建立骨架和映射（对齐结构），第二阶段并行填充内容（对齐细节）。

**Q6：本准则的版本更新策略是什么？**

v2.x 系列定位为"内嵌通用方法论"的独立文档版本，v3.0 及以后定位为"基于通用方法论引用"的专有准则版本。版本号变动意味着准则的结构或定位发生了变化，使用者应关注版本文档头中的定位说明。

**Q7：EOS 的端到端业务框架如何在 BA 层落地？**

端到端项目（BA 2级）按三类客户维度（主价值链/管理监管/支持服务）划分业务边界。每个端到端项目下，价值业务由被实现对象的系统架构节点类型推导（§2.2.4），管理业务独立于价值业务之外单独补充。跨域协作通过需求—响应契约实现，而非跨域吞并业务。

### 6.3 与其他规范的关系

| 本文章节 | 对应文档 | 关系说明 |
|---------|---------|---------|
| §一~§二（产品语境+设计框架） | 全部 02-06 层规范 | 定义 EOS 的产品语境和特有设计框架 |
| §三（方法论精要） | [通用准则](../../../00-general/10-general-system-design-standards/01-general-system-design-standards.md) | 通用方法论核心引用自此处 |
| §四.1 第1层 | [OR 规范](02-eos-original-requirements-specification.md) | 本文定义 OR 在 EOS 语境下的关注点，规范定义详细操作方法 |
| §四.2 第2层 | [SR 规范](03-eos-stakeholder-requirements-specification.md) | 同上 |
| §四.3 第3层 | [BA 规范](04-eos-business-architecture-specification.md) | 同上 |
| §四.4 第4层 | [SysReq 规范](05-eos-system-requirements-specification.md) | 同上 |
| §四.5 第5层 | [PA 规范](06-eos-product-architecture-specification.md) | 同上 |
| §五（验收标准） | 各层规范的验收章节 | 本文定义跨层通用验收项中的 EOS 特有项，层规范定义本层专用验收项 |
| §六.1（常见错误） | 各层规范对应章节 | 本文关注跨层通用错误，层规范关注本层特有错误 |
| — | [通用指南](../../../00-general/10-general-system-design-standards/02-general-system-design-4modes-guide.md) | 四种设计方法的框架描述及选择决策，位于通用标准目录 |
| — | [通用术语对照](../../../00-general/10-general-system-design-standards/04-general-terminology-glossary.md) | 产品无关的核心术语英中对照 |
| — | [EOS 术语对照](07-eos-system-design-terminology-glossary.md) | 本准则在产品语境中的专用术语对照 |

**阅读建议**：
- 首次接触时：先读本文档 §一~§二，建立 EOS 的产品语境认知
- 进行具体设计时：先读本文档 §四 的对应章节，再查阅对应层规范的详细操作指导
- 需要通用方法时：查阅[通用准则](../../../00-general/10-general-system-design-standards/01-general-system-design-standards.md)对应章节
- 遇到跨层问题时：回到本文档 §三 或通用准则 §三 寻找通用解决方案

---

**文档版本**：v3.0
**创建日期**：2026-05-25
**最近修订**：2026-05-25
**状态**：完成
