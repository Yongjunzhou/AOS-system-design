---
name: 投资工程研究项目
description: 投资工程研究子项目主记忆——本子项目的定位/内容积累/决策（独立于 AOS 项目记忆，2026-09-19 起存放于 61-投资工程研究/10-claude memory/）
metadata:
  type: project
---

> 本文件是 **61-投资工程研究 子项目** 的独立主记忆（2026-09-19 子项目创建时从零建立），与 AOS 根 `.claude/memory/` 互不干扰。处理本子项目内容时读/写本目录。

**2026-09-19 子项目创建**：用户令「把 61 号目录作为一个子项目」，按 AOS 根 CLAUDE.md §九 配方落地——建子 CLAUDE.md（含隔离协议）＋ 建独立记忆目录 `10-claude memory/` ＋ 主 CLAUDE.md 加指针行（含目录结构一行）。

**当前库存（两份素材）**：`00-投资工程研究报告/00-知行小酒馆E251-你相信的是价值投资还是巴菲特的造富神话.md`（585 行）。内容＝播客「知行小酒馆」E251 对话方言（《第二天：金钱进化论》作者）的**整合版转写稿**，两部分：要点摘要（一句话主旨 + 时间轴表）＋完整逐字稿（Whisper large-v3 / Apple MLX 本地转写，逐句时间戳）。主旨＝价值投资故事在流传中被省略了大量前置条件（本金从哪来、靠什么撑过等待、凭什么说看懂一家公司）。同源 SRT 字幕（3362 条）存于**目录外**本地工作区 `podcast/_原分片备份/`，未入本仓库。

**定位定案（2026-09-19 用户裁）**：本子项目**是产出型**——「这个项目以后准备输出，现在先准备材料」。故工作路径＝**材料准备 → 输出**两阶段：当前处**材料准备期**（广积材料，不求产出），产出形态（研究报告书／方法论提炼／培训材料等）**待后续确定**。子 CLAUDE.md 已据此实写「定位与使命」节，主 CLAUDE.md 指针行同步。

**材料体例定案（2026-09-19 定，沿用首份材料）**：首份材料确立的**整合版**单文件体例写入子 CLAUDE.md「材料体例」节——标题 ＋ 元信息表（播客/主播/嘉宾/时长/链接/转写方式）＋ 转写保真说明注 ＋ 目录 ＋ 第一部分要点摘要（一句话主旨／时间轴表／核心观点／术语校正清单／值得记住的一段）＋ 第二部分完整逐字稿（`**[HH:MM:SS]**` ＋ 段）。**材料保真原则**＝转写稿保留口语原貌（重复、语气词不删改），只清复读幻觉与校正专有名词，且每处校正留痕可回溯。

**编号规则定案（2026-09-19 用户裁）**：`NN-` 是**按素材（批次/期数）顺号**，不是「批内文件序号」——即一份素材一个号，该素材的全部派生件共用同一前缀。首份素材为 `00-`，第二份为 `01-`。此前「按批内序号还是按批次」的遗留疑问就此关闭。

**第二份素材（2026-09-19 入）**：`00-投资工程研究报告/01-张小珺商业访谈录153-和曾鸣聊产业史观.md`（900 行、约 6.6 万字符）。内容＝播客「张小珺Jùn｜商业访谈录」153 期对话曾鸣（战略学家、阿里前总参谋长）的**整合版转写稿**，体例与首份一致（元信息表＋说明注＋目录＋要点摘要〔一句话主旨／17 行时间轴／10 条核心观点／95 词条·145 处校正清单／值得记住的一段〕＋完整逐字稿 346 段）。主旨＝AI 产业化三阶段与「模型公司就是 AI 云公司」，OpenAI／Anthropic 大概率不是原生应用阶段的赢家；范式一变，公司这一工业时代的组织形态会被淘汰，组织先于战略、战略从「规划」变为「生成」。转录方式＝本地 Whisper large-v3 / Apple MLX，未上传云端。

**流水线经验（2026-09-19 第二份素材，可复用）**：本机既有 `~/.workbuddy/binaries/python/envs/mlx`（含 torch 2.14 与 mlx-whisper，模型 `mlx-community/whisper-large-v3-mlx` 已缓存）可直接跑转写；154 分钟音频 16 分块耗时 79.8 分钟（约 1.9x 实时），分块产物落盘可断点续跑。**关键发现：不传 `initial_prompt` 时 Whisper 对本期音频几乎不输出标点**（全稿仅 3 处），加提示词也只到约 1 处/110 字——故改用 `p208p2002/zh-wiki-punctuation-restore`（HF 镜像 `hf-mirror.com` 可取；依赖 `transformers` 用 `pip --target` 装到临时目录，不动本机 env）做逐字标点恢复，再以规则清理「虚词后逗号」「拉丁词前后标点」等误标，得到约 1 处/11 字的标点密度。质检口径：相邻片段间隔 >20s 视为吞内容（本期 0 处），另清 1 处整段样板幻觉与 1 处三连复读。中间产物（音频、分块 JSON、脚本、SRT）留在 `/tmp`，未入本仓库。

**遗留已关闭（2026-09-19 用户裁「做」）**：教材子项目记忆路径的历史漂移已全文清扫，共 9 处由 `50-系统工程研究/记忆/` 更正为磁盘实际目录 `50-系统工程研究/10-claude memory/`——主 CLAUDE.md §九 新建步骤与 §十 工作风格、AOS 根 `.claude/memory/MEMORY.md` 与 `feedback-no-option-interaction.md`、教材子项目 `10-claude memory/` 的 `MEMORY.md` 三处与 `教材项目.md` 两处。另同步清除 50／52 两份子 CLAUDE.md 记忆协议中残留的旧本机 auto-memory 路径（新机制下 auto-memory 即仓库 `.claude/memory/`）。
