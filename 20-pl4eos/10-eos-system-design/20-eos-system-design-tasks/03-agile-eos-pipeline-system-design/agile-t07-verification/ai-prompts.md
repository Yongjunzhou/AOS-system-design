# 敏捷式流程第7步 AI 提示词
**Agile Step 7: AI Prompt Templates — Completeness and Consistency Verification**

**文档版本**：v1.0
**创建日期**：2026-05-28
**对应场景**：03-agile · 第7步
**对应操作规程**：`task-definition.md`（同目录）

---

## 一、跨步骤上下文管理

### 1.1 上下文传递方式

8 步流程中，每一步的产出是下一步的核心输入。AI 在执行每一步前，应确保完整继承了上一步的关键决策：

| 继承内容 | 说明 | 保存位置 |
|---------|------|---------|
| 架构决策 | 合并/拆分、分类方式、层级结构 | 上一步的决策记录 |
| 编号体系 | 所有已分配的 ID 和编号规则 | 各产品数据文件 |
| 待确认项 | 上一步标记的 ⚠️ 项 | 汇总清单 |
| AI推断记录 | AI 自行判断补齐的内容 | 决策记录文件的 AI推断项 |

### 1.2 决策记录机制

每步完成后，AI 将本步的关键决策记录在**本步决策记录模板**（见本章末尾）中，供后续步骤引用。

### 1.3 回溯修正

如果在第 N 步发现需要修改第 M 步（M < N）的产出：
1. 评估影响范围（见 AI决策框架 §7.2 跨步骤影响范围表）
2. 定位到受影响的具体步骤和节点
3. 修改后检查下游映射是否需要同步调整
4. 标注变更记录

---

## 二、AI 经济原则

本步操作以下列顺序执行，AI 需注意：

1. **执行顺序不可逆**：增量验证分析 → 1:1 约束验证 → N:1 承接验证 → 双向追溯验证 → 增量一致性检查 → 版本一致性验证
   核心驱动力：从增量产出出发，逐项验证设计链的完整性、一致性和正确性。验证范围限定在增量部分，不重新检查未受影响的基线内容
2. **基于资产优先**：以本次迭代前的完整基线文档为基准，对比增量产出的正确性。资产文件分区（v1 基线区·只读 / v2 增量区·活跃）用于版本一致性验证
3. **增量设计特点**：本步是设计中唯一的纯验证步骤，不产出新设计内容。所有验证检查限定在增量部分（新增/修改的节点和映射），未受影响的基线内容仅做引用检查

---

## 三、本步提示词模板

### 3.1 本步提示词

详细 AI 提示词模板见：
`30-eos-pipeline-system-design-ai-support/03-agile-eos-pipeline-system-design/workflow-prompts.md` 第7步

请在开始前确认上下文状态：

- 是否已加载 task-01 ~ task-06 的全部增量产出？
- 是否已加载本次迭代前的完整基线文档（5 层 + 映射矩阵）？
- 是否已加载各步的决策记录？
- 增量部分和基线部分的边界是否清晰明确？

具体操作按 task-definition.md 七个阶段执行：增量验证分析 → 逐项约束验证（1:1/N:1/双向追溯/增量一致性/版本一致性）→ 审查。

---

## 四、决策记录模板

```
本步关键决策记录：
├─ 阶段状态：
│  ├─ 增量验证分析：[已完成] — [说明符合性：通过 N 项/未通过 N 项 / 性能符合性：通过 N 项/未通过 N 项]
│  ├─ 1:1 约束验证：[通过/未通过] — [增量部分 N 条映射检查，违反 N 条]
│  ├─ N:1 承接验证：[通过/未通过] — [N:1 不合理节点数]
│  ├─ 双向追溯验证：[通过/未通过] — [增量部分 N 条追溯链]
│  ├─ 增量一致性检查：[通过/未通过] — [遗漏 N 处]
│  └─ 版本一致性验证：[通过/未通过] — [版本不一致 N 处]
├─ 验证结论：
│  ├─ 整体状态：[通过/有条件通过/未通过]
│  ├─ 主要问题：[问题汇总]
│  └─ 修复建议：[建议汇总]
├─ ⚠️ 待确认项：[需要人工确认的问题]
├─ 待传递的约束：[t08 发布步骤需要关注的问题]
└─ 执行模式：[全自动/半自动]
```

---

## 五、审查修复流程

### 5.1 审查输入

AI 完成指定步骤后，输出产物供 rule-conformance-review 审查。审查报告包含：
- 通过/未通过的项目清单
- 问题定位（涉及的具体文件、章节、编号）
- 问题类型（结构性/内容性/格式性）

### 5.2 AI 修复流程

收到审查报告后，AI 按以下优先级处理：

1. **格式性问题**（编号错误、格式不规范）→ 直接修复，不需要人工确认
2. **内容缺失**（某节为空、映射不完整）→ 补充内容，标注修复说明
3. **结构性问题**（粒度不匹配、分类错误）→ 提出修复方案，标注 ⚠️ 待确认
4. **跨步骤一致性问题** → 参考AI决策框架的跨步骤影响范围表进行影响范围评估，列出所有受影响的下游节点

### 5.3 影响范围评估

| 问题定位 | 影响范围 | 修复策略 |
|---------|---------|---------|
| 某步的架构定义 | 本层 + 下游已建立映射的节点 | 修改后同步更新映射 |
| 详细定义 | 下层方案 | 修改后检查下层是否受影响 |
| 映射关系 | 所有关联节点 | 需重新运行追溯验证 |
| 编号变更 | 全链路 | 需逐层更新引用，重新验证 |

---

## 变更日志

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-05-28 | v1.0 | 首次构建 |
