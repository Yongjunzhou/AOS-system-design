# EOS 产品视角业务流程概要
**EOS Product-Perspective Business Process — Framework**

**文档版本**：v1.3
**创建日期**：2026-09-16
**用途**：承载业务流程的 **P 树**——产品视角（业务域 → 产品类型 → 产品构件类型 → 构件开发WBS（包）→ 业务文档 → 任务节点 → 用例场景）。数据分解结构见 [94 号文档](../10-pl4eos-subpl-sysdev/10-wfsysdev-4-eos/94-eos-sys-dev-dbs.md) §2.2；节点 schema 见 [91 规范](../10-pl4eos-subpl-sysdev/10-wfsysdev-4-eos/91-eos-biz-eng-spec.md) §11。
**产出与消费**：由 `biz01abp` 产出（输入 = `02-eos-business-role-process.md`）；本侧止于概要、链上无下游任务，其 P 坐标随末三级的同名视图进入下一对。
**状态**：骨架就位，节点待 `biz01abp` 产出

---

## AI读取文档说明

本文件是树形资产文档。AI 读取时应按需读取，不默认读取全部正文。

默认读取顺序：

1. 读取文档元信息与本说明，确认文档用途、状态和读取规则。
2. 读取 `0. 产品视角业务流程树画像`，建立业务域、产品类型、构件类型、分片与节点规模的整体感知。
3. 读取 `1. 业务流程状态与处理队列`，确定本轮目标节点、活动锁、当前待办 ID 与下游选择记录。
4. 读取 `2. 业务流程节点索引 / 分片索引`，定位目标节点正文块、候选同产品节点块与必要追溯关系。
5. 仅读取目标节点正文块、追溯关系指向的 `02-eos-business-role-process.md` 视图，以及本轮需要写回的反馈区或建议区。

扩大读取范围仅在以下情况允许：

- 节点索引缺失或块 ID 无法定位。
- 追溯关系断裂，无法判断业务文档在两棵树之间的认亲关系。
- 候选节点存在归属冲突、误新建、误引用，或 WBS 归组冲突。
- 需要执行全局一致性检查或批量迁移。
- 人类明确要求通读全文。

禁止事项：

- 不得在常规运行中默认全量读取正文节点块。
- 不得将旧版正文、废弃节点或历史归档作为当前资产参与判断。
- 不得只凭对话上下文推进状态；人类反馈必须先写入节点反馈区、状态队列或转交记录。

---

## 0. 产品视角业务流程树画像

### 0.1 架构统计

| bpd-biz 节点数 | 其中平台侧（P0） | 各业务域 | 待确认方案 | 可以srb设计 | 需biz01修订 | 最近更新 |
|--------------|----------------|---------|------------|------------|------------|----------|
| 0 | 0 | 0 | 0 | 0 | 0 | 2026-09-16 |

### 0.2 分片目录

| 分片ID | 归组维度 | 条目数 | 说明 |
|--------|----------|--------|------|
| 【待定】 | 业务域 / 产品类型 | 0 | 节点按产品类型归组；平台侧走 P0——构件＝引擎，单分支只有配置单元 |

> 【待定】**节点 ID 前缀**：R 侧那份用 `bpd-biz-*`；本份若沿用同一前缀会与它撞号，且两侧的干枝不是同一批节点。前缀定在哪，待 91 §11.5 与节点粒度一并定。

### 0.3 架构树形结构

```text
输出产品业务                              ← 根
└── 业务域 ──────────────────────────── ①
    └── 产品类型 ─────────────────────── ②　部门级的端到端业务输出
        └── 产品构件类型 ─────────────── ③　科室级的端到端业务输出
            └── 构件开发WBS（包）──────── ④　一批数据依赖闭合的产品数据单元
                └── 业务文档 ─────────── ⑤　业务运行结束后应输出的产品数据文档
                    └── 任务节点 ─────── ⑥　枝梢
                        └── 用例场景 ─── ⑦　叶
```

```text
（各业务域 · 待 biz01abp 产出）        0 个节点
```

> **画像列到哪一层**：节点块内的 ⑤⑥⑦ 在正文里列，不另占画像位。
> **实体归属**：末三级（⑤⑥⑦）的**实体由 `02-eos-business-role-process.md` 持有**，本文件带**同名视图**——同一次序、同一个名称，标定的是它在 P 侧的坐标。

---

## 1. 业务流程状态与处理队列

### 1.1 业务流程节点状态队列

| 待办ID | 待办类型 | 节点编号 | 名称 | 业务域 | 当前状态 | 活动锁 | 锁定Skill | 下一步Skill | 人类决策 | AI建议 | 转交记录 | 反馈入口 | 最后更新 |
|--------|----------|---------|------|--------|----------|--------|-----------|-------------|----------|--------|----------|----------|----------|
| — | — | — | — | — | — | 无 | — | — | — | — | — | — | — |

状态取值：

| 状态 | 含义 |
|------|------|
| `待确认方案` | biz01abp 已产出或修订方案，等待人类「整体确认」 |
| `可以srb设计` | 人类已确认 |
| `在srb设计` / `已srb设计` | 下游消费锁定 / 消费完成（消费版本对齐） |
| `待补充srb设计` | 已被下游消费后上游汇入新需求（版本 > 消费版本），待增量补全 |
| `需biz01修订` | 下游检出缺陷退回（读缺失说明修订后再 → `可以srb设计`） |
| `已废弃` | 节点停止使用 |

> 四态主线与上游变更分流引 [91 规范 §A.6](../10-pl4eos-subpl-sysdev/10-wfsysdev-4-eos/91-eos-biz-eng-spec.md#a6-节点生命周期模型)。本文件写入 `待确认方案` / `可以srb设计` / `待补充srb设计` / `需biz01修订`；`在srb设计` / `已srb设计` 由下游写入。

---

## 2. 业务流程节点索引 / 分片索引

### 2.1 业务流程节点索引

| 节点编号 | 名称 | 业务域 | 产品类型 | 产品构件类型 | 当前状态 | 分片 | 块ID |
|---------|------|--------|----------|-------------|----------|------|------|
| — | — | — | — | — | — | — | — |

### 2.2 分片索引

| 分片ID | 归组维度 | 条目数 | 说明 |
|--------|----------|--------|------|
| — | — | — | — |

---

## 3. 规则区 / 口径区

### 3.1 节点口径

- 一个节点 = **一个构件开发WBS（包）**，即一批数据依赖闭合的产品数据单元，由一组按数据依赖链排定的业务文档构成。
- 产品类型（②）＝部门级的端到端业务输出；产品构件类型（③）＝科室级的端到端业务输出。
- 节点内的业务文档 = 本节点的 ⑤ 级，与 `02-eos-business-role-process.md` 里的同名文档**同一次序、同一名称**、实体在那边。

### 3.2 schema 单源与判据落点

- 节点正文 schema 单源 = [91 规范](../10-pl4eos-subpl-sysdev/10-wfsysdev-4-eos/91-eos-biz-eng-spec.md) §11 + 本文件 §4.1 模板。
- 树形与级语义单源 = [94 号文档](../10-pl4eos-subpl-sysdev/10-wfsysdev-4-eos/94-eos-sys-dev-dbs.md) §2.2。
- 产品数据即资产；R 树与 P 树即资产，不另立资产文件。

### 3.3 关联产品数据

| 产品数据 | 关系 |
|---------|------|
| `02-eos-business-role-process.md` 职责视角业务流程 | 本文件的 X；同一批业务文档的另一面。末三级带同名视图，**经认亲互指** |
| `01-eos-specified-requirements.md` 相关方需求 | 末三级的源头；本文件不直接消费，经 `02` 的坐标取用 |
| `05-eos-system-requirement-summary-architecture.md` 系统需求框架 | 本侧的 P 坐标随末三级同名视图进入下一对；本文件不被单独承接 |

---

## 4. 正文节点块

### 4.1 节点模板

> 一个节点 = 一个构件开发WBS（包）的**概要**：P 树干枝（业务域 → 产品类型 → 产品构件类型 → 构件开发WBS（包））+ 该包下的业务文档视图 + 认亲结果 + P 树一致性结论，各级只到**说明**。schema 单源引 91 §11 + 本文件；树形与级语义引 94 号文档 §2.2。

<!-- BLOCK: TEMPLATE -->
#### {节点编号} {WBS 包名称}

**节点类型**：【待定】——随节点 ID 前缀一并定
**块ID**：TEMPLATE

| 块头字段 | 值 |
|---------|-----|
| 业务域 / 产品类型 / 产品构件类型 | 承 X 的「所属产品构件」定位 |
| 当前状态 | 待确认方案 |

| 要素 | 内容 |
|------|------|
| 构件开发WBS（包） | 本包的数据依赖闭合范围；包内文档按数据依赖链排定 |
| **认亲**（逐文档） | R 树的文档逐一标定 P 坐标（所属 产品类型 × 构件类型 × WBS 包 + 层级判定档位）；本体唯一——可被多场景引用，只占一个产品数据位 |
| 业务文档视图（逐文档） | 与 `02` 同名、同一次序；**不重写实体内容**，只写本侧的 P 坐标与依赖 |
| P 树一致性结论 | 三类检查：归属冲突 / 误新建 / 误引用 |
| **缺口推断** | 该产品构件类型应有的产品数据，应有而缺失 → 以**候选节点**挂 P 树、标疑似态；候选只是建议，不直成正式文档 |
| 推理依据与补全说明 | 层级三档判定（引用 / 扩展 / 新建）；标注 `[推断]` |
| 完备性检查结果 | 端到端闭合检查 + P 树一致性三类；缺口走补充业务类原始需求材料（不脑补） |
| 补充材料引用 | （如有） |
| 变更影响声明 / 版本号 | 变更类型 + 下游影响；版本号 |

**逐文档认亲**（对应 X 的 ⑤ 及其下 ⑥ ⑦）

1. **{文档名}**（X 侧同名同一次序）
   - P 坐标：{产品类型} × {产品构件类型} × {构件开发WBS（包）}；判定档位：引用 / 扩展 / 新建
   - 数据依赖：{前序文档 · 依赖什么数据}
   - 层级判定依据：{为什么落在这一级}
2. **…（下一文档）**

**标注纪律**：`[推断]` / `[待下游推断]` / `供人类确认` 定义引 91 §A.3.1；本文件不写 R 侧内容（角色职责 / E2E职责 / 场景职责边界 / 任务节点与用例场景的实体）——那属职责视角那份文档。

##### 反馈区
<!-- 人类在此输入：[同意] / [修改] / [驳回] 及具体意见 -->
<!-- /BLOCK: TEMPLATE -->

### 4.2 既有 P 侧内容迁移区

> 2026-09-16：`02-eos-business-summary-architecture.md`（旧 02）的 9 个治理预置节点上，各有一段 **P 侧内容**（原字段名「P 组装结论」）。该文件退休时把这些内容迁到此，**只保内容、不发明字段**——它们对应本文件哪些节点、落在 P 树的哪一级，待 biz01abp 首轮运行时按 §4.1 模板归位。归位前一律标 `[待归位]`。

| 来源节点 | 既有 P 侧内容 |
|---------|--------------|
| `bpd-biz-GOV-001` 业务架构管理员配置业务菜单 | `[待归位]` WBS 归组：菜单管理治理（数据依赖链：菜单方案 → 可见性 → 看板）；一致性与缺口推断待比对 `[推断]` |
| `bpd-biz-GOV-002` 业务架构管理员维护产品架构 | `[待归位]` 承接对齐判定（Q2/Q3）→ 产品路径扩展治理业务；缺口推断待比对落定 |
| `bpd-biz-GOV-003` 业务架构管理员维护业务资产 | `[待归位]` 承接业务资产的对齐判定（Q2/Q3）；缺口推断待比对落定 |
| `bpd-biz-GOV-004` 业务架构管理员维护文档定义 | `[待归位]` 承接下游写回 04/05/06 的文档定义对齐判定；缺口推断待比对落定 |
| `bpd-biz-GOV-005` 业务架构管理员维护资源资产 | `[待归位]` 承接资源资产的对齐判定；缺口推断待比对落定 |
| `bpd-biz-GOV-006` 业务架构管理员配置指标聚合 | `[待归位]` 承接上层指标方案的对齐判定；缺口推断待比对落定 |
| `bpd-biz-GOV-007` 业务架构管理员维护引擎资产 | `[待归位]` 承接引擎能力缺口线索（FR-ENG，经预处理链后以规范化的相关方需求进入）；缺口推断待比对落定 |
| `bpd-biz-GOV-008` 数据治理员管理数据标准 | `[待归位]` 数据标准作为业务数据流转的基础治理项，随业务域文档定义落位（缺口待比对） |
| `bpd-biz-GOV-009` 数据治理员管理数据质量 | `[待归位]` 质量规则 / 异常工单随业务数据质量保障落位（缺口待比对） |

---

## 5. 追溯关系

| 源节点 | 关系类型 | 目标节点 | 目标文档 | 状态 | 说明 |
|--------|----------|----------|----------|------|------|
| — | — | — | — | — | — |

---

## 6. 反馈区 / 建议区

### 6.1 方案反馈

| 反馈ID | 反馈类型 | 关联节点 | 反馈内容 | 状态 | 最后更新 |
|--------|----------|----------|----------|------|----------|
| — | — | — | — | — | — |

---

## 9. 文档版本与变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-09-17 | v1.2 | 术语更名（人类指示 2026-09-17）——角色履职→职责（本文件引 `02` 处的称谓随之）；历史变更记录保留旧词。 |
| 2026-09-17 | v1.1 | 产出者更正为 `biz01abp`（原误作 `biz01cr`）；状态值 `需biz01cr修订` → `需biz01修订`；下游改述为「P 坐标随末三级同名视图进入下一对，本文件不被单独承接」；§4.1 完整态 → **概要**，去单侧双视角表述 |
| 2026-09-16 | v1.0 | 由 `04-eos-business-detailed.md`（业务流程方案，两视角合装）拆出的 **P 侧**文档；骨架与节点模板按 P 树新写；旧 02 的 9 个治理预置节点上的 P 侧内容迁入 §4.2 迁移区，标 `[待归位]` |
