# EOS wft05-eng NFR 约束包
**NFR Constraint Package (wft05-eng)**

**文档版本**：v1.0
**创建日期**：2026-08-22
**修订日期**：2026-08-22
**状态**：骨架已建立，待 wft04-nfr 装配填充
**资产类型**：跨路径契约文档（nfr → eng）

> **文档定位**：wft04-nfr 装配、wft05-eng 消费的 NFR 约束契约。承载 SysReq-NFR 量化约束的分层装配（七组范围：全局/业务域/srh-biz/页面/CU/PA组件/共享），只引用分层约束表行 ID 和页面 NFR 条目、不复制正文；带包版本/变更声明/消费状态。wft05-eng 逐约束行判定消费目标并映射到 pa-eng 组件，wft06 核对「承接不降级 + 分解预算守恒」。
>
> **装配方**：wft04-nfr（§3.5，七组装配视图）｜**消费方**：wft05-eng（§5.4，逐约束行判定 + 分解处理）+ wft06（§3.6 跨路径验证）+ 构件开发流水线
>
> **版本节奏**：包版本由 wft04-nfr 装配/发布时递增（首版 `待消费`）；消费版本与消费状态由 wft05-eng 消费后回写（本文件消费状态区）。

---

## 一、包头

| 字段 | 值 |
|------|-----|
| 约束包 ID | NFR-PKG-{YYYYMMDD}-{序号} |
| 包版本 | v1 |
| 生成时间 | — |
| 来源 SysReq-NFR ID/版本 | — |
| 上一版本 | — |
| 变更类型 | 首版 / 无变更 / 增量型 / 修订型 / 结构型 |
| 新增行 | — |
| 修订行 | — |
| 删除/失效行 | — |
| 消费目标 | wft05-eng |
| 消费状态 | 待消费 / 已消费 / 需重消费 |
| wft05-eng 已消费版本 | — |

> **消费状态写者约定**：本文件发布时由 wft04-nfr 写 `待消费`；消费完成后由 wft05-eng 回写 `已消费` + 已消费版本（单写者分离，wft04-nfr 不自行置已消费）。

## 二、约束正文区（七组装配视图）

> 只引用分层约束表行 ID 和页面 NFR 条目，不复制正文。分解子指标（wft04-nfr §2.4.1）作为独立约束行装配，标注「分解运算 + 父指标引用」。

### 2.1 全局约束

| 约束行 ID | 适用链路 | 约束对象 | 量化指标引用 | 分解运算/父引用 | 影响级别 | 消费目标 |
|-----------|----------|----------|--------------|----------------|----------|----------|
| NFR-ROW-{seq} | eng / 共享 | EOS 整体 | {SysReq-NFR ID / 指标 ID} | 乘法（父=NFR-ROW-xxx）/ 加法 / 弱链 / 职责分布 / — | 阻断型/非阻断型/待判定 | wft05-eng |

### 2.2 业务域约束

（同 2.1 结构，约束对象 = 业务域名）

### 2.3 srh-biz 约束

（同 2.1 结构，约束对象 = srh-biz ID）

### 2.4 页面级约束

（同 2.1 结构，约束对象 = 七种页面类型之一或具体页面 ID；指标 ID + 约束行 ID）

### 2.5 CU 约束

（同 2.1 结构，约束对象 = 目标引擎 + CU 类型；指标 ID + 约束行 ID）

### 2.6 pa-eng 组件约束

（同 2.1 结构，约束对象 = 组件类型；指标 ID + 约束行 ID；pa-eng 未生成时标 `待wft05映射`，不得虚构 pa-eng ID）

### 2.7 共享约束

（同 2.1 结构）

> **首版发布门禁**（wft04-nfr §3.5）：全局/共享约束可无具体对象 ID；业务域须绑定业务域；bph-biz/bph-eng/CU/页面级须绑定已存在目标 ID；pa-eng 组件级约束只记组件类型与映射条件。存在未满足条件的阻断型约束时，保持 `待目标绑定`，不发布首版。

## 三、消费状态区（wft05-eng 回写）

| 消费轮次 | 消费版本 | 消费状态 | 回写时间 |
|---------|---------|---------|---------|
| — | — | 待消费 | — |

> 消费版本与消费状态由 wft05-eng 每次消费完成后回写（对齐 wft04-nfr §3.5 消费状态回写约定）。

## 四、变更记录

| 日期 | 版本 | 变更摘要 | 操作 Skill |
|------|------|----------|-----------|
| 2026-08-22 | v1.0 | 骨架建立——跨路径契约文档（wft04-nfr 装配 / wft05-eng 消费 / wft06 验证），待装配填充 | eng 链第四轮 |
