# EOS 系统设计状态文档

**用途**：设计链全流程状态追踪的共享接口。AI 通过此表感知上游产出、通知下游，人类通过此表做批量决策（选材、确认进入下游）。本表是材料级和条目级状态追踪的**权威源**——各产品文档头部的内嵌状态表由本表对应子表派生。

**同步契约**：AI 每次写入时必须**先更新本表，再从本表推导写入各产品文档的内嵌状态表**。若检测到两者不一致，以本表为准重建内嵌状态表。

---

## 一、OR 阶段

### 表A — OR原料处理建议及进展

| 记录ID | 内容摘要 | 源文件 | 文本化文档 | 生命周期 | AI建议 | 人类决策 | 操作指引 | 最后更新 |
|--------|---------|--------|-----------|---------|-------|---------|---------|---------|
| ORT-20260628-001 | 平台治理角色原始需求材料：补充业务架构管理员与数据治理员，承接 Bn 业务产品资产树、主数据和业务数据治理的可管理需求 | `10-raw-files/20260628-平台治理角色原始需求材料.md` | — | 已登记 | 建议进入文本化与切分，优先切分为“业务架构管理员”和“数据治理员”两组 FR-BIZ 原料 | — | 人类确认后进入 ort00/ort01 预处理链 | 2026-06-28 |

**粒度**：一行/原始材料（PDF/邮件/纪要等），追踪从入库、文本化、切分方案确认到切分完成的整段旅程。

**生命周期取值**：已登记 → 已文本化 → 切分方案待确认 → 已切分 → 已废弃（含已废弃（重复））

**控制流**：人类设 `人类决策=同意切分` → ort01 为 `已文本化` 材料生成切分方案并写入 `切分方案待确认`；人类确认方案后由 ort01 执行切分并更新为 `已切分`。人类设 `人类决策=已终止` → 废弃处理。

**字段说明**：

| 字段 | 写入者 | 说明 |
|------|--------|------|
| 记录ID | AI（首次登记） | 自动生成的唯一标识，如 ORT-001 |
| 内容摘要 | 各步更新 | 该材料当前处理阶段的一句话摘要 |
| 源文件 | ort00 | 原始文件名（含扩展名） |
| 文本化文档 | ort00（首次）/ ort01（更新） | ort00 写入文本化 `.textualized.md` 文件名；ort01 切分后更新为各切分文件 `.chunk-{seq}.md` 名 |
| 生命周期 | 各步转迁 | 已登记/已文本化/切分方案待确认/已切分/已废弃（含已废弃（重复）） |
| AI建议 | 各步更新 | AI 对当前阶段的判断（如"可以切分"） |
| 人类决策 | 人类 | 空（未处理）/ 同意切分（选入切分）/ 不切分（不入切分）/ 已终止（废弃） |
| 操作指引 | 各步更新 | 人类下一步应执行的操作 |
| 最后更新 | 各步更新 | 格式 YYYY-MM-DD |

---

### 表B — OR切分处理建议及进展

| 源文件 | 子文件 | 内容摘要 | 生命周期 | AI建议 | 人类决策 | last_size | iteration_count | unresolved_count | 操作指引 | 最后更新 |
|--------|--------|---------|---------|-------|---------|----------|----------------|-----------------|---------|---------|

**粒度**：一行/切分子文件（`.chunk-{seq}.md`），追踪从切分创建到规范化完成的子文件预处理全生命周期。

**生命周期取值**：

```
已切分 → 澄清中 → 已澄清 → 待确认规范方案 → 已规范化 → 已废弃
```

| 阶段 | 说明 | 管理步骤 |
|------|------|---------|
| 已切分 | 切分执行完成 | ort01 完成 |
| 澄清中 | AI 已做首轮标注或正在迭代，等待人类反馈 | ort02 迭代中 |
| 已澄清 | 三项条件全满足，澄清完成 | ort02 完成 |
| 待确认规范方案 | AI 已给出规范化方案，等待人类确认 | ort03 等待反馈 |
| 已规范化 | 规范化完成，OR 条目已写入基线 | ort03 完成 |
| 已废弃 | 子文件不再处理 | 任何步骤 |

**控制流**：
- 人类设 `人类决策=同意澄清` → ort02 选材入澄清
- 人类设 `人类决策=同意规范化` → ort03 选材入规范化
- 人类设 `人类决策=已终止` → 废弃处理

**单对字段覆盖机制**：生命周期转迁时，AI 更新 `AI建议` 为针对新阶段的前视建议，同时 `人类决策` **自动清空**，等待人类针对新建议重新决策。

**字段说明**：

| 字段 | 写入者 | 说明 |
|------|--------|------|
| 源文件 | ort01 | 所属原始材料文件名 |
| 子文件 | ort01 | 切分子文件路径（`.chunk-{seq}.md`） |
| 内容摘要 | 各步更新 | 该子文件核心内容一句话摘要 |
| 生命周期 | 各步转迁 | 见上方生命周期取值 |
| AI建议 | 各步更新 | 对当前阶段的前视建议（如"可以澄清""可以规范化"） |
| 人类决策 | 人类 | 空（未处理）/ 同意澄清 / 同意规范化 / 已终止 |
| last_size | ort02/ort03 | 最近处理时的正文区字节数，用于增量检测 |
| iteration_count | ort02 | 澄清迭代轮次计数 |
| unresolved_count | ort02 | 当前未解决的模糊项数量 |
| 操作指引 | 各步更新 | 人类下一步应执行的操作 |
| 最后更新 | 各步更新 | 格式 YYYY-MM-DD |

---

### 表C — OR基线处理建议及进展

| 源文件 | OR编号 | 内容摘要 | 标题 | 类型 | 优先级 | 当前状态 | 人类决策 | AI建议 | 操作指引 | 最后更新 |
|--------|--------|---------|------|------|--------|---------|---------|--------|---------|---------|

**粒度**：一行/OR条目，追踪规范化产出的每条 OR 基线条目从生成、进入 t01 分配、到进入 t06 追溯验证并冻结的全过程。

**状态取值**：待分配 → 待确认方案 → 已分配 → 验证中 → 已验证冻结 → 已废弃

| 状态 | 含义 | 管理步骤 |
|------|------|---------|
| 待分配 | 初写入（ort03 设定初始值） | ort03 写入 → t01 启动时自动扫描 |
| 待确认方案 | t01 已给出 OR→STR 分配方案，等人类确认 | t01（人类审阅） |
| 已分配 | OR→STR 分配方案已通过，等待或进入下游承接 | t01（STR 审阅通过时同步回写） |
| 验证中 | 该 OR 的承接链已进入全链追溯/验证 | t06 |
| 已验证冻结 | 全链验证通过 | t06 批量推进 |
| 已废弃 | OR 条目被废弃 | 本步/下游均可触发 |

**人类决策取值**：**同意分配**→选入当前批次进入 t01 分解分配 | **—**→暂不处理

**控制流**：人类设 `人类决策=同意分配` → AI 在下一轮 t01 中读取处理；t01 首次生成方案后写为 `待确认方案`；相关 STR 节点审阅通过并进入 `待分解分配` 时，对应 OR 同步更新为 `已分配`；t06 选入验证时推进到 `验证中`，验证通过后更新为 `已验证冻结`。

---

## 二、STR 阶段

### 表D — STR架构节点进展状态及处理建议一览表

> **派生视图**：本表是 `02-eos-stakeholder-requirements-architecture.md` 管理区内嵌表D 的派生视图，不直接编辑。AI 在每次 t01 执行后从 02-*.md 管理区同步写入本表，供人类集中查阅和批量选材。如需编辑，请直接操作 02-*.md 管理区中的表D。

| STR编号 | 内容摘要 | 名称 | 角色类型 | 承接OR | 当前状态 | 人类决策 | AI建议 | 操作指引 | 最后更新 |
|--------|---------|------|-------|--------|---------|---------|--------|---------|---------|

**状态取值**：待分解分配 / 待确认方案 / 迭代中 / 已分解分配 / 验证中 / 已验证冻结 / 已废弃

| 状态 | 含义 | 管理步骤 |
|------|------|---------|
| 待分解分配 | t01 审阅通过，STR 方案已定稿，等待人类选入 t02 | t01 |
| 待确认方案 | t01 已生成 STR 方案，等待人类审阅反馈 | t01（人类审阅） |
| 迭代中 | 人类已对当前 STR 方案提出修改意见，等待 t01 再处理 | t01（AI 按反馈调整） |
| 已分解分配 | t02 已实际消费该 STR 节点，并完成 BA 分解分配回写 | t02 |
| 验证中 | 该 STR 所在承接链已进入 t06 全链追溯/验证 | t06 |
| 已验证冻结 | 全链验证通过，节点冻结 | t06 |
| 已废弃 | STR 节点停止使用 | t01/t02/t06 或人工处置 |

**人类决策取值**：**同意分解分配**→选入当前批次进入 t02 详细定义 | **—**→暂不处理

**控制流**：t01 首次生成方案后写为 `待确认方案`；人类反馈要求修改时写为 `迭代中` 并由 t01 继续处理；t01 审阅通过后，STR 节点进入 `待分解分配`；人类设 `人类决策=同意分解分配` → AI 在下一轮 t02 中读取处理；t02 实际消费后更新为 `已分解分配`；t06 选入验证时推进到 `验证中`，验证通过后更新为 `已验证冻结`。

**状态验证**：状态流转到"已验证冻结"的 STR 节点即表明已通过 t06 全链验证（OR→STR→BA→SysReq→PA 全链路走通）。

---

### 2.2 STR详细节点进展状态及处理建议一览表

| STR详细编号 | 内容摘要 | 名称 | 所属链 | 父节点 | 当前状态 | 人类决策 | AI建议 | 操作指引 | 最后更新 |
|-----------|---------|------|-------|-------|---------|---------|--------|---------|---------|

**状态取值**：待BA承接 / 待确认方案 / 迭代中 / 已BA承接 / 验证中 / 已验证冻结 / 已废弃

**人类决策取值**：**同意详细分解**→选入当前批次进入下游 | **—**→暂不处理

**控制流**：t02 生成 STR 详细节点并等待人类审阅时写为 `待确认方案`；人类确认并完成 BA 承接后更新为 `已BA承接`；t06 选入验证时推进到 `验证中`，验证通过后更新为 `已验证冻结`。

---

## 三、BA 阶段

### 3.1 BA节点进展状态及处理建议一览表

| BA编号 | 内容摘要 | 名称 | 业务域 | 所属链 | 当前状态 | 人类决策 | AI建议 | 操作指引 | 最后更新 |
|--------|---------|------|-------|-------|---------|---------|--------|---------|---------|

**状态取值**：待架构映射 / 待确认方案 / 迭代中 / 已架构映射 / 验证中 / 已验证冻结 / 已废弃

| 状态 | 含义 | 管理步骤 |
|------|------|---------|
| 待架构映射 | t02 审阅通过，BA 方案已定稿，等待人类选入 t03 | t02 |
| 待确认方案 | t02 已生成 BA 节点方案，等待人类审阅反馈 | t02（人类审阅） |
| 迭代中 | 人类已对当前 BA 方案提出修改意见，等待 t02 再处理 | t02（AI 按反馈调整） |
| 已架构映射 | t03 已实际消费该 BA 节点，并完成 SysReq 映射回写 | t03 |
| 验证中 | 该 BA 所在承接链已进入 t06 全链追溯/验证 | t06 |
| 已验证冻结 | 全链验证通过，节点冻结 | t06 |
| 已废弃 | BA 节点停止使用 | t02/t03/t06 或人工处置 |

**人类决策取值**：**同意架构映射**→选入当前批次进入 t03 SysReq 映射 | **—**→暂不处理

**控制流**：t02 首次生成方案后写为 `待确认方案`；人类反馈要求修改时写为 `迭代中` 并由 t02 继续处理；t02 审阅通过后，BA 节点进入 `待架构映射`；人类设 `人类决策=同意架构映射` → AI 在下一轮 t03 中读取处理；t03 实际消费后更新为 `已架构映射`；t06 选入验证时推进到 `验证中`，验证通过后更新为 `已验证冻结`。

---

## 四、SysReq 阶段

### 4.1 SysReq节点进展状态及处理建议一览表

| SysReq编号 | 内容摘要 | 名称 | 层级 | 所属链 | 当前状态 | 人类决策 | AI建议 | 操作指引 | 最后更新 |
|-----------|---------|------|-----|-------|---------|---------|--------|---------|---------|

**状态取值**：待产品架构 / 待确认方案 / 迭代中 / 已产品架构 / 验证中 / 已验证冻结 / 已废弃

**人类决策取值**：**同意产品架构**→选入当前批次进入 t05 PA 映射 | **—**→暂不处理

**控制流**：t03 生成人类审阅通过后的 SysReq 节点写为 `待产品架构`；人类设 `人类决策=同意产品架构` → AI 在下一轮 t05 中读取处理；t05 实际消费后更新为 `已产品架构`；t06 选入验证时推进到 `验证中`，验证通过后更新为 `已验证冻结`。

---

## 五、PA 阶段

### 5.1 PA组件进展状态及处理建议一览表

| PA编号 | 内容摘要 | 名称 | 类型 | 所属链 | 当前状态 | 人类决策 | AI建议 | 操作指引 | 最后更新 |
|--------|---------|------|-----|-------|---------|---------|--------|---------|---------|

**状态取值**：待追溯验证 / 待确认方案 / 迭代中 / 验证中 / 已验证冻结 / 已废弃

| 状态 | 含义 | 管理步骤 |
|------|------|---------|
| 待追溯验证 | t05 审阅通过，PA 方案已定稿，等待人类选入 t06 | t05 |
| 待确认方案 | t05 已生成 PA 组件方案，等待人类审阅反馈 | t05（人类审阅） |
| 迭代中 | 人类已对当前 PA 方案提出修改意见，等待 t05 再处理 | t05（AI 按反馈调整） |
| 验证中 | t06 已实际消费该 PA 组件，并正在执行全链追溯/验证 | t06 |
| 已验证冻结 | 全链验证通过，组件冻结 | t06 |
| 已废弃 | PA 组件停止使用 | t05/t06 或人工处置 |

**人类决策取值**：**同意进入t06**→选入当前批次进入 t06 追溯验证 | **—**→暂不处理

**控制流**：t05 首次生成方案后写为 `待确认方案`；人类反馈要求修改时写为 `迭代中` 并由 t05 继续处理；t05 审阅通过后，PA 组件进入 `待追溯验证`；人类设 `人类决策=同意进入t06` → AI 在下一轮 t06 中读取处理；t06 将本轮组件及其承接链路推进到 `验证中`，验证通过后更新为 `已验证冻结`。
