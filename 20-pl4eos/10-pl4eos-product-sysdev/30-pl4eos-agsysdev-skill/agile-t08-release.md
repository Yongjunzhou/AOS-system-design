# 文档完善和发布
**Skill ID**: agile-t08 · **Step 8: Document Completion and Release**

> **概述**：完善增量更新后的文档，生成变更日志，发布新版本。v2 增量区与 v1 基线区合并为新版本基线。
>
> **输入 → 输出**：task-01~task-06 全部增量产出 + task-07 验证报告 → 更新后的完整五层文档 + 变更日志 + 版本发布说明
>
> **适用场景**：敏捷式第8步（收尾步骤）。处理验证问题闭环、合并增量和基线、生成变更日志和版本发布说明。
>
> **版本**：v1.0 | **修订**：2026-05-28

---

## 一、设计角色

本步在设计链中承担的角色：
- **输入**：task-01 ~ task-06 的全部增量产出，task-07 产出的验证报告
- **最终产出**：更新后的完整五层文档（增量更新版本），变更日志（按层分类的变更明细），版本发布说明

### 与三条工程化设计原则的关系

| 原则 | 在本步中的体现 |
|------|--------------|
| **详概分离** | 本步不涉及新的设计工作，仅做文档完善、问题修复和版本管理。基线区与增量区合并为新版本基线 |
| **文档结对** | 本步验证所有文档结对关系的完整性——验证报告中的问题通过回溯修复后，重新检查受影响的结对关系 |
| **基于资产** | 本次迭代的完整设计文档集为发布资产。v2 增量区与 v1 基线区合并，生成新的 v2 基线（冻结·只读），作为下一次迭代的资产 |

---

## 二、前置条件

| 条件 | 说明 |
|------|------|
| 输入 | task-01 ~ task-06 的全部增量产出，task-07 产出的验证报告（`09-*.md`） |
| 参考 | 通用系统设计规范 §八（设计验收标准）、§九（设计检查清单）；EOS 流水线系统设计规范 §三（通用方法论精要） |

### 文档结对关系

本步执行文档发布和版本管理的收尾工作：

| 结对 | 内容 | 工作方式 |
|------|------|---------|
| 增量内容 ↔ 基线文档 | 合并生成新版本 | 增量标记内容整合进基线，v2 增量区→新的 v2 基线区 |
| 验证报告问题 ↔ 修复后的文档 | 闭环处理 | 根据验证报告问题清单逐项修复（回溯到 t01~t06 对应步骤修复） |
| 变更日志 ↔ 各层文档 | 变更明细记录 | 按层分类汇总，生成变更日志 |

---

## 三、操作流程

五阶段：问题修复 → 变更汇总 → 生成日志 → 发布准备 → 终验。

核心驱动逻辑：先处理验证报告的问题，再汇总变更并生成发布物。确保所有问题闭环后再发布。

操作流程概览：

```
[阶段一] 审查修复               [阶段二] 变更汇总               [阶段三~四] 发布准备
  按验证报告逐项修复              按层分类汇总变更                 生成变更日志
  回溯到对应步骤                   影响分析摘要                    生成版本发布说明
[阶段五] 终验
  人类确认迭代完成
```

### 阶段一：审查修复

| # | 操作 | 执行者 | 说明 |
|---|------|--------|------|
| 1 | 按审查修复流程处理问题 | AI | 根据验证报告的问题清单逐项修复；格式性问题直接修复，结构性问题提修复方案标记 ⚠️ |

### 阶段二：变更汇总

| # | 操作 | 执行者 | 说明 |
|---|------|--------|------|
| 2 | 汇总所有变更 | AI | 按层分类汇总（OR/SR/BA/SysReq/PA 各层），包含变更明细和影响分析摘要 |

### 阶段三：生成日志

| # | 操作 | 执行者 | 说明 |
|---|------|--------|------|
| 3 | 生成变更日志 | AI | 变更明细、影响分析摘要 |

### 阶段四：发布准备

| # | 操作 | 执行者 | 说明 |
|---|------|--------|------|
| 4 | 生成版本发布说明 | AI | 版本号、发布日期、本次迭代范围摘要 |

### 阶段五：终验

| # | 操作 | 执行者 | 说明 |
|---|------|--------|------|
| 5 | 终验 | 人类 | 确认本次迭代完成 |

### 启动方式

> 开始敏捷式 t08（或 agile-t08-release）

AI 动作序列：读取本文件 → 加载 task-01~task-06 的全部增量产出 → 加载 task-07 产出的验证报告 → 加载完整基线文档集 → 按五阶段流程执行 → 全部完成后通知人类终验。

---

## 四、AI 执行指南

### 4.1 跨步骤上下文管理

同 agile-t01（继承上下文、决策记录、回溯修正）。

### 4.2 AI 经济原则

1. **执行顺序不可逆**：审查修复 → 变更汇总 → 生成日志 → 发布准备
2. **基于资产优先**：本次迭代的完整设计文档集为发布资产。v2 增量区与 v1 基线区合并，生成新的 v2 基线（冻结·只读），作为下一次迭代的资产
3. **增量设计特点**：本步是 8 步敏捷流程的最后一步，不涉及新设计工作。核心工作是：处理验证问题闭环、合并增量和基线、生成变更日志和版本发布说明。变更日志按层分类（OR/SR/BA/SysReq/PA），清晰记录本次迭代的范围

---

## 五、AI 提示词模板

### 5.1 本步提示词

详细 AI 提示词模板见 `03-agile-eos-pipeline-system-design/workflow-prompts.md` 第8步。

请在开始前确认：
- 是否已加载 task-01 ~ task-06 的全部增量产出？
- 是否已加载 task-07 产出的验证报告？
- 是否已加载本次迭代前的完整基线文档？
- 验证报告中的问题是否已全部理解（哪些是格式性问题可直接修复，哪些是结构性问题需提方案）？
- 变更日志的按层分类汇总是否已有初步框架？

具体操作按本文件§三的五个阶段执行。

---

## 六、决策记录模板

```
本步关键决策记录：
├─ 阶段状态：
│  ├─ 审查修复：[已完成] — [格式性问题 N 条 / 内容缺失 N 条 / 结构性问题 N 条]
│  ├─ 变更汇总：[已完成] — [OR 层 N 条 / SR 层 N 条 / BA 层 N 条 / SysReq 层 N 条 / PA 层 N 条]
│  ├─ 变更日志：[已完成] — [按层分类变更明细]
│  └─ 版本发布说明：[已完成] — [新版本号]
├─ 修复记录：
│  ├─ 已直接修复：[N 项 — 格式性问题/内容缺失]
│  ├─ 标记 ⚠️ 待确认：[N 项 — 结构性问题，提了方案等确认]
│  └─ 未修复（遗留）：[N 项 — 说明原因]
├─ 版本信息：
│  ├─ 新版本号：[vX.Y]
│  └─ 发布日期：[日期]
├─ ⚠️ 终验待确认项：[汇总需要人工终验确认的项]
└─ 执行模式：[全自动/半自动]
```

---

## 七、审查流程

### 7.1 审核要点

- [ ] 变更日志准确性（按层分类、无遗漏）
- [ ] 新版本号确认
- [ ] 验证报告问题闭环处理（所有问题已修复或标记）
- [ ] 所有文档版本号一致更新
- [ ] 超链接有效性（跨文档引用是否正确）

### 7.2 审查修复流程

同 agile-t01。

---

## 八、质量门禁

- [ ] 审查修复流程完成（验证报告问题全部处理）
- [ ] 变更日志已生成（按层分类汇总）
- [ ] 版本发布说明已生成（版本号+日期+范围）
- [ ] 所有文档版本号一致更新
- [ ] 增量内容已正确合并进基线文档
- [ ] 终验通过（人类确认）
- [ ] 运行 rule-conformance-review

---

## 九、产出物

| 文件 | 角色 | 说明 | 路径 |
|------|------|------|------|
| 更新后的五层文档 | 全部设计层 | 增量更新后的完整系统设计文档集 | `90-eos-system-product-data/01`~`09`（增量更新版本） |
| 变更日志 | 发布管理 | 按层分类的变更明细和影响分析摘要 | 附录到产品数据文件或独立发布文档 |

---

## 变更日志

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-05-28 | v1.0 | 首次构建 |
