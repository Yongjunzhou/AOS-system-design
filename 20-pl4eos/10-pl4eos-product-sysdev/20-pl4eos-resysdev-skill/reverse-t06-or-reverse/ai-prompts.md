# 逆向工程流程第6步 AI 提示词
**Reverse Engineering Step 6: AI Prompt Templates — Original Requirements Reverse Engineering**

**文档版本**：v1.0
**创建日期**：2026-05-28
**对应场景**：02-reverse-engineering · 第6步
**对应操作规程**：`task-definition.md`（同目录）

---

## 一、跨步骤上下文管理

### 1.1 上下文传递方式

8 步流程中，每一步的产出是下一步的核心输入。AI 在执行每一步前，应确保完整继承了上一步的关键决策：

| 继承内容 | 说明 | 保存位置 |
|---------|------|---------|
| 架构决策 | 合并/拆分、分类方式、层级结构 | 上一步的决策记录 |
| 编号体系 | 所有已分配的 ID 和编号规则 | 各产品数据文件 |
| 待确认项 | 上一步标记的 ⚠️ 项 | 汇总清单 |
| AI推断记录 | AI 自行判断补齐的内容 | 决策记录文件的 AI推断项 |

### 1.2 决策记录机制

每步完成后，AI 将本步的关键决策记录在**本步决策记录模板**（见本章末尾）中，供后续步骤引用。

### 1.3 回溯修正

如果在第 N 步发现需要修改第 M 步（M < N）的产出：
1. 评估影响范围（见 AI决策框架 §7.2 跨步骤影响范围表）
2. 定位到受影响的具体步骤和节点
3. 修改后检查下游映射是否需要同步调整
4. 标注变更记录

---

## 二、AI 经济原则

本步操作以下列顺序执行，AI 需注意：

1. **执行顺序不可逆**：分析活动 → 聚合 OR → OR 规范化 → OR 分解 → 冲突维护 → 映射更新 → 建映射 → 结果审核
   核心驱动力：从"谁在什么场景下有什么需求"聚合回"原始的需求来源是什么"，按业务功能聚类合并为完整的需求描述
2. **基于资产优先**：SR 详细定义末级是本步推导 OR 的核心资产。OR 的聚合优先级：复用已有 OR 表述 → 根据 SR 特征改进 → 全新创建
3. **逆向推导特点**：本步是逆向推导的终点——从结构化 SR 倒推原始的未加工表述。OR 的粒度和表述方式应模拟真实的"原材料"，而非 SR 的精确结构化表达。5 层闭环（PA → SysReq → BA → SR → OR）至此完成

## 三、本步提示词模板

### 3.1 本步提示词

```
请在开始前确认上下文状态：
1. 是否已读取 task-definition.md 了解本步操作流程？
2. 是否已获取 t05 产出的 SR 架构定义和详细定义？
3. 是否理解 OR 层是 5 层闭环（PA→SysReq→BA→SR→OR）的终点？
4. 是否准备按阶段一→阶段八的顺序执行？

具体操作按 task-definition.md 八个阶段执行。
```

## 四、决策记录模板

```
本步关键决策记录：
├─ 阶段状态：
│  ├─ 分析活动：[已完成] — [SR→OR追溯验证结果]
│  ├─ OR聚合：[已完成] — [N个OR条目]
│  ├─ OR规范化：[已完成] — [四项检查通过]
│  ├─ OR分解：[已完成] — [N个OR末级]
│  ├─ 冲突维护：[已完成] — [N项冲突/重复]
│  ├─ 映射更新：[已完成] — [SR文档已补充OR映射]
│  └─ 映射关系：[已完成] — [OR→SR映射]
├─ 资产使用记录：
│  ├─ 资产来源：[t05 SR详细定义]
│  └─ 聚合依据：[SR→OR 映射摘要]
├─ OR清单：[OR编号，条目名称，优先级]
├─ 5层闭环状态：[PA→SysReq→BA→SR→OR 已确认]
├─ ⚠️ 待确认项：[需要人工确认的OR聚合/优先级争议]
├─ AI推断记录：[AI 自行判断的OR条目/优先级]
├─ 待传递的约束：[第7步需要注意的OR特征]
└─ 执行模式：[全自动/半自动]
```

---

## 五、审查修复流程

### 5.1 审查输入

AI 完成指定步骤后，输出产物供 rule-conformance-review 审查。审查报告包含：
- 通过/未通过的项目清单
- 问题定位（涉及的具体文件、章节、编号）
- 问题类型（结构性/内容性/格式性）

### 5.2 AI 修复流程

收到审查报告后，AI 按以下优先级处理：

1. **格式性问题**（编号错误、格式不规范）→ 直接修复，不需要人工确认
2. **内容缺失**（某节为空、映射不完整）→ 补充内容，标注修复说明
3. **结构性问题**（粒度不匹配、分类错误）→ 提出修复方案，标注 ⚠️ 待确认
4. **跨步骤一致性问题** → 参考AI决策框架的跨步骤影响范围表进行影响范围评估，列出所有受影响的下游节点

### 5.3 影响范围评估

| 问题定位 | 影响范围 | 修复策略 |
|---------|---------|---------|
| 某步的架构定义 | 本层 + 下游已建立映射的节点 | 修改后同步更新映射 |
| 详细定义 | 下层方案 | 修改后检查下层是否受影响 |
| 映射关系 | 所有关联节点 | 需重新运行追溯验证 |
| 编号变更 | 全链路 | 需逐层更新引用，重新验证 |

---

## 变更日志

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-05-28 | v1.0 | 首次构建 |
