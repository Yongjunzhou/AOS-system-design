# 模式2·局部设计
**人类方案ID**: devops-t03 · **DevOps Step 3: Mode 2 — Local Design**

> **概述**：对涉及 系统需求概要 + pa-eng 层的局部设计变更进行修复，在 1 天内完成。需要修改系统需求和平台产品架构文档，但不涉及 SR/bpd-biz 层。
>
> **输入 → 输出**：影响范围评估报告（模式2） → 更新后的 系统需求概要 文档 + pa-eng 文档（增量标记）
>
> **适用场景**：DevOps 第3步·模式2。模式2直接发布，不经过 t04 验证。
>
> **版本**：v1.2 | **修订**：2026-08-28

---

## 一、设计角色

本步在设计链中承担的角色：
- **输入**：t01 产出的影响范围评估报告（选定模式2）
- **产出**：更新后的 系统需求概要 文档 + pa-eng 文档（增量标记）
- **发布方式**：模式2直接发布，不经过 t04 验证

适用条件：
- 任务定义中缺少关键步骤
- 能力定义（系统需求概要）与知识文档（pa-eng）描述不一致
- 某层分析活动缺失或不完整
- **需要修改 系统需求概要 和 PA**，但不涉及 SR/bpd-biz 层

在 EOS 双链设计体系下，模式2变更可局限单一设计链或跨越双链。如变更涉及**平台设计链**，bpd-biz 末级节点对应组件定义（引擎组件），系统需求概要 和 pa-eng 的变更需与引擎组件类型对齐；如变更涉及**业务设计链**，bpd-biz 末级节点对应业务组件，系统需求概要 和 pa-eng 的变更需与业务配置类型对齐。跨链变更时需分别处理两链的 系统需求概要 → pa-eng 映射。

### 与三条工程化设计原则的关系

| 原则 | 在本步中的体现 |
|------|--------------|
| **详概分离** | 本步仅修改 系统需求概要 和 pa-eng 层，不涉及 SR/bpd-biz 层。系统需求概要 架构定义和详细定义同步更新，pa-eng 层仅做对应调整 |
| **文档结对** | 系统需求概要（需求方）与 PA（方案方）结对。修改后需验证 系统需求概要 → pa-eng 映射关系的一致性和完整性。双链场景下需分别验证平台链和业务链的映射关系 |
| **基于资产** | 局部设计基于现有 系统需求概要 + pa-eng 文档进行，只修改受影响的末级节点和映射关系，保留未受影响部分不变 |

---

## 二、前置条件

| 条件 | 说明 |
|------|------|
| 输入 | t01 产出的影响范围评估报告（选定模式2，含受影响的 系统需求概要 和 pa-eng 节点定位） |
| 参考基线 | 现有 系统需求概要 文档 `90-eos-system-product-data/05-*.md` + `06-*.md`，pa-eng 文档 `07-eos-platform-product-architecture.md` |

### 文档结对关系

| 结对 | 需求方 | 方案方 | 说明 |
|------|--------|--------|------|
| 结对1（本步） | t01 影响范围评估报告（模式2） | 系统需求概要 + pa-eng 文档增量更新 | 本步完成：定位→分析→系统需求概要 增量→pa-eng 增量→映射更新→回归 |
| 发布衔接 | 更新后的 系统需求概要 + pa-eng 文档 | — | 模式2直接发布，不经过 t04 验证 |

---

## 三、操作流程

六阶段：系统需求概要 定位 → 影响分析 → 系统需求概要 增量定义 → pa-eng 增量定义及映射更新 → 回归验证 → 审查。

核心驱动逻辑：基于 t01 评估报告的定位结果，从 系统需求概要 层开始逐层修改到 pa-eng 层，保持映射关系一致。

操作流程概览：

```
[阶段一] 系统需求概要定位              [阶段二] 影响分析              [阶段三] 系统需求概要增量定义
  定位受影响的系统需求概要节点           内容完整性分析                  更新系统需求概要和详细分解
                                  一致性分析

[阶段四] PA增量及映射更新         [阶段五] 回归验证             [阶段六] 审查
  更新PA组件定义                  确保不影响其他功能             人类确认局部设计
  更新系统需求概要→平台产品架构映射
```

### 阶段一：系统需求概要 定位

| # | 操作 | 执行者 | 说明 |
|---|------|--------|------|
| 1 | 定位受影响的 系统需求概要 和 pa-eng 节点 | AI | 基于 t01 评估结果，定位到具体的 系统需求概要 末级节点和 pa-eng 末级节点 |

### 阶段二：影响分析

| # | 操作 | 执行者 | 说明 |
|---|------|--------|------|
| 2 | 内容完整性分析 | AI | 分析问题影响哪些 系统需求概要 节点，确认修改后知识文档内容的一致性 |
| 3 | 一致性分析 | AI | 分析修改后 系统需求概要 → pa-eng 映射是否一致，确认跨文档引用关系不断裂 |

### 阶段三：系统需求概要 增量定义

| # | 操作 | 执行者 | 说明 |
|---|------|--------|------|
| 4 | 系统需求概要 增量定义 | AI | 更新受影响的 系统需求概要 架构定义（`05-*.md`）和 系统需求概要 详细定义（`06-*.md`），以增量标记标识修改 |

### 阶段四：pa-eng 增量定义及映射更新

| # | 操作 | 执行者 | 说明 |
|---|------|--------|------|
| 5 | pa-eng 增量定义 | AI | 更新受影响的 pa-eng 组件定义（`07-*.md`），以增量标记标识修改 |
| 6 | 更新映射关系 | AI | 同步更新 系统需求概要 → pa-eng 映射表，确保映射关系一致 |

### 阶段五：回归验证

| # | 操作 | 执行者 | 说明 |
|---|------|--------|------|
| 7 | 回归验证 | AI | 验证局部设计不影响其他功能。检查被修改节点的上下游依赖关系是否完整 |

### 阶段六：审查

| # | 操作 | 执行者 | 说明 |
|---|------|--------|------|
| 8 | 结果审核 | 人类 | 确认局部设计。重点审核：是否影响其他功能、系统需求概要 修改的准确性、是否需要升级为模式3 |

### 启动方式

> 开始 DevOps t03（或 devops-t03-mode2-local-design）

AI 动作序列：读取本文件 → 加载 t01 影响范围评估报告（模式2） → 读取现有 系统需求概要 + pa-eng 文档 → 按六阶段流程执行 → 完成后通知人类审核。

---

## 四、AI 执行指南

### 4.1 跨步骤上下文管理

本步的输入来自 t01，产出直接发布。AI 应确保完整继承了 t01 的关键决策（问题定位、模式确认、待确认项）。如发布前发现需要修改 t01 的评估，需评估是否影响模式选择。

### 4.2 AI 经济原则

1. **执行顺序不可逆**：系统需求概要 定位 → 影响分析 → 系统需求概要 增量定义 → pa-eng 增量定义及映射更新 → 回归验证 → 审查
2. **快速响应优先**：DevOps 场景追求最短时间完成修复，1 天内完成
3. **最小化变更原则**：只修改受影响的 系统需求概要 和 pa-eng 节点及映射关系
4. **增量标记**：所有修改以 `~` 修改 / `+` 新增 标记标识

---

## 五、AI 提示词模板

### 5.1 本步提示词

详细 AI 提示词模板见 `04-devops-eos-pipeline-system-design/workflow-prompts.md` 第3步（模式2部分）。

请在开始前确认：
- 当前处理的 DevOps 模式2：局部设计
- 输入：t01 影响范围评估报告（模式2）
- 参考基线：现有 系统需求概要 文档 + pa-eng 文档

具体操作按本文件§三的六个阶段执行，注意保持 系统需求概要 → pa-eng 映射的一致性。

---

## 六、决策记录模板

```
本步关键决策记录：
├─ 阶段状态：
│  ├─ 系统需求概要定位：[受影响的 系统需求概要 末级节点清单]
│  ├─ PA定位：[受影响的 pa-eng 末级节点清单]
│  ├─ 影响分析：[符合性分析结果]
│  ├─ 系统需求概要 增量：[~修改 N 处 / +新增 N 处]
│  ├─ pa-eng 增量：[~修改 N 处 / +新增 N 处]
│  ├─ 映射更新：[系统需求概要→平台产品架构映射更新内容]
│  └─ 回归验证：[通过/未通过]
├─ 变更清单：
│  ├─ ~ 修改：[修改内容 + 变更说明]
│  └─ + 新增：[新增内容 + 新增原因]
├─ 升级判断：[是否需要升级为模式3？是/否 + 理由]
├─ ⚠️ 待确认项：[汇总需要人工确认的项]
└─ 执行模式：[全自动/半自动]
```

---

## 七、审查流程

### 7.1 审核要点

- [ ] 局部设计是否影响其他功能（回归验证充分性）
- [ ] 系统需求概要 修改的准确性
- [ ] 是否需要升级为模式3（如发现需要修改 SR/BP）
- [ ] 系统需求概要 → pa-eng 映射关系更新完整
- [ ] 增量标记准确（`~` 修改 / `+` 新增）

### 7.2 审查修复流程

同 devops-t01。

---

## 八、质量门禁

- [ ] 系统需求概要 和 pa-eng 节点已定位到末级
- [ ] 内容完整性分析已执行（映射一致性验证通过）
- [ ] 一致性分析已执行（跨文档引用关系验证通过）
- [ ] 系统需求概要 文档已增量更新（架构定义 + 详细定义）
- [ ] pa-eng 文档已增量更新（组件定义）
- [ ] 系统需求概要 → pa-eng 映射关系已同步更新
- [ ] 回归验证已通过（不影响其他功能）
- [ ] 运行 rule-conformance-review

---

## 九、产出物

| 文件 | 说明 | 路径 |
|------|------|------|
| 更新后的 系统需求概要 架构定义 | 增量更新后的系统需求架构定义 | `90-eos-system-product-data/05-eos-system-requirement-summary-architecture.md` |
| 更新后的 系统需求概要 详细定义 | 增量更新后的系统需求方案 | `90-eos-system-product-data/06-eos-system-requirement-detailed.md` |
| 更新后的 pa-eng 文档 | 增量更新后的平台产品架构文档 | `90-eos-system-product-data/07-eos-platform-product-architecture.md` |

---

## 变更日志

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-08-28 | v1.2 | 层名更名（人类指示 2026-08-28）——业务流程概要定义→业务流程概要、业务流程详细定义→业务流程方案、系统需求概要定义→系统需求概要、系统需求详细定义→系统需求方案（nfr/eng 链同构，含短名/标题后缀）；历史变更记录保留旧词。 |
| 2026-05-28 | v1.0 | 首次构建 |
