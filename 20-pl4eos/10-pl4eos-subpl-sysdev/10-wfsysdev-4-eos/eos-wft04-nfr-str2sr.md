# bph-nfr → SysReq-NFR · 非功能需求架构与页面约束设计
**人类方案ID**: wft04-nfr

> **概述**：接收已确认的 bph-nfr 角色非功能需求分类，经前置校验、材料加载和反馈处理后，生成系统侧非功能需求架构（SysReq-NFR）和页面非功能需求。该方案承接 wft01-nfr 分类需求清单中相关方经澄清确认的目标值（[通用设计规范 §1.2](../../../00-generalspec/01-generalspec-sysdev.md#12-需求详解) 需求=需求描述+性能指标），展开为可验证的量化约束、分层约束表、页面约束包、22 具体需求资产和 06 系统需求详细定义。
>
> **输入 → 输出**：bph-nfr 节点（`可以NFR设计` / `待补充NFR设计`）/ 有反馈的 SysReq-NFR 节点 → SysReq-NFR 架构节点 + 量化指标 + 分层约束表 + 七种页面类型及具体页面 ID 的 NFR 约束 + wft05-eng NFR 约束包（含版本与变更声明）+ 22 具体需求写回 + 06 系统需求详细定义 + bph-nfr 推进为 `已NFR设计`
>
> **适用场景**：瀑布式系统设计第4步 · 非功能需求路径。
>
> **设计模式参考**：[91-eos-biz-eng-spec.md](91-eos-biz-eng-spec.md)
>
> **版本**：v5.8 | **修订**：2026-08-24

---

## 一、职责定位

### 1.1 本 Skill 目的与目标

> **主要目的**：取信于架构师和验证者——「指标可验证，约束分层正确，目标页面与实现组件可追溯」。把 bph-nfr 转化为系统侧 SysReq-NFR 架构和页面非功能需求，将约束绑定到 91 规范七种页面类型之一或具体页面 ID，并关联 CU/pa-eng 组件和 06 详细定义，形成供 wft05-eng 单一实现消费并支持追溯验证的 NFR 约束包。

**目标**：

1. **生成非功能需求架构**——按 22 分类维度和系统约束边界生成或复用 SysReq-NFR 节点，保证每条 bph-nfr 分类需求项有唯一系统侧宿主
2. **生成量化约束**——承接相关方经澄清确认的目标值，展开为可验证的指标（单位/统计口径/测量方法和验收条件）；目标值缺失或信息不足时标记待确认，不伪造确定值
3. **生成页面非功能需求**——把 NFR 约束落到全局、业务域、srh-biz、CU、pa-eng 组件和页面对象，形成 wft05-eng 可消费的约束包
4. **写回 22 具体需求资产**——复用、升级或新增 22 具体需求条目及指标集，保留 bph-nfr / SysReq-NFR 追溯
5. **展开 06 详细定义**——将确认后的 SysReq-NFR 量化约束写为可验证系统需求详细定义，供后续验证与实现使用

本 Skill 负责：

1. **接收**——接收人类选中的一批 bph-nfr 节点（≤5 个），校验状态为 `可以NFR设计` / `待补充NFR设计`；状态/类型不符时退出并提示
2. **量化**——消费 bph-nfr 分类需求清单，承接相关方确认的目标值，逐项展开系统级量化指标（指标名称/单位/统计口径/测量方法/验收条件）；目标值缺失时标记待确认，不伪造确定值
3. **归架构**——按 22 分类维度、约束对象和语义内聚度匹配或新建 SysReq-NFR 节点，保证每条分类需求项有唯一系统侧宿主
4. **分层**——生成分层约束表，明确约束范围、适用链路、约束对象、消费目标、影响级别和影响点
5. **落页面**——对影响业务操作体验、界面行为、页面性能、安全、可访问性、审计提示等诉求，生成页面非功能需求
6. **落账**——写回 22 具体需求资产、06 系统需求详细定义、05 SysReq-NFR 节点和 bph-nfr 消费版本
7. **推进**——人类确认后 bph-nfr → `已NFR设计`，SysReq-NFR → `已验证冻结`
8. **退回**——当 bph-nfr 分类边界、角色语境或适用范围线索不足时，将节点退回为 `需wft01修订` 并附缺失说明

本 Skill 不负责：

1. 修改 wft01-nfr 已确认的角色语境、NFR 分类边界或 OR 分组；发现问题时退回 wft01-nfr
2. 设计业务场景、输出文档或功能表单（→ biz 路径）
3. 设计引擎场景、CU 详细定义或前后端组件结构（→ eng 路径）
4. 修改 22 已确认分类维度本身；本 Skill 写入的是具体需求、指标集、引用和候选升级
5. 替代人类确认无法从 bph-nfr 或资产推断出的目标值

### 1.2 与上下游的衔接

**上游 wft01-nfr**：提供已确认 bph-nfr，包含角色语境、22 分类路径、NFR 分类需求清单、适用对象线索、待量化线索和变更影响声明。本 Skill 在 Step Start 中通过 `02-*.md` 的 `## AI可以处理节点` 分节（[91 规范 §11.7](91-eos-biz-eng-spec.md#117-ai可以处理节点)）检测待处理 bph-nfr 节点，定位并读取节点块（按 [§A.3.3](91-eos-biz-eng-spec.md#a33-产品数据文档与资产文档读取与写回约定) 读取协议执行）。重入时读取 bph-nfr 版本号和变更声明，按无变更/增量型/修订型/结构型处理。若 bph-nfr 分类边界或适用对象不足，本 Skill 退回为 `需wft01修订`（[§A.4.3](91-eos-biz-eng-spec.md#a43-规则二退回节点的门禁) 退回门禁）。

**下游消费方**：wft05-eng 消费 CU/pa-eng 组件/运行机制相关 NFR 约束（含页面 NFR 和分层约束表）。06 系统需求详细定义供验证与追溯使用。SysReq-NFR 的分层约束表是下游消费 NFR 的唯一入口。NFR 约束全部流向 eng 路径——性能/安全/可用性等非功能质量约束由平台的引擎层保障，不进入 biz 路径（业务配置链）。

**时序说明**：wft01-nfr / wft04-nfr 可与 eng 路径前三步并行。wft04-nfr 可先设计全局、共享、业务域级约束，并为 bph-eng/CU/页面级约束记录目标候选；相关目标尚未形成时不得虚构目标 ID，节点保持 `待目标绑定`。biz 路径的功能表单/窗口标签页或 eng 路径的配置页面 srh-biz 可定位后，wft04-nfr 完成目标绑定并确认首版 NFR 约束包。该约束包是 wft05-eng 首次启动的必备前置输入：未取得已确认的首版约束包时，wft05-eng 不得开始产品架构设计。首版被消费后，约束包版本递增可触发 wft05-eng 按变更声明和约束行 ID 增量重入。

---

## 二、非功能需求架构与页面级约束基础

**标注纪律**（框架全链一致，各 Skill 按域应用）：AI 设计产出中凡需人类确认/裁决的项，统一显式标注——①`[推断]`：AI 按规则推断、供人类确认或裁决（如量化指标口径、分层约束边界存疑）；②`[待下游推断]`：信息不足、跨 Skill 持久化待确认（本 Skill 承接已确认锚点，此类别少用）；③`供人类确认`：业务/权威锚点、必须由人类确认（本 Skill 对指标/约束归属争议标 `[推断]`/`需澄清` 处理）。

### 2.1 非功能需求架构（SysReq-NFR）

**定义**：SysReq-NFR 是 bph-nfr 在系统需求层的结构化表达，末级节点为**非功能需求分类**。它回答"系统需要满足哪类非功能质量、以什么指标验证、约束哪些业务/平台对象、由哪些下游设计对象消费"。NFR 路径分工见 [91 规范 §10.5](91-eos-biz-eng-spec.md#105-nfr-路径-skill)。

**宿主匹配锚点**（四元组）：

| 锚点 | 说明 |
|------|------|
| 22 分类维度 | 一级类目 + 分类维度，决定主要质量属性 |
| 约束对象 | 全局、业务域、srh-biz、CU、pa-eng 组件、页面级对象 |
| 指标口径 | 目标值、单位、统计口径、测量方法 |
| 适用链路 | `biz` / `eng` / `共享` |

**节点完整要素**（写入 `05-*.md` SysReq-NFR 节点块）：

| 要素 | 说明 |
|------|------|
| 身份信息 | ID / 名称 / 22 分类路径 |
| 来源追溯 | 来源 bph-nfr 清单与消费版本 |
| 量化指标 | 量化指标清单（§2.2） |
| 约束结构 | 分层约束表 + 页面 NFR 清单（§2.3） |
| 资产对齐 | 22 具体需求写回项 + 06 详细定义引用 |
| 检查状态 | 闭环检查结果 + 版本号 / 确认状态 |

#### 完备性判据

| # | 检查项 | 判据 | 缺口处理 |
|---|--------|------|----------|
| 1 | 量化完整 | 全部 bph-nfr 需求项已生成量化指标，或明确标注待确认缺口 | 标记缺口，不进入写回 |
| 2 | 宿主唯一 | 每条 bph-nfr 需求项有唯一 SysReq-NFR 宿主 | 标记宿主冲突 |
| 3 | 分层正确 | 全局/业务域/srh-biz/CU/pa-eng组件/页面级未错层 | 修正分层 |
| 4 | 页面可消费 | 影响页面体验的 NFR 已生成页面级约束 | 补充页面 NFR |
| 5 | eng 一致 | 共享指标在不同引擎/组件间保持同一指标 ID、目标值和测量方法 | 对齐指标 |
| 6 | 22 可落账 | 具体需求写回项指标集完整、分类路径明确 | 标记缺口 |
| 7 | 06 可验证 | 详细定义包含来源、指标、测量、验收和追溯 | 补充 06 条目 |

---

### 2.2 量化指标

量化指标是 SysReq-NFR 的核心条目，承接 bph-nfr 中相关方经澄清确认的目标值，展开为可验证的指标定义。每条指标至少记录：

| 字段 | 说明 |
|------|------|
| 指标名称 | 可验证的非功能指标 |
| 目标值 / 单位 | 明确阈值、范围或布尔要求；无法确定则标记 `待确认` |
| 统计口径 | 时间窗口、样本范围、百分位、业务条件或环境条件 |
| 测量方法 | 压测、审计检查、日志统计、模拟、外部认证、人工检查等 |
| 验收条件 | 满足/不满足的判断标准 |
| 来源 bph-nfr | 需求项 ID 与消费版本 |
| 22 具体需求引用 | 复用或新增的 `@nfr-xxx-REQ-yy` |
| 确认状态 | 人类反馈字段 |

模糊诉求处理原则：可依据 22 具体需求模板、同类场景和行业常识推断时标记 `[需裁决]` 并给出依据；无法推断时保留 `待确认`，不得推进到写回。

---

### 2.3 分层约束与页面 NFR

#### 2.3.1 分层约束表定义

每个 SysReq-NFR 节点必须带分层约束表，供 wft05-eng 消费：

| 约束范围 | 适用链路 | 约束对象 | 消费目标 | 影响级别 | 影响点 | 说明 |
|----------|----------|----------|----------|----------|--------|------|

字段取值：

| 字段 | 取值 |
|------|------|
| 约束范围 | `全局` / `业务域` / `srh-biz` / `CU` / `pa-eng组件` / `页面级` |
| 适用链路 | `biz` / `eng` / `共享` |
| 约束对象 | EOS、业务域名、srh-biz ID、目标引擎+CU 类型、组件类型、七种页面类型之一或具体页面 ID |
| 消费目标 | `wft05-eng:CU/组件约束` / `wft05-eng:页面级约束` / `wft05-eng:共享约束` |
| 影响级别 | `阻断型` / `非阻断型` / `待判定` |
| 影响点 | 配置项、配置规则、配置可达性、验收标准、页面交互、页面性能、权限/审计、组件边界、接口机制、运行约束机制等 |

填写约束：

1. 一行只表达一个约束对象和一个消费目标的关系。
2. 阻断型约束必须填写影响点。
3. 待判定约束必须说明待判原因。
4. 影响面大时优先使用全局或业务域，不强制枚举所有 srh-biz。
5. 明确影响引擎实现时，至少补一行 `适用链路=eng` 的 CU 或 pa-eng 组件约束。
6. NFR 约束仅流向 eng 路径——所有约束行消费目标均为 `wft05-eng`，不进入 biz 路径。

#### 2.3.2 页面非功能需求

页面 NFR 是分层约束表中 `约束范围=页面级` 的特化条目，覆盖页面类型、交互行为和用户可感知质量。页面类型必须从 91 规范定义的七种类型中选择：菜单标签页、场景标签页、窗口标签页、全屏单据页、辅助栏标签页、弹窗页面、配置页面。

典型类型：

| 类型 | 示例 |
|------|------|
| 页面性能 | 首屏加载、列表查询、下钻切换、弹窗响应、批量操作反馈 |
| 页面安全 | 字段脱敏、按钮权限、行列权限、敏感操作二次确认 |
| 易用性/工效 | 操作步数、错误提示、批量处理、键盘可达性、认知负荷 |
| 可访问性 | 对比度、焦点顺序、读屏标签、无鼠标操作 |
| 可恢复性 | 草稿保存、异常重试、重复提交防护 |
| 可观测性 | 操作日志、审计轨迹、错误定位信息 |

页面级约束应引用 91 规范定义的页面类型及其层级，并按需关联辅助栏、分屏、下钻、菜单和引擎生成模型。能落到具体功能表单、窗口标签页、配置页面或其他页面时写具体 ID；暂不能定位时写页面类型或业务域，并标记 `[需裁决]`。

#### 2.3.3 22 具体需求资产

wft04-nfr 写回 22 的对象是**具体需求及其指标集**，不是分类维度本身。

| 判定 | 条件 | 动作 |
|------|------|------|
| 复用 | 22 已有同义具体需求，指标口径兼容 | 引用已有 `@nfr-xxx-REQ-yy` |
| 升级候选 | 候选区存在同义候选，且本轮指标集完整 | 状态→已确认，补充指标集和来源 |
| 新增 | 无同义具体需求或候选 | 新建具体需求条目，挂到已有分类维度 |
| 回退 | 指标集不完整或口径无法确认 | 回退 Step 3，不写入 22 |

---

### 2.4 NFR 架构要素提取规则

下文 §2.4.1~§2.4.4 的提取规则同时适用于 Phase B（修订已有 SysReq-NFR）与 Phase C（新建 SysReq-NFR）。

#### 2.4.1 量化指标生成

逐 bph-nfr 分类需求项生成量化指标：

| 输入线索 | 处理 |
|----------|------|
| OR 明示数值或布尔约束 | 直接采信，记录来源 |
| OR 有比较词或质量方向 | 基于 22 具体需求模板和同类场景推断，标记 `[需裁决]` |
| OR 有验证语义但无目标值 | 生成测量方法和待确认目标值 |
| 无可验证线索 | 标记缺口，回退补充 NFR，不推进写回 |

量化输出必须包含指标名称、目标值/单位、统计口径、测量方法、验收条件、来源 bph-nfr、22 具体需求判定和确认状态。

**分解规则**——逐条量化指标判定「约束对象」是否多对象（约束范围判定见 §2.4.2）。仅当一条量化指标需约束多个对象类型、且指标可按运算关系切分时，进行分解。分解运算四类（wft05-eng 消费、wft06 守恒核对的依据）：

| 分解运算 | 适用指标 | 预算方向 | 处理 |
|---------|---------|---------|------|
| **加法** | 响应时间等可切分指标 | Σ 子目标 ≤ 系统目标 | 按组件类型切预算：父指标 + N 条子指标（各约束对象类型），父→子引用标注；候选子预算基于 22 模板/同类场景推断，标 `[需裁决]`，人类确认后入约束包 |
| **乘法** | 可用性等可切分指标 | Π 子目标 ≥ 系统目标 | 按组件类型切可用性目标，父→子引用标注；候选子目标标 `[需裁决]` |
| **弱链** | 吞吐量/并发等 | 每子目标 ≥ 系统目标 | **不切预算**，逐对象类型指派（各承接全量） |
| **职责分布** | 安全/审计/权限/易用性等 | 无守恒 | **不切预算**，逐对象类型指派职责 |

**多对象识别**：约束对象类型按 91 §8.5 三类组件（前端/后端/平台服务）+ srh-biz/CU/页面级判定；一条指标同时影响多个对象类型时为多对象。加法/乘法指标的多对象情形生成子指标（组件类型级）；弱链/职责分布指标的多对象情形逐对象类型生成约束行、不切预算。**单对象指标不分解**，一条到底（保持原约束范围）。子指标目标到组件类型级，不预判具体组件（`待wft05映射`，见 §3.5）。**记录**：父指标记「分解运算 + 子指标引用」，子指标记「父指标引用 + 分解运算 + 目标对象类型」。

#### 2.4.2 分层约束判定

对每条量化指标判定约束范围：

| 约束范围 | 判定线索 | 典型消费 |
|----------|----------|----------|
| 全局 | 影响 EOS 整体运行质量 | wft05-eng |
| 业务域 | 只影响某业务域或产品类型 | wft05-eng |
| srh-biz | 指向具体功能性系统需求 | wft05-eng |
| CU | 指向配置单元、配置规则或配置可达性 | wft05-eng |
| pa-eng组件 | 指向前后端组件类型、接口或运行机制 | wft05-eng |
| 页面级 | 指向七种页面类型之一、具体页面 ID、交互行为或页面体验 | wft05-eng |

#### 2.4.3 Phase B SysReq-NFR 宿主匹配检查

Phase B 修订已有 SysReq-NFR 时，按 `22 分类维度 + 约束对象 + 指标口径` 匹配已有宿主：

| 结果 | 判定 | 动作 |
|------|------|------|
| Q1 复用宿主 | 已有 SysReq-NFR 分类、约束对象和指标口径兼容 | 追加指标和追溯，标记 `[确认]` |
| Q2 扩展宿主 | 已有宿主可承载但边界需扩展 | 修订宿主，标记 `[变更]` |
| Q3 新建宿主 | 无合适宿主 | 新建 SysReq-NFR |
| 宿主冲突 | 多个宿主均可承载或语义不内聚 | 标记 `[需裁决]` |

#### 2.4.4 Phase B（修订）与 Phase C（新建）的提取差异

| 维度 | Phase B 修订已有 SysReq-NFR | Phase C 新建 SysReq-NFR |
|------|---------------------------|------------------------|
| 判定范围 | SysReq-NFR 已承接的全部 bph-nfr + 本轮新 bph-nfr | 待聚合池本组 bph-nfr |
| 量化指标基线 | 以已有量化指标清单为基线，仅扫描本轮新需求项 | 从零构建 |
| 宿主匹配 | 执行宿主匹配检查（§2.4.3），可能检出 Q1/Q2/Q3/宿主冲突 | 无需匹配，直接新建 |
| 分层约束 | 增量合并新指标 → 更新分层约束表行 | 全量判定（§2.4.2） |
| 22 重识别 | 合并重识别可能导致已有指标的 22 判定变更——标记 `[22判定变更]` 并附变更原因 | 全量判定，无此问题 |
| 变更传播 | 需输出变更影响声明（§2.7） | 写"首版，无变更" |

---

### 2.5 NFR 量化推断规则

以下推断规则对 Phase B/C 通用，承接 §2.4 提取的线索后执行量化、锁定和关系检测。

#### 2.5.1 量化指标推断

对每个 bph-nfr 分类需求项：

1. 提取待量化线索（数值、比较词、验证条件、约束对象线索）。
2. 匹配 22 具体需求模板和同类场景的指标口径。
3. 生成目标值/单位/统计口径/测量方法/验收条件。
4. 信息不足时标注 `待确认`，不得伪造确定值；能推断时标注 `[需裁决]` 并给出依据。
5. 判定 22 具体需求归属：复用/升级候选/新增/回退（§2.3.3）。

#### 2.5.2 页面 NFR 推断

当指标影响用户可感知页面行为时，必须生成页面 NFR：

| 页面线索 | 输出 |
|----------|------|
| 页面类型明确 | 绑定七种页面类型之一，说明约束行为 |
| srh-biz 明确 | 引用具体 srh-biz，并绑定功能表单/窗口标签页/配置页面等具体页面 ID |
| 只知道业务域 | 写业务域级页面约束，标记 `[需裁决]` |
| 影响辅助栏/分屏/下钻/面包屑 | 明确关联 91 的交互模型要素 |
| 影响权限/审计/安全提示 | 同时生成页面 NFR 条目和 eng 权限/审计实现约束，均装入 wft05-eng NFR 约束包 |

页面 NFR 条目包含九项：指标 ID / 页面类型 / 具体页面 ID（暂缺时标记待目标绑定）/ 约束内容 / 量化目标 / 测量方法 / 影响点 / 消费目标 / 确认状态。

#### 2.5.3 Phase B 量化指标关系检测

Phase B 修订已有 SysReq-NFR 时，宿主匹配通过后，将本轮生成的量化指标与 SysReq-NFR 已有量化指标清单比对。比对锚点为**指标名称+22分类维度匹配**。

> 内部细分结论写在变更影响声明的变更项说明字段中，方便下游追溯每条指标变化的细类。

| 关系 | 判定 | 处理（内部细分结论） |
|------|------|---------------------|
| 确认 | 指标匹配 + 22 分类一致 + 来源升级 | 追加 bph-nfr 引用（`确认`） |
| 无新增 | 指标匹配 + 22 分类一致 + 所有来源已确认 | 增量合并口径/测量细节（`改进`） |
| 指标口径变更 | 指标匹配 + **目标值/统计口径/测量方法变化**（bph-nfr 修订导致） | 更新指标定义 + 变更原因，不新增指标（`指标口径变更`） |
| 22 判定变更 | 指标匹配 + **22 具体需求判定不同**（合并重识别导致） | 更新 22 判定 + 变更原因（`22判定变更`） |
| 补充 | 指标不匹配 | 新增量化指标（`新增`） |
| 过时 | 基线已有指标 **未被**本轮需求项覆盖（需求项移除导致） | 追加说明"请确认是否移除"，保留该指标不删除——移除是架构决策，由人类在反馈中触发（`过时`） |

关系检测前先基于**全部 bph-nfr 重新判定 22 归属**——新需求项可能改变指标特征导致 22 判定变更（表中 `22判定变更` 行）。

`[调整]` 是与关系类型正交的叠加标记：已有指标的目标值/统计口径/测量方法变化，在关系标记之外叠加 `[调整]`。

**共享指标一致性检查**（Phase B 修订完成后执行）：扫描所有涉及多引擎/多组件共享的量化指标（同一 SysReq-NFR 宿主内不同约束对象共享的指标），检查同一指标 ID 的目标值、统计口径和测量方法是否保持一致。不一致时以最新需求为准对齐，标记 `[共享指标对齐]` 并附原因。该检查是 `eng一致`（§2.1 完备性判据）闭环检查项的前置操作。

---

### 2.6 SysReq-NFR 节点生命周期

wft04-nfr 同时管理两条生命周期线：bph-nfr（上游节点，本 Skill 消费并推进）和 SysReq-NFR（本 Skill 产出节点）。通用模型见 [91 规范 §A.6](91-eos-biz-eng-spec.md#a6-节点生命周期模型)。

**bph-nfr 侧**（wft01-nfr ←→ wft04-nfr）：

```
可以NFR设计 ──wft04锁定──→ 在NFR设计 ──wft04完成──→ 已NFR设计
     ↑                         │                         │
     │                         │               wft04退回  │
     │                         │                         ↓
     └─── wft01修订 ───── 需wft01修订 ←───────────────┘
```

**SysReq-NFR 侧**（wft04-nfr 产出 → 验证与 wft05-eng 消费）：

```
待确认方案 ──目标未就绪──→ 待目标绑定 ──完成绑定──→ 待确认方案
     │                                                   │
     └──────────────────人类确认+落账────────────────────→ 已验证冻结
                                                             │
                                               验证/消费退回   ↓
                              待确认方案 ←── wft04修订 ── 需wft04修订
```

**上游变更**（新 bph-nfr 汇入触发 SysReq-NFR 修订）：

```
已验证冻结 ──新bph-nfr修订──→ 待补充NFR验证 ──wft04补全──→ 已验证冻结
```

**本 Skill 的写回范围**：SysReq-NFR `待确认方案` / `待目标绑定` / `待补充NFR验证` / `需wft04修订`。`在NFR设计` 由本 Skill 锁定期间设置，`已验证冻结` 由本 Skill 完成目标绑定、人类确认和落账后写入。

本 Skill 在首次消费 bph-nfr 时记录 `bph-nfr 消费版本：v{N}`；重入时若 bph-nfr 当前版本大于消费版本，则读取变更影响声明并增量处理。

下游退回 `需wft04修订` 时：读取验证或消费方的缺陷说明 → 按人类改进方案修订 SysReq-NFR（走 Phase B 修订流程）→ 人类确认 → 重新推进。根因在 bph-nfr 时退回 wft01-nfr。

### 2.7 SysReq-NFR 变更传播协议

变更传播协议的通用定义（变更类型分类、判定优先级、声明格式模板、增量清单与变更影响声明的分工）见 [91 规范 §A.7](91-eos-biz-eng-spec.md#a7-变更传播协议)。

**本 Skill 的变更影响声明格式**，每次修订已有 SysReq-NFR 后写入 SysReq-NFR 节点末尾（`05-*.md` 中 SysReq-NFR 节点块内）：

```text
### 变更影响声明

SysReq-NFR 版本：v{N}（本次修订前为 v{N-1}）
本轮变更类型：无变更 / 增量型 / 修订型 / 结构型

变更项：
- [新增] / [变更] / [确认] / [需裁决] ...
  说明字段中记录细分原因：量化指标新增 / 指标口径变更 / 22判定变更 / 过时 / 宿主冲突等

对应 wft05-eng NFR 约束包：<约束包 ID / 包版本>
wft05-eng 消费状态：待消费 / 已消费 / 需重消费
wft05-eng 已消费版本：<包版本；未消费则写"尚未消费">

对 wft05-eng 已消费版本的影响：
  受影响约束行：<约束行 ID + 受影响原因>（指标口径变更 / 新增约束 / 失效约束等）
  不受影响约束行：<约束行 ID>

建议下游动作：
  - 无变更：无需任何动作，约束包保持有效
  - 增量型：wft05-eng 增量消费新增约束行即可
  - 修订型：修订受影响约束行的承接 → 标记 [来自上游变更]
  - 结构型：输出影响范围，等人类裁决后再处理；含分类维度变化时下游需重新匹配约束
```

---

## 三、操作流程

### Step 布局总览

| Step | 名称 | 职责 | 人类参与 | 执行模式 | 产出 | 写资产 |
|------|------|------|---------|---------|------|--------|
| Start | 前置校验与路由 | 变更感知 + 入口检测、输入校验、退回优先路由 | 无（AI 自动） | 统一 | 就绪 / 结束 | — |
| 1 | 设计材料加载 | 安全复查 + 读 AI 可以处理节点 + 加载 bph-nfr/05/22 资产 | 无（AI 自动） | 统一 | bph-nfr / 05 / 22 / 06 / srh-biz与页面线索 | — |
| 2 | 方案反馈处理 | 读"待反馈处理"分节 + 对话处理人类修改意见 | **有**——对话反馈修改意见 / 整体确认 | 对话 | 调整后的 SysReq-NFR 方案 | `05` |
| 3 | 非功能需求架构设计 | 量化指标 + 22 具体需求候选 + 宿主匹配 | 无（AI 产出，之后对话反馈） | 统一 | SysReq-NFR + 量化指标 + 22 候选 | `05` |
| 4 | 页面与分层约束设计 | 分层约束表 + 页面 NFR + 下游约束包 | 无（AI 产出，之后对话反馈） | 统一 | 分层约束表 + 页面 NFR + 下游约束包 | `05` |
| 5 | 闭环检查 | 量化完整 / 分层正确 / 页面可消费 / 资产可落账 | 无（AI 自动） | 统一 | 闭环检查结论 | — |
| 6 | 资产落账与状态推进 | 22 写回 + 06 详细定义 + 状态推进 + 提交基线 | 无（AI 自动，写回已确认节点） | 统一 | 22 写回 + 06 定义 + 05/02 状态推进 | `22/06/05/02` |
| End | 运行结束输出 | 三区摘要输出 + 等待人类反馈 | **有**——查看摘要、对话反馈 | 对话 | 人类下一步操作提示 | — |

> **执行模式白话**：统一 = AI 自动跑完这步，不需你介入；对话 = AI 输出后需要你说句话（反馈或确认）才继续。Step 1/3/4/5/6 为运行内自动步骤；Step 2 / Step End 为人机交互步骤（对话反馈，见 §3.3）。

**反馈机制一句话**：反馈走**自然语言对话**（AI 产出方案后不结束对话，§3.3）；跨轮余留反馈由 AI 标 `[修改]`/`[驳回]` 入"待反馈处理"分节（§3.3 状态机）；人类线下修订经 Step Start 变更感知（git diff，§3.1 变更感知）检出后纳入"待反馈处理"分节；AI 每轮写回提交基线（§3.6），保证下一轮 Step Start 的 `git diff <HEAD @上次 AI 运行>..HEAD` 只含人类变更。

### 3.1 Step Start

**变更感知（检出人类线下修订，先于入口判定）**

读取目标文件前先检测自上次 AI 运行以来人类是否修改过文件（[§11.10](91-eos-biz-eng-spec.md#1110-ai-反馈检测规则)）：

1. 读取文件头 `**HEAD @上次 AI 运行**` 字段，获取上次提交 hash H
2. Bash("git diff H..HEAD -- 目标文件") → 返回 H 到当前 HEAD 之间的全部变更
3. 无变更 → 进入入口判定
   有变更 → 解析变更行号，读取变更行附近的节点块（上下 5 行）
   → 判定变更类型：结构化标注（`[同意]`/`[修改]`/`[驳回]`）→ 纳入待反馈；非标自由文本 → 标记 `[需确认]`；无关修改（格式/排版）→ 忽略
   → 将存在反馈的节点加入AI可以处理节点"待反馈处理"分节

> 变更感知必须先于入口判定——人类纯线下修订（不选新 bph-nfr）时，只有 git diff 能发现该反馈并将其纳入"待反馈处理"分节，入口判定②才命中。若入口判定先于变更感知，纯线下修订会被误判"无待处理对象"退出。

**入口判定**（在变更感知后执行）：按 [§A.4](91-eos-biz-eng-spec.md#a4-入口条件与优先级step-start) 入口决策流程执行。先读取 `02-*.md`/`05-*.md` 的 `## AI可以处理节点` 分节（[§11.7](91-eos-biz-eng-spec.md#117-ai可以处理节点)），获取三类入口的节点列表，再按以下顺序处理：

#### 3.1.1 输入校验

| 校验条件 | 判定 | 动作 |
|---------|------|------|
| bph-nfr 状态不是 `可以NFR设计` / `待补充NFR设计` | 状态不符（硬退出） | 输出不合格节点及当前状态 → **退出** |
| bph-nfr 分类边界或角色语境明显缺失 | 要素缺失（硬退出） | bph-nfr → `需wft01修订`，输出缺失说明 → **退出** |
| 选中 bph-nfr >5 个 | 超建议阈值（软提示） | 输出超限提示 → 人类确认后继续或退出 |
| 无任何入口条件满足 | 无待处理对象（硬退出） | 输出”无待处理对象”→ **退出** |

> 软提示不强制退出，由人类选择继续或退出；硬退出为逻辑上不可能的条件，输出后退出（§A.4.5）。

#### 3.1.2 退回优先（§A.4.3 门禁）

存在 `需wft04修订` 的 SysReq-NFR 节点（来自”待下游退回处理”分节）：
- 读取退回节点的缺失说明，检查其中是否含人类标注（`[修改]` / `[同意]`）或下游 Skill 产出的具体改进指引
- 已提供 → 通过门禁，定位受影响 SysReq-NFR，进入 Step 2 按人类方案修订
- 未提供（仅含概括性退回原因或无人类标注）→ 输出缺失说明 + 提示提供改进方案 → **退出**

#### 3.1.3 反馈处理

存在”待反馈处理”分节中的 SysReq-NFR 节点（确认状态 = `[修改]`/`[驳回]` 且无 `[已处理]`）→ 进入 Step 2 处理（§3.3）。若全部条目已 `[同意]`+`[已处理]`，不阻塞新 bph-nfr，Step End 时提示。

#### 3.1.4 新 SysReq-NFR 路由

有未处理的 bph-nfr 节点（来自”待 wft04-nfr 处理”分节）→ 进入 Step 1→Step 3，逐节点执行 Phase A 消费与变更感知，匹配已有 SysReq-NFR 宿主（§3.4.1）或进入待聚合池新建。

**顺序约束**：Step 2（反馈处理）必须先于 Step 3（新增设计），因为反馈可能改变 SysReq-NFR 的量化指标/分层约束/页面 NFR。Step 3 的 Phase C 宿主匹配必须基于 Step 2 修改后的最新 SysReq-NFR 内容，不得使用缓存旧状态。此顺序与 §A.4.4 规则三一致（退回优先 → 反馈处理 → 新输入路由）。

### 3.2 Step 1 · 设计材料加载

加载产品数据与资产文档遵循 [91 规范 §A.3.3](91-eos-biz-eng-spec.md#a33-产品数据文档与资产文档读取与写回约定) 的统一读取协议：AI可以处理节点优先 → 索引导航。变更感知已在 Step Start 完成（§3.1）。

**步骤一：安全复查**

Step Start 已检出人类反馈并纳入"待反馈处理"分节（§3.1 变更感知）。此处按 §A.3.3 R0a 做加载前安全检查：`detect-changes.sh` 检出人类非预期修改时标记 `[需确认]`，不自动推进。

**步骤二：优先读取**（通过 `## AI可以处理节点` 定位本轮入口）：

入口判定通过后（§3.1.1），从 `## AI可以处理节点` 对应分节获取待处理节点 ID 列表：
- 有新输入 → 读「待 wft04-nfr 处理」分节
- 有待处理反馈 → 读「待反馈处理」分节
- 有下游退回 → 读「待下游退回处理」分节

逐节点：Bash("grep -n '@{节点ID}' 目标文件") 定位行号 → Read 节点块。

**步骤三：索引导航**（AI可以处理节点无待处理时，或按域浏览时）：

1. Bash("grep -n '索引行数：' 目标文件") → 定位索引行号 L
2. 从 L 提取 N → Read 业务域索引
3. 按锚点 Read 域内索引 → 获取节点 ID 列表
4. 按需：Bash("grep -n '@{节点ID}' 目标文件") → 定位行号 → Read 节点

**加载清单**（按需扩大 - 仅在索引缺失、候选冲突、资产全貌不足以判断边界或追溯关系断裂时）：

1. `02-*.md` STR 树画像 / 状态队列 / 节点索引 — 定位本轮 bph-nfr 正文块（含角色语境、分类需求清单、适用对象线索）
2. `05-*.md` SysReq 树画像 / 状态队列 / 节点索引 — 定位已有 SysReq-NFR 节点块，同时读取 srh-biz 节点 ID 清单和页面/功能线索
3. `06-*.md` 系统需求详细定义索引 — 定位写入目标
4. `22-eos-nfr-taxonomy.md` — NFR 分类维度/具体需求/候选条目清单（运行时动态读取，不缓存）
5. `25-eos-engine-models.md` — 引擎/CU 类型/布局组件/构件和页面生成线索（只读）
6. `91-eos-biz-eng-spec.md` — 页面类型、辅助栏、分屏下钻、CU/构件关系和通用协议

**按需扩大规则**（[91 规范 §11.12](91-eos-biz-eng-spec.md#1112-按需读取规则)）：

- bph-nfr / SysReq-NFR 正文块 — 按 ID 从索引定位
- 候选 SysReq-NFR 块 — 按同 22 分类维度/同约束对象/同指标口径筛选，用于宿主匹配
- 反馈重入 — 仅读取条目的确认状态字段及反馈指向块，不做全量重读
- 扩大触发：索引缺失 / 候选冲突 / 资产全貌不足以判断边界 / 追溯关系断裂时

> 禁止行为：不得在常规 Step 运行中默认加载完整文件正文；不得将 `疑似` 或 `待确认` 节点的 ID 作为下游强依赖（§A.3.3 R3）。

### 3.3 Step 2 · 方案反馈处理

**一句话**：处理人类反馈——对话内即时处理（修改 / 整体确认，§3.3.1），跨轮余留按 `[修改]`/`[驳回]` 标记处理（§3.3 状态机）。

人类审阅与 AI 处理的通用协议见 [91 规范 §A.5](91-eos-biz-eng-spec.md#a5-反馈交互协议)（反馈通道双轨：对话 + 线下修订）。以下为本 Skill 的特化规则。

**反馈双轨**——人类可任选两种方式反馈，本 Skill 均支持：

- **AI 对话反馈**：Step End 后不结束对话，直接在对话中提出修改意见（自然语言）——AI 按 §3.3.1 即时处理（主通道）
- **线下文档修订**：直接打开 `05-*.md`/`06-*.md` 修订——在确认状态字段标注 `[同意]`/`[修改]`/`[驳回]`，或直接编辑方案内容（自由文本）——经 Step Start 变更感知（git diff，§3.1 变更感知）检出后按 §A.5 处理

两轨等价：对话反馈由 AI 即时落账（追 `[已处理]`），线下修订经 git diff 检出后同样落账；AI 每轮写回提交基线（§3.6），保证下一轮 diff 只含人类变更。

**本 Skill 的条目**：量化指标 + 分层约束表行 + 页面 NFR + 22 写回项 + 06 详细定义。各条目的 `确认状态` 字段位于 `05-*.md` SysReq-NFR 节点块各条目正文中。

**确认状态标记状态机**（本 Skill 反馈落账标记，定义单源；对话反馈写入者=AI，线下标注写入者=人类——经 git diff 检出后按 §A.5 处理）：

| 标记 | 含义 | 写入时机 | 转迁 |
|------|------|---------|------|
| `(空)` | 条目待人类反馈 | Step 3~4 产出时初始化 | 人类反馈后按情况转下 |
| `[修改]` | 人类提出修改意见、当轮未落地 | AI 在运行结束仍有余留修改意见时写入（跨轮持久化） | 下轮 Step 2 处理完追 `[已处理]` |
| `[驳回]` | 人类否决该条目方案、当轮未落地 | 同上 | 同上 |
| `[同意]` | 人类确认该条目 | "整体确认"时 | 随 `[已处理]` 一并追加 |
| `[已处理]` | 反馈已处理完毕（git 可追溯） | 处理完成后 | 默认跳过；满足清除条件（§3.3.1）时回到待处理 |

> 当轮对话内的反馈由 AI 即时处理（§3.3.1），不经 `[修改]`/`[驳回]` 态；`[修改]`/`[驳回]` 只在运行结束仍有余留反馈时出现，供下一轮 Step Start 检出（§3.1.3）。

**跨轮反馈持久化**：人类反馈（对话为主，§3.3.1）由 AI 当轮处理并追 `[已处理]`；若当轮对话结束仍有未落地的修改/驳回意见，AI 将对应条目标记 `[修改]`/`[驳回]` 并加入"待反馈处理"分节 → 下一轮 Step Start 检出（§3.1 变更感知）、Step 2 处理。AI 每轮写回后提交基线（§3.6），保证下一轮 Step Start 的 `git diff <HEAD @上次 AI 运行>..HEAD` 只含人类变更。

#### 3.3.1 AI 处理流程

按 [91 规范 §A.5](91-eos-biz-eng-spec.md#a5-反馈交互协议) 的统一流程处理人类反馈：扫描待处理条目 → 收集整体确认 → 逐条处理 `[同意]`/`[修改]`/`[驳回]` → 级联检查（需求优先、资产对齐随后）→ 判定节点状态 → 确定写回时机 → 落反馈处理记录。

| 人类输入 | AI 动作 |
|---------|--------|
| 显式修改指令（"X 改为 Y"） | 定位条目 → 理解修改意图 → 执行修改 → 检查是否仍满足 bph-nfr 需求 → 追加 `[已处理]` → 输出修改摘要 → 继续等待 |
| 质疑/讨论（"为什么选 X？"） | 解释理由，不自动修改，继续等待决策 |
| 整体确认（"整体确认"/"可以"） | 全部条目追加 `[同意] [已处理]` → 总结反馈要点写入 `## AI最近变更` → 满足冻结条件则推进 SysReq-NFR → `已验证冻结` → **提交基线并更新 HEAD（机制见 §3.6，防下轮把 AI 自写当人类变更）** |
| 模糊意见（"粒度太粗"） | 追问具体化，引导可执行指令 |
| 线下修订检出（git diff 发现确认状态标注/自由文本编辑） | 定位条目 → Edit 修订 → 追加 `[已处理]` → 输出修改摘要 → 继续等待 |

> **原则**：反馈处理不是对人类标注的机械执行，而是以 bph-nfr 分类需求清单为锚点的方案修订。AI 在 Step 2 中与 Step 3~4 同为设计者——先理解人类反馈在 bph-nfr 需求语境中的含义，再执行修订，最后检查修订后的 SysReq-NFR 是否仍满足 bph-nfr 需求。

**本 Skill 反馈处理特化参数**（[91 规范 §A.5.4](91-eos-biz-eng-spec.md#a54-其他规则) 要求各 Skill 定义以下参数）：

| 参数 | 本 Skill 值 |
|------|-------------|
| **锚点需求** | 本 Skill 的锚点是 bph-nfr 分类需求清单——反馈处理以 bph-nfr 需求理解为锚，不脱离上游需求内容自行修订 |
| **操作条目** | 量化指标 + 分层约束表行 + 页面 NFR + 22 写回项 + 06 详细定义。各条目的 `确认状态` 字段写入 `05-*.md` SysReq-NFR 节点的对应条目中 |
| **推进目标** | 全部条目含 `[同意]`+`[已处理]` → SysReq-NFR → `已验证冻结`，bph-nfr → `已NFR设计`。存在 `[修改]`/`[驳回]` 且无 `[需重设计]` → 保持 `待确认方案`。存在 `[需重设计]` → 保持 `待确认方案`，需进入 Step 3~4 重做 |
| **级联触发条件** | 量化指标或被驳回/修改导致指标口径变更、分层约束拓扑变化或 22 判定变更 → 触发需求级联检查（重验修改后 SysReq-NFR 是否仍满足 bph-nfr 需求）→ 通过后触发资产对齐级联 |
| **资产对齐级联** | 方案变更影响 22/06 资产时，更新对齐结论——具体需求判定变更 → 更新 22 引用；指标口径变更 → 更新 06 详细定义。资产映射由 Step 6 维护 |

**处理规则**：

1. 从当前轮次对话上下文或退回缺失说明中读取人类反馈意图
2. **逐条处理**（按人类反馈内容）：
   - AI 输出方案后人类直接回复修改意见 → 在当前轮次继续对话处理，不重新走 Step Start
   - wft05 退回的 `需wft04修订` → 下一轮 Skill 调用时走 Step Start 退回优先
3. 修改后检查是否仍满足该 SysReq-NFR 所承接的 bph-nfr 需求
4. 修改导致结构性变更（量化指标/分层约束/页面 NFR 重组）→ 按需重入 Step 3~4
5. 全部条目获流水线开发者认可 → SysReq-NFR → `已验证冻结`

按 [91 规范 §A.5.3](91-eos-biz-eng-spec.md#a53-ai-处理流程) 的流程处理人类自然语言反馈：

| 人类输入 | AI 动作 |
|---------|--------|
| 显式修改指令（"X 改为 Y"） | 定位条目 → 理解修改意图 → 执行修改 → 检查是否仍满足 bph-nfr 需求 → 追加 `[已处理]` → 输出修改摘要 → 继续等待 |
| 质疑/讨论（"为什么选 X？"） | 解释理由，不自动修改，继续等待决策 |
| 整体确认（"整体确认"/"可以"） | 全部条目追加 `[同意] [已处理]` → 总结反馈要点写入 `## AI最近变更` → 推进 SysReq-NFR 至 `已验证冻结` → **提交基线并更新 HEAD（机制见 §3.6，防下轮把 AI 自写当人类变更）** |
| 模糊意见（"粒度太粗"） | 追问具体化，引导可执行指令 |
| 线下修订检出（git diff 发现确认状态标注/自由文本编辑） | 定位条目 → Edit 修订 → 追加 `[已处理]` → 输出修改摘要 → 继续等待 |

#### 3.3.3 反馈总结

人类「整体确认」后，AI 将本轮反馈要点总结为一句话，通过 `update-meta.sh add-recent-change` 写入 `05-*.md` 的 `## AI最近变更` 表：

```bash
bash ../scripts/update-meta.sh ../../80-pl4eos-2-eosdata/05-eos-system-requirement-summary-architecture.md add-recent-change "wft04-nfr" "反馈处理" "<SysReq-NFR-ID>" "<AI总结的反馈要点>"
```

摘要示例：`量化口径修正：P95 响应时间统计口径改高峰期100并发压测；分层约束补采购履约看板窗口标签页 ID`

#### 3.3.2 确认状态格式示例

**AI 写入（Step 3~4 完成时）** —— 为每个条目初始化 `确认状态` 字段，初始为空：

```text
- 量化指标：列表查询 P95 响应时间 ≤ 2000ms（统计口径：高峰期100并发，测量方法：压测） | 确认状态: (空)
- 分层约束表行：页面级 / biz / 窗口标签页 / @page-procurement-list / 页面性能 / 阻断型 | 确认状态: (空)
- 页面 NFR：NFR-PAGE-001 / 窗口标签页 / @page-procurement-list / 首屏加载 / ≤3s（P95）/ 压测 / 页面性能 / wft05-eng / (空)
- 22 写回项：新增 @nfr-001-REQ-001 页面查询响应时间 | 确认状态: (空)
- 06 详细定义：SRD-NFR-PERF-001 采购履约页面性能要求 | 确认状态: (空)
```

**人类在条目 `确认状态` 字段标注** —— 三种标注形式：

```text
- 量化指标：列表查询 P95 响应时间 ≤ 2000ms（统计口径：高峰期100并发，测量方法：压测） | 确认状态: [同意]
- 分层约束表行：页面级 / biz / 窗口标签页 / @page-procurement-list / 页面性能 / 阻断型 | 确认状态: [修改]：应补充采购履约看板对应的窗口标签页 ID
- 页面 NFR：NFR-PAGE-001 / 窗口标签页 / @page-procurement-list / 首屏加载 / ≤3s（P95）/ 压测 / 页面性能 / wft05-eng / [驳回]：已在 SysReq-NFR-PERF-002 中覆盖
- 22 写回项：新增 @nfr-001-REQ-001 页面查询响应时间 | 确认状态: [同意]
- 06 详细定义：SRD-NFR-PERF-001 采购履约页面性能要求 | 确认状态: [修改]：验收条件应补充"高峰期100并发"场景
```

**AI 处理完成后** —— 在人类标注末尾追加 `[已处理]`：

```text
- 量化指标：列表查询 P95 响应时间 ≤ 2000ms（统计口径：高峰期100并发，测量方法：压测） | 确认状态: [同意] [已处理]
- 分层约束表行：页面级 / biz / 窗口标签页 / @page-procurement-list / 页面性能 / 阻断型 | 确认状态: [修改]：应补充采购履约看板对应的窗口标签页 ID [已处理]
- 页面 NFR：NFR-PAGE-001 / 窗口标签页 / @page-procurement-list / 首屏加载 / ≤3s（P95）/ 压测 / 页面性能 / wft05-eng / [驳回]：已在 SysReq-NFR-PERF-002 中覆盖 [已处理]
- 22 写回项：新增 @nfr-001-REQ-001 页面查询响应时间 | 确认状态: [同意] [已处理]
- 06 详细定义：SRD-NFR-PERF-001 采购履约页面性能要求 | 确认状态: [修改]：验收条件应补充"高峰期100并发"场景 [已处理]
```

### 3.4 Step 3 · 非功能需求架构设计

设计规则来源：本节 Phase A~C 引用的提取规则见 §2.4、量化推断规则见 §2.5、宿主匹配规则见 §2.4.3、变更声明见 §2.7。

#### 3.4.1 Phase A：bph-nfr 消费感知与宿主匹配

逐 bph-nfr 检查消费版本——首次消费→全量设计，版本递增→按变更声明执行增量/修订/结构型分流；分类边界或角色语境不足→退回 wft01-nfr。版本确认后，按 22 分类维度 + 约束对象 + 指标口径匹配已有宿主：

| 条件 | 路由 |
|------|------|
| 已有 SysReq-NFR 分类维度 + 约束对象 + 指标口径兼容 | 归入该宿主 → Phase B |
| 不满足 | 进入待聚合池 → Phase C |
| 多个宿主均可承载或语义不内聚 | 标记 `[需裁决]` |

待聚合池按**同一 22 分类维度 + 同一约束对象类型**分组（`{22分类维度, 约束对象类型, bph-nfr列表}`）。此阶段仅粗略分组，量化指标生成和完整要素提取在 Phase C 执行。

#### 3.4.2 Phase B：修订已有 SysReq-NFR

1. **要素提取**：从 bph-nfr 分类需求清单提取待量化线索、适用对象线索和 22 分类路径。已有指标口径仅追加不重定
2. **宿主匹配检查**：确认 bph-nfr 需求仍在 SysReq-NFR 宿主范围内。Q1→追加指标和追溯；Q2→修订宿主+变更声明；宿主冲突→标记 `[需裁决]`。Phase A 匹配是候选筛选、Phase B 是精确匹配——宿主扩展时不得回退 Phase A 重选宿主，继续完成 Phase B 并标记 `[推断]` 供人类裁决
3. **重判定与关系检测**：基于全部 bph-nfr 重新量化指标，比对已有基线判定六种关系（§2.5.3）。新增/变更指标→更新分层约束表行；影响页面体验→生成页面 NFR；22 判定变更→更新具体需求归属
4. **变更影响声明**：判定变更类型（无变更/增量型/修订型/结构型），写入 SysReq-NFR 节点末尾

#### 3.4.3 Phase C：新建 SysReq-NFR

1. **确定 22 分类路径** → 一级类目 → 分类维度 → 具体需求归属判定
2. **生成量化指标** → 逐需求项提取待量化线索 → 匹配 22 模板和同类场景 → 目标值/单位/统计口径/测量方法/验收条件。信息不足时标注 `待确认`，不伪造确定值
3. **判定分层约束** → 六类约束范围 → 适用链路 → 约束对象 → 消费目标 → 影响级别 → 影响点
4. **生成页面 NFR** → 影响用户可感知页面行为时绑定页面类型/srh-biz/业务域 → 约束内容 → 量化目标 → 测量方法 → 消费目标
5. **装配分层约束包** → 生成唯一的 wft05-eng NFR 约束包，按全局/业务域/srh-biz/页面级/CU/pa-eng 组件/共享七组独立组织，引用分层约束表行
6. **组装 SysReq-NFR** → 22 分类路径 + 量化指标清单 + 分层约束表 + 页面 NFR + 22 写回项 + 06 引用，写入 `05-*.md`

### 3.5 Step 4 · 页面与分层约束设计

Step 4 按每个 SysReq-NFR 实例独立执行。新建 SysReq-NFR → 全量设计；修订已有 SysReq-NFR → 仅当量化指标或约束范围变更时重验分层，要素机械检查始终针对变更节点执行。

**1. 分层约束表生成**

基于 Step 3 产出的量化指标，对每条指标按 §2.4.2 的约束范围判定表判定分层（全局/业务域/srh-biz/CU/pa-eng组件/页面级）。每条分层约束记录约束行 ID、约束范围、约束对象、量化指标引用、影响级别和消费目标。无法确定约束范围时标注 `[推断]`。

**2. 页面 NFR 生成**

当指标影响用户可感知页面行为时，按页面线索逐条生成页面 NFR（详见 §2.3）：

| 页面线索 | 输出 |
|----------|------|
| 页面类型明确 | 绑定七种页面类型之一，说明约束行为 |
| srh-biz 明确 | 引用具体 srh-biz，并绑定功能表单/窗口标签页/配置页面等具体页面 ID |
| 只知道业务域 | 写业务域级页面约束，标记 `[需裁决]` |
| 影响辅助栏/分屏/下钻/面包屑 | 明确关联 91 的交互模型要素 |
| 影响权限/审计/安全提示 | 同时生成页面约束和 eng 权限/审计约束 |

每个页面 NFR 写入九项：指标 ID / 页面类型 / 具体页面 ID（或待目标绑定）/ 约束内容 / 量化目标 / 测量方法 / 影响点 / 消费目标 / 确认状态。所有页面 NFR 标注 `[推断]`，经由人类确认后方可落账。

**3. wft05-eng NFR 约束包装配**

Step 4 末尾生成一个且仅一个 wft05-eng NFR 约束包，只引用分层约束表行 ID 和页面 NFR 条目，不复制正文。分解后的子指标（§2.4.1）作为独立约束行装配，标注「分解运算 + 父指标引用」。`适用链路=biz/eng/共享` 表示约束影响的业务、平台或共享语境，不表示 NFR 直接流向 biz 路径。

约束包头必须记录：`约束包 ID`、`包版本 vN`、`生成时间`、`来源 SysReq-NFR ID/版本`、`上一版本`、`变更类型（首版/无变更/增量型/修订型/结构型）`、`新增行`、`修订行`、`删除/失效行`、`消费目标=wft05-eng`、`消费状态（待消费/已消费/需重消费）`、`wft05-eng 已消费版本`。消费状态与已消费版本由 wft05-eng 消费后回写（本 Skill 发布时写 `待消费`，不自行置 `已消费`）。

约束包正文按以下七组分别列示，不合并业务域与 srh-biz：

1. 全局约束
2. 业务域约束
3. srh-biz 约束
4. 页面级约束（页面对象 + 指标 ID + 约束行 ID）
5. CU 约束（目标引擎 + CU 类型 + 指标 ID + 约束行 ID）
6. pa-eng 组件约束（组件类型 + 指标 ID + 约束行 ID）
7. 共享约束

首版包版本为 `v1`、变更类型为 `首版`、消费状态为 `待消费`。后续包版本必须递增并声明相对上一版本的新增、修订、删除/失效行；若 wft05-eng 已消费旧版且存在有效变化，消费状态写为 `需重消费`。

**首版发布门禁**：全局/共享约束可以没有具体对象 ID；业务域约束必须绑定业务域；bph-biz/bph-eng/CU/页面级约束必须绑定已存在的目标 ID。pa-eng 组件级约束在 PA 尚未生成时只记录组件类型与映射条件，标记 `待wft05映射`，不得虚构 PA ID。存在未满足上述条件的阻断型约束时，SysReq-NFR 保持 `待目标绑定`，不得发布首版约束包。

**4. 要素机械检查**（逐 SysReq-NFR，发现即标注——在对应条目中写入 `[覆盖缺口]` 及缺失说明，留待下一轮补全）

- [ ] 每条量化指标的约束范围 **已判定**，约束行 ID/约束对象/影响级别/消费目标 **已记录**
- [ ] 影响用户可感知页面行为的指标 **已生成**页面 NFR
- [ ] 每个页面 NFR 的九项要素 **齐全**
- [ ] 下游约束包装配视图 **已生成**，只引用行 ID 不复制正文
- [ ] wft05-eng 消费包覆盖的约束范围 **无遗漏**
- [ ] 约束包 ID、版本、上一版本、变更类型、增量清单和消费状态 **已记录**
- [ ] 七类约束组 **分别列示**，业务域与 srh-biz 未合并
- [ ] 阻断型目标级约束 **已完成目标绑定**，不存在虚构的 bph-eng/CU/srh-biz/pa-eng ID

### 3.6 Step 5 · 闭环检查与变更声明

Step 5 按每个 SysReq-NFR 实例独立执行。新建 SysReq-NFR → 全量检查；修订已有 SysReq-NFR → 仅当量化指标、约束范围或页面 NFR 变更时重验闭环，要素机械检查始终针对变更节点执行。

**1. 闭环检查**

对每个 SysReq-NFR 节点，按七项逐条检查，发现遗漏优先当场补齐，无法补齐时标记 `[需裁决]` 或退回 wft01-nfr——在对应条目中写入 `[覆盖缺口]` 及缺失说明，留待下一轮补全：

- [ ] 量化完整：全部 bph-nfr 分类需求项已生成量化指标，或明确标注待确认缺口
- [ ] 宿主唯一：每条 bph-nfr 分类需求项有唯一 SysReq-NFR 宿主
- [ ] 分层正确：全局/业务域/srh-biz/CU/pa-eng组件/页面级未错层
- [ ] 页面可消费：影响页面体验的 NFR 已生成页面级约束
- [ ] eng 一致：不同引擎/组件间共享指标保持同一指标 ID、目标值和测量方法
- [ ] 22 可落账：具体需求写回项指标集完整、分类路径明确
- [ ] 06 可验证：详细定义包含来源、指标、测量、验收和追溯

**2. 缺口处理**

| 类型 | 典型场景 | 动作 |
|------|---------|------|
| 可当场补齐 | 指标定义不全 / 约束范围未判定 / 页面 NFR 缺失 | 本 Skill 内补齐后重验 |
| 根因在上游 | bph-nfr 分类边界不清 / 角色语境缺失 / 适用范围不足 | 退回 wft01-nfr 为 `需wft01修订`，携带缺失说明 |
| 需人类裁决 | 目标值无法推断 / 约束层级归属有争议 | 标记 `[需裁决]`，不进入 Step 6 写回 |
| 不可脑补 | 任何无法确定的缺口 | 缺口就是缺口——不虚构节点或指标值 |

**3. 要素机械检查**（逐 SysReq-NFR，发现即标注——在对应条目中写入 `[覆盖缺口]` 及缺失说明，留待下一轮补全）

- [ ] 七项闭环检查 **已执行**，遗漏项已标注 `[覆盖缺口]`
- [ ] 缺口处理 **已分流**，无未裁决的边界情况
- [ ] SysReq-NFR 与 wft05-eng NFR 约束包版本号 **已递增**（修订时），首版均为 v1

**4. 变更影响声明**

每次新建或修订 SysReq-NFR 后写入变更影响声明；首版写"首版，无变更"。修订时按 §2.7 判定变更类型（无变更/增量型/修订型/结构型），列出受影变量化指标、页面 NFR、约束包版本变化、相对上一包版本的增量清单和 wft05-eng 建议动作。格式见 §2.7。

### 3.7 Step 6 · 资产落账与状态推进

遵循 [91 规范 §11.11](91-eos-biz-eng-spec.md#1111-ai-写回规则) 的统一写回协议。仅写回人类已在 Step 2 确认的 SysReq-NFR 条目。不设二次确认。

**写回资产操作**：

1. 遍历已确认 SysReq-NFR，按分类路径写回 22 资产：
   - 复用 → 定位已有条目，追加反向引用（去重）
   - 升级 → 扩展已有条目，追加引用 + 扩展指标集
   - 新增 → 新建条目，写入具体需求与指标集
2. `06` 写入 NFR 系统需求详细定义（来源 bph-nfr / SysReq-NFR 宿主 / 指标 / 测量方法 / 验收条件 / 分层约束表引用 / 22 资产引用）
3. `05` SysReq-NFR → `已验证冻结`，记录 22/06 引用和 bph-nfr 消费版本
4. `02` 对应 bph-nfr 节点写入 `bph-nfr 消费版本：v{N}`，状态推进为 `已NFR设计`；若仅部分增量完成，保留待补充说明
5. 写入/更新 wft05-eng NFR 约束包头、七组装配视图和消费状态；首版标记 `待消费`，后续有效变化且旧版已消费时标记 `需重消费`
6. 写入资产写回记录（日期/来源 bph-nfr/目标 SysReq-NFR/动作/消费版本/约束包版本/下游状态）

**写回时的同步维护**（[91 规范 §11.11](91-eos-biz-eng-spec.md#1111-ai-写回规则) 要求每次写回同时维护以下内容）：

| 操作 | 同步更新 |
|------|---------|
| 新增/修改节点 | 更新域产品索引的 `节点数`；更新 `## AI可以处理节点` 对应分节——节点状态变更时移入或移出 |
| 修改节点状态 | 更新AI可以处理节点对应分节：状态符合消费条件时加入，不符合时移出 |
| 新增/删除子条目 | 更新父节点 `**子条目状态**` 汇总值 |
| 人类标注处理 | 同步更新父节点 `**子条目状态**`（子条目标注时）和 `**确认状态**`（父节点标注时）；AI可以处理节点"待反馈处理"同步 |

**提交基线**（链级不变式，每次写回后必须执行；顺序对齐 wft01-eng §5.6，AI 最近变更 随基线入库）：

1. 在 `## AI最近变更` 表追加一条记录（[91 规范 §11.6](91-eos-biz-eng-spec.md#116-ai最近变更)），含时间/操作 Skill/操作类型/节点ID/变更摘要，操作类型列含 `[→](#节点ID)` 超链接
2. 保留 AI最近变更最近 30 条，超出时从最旧行滚动删除
3. `git add -A` + `git commit`（带 Co-Authored-By: Claude），把本轮 AI 产出（SysReq-NFR 节点、22/06 写回、AI 可以处理节点维护、AI 最近变更）全部落基线——链级不变式：基线 = 最后 AI 提交，下一轮 Step Start 的 `git diff <HEAD @上次 AI 运行>..HEAD` 只含人类变更。反馈确认后的补充提交（§3.3.1 整体确认）同此机制
4. 更新文件头 `**文档版本**`（递增次版本号）、`**修订日期**`、`**HEAD @上次 AI 运行**`（=刚提交的基线 hash）

**写回要求**：幂等去重、局部写回、全貌同步。所有操作在本 Step 内完成，不允许"本次写节点、下次更新索引"。资产写回失败时不得推进状态。

说明：22/06 资产落账是独立收尾，不替代约束包契约；已确认的首版 wft05-eng NFR 约束包仍是 wft05-eng 首次启动的前置输入。

### 3.8 Step End · 运行结束输出

Step End 遵循 [91 规范 §A.3.2](91-eos-biz-eng-spec.md#a32-step-end-输出模板三区结构) 的三区输出模板（结果→方案反馈→下一步）。本 Skill 实例化如下：

```text
当前状态：<待确认方案 / 已验证冻结>
  本轮 SysReq-NFR：<SysReq-NFR-ID / 新建或修订 / 版本变化 / 来源 bph-nfr>
  NFR 约束包：<约束包 ID / 包版本 / 变更类型 / 消费状态 / wft05-eng 已消费版本>
资产落账：<未落账 / 已写回 22/06/05/02>

一、方案反馈
  直接回复修改意见即可，例如：
    - "响应时间指标补测量口径"
    - "整体确认"（快捷同意全部未标注条目）
    - "分层约束调整粒度"
  审核锚点：量化指标是否可验证、分层约束是否正确、页面 NFR 是否可消费、22/06 归属是否合理
  可用性锚点：量化指标是否可验证（单位/统计口径/测量方法/验收条件）？追溯链（bph-nfr→SysReq-NFR→22/06）是否可追踪？分层约束是否足以指导 wft05-eng 展开架构设计？

二、下一步
  本Skill → 选择 bph-nfr 节点（`可以NFR设计` / `待补充NFR设计`）或 `需wft04修订` 的 SysReq-NFR 节点重新运行
  后续    → wft05-eng 仅在首版约束包已确认时首次启动；后续包版本递增时按变更声明增量重入
```

**输出后不要结束对话**，等待人类反馈——人类可直接用自然语言回复修改意见（§3.3.1）：

> 以上是本轮方案。直接回复修改意见即可：
> - "量化指标的口径改高峰期100并发压测"
> - "整体确认"
> - "分层约束缺采购履约看板的窗口标签页 ID，补一下"

---

## 四、AI 标记约定

四类增量标记（`[新增]`/`[变更]`/`[确认]`/`[需裁决]`）的定义及使用规则见 [91 规范 §A.3.1](91-eos-biz-eng-spec.md#a31-ai-增量标记)。

每次 Step 3 完成后，向人类输出一份增量清单，格式示例如下：

```text
=== SysReq-NFR-PERF-001 本轮更新清单 ===

[新增] 承接 bph-nfr-NFR-PERF-001（消费版本 v1）

量化指标
  [新增] 列表查询 P95 响应时间 ≤ 2000ms（统计口径：高峰期100并发，测量方法：压测）  资产对齐：22=新增具体需求

分层约束
  [新增] 页面级 / biz / 窗口标签页 / @page-procurement-list / 页面性能 / 阻断型
  [新增] pa-eng组件 / eng / 前端业务能力支持组件 / 运行约束机制 / 非阻断型

完备性检查
  量化完整 ✓ / 宿主唯一 ✓ / 分层正确 ✓ / 页面可消费 ✓
  → [需裁决] 批量导入期间查询响应目标值缺少业务可接受阈值（请确认 P95 目标或允许先标为待确认）
```

---

## 五、自检清单

**一句话**：每次运行收尾前按此清单逐项自查——AI 按 Step 分组核对是否漏做、漏写、漏提交。

**前置与加载（Step Start / 1）**
- [ ] Step Start 已变更感知（git diff，基线前提已满足）并判定变更类型，线下修订节点已纳入"待反馈处理"分节
- [ ] 输入校验通过：bph-nfr 状态符合、分类边界/角色语境完整、数量未超阈值
- [ ] Step 1 已安全复查（§A.3.3 R0a）并判定非预期修改
- [ ] 已加载 bph-nfr / 05 / 22 分类资产 / 06 / srh-biz 页面线索，未默认加载全文件正文
- [ ] "待反馈处理"分节条目已处理（`[修改]`/`[驳回]` 无 `[已处理]`）

**方案反馈处理（Step 2）**
- [ ] 对话反馈已按 §3.3 处理：修改指令 → 检查仍满足 bph-nfr → 追加 `[已处理]`；整体确认 → 推进 `已验证冻结` + 提交基线
- [ ] 跨轮余留反馈已按状态机标记 `[修改]`/`[驳回]` 并加入"待反馈处理"分节
- [ ] 反馈总结已写入 `## AI最近变更`（整体确认后）

**非功能需求架构设计（Step 3）**
- [ ] Phase A 消费感知与宿主匹配已执行（首版全量 / 版本递增分流 / 分类边界退回）
- [ ] 量化指标已展开（单位/统计口径/测量方法/验收条件，目标值缺失标待确认不伪造）
- [ ] 22 具体需求候选已生成，资产对齐判定完成
- [ ] 变更影响声明已写入

**页面与分层约束设计（Step 4）**
- [ ] 分层约束表已生成（页面级 / biz / pa-eng组件 分层正确）
- [ ] 页面 NFR 已定义（含适用页面/指标/验收/下游消费方）
- [ ] 下游约束包已生成（约束包 ID/版本/变更类型/消费状态）

**闭环检查（Step 5）**
- [ ] 量化完整 / 分层正确 / 页面可消费 / 资产可落账 四项检查已执行

**资产落账与状态推进（Step 6）**
- [ ] 22/06 写回按判定分流执行，幂等去重
- [ ] 05/02 状态已推进，引用链已记录
- [ ] AI 最近变更表、AI 可以处理节点分节已维护
- [ ] 提交基线已执行（AI最近变更 → 滚动删除 → git commit → update-head）

**运行结束（Step End）**
- [ ] 三区摘要已输出，对话等待反馈；整体确认后已提交基线

---

## 六、变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-08-24 | v5.9 | 缩写更名（人类定案）——sfh→srh、sfd→srd（sr=系统需求）+ 中文层名统一（系统需求概要/详细定义）；资产名/节点ID/状态名/文件名随迁。 |
| 2026-08-24 | v5.8 | 资产名更名（人类定案）——STR-NFR→bph-nfr（SysReq-NFR 保留），版本头同步递增 |
| 2026-08-22 | v5.7 | NFR 分解设计（人类逐项裁决 2026-08-22）：①**§2.4.1 新增分解规则**——四类运算（加法=响应时间 Σ子≤父 / 乘法=可用性 Π子≥父 / 弱链=吞吐量 每项≥目标 / 职责分布=安全审计权限 无守恒）+ 多对象识别（按组件类型 + SR-F/CU/页面级判定）+ 候选子预算（22 模板/同类场景推断标 `[需裁决]`，人类确认后入约束包）+ 父→子引用记录；单对象指标不分解保持原约束范围；子指标到组件类型级（`待wft05映射`）；②**§3.5 约束包装配**补「分解子指标作为独立约束行装配，标注分解运算+父指标引用」+「消费状态/已消费版本由 wft05-eng 消费后回写，本 Skill 发布时写 `待消费` 不自行置 `已消费`」。 |
| 2026-08-21 | v5.6 | 非主价值链 Step 框架链级统一（人类要求"eng 与 biz 一致，非主价值链以 biz 为基准"；反转 v5.5 三角色四区）：①移除 §三「三角色分工」表（角色信息并入 §一 与 Step End 锚点，EOS 开发者"可用性确认"维度保留为锚点内容）；②Step End 双区（设计审核/可用性确认）合并为单区「方案反馈」，审核/可用性锚点内容保留为 bullet；③Step 2 补内联 5 行处理表（对齐 biz/eng 链与 91 §A.5）；④输入校验表统一 biz「判定|动作」格式（硬退出/软提示并入判定列标注）；⑤§二 补「标注纪律」框架；⑥SKILL 同步（Step End 单区）。主价值链设计步内容不动 |
| 2026-08-20 | v5.5 | Step 框架对齐 biz/eng 骨架（人类确认 nfr 更需要调整，六个 Skill 一起修基础上补框架件）：①**Step 布局表**补「人类参与/执行模式」列 + 执行模式白话注 + 反馈机制一句话（含线下修订经 Step Start 检出）+ **三角色分工表**（§8.3）；②**Step 2 补反馈双轨**（AI 对话反馈主通道 + 线下文档修订，两轨等价均落账）+ 确认状态标记状态机 + 跨轮反馈持久化 + 处理规则 + 3.3.3 反馈总结（自原 Step End 移入）；③**Step 6 提交基线顺序修正**——由「AI最近变更→文件头→滚动删除→git commit」改为「AI最近变更→滚动删除→git commit→文件头（HEAD=刚提交基线 hash）」，对齐 wft01-eng v8.5 M1 链级不变式；④**Step End 改三角色四区**——设计审核（流水线开发者）+ 可用性确认（EOS 开发者）+ 下一步 + 输出后不结束对话；⑤新增 **§五 自检清单**（按 Step 分组），变更记录 §五→§六。§ 编号保持 3.x（nfr 未拆知识层三章，操作流程仍在 §三，5.x 编号迁移留待知识层重构时一并做） |
| 2026-08-20 | v5.4 | 反馈检出前置到 Step Start（链级推广，人类确认六个 Skill 一起修）：①**Step Start 增加变更感知引导段**（无编号，先于入口判定）——git diff H..HEAD 检出人类线下修订，判定变更类型（结构化标注→纳入待反馈 / 自由文本→[需确认] / 格式→忽略）后加入"待反馈处理"分节，对齐 91 §11.10；②**Step 1 步骤一改安全复查**——变更感知已上移 Step Start，此处仅按 §A.3.3 R0a 做加载前安全检查（detect-changes.sh 检出非预期修改标记 [需确认]） |
| 2026-08-18 | v5.3 | NFR 定量指标来源链级联动（对齐 ort02 v0.27 第六子类型 / [通用设计规范 §1.2](../../../00-generalspec/01-generalspec-sysdev.md#12-需求详解) 需求=需求描述+性能指标）：概述、目标2 生成量化约束、任务2 量化、§2.2 量化指标 四处改「承接相关方经澄清确认的目标值，展开为可验证的指标（单位/统计口径/测量方法/验收条件）；目标值缺失标待确认，不伪造确定值」。 |
| 2026-07-13 | v5.2 | 可读性修订：§2.1 定义碎片合并（2.1.1+2.1.2→统一定义、节点要素表化）、§2.2.1 子节号消除；§2.4/§2.5 重复"Phase B/C 共用"引导句精简为一次说明；§2.5.3 关系检测表去掉每格重复"内部细分结论="前缀，改为表头注释+括号标注；§3.5 去掉与 §2.4.2 完全相同的约束范围判定表（改为引用）；§3.4 开头集中声明规则来源，各步去掉零散"详见 §2.x"引用链；§3.6 缺口处理散文段落改为表格。 |
| 2026-07-13 | v5.1 | 审视修复三项：F1 §2.1.3/§3.6 闭环检查 `eng一致` 措辞与 91 §9.2 统一（91 规范同步更新 §9.2 任务 7）；F2 §3.1.1/§3.1.2 将 `需wft04修订` 预检移入退回门禁，消除重复判断；F3 §2.5.3 补充 Phase B 共享指标一致性检查。 |
| 2026-07-13 | v5.0 | 对齐 91 页面概念体系：操作页面 NFR 统一为页面 NFR；约束范围改为页面级；页面对象必须绑定七种页面类型之一，并优先绑定具体页面 ID。 |
| 2026-07-13 | v4.4 | 对齐 91 规范 v5.1：页面 NFR 明确区分业务应用页面与平台操作页面；两类页面均可作为适用对象，软件实现仍由 wft05-eng 单一消费。 |
| 2026-07-12 | v4.3 | 对齐 91 规范 v5.0：wft04 可并行设计全局/共享约束；目标级约束待 STR-E/CU/SR-F 就绪后绑定；增加 `待目标绑定` 状态和首版发布门禁，禁止虚构目标 ID。 |
| 2026-07-12 | v4.2 | 对齐 91 规范 v4.8——明确 NFR 仅由 wft05-eng 单一消费，首版 NFR 约束包是 wft05-eng 首次启动的必备前置输入，后续版本变化触发增量重入；约束包按全局/业务域/SR-F/操作页面/CU/PA组件/共享七组独立组织，新增包 ID、版本、上一版本、变更类型、增量清单、消费状态和已消费版本契约；变更影响声明统一面向 wft05-eng 消费版本。 |
| 2026-07-12 | v4.1 | §3.1 Step Start 重构对齐 §A.2.4 规定结构——从「入口检测→输入校验→分流处理」改为「输入校验→退回优先(§A.4.3门禁)→反馈处理→新SysReq-NFR路由」四子节模式；对齐 91 规范 v4.8 §9.2 的 NFR 约束流向——全部约束仅流向 eng 路径（wft05-eng），移除所有 wft03-biz 消费引用（§1.2/§2.3.1/§2.4.2/§2.1.3/§3.5/§3.8）；更新约束包装配为单一 wft05-eng 约束包；biz/eng 一致→eng 一致。 |
| 2026-07-12 | v4.0 | 对齐 91 规范 v4.8——引用体系迁移 §A.x（§2.1.1→§10.5、§2.6→§A.6、§2.7→§A.7、§3.3→§A.5、§3.8→§A.3.2、§四→§A.3.1）；§1.2 新增 §11.7/§A.3.3/§A.4.3 读取与退回引用；§3.1 Step Start 对齐 §A.4 入口决策流程（三类入口+AI可以处理节点检测+退回门禁+硬退出/软提示分类）；§3.2 Step 1 对齐 §A.3.3 读取协议（变更感知→AI可以处理节点优先→索引导航+禁止行为）；§3.3 Step 2 新增本 Skill 反馈处理特化参数表（锚点需求/操作条目/推进目标/级联触发条件/资产对齐级联）；§3.7 Step 6 对齐 §11.11 写回协议（同步维护 AI最近变更/AI可以处理节点/子条目状态/文件头字段+提交规范）。 |
| 2026-07-09 | v3.7 | §3.8 Step End 风格对齐 wft02-biz：补"Step End"引导句+三区说明"（结果→方案反馈→下一步）。本 Skill 实例化如下："、当前状态修正为 SysReq-NFR 侧生命周期状态（已NFR设计→已验证冻结，需wft01修订→移除——那是 STR-NFR 状态非本 Skill 产出状态）+ 从 `<N>` 占位概括改为逐项结果枚举行（本轮 SysReq-NFR）+ 新增资产落账行（22/06/05/02）、方案反馈 `->`→`→`、下一步从泛化描述改为具体 SysReq-NFR-ID + 状态 + STR-NFR 入口条件引用。无补充材料/建议行——Step 5 闭环检查缺口在 Skill 内分流处理（补齐/退回 wft01/标记需裁决），不生成外部补充文件 |
| 2026-07-09 | v3.6 | §3.4 风格对齐 wft01-biz：三 Phase（A~C）→ 三编号子节（§3.4.1~§3.4.3）；Phase A 消费感知+宿主匹配合并为散文+路由表+聚合池（移除4项版本检查列表、宿主匹配结果表——见 §2.4.3）；Phase B 新增为四步编号列表+`→`动作链+`（详见 §x.x）`引用（要素提取/宿主匹配检查/重判定与关系检测/变更影响声明）；Phase C 改为六步编号列表+`→`链+`（详见 §x.x）`引用；移除冗余量化指标生成线索表（§2.4.1）/SysReq-NFR 节点要素清单（§2.1.2）/48行节点块模板（不属于 §3.4 执行流） |
| 2026-07-09 | v3.5 | §3.3 风格对齐 wft01-biz/wft01~05-eng/wft01-nfr：拆为 §3.3.1（AI 处理流程）+ §3.3.2（确认状态格式示例），实例化参数表移至 91 规范引用，新增 wft04-nfr 特化示例（量化指标/分层约束表行/操作页面NFR/22写回项/06详细定义五类条目） |
| 2026-07-09 | v3.4 | §3.2 风格对齐 wft01-nfr v3.4：开头改为"每次运行加载以下资产"、`—` 替换 `：`、02/05 补"树画像/状态队列/节点索引"模式+动作导向目的描述、06 保留"索引"标注简化为定位写入目标、22 补"运行时动态读取不缓存"、25 补"只读"标注、新增"按需扩大"段落（正文块/候选 SysReq-NFR/反馈重入/扩大触发）、移除尾句约定引用和旧版扩大读取描述 |
| 2026-07-09 | v3.3 | §二 风格对齐 wft01-biz/wft01~05-eng/wft01-nfr 第二章：移除章节引导段、拆为 7 节 15 子节（§2.1.1~§2.7）；定义与提取规则分离（§2.1~§2.3 定义"是什么"→ §2.4~§2.5 定义"怎么做"）；新增 Phase B/C 提取差异表（§2.4.4）+ 量化指标生成规则（§2.4.1）+ 分层约束判定表（§2.4.2）+ Phase B 量化指标关系检测六种判定（§2.5.3）；原 §2.6 拆为 §2.6（双生命周期 ASCII 流转图——STR-NFR侧+SysReq-NFR侧+上游变更）和 §2.7（变更传播格式模板含约束行级影响声明）独立成节；完备性判据从 Step5 闭环检查提取到 §2.1.3；操作页面NFR推断规则从 §3.5.2 提取到 §2.5.2；22 具体需求资产从 §2.5 归入 §2.3.3 |
| 2026-07-09 | v3.2 | §1.1 风格对齐 wft01-biz：主要目的改为"不仅…更…"递进式修辞、负责项展开叙事细节（量化指标六要素枚举/归架构宿主保证/不负责#5 补充说明）、不负责项去句号 |
| 2026-07-08 | v3.1 | 精修执行口径：补充 SysReq-NFR 节点块模板；明确 wft04-nfr 对 wft03-biz 是约束包回补/重入输入；将 SysReq-NFR 补充态统一为 `待补充NFR验证`；与 91 v3.16 对齐。 |
| 2026-07-08 | v3.0 | 按 91 规范 v3.15 重构；明确 wft04-nfr 负责“生成非功能需求架构 + 生成操作页面非功能需求”，从 STR-NFR 分类需求清单生成 SysReq-NFR、量化指标、分层约束表、操作页面 NFR、22 具体需求写回和 06 系统需求详细定义；公共反馈/生命周期/变更协议集中引用 91。 |
| 2026-06-21~2026-06-29 | v1.0~v2.2 | 历史版本，详见 Git 历史。 |
