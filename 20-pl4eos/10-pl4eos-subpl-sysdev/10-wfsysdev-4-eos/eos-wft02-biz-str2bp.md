# STR-F → BP · 业务流程设计与资产化
**人类方案ID**: wft02-biz-str2bp

> **概述**：接收 ≥1 个 STR-F 节点（`可以分解分配` / `待补充分解分配`），经前置校验、材料加载、反馈处理后，完成 A2+Bn 双视角 BP 设计（Step 3 A2 角色场景→Step 4 Bn 输出产品：节点生成→文档方案→活动设计→权限→PL6-B→任务关系→缺口推断），业务缺口生成 FR-BIZ OR 材料，最后将资产对齐结论写回 23/24/26/27/28，形成可进入 wft03-biz 的 BP 节点。
>
> **输入 → 输出**：STR-F 节点 / 有反馈的 BP 节点 → BP 方案 + 23/24/26/27/28 资产写回（状态=`可以SR设计`）+ STR-F→`已分解分配` + 原始需求材料文档（如有）
>
> **适用场景**：瀑布式系统设计第2步 · 业务流程路径。
>
> **设计模式参考**：[91-eos-biz-eng-spec.md](91-eos-biz-eng-spec.md)
>
> **版本**：v9.0 | **修订**：2026-08-20 | **说明**：反馈检出前置到 Step Start（§5.1 变更感知）——变更感知 git diff 先于入口判定，线下文档修订在纯线下场景也能被检出；Step 1 ① 改安全复查（职责分离，对齐 91 §11.10 / §A.3.3 R0a）

---

## 一、职责定位

**一句话**：本章说明本 Skill 的定位——要达成什么（1.1 目的与目标）、与上下游如何衔接（1.2）。

### 1.1 本 Skill 目的与目标

> **主要目的**：取信于业务方——「你给出的业务方案是对的」。
>
> 把 STR-F 的场景业务展开为 BP 方案：
> - **A2 角色场景视角**：角色→场景业务→功能表单/主子表单的使用顺序
> - **Bn 输出产品视角**：构件类型→WBS→输出文档 + PL6-A/PL6-B
>
> 消费 wft01-biz 的任务类型/节点/关系（**业务语言**）→ 转化为引擎 CU 候选（**引擎语言**），为下游系统需求设计（wft03-biz）提供可展开的输入。
>
> D 和 PCA 处理逻辑**流程骨架同构**——处理流程结构（场景业务→输出文档序列→每文档封面/正文/任务/引用）不因类型分叉，差异在**文档模式与闭合形态**（详见 §2.2.1）；类型判定仅影响引擎推断偏好。
>
> **目标**：
> 1. **装配 A2 产品业务流程**——将场景业务展开为 SL3~SL5 角色场景 BP，将需求分配到输出文档
> 2. **装配 Bn 产品业务流程**——将输出文档定位到 PL1~PL5 路径
> 3. **推断 Bn 构件输出文档**——检查 26 资产文档定义覆盖，按构件类型推断缺失文档
> 4. **基于 Bn 补充业务需求**——汇聚业务缺口，统一生成 FR-BIZ OR 材料
> 5. **定义输出文档方案**——正式文档：封面字段 + 正文实体结构 + 支撑引擎 + 支持信息引用；轻量文档：正文实体结构 + 支撑引擎 + 支持信息引用
> 6. **定义 PL6-A 活动节点需求**——消费 wft01 的任务类型与主要节点（业务语言）→ 映射到引擎 CU 候选：计划→@engine-task、流程→@engine-flow、工单→@engine-workorder；补充系统自动节点/异常路径/超时处理。全量 `[推断]`
> 7. **判定并定义 PL6-B 正文条目需求**——先判定正文是否为集合容器，再按三独立逐类判定；满足时定义名称、定义要素和生命周期。PCA 不预设全部通过，轻量原子正文行不得重复展开
> 8. **任务关系引擎化**——消费 wft01 的任务关系→转化为引擎任务触发规则配置（触发条件/启动参数）；计划/流程/工单可任意触发任意类型，包括同类型

本 Skill 负责：

| # | 职责 | 要点 |
|---|------|------|
| 1 | **接收** | 人类选中 ≥1 个 STR-F 节点（`可以分解分配` / `待补充分解分配`），校验状态/类型/数量/跨域；不合格则说明原因并退出 |
| 2 | **匹配** | 按产品路径+用户角色匹配已有 BP 节点——同路径同角色归入同一 BP（Q1/Q2），无则新建（Q3） |
| 3 | **收敛** | 将本轮新 STR-F 的信息载体与 BP 已有内容逐项比对——确认则升级来源、补充则全量展开、矛盾则记录待裁决、无新增则仅加引用 |
| 4 | **角色场景设计**（Step 3） | 生成 SL3~SL5 角色场景 BP + 需求分配到输出文档 |
| 5 | **输出产品设计**（Step 4） | 定位 PL1~PL5 路径 + 文档方案 + 流程/工单活动设计 + 数据权限 + 文档缺口推断 |
| 6 | **PL6-B 正文条目需求**（Step 4） | 先判定正文集合性，再按三独立条件逐类判定；满足时定义名称、定义要素和生命周期。轻量原子正文行及运行实例不展开 |
| 7 | **增补业务需求**（Step 5） | 汇聚业务缺口，AI 推断补充 + `[推断-待OR确认]` 标注，统一生成 FR-BIZ OR 材料并登记 OR 原料状态表 |
| 8 | **写回**（Step 6） | 人类确认方案后统一写回 23/24/26/27/28 资产（Q1/Q2/Q3 均直接写入，不二次确认） |
| 9 | **推进** | 人类确认时 BP 节点→`可以SR设计`（wft03 可即时进入）；资产落账后 STR-F→`已分解分配` |
| 10 | **回修** | 接收 wft03 退回的 `需wft02修订` BP 节点，读取缺失说明，按人类方案修订 BP（走 Step 3~5），确认后→`可以SR设计` |

本 Skill 不负责：

| 项目 | 转交目标 |
|------|---------|
| 引擎路径需求与设计（STR-E / A1 平台引擎产品） | `wft02-eng-str2bp` |
| 非功能需求（STR-NFR） | `wft04-nfr` |
| 下游 wft03 的 SR 架构设计展开 | 由 wft03 自行处理 |
| 业务人员确认 | 人类线下活动，本 Skill 不参与 |
| PL6-B 条目的字段详细建模（字段类型/校验规则/约束） | `wft03-biz` 在功能表单需求中定义 |

### 1.2 与上下游的衔接

| 衔接方 | 交接内容 | 协议 |
|--------|---------|------|
| **上游** `wft01-biz` | 产出 STR-F 节点（`可以分解分配`/`待补充分解分配`），含业务链文档序列 + 任务类型/节点（业务语言）+ 引擎候选建议（全量 `[推断]`）+ PL6-B 条目名称级捕获 + Bn 对齐判定 | 本 Skill 通过 `## AI可以处理节点`（[§11.7](91-eos-biz-eng-spec.md#117-ai可以处理节点)）检测待处理节点，按 [§A.3.3](91-eos-biz-eng-spec.md#a33-产品数据文档与资产文档读取与写回约定) 读取协议加载 |
| **下游** `wft03-biz` | 消费 BP 方案，推断功能表单和窗口标签页，建立菜单→场景→窗口三层页面编排，展开为 SR-F 架构（[§7.3](91-eos-biz-eng-spec.md#73-业务配置需求开发wft03-biz)） | BP 缺陷时退回 `需wft02修订`（[§A.4.3](91-eos-biz-eng-spec.md#a43-规则二退回节点的门禁) 门禁），附带缺失说明；本 Skill 按人类方案修订后→`可以SR设计` |
| **治理** STR-F | 平台资产治理业务 OR 预置在 `02-*.md`，与业务 STR-F 完全同构（[§六](91-eos-biz-eng-spec.md#六平台资产治理业务)） | 当前 OR 承接为空，暂不进入。待 OR 补充后经 wft01→`可以分解分配`，按正常 biz 路径处理，不设特殊分支 |

> 本 Skill 的 I/O 简图：[91 规范 §7.2](91-eos-biz-eng-spec.md#72-业务流程开发wft02-biz)（STR-F `可以分解分配` → BP `可以SR设计`，末级节点=输出文档 PL5，细分 PL6-A/PL6-B）

---

## 二、基本概念：从 STR-F 到 BP

**一句话**：本章讲**基本概念**——从 STR-F 到 BP 的核心产物是什么：业务流程（2.1）、A2 角色场景（2.2）、Bn 输出产品（2.3）、引擎关系（2.4）与任务引擎化（2.5）；转换总览先给整条设计主线（六步一条链 + 规则地图）。设计原理与判据见 §三，节点演进见 §四，操作步骤见 §五。

### 转换总览：STR-F → BP

**一句话**：本节给整条设计的路线图——语言层级（业务语言→架构语言）、转换方法（六步一条链）与规则地图（判据索引）；转换的"为什么"见 §三 3.1 转换原理。

#### 转换方法：六步一条链

**输入**：STR-F 节点（`02-*.md`，状态=`可以分解分配`/`待补充分解分配`）。
**输出**：BP 方案（`04-*.md`，业务流程架构），供 wft03-biz 展开为系统需求（05/06）。

| 步 | 做什么 | 判据 / 细则 |
|----|--------|-------------|
| ① 接收 | 校验状态/类型/数量/跨域，退回优先 → 反馈处理 → 新输入路由 | §5.1 Step Start |
| ② 匹配 | 按产品路径+用户角色归入已有 BP（Q1/Q2）或新建（Q3） | §5.4 Step 3 Phase A |
| ③ A2 设计 | 落 SL3~SL5 角色场景 BP，需求分配到输出文档 | §5.4 Step 3 |
| ④ Bn 设计 | 定位 PL 路径 + 文档方案 + 活动/指标 + PL6-B + 任务关系 + 缺口推断 | §5.5 Step 4 |
| ⑤ 增补 | 汇聚业务缺口 + 覆盖自查，生成 FR-BIZ OR 材料 | §5.6 Step 5 |
| ⑥ 写回 | 资产落账（23/24/26/27/28）+ 推进 BP/STR-F 状态 + 提交基线 | §5.7 Step 6 |

> 读法：①~⑥ 是设计主线，具体判据在 §三 与 §五；可执行步骤见 §五 操作流程（Start/1/2/3/4/5/6/End）。

#### 语言层级：从业务语言到架构语言

**一句话**：转换的每个产物都有自己的语言——业务语言（STR-F）→ 架构语言（BP）→ 配置语言（wft03）；架构语言 = 落在 EOS 系统模型上、可被下游直接消费的"施工图"。

**架构语言** = 描述解决方案实体的语言——每个词落在 EOS 系统模型上（引擎 @engine-x、文档模型 封面-正文-PL6、层级 SL/PL、资产 23/26、配置单元 @xxx-cu-xxx），回答"业务要素在系统模型的哪个位置、用什么机制实现"，**可被下游 Skill 直接消费**（wft03 据此推断功能表单、检查引擎能力、分配指标）。

> **施工图比喻**：业务语言是客户口述"我要一栋能住的房子"（需求）；架构语言是建筑师画的施工蓝图——标注墙体承重、管线走向、材料规格（方案）；wft03 按图备料施工（配置/构造）。没有施工图，"能住的房子"无法施工；没有架构语言，"采购申请审批"无法建表。

| 产物 | 语言 | 形态 | 可消费性 |
|------|------|------|---------|
| STR-F（wft01） | 业务语言 | 采购员填采购申请单、部门经理审批 | 供人类确认理解 |
| | 准架构语言 | `[推断]@engine-flow`、封面模式、Q 判定 | 用架构词但带 `[推断]` 候选标记，**不可消费** |
| BP（wft02） | 架构语言 | 引擎确认值、DocumentCover 映射、PL 路径、@flow-cu-node-config、触发规则 | **wft03 直接消费** |
| wft03 产出 | 配置语言 | 功能表单、窗口标签页、引擎能力使用需求 | 构件开发流水线消费 |

> **准架构语言 → 架构语言**：wft01 的 `[推断]` 用架构词但不是可消费的最终值——它是给人类看的业务理解翻译；wft02 校验确认（确认/修正/推翻，§3.2）后成为架构语言。这正是"引擎权威在 BP 校验值、不回写 STR-F"（§3.2）的原因——STR-F 的语言是业务理解，不是构造输入。

#### 规则地图：判据索引

| 判据 | 回答什么问题 | 位置 |
|------|-------------|------|
| 双视角交汇 | A2 与 Bn 两个视角怎么互相校验 | §2.1.2 |
| 引擎候选校验 | wft01 的引擎建议确认/修正/推翻 | §3.2 |
| 资产 Q 判定 | 设计对象与已有资产匹配程度（Q1/Q2/Q3） | §3.3.1 |
| SL4/PL4 归纳 | 角色职责/WBS 怎么归组（含完成耦合） | §3.3.2 |
| 23 一致性检查 | wft01 23B 判定与 BP 路径是否冲突 | §3.3.3 |
| PL6-B 三独立 | 正文条目是否展开为 PL6-B | §3.4.1 |
| PL6-B 定义优先级 | wft01 捕获条目怎么定义（确认/推断/补缺） | §3.4.2 |
| 完备性判据 | BP 方案是否完备（双视角互证 + 缺口闭环） | §3.5 |
| 变更类型判定 | 增量/修订/结构，怎么影响下游 | §4.2 |
| 覆盖自查 | 设计输出是否漏做（Step 5 末尾机械检查） | §5.6 |

**① 设计产物——BP 与双视角**

### 2.1 业务流程（BP）

**一句话**：本节讲 BP 的概念——定义与双视角模型（2.1.1）、A2 与 Bn 的交汇点（2.1.2）；设计规则见 §三，操作见 §五。

#### 2.1.1 定义与双视角模型

**一句话**：本节分两块看——**BP 定义**（五层设计链的第三层）与**双视角模型**（A2/Bn 两个互补视角互相校验），末尾给出 BP 节点完整要素清单。

BP 是五层设计链的第三层（OR→STR→**BP**→SR→PA），将 STR-F 的场景业务展开为可被系统需求（SR-F）承接的业务方案。BP 的权威定义见 [通用设计规范 §7.4](../../../00-generalspec/01-generalspec-sysdev.md)。

**双视角模型**——EOS 的 BP 包含两个并存互补的视角：

- **A2 角色场景视角**（§2.2）：回答"谁在什么场景、按什么顺序使用哪些功能表单、完成端到端的职责闭环"；菜单入口和页面类型不在 BP 层确定
- **Bn 输出产品视角**（§2.3）：回答"某类输出文档属于哪个业务域/构件类型、需要哪些前序文档和业务规则才能生成"

两个视角互为投影、交汇于功能表单（§2.1.2），一致性由同一 STR-F 业务链天然保证；完备性由**各自闭环**保证——A2 侧靠 STR-F 端到端闭环（wft01 已做），Bn 侧靠构件文档缺口推断（§5.5.5），**Bn 侧检出的缺口经 FR-BIZ OR 回路（§5.6）在下轮收敛回 A2**（单向补，不脑补节点）。A2/Bn 的定义见 [91 规范 §5.2~§5.3](91-eos-biz-eng-spec.md#52-a2产品)。

**BP 节点完整要素**（写入 `04-*.md` BP 节点块）：

- 角色场景视角（A2产品）BP 方案——SL3~SL5（角色类型/角色职责/场景业务，SL6 功能表单由 Bn 侧 PL5 覆盖）
- 输出产品视角（Bn产品）BP 方案——PL3~PL5（构件类型/PL4 WBS/PL5 业务输出文档）+ 输出文档路径 + 数据权限 + 文档缺口推断
- **PL6-B 正文条目需求定义**：条目名称 / 定义要素 / 生命周期（按三独立判定，详见 §3.4.1）
- 资产对齐判定（23/24/26/27/28 Q1/Q2/Q3）
- 变更影响声明（变更类型 + 下游影响 + 建议动作，Step 4 判定变更类型 + Step 5 统一写入）
- BP 版本号（初始 v1，每次上游 STR-F 修订递增）

#### 2.1.2 A2 与 Bn 的交汇点

**一句话**：两个视角通过"输出文档→功能表单/主子表单"的转化关系交汇——PL5 是 Bn 侧产品数据节点，经引擎转化组装后成为 A2 侧 SL6 使用的功能表单。

两个视角通过"输出文档→功能表单/主子表单"的转化关系交汇——PL5 输出文档是 Bn 侧的产品数据节点，经实体引擎和对应功能引擎转化、组装后，形成 A2 侧 SL6 使用的功能表单/主子表单。两者存在稳定映射，但不是同一层级对象。

对于流程类和工单类输出文档，BP 还需定义其**文档状态**（详见 §2.5.1）。

---

### 2.2 A2 产品：角色场景视角

**一句话**：本节讲 A2 角色场景视角——定义与 SL 层级（2.2.1）、本 Skill 在 A2 侧的职责（2.2.2）。

#### 2.2.1 定义与 SL 层级

**一句话**：本节分两块看——**A2 定义**（各业务域的角色场景，构件=角色，末级=场景业务 SL5）与 **SL 层级表**（SL0~SL6）。

A2 是**各业务域的角色场景**。定义见 [91 规范 §5.2](91-eos-biz-eng-spec.md#52-a2产品)。构件为角色（如采购员、库存管理员），末级节点为场景业务（SL5）。

沿角色场景视角（SL）展开：

| 层级 | 名称 | 说明 |
|------|------|------|
| SL0 | 平台 | EOS 平台 |
| SL1 | 业务域 | 如采购管理、生产管理 |
| SL2 | A2产品 | 角色场景产品 |
| SL3 | 角色类型 | 如采购员、库存管理员 |
| SL4 | 角色职责 | 归纳层，可选占级 |
| SL5 | 场景业务 | **末级节点**，与 STR-F 1:1 映射 |
| SL6 | 功能表单/主子表单 | 由 PL5 输出文档经引擎转化和组装的抽象表单定义。窗口标签页及页面编排由 wft03-biz 设计（交汇关系见 §2.1.2） |

**场景业务（SL5）**：STR-F 末级节点，定义端到端职责闭环。

场景业务定义以下内容：
- **用户角色**：谁在执行
- **入口条件**：何时进入
- **可用功能表单**：用哪些表单完成工作
- **跨部件上下文传递**：数据如何在表单间流转

BP 只定义**场景边界**和**表单使用顺序**。wft03-biz 负责配置页面组件、将窗口标签页编排为场景标签页、将场景标签页编排进菜单标签页。

场景业务分为**执行类（D）**和**管理类（PCA）**，分类定义见 [91 规范 §1.4](91-eos-biz-eng-spec.md#14-业务场景)。D 和 PCA 在 wft01/02/03-biz 的处理逻辑上**流程骨架同构**——指处理流程结构（都是场景业务→输出文档序列→每文档封面/正文/任务/引用）不因类型分叉，差异在**文档模式与闭合形态**（PCA 有额外的计划清单文档与 P/C/A 文档模式，[91 规范 §2.7](91-eos-biz-eng-spec.md#27-pca-场景的-pca)）。类型判定仅影响本 Skill 的引擎推断偏好（D→交付类引擎优先，PCA→业务管理类引擎优先），不影响处理流程结构。

#### 2.2.2 本 Skill 在 A2 侧的职责

**一句话**：装配 A2 产品业务流程——将场景业务展开为 SL3~SL5 角色场景 BP，需求分解分配到各输出文档。

**装配 A2 产品业务流程**——将 STR-F 的场景业务展开为 SL3~SL5 角色场景 BP，将需求分解分配到各输出文档。指标文档、看板文档等均为独立 PL5 输出文档，与执行类文档同等处理——按 A2 侧分配到场景业务、按 Bn 侧定位 PL 路径和文档定义，不设独立的处理流程。流程/工单/计划的任务节点引擎化分工见 §2.5。

---

### 2.3 Bn 产品：输出产品视角

**一句话**：本节讲 Bn 输出产品视角——定义与 PL 层级（2.3.1）、输出文档分类（2.3.2）、本 Skill 在 Bn 侧的职责（2.3.3）。

#### 2.3.1 定义与 PL 层级

**一句话**：本节分两块看——**Bn 定义**（各业务域的输出产品，构件=产品构件，末级=输出文档 PL5）与 **PL 层级表**（PL0~PL6）。

Bn 是**各业务域的输出产品**。定义见 [91 规范 §5.3](91-eos-biz-eng-spec.md#53-bn产品)。构件为产品构件，末级节点为构件的产品数据——即**输出文档**（PL5）。这也是 wft02-biz 的末级节点（[91 规范 §7.2](91-eos-biz-eng-spec.md#72-业务流程开发wft02-biz)）。

沿产出视角（PL）展开：

| 层级 | 名称 | 说明 |
|------|------|------|
| PL0 | 平台 | EOS 平台 |
| PL1 | 业务域 | 如采购管理、生产管理 |
| PL2 | E2E 项目业务 | 如采购申请管理 |
| PL3 | 构件类型 | 产品的分解分类节点。按开发方式分四类，产生不同的输出文档集合。完整定义见 [91 规范 §5.3](91-eos-biz-eng-spec.md#53-bn产品) |
| | | **示例**：设备产品 → 系统/电子模块/电路板/软件模块/复用构件/外购构件/项目 |
| | | **开发方式后缀**：`自研` / `咨询` / `外购` / `复用` |
| | | **文档差异**：自研→全套研发文档；咨询→咨询交付文档；外购→采购规格/验收标准；复用→适配说明 |
| PL4 | WBS | 归纳层，可选占级 |
| PL5 | 业务输出文档 | **末级节点**；按条件细分 PL6-A 活动节点和 PL6-B 正文条目 |
| PL6-A | 活动节点 | 有任务类型时存在；定义处理角色、动作和状态变迁 |
| PL6-B | 正文条目 | 正文条目满足三独立时存在 |

#### 2.3.2 输出文档分类

**一句话**：输出文档按生成方式分内置 CAX 与外置 CAX 两类——wft01 在名称级识别候选支撑引擎链，本 Skill 在 BP 层展开输出文档设计。

输出文档按生成方式分为内置 CAX 和外置 CAX 两类，定义及引擎推导路径详见 §2.4.2。

正式输出文档在引擎层形成封面—正文 1:1 主子表单；轻量文档以正文功能表单直接呈现。存在关联文档下钻时，再在此基础上扩展 1+N 子表单。wft01-biz 在名称级识别输出文档及其候选支撑引擎链，wft02-biz 在 BP 层展开输出文档设计。

#### 2.3.3 本 Skill 在 Bn 侧的职责

**一句话**：装配 Bn 产品业务流程 + 推断 Bn 构件输出文档——定位 PL1~PL5 路径，展开每个输出文档的设计。

**装配 Bn 产品业务流程** + **推断 Bn 构件输出文档**——定位输出文档的 PL1~PL5 路径，展开每个输出文档的设计。正式文档：封面字段（DocumentCover 表映射）+ 正文实体结构 + 支撑引擎 + 支持信息引用；轻量文档：正文实体结构 + 支撑引擎 + 支持信息引用。推断文档缺口，定义权限。任务节点引擎化见 §2.5。

---

### 2.4 功能引擎与输出文档的关系

**一句话**：本节讲引擎与输出文档的关系——三层引擎生成链（2.4.1）、输出文档推导路径（2.4.2）；引擎候选校验规则见 §三 3.2。

#### 2.4.1 三层引擎生成链

**一句话**：EOS 平台通过三层引擎（EL1~EL3）逐级生成业务——实体引擎生成物理表、功能引擎生成功能表单、菜单引擎编排菜单树。

EOS 平台通过三层引擎（EL1~EL3）逐级生成业务（[91 规范 §3.2~§3.4](91-eos-biz-eng-spec.md#32-引擎及层级)）。权威资产定义见 [25 资产 §1.1](../../80-pl4eos-2-eosdata/25-eos-engine-models.md#11-引擎全景)。

```
实体引擎（EL3）──生成──→ 物理表
                           ↓ 被消费
功能引擎（EL2）  ──生成──→ 功能表单 / 主子表单
                           ↓ 被消费
菜单引擎（EL1）  ──生成──→ 菜单树 + 场景业务
```

**功能引擎**是 EL2 层级，将物理表转换为功能表单并按主子关系组装。具体引擎清单见 [25 资产 §1.1](../../80-pl4eos-2-eosdata/25-eos-engine-models.md#11-引擎全景)，wft02-biz 不重复枚举。

#### 2.4.2 输出文档推导路径

**一句话**：自顶向下推导输出文档的引擎链——内置 CAX 走三层引擎链，外置 CAX 走计划引擎承载 + 附件回传；候选建议的校验规则见 §三 3.2。

自顶向下推导（[91 规范 §3.4](91-eos-biz-eng-spec.md#34-业务组装)）：

- **内置 CAX**：输出文档条目化内容 → 实体引擎（`@engine-entity`）生成物理表 → 功能引擎转换为功能表单 → 按主子关系（1+N）组装为主子表单 → 菜单引擎（`@engine-menu`）编排为场景业务
- **外置 CAX**：输出文档 → 计划引擎（`@engine-task`）管理任务/状态/附件/评审/归档，附件能力由实体引擎提供，N=0

wft01-biz 已在名称级产出每个输出文档的**候选支撑引擎建议**（全量 `[推断]`）。本 Skill 在 BP 层展开输出文档设计时，基于 BP 上下文校验候选建议，确认/修正/推翻——校验规则见 §3.2。wft03-biz 仅对照引擎/CU 能力判断需求能否被承接并生成缺口线索，不承担 CU 设计（91 规范 §7.3）。

---

### 2.5 任务节点引擎化与任务关系引擎化

**一句话**：本节讲任务如何引擎化——任务节点引擎化（2.5.1，类型→引擎映射 + 业务语言→引擎语言）、任务关系引擎化（2.5.2，触发规则）、PCA 任务判定（2.5.3）、PL6-B 定义概念（2.5.4）。适用于所有有任务类型的正式或轻量文档。正式文档的活动节点附着于封面实体，轻量文档的活动节点附着于正文实体。

#### 2.5.1 任务节点引擎化

**一句话**：消费 wft01 的任务类型与主要节点（业务语言），转化为引擎 CU 候选映射（引擎语言）——类型→引擎映射 + 节点转化规则。

消费 wft01-biz 的任务类型与主要节点（业务语言），转化为引擎 CU 候选映射（引擎语言）：

**类型→引擎映射**：
- 计划 → @engine-task（计划引擎）
- 流程 → @engine-flow（流程引擎）
- 工单 → @engine-workorder（工单引擎）

> **耦合判定**：任务类型与引擎一一映射，二者是同一判定（业务处理模式）的两个表达。wft01 判的任务类型是业务语言初步值，最终权威在 BP 引擎校验（§3.2，修正引擎即终审处理模式并同步修正任务类型）。

**节点转化规则**：
- wft01 已捕获的节点 → 映射到引擎 CU——角色→引擎角色配置（如 `审批人=部门经理`）、动作→引擎动作配置（如 `[通过→财务审批, 驳回→申请人]`）、状态变迁→引擎状态配置（如 `部门已通过/已驳回`）。**wft01 已捕获节点是业务流转确认结果**（供人类确认过，[wft01-biz §1.1 目标 3](eos-wft01-biz-or2str.md)），wft02 在此基础上做架构序列设计，**不重新推断业务流转**（谁/动作/顺序），除非有明确证据
- wft01 标注 `[待wft02推断]` 的节点 → 设计完整节点序列（含系统自动节点、异常路径、超时处理）
- 全部节点标注 `[推断]`，未经人类确认不得落账

**业务语言→引擎语言示例**：

```yaml
# wft01 产出（业务语言）
任务类型：流程
节点：部门经理审批，通过后到财务

# wft02 转化（引擎语言）
引擎：@engine-flow
CU候选：@flow-cu-node-config
  角色=部门经理
  动作=[通过→财务审批, 驳回→申请人]
  状态变迁=部门已通过/已驳回
```

#### 2.5.2 任务关系引擎化

**一句话**：消费 wft01 的任务关系（文档间流转顺序 + OR 明示的触发关系），转化为引擎可执行的触发规则——每个关系定义源文档/触发条件/目标文档/启动参数。

消费 wft01-biz 的任务关系（文档间流转顺序 + OR 明示的触发关系），转化为引擎可执行的触发规则：

- **文档间流转顺序** → 输出文档序列编排
- **OR 明示的触发关系** → 触发条件（如"审批通过后生成合同"→文档A(流程)节点"审批"=通过→创建文档B）
- **任意任务触发**（91 规范 §2.5 R5）→ 计划/流程/工单可触发任意任务类型，包括同类型；仅依据业务需要生成具体关系，不套固定拓扑

每个关系定义：源文档、触发条件（状态/节点）、目标文档、启动参数。

#### 2.5.3 PCA 策划阶段的任务判定

**一句话**：PCA 与 D 场景遵循同一任务规则——每文档零个或一个任务类型，首个有任务文档为计划（但仍是开发/记录类文档），其余文档的任务配置由业务需要决定、不得推定全量计划。

PCA 场景与 D 场景遵循同一任务规则：每个正式或轻量文档可配置零个或一个任务类型；场景业务中第一个有任务类型的输出文档必然为计划，但文档本体仍是开发/记录类业务文档，不因此变成"计划清单"。其余文档是否配置任务及任务类型，必须由业务需要、上游节点和任务触发关系决定，不得推定为全量计划，也不得自动生成无 OR 或 E2E 缺口依据的自指任务条目。

#### 2.5.4 PL6-B 正文条目需求定义

**一句话**：本节讲 PL6-B 正文条目需求定义的**概念**——适用范围与定义要素表；三独立判定与定义优先级/关系检测见 §三 3.4。

> wft01-biz 在业务语言层先判定正文集合性，再按三独立捕获 PL6-B 条目类型；PCA 定义型/管理型条目通常满足但仍逐类判定。本 Skill 在 BP 层将其展开为结构化需求定义。

**定义要素**：

| 维度 | 说明 | 示例（指标"订单及时率"） |
|------|------|------------------------|
| **条目名称** | 确认/精炼 wft01 捕获的名称（业务语言→标准化名称） | 订单及时率 |
| **定义要素** | 条目的核心定义内容——度量口径/维度/目标值（指标）、数据源/图表类型/刷新频率（看板卡片）、清单类型/优先级规则（计划项）、分类/等级（风险/问题）、状态集（行动项） | 口径=按约定交期准时交付的订单数/订单总数；维度=供应商/月份；目标值≥95% |
| **生命周期** | 独立于父文档的状态变迁路径 | 草稿→已发布→已废弃（审批后发布） |
| **支撑引擎** | 继承自文档支撑引擎，不单独判定 | 继承自父文档 @engine-kanban |

**与封面模式及 PL6-A 的关系**：正式/轻量是 PL5 封面模式，PL6-A 是任务活动分解，PL6-B 是正文集合内的条目类型分解，三者相互正交。同一文档可兼有 PL6-A 和 PL6-B，但不得把轻量文档正文实例同时作为 PL5 文档实例和 PL6-B 条目重复建模。

---

## 三、设计原理与判据：为什么这样设计、怎么判定

**一句话**：本章讲设计的逻辑——为什么用双视角承接 STR-F（3.1 转换原理）、怎么校验引擎候选（3.2）、怎么对齐资产（3.3）、怎么判 PL6-B 与完备性（3.4/3.5）；概念见 §二，节点演进见 §四，操作步骤见 §五。

### 3.1 转换原理：为什么用双视角承接 STR-F

**STR-F 描述"要什么业务"，BP 回答"怎么用系统完成"**。STR-F 是场景业务（一个主责角色的业务对象闭环，[91 规范 §1.4](91-eos-biz-eng-spec.md#14-业务场景)），它定义职责边界和业务链文档序列；BP 要把这些展开为可被系统需求（SR-F）承接的业务方案——谁在什么场景按什么顺序使用哪些功能表单、每个输出文档怎么生成。承接单位 = BP。

**双视角 = A2 回答"谁用什么"，Bn 回答"产出什么"**。A2 角色场景视角（SL）回答"谁在什么场景、按什么顺序使用哪些功能表单、完成端到端的职责闭环"（§2.2）；Bn 输出产品视角（PL）回答"某类输出文档属于哪个业务域/构件类型、需要哪些前序文档和业务规则才能生成"（§2.3）。两者是同一 BP 的两个互补剖面，不存在主次。

**两个视角互为投影、完备性各自闭环**。A2 描述角色怎么工作、Bn 描述角色产出什么——单看任一视角都可能漏：只从 A2 看得到角色操作顺序、看不到文档生成规则；只从 Bn 看得见文档、看不见角色使用顺序。交汇点 = 输出文档→功能表单/主子表单的转化关系（§2.1.2），两视角通过它稳定映射、一致性由同一 STR-F 业务链天然保证。完备性由**各自闭环**保证：A2 侧靠 STR-F 端到端闭环（wft01 已做），Bn 侧靠构件文档缺口推断（§5.5.5）——**缺口发现集中在 Bn 侧，经 FR-BIZ OR 回路（§5.6）在下轮收敛回 A2**，不脑补节点。这正是"双视角"而非"单视角"承接的原因。

**推断 + 补全，不是转录**。本 Skill 不只复述 wft01 的业务链，还在 BP 上下文校验引擎候选建议（§3.2，可能确认/修正/推翻）、推断文档缺口（§5.5 缺口推断）、设计活动节点与指标序列（§5.5，主驱动=对 STR-F 需求的理解）。识别出的是"BP 蕴含的业务"；识别不出但闭环必需的，进业务缺口增补生成 FR-BIZ OR 材料（§5.6），不脑补节点。

**通过 FR-BIZ OR 渐进收敛**。文档缺口不直接写入资产，而是生成 OR 材料（§5.6）→ 经 OR 预处理链 → 成为新一轮 FR-BIZ OR → wft01 设计 → 新一轮 STR-F → Phase B 归入已有 BP（§5.4 收敛规则）。多轮循环后 BP 收敛到完备的文档集合。这是本 Skill 的核心运行模型：**缺口外推与 BP 收敛互为镜像**，由"FR-BIZ OR"这条回路衔接。

**缺口为什么必须回 STR，而不能由 BP 直接设计**。wft02 只有构件完备视角——能**发现**缺口（"这个构件应该有评估报告"），但不能**定义**缺口文档的业务身份：它在业务链的哪个位置、承担什么场景职责、与已有文档的前后序关系——这些是 STR-F 层（wft01）的业务语义，wft02 无 OR 语义、推断不出来。且 BP 的每个正式文档必须能**追溯**到 STR-F 节点与 OR 条目（追溯矩阵）；先于 OR/STR-F 存在的 BP 文档会断裂追溯链。因此缺口须经 OR→STR-F 确认（含业务链定位 + 闭合检查补相邻缺口，如评估报告的上游准入申请）后，wft02 才能设计。人类确认缺口 = **发起 OR 的签字**，不绕过管道（§5.6）。

### 3.2 引擎候选校验规则

**一句话**：本节讲怎么校验 wft01 的引擎候选建议——校验依据（BP 展开后的完整上下文）与三种结果（确认/修正/推翻）。

本 Skill 在 Step 4（Bn 侧）设计中对 wft01 的引擎候选建议执行校验（推导路径概念见 §2.4.2）。校验依据 BP 展开后的完整上下文——输出文档在业务链中的职责、需执行的处理动作、文档状态变迁路径——而非 wft01 阶段仅有的 OR 文本。

> **任务类型与引擎是一对耦合判定**：任务类型（计划/流程/工单）↔ 引擎（@engine-task/@engine-flow/@engine-workorder）一一映射。BP 修正引擎 = 隐含修正任务类型——wft01 从 OR 语义判的任务类型是**业务语言初步值**（完整处理模式只有 BP 展开后才看清），BP 在完整上下文中校验引擎即对处理模式的终审。修正引擎时在变更原因中写清"任务类型从 X 修正为 Y"。

| 校验结果 | 判定条件 | 动作 |
|---------|---------|------|
| **确认** | 候选引擎与 BP 展开后的文档职责和处理模式完全匹配 | 去掉 `[推断]` 标记，升级为已确认的支撑引擎 |
| **修正** | 候选引擎部分匹配但需调整（如 @engine-flow → @engine-workorder） | 写入修正后的引擎 + 修正原因（如"BP 设计发现需派发/验收能力，@engine-flow 不覆盖"）+ **同步修正任务类型**（流程→工单）+ 去掉 `[推断]` |
| **推翻** | 候选引擎与 BP 展开后的文档处理模式不兼容，且 25 资产中无可替代引擎 | 标注 `[引擎待确认]` + 不兼容原因说明 → Step End 提示人类裁决 → 不阻塞其余文档的设计 |

> **引擎权威定位**：STR-F 节点中的引擎字段是 wft01 的业务语言**线索**（`[推断]`），设计链的引擎权威是本 Skill 校验后的结论——wft03 消费的引擎信息以此为准（[91 规范 §7.2](91-eos-biz-eng-spec.md#72-业务流程开发wft02-biz) 任务 6）。修正/推翻时记录变更原因（写入文档的引擎判定依据字段），供 wft01 回查；**不回写 STR-F 节点**——`[推断]` 值为线索非结论，wft01 下次 Phase B 修订时关系检测会重新识别引擎（引擎判定变更行），天然对齐。

### 3.3 资产对齐与一致性检查规则

**一句话**：本节讲资产对齐怎么判定——对齐范围与 Q 判定（3.3.1）、SL4/PL4 归纳分组（3.3.2）、23 资产一致性检查（3.3.3）。Step 3（角色场景视角）与 Step 4（输出产品视角）共用以下规则。

#### 3.3.1 资产对齐范围与 Q 判定

**一句话**：本节分两块看——**资产对齐范围**（23/24/26/27/28 各对齐什么）与 **Q1/Q2/Q3 判定**（当前设计对象与已有资产的匹配程度）。

**资产对齐范围**：

| 资产 | 内容 | Q1/Q2/Q3 含义 |
|------|------|--------------|
| **23** 产品架构 | A2产品/Bn产品产品路径（含 A2 菜单可达性路径）。A2产品侧对齐记为 `23A`，Bn产品侧对齐记为 `23B` | Q1=直接引用已有路径 / Q2=扩展已有 / Q3=新建 |
| **24** 业务资产 | SL4 角色职责、SL5 场景业务 | Q1=已有资产复用 / Q2=扩展已有业务 / Q3=新建业务块 |
| **26** 文档定义 | 输出文档对应功能表单的定义与归属 | Q1=同名同义文档已存在 / Q2=相似文档需扩展 / Q3=无匹配需新建 |
| **27** 资源资产 | 支持信息中的 CAX 工具/AI 工具/模板/专业能力/作业指南 | 已有资源追加引用；缺失资源形成建议 |
| **28** 看板指标资产 | 看板文档定义、指标文档定义（均为独立 PL5） | Q1=已有文档复用 / Q2=扩展已有文档 / Q3=新建文档 |

**Q1/Q2/Q3 对齐判定**——对每个设计对象，按资产对齐范围执行对齐判定。Q1/Q2/Q3 描述当前设计对象与已有资产的匹配程度——**Q1（复用）**：已有资产完全匹配，直接引用；**Q2（扩展）**：已有资产部分匹配，需扩展边界；**Q3（新建）**：无匹配资产，需新建。

| 资产 | Q1 复用 | Q2 扩展 | Q3 新建 |
|------|---------|---------|---------|
| **23** 产品架构 | A2/Bn 产品路径存在且职责边界一致，直接引用已有路径 | 路径存在但需扩展构件类型、子节点或菜单可达性边界，引用路径并写扩展建议 | 路径不存在，写新建路径建议（名称/归属/边界） |
| **24** 业务资产 | 已有同名/同义业务块且职责范围一致，直接引用 | 存在相似业务块但职责需扩展，引用并写扩展建议 | 无匹配业务块，写新建业务块建议（名称/职责/归属路径） |
| **26** 文档定义 | 已有同名/同义文档定义且业务域、构件类型一致，直接引用 | 存在相似文档定义但名称、归属或范围需扩展，引用并写扩展建议 | 无匹配文档定义，写新建文档定义建议（名称/职责/归属业务域） |
| **28** 看板指标资产 | 已有同名/同义看板且指标方案一致，直接引用 | 存在相似看板但指标方案需扩展（新增/修改指标），引用并写扩展建议 | 无匹配看板，写新建看板建议（名称/职责/归属路径） |

**27 资源资产对齐判定**：27 不使用 Q1/Q2/Q3。已有资源直接追加 BP 引用（去重）；缺失资源形成资源资产建议（名称/类型/用途，建议状态=`待确认`）。

**28 看板指标资产对齐判定**：28 的 Q1/Q2/Q3 聚焦看板级别——已有看板匹配则 Q1，需扩展看板则 Q2，无看板则 Q3。看板内的单个指标不独立走 Q 判定，随看板方案整体对齐。

#### 3.3.2 SL4/PL4 归纳分组

**一句话**：SL4 角色职责从 SL5 场景业务归纳、PL4 WBS 从 PL5 输出文档归纳——归入同一 WBS 的输出文档满足**完成耦合**。

SL4 角色职责从 SL5 场景业务的职责边界归纳分组，PL4 WBS 从 PL5 输出文档的业务模块归属归纳分组；命名取共性特征。

**WBS 分组硬判据——数据依赖链**：归入同一 WBS 的交付类输出文档，其正文数据须在同一数据依赖链上——前一文档的正文数据成为后一文档的输入（[91 规范 §5.3](91-eos-biz-eng-spec.md#53-bn产品) 交付类主价值链），由此组内文档满足完成耦合。**跨数据依赖链的文档不归入同一 WBS**；同链文档按业务环节切段归组（链过长时，如"采购申请创建与维护" vs "供应商评估与准入"）。管理类文档（指标/看板/风险/问题/计划清单等）不单独成链，按其支撑的交付链归属 WBS。

归入同一 WBS 的输出文档满足**完成耦合**——WBS 中最后一个文档未确认完成时，其内容变化将迭代要求前序文档相应调整；仅当 WBS 内全部文档均确认完成，该 WBS 的产出才稳定。角色仅一个 SL5 时 SL4 不独立占级（菜单深度降为 3 级），PL4 同理。已有 SL4/PL4 可扩展边界容纳新 L5 节点（Q2），无法归入时新建（Q3）。

#### 3.3.3 23 资产一致性检查

**一句话**：BP 展开后对 wft01 已写回的 23 引用执行一致性检查——检出三种不一致（归属冲突/误新建/误引用），矛盾而非静默覆盖。

wft01-biz Step 5 已按 STR-F 的 23B 判定（Q1/Q2/Q3）写回 23 资产的 Bn 侧引用——即输出文档→E2E 项目业务/构件类型的对齐关系。本 Skill 在 BP 展开后拥有更完整的上下文（文档职责/处理动作/状态变迁路径），需对 wft01 已写回的 23 引用执行一致性检查，检出矛盾而非静默覆盖。

**检查时机**：Step 4 PL 路径确定后（§5.5.1 ② PL4 节点生成完成后），逐文档比对 wft01 的 23B 判定与 BP PL 路径推导结论。

| 不一致类型 | 判定条件 | 动作 |
|-----------|---------|------|
| **归属冲突** | wft01 23B-Q2/Q3 建议的 E2E 项目业务或构件类型归属，与 BP PL 路径推导的 PL2/PL3 不一致 | 标注 `[23归属冲突]` + 两者路径对比（wft01 建议 vs BP 推导）+ 冲突原因说明，提示人类裁决 |
| **误新建** | wft01 23B-Q3 建议新建 23 条目，BP 上下文发现已有 23 条目（同名/同义且职责范围覆盖）可承接 | 降为 Q2（扩展已有），记录降级原因和已有条目路径；不写入两条并行路径 |
| **误引用** | wft01 23B-Q1 引用已有条目，BP 展开后发现该条目职责边界不能覆盖当前文档 | 升为 Q2（扩展）或 Q3（新建），记录升级原因和 BP 上下文中发现的覆盖缺口 |

**与 Step 6 资产写回的关系**：一致性检查聚焦**检出矛盾**——产出矛盾标注和裁决提示，不自行修正 23 资产中的已有内容。矛盾项在 Step 6 写回时**暂不写回 23**（待人类裁决后下一轮处理），其余无矛盾项正常写回。

**与并行推进的关系**：wft01 资产写回（Step 5）与 wft02 BP 设计可并行推进。若 wft02 运行时 wft01 尚未完成 23 写回：

- STR-F 23B 判定仅有 Q 级别和路径建议（无实际 23 资产块 ID）
- → 一致性检查降级为校验 Q 判定与 PL 路径的语义一致性
- → 仅检出归属冲突，不执行误新建/误引用判断（因无可比对的实际资产块）
- → Step End 时提示"wft01 23 写回未完成，一致性检查不完整"

### 3.4 PL6-B 正文条目需求判据

**一句话**：本节讲 PL6-B 的判定规则——三独立判定（3.4.1）与定义优先级/关系检测（3.4.2）；定义要素概念见 §2.5.4。

#### 3.4.1 三独立判定

**一句话**：先判定 PL5 正文是否为集合容器，再对其中的条目类型执行三独立判定（三条件见 wft01 §2.2.2）；轻量原子正文行、事务性物料行和普通议题记录等运行实例不展开。

先判定 PL5 正文是否为集合容器，再对其中的条目类型执行三独立判定。PCA 场景的看板、指标、计划、风险、问题、行动项等定义型/管理型条目通常满足三独立，但仍须逐类判定。轻量文档若正文是一行一个原子文档实例，该正文行停留在 PL5 实例层，不得重复展开为 PL6-B；事务性物料行和普通议题记录等运行实例同样不展开。

三独立的三项条件（独立名称 / 独立定义要素 / 独立生命周期）的判定口径与示例见 [wft01-biz §2.2.2](eos-wft01-biz-or2str.md#222-pl6-b-正文条目概念与三独立条件)，本 Skill 消费 wft01 在名称级捕获的条目类型，不重复判定条件。

#### 3.4.2 定义优先级与关系检测

**一句话**：本节分两块看——**定义优先级**（wft01 已捕获/标注 `[待wft02推断]`/未捕获条目怎么定义）与 **Phase B 关系检测**（条目级确认/补充/过时）。

wft01 已确认或标注 `[待wft02推断]` 的 PL6-B 条目，本 Skill 按以下优先级定义：

| 条目情况 | 定义方式 |
|---------|---------|
| OR 明示/隐含的条目名称 | 原样确认并展开定义要素 |
| OR 仅提管理模式的条目（wft01 标 `[待wft02推断]`） | 基于 BP 上下文推断定义要素，标注 `[推断]` |
| wft01 未捕获但对闭环必要的条目 | 先行推断 + `[推断-待OR确认]`，由 Step 5 生成 OR 材料 |

**Phase B 关系检测**（PL6-B 条目级）：

| 关系 | 判定 | 处理 |
|------|------|------|
| 确认 | 条目名称匹配 + 定义范围一致 | 来源升级，追加 BP 引用，定义要素可细化 |
| 补充 | 条目名称不匹配 | 新增 PL6-B 条目，全面定义三要素 |
| 过时 | 基线条目未被本轮覆盖 | `[过时]` + 说明，保留不删除 |

### 3.5 完备性判据

**一句话**：判据汇总表——按三层核对 BP 是否完备；实际执行在 §5.6 Step 5（覆盖自查）与 §七 自检清单。

| 层 | 完备性判据 | 缺口处理 |
|---|----------|---------|
| 双视角覆盖 | A2 侧覆盖场景已提到的业务（SL3~SL5 角色场景 + 需求分配到输出文档）；Bn 侧覆盖输出文档清单（PL 路径 + 文档方案 + 权限 + 缺口推断）；两视角经交汇点（§2.1.2）保持一致性（同一 STR-F 业务链天然保证）。**PCA 场景的业务链必须包含一个计划清单输出文档**（[91 规范 §2.7](91-eos-biz-eng-spec.md#27-pca-场景的-pca)，正文条目三独立展开 PL6-B，首条目=策划，与 R4 首文档（§2.5.3）为两个文档实例） | 缺口 → 标注 `[覆盖缺口]` + 缺失说明，留待下一轮补全（§5.6 覆盖自查） |
| 资产对齐 | 23/24/26/27/28 对齐判定到位（Q1/Q2/Q3），23 一致性检查完成（归属冲突/误新建/误引用已检出或标注） | 矛盾项标注 `[23归属冲突]` → Step End 提示人类裁决（§3.3.3） |
| 业务缺口闭环 | 文档缺口推断已执行，缺口不脑补节点——进业务缺口增补生成 FR-BIZ OR 材料。**C/A 是任务链运行时推进的结果**（指标引擎持续度量→看板呈现→发现偏差→生成风险/问题条目），不作为必须由 OR 覆盖的缺口；仅当业务链缺承载 C/A 的输出文档（指标/看板/风险/问题清单）时才构成缺口（[91 规范 §2.7](91-eos-biz-eng-spec.md#27-pca-场景的-pca)） | 缺口 → 生成 FR-BIZ OR 材料 + 登记 OR 原料状态表（§5.6） |

---

## 四、BP 节点演进：生命周期与变更传播

**一句话**：本章讲 BP 节点设计与确认后的演进机制——生命周期（4.1，节点状态四态主线 + 上游变更分流 + 下游退回）与变更传播（4.2，修订后写变更影响声明告诉下游 wft03）。

### 4.1 BP 节点生命周期

**一句话**：本节讲节点状态怎么走——四态主线（待确认方案→可以SR设计→在SR设计→已SR设计）+ 上游变更三条分流 + 下游退回态。

BP 节点沿 [91 规范 §A.6](91-eos-biz-eng-spec.md#a6-节点生命周期模型) 的四态主线流转及上游变更三条分流规则运行。本 Skill 的状态名映射按 §A.6 通用六态自声明：`待确认方案`/`可以SR设计`/`待补充SR设计`/`需wft02修订` 见下方主线流转与写回范围，`在SR设计`/`已SR设计` 由 wft03-biz 写入并推进。并行推进规则（资产写回与 SR-F 展开互不阻塞）见 91 规范 §A.6.3。

**主线流转**（wft02-biz ←→ wft03-biz）：

```
待确认方案 ──人类确认──→ 可以SR设计 ──wft03锁定──→ 在SR设计 ──wft03完成──→ 已SR设计
     ↑                      ↑                       │
     │                      │              wft03退回 │
     │                      │                       ↓
     └──── wft02修订 ──── 需wft02修订 ←─────────────┘
```

**上游变更**（STR-F 修订触发 BP 重入）：

```
可以SR设计 ──上游修订──→ 可以SR设计（版本递增，wft03 未消费）
在SR设计   ──上游修订──→ 在SR设计（版本递增，wft03 增量处理）
已SR设计   ──上游修订──→ 待补充SR设计 ──wft03补全──→ 已SR设计
```

> **分工**：wft02-biz 写入 `待确认方案` / `可以SR设计` / `待补充SR设计` / `需wft02修订`；wft03 写入 `在SR设计` / `已SR设计`。

#### 4.1.1 推进态（按顺序）

| 状态 | 含义 | 触发条件 |
|------|------|---------|
| `待确认方案` | 方案已生成或修订，等待人类反馈 | Step 3~5 方案与缺口出口生成完成后 |
| `可以SR设计` | 方案已确认，wft03 可启动 SR 架构设计 | 全部条目 `[同意]` + `[已处理]` |
| `在SR设计` | wft03 已锁定此 BP，正在展开 SR-F | wft03 写入活动锁 |
| `已SR设计` | SR-F 展开完成，BP 消费版本与当前版本一致 | wft03 处理完成且未检测到上游新版本 |

人类确认方案后，BP 节点推进为 `可以SR设计`，此时 wft03 即可启动 SR-F 展开——wft03 直接从 BP 节点（`04-*.md`）读取设计内容，不依赖 23/24/26/27/28 资产文件的写回结果。资产写回（Step 6）与 SR-F 展开（wft03）并行推进、互不阻塞。

#### 4.1.2 上游变更态（STR-F 汇入触发 BP 修订）

当新 STR-F 汇入已有 BP 触发修订时，BP 内容变更、版本号递增——但下游 wft03 可能已基于旧版本生成了 SR-F。修订后 BP 状态按 wft03 的消费进度分流：

| 修订前状态 | 修订后 | 说明 |
|-----------|--------|------|
| `可以SR设计` | 保持，版本递增 | wft03 尚未消费，不受影响 |
| `在SR设计` | 保持，版本递增 | wft03 正在处理中，重入时检测版本差异，按变更影响声明增量处理 |
| `已SR设计` | 降为 **`待补充SR设计`** | wft03 之前生成的 SR-F 基于旧版本 BP，需 wft03 重新介入补全 |

- **`待补充SR设计`**：wft03 之前已消费的 SR-F 节点不动（它们仍然有效），但 BP 新增了业务或功能表单需要生成新的 SR-F。wft03 进入后按 BP 的变更影响声明确定增量映射范围，处理完成后若消费版本追上 BP 当前版本 → 推进回 `已SR设计`

#### 4.1.3 下游退回态（wft03 发现缺陷退回修订）

| 状态 | 含义 | 触发条件 |
|------|------|---------|
| **`需wft02修订`** | BP 方案存在缺陷，需本 Skill 修订后重新交付 | wft03 写入，附带缺失说明 |

本 Skill 重入 `需wft02修订` 的 BP 时：读取 wft03 的缺失说明 → 按人类改进方案修订 BP（走 Step 3 修订流程）→ 人类确认 → BP → `可以SR设计`

#### 4.1.4 版本标记与消费追溯

- BP 节点初次生成时版本 = `v1`，变更影响声明写"首版，无变更"
- 每次因新 STR-F 触发 Step 3 修订，版本号递增
- 版本号写入 BP 节点头部（与节点 ID 同级）
- wft03 首次消费 BP 时，在对应 SR-F 节点中记录 `BP 消费版本：v{N}`
- wft03 重入同一 BP 时比对版本：若 BP 当前版本 > 已消费版本 → 触发变更感知

### 4.2 BP 变更传播协议

**一句话**：每次修订已有 BP 后写变更影响声明——三类变更（增量/修订/结构）各告诉下游 wft03 该怎么做。

变更传播协议的通用定义（变更类型分类、判定优先级、声明格式模板、增量清单与变更影响声明的分工）见 [91 规范 §A.7](91-eos-biz-eng-spec.md#a7-变更传播协议)。

#### 4.2.1 变更类型分类（BP → SR-F 特化）

| 变更类型 | 判定标准 | 对下游影响 |
|---------|---------|----------|
| **无变更** | 本轮新输入全部走来源确认或引用追加；无新增/改进/边界扩展/结构调整 | 下游无需任何动作 |
| **增量型** | 仅新增业务/场景要素，已有业务/产品路径/功能表单结构均未改动 | 已有 SR-F 节点不受影响，仅需映射新业务 |
| **修订型** | 已有业务/场景要素被修改；产品路径与功能表单结构未变 | 对应 SR-F 节点需局部调整 |
| **结构型** | 产品路径需新增 SL2/PL2 节点或跨业务域重组、菜单新增/合并/拆分、BP 结构需重塑 | SR-F 结构可能重塑，需人类裁决后方可处理 |

> 注：Q2 在已有路径下扩展子节点属「修订型」，不触发「结构型」。判定优先级：结构型 > 修订型 > 增量型 > 无变更。

#### 4.2.2 变更影响声明格式

每次修订已有 BP 后，在 BP 节点末尾写入（`04-*.md` 中 BP 节点块内）：

```text
### 变更影响声明

BP 版本：v{N}（本次修订前为 v{N-1}）
本轮变更类型：无变更 / 增量型 / 修订型 / 结构型

变更项：
- [新增] / [变更] / [确认] / [需裁决] ...
  （无变更时写"无实质变更，本轮仅来源确认/引用追加"）

下游已消费此 BP 的 SR-F 节点：
  <SR-F节点ID列表>（如无则写"尚未被下游消费"）

对已消费 SR-F 节点的影响：
  受影响节点：<SR-F节点ID + 受影响原因>
  不受影响节点：<SR-F节点ID>
  待新增 SR-F 映射：<新业务/新功能表单/权限需求对应的待映射说明>

建议 wft03 动作：
  - 无变更：无需任何动作
  - 增量型：增量映射新增 PL5/权限需求即可，已有 SR-F 节点不动
  - 修订型：修订受影响的 SR-F 节点 → 标记 [来自上游变更]
  - 结构型：输出影响范围，等人类裁决后再处理
```

#### 4.2.3 增量清单与变更影响声明

分工见 [91 规范 §A.7.4](91-eos-biz-eng-spec.md#a74-增量清单与变更影响声明的分工) 对照表。

---

## 五、操作流程

**一句话**：一次运行 = Step Start 入口路由 → Step 1 材料加载 → Step 2 反馈处理 → Step 3 角色场景设计 → Step 4 输出产品设计 → Step 5 业务需求增补 → Step 6 资产写回 → Step End 输出与对话反馈。反馈走自然语言对话（§5.3），跨轮余留由 AI 标记持久化。

### Step 布局总览

| Step | 名称 | 职责 | 人类参与 | 执行模式 | 产出 | 写资产 |
|------|------|------|---------|---------|------|--------|
| Start | 前置校验与路由 | 变更感知 + 入口检测、输入校验、退回优先路由 | 无（AI 自动） | 统一 | 就绪 / 结束 | — |
| 1 | 设计材料加载 | 安全复查 + 读 AI 可以处理节点 + 加载 STR-F/BP/资产 | 无（AI 自动） | 统一 | STR-F / BP / 资产全景 | — |
| 2 | 方案反馈处理 | 读"待反馈处理"分节 + 对话处理人类修改意见 | **有**——对话反馈修改意见 / 整体确认 | 对话 | 调整后的 BP 方案 | `02/04` |
| 3 | 角色场景视角 BP 设计 | SL3~SL5 角色场景 + 需求分配到输出文档 + 23/24 对齐 | 无（AI 产出，之后对话反馈） | 统一 | A2 侧 BP 方案 | `02/04` |
| 4 | 输出产品视角 BP 设计 | PL 路径 + 文档方案 + 活动/指标 + PL6-B + 任务关系 + 缺口 + 权限 | 无（AI 产出，之后对话反馈） | 统一 | Bn 侧 BP 方案 | `02/04` |
| 5 | 业务需求增补 | 汇聚缺口 + 覆盖自查 + 生成 FR-BIZ OR 材料 | 无（AI 自动） | 统一 | 缺口说明 + OR 材料 + 状态表登记 | `04` |
| 6 | 资产写回与落账 | 23/24/26/27/28 写回 + 推进 BP/STR-F + 提交基线 | 无（AI 自动，写回已确认节点） | 统一 | 资产写回记录 | `23/24/26/27/28/02/04` |
| Step End | 运行结束输出 | 三区摘要输出 + 等待人类反馈 | **有**——查看摘要、对话反馈 | 对话 | 人类下一步操作提示 | — |

> **执行模式白话**：统一 = AI 自动跑完这步，不需你介入；对话 = AI 输出后需要你说句话（反馈或确认）才继续。Step 1/3/4/5/6 为运行内自动步骤；Step 2 / Step End 为人机交互步骤（对话反馈，见 §5.3）。

**反馈机制一句话**：反馈走**自然语言对话**（AI 产出方案后不结束对话，§5.3）；跨轮余留反馈由 AI 标 `[修改]`/`[驳回]` 入"待反馈处理"分节（§5.3 状态机）；人类线下修订经 Step Start 变更感知（git diff，§5.1 变更感知）检出后纳入"待反馈处理"分节；AI 每轮写回提交基线（§5.7），保证下一轮 Step Start 的 `git diff <HEAD @上次 AI 运行>..HEAD` 只含人类变更。

---

### 5.1 Step Start

**一句话**：先变更感知（检出人类线下修订并纳入待反馈分节），再按 [§A.4](91-eos-biz-eng-spec.md#a4-入口条件与优先级step-start) 入口决策路由（退回优先 → 反馈处理 → 新输入），无待处理对象即退出。

**变更感知（检出人类线下修订，先于入口判定）**

读取目标文件前先检测自上次 AI 运行以来人类是否修改过文件（[§11.10](91-eos-biz-eng-spec.md#1110-ai-反馈检测规则)）：

1. 读取文件头 `**HEAD @上次 AI 运行**` 字段，获取上次提交 hash H
2. Bash("git diff H..HEAD -- 目标文件") → 返回 H 到当前 HEAD 之间的全部变更
3. 无变更 → 进入入口判定
   有变更 → 解析变更行号，读取变更行附近的节点块（上下 5 行）
   → 判定变更类型：结构化标注（`[同意]`/`[修改]`/`[驳回]`）→ 纳入待反馈；非标自由文本 → 标记 `[需确认]`；无关修改（格式/排版）→ 忽略
   → 将存在反馈的节点加入AI可以处理节点"待反馈处理"分节

> 变更感知必须先于入口判定——人类纯线下修订（不选新 STR-F）时，只有 git diff 能发现该反馈并将其纳入"待反馈处理"分节，入口判定②才命中。若入口判定先于变更感知，纯线下修订会被误判"无待处理对象"退出。

**入口判定**（在变更感知后执行）：按 [§A.4](91-eos-biz-eng-spec.md#a4-入口条件与优先级step-start) 入口决策流程执行。

首先读取 `04-*.md` 的 `## AI可以处理节点` 分节（[§11.7](91-eos-biz-eng-spec.md#117-ai可以处理节点)），获取三类入口节点列表：

> **待 wft02-biz 处理** → 新 STR-F 节点  
> **待反馈处理** → 人类退回的 BP 条目  
> **待下游退回处理** → `需wft02修订` 的 BP 节点  

然后按以下顺序处理：

#### 5.1.1 输入校验

| 校验条件 | 判定 | 动作 |
|---------|------|------|
| BP 节点状态为 `需wft02修订` 且人类未提供改进方案 | 退回方案缺失 | 输出 wft03 的缺失说明，提示提供改进方案 → **退出** |
| 无 STR-F 选中 + 无"待反馈处理"节点 + 无"待下游退回处理"节点 | 无待处理对象 | 输出"无待处理对象"→ **退出** |
| STR-F 节点状态不是 `可以分解分配` / `待补充分解分配` | 状态不符 | 输出状态分布 → **退出** |
| 选中节点含 STR-E / STR-NFR 类型 | 类型不匹配 | 输出类型分布（STR-E→`wft02-eng`，STR-NFR→`wft04-nfr-str2sr`）→ **退出** |
| 选中 STR-F >5 个 | 超建议阈值（软提示） | 输出建议分批 + 节点 ID 列表 → 人类确认后继续或退出 |

> 软提示不强制退出，由人类选择继续或退出。逻辑上不可能的条件（状态不符、类型不匹配）为硬退出（§A.4.5）。

#### 5.1.2 退回优先（§A.4.3 门禁）

存在 `需wft02修订` 的 BP 节点（来自"待下游退回处理"分节）：
- 检查人类是否已提供改进方案（读取退回节点的缺失说明中是否含人类标注或下游 Skill 的具体改进指引）
- 已提供 → 通过门禁，读取 wft03 的缺失说明，按人类方案修订 BP
- 未提供 → 输出 wft03 的缺失说明 + 提示提供改进方案 → **退出**

#### 5.1.3 反馈处理

存在"待反馈处理"分节中的 BP 节点（确认状态 = `[修改]`/`[驳回]` 且无 `[已处理]`）→ 进入 Step 2 处理。若全部条目已 `[同意]`+`[已处理]`，不阻塞新 STR-F，Step End 时提示。

#### 5.1.4 新 STR-F 路由

有未处理的 STR-F 节点（来自"待 wft02-biz 处理"分节）→ 进入 Step 1→Step 3，按产品路径+用户角色匹配已有 BP（Q1 归入/Q2 扩展归入/Q3 新建），逐节点展开设计。

**执行顺序约束**：Step 2（反馈处理）必须先于 Step 3（新增设计）。

| 原因 | 规则 |
|------|------|
| 反馈可能改变 BP 的产品路径、业务清单或权限结论 | Step 3 必须基于 Step 2 修改后的最新 BP 内容，不得使用缓存旧状态 |
| 与 §A.4.4 规则三一致 | 退回优先 → 反馈处理 → 新输入路由 |

---

### 5.2 Step 1 · 设计材料加载

**一句话**：把本轮要处理的材料加载齐——先安全复查（git diff 检非预期修改），再读 AI 可以处理节点，最后按需索引导航。变更感知已在 Step Start 完成（§5.1）。

按 [§A.3.3](91-eos-biz-eng-spec.md#a33-产品数据文档与资产文档读取与写回约定) 读取协议执行，分三步：

**① 安全复查**

Step Start 已检出人类反馈并纳入"待反馈处理"分节（§5.1 变更感知）。此处按 §A.3.3 R0a 做加载前安全检查：`detect-changes.sh` 检出人类非预期修改时标记 `[需确认]`，不自动推进。

**② 读 AI 可以处理节点（优先）**

读取 `04-*.md` 的 `## AI可以处理节点` 中本 Skill 对应分节：

| 分节 | 获取内容 |
|------|---------|
| "待 wft02-biz 处理" | 待处理 STR-F 节点 ID 列表 |
| "待反馈处理" | 待处理的 BP 反馈条目 |
| "待下游退回处理" | `需wft02修订` 的 BP 节点 |

- 有节点 → 逐节点 `grep -n '@{节点ID}'` 定位行号 → Read 节点块
- 无节点 → 进入步骤三

**③ 索引导航**（AI可以处理节点无待处理时作为备用）

通过业务域索引/角色索引定位目标段：

1. `grep -n '索引行数：' 目标文件` → 定位索引行号 L，提取 N
2. Read 业务域索引/角色索引 → 获取锚点
3. 按锚点 Read 域内索引 → 获取节点 ID 列表
4. 按需 `grep -n '@{节点ID}'` 定位行号 → Read 节点块

**加载清单**（定位后的具体加载内容）：

| 资产 | 用途 |
|------|------|
| `02-*.md` — STR-F 节点块 | 定位本轮 STR-F 正文（含输出文档清单、引擎候选、PL6-B 条目、Bn 对齐） |
| `04-*.md` — BP 节点块 + AI可以处理节点 | 定位本轮 BP 节点 + 待处理节点清单 |
| `23-eos-output-architecture.md` 资产全貌层 | A2/Bn 路径对齐锚点 |
| `24-eos-business-assets.md` 资产全貌层 | 可复用业务资产匹配 |
| `26-eos-document-definitions.md` 资产全貌层 | 文档定义查重与引用 |
| `25-eos-engine-models.md` §1.1 | 引擎清单（运行时动态读取，不缓存） |

**按需扩大**（仅在以下情况允许）：

- 目标节点 ID 在索引中无法定位
- 需要跨域检查引用一致性
- 人类明确要求通读
- 候选 BP 块 — 按同业务域/同角色/同产品类型筛选，用于 Q1/Q2 归入匹配
- 候选 24/26 资产段 — 按输出文档名称/构件类型匹配

> 禁止行为：不得在常规运行中默认加载完整文件正文；不得将 `疑似` 或 `待确认` 节点的 ID 作为下游强依赖（§A.3.3 R3）。

---

### 5.3 Step 2 · 方案反馈处理

**一句话**：处理人类反馈——对话内即时处理（修改 / 整体确认，§5.3.1），跨轮余留按 `[修改]`/`[驳回]` 标记处理。

人类审阅与 AI 处理的通用协议见 [91 规范 §A.5](91-eos-biz-eng-spec.md#a5-反馈交互协议)。以下为本 Skill 的特化规则。

**反馈双轨**——人类可任选两种方式反馈，本 Skill 均支持：

- **AI 对话反馈**：Step End 后不结束对话，直接在对话中提出修改意见（自然语言）——AI 按 §5.3.1 即时处理（主通道）
- **线下文档修订**：直接打开 `04-*.md` 修订——在确认状态字段标注 `[同意]`/`[修改]`/`[驳回]`，或直接编辑方案内容（自由文本）——经 Step Start 变更感知（git diff，§5.1 变更感知）检测后按 §A.5 处理

两轨等价：对话反馈由 AI 即时落账（追 `[已处理]`），线下修订经 git diff 检出后同样落账；AI 每轮写回提交基线（§5.7），保证下一轮 diff 只含人类变更。

**本 Skill 的条目**：BP 节点的 A2 方案 / Bn 方案 / 输出文档设计 / PL6-B 正文条目需求 / 数据权限 / 资产对齐结论。各条目的 `确认状态` 字段位于 `04-*.md` BP 节点块各条目正文中。

#### 5.3.1 AI 处理流程

按 [91 规范 §A.5](91-eos-biz-eng-spec.md#a5-反馈交互协议) 的统一流程处理人类反馈：扫描待处理条目 → 收集整体确认 → 逐条处理 `[同意]`/`[修改]`/`[驳回]` → 级联检查（需求优先、资产对齐随后）→ 判定节点状态 → 确定写回时机 → 落反馈处理记录。

> **原则**：反馈处理不是对人类标注的机械执行，而是以 STR-F 需求理解为锚点的方案修订。AI 在 Step 2 中与 Step 3~5 同为设计者——先理解人类反馈在 STR-F 需求语境中的含义，再执行修订，最后检查修订后的方案是否仍满足 STR-F 需求。

本 Skill 特化参数（锚点需求 / 操作条目 / 推进目标 / 级联触发条件 / 资产对齐级联）见下表（按 [§A.5.4](91-eos-biz-eng-spec.md#a54-其他规则) 实例化）：

| 特化维度 | wft02-biz 值 |
|---------|-------------|
| **锚点需求** | 当前 BP 所承接的全部 STR-F 节点——人类反馈理解和修订均以 STR-F 场景业务需求为最终锚点 |
| **操作条目** | A2 方案 / Bn 方案 / 输出文档设计（含 PL6-A 活动节点 + PL6-B 正文条目需求）/ 数据权限 / 23/24/26/27/28 资产对齐结论。条目 `确认状态` 字段位于 `04-*.md` BP 节点块各条目正文中 |
| **推进目标** | 全部条目含 `[同意]`+`[已处理]` → BP 推进至 `可以SR设计` |
| **清除条件** | 已有 `[同意]`+`[已处理]` 默认跳过。仅当本轮新输入满足**任一**条件时，清除 `[已处理]`（保留 `[同意]`）：<br>1. 新输入是该条目的直接上游节点（如新 STR-F 是 BP 来源）<br>2. 新输入与条目同属一个架构层级且边界重叠（如同属 PL5，新文档改变了已有文档的范围/归属）<br>3. 新输入导致条目定义被修订（变更影响声明标注为修订型/结构型，§A.5.3 Step 1） |
| **级联触发条件** | `[修改]`/`[驳回]` 导致方案结构性变更（产品路径变更/文档增删/引擎变更/PL6-B 条目增删）→ 标记受影响条目为 `[需重设计]` |
| **资产对齐级联** | 方案变更影响 23/24/26/27/28 资产引用 → 更新受影响资产的 Q 判定和引用路径 |

#### 5.3.2 确认状态格式示例

**AI 写入（Step 3~5 完成时）** —— 为每个条目初始化 `确认状态` 字段，初始为空：

```text
- A2-场景业务-采购员执行采购申请 | 确认状态: (空)
- 采购申请单（输出文档，@engine-flow + @engine-entity） [23B-Q2] | 确认状态: (空)
- 采购申请合格率（看板，@engine-kanban） | 确认状态: (空)
```

**人类在条目 `确认状态` 字段标注** —— 三种标注形式：

```text
- A2-场景业务-采购员执行采购申请 | 确认状态: [同意]
- 采购申请单（输出文档，@engine-flow + @engine-entity） [23B-Q2] | 确认状态: [修改]：引擎应改为 @engine-flow + @engine-entity
- 采购申请合格率（看板，@engine-kanban） | 确认状态: [驳回]：该指标已有 BP-YYY 覆盖，不重复
```

**AI 处理完成后** —— 在人类标注末尾追加 `[已处理]`：

```text
- A2-场景业务-采购员执行采购申请 | 确认状态: [同意] [已处理]
- 采购申请单（输出文档，@engine-flow + @engine-entity） [23B-Q2] | 确认状态: [修改]：引擎应改为 @engine-flow + @engine-entity [已处理]
- 采购申请合格率（看板，@engine-kanban） | 确认状态: [驳回]：该指标已有 BP-YYY 覆盖，不重复 [已处理]
```

人类改写 `确认状态` 即覆盖旧内容，`[已处理]` 自然消失，条目回到待处理状态。完整历史追溯通过 BP 版本号 + 变更记录实现。

---

### 5.4 Step 3 · 角色场景视角 BP 设计

**一句话**：核心设计步（A2 侧）——以 STR-F 场景职责边界为锚，落成 A2 角色场景视角 BP，核心工作是给每个 STR-F 的 SL5 场景业务确定一个 SL4 角色职责归宿。

以 STR-F 场景职责边界为锚，落成 A2 角色场景视角 BP。核心工作是给每个 STR-F 的 SL5 场景业务确定一个 SL4 角色职责归宿。

#### 5.4.1 推断 SL4 角色职责

SL4 角色职责是 Step 3 的核心产出——从 STR-F 的场景职责归纳，取名取共性特征。按已有资产情况分流：

| 判定 | 条件 | 动作 |
|------|------|------|
| **Q1 复用** | 已有同名/同义 SL4 且职责范围一致 | 直接引用，STR-F 归入该 SL4 |
| **Q2 扩展** | 已有相近 SL4 但需扩展其职责边界 | 扩展已有 SL4，归纳规则见 §3.3.2 |
| **Q3 新建** | 无匹配的已有 SL4 | 新建 SL4 角色职责节点，写入 `04-*.md` |

SL4 占级规则见 §3.3.2。SL3 角色类型从 STR-F 用户角色直接映射，不重复判定。

Q1/Q2 归入已有 SL4 时，将本轮新 STR-F 的输出文档清单与该 SL4 下已有内容逐项比对，按 §1.1 收敛规则（确认/补充/无新增/矛盾）执行对应动作：确认→升级来源，补充→全量重新展开（非增量追加），无新增→仅加引用，矛盾→记录待人类裁决。

#### 5.4.2 输出文档需求定义

场景业务的需求由 STR-F 定义，Step 3 将其分解分配到各输出文档——每个输出文档承接一部分场景职责。对 STR-F 业务链中的每个输出文档，确定：

- **文档职责**：该文档在场景中承担什么角色（如"承载申请数据""记录审批结果""汇总指标"）
- **信息来源**：数据从哪个前序文档流入，或由用户直接录入
- **输出去向**：产出物被哪个后续文档消费，或直接交付给用户

这是将 STR-F 的"场景业务"分解为 BP 的"文档级需求"的第一步。输出文档的完整设计（PL 路径、生命周期、权限）在 Step 4 完成。

SL5 与 STR-F 1:1 映射，场景职责复用 STR-F 的场景职责边界。将上述分配结果连同触发动作和上下文传递点按 STR-F 的业务链顺序写入 BP 节点。流程/工单类输出文档的活动节点引用规则见 §2.5.2。

完成写入后执行 23/24 对齐判定（规则见 §3.3.1）。

---

### 5.5 Step 4 · 输出产品视角 BP 设计

**一句话**：核心设计步（Bn 侧）——将 STR-F 的输出文档清单落成 Bn 输出产品视角 BP：PL 路径 + 文档方案 + 活动/指标 + PL6-B + 任务关系 + 缺口 + 权限。

将 STR-F 的输出文档清单落成 Bn 输出产品视角 BP——生成 PL4 WBS 节点，定义输出文档方案（正式文档：封面字段+正文实体结构+支撑引擎+支持信息引用；轻量文档准用），设计流程/工单的活动节点与任务关系，定义 PL6-B 正文条目需求，推断文档缺口，推导权限。设计过程中识别的业务缺口不在此步生成 OR 材料，交由 Step 5 统一处理。

#### 5.5.1 引擎校验与 PL4 节点生成

**① 引擎候选建议校验**：对每个输出文档，按 §3.2 校验规则，基于 BP 上下文逐文档校验 wft01 的引擎候选建议——确认则去掉 `[推断]`、修正则写入修正后引擎+修正原因、推翻则标注 `[引擎待确认]`。校验后的引擎结论（确认/修正/推翻）写入输出文档的引擎标注字段。PL3 构件类型的判定来源见 ②。

**② PL4 节点生成**：对每个输出文档，PL1（业务域）从 STR-F 读取，PL2（E2E 项目业务）和 PL3（构件类型）从 STR-F 23B 判定读取。若 wft01-biz 尚未产出 23B 判定，AI 基于输出文档性质和校验后的支撑引擎类型推断，标注 `[推断]`。

PL4 WBS 是 Step 4 的核心结构产出——将同一构件类型下的输出文档按业务模块归纳分组，形成 PL3 到 PL5 之间的中间节点。取输出文档名称/用途/支撑引擎的共性特征命名 WBS 包（如"采购申请创建与维护""供应商评估与准入"）。已有 WBS 可扩展边界容纳新文档（Q2），无法归入时新建（Q3）。WBS 归组规则（数据依赖链硬判据 + 完成耦合）见 §3.3.2。

**③ 23 资产一致性检查**：PL 路径确定后，按 §3.3.3 规则逐文档比对 wft01 的 23B 判定与 BP PL 路径推导结论。检出归属冲突→标注 `[23归属冲突]` + 提示人类裁决；检出误新建→降为 Q2 + 记录降级原因；检出误引用→升为 Q2/Q3 + 记录升级原因。矛盾项在 Step 6 写回时暂不写回 23，待人类裁决后下一轮处理。

#### 5.5.2 输出文档方案定义

对每个输出文档，在 PL 路径和支撑引擎确定后，定义文档的完整结构方案。正式文档与轻量文档的方案要素不同。

**正式文档方案**：

| 要素 | 定义内容 | 示例（采购申请单） |
|------|---------|------------------|
| **封面字段** | DocumentCover 表映射——单据号/标题/状态/版本/创建人/部门/日期等文档级元数据字段 | 单据号（自动生成）、标题（手动录入）、状态（草稿/已提交/审批中/已通过/已驳回）、版本（v1.0） |
| **正文实体结构** | 内容实体的字段集——字段名/字段类型/约束/CRUD。从 STR-F 输出文档的 `业务链文档序列` 提取，确保字段覆盖场景需求 | 物资编码（FK→物资主数据）、物资名称、规格型号、申请数量、预算科目、期望到货日期 |
| **支撑引擎** | 由 §5.5.1 ① 校验后的引擎结论直接引用，不再重新判定 | @engine-entity + @engine-flow |
| **支持信息引用** | 引用关系——引用文档名称/引用方式（只读/更新）/引用用途。去重，同一文档被多次引用只记录一次 | 引用「采购策略表」（只读，用于匹配审批路径） |

**轻量文档方案**：

轻量文档无封面，正文实体结构定义同正式文档的正文实体结构；支撑引擎由校验后的引擎结论直接引用；支持信息引用处理规则同正式文档。

**执行规则**：
- 封面字段以 DocumentCover 表模板（[91 规范 §2.2](91-eos-biz-eng-spec.md#22-正式文档的组成)）为基，按文档类型裁剪——正式流程文档添加审批相关字段、正式计划文档添加计划周期字段
- 正文实体字段集从 STR-F 输出文档的业务链文档序列提取
- 支持信息引用去重处理
- 全部方案内容标注 `[推断]`，经人类确认后方可落账

---

#### 5.5.3 任务活动设计

仅针对 STR-F 已确认的输出文档。对**有任务类型**的输出文档（计划/流程/工单）按 §2.5 定义活动节点序列——逐节点定义处理角色、可执行动作、状态变迁、系统自动节点/异常路径/超时处理，画出状态变迁路径（如草稿→已提交→审批中→已通过/已驳回）。按任务类型分流：

- **流程类**：审批流转序列（如创建→提交→审批→通过/驳回），按业务需求设计完整节点
- **工单类**：派发/处理/验收序列，按业务需求设计完整节点
- **计划类**：策划→发布标准化节点（[91 规范 §2.6](91-eos-biz-eng-spec.md#26-任务间触发关系)），消费 wft01 捕获/推断的节点（业务语言）→ 转 @engine-task 引擎配置（§2.5.1）；通常为标准序列，仅补系统节点/异常/超时

推断依据（按优先级逐层驱动，非平铺并列）：
  - **主驱动——对 STR-F 场景需求的理解**：该输出文档在业务链中承担什么职责、需要哪些处理步骤方能完成端到端闭环、每个步骤由哪个角色执行并产出什么文档状态。活动节点本质是对 STR-F 需求的方案响应（需求/方案相对原则），而非对 OR 词汇的机械映射
  - **约束——支撑引擎的能力边界**：设计的活动节点是否在 `@engine-flow` / `@engine-workorder` / `@engine-task` 的能力范围内（如流转模板定义的可用节点类型和合法状态迁移）。超出引擎能力的节点不修改方案迁就，标注为引擎需求缺口
  - **参考——同类文档模式 + AI 经验**：同域/同引擎类型的已有活动节点序列作为参考模板，行业通用流程模式作为补充材料，不替代对 STR-F 需求的分析
  - **门禁——人类反馈与确认**：全部活动节点标注 `[推断]`，经由人类在确认状态字段标注 `[同意]` / `[修改]` / `[驳回]` 后方可落账，不得未经确认直接写入资产

无任务类型的输出文档无需此项，跳过。

**缺陷路由**：设计中发现 wft01 捕获的业务流转有缺陷（如审批链缺环节/顺序不对）→ 标注 `[需裁决]` + 缺陷说明 → Step End 提示人类裁决，或走退回 wft01（`需wft01修订`）——**不自主改业务流转**（业务流转是 STR 权威，边界判据见 §3.2）。

#### 5.5.4 看板指标设计

仅针对 STR-F 已确认的看板类输出文档。对看板类输出文档，按 §2.5.2 定义指标序列——逐指标标注指标名称、度量口径、度量目标、聚合维度、显示图示和刷新频率。

推断依据（按优先级逐层驱动，非平铺并列）：
  - **主驱动——对 STR-F 场景需求的理解**：该看板在业务链中承担什么度量职责、需要哪些指标方能完整反映业务健康状况、每个指标如何度量与展示。指标本质是对 STR-F 需求的方案响应，而非对 OR 词汇的机械映射
  - **约束——支撑引擎的能力边界**：设计的指标及其显示图示是否在 `@engine-kanban` 的能力范围内（如支持的图表类型、聚合规则、刷新机制）。超出引擎能力的指标不修改方案迁就，标注为引擎需求缺口
  - **参考——同类看板模式 + AI 经验**：同域/同引擎类型的已有指标方案作为参考模板，行业通用指标设计模式作为补充材料，不替代对 STR-F 需求的分析
  - **门禁——人类反馈与确认**：全部指标标注 `[推断]`，经由人类在确认状态字段标注 `[同意]` / `[修改]` / `[驳回]` 后方可落账，不得未经确认直接写入资产

非看板类输出文档无需此项，跳过。

#### 5.5.5 文档缺口推断

**逐文档检查**：对每个输出文档，检查 26 资产中是否已有对应的文档定义。有则引用（26-Q1），需扩展则标注（26-Q2），无则形成新建建议（26-Q3）。

**按 WBS 推断缺口**：对每个 PL4 WBS，推断该 WBS 下应有的输出文档清单，与 STR-F 已有文档比对：

| 推断来源 | 说明 | 处理 |
|---------|------|------|
| 26 资产已定义 | 23 WBS 行引用的文档定义清单 | 正式依据，直接比对 |
| AI 经验推断 | 基于同类 WBS 模式、行业知识推断的应有文档。**PCA 场景按 [91 规范 §2.7](91-eos-biz-eng-spec.md#27-pca-场景的-pca) 核对业务链是否含计划清单文档**（缺则推断） | 标注 `[推断-待OR确认]`，由 Step 5 生成 OR 材料 |
| STR-F 已确认 | wft01-biz 已确认的输出文档 | 正式产品数据，视为已覆盖 |

比对后，STR-F 未覆盖的为缺口——写入 BP 节点缺口说明，不直接写入 24/26 资产。

#### 5.5.6 文档权限设计

仅针对 STR-F 已确认的输出文档。基于 STR-F 的 `用户角色` 和角色在业务链中的位置，按四维度推导：

| 维度 | 推导来源 | 示例（采购申请单 / 采购员） |
|------|---------|--------------------------|
| 字段可见性 | 角色职责范围、风险合规约束 | 审批意见字段对采购员只读 |
| 字段可编辑性 | 角色在业务链中的位置：录入者/审批者/查询者 | 草稿态字段全部可编辑 |
| 行级数据范围 | 角色组织归属 | 仅可见本人创建的申请单 |
| 按钮操作权限 | 角色可执行动作 | 提交/撤回可操作，审批不可操作 |

#### 5.5.7 PL6-B 正文条目需求定义

逐输出文档先执行"正文是否为集合容器"判定，再按三独立执行（三独立判定见 §3.4.1）。PCA 场景的定义型/管理型条目逐类判定；轻量文档原子正文行、事务性物料行和普通议题记录跳过。

消费 wft01-biz 在名称级捕获的 PL6-B 条目清单，按 §2.5.4 的定义要素逐条展开：

| 维度 | 说明 |
|------|------|
| **确认条目名称** | wft01 已捕获 → 确认并标准化名称；wft01 标注 `[待wft02推断]` → 基于 BP 上下文推断命名 |
| **定义要素** | 指标：口径/维度/目标值；看板卡片：数据源/图表类型/刷新频率；计划项：清单类型/优先级规则；风险/问题：分类/等级；行动项：状态集 |
| **定义生命周期** | 独立于父文档的状态变迁路径（如指标：草稿→已发布→已废弃） |
| **支撑引擎** | 继承自文档支撑引擎（§5.5.1 ① 校验后的结论），不单独判定 |

全部 PL6-B 条目标注 `[推断]`（名称+定义要素+生命周期均标注 `[推断]`），经由人类确认后方可落账。发现对闭环必要的但 wft01 未捕获的条目 → 先行推断 + `[推断-待OR确认]`，由 Step 5 生成 OR 材料（定义优先级见 §3.4.2）。

PL6-B 与 PL6-A 的关系见 §2.5.4 末段。

#### 5.5.8 任务关系引擎化

在全部输出文档方案及活动节点设计完成后，对 Step 4 涉及的输出文档之间及文档内部的任务间关系进行引擎化配置定义。

**执行时机**：PL4 WBS 确立、输出文档方案定义、活动节点设计完成后——此时文档结构和节点序列已明确，可导出完整触发关系网。

**定义内容**：

| 关系类型 | 来源 | 转化规则 | 示例 |
|---------|------|---------|------|
| **文档间流转顺序** | wft01 `业务链文档序列` 的先后顺序 | → 输出文档序列编排，定义文档间的依赖先后关系 | 采购申请单(创建)→审批记录(审批)→采购订单(生成) |
| **OR 明示的触发关系** | wft01 在 OR 中捕获的任务间触发关系 | → 引擎任务间触发规则配置——源文档、触发条件（状态/节点）、目标文档、启动参数 | 审批记录.节点"审批"=通过 → 创建采购订单 |
| **任意任务触发** | 91 规范 §2.5 R5 | 计划/流程/工单可触发任意类型，包括同类型；仅基于需要生成具体关系，不套固定拓扑 | 采购计划(计划)发布 → 触发采购申请单(流程) |

**每个关系的定义要素**：

```yaml
源文档：<PL5 输出文档ID>
源状态/节点：<触发条件>
目标文档：<PL5 输出文档ID>
启动参数：<选项。需传递到目标文档的初始化字段值>
关系来源：<OR 明示 / wft01 业务链序列 / AI 推断>
```

**门禁**：全部关系定义标注 `[推断]`（来源为业务链序列的直接映射除外——继承 wft01 的业务语言确认，无需 `[推断]`）。经由人类在确认状态字段标注后方可落账。

---

### 5.6 Step 5 · 业务需求增补

**一句话**：汇聚 Step 4 文档缺口推断中标注 `[推断-待OR确认]` 的业务缺口，统一生成 FR-BIZ OR 材料——缺口必须经 OR→STR-F→BP 管道确认后才进入资产写回；末尾执行覆盖自查。

汇聚 Step 4 文档缺口推断中标注 `[推断-待OR确认]` 的业务缺口，统一生成 FR-BIZ OR 材料。缺口必须经 OR→STR-F→BP 管道确认后才进入资产写回。

**1. 汇聚缺口**

在 BP 节点中创建 `### 业务缺口说明`，逐条列出 Step 4 已标注的缺口，按 WBS 分组，附加置信度和推断依据。

**2. 生成 OR 材料**

写入 `80-pl4eos-2-eosdata/00-origin-requirement-materials/10-raw-files/{YYYYMMDD}-wft02-biz-{批次ID}-业务需求增补.md`，包含：材料编号 / 来源（BP 节点 ID + STR-F 节点 ID）/ 背景 / 缺口清单（按 WBS 分组）/ 边界说明（不是正式 OR，不写入 `01-*.md`，需经 OR 预处理链确认）。

**人类确认与例外**（Step End 人类裁决缺口时）：

- **人类确认缺口的语义**：确认的是缺口的**存在性**（这个文档确实需要），**不等于提供业务身份**——文档的业务身份（业务链位置/场景职责/前后序）仍须经 OR→STR-F 确认（§3.1）。人类确认 = **发起 OR 的签字**，不绕过管道；本轮仍只产出缺口声明（`[推断-待OR确认]`），不直接设计缺口文档。
- **例外——26 资产已定义的补列**：若缺口文档在 26 资产已有定义（26-Q1）且业务链位置可从 23 资产（E2E 项目业务/构件类型）明确推出，则该文档属"STR-F 已定义文档的补列遗漏"而非"新业务"——wft02 可直接引用 26 定义设计该文档（走 Bn 侧完整设计，§5.5），在缺口说明中标注"26 已定义补列"并生成 OR 材料备案，同时在 Step End 提示人类"建议 wft01 补列 STR-F 节点"（保持追溯链完整）。

**3. 登记状态**

更新 `01-eos-sysdev-status.md` 表 A，生命周期=`已登记`。

**4. 覆盖自查**（Step 5 末尾使用，机械检查，逐项打勾；判据见 §3.5）

发现遗漏**仅标注、不回退**——在对应条目中写入 `[覆盖缺口]` 及缺失说明，留待下一轮补全。核对项见 §七 自检清单「业务需求增补与设计输出完整性核对（Step 5）」组——含 SL3~SL5 / PL3~PL5 方案、Step 4 执行完整性（PL4 WBS、任务节点引擎化、任务关系引擎化、PL6-B 定义、缺口推断、权限）、PCA 计划清单文档、23/24/26/27/28 对齐、变更影响声明、BP 版本号。

---

### 5.7 Step 6 · 资产写回与落账

**一句话**：写回已确认 BP 的资产并落账（推进 BP/STR-F、维护分节、提交基线）——不设二次确认。

仅写回状态=`可以SR设计` 的 BP 节点。人类已在 Step 2 确认方案，不区分 Q 级别、不设二次确认。

**写回资产**

遍历已确认 BP 节点的对齐结论，按 Q 级别写回：

| Q 级别 | 操作 |
|--------|------|
| Q1 复用 | 定位已有块，追加 BP 引用（去重） |
| Q2 扩展 | 扩展已有块，追加引用 + 扩展说明 |
| Q3 新建 | 新建块，写入定义 + 引用 |
| **23 例外** | ③ 检出 `[23归属冲突]` 的条目暂不写回，待人类裁决（详见 §3.3.3） |

**记录追溯**

| 操作 | 写入位置 | 内容 |
|------|---------|------|
| 资产写回记录 | `04-*.md` `### 资产写回记录` | 日期/路径/原子单元/目标资产块/动作/判定 |
| STR-F 推进 | `02-*.md` 对应节点 | `已分解分配` + 记录 `STR-F 消费版本：v{N}` |
| AI最近变更 | `02/04-*.md` `## AI最近变更` | 追加一行（时间/操作 Skill/操作类型+节点/摘要），保留近 30 条（[§11.6](91-eos-biz-eng-spec.md#116-ai最近变更) R7/R8） |
| AI可以处理节点 | `02/04-*.md` `## AI可以处理节点` | 已处理节点移出"待 wft02-biz 处理"；`[修改]`/`[需裁决]` 条目移入"待反馈处理"（[§11.7](91-eos-biz-eng-spec.md#117-ai可以处理节点)） |
| 文件头字段 | `02/04-*.md` 文件头 | 递增 `**文档版本**` 次版本号，更新 `**修订日期**`、`**HEAD @上次 AI 运行**` = 当前提交 hash（[§11.11](91-eos-biz-eng-spec.md#1111-ai-写回规则) R17） |
| 提交基线 | 全仓库 | `git add -A` + `git commit`（Co-Authored-By: Claude），把本轮 AI 产出（BP 节点、资产写回、AI 可以处理节点维护、AI 最近变更）全部落基线——链级不变式：基线 = 最后 AI 提交，下一轮 Step Start 的 `git diff <HEAD @上次 AI 运行>..HEAD` 只含人类变更 |

**写回要求**：幂等去重、局部写回、全貌同步。

---

### 5.8 Step End · 运行结束输出

**一句话**：输出三区摘要后不结束对话——等待人类反馈（修改意见或整体确认，§5.3）。

Step End 遵循 [91 规范 §A.3.2](91-eos-biz-eng-spec.md#a32-step-end-输出模板三区结构) 的三区输出模板（结果→方案反馈→下一步）。本 Skill 实例化如下：

```text
当前状态：<待确认方案 / 可以SR设计>
  本轮 BP：<BP-ID / 新建或修订 / 版本变化 / 来源 STR-F>
资产落账：<未落账 / 已写回 23/24/26/27/28>
补充原始需求材料：<无 / YYYYMMDD-wft02-biz-批次ID-业务需求增补.md>（已登记 OR 原料状态表）

一、方案反馈
  A. 整体确认 → 回复「整体确认」，快捷同意全部未标注条目
  B. 局部修改 → 在确认状态中标注 [修改]：...
  C. 驳回重做 → 在确认状态中标注 [驳回]：...

二、下一步
  本Skill → 选择 STR-F 节点（`可以分解分配` / `待补充分解分配`）或 `需wft02修订` 的 BP 节点重新运行
  后续    → BP-XXX（可以SR设计）、BP-YYY（待补充SR设计）→ wft03-biz
```

---

## 六、AI 标记约定

四类增量标记（`[新增]`/`[变更]`/`[确认]`/`[需裁决]`）的定义及使用规则见 [91 规范 §A.3.1](91-eos-biz-eng-spec.md#a31-ai-增量标记)。

每次 Step 3~5 完成后，向人类输出一份增量清单，格式示例如下：

```text
=== BP-XXXX 本轮更新清单 ===

[新增] 新建 BP 节点 BP-XXXX（Q3，来源 STR-F-YYYY）
  修订 STR-F-ZZZZ（+2 条 STR-F，版本 v2→v3）

A2 角色场景视角
  [新增] SL4 角色职责：采购申请执行    资产对齐：23A=Q1、24=Q3
  [确认] SL5 场景业务：采购员执行采购申请 → 归入已有 SL4

Bn 输出产品视角
  [新增] PL4 WBS：采购申请创建与维护    资产对齐：23B=Q2
  [新增] 输出文档"采购申请单"（@engine-flow + @engine-entity）  资产对齐：26=Q3
  [确认] 输出文档"审批记录"（已有定义）  资产对齐：26=Q1
  [变更] "采购申请单"权限调整——采购员审批意见字段→只读

任务活动设计
  [新增] "采购申请单"活动序列：创建→提交→审批→通过/驳回

PL6-B 正文条目需求
  [新增] "采购履约看板" PL6-B：订单及时率（口径=按时交付数/订单总数，目标≥95%） [三独立：是]
  [新增] "采购履约看板" PL6-B：到货合格率（口径=合格批次/总批次，目标≥98%） [三独立：是]

文档缺口推断
  [需裁决] 推断应存在"供应商评估报告"（WBS：供应商评估与准入），是否转为 OR？

变更影响声明
  变更类型：修订型（已有 BP 归入新 STR-F）
  对下游影响：受影响的 SR-F 需局部调整"采购申请单"表单结构
```

---

## 七、自检清单

**一句话**：每次运行收尾前按此清单逐项自查——AI 按 Step 分组核对是否漏做、漏写、漏提交。

**前置与加载（Step Start / 1）**
- [ ] Step Start 已按 §A.4 完成入口决策（退回优先 → 反馈处理 → 新输入），无待处理对象已退出
- [ ] 输入校验通过：状态符合、类型匹配（STR-E→`wft02-eng`、STR-NFR→`wft04-nfr-str2sr` 已路由）、跨域已检、数量未超阈值
- [ ] Step Start 已变更感知（git diff，基线前提已满足）并判定变更类型，线下修订节点已纳入"待反馈处理"分节
- [ ] 已加载 STR-F / BP / 23/24/26/27 资产全貌 / 25 引擎清单，未默认加载全文件正文
- [ ] "待反馈处理"分节条目已处理（`[修改]`/`[驳回]` 无 `[已处理]`）

**方案反馈处理（Step 2）**
- [ ] 对话反馈已按 §5.3 处理：修改指令 → 检查仍满足 STR-F → 追加 `[已处理]`；整体确认 → 推进 `可以SR设计`
- [ ] 跨轮余留反馈已按状态机标记 `[修改]`/`[驳回]` 并加入"待反馈处理"分节

**角色场景视角设计（Step 3）**
- [ ] 按产品路径+用户角色匹配已有 BP（Q1/Q2 归入 / Q3 新建）分流正确
- [ ] SL4 角色职责推断完成（Q1/Q2/Q3 分流正确，SL4 占级规则已应用，§3.3.2）
- [ ] 输出文档需求定义完成（职责 / 信息来源 / 输出去向）
- [ ] 23A/24 对齐判定完成（§3.3.1）

**输出产品视角设计（Step 4）**
- [ ] 引擎候选校验完成（确认/修正/推翻分流正确，校验原因已记录，§3.2）
- [ ] PL4 WBS 生成完成（归组规则 + 完成耦合已应用，§3.3.2）
- [ ] 23 资产一致性检查完成（归属冲突/误新建/误引用检出，§3.3.3）
- [ ] 输出文档方案定义完成（正式：封面+正文+引擎+支持信息；轻量准用）
- [ ] 任务活动设计（计划/流程/工单，§5.5.3）+ 看板指标设计完成（主驱动→约束→参考→门禁）
- [ ] 文档缺口推断 + 权限设计完成
- [ ] PL6-B 三独立判定 + 定义要素完成（§3.4）
- [ ] 任务关系引擎化完成（§2.5.2 / §5.5.8）

**业务需求增补与设计输出完整性核对（Step 5）**
- [ ] 业务缺口已汇聚（`### 业务缺口说明`）+ OR 材料已生成 + OR 原料状态表已登记
- [ ] 设计输出完整性核对（判据 §3.5，遗漏仅标 `[覆盖缺口]` 不回退）：
  - [ ] SL3~SL5 角色场景 BP 方案（含需求分配到输出文档）
  - [ ] PL3~PL5 产出产品 BP 方案（含输出文档路径 / 数据权限 / 文档缺口推断）
  - [ ] Step 4 执行完整性——逐 STR-F 输出文档核对已生成 PL4 WBS、任务节点引擎化（流程/工单/计划）、任务关系引擎化、PL6-B 定义、缺口推断、权限（无遗漏）
  - [ ] **PCA 场景业务链已含计划清单文档**（[91 §2.7](91-eos-biz-eng-spec.md#27-pca-场景的-pca)，与 R4 首文档为两个实例）
  - [ ] 23 对齐判定（A2/Bn 产品路径 / 菜单可达性路径）
  - [ ] 24 对齐判定（角色职责 / 场景业务）
  - [ ] 26 对齐判定（文档定义）
  - [ ] 27 对齐判定（资源资产引用）
  - [ ] 28 对齐判定（看板指标资产）
  - [ ] 变更影响声明
  - [ ] BP 版本号

**资产写回与落账（Step 6）**
- [ ] 资产写回按 Q 级别分流执行（23 例外：`[23归属冲突]` 暂不写回），幂等去重
- [ ] STR-F 已推进 `已分解分配`，引用链已记录
- [ ] AI 最近变更表、AI 可以处理节点分节已维护
- [ ] 提交基线已执行（`git add` + `git commit` + update-head）

**运行结束（Step End）**
- [ ] 三区摘要已输出，对话等待反馈；整体确认后已提交基线

---

## 八、变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-08-20 | v9.0 | 反馈检出前置到 Step Start（链级推广，与 wft02-eng v9.1 同款，人类确认三个 Skill 一起修）：①**Step Start 增加变更感知**（先于入口判定）——git diff H..HEAD 检出人类线下修订，判定变更类型（结构化标注→纳入待反馈 / 自由文本→[需确认] / 格式→忽略）后加入"待反馈处理"分节，**先于入口判定**；补注记说明"否则纯线下修订会被误判无待处理对象退出"（对齐 91 §11.10）；②**Step 1 ① 改安全复查**——变更感知已上移 Step Start，此处仅按 §A.3.3 R0a 做加载前安全检查（detect-changes.sh 检出非预期修改标记 [需确认]）；③同步更新：Step 布局表（Start 加变更感知 / Step 1 改安全复查）、反馈机制一句话、§5.3 反馈双轨线下修订表述、§5.7 提交基线、§七 自检清单 |
| 2026-08-19 | v8.9 | 反馈双轨显式化（人类提出"wft02 处理文档也应支持 AI 对话反馈 + 线下人类直接打开文档修订"→确认，对齐 wft01 反馈双轨 / 91 §A.5）：§5.3 补「反馈双轨」——①AI 对话反馈（Step End 后不结束对话，§5.3.1 即时处理，主通道）；②线下文档修订（打开 `04-*.md` 在确认状态字段标注 `[同意]`/`[修改]`/`[驳回]` 或直接编辑方案内容自由文本，经 Step 1 变更感知 git diff 检测按 §A.5 处理）。两轨等价均落账（追 `[已处理]`），AI 每轮提交基线（§5.7）保证下一轮 diff 只含人类变更。 |
| 2026-08-19 | v8.8 | WBS 分组硬判据（人类问"WBS 节点如何产生"→确认补硬判据）：§3.3.2 补**数据依赖链判据**——归入同一 WBS 的交付类输出文档须在同一数据依赖链上（前一文档正文数据成为后一文档输入，91 §5.3 交付类主价值链），完成耦合由它自然保证（从"验证"变"可判定"）；**跨链不归组**、同链按业务环节切段；管理类文档（指标/看板/风险/问题/计划清单）不单独成链、按其支撑的交付链归属。§5.5.1 ② 引用同步（数据依赖链硬判据 + 完成耦合）。 |
| 2026-08-19 | v8.7 | 任务活动设计接力边界显式化（人类问"任务活动设计落在 wft01 还是 wft02"→确认分层，接力检查 F2/F3）：①**F2 接力边界**——§2.5.1 节点转化规则补"wft01 已捕获节点是业务流转确认结果（供人类确认过），wft02 在此基础上做架构序列设计，不重新推断业务流转（谁/动作/顺序），除非有明确证据"；②**F3 缺陷路由**——§5.5.3 末尾补"发现 wft01 捕获的业务流转缺陷 → 标注 [需裁决] / 退回 wft01（需wft01修订），不自主改业务流转（业务流转是 STR 权威）"。配 wft01 v3.12（F1 业务确认定位）。 |
| 2026-08-19 | v8.6 | 缺口处理边界显式化（人类问"wft02 新增的文档怎么处理"→详细分析落定）：①**§3.1 补「缺口为什么必须回 STR」**——wft02 只有构件完备视角（能发现缺口）、无 OR 语义（不能定义业务身份：业务链位置/场景职责/前后序）；BP 文档须可追溯至 STR-F 与 OR；缺口经 OR→STR-F 确认（含业务链定位 + 闭合检查补相邻缺口）；②**§5.6 补「人类确认与例外」**——人类确认缺口=发起 OR 的签字、确认存在性非业务身份、不绕过管道；**例外**：26 资产已定义（26-Q1）+ 23 资产可推位置 → 属"补列遗漏"非"新业务"，wft02 可直连引用 26 设计 + 反向提示 wft01 补 STR-F 节点 + OR 材料备案。 |
| 2026-08-19 | v8.5 | 任务活动设计补齐（人类指出"对每个文档是否设计任务类型和任务流程节点"→确认计划类缺失）：§5.5.3「流程工单活动设计」→「任务活动设计」——覆盖三类（计划/流程/工单），按任务类型分流（流程=审批流转、工单=派发处理验收、计划=策划→发布标准化节点 + wft01 节点转 @engine-task 配置）；约束行补 @engine-task；末尾「非流程/非工单跳过」→「无任务类型跳过」；同步 §六 增量清单示例标题与 §七 自检清单项。消除 §1.1 目标 6（三类）与操作层（原仅两类）不一致。 |
| 2026-08-19 | v8.4 | 语言层级显式化（人类讨论张力三"业务语言→架构语言边界"确认，人类选「保留架构语言+施工图比喻」）：§二 转换总览补 **语言层级** 小节——架构语言定义（=描述解决方案实体的语言，词落在 EOS 系统模型 引擎/文档/层级/资产/CU，可被下游直接消费）+ **施工图比喻**（业务语言=口述需求、架构语言=施工蓝图、wft03=按图施工）+ 语言演进表（业务语言→准架构语言[推断]→架构语言→配置语言）；"可消费性"为判据，统一收口张力一（引擎权威在 BP=准架构→架构）、张力二（Bn 单向补经 OR 回路）。 |
| 2026-08-19 | v8.3 | STR→BP 双视角互证修正（人类讨论张力二"A2/Bn 分开做靠什么保证一致"确认）：A2/Bn 不是"互相校验完备性"，而是**互为投影 + 交汇点一致性**——一致性由同一 STR-F 业务链天然保证；完备性由各自闭环保证（A2=STR-F 端到端闭环 wft01 已做、Bn=构件文档缺口推断）；**缺口发现集中在 Bn 侧，经 FR-BIZ OR 回路下轮收敛回 A2（单向补，不脑补节点）**。修正 §2.1.1/§3.1/§3.5「互相校验」表述（§3.5 改「保持一致性」）。确认现状正确（A2 只用 STR-F 已确认文档、缺口走确认管道，防止 AI 推断影响 A2）。 |
| 2026-08-19 | v8.2 | STR→BP 边界显式化（人类讨论张力一"展开 vs 再设计"确认）：①**任务类型-引擎耦合判定**——§3.2 补耦合 blockquote（任务类型↔引擎一一映射，wft01 判的任务类型=业务语言初步值，BP 引擎校验=终审处理模式，修正引擎同步修正任务类型"从 X 修正为 Y"）+ 修正行动作补"同步修正任务类型"+ §2.5.1 概念层补耦合句；②**引擎权威定位**——§3.2 末尾补"STR-F 引擎字段=业务语言线索（[推断]），设计链引擎权威=BP 校验值，wft03 以此消费；不回写 STR-F（线索非结论，wft01 Phase B 关系检测会重新识别天然对齐）"。边界判据：判定依据为业务语义→STR 权威（BP 遵从）；为方案上下文→BP 权威（自主）。 |
| 2026-08-19 | v8.1 | 原理与方法审视修复（人类提出"我们看看原理和方法"，对齐 wft01 v3.11 先例）：①**M1 PCA 计划清单强制要求补入方法层**——§3.5 完备性判据「双视角覆盖」层补"PCA 场景业务链必须包含计划清单输出文档（91 §2.7，正文条目三独立展开 PL6-B，与 R4 首文档为两个实例）"、§5.5.5 缺口推断 AI 经验推断补 PCA 核对、§七 Step 5 组补核对项；②**M2 C/A 运行时口径显式化**——§3.5「业务缺口闭环」层补"指标引擎度量→看板呈现→生成风险/问题条目为任务链运行时结果，不作为必须由 OR 覆盖的缺口，仅缺承载 C/A 的输出文档才构成缺口"；③**M3 变更类型补「无变更」类**——§4.2.1 表 + §4.2.2 模板对齐 91 §A.7.2 四类，判定优先级补无变更；④**L1 PL3 开发方式四类**——§2.3.1 补「咨询」类（自研/咨询/外购/复用，对齐 91 §5.3）；⑤**L2 覆盖自查并入自检清单**——§5.6 覆盖自查收敛为指针引 §七 Step 5 组（十项完整迁入 + 补 PCA 计划清单/PL6-B/任务关系引擎化核对项，消除 v8.0 引入的重复核对）；⑥**L3 D/PCA 差异表述统一**——§1.1 与 §2.2.1 同词「文档模式与闭合形态」。 |
| 2026-08-19 | v8.0 | 以 wft01-biz 为标杆知识层三章重构（人类提出"以 wft01-biz 为标杆，重构第二章"，完整对标）：①§二 改为**基本概念**——转换总览（转换方法六步一条链 + 规则地图判据索引）+ 2.1~2.5 概念（2.1.1 合并双视角模型+节点要素、2.2.1/2.3.1 合并层级）；②新 §三 **设计原理与判据**——3.1 转换原理（新增，为什么用双视角承接 STR-F）+ 3.2 引擎候选校验（原 2.4.2）+ 3.3 资产对齐与一致性检查（原 2.6，拆 3.3.1/3.3.2/3.3.3）+ 3.4 PL6-B 判据（原 2.5.4 规则部分，拆 3.4.1/3.4.2）+ 3.5 完备性判据（新增）；③新 §四 **节点演进**——4.1 生命周期（原 2.7）+ 4.2 变更传播（原 2.8）；④原 §三 操作流程 → §五（3.x→5.x 重编号，Step 布局补「人类参与/执行模式」列 + 执行模式白话注 + 反馈机制一句话，Step 6 补提交基线，原 2.6.3 覆盖自查迁入 §5.6 Step 5）；⑤新 §七 **自检清单**（按 Step 分组，镜像 wft01 §七）；⑥术语对齐——D/PCA「完全同构」→「流程骨架同构」（对齐 wft01 v3.11 M3，§1.1 + §2.2.1）；⑦SKILL 锚点迁移（#262→#332）。 |
| 2026-08-19 | v7.4 | 链级术语统一：「正文转化引擎 / 正文支撑引擎」→「支撑引擎」（对齐 wft01-biz v3.8 / 91 BP 字段名）；正文替换，变更记录历史保留原术语。 |
| 2026-07-13 | v7.2 | 全篇可读性翻新——目的段/双视角拆分（§1.1）、负责/不负责→表格（§1.1）、上下游衔→表格（§1.2）、SL6/PL3 长描述→分行列点（§2.2.2/§2.3.2）、23 一致性并行段→子弹列表（§2.6.4）、Step Start 入口列表显式化（§3.1）、Step 1 加载步骤→表格（§3.2）、清除条件→列表（§3.3）、Step 6 写回→表格（§3.7）。修复 typo「定义定义要素」→「定义要素」。 |
| 2026-07-13 | v6.4 | 对齐 91 规范 v5.3：正式/轻量与 PL6-B 正交；增加正文集合前置判定和轻量文档正文行去重门禁。 |
| 2026-07-12 | v6.3 | 依据 91 规范 v4.8 修订 PL6-A/PL6-B 层级、三独立判据、任意任务触发规则和 A2/Bn 转化关系，删除无依据的 PCA 全计划规则。 |
| 2026-07-12 | v6.2 | 深层审视修复——修复 §3.6 重复标题；§3.3 Step 2 特化参数表新增清除条件（§A.5.3 Step 1）。详见 Git 历史。 |
| 2026-07-12 | v6.1 | 对齐 91 规范 v4.8——§1.1 目标补充定义输出文档方案+PL6-B 正文条目需求；§2.5.4 新增 PL6-B 正文条目需求定义；引用迁移 §A.x；§3 Step Start/1/2 对齐附录A入口/读取/反馈协议；§3.5.6 新增 PL6-B 正文条目需求定义步骤；§3.7 补 AI最近变更/AI可以处理节点维护；§四 增量清单补 PL6-B 示例。详见 Git 历史。 |
| 2026-07-10 | v6.0 | 对齐 91 规范 v4.0——任务类型三选一、正式/轻量文档设计差异、D/PCA 处理同构。 |
| 2026-07-08 | v5.5 | 23 资产一致性检查新增。与审视报告 §三.3.5 对应。 |
| 2026-07-08 | v5.4 | 引擎校验步骤新增。与 wft01-biz v1.98 配套。 |
| 2026-06-24~2026-07-08 | v1.0~v5.3 | 历史版本，详见 Git 历史。 |
