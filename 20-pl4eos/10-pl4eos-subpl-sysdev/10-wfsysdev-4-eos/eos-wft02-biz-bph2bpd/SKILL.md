---
name: eos-wft02-biz-bph2bpd
description: bph-biz→业务流程详细定义（s02）· bpd-biz 业务流程设计与资产化。接收≥1个bph-biz节点（可分解分配/待补充分解分配），
             完成A2+Bn双视角业务流程详细定义（Step 3角色场景→表单使用顺序 + Step 4输出产品：消费Bn组装→引擎校验→文档方案→活动设计→权限→PL6-B→任务关系），
             写回23/24/27/28资产，形成可进入wft03-biz的bpd-biz节点。
---

# eos-wft02-biz · 业务流程概要（bph-biz）→ 业务流程详细（bpd-biz）· 业务流程设计与资产化

> **设计依据**：[eos-wft02-biz-bph2bpd.md](../eos-wft02-biz-bph2bpd.md)（人类方案——权威源）
> **运行时协议**：[91-eos-biz-eng-spec.md](../91-eos-biz-eng-spec.md) 附录A（入口条件/读取协议/反馈交互/Step End输出模板）
> **领域规范**：[91-eos-biz-eng-spec.md](../91-eos-biz-eng-spec.md)

---

## 一、职责定位

### 负责与不负责

| 负责 | 不负责（路由指向） |
|------|-------------------|
| 接收≥1个 bph-biz 节点（`可以分解分配`/`待补充分解分配`），校验状态/类型/数量/跨域 | bph-eng → `wft02-eng-bph2bpd` |
| 按产品路径+用户角色匹配已有 bpd-biz 节点——同路径同角色归入同一 bpd-biz（Q1/Q2），无则新建（Q3） | NFR → `wft04-nfr-str2sr` |
| A2 角色场景视角——推断 SL4 角色职责 + 输出文档需求定义 + 表单使用顺序 + 23A/24 对齐判定 | 下游 srh-biz 架构设计展开 → `wft03-biz` |
| Bn 输出产品视角——消费 wft01 Bn 组装（PL 路径 + WBS 归组）+ 引擎校验 + 文档方案 + 活动设计 + 权限 + PL6-B + 任务关系引擎化 | 业务人员确认（人类线下活动） |
| 汇聚业务缺口，生成补充规范化需求材料 + 登记 材料状态表 | PL6-B 条目字段详细建模 → `wft03-biz` |
| 人类确认后写回 23/24/27/28 资产（Q1/Q2/Q3 均直接写入，不二次确认） | — |
| 锁定 bph-biz→`在分解分配`（Step 3 开始处理时）+ 推进 bpd-biz→`可以srh设计` + bph-biz→`已分解分配` | — |
| 接收 wft03 退回的 `需wft02修订` bpd-biz 节点，按人类方案修订 | — |

### 上下游衔接

| 方向 | Skill | 交接内容 |
|------|-------|---------|
| 上游 | `wft01-biz` | 产出 bph-biz 节点（`可以分解分配`/`待补充分解分配`），含业务链文档序列 + 任务类型/节点（业务语言）+ 引擎候选建议（全量 `[推断]`）+ PL6-B 条目名称级捕获 + **Bn 组装结论（PL 路径 + WBS 归组 + 23 一致性）**。通过 `## AI可以处理节点` 的"待 wft02-biz 处理"分节检测 |
| 下游 | `wft03-biz` | 消费 bpd-biz 方案（`可以srh设计`），展开为 srh-biz 架构。缺陷时退回 `需wft02修订`+缺失说明 |
| 同级 | `wft02-eng`、`wft04-nfr` | 各自处理 bph-eng / NFR，本 Skill 输入校验检出后退出并提示路由 |
| 治理 | bph-biz | 平台资产治理业务 bph-biz 预置在 `02-*.md`，与业务 bph-biz 完全同构。当前规范化需求承接为空，暂不进入 |

---

## 二、操作流程

### Step Start · 前置校验与路由

**变更感知**（先于入口检测，检出人类线下修订）：

```bash
bash ../scripts/detect-changes.sh ../../../80-pl4eos-2-eosdata/02-eos-business-summary-architecture.md
bash ../scripts/detect-changes.sh ../../../80-pl4eos-2-eosdata/04-eos-business-detailed.md
```

检出 `HAS_CHANGES=1` → AI 检查变更行，判定变更类型：结构化标注（`[同意]`/`[修改]`/`[驳回]`）→ 纳入"待反馈处理"分节；自由文本 → 标记 `[需确认]`；格式/排版 → 忽略。**先变更感知再入口判定**——纯线下修订若不先检出，会被误判"无待处理对象"退出（详见 human spec §5.1 变更感知）。

**入口检测**（按 [91 规范 §A.4](../91-eos-biz-eng-spec.md#a4-入口条件与优先级step-start)）：

```bash
# 1. 读取待处理节点（三个分节）
bash ../scripts/read-section.sh ../../../80-pl4eos-2-eosdata/04-eos-business-detailed.md "AI可以处理节点"
```

三类入口节点：
- **待 wft02-biz 处理** → 新 bph-biz 节点
- **待反馈处理** → 人类退回的 bpd-biz 条目
- **待下游退回处理** → `需wft02修订` 的 bpd-biz 节点

**输入校验**（按顺序，命中即退出）：

| 条件 | 判定 | 动作 |
|------|------|------|
| bpd-biz 状态=`需wft02修订` 且人类未提供改进方案 | 退回方案缺失 | 输出 wft03 缺失说明 → **退出** |
| 无任何待处理节点 | 无待处理对象 | 输出"无待处理对象"→ **退出** |
| bph-biz 状态≠`可以分解分配`/`待补充分解分配` | 状态不符 | 输出状态分布 → **退出** |
| 选中节点含 bph-eng/bph-nfr 类型 | 类型不匹配 | 输出类型分布 + 路由指引 → **退出** |
| 选中 bph-biz >5 个 | 超建议阈值（软提示） | 输出建议分批 + 节点列表 → 人类确认 |

**退回优先**：存在 `需wft02修订` bpd-biz 节点 → 检查人类是否提供改进方案。已提供 → 读取缺失说明，走 Step 3~5 修订。未提供 → 输出缺失说明 → **退出**。

**反馈处理**：存在"待反馈处理"分节中 bpd-biz 节点（确认状态=`[修改]`/`[驳回]` 且无 `[已处理]`）→ 进入 Step 2。

**新 bph-biz 路由**：有未处理 bph-biz → 进入 Step 1→Step 3，按产品路径+用户角色匹配已有 bpd-biz（Q1/Q2 归入 / Q3 新建）。

> **执行顺序**：Step 2（反馈处理）必须先于 Step 3（新增设计）——反馈可能改变 bpd-biz 的产品路径、业务清单或权限结论。

---

### Step 1 · 设计材料加载

**1. 安全复查**（加载前检查；变更感知已在 Step Start 完成）：

```bash
bash ../scripts/detect-changes.sh ../../../80-pl4eos-2-eosdata/02-eos-business-summary-architecture.md
bash ../scripts/detect-changes.sh ../../../80-pl4eos-2-eosdata/04-eos-business-detailed.md
```

检出非预期修改时标记 `[需确认]`，不自动推进（§A.3.3 R0a）。

**2. 读取 AI 可以处理节点（优先）**：

读取 `04-*.md` 的 `## AI可以处理节点` 中本 Skill 对应分节：

| 分节 | 获取内容 |
|------|---------|
| "待 wft02-biz 处理" | 待处理 bph-biz 节点 ID 列表 |
| "待反馈处理" | 待处理的 bpd-biz 反馈条目 |
| "待下游退回处理" | `需wft02修订` 的 bpd-biz 节点 |

有节点 → 逐节点 `grep -n '@{节点ID}'` 定位行号 → Read 节点块。无节点 → 进入索引导航。

**3. 索引导航**（AI可以处理节点无待处理时备用）：

```bash
# 通过业务域索引/角色索引定位目标段
grep -n '索引行数：' ../../../80-pl4eos-2-eosdata/04-eos-business-detailed.md
```

**4. 加载清单**（定位后的具体加载内容）：

| 资产 | 用途 |
|------|------|
| `02-*.md` — bph-biz 节点块 | 定位本轮 bph-biz 正文（含输出文档清单、引擎候选、PL6-B 条目、Bn 对齐） |
| `04-*.md` — bpd-biz 节点块 + AI可以处理节点 | 定位本轮 bpd-biz 节点 + 待处理节点清单 |
| `23-eos-output-architecture.md` 资产全貌层 | A2/Bn 路径对齐锚点 |
| `24-eos-business-assets.md` 资产全貌层 | 可复用业务资产匹配 |
| `25-eos-engine-models.md` §1.1/§1.7 | 引擎清单 + 业务文档承接映射 |

**按需扩大原则**：仅在索引无法定位、候选冲突、追溯断裂或人类明确要求时扩大读取范围。禁止默认加载全文件正文。

---

### Step 2 · 方案反馈处理

**触发条件**：Step Start 检出"待反馈处理"分节中有未处理的 bpd-biz 条目，或上一轮 Step End 的反馈等待中人类提出修改意见。

**反馈双轨**（人类任选其一，详见人类方案 §5.3）：①AI 对话反馈——Step End 后不结束对话，直接回复修改意见，AI 即时处理；②线下文档修订——打开 `04-*.md` 在确认状态字段标注 `[同意]`/`[修改]`/`[驳回]` 或直接编辑方案内容（自由文本），经 Step Start 变更感知 git diff 检出后按 §A.5 处理。两轨等价均落账。

**本 Skill 的条目**：A2 方案 / Bn 方案 / 输出文档设计（含 PL6-A 活动节点 + PL6-B 正文条目需求）/ 数据权限 / 23/24/27/28 资产对齐结论。条目 `确认状态` 字段位于 `04-*.md` bpd-biz 节点块各条目正文中。

#### 特化参数

| 特化维度 | wft02-biz 值 |
|---------|-------------|
| **锚点需求** | 当前 bpd-biz 所承接的全部 bph-biz 节点——人类反馈理解和修订均以 bph-biz 场景业务需求为最终锚点 |
| **操作条目** | A2 方案 / Bn 方案 / 输出文档设计（含 PL6-A + PL6-B）/ 数据权限 / 23/24/27/28 资产对齐结论 |
| **推进目标** | 全部条目含 `[同意]`+`[已处理]` → bpd-biz 推进至 `可以srh设计` |
| **清除条件** | 已有 `[同意]`+`[已处理]` 默认跳过。仅当本轮新输入满足**任一**条件时清除 `[已处理]`（保留 `[同意]`）：<br>1. 新输入是该条目的直接上游节点（如新 bph-biz 是 bpd-biz 来源）<br>2. 新输入与条目同属一个架构层级且边界重叠<br>3. 新输入导致条目定义被修订（变更影响声明标注为修订型/结构型） |
| **级联触发条件** | `[修改]`/`[驳回]` 导致方案结构性变更（产品路径变更/文档增删/引擎变更/PL6-B 条目增删）→ 标记受影响条目为 `[需重设计]` |
| **资产对齐级联** | 方案变更影响 23/24/27/28 资产引用 → 更新受影响资产的 Q 判定和引用路径 |

#### 确认状态格式示例

**AI 写入（Step 3~5 完成时）** —— 为每个条目初始化 `确认状态` 字段，初始为空：

```text
- A2-场景业务-采购员执行采购申请 | 确认状态: (空)
- 采购申请单（输出文档，@engine-flow + @engine-entity） [23B-Q2] | 确认状态: (空)
```

**人类在条目 `确认状态` 字段标注**：

```text
- A2-场景业务-采购员执行采购申请 | 确认状态: [同意]
- 采购申请单（输出文档，@engine-flow + @engine-entity） [23B-Q2] | 确认状态: [修改]：引擎应改为 @engine-workorder + @engine-entity
```

**AI 处理完成后** —— 在人类标注末尾追加 `[已处理]`：

```text
- A2-场景业务-采购员执行采购申请 | 确认状态: [同意] [已处理]
- 采购申请单（输出文档，@engine-workorder + @engine-entity） [23B-Q2] | 确认状态: [修改]：引擎应改为 @engine-workorder + @engine-entity [已处理]
```

人类改写 `确认状态` 即覆盖旧内容，`[已处理]` 自然消失，条目回到待处理状态。

---

### Step 3 · 角色场景视角 bpd-biz 设计

以 bph-biz 场景职责边界为锚，落成 A2 角色场景视角 bpd-biz。核心工作：给每个 bph-biz 的 SL5 场景业务确定 SL4 角色职责归宿。

> **bph-biz 锁步**：开始处理每个 bph-biz 前，将 `02-*.md` 对应节点状态写为 `在分解分配`（活动说明，防并发重入）；Step 6 推进 `已分解分配`。

#### 3.1 推断 SL4 角色职责

| 判定 | 条件 | 动作 |
|------|------|------|
| **Q1 复用** | 已有同名/同义 SL4 且职责范围一致 | 直接引用，bph-biz 归入该 SL4 |
| **Q2 扩展** | 已有相近 SL4 但需扩展其职责边界 | 扩展已有 SL4 + 归纳新 bph-biz |
| **Q3 新建** | 无匹配的已有 SL4 | 新建 SL4 角色职责节点，写入 `04-*.md` |

SL3 角色类型从 bph-biz 用户角色直接映射，不重复判定。SL4 占级规则：角色仅一个 SL5 时 SL4 不独立占级，已有 SL4 可扩展边界容纳新 L5 节点（Q2），无法归入时新建（Q3）。

Q1/Q2 归入已有 SL4 时，将本轮新 bph-biz 的输出文档清单与该 SL4 下已有内容逐项比对——确认则升级来源、补充则全量重新展开（非增量追加）、无新增则仅加引用、矛盾则记录待人类裁决。

#### 3.2 输出文档需求定义

场景业务的需求由 bph-biz 定义，Step 3 将其分解分配到各输出文档——每个输出文档承接一部分场景职责。对 bph-biz 业务链中的每个输出文档，确定：

- **文档职责**：该文档在场景中承担什么角色（如"承载申请数据""记录审批结果""汇总指标"）
- **信息来源**：数据从哪个前序文档流入，或由用户直接录入
- **输出去向**：产出物被哪个后续文档消费，或直接交付给用户

SL5 与 bph-biz 1:1 映射，场景职责复用 bph-biz 的场景职责边界。将上述分配结果连同触发动作和上下文传递点按 bph-biz 的业务链顺序写入 bpd-biz 节点。

**表单使用顺序**（bpd-biz 只定义场景边界和表单使用顺序，§2.2.1）：按 bph-biz 业务链顺序排列该场景业务使用的功能表单/主子表单，定义使用先后顺序与上下文传递点（如"采购申请单录入 → 审批记录审批 → 采购订单生成"），标注 `[推断]`。页面组件/菜单编排由 wft03-biz 设计。

完成写入后执行 23A/24 对齐判定（规则见附录 A.1），写入 `04-*.md`。

---

### Step 4 · 输出产品视角 bpd-biz 设计

将 bph-biz 的输出文档清单落成 Bn 输出产品视角 bpd-biz。分五个 Phase 执行。

#### Phase A — 引擎校验与消费 Bn 组装

**① 引擎候选建议校验**（权威，§3.2）：对每个输出文档，基于 bpd-biz 上下文逐文档校验 wft01 的引擎候选建议：

| 校验结果 | 判定条件 | 动作 |
|---------|---------|------|
| **确认** | 候选引擎与 bpd-biz 展开后的文档职责和处理模式完全匹配 | 去掉 `[推断]` 标记 |
| **修正** | 候选引擎部分匹配但需调整 | 写入修正后引擎 + 修正原因 + **同步修正任务类型** + 去掉 `[推断]` |
| **推翻** | 候选引擎与 bpd-biz 展开后不兼容，且 25 资产中无可替代引擎 | 标注 `[引擎待确认]` + 不兼容原因 → Step End 提示人类裁决 |

**② 消费 Bn 组装结论**：PL1（业务域）从 bph-biz 读取；PL2（E2E 项目业务）/PL3（构件类型）/PL4（WBS 归组）/23 一致性结论由 wft01 的 Bn 组装完成（[wft01-biz §3.3.3](eos-wft01-biz-spr2bph.md#333-bn-资产对齐判定与-bn-组装)），本 Skill 直接读取 bph-biz 节点的 Bn 组装结论，**不重复归组、不重复做 23 一致性检查**。若 wft01 尚未产出 Bn 组装结论（旧版本 bph-biz），AI 基于输出文档性质和校验后引擎类型推断 PL 路径，标注 `[推断]` 并在 Step End 提示补跑 wft01。

**③ 23 一致性检查（已上移 wft01）**：wft01 的 Bn 组装在 23B Q 判定 + WBS 归组完成后校验两者是否矛盾——归属冲突（`[23归属冲突]` 提示人类裁决）、误新建（降为 Q2）、误引用（升为 Q2/Q3），矛盾项由 wft01 暂不写回 23。本 Skill 只消费其结论，Step 6 写回时不设「23 例外」。

#### Phase B — 输出文档方案定义

对每个输出文档，PL 路径和支撑引擎确定后，定义文档的完整结构方案。

**正式文档方案**：

| 要素 | 定义内容 |
|------|---------|
| **封面字段** | DocumentCover 表映射——单据号/标题/状态/版本/创建人/部门/日期等。按文档类型裁剪（流程文档添加审批字段、计划文档添加计划周期字段） |
| **正文实体结构** | 内容实体的字段集——字段名/字段类型/约束/CRUD。从 bph-biz 输出文档的 `业务链文档序列` 提取 |
| **主子/下钻关系** | 关联文档下钻的 1+N 子文档列表——子文档名称/关联方式（1:N 主子组装）。由正文实体结构中的子实体/关联文档导出，供 wft03 场景标签页 TAB 编排消费；无则写"无" |
| **支撑引擎** | 由 Phase A ① 校验后的引擎结论直接引用，不再重新判定 |
| **支持信息引用** | 引用关系——引用文档名称/引用方式（只读/更新）/引用用途。去重 |

**轻量文档方案**：无封面，正文实体结构定义同正式文档；支撑引擎由校验后结论直接引用。全部方案内容标注 `[推断]`，经人类确认后方可落账。

#### Phase C — 任务活动设计 + 看板指标设计

**任务活动设计**（仅针对 bph-biz 已确认的有任务类型输出文档——计划/流程/工单）：

按 §2.5 定义活动节点序列——逐节点定义处理角色、可执行动作、状态变迁、系统自动节点/异常路径/超时处理。按任务类型分流：流程=审批流转、工单=派发处理验收、计划=策划→发布标准化节点（wft01 节点转 @engine-task 配置）。推断依据（按优先级逐层驱动）：
1. **主驱动**：对 bph-biz 场景需求的理解——该文档在业务链中承担什么职责、需要哪些处理步骤
2. **约束**：支撑引擎的能力边界——是否在 `@engine-flow`/`@engine-workorder`/`@engine-task` 能力范围内
3. **参考**：同类文档模式 + AI 经验
4. **门禁**：全部标注 `[推断]`，经人类确认后方可落账

无任务类型的输出文档跳过。

**看板指标设计**（仅针对 bph-biz 已确认的看板类输出文档）：

逐指标标注指标名称、度量口径、度量目标、聚合维度、显示图示和刷新频率。推断依据同活动设计（主驱动→约束→参考→门禁）。非看板类输出文档跳过。

#### Phase D — 权限设计（结构性缺口已上移 wft01）

**结构性缺口推断已上移 wft01**：按构件产品数据清单完整性做名称级比对，由 wft01 的 Bn 组装完成（[wft01-biz §3.3.3](eos-wft01-biz-spr2bph.md#333-bn-资产对齐判定与-bn-组装)），结构性缺口经 wft01 Step 4 补充规范化需求回路补回。本 Skill 不按 WBS 推断文档缺口；文档定义承载于 04 正文实体结构（§5.5.2），承接锚点 25 §1.7（26 号资产已取消），与结构性缺口推断无关。

**文档权限设计**（仅针对 bph-biz 已确认的输出文档）：

基于 bph-biz 的 `用户角色` 和角色在业务链中的位置，按四维度推导：

| 维度 | 推导来源 |
|------|---------|
| 字段可见性 | 角色职责范围、风险合规约束 |
| 字段可编辑性 | 角色在业务链中的位置：录入者/审批者/查询者 |
| 行级数据范围 | 角色组织归属 |
| 按钮操作权限 | 角色可执行动作 |

#### Phase E — PL6-B 正文条目需求 + 任务关系引擎化

**PL6-B 正文条目需求定义**：逐输出文档先判定"正文是否为集合容器"，再按三独立（附录 A.8）逐类判定。PCA 场景逐类判定不预设通过；轻量文档原子正文行、事务性物料行和普通议题记录跳过。

消费 wft01 在名称级捕获的 PL6-B 条目清单，逐条展开：

| 维度 | 说明 |
|------|------|
| **确认条目名称** | wft01 已捕获 → 确认并标准化；wft01 标注 `[待wft02推断]` → 基于 bpd-biz 上下文推断 |
| **定义要素** | 指标：口径/维度/目标值；看板卡片：数据源/图表类型/刷新频率；计划项：清单类型/优先级规则；风险/问题：分类/等级；行动项：状态集 |
| **生命周期** | 独立于父文档的状态变迁路径 |
| **支撑引擎** | 继承自文档支撑引擎（Phase A 校验后的结论），不单独判定 |

全部 PL6-B 标注 `[推断]`，经人类确认后方可落账。wft01 未捕获但对闭环必要的条目 → 先行推断 + `[推断-待规范化需求确认]`，由 Step 5 生成补充规范化需求材料。

**任务关系引擎化**：在全部输出文档方案及活动节点设计完成后，配置引擎任务间触发规则：

| 关系类型 | 来源 | 转化规则 |
|---------|------|---------|
| 文档间流转顺序 | wft01 `业务链文档序列` | → 输出文档序列编排 |
| 规范化需求明示的触发关系 | wft01 规范化需求中捕获 | → 引擎任务间触发规则（源文档/触发条件/目标文档/启动参数） |
| 任意任务触发 | 91 规范 §2.5 R5 | 计划/流程/工单可触发任意类型，仅基于需要生成，不套固定拓扑 |

每个关系定义：源文档、触发条件（状态/节点）、目标文档、启动参数、关系来源。来源为业务链序列的直接映射除外（继承 wft01 确认），其余全部标注 `[推断]`。

---

### Step 5 · 业务需求增补

汇聚详设层**设计性缺口**（PL6-B/指标/活动节点级，Phase E 标注 `[推断-待规范化需求确认]`）统一生成补充规范化需求材料——结构性缺口已由 wft01 Bn 组装处理（wft01 Step 4 补充规范化需求回路），本 Skill 不重复生成。

**1. 汇聚缺口**：在 bpd-biz 节点中创建 `### 业务缺口说明`，逐条列出详设层已标注的设计性缺口，按所属文档/WBS 分组，附加置信度和推断依据。

**2. 生成补充规范化需求材料**：

```bash
# 写入路径
echo "..." > ../../../80-pl4eos-2-eosdata/00-origin-requirement-materials/10-raw-files/$(date +%Y%m%d)-wft02-biz-{批次ID}-业务需求增补.md
```

包含：材料编号 / 来源（bpd-biz 节点 ID + bph-biz 节点 ID）/ 背景 / 缺口清单（按 WBS 分组）/ 边界说明（不是正式规范化需求，不写入 `01-*.md`，需经规范化需求预处理链确认）。

**3. 登记状态**：更新 `01-eos-sysdev-status.md` 表 A，生命周期=`已登记`。

**4. 人类确认与例外**（Step End 人类裁决缺口时，详见人类方案 §5.6）：

- 人类确认缺口 = **发起补充规范化需求的签字**——确认存在性（文档确实需要），不等于提供业务身份；缺口仍走规范化需求→bph-biz 管道，本轮只产出缺口声明，不直接设计缺口文档
- **例外**：缺口文档在 23 已有 PL5 输出文档节点或 04 已有 bpd-biz 文档方案（业务链位置可从 23 明确推出）→ 属"bph-biz 补列遗漏"非"新业务"，可直接引用已有文档设计 + 提示人类建议 wft01 补 bph-biz 节点 + 生成补充规范化需求材料备案（26 号资产已取消，2026-08-22）

**5. 覆盖自查**：按 [附录 A.6](#a6-覆盖自查清单) 逐项打勾；发现遗漏**仅标 `[覆盖缺口]` 不回退**（判据见人类方案 §3.5）。

---

### Step 6 · 资产写回与落账

仅写回状态=`可以srh设计` 的 bpd-biz 节点。人类已在 Step 2 确认方案，不区分 Q 级别、不设二次确认。

**写回资产**：

遍历已确认 bpd-biz 节点的对齐结论，按 Q 级别写回：

| Q 级别 | 操作 |
|--------|------|
| Q1 复用 | 定位已有块，追加 bpd-biz 引用（去重） |
| Q2 扩展 | 扩展已有块，追加引用 + 扩展说明 |
| Q3 新建 | 新建块，写入定义 + 引用 |

| 资产 | 写回内容 |
|------|---------|
| **23** | A2/Bn 产品路径 + 菜单可达性路径（23A/23B） |
| **24** | L4~L6 可复用业务（SL5 场景业务→L4、PL5 输出文档→L5、PL6 功能元件→L6，按 24 资产业务本体层级写回） |
| **27** | 资源资产引用（CAX 工具/AI 工具/模板/专业能力/作业指南） |
| **28** | 看板指标资产（看板文档定义、指标文档定义） |

> 文档定义不再写 26（26 号资产已取消，2026-08-22）——文档方案定义承载在 04（§5.5.2 正文实体结构），承接锚点在 25 §1.7。

**记录追溯**：

| 操作 | 脚本/位置 |
|------|----------|
| 资产写回记录 | `04-*.md` `### 资产写回记录`（日期/路径/原子单元/目标资产块/动作/判定） |
| bph-biz 推进 | `02-*.md` 对应节点 → `在分解分配`（Step 3 锁步）→ `已分解分配`（消费版本由 bpd-biz 节点「来源 bph-biz」记录） |
| AI最近变更 | `02/04-*.md` `## AI最近变更` 追加一行，保留近 30 条 |
| AI可以处理节点 | 已处理节点移出"待 wft02-biz 处理"；`[修改]`/`[需裁决]` 条目移入"待反馈处理" |

**元信息维护**（用脚本，不手动）：

```bash
# 更新文件头
bash ../scripts/update-meta.sh ../../../80-pl4eos-2-eosdata/02-eos-business-summary-architecture.md bump-version
bash ../scripts/update-meta.sh ../../../80-pl4eos-2-eosdata/02-eos-business-summary-architecture.md update-head
bash ../scripts/update-meta.sh ../../../80-pl4eos-2-eosdata/04-eos-business-detailed.md bump-version
bash ../scripts/update-meta.sh ../../../80-pl4eos-2-eosdata/04-eos-business-detailed.md update-head
bash ../scripts/update-meta.sh ../../../80-pl4eos-2-eosdata/23-eos-output-architecture.md bump-version
bash ../scripts/update-meta.sh ../../../80-pl4eos-2-eosdata/23-eos-output-architecture.md update-head
bash ../scripts/update-meta.sh ../../../80-pl4eos-2-eosdata/24-eos-business-assets.md bump-version
bash ../scripts/update-meta.sh ../../../80-pl4eos-2-eosdata/24-eos-business-assets.md update-head

# 追加 AI最近变更 记录
bash ../scripts/update-meta.sh ../../../80-pl4eos-2-eosdata/04-eos-business-detailed.md add-recent-change "wft02-biz" "资产写回" "<bpd-biz-ID>" "23/24/27/28 资产写回"
```

---

### Step End · 运行结束输出（含反馈等待）

按三区模板输出方案摘要：

```text
当前状态：<待确认方案 / 可以srh设计>
  本轮 bpd-biz：<bpd-biz-ID / 新建或修订 / 版本变化 / 来源 bph-biz>
资产落账：<未落账 / 已写回 23/24/27/28>
补充规范化需求材料：<无 / YYYYMMDD-wft02-biz-批次ID-业务需求增补.md>（已登记 材料状态表）

一、方案反馈
  A. 整体确认 → 回复「整体确认」，快捷同意全部未标注条目
  B. 局部修改 → 在确认状态中标注 [修改]：...
  C. 驳回重做 → 在确认状态中标注 [驳回]：...

二、下一步
  本Skill → 选择 bph-biz 节点（`可以分解分配` / `待补充分解分配`）或 `需wft02修订` 的 bpd-biz 节点重新运行
  后续    → bpd-biz-XXX（可以srh设计）、bpd-biz-YYY（待补充srh设计）→ wft03-biz
```

**输出后不要结束对话。** 等待人类反馈——人类可直接用自然语言回复修改意见：

> 以上是本轮方案。直接回复修改意见即可：
> - "第三个文档引擎改为 @engine-workorder"
> - "整体确认"
> - "采购申请单的 PL6-B 粒度太粗，拆分一下"

**AI 处理反馈规则**：

| 人类输入 | AI 动作 |
|---------|--------|
| 显式修改指令（"X 改为 Y"） | 定位条目 → Edit 修改 → 追加 `[已处理]` → 输出修改摘要 → 继续等待 |
| 质疑/讨论（"为什么选 X？"） | 解释理由，不自动修改，继续等待决策 |
| 整体确认（"可以"/"整体确认"） | 全部条目追加 `[同意] [已处理]` → **整理本次反馈总结**（见下）→ 推进状态 → Step 6 落账 → 结束 |
| 模糊意见（"粒度太粗"） | 尝试具体化追问，引导可执行指令 |
| 线下修订检出（git diff 发现确认状态标注/自由文本编辑） | 定位条目 → Edit 修订 → 追加 `[已处理]` → 输出修改摘要 → 继续等待 |

**反馈总结**（仅在人类"整体确认"后执行）：

```bash
bash ../scripts/update-meta.sh ../../../80-pl4eos-2-eosdata/04-eos-business-detailed.md add-recent-change "wft02-biz" "反馈处理" "<bpd-biz-ID>" "<AI 总结的反馈要点>"
```

摘要示例：`引擎修正：@engine-flow→@engine-workorder；PL6-B"订单及时率"新增口径定义`

---

## 三、AI 标记约定

四类增量标记（`[新增]`/`[变更]`/`[确认]`/`[需裁决]`）的定义详见 [91 规范 §A.3.1](../91-eos-biz-eng-spec.md#a31-ai-增量标记)。

每次 Step 3~5 完成后，向人类输出增量清单，格式如：

```text
=== bpd-biz-XXXX 本轮更新清单 ===

[新增] 新建 bpd-biz 节点 bpd-biz-XXXX（Q3，来源 bph-biz-YYYY）
  修订 bph-biz-ZZZZ（+2 条 bph-biz，版本 v2→v3）

A2 角色场景视角
  [新增] SL4 角色职责：采购申请执行    资产对齐：23A=Q1、24=Q3
  [确认] SL5 场景业务：采购员执行采购申请 → 归入已有 SL4

Bn 输出产品视角
  [新增] 消费 wft01 Bn 组装：PL4 WBS=采购申请创建与维护    资产对齐：23B=Q2
  [新增] 输出文档"采购申请单"（@engine-flow + @engine-entity）  文档定义落 04（正文实体结构）+ 承接锚点 25 §1.7
  [确认] 输出文档"审批记录"（04 已有文档方案）

任务活动设计
  [新增] "采购申请单"活动序列：创建→提交→审批→通过/驳回

PL6-B 正文条目需求
  [新增] "采购履约看板" PL6-B：订单及时率（口径=按时交付数/订单总数，目标≥95%） [三独立：是]

设计性缺口
  [推断-待规范化需求确认] "采购履约看板"缺"到货合格率"条目（详设层闭环必需）→ 转补充规范化需求材料

变更影响声明
  变更类型：修订型（已有 bpd-biz 归入新 bph-biz）
  对下游影响：受影响的 srh-biz 需局部调整"采购申请单"表单结构
```

---

## 附录A：判据速查

### A.1 Q1/Q2/Q3 对齐判定

| 资产 | Q1 复用 | Q2 扩展 | Q3 新建 |
|------|---------|---------|---------|
| **23A** A2 路径 | 路径存在且职责边界一致 | 路径存在但需扩展菜单可达性边界 | 路径不存在，写新建建议 |
| **23B** Bn 路径 | E2E 项目业务+构件类型路径存在且一致 | 需扩展构件类型、子节点或归属 | 路径不存在，写新建建议 |
| **24** 业务资产 | 已有同名/同义业务块且职责范围一致 | 存在相似业务块但职责需扩展 | 无匹配，写新建业务块建议（按 24 资产业务本体层级 L4~L6 对齐） |
| **28** 看板指标 | 已有同名/同义看板且指标方案一致 | 需扩展指标方案（新增/修改指标） | 无匹配，写新建看板建议 |

27 资源资产不使用 Q 判定——已有资源直接追加引用（去重）；缺失资源形成资源资产建议（名称/类型/用途）。

### A.2 引擎校验三结果

> 任务类型与引擎一一映射（耦合判定）——修正引擎即修正任务类型，变更原因写清"任务类型从 X 修正为 Y"（详见人类方案 §3.2）。

| 校验结果 | 判定条件 | 动作 |
|---------|---------|------|
| **确认** | 候选引擎与 bpd-biz 展开后的文档职责和处理模式完全匹配 | 去掉 `[推断]` 标记，升级为已确认 |
| **修正** | 候选引擎部分匹配但需调整（如 @engine-flow → @engine-workorder） | 写入修正后引擎 + 修正原因 + **同步修正任务类型（流程→工单）** + 去掉 `[推断]` |
| **推翻** | 候选引擎与 bpd-biz 处理模式不兼容，且 25 资产中无可替代引擎 | 标注 `[引擎待确认]` + 不兼容原因 → Step End 提示人类裁决 |

### A.3 23 资产一致性检查（已上移 wft01）

**23 一致性检查在 wft01 完成**：wft01 的 Bn 组装（[wft01-biz §3.3.3 ③](eos-wft01-biz-spr2bph.md#333-bn-资产对齐判定与-bn-组装)）在 23B Q 判定 + PL4 WBS 归组完成后校验两者是否矛盾——检出归属冲突（标 `[23归属冲突]` 提示人类裁决）、误新建（降为 Q2）、误引用（升为 Q2/Q3）；矛盾项由 wft01 暂不写回 23，待人类裁决后下一轮处理。本 Skill 只消费其结论（bph-biz 节点 Bn 组装结论），Step 6 写回时不设「23 例外」。

### A.4 结构性缺口推断（已上移 wft01）

**结构性缺口推断在 wft01 完成**：按构件产品数据清单完整性做名称级比对（[wft01-biz §3.3.3 ③](eos-wft01-biz-spr2bph.md#333-bn-资产对齐判定与-bn-组装)），结构性缺口经 wft01 Step 4 补充规范化需求回路补回。本 Skill 只处理详设层**设计性缺口**（PL6-B/指标/活动节点级，Phase E 标注 `[推断-待规范化需求确认]`），由 Step 5 生成补充规范化需求材料。文档定义承载于 04 正文实体结构（§5.5.2），锚点 25 §1.7（26 号资产已取消），与结构性缺口推断无关。

### A.5 变更类型判定

| 变更类型 | 判定标准 | 对下游影响 |
|---------|---------|----------|
| **无变更** | 本轮新输入全部走来源确认或引用追加；无新增/改进/边界扩展/结构调整 | 下游无需任何动作 |
| **增量型** | 仅新增业务/场景要素，已有业务/产品路径/功能表单结构均未改动 | 已有 srh-biz 节点不受影响，仅需映射新业务 |
| **修订型** | 已有业务/场景要素被修改；产品路径与功能表单结构未变 | 对应 srh-biz 节点需局部调整 |
| **结构型** | 产品路径需新增 SL2/PL2 节点或跨业务域重组、菜单新增/合并/拆分 | srh-biz 结构可能重塑，需人类裁决后方可处理 |

判定优先级：结构型 > 修订型 > 增量型 > 无变更。Q2 在已有路径下扩展子节点属修订型。

### A.6 覆盖自查清单

Step 5 末尾使用，逐项打勾，发现遗漏仅标注不回退——写入 `[覆盖缺口]` 及缺失说明：

1. 业务缺口已汇聚（`### 业务缺口说明`）+ 补充规范化需求材料已生成 + 材料状态表已登记
2. SL3~SL5 角色场景 bpd-biz 方案（含需求分配到输出文档 + 表单使用顺序）
3. PL3~PL5 产出产品 bpd-biz 方案（含输出文档路径 / 数据权限；结构性缺口已由 wft01 Bn 组装处理）
4. Step 3/4 执行完整性——逐 bph-biz 输出文档核对已消费 Bn 组装、任务节点引擎化、任务关系引擎化、PL6-B 定义、设计性缺口、权限、表单使用顺序（Step 3）
5. 23 对齐判定（A2/Bn 产品路径 / 菜单可达性路径）
6. 24 对齐判定（L4~L6 可复用业务，按 24 资产业务本体层级）
7. 27 对齐判定（资源资产引用）
8. 28 对齐判定（看板指标资产）
9. PCA 场景业务链已含计划清单文档（[91 §2.7](../91-eos-biz-eng-spec.md#27-pca-场景的-pca)，与 R4 首文档为两个实例）
10. 变更影响声明
11. bpd-biz 版本号

### A.7 bpd-biz 节点生命周期状态流转

**主线流转**（wft02-biz ←→ wft03-biz）：

```
待确认方案 ──人类确认──→ 可以srh设计 ──wft03锁定──→ 在srh设计 ──wft03完成──→ 已srh设计
     ↑                      ↑                       │
     │                      │              wft03退回 │
     │                      │                       ↓
     └──── wft02修订 ──── 需wft02修订 ←─────────────┘
```

**上游变更分流**（bph-biz 修订触发 bpd-biz 重入）：

```
可以srh设计 → 可以srh设计（版本递增，wft03 未消费）
在srh设计   → 在srh设计（版本递增，wft03 增量处理）
已srh设计   → 待补充srh设计 → wft03补全 → 已srh设计
```

wft02-biz 写入 `待确认方案`/`可以srh设计`/`待补充srh设计`/`需wft02修订`；wft03 写入 `在srh设计`/`已srh设计`。

### A.8 PL6-B 三独立判定

| 条件 | 判据 | 正例 | 反例 |
|------|------|------|------|
| ① 独立名称 | 在文档内有固有称谓 | ✓ "订单及时率"、"采购预警" | ✗ 物料行（"序号1"不是名称） |
| ② 独立定义要素 | 有独立业务约束/规则/格式 | ✓ 指标有口径/维度/目标值 | ✗ 物料行的编码/数量由文档字段结构定义 |
| ③ 独立生命周期 | 独立于父文档的状态变迁 | ✓ 指标需审批后发布 | ✗ 物料行随申请单创建/删除 |

三项全满足 → 展开 PL6-B。轻量文档原子正文行不重复判定。

### A.9 任务节点引擎化类型→引擎映射

| 任务类型 | 引擎 | 节点转化 |
|---------|------|---------|
| 计划 | @engine-task | 角色→引擎角色配置，动作→引擎动作配置，状态变迁→引擎状态配置 |
| 流程 | @engine-flow | 同上。wft01 已捕获节点直接映射；`[待wft02推断]` 节点设计完整序列（含系统自动节点/异常路径/超时处理） |
| 工单 | @engine-workorder | 同上 |

wft01 已捕获节点是业务流转确认结果（供人类确认过），wft02 在此基础上映射引擎 CU、**不重新推断业务流转**；发现缺陷 → 标注 `[需裁决]` 或退回 wft01（详见人类方案 §2.5.1/§5.5.3）。

全部节点标注 `[推断]`，未经人类确认不得落账。

---

## 附录B：脚本接口速查

| 脚本 | 用法 | 说明 |
|------|------|------|
| `read-section.sh` | `bash ../scripts/read-section.sh <文件> <分节名>` | 提取 ##/### 分节完整内容 |
| `read-node.sh` | `bash ../scripts/read-node.sh <文件> <节点ID>` | 按节点ID提取节点块（支持 bph-biz-XXX / bpd-biz-XXX / ROLE-XXX / @engine-xxx 等） |
| `detect-changes.sh` | `bash ../scripts/detect-changes.sh <文件>` | 检测人类自上次 AI 运行以来的文档变更 |
| `update-meta.sh` | `bash ../scripts/update-meta.sh <文件> <操作>` | 维护文件元信息（bump-version / update-head / add-recent-change / move-node） |

所有脚本路径相对于 `eos-wft02-biz-bph2bpd/` 目录。

---

## 变更记录

| 日期 | 版本 | 说明 |
| 2026-08-25 | v1.21 | 链级机械修正（wft01-nfr 深审链级联动，人类裁决 2026-08-25）——数据文件相对路径修正 `../../80-pl4eos-2-eosdata/`→`../../../80-pl4eos-2-eosdata/`（自 skill 子目录上跳 3 级）。AI 执行规则语义不变 |
|------|------|------|
| 2026-08-24 | v1.20 | 材料状态表联动（wft03 单独轮 B4）——「规范化需求 原料状态表」→「材料状态表」（3 处）。AI 执行规则语义不变 |
| 2026-08-24 | v1.19 | 人类方案同步（缩写更名 sfh→srh/sfd→srd + 中文层名统一）；AI 执行规则语义不变（术语随迁）。 |
| 2026-08-24 | v1.18 | 人类方案 v9.11 同步（审视修复，人类逐项裁决）——①**Phase B 补「主子/下钻关系」要素**（对齐 §5.5.2，供 wft03 TAB 编排消费）；②**Phase A 修正行补「同步修正任务类型」**（对齐 §3.2/A.2）；③**Step 5 补「覆盖自查」步**（引附录 A.6）；④**A.6 覆盖自查补三项**（业务缺口闭环 / PCA 计划清单文档 / 任务关系引擎化+PL6-B 定义）；⑤术语同步（业务流程概要/业务流程详细全称、规范化需求→规范化需求、`[推断-待规范化需求确认]`→`[推断-待规范化需求确认]`）。AI 执行规则语义变更（新增主子/下钻关系定义 + 覆盖自查执行） |
| 2026-08-24 | v1.17 | 资产名更名（人类定案）——STR-F→bph-biz、BP→bpd-biz、SR-F→srh-biz（节点类型/节点ID/状态名/分节名），版本头版本号同步递增 |
| 2026-08-24 | v1.16 | 文件名更名同步（人类定名 2026-08-24）——`eos-wft02-biz-str2bp` → `eos-wft02-biz-bph2bpd`（name/frontmatter/设计依据/全库引用改向）。AI 执行规则语义不变 |
| 2026-08-24 | v1.15 | 人类方案 v9.8 同步（深审第四轮跨文件接口修复——STR-F 锁步）：负责表补「锁定 STR-F→`在分解分配`」、Step 3 补锁步动作（写 `02-*.md` 节点状态 `在分解分配` + 活动说明）、Step 6 追溯表「STR-F 推进」改「`在分解分配` → `已分解分配`」。AI 执行规则随迁（新增锁步执行动作） |
| 2026-08-22 | v1.14 | 人类方案 v9.7 同步（深审第三轮修复）——①Step 6 写回表「STR-F 推进」改只推进状态（消费版本由 BP 节点「来源 STR-F」记录，资产写回补消费版本列）；②自检项改「Step 3/4 执行完整性」（表单使用顺序标注 Step 3）。AI 执行规则语义变更（消费版本记录位置 02→BP 节点） |
| 2026-08-22 | v1.13 | 人类方案 v9.6 同步（深审 C 组 + 取消 26 扩展 25）：description/负责表/特化参数写回集 23/24/26/27/28→23/24/27/28；Step 1 加载清单删 26（补 25 §1.7 承接映射）；Phase D「26 文档定义对齐」→「文档定义承载（04 + 25 §1.7）」；Step 5 例外「26 资产已定义补列」→「已定义文档补列（23/04）」；Step 6 写回表 24 行改 L4~L6 业务本体层级 + 删 26 行 + 元信息删 26；A.1 判据表删 26 行 + 24 行口径更新；A.6 覆盖自查去 26 项；增量清单示例 26=Q 改「文档定义落 04」。AI 执行规则语义不变（文档定义不再对齐 26 资产） |
| 2026-08-22 | v1.12 | 人类方案 v9.3~v9.5 同步（深审 B 组）——跨文件锚点 `#333-bn-资产对齐判定` 补全为 `#333-bn-资产对齐判定与-bn-组装`（4 处，对齐主文件 v9.5）。AI 执行规则语义不变 |
| 2026-08-21 | v1.11 | 人类方案 v9.2 同步（biz 链「概要/详细」分工框架定案实施——本 Skill 定位为业务详细定义 s02，收缩纯详细层）：description/负责表/上游衔接改「消费 wft01 Bn 组装」；Step 3 补「表单使用顺序」；Step 4 Phase A 改「引擎校验与消费 Bn 组装」（② 消费 Bn 组装结论不重复归组、③ 23 一致性已上移 wft01）、Phase D 改「权限设计（结构性缺口已上移 wft01）」；Step 5 改设计性缺口口径（结构性缺口由 wft01 Step 4 处理）；Step 6 去掉「23 例外」行；§三 增量清单补消费 Bn 组装段、文档缺口推断→设计性缺口；附录 A.3/A.4 改「已上移 wft01」、A.6 覆盖自查同步。AI 执行规则语义不变（WBS/一致性/结构性缺口不重复执行） |
| 2026-08-21 | v1.10 | 人类方案 v9.1 同步（非主价值链 Step 框架链级统一）——Step End AI 处理反馈规则表补「线下修订检出」行。AI 执行规则语义不变 |
| 2026-08-19 | v1.8 | 人类方案 v8.9——Step 2 补「反馈双轨」（AI 对话反馈 + 线下文档修订经 git diff 检测，两轨等价均落账）；AI 执行规则语义不变 |
| 2026-08-19 | v1.7 | 人类方案 v8.8——WBS 归组引用补数据依赖链硬判据（§3.3.2，跨链不归组/同链切段/管理类按其支撑的交付链归属）；AI 执行规则语义不变 |
| 2026-08-19 | v1.6 | 人类方案 v8.7 + wft01 v3.12——A.9 任务节点引擎化补接力边界（wft01 已捕获节点=业务流转确认结果、不重新推断；缺陷标注 [需裁决]/退回 wft01）；AI 执行规则语义不变 |
| 2026-08-19 | v1.5 | 人类方案 v8.6——Step 5 补「人类确认与例外」（人类确认缺口=发起 规范化需求 签字不绕过管道；26 资产已定义补列例外可直连）；AI 执行规则语义不变 |
| 2026-08-19 | v1.4 | 人类方案 v8.2~v8.5——A.2 引擎校验补任务类型-引擎耦合（修正引擎同步修正任务类型）；Phase C 流程工单活动设计→任务活动设计（覆盖计划/流程/工单三类，计划走策划→发布标准化节点）；AI 执行规则语义不变 |
| 2026-08-19 | v1.3 | 人类方案 v8.1 原理与方法审视修复——A.5 变更类型判定补「无变更」类（对齐 §4.2.1，判定优先级补无变更）；AI 执行规则语义不变 |
| 2026-08-19 | v1.2 | 人类方案 v8.0 知识层三章重构——锚点迁移（#262→#332，对齐 §3.3.2）；AI 执行规则语义不变 |
| 2026-08-19 | v1.1 | 链级术语统一：正文转化引擎/正文支撑引擎→支撑引擎（对齐人类方案 v7.4）；AI 执行规则语义不变 |
| 2026-07-20 | v1.0 | 初始版本——从人类方案文档 v7.2 提取 Skill，脚本化数据访问，领域知识引用人类方案 |
