# 平台能力系统需求概要定义 → 平台能力系统需求详细定义 · 组件业务详细定义
**人类方案ID**: wft04-eng-srh2srd

> **概述**：承接 wft03-eng 产出的系统需求概要 srh-eng（功能表单清单 + 结构判定级概要说明 + 操作活动清单 + 页面编排概要），展开为**系统需求详细定义**（eng 链 04 层）——判定区域内布局组件与构件、定义功能表单上各组件及其业务（功能级不写实现）、标注 pa-eng 层收敛项。**04 组件业务是 wft05-eng 推断前后端组件的依据**；pa-eng 层收敛项（原 wft03 §2.3.3 下移）在组件业务条目标注。
>
> **输入 → 输出**：系统需求概要 srh-eng 节点（`可以详细定义` / `待补充详细定义`）/ 有反馈的 srd-eng 节点 → 系统需求详细 srd-eng（区域内布局组件/构件 + 组件业务 + pa-eng 层收敛项）+ srd-eng 推进为 `已详细定义`
>
> **适用场景**：瀑布式系统设计第4步 · 平台能力系统设计路径。
>
> **设计模式参考**：[91-eos-biz-eng-spec.md](91-eos-biz-eng-spec.md)
>
> **版本**：v2.2 | **修订**：2026-08-25

---

## 一、职责定位

**一句话**：本章说明本 Skill 的定位——要达成什么（1.1 目的与目标）、与上下游如何衔接（1.2）。

### 1.1 本 Skill 目的与目标

> **主要目的**：取信于平台开发人员——「这个功能表单的区域内由哪些组件构成、每个组件的业务是什么、有哪些问题需留待 pa-eng 判断」。承接 wft03-eng 产出的系统需求概要 srh-eng，展开为系统需求详细 srd-eng——判定区域内布局组件与构件、定义组件业务（功能级不写实现）、标注 pa-eng 层收敛项，为 wft05-eng 设计前后端组件提供完整输入。

> **三层定位（按 EOS 流水线系统设计规范 §8.1）**：本 Skill 属于第1层（流水线设计）活动——在 EOS 流水线设计层将 wft03-eng 产出的系统需求概要 srh-eng 展开为系统需求详细 srd-eng；产出物经 wft05-eng 展开为 pa-eng 后，以设计规范/AI SKILL 形态交付第2层（构件开发），最终由第3层 EOS 开发者（确认者/用户）使用。本 Skill 的执行分工按 EOS 流水线系统设计规范 §8.3：流水线开发者（主导者/审核者）审核 AI 的组件业务方案并决策变更路径，AI（执行者）执行标准操作流程，EOS 开发者（确认者/用户）确认交付物可用性。

**目标**——完成以下五项即为本 Skill 使命达成：

| # | 目标 | 说明 |
|:-:|------|------|
| 1 | **承接系统需求概要 srh-eng** | 读取功能表单清单/概要说明/操作活动清单/页面编排概要，校验完整；不重新定义功能表单或操作活动清单 |
| 2 | **判定布局组件与构件** | 在页面编排概要（区域/TAB/弹窗入口）基础上，判定区域内布局组件组合（多级横版/列表/单据/辅助栏/弹窗/指标/看板），按展示/交互/数据/反馈四类识别构件 |
| 3 | **定义组件业务** | 为每个布局组件/构件定义业务（功能级不写实现）——承载的操作活动清单、数据来源、交互行为、状态反馈 |
| 4 | **标注 pa-eng 层收敛项** | 组件边界/接口/数据/运行/NFR 的待判断问题在组件业务条目标注 `pa-eng层收敛-*`，供 wft05-eng 设计组件 |
| 5 | **保留完整追溯** | 组件业务 → 功能表单 → 配置信息组 → 来源 CU → bpd-eng → bph-eng 的追溯链完整可验证 |

**本 Skill 负责**：

| # | 职责 | 动作 |
|---|------|------|
| 1 | **接收** | 人类选中的 ≤5 个系统需求概要 srh-eng 节点 → 校验状态 `可以详细定义`/`待补充详细定义`，检查功能表单清单/操作活动清单完整；缺失时退回 wft03-eng |
| 2 | **匹配** | 按 `引擎 + CU + 功能表单 + 区域内组件` 匹配已有系统需求详细条目 → 职责一致则复用，扩展则全量重写，无可匹配则新建，冲突标记 `[需裁决]` |
| 3 | **布局组件判定** | 在页面编排概要基础上，判定区域内布局组件组合（自 wft03-eng 收缩前 §2.2.1/§3.2.3 迁入，[91 规范 §四](91-eos-biz-eng-spec.md#四布局组件构件与路径边界)） |
| 4 | **构件识别** | 按展示/交互/数据/反馈四类逐区域识别构件，标注类型和跨功能表单复用范围 |
| 5 | **组件业务定义** | 为每个布局组件/构件定义业务（功能级不写实现）——承载操作活动、数据来源、交互行为、状态反馈 |
| 6 | **pa-eng 层收敛项标注** | 组件边界/接口/数据/运行/NFR 的待判断问题在组件业务条目标注（自 wft03-eng 收缩前 §2.3.3 迁入） |
| 7 | **推进** | 人类确认后：srd-eng → `已详细定义`，记录消费版本 |
| 8 | **回修** | 接收 wft05-eng 退回的 `需wft04修订` srd-eng，读取缺失说明，按人类方案修订；根因在系统需求概要时退回 wft03-eng |

**本 Skill 不负责**：

| 职责 | 归属 |
|------|------|
| 系统需求概要（功能表单清单+概要说明+操作活动清单+页面编排概要） | → `wft03-eng`（本 Skill 只承接，不重新定义） |
| 操作活动定义（配置操作/系统处理/运行期能力） | → `wft02-eng` |
| pa-eng 前后端组件设计（前端/后端/平台服务，07 pa-eng） | → `wft05-eng`（本 Skill 只标注收敛项，不设计组件） |
| 代码实现、测试用例、数据库物理表或字段级接口协议 | → 构件开发流水线 |

### 1.2 与上下游的衔接

**上游 wft03-eng**：产出系统需求概要 srh-eng，包含功能表单清单（配置类+运行类）、结构判定级概要说明（页面职责/页面类别/使用角色/承载关系）、操作活动清单（承接 02 标注入口）、页面编排概要（区域/TAB/弹窗入口）。本 Skill 承接该概要展开组件业务，不重新定义功能表单或操作活动清单。

**下游 wft05-eng**：消费 `已详细定义` 的系统需求详细 srd-eng（区域内布局组件/构件 + 组件业务 + pa-eng 层收敛项），结合 FR-BIZ 指标约束包和 NFR 约束包设计 pa-eng 三类组件（前端/后端/平台服务）。若发现组件业务或 pa-eng 收敛项缺失，将 srd-eng 退回为 `需wft04修订`。

**交付链条**（按 EOS 流水线系统设计规范 §8.1 三层框架）：本 Skill 产出（**系统需求详细定义**——功能表单组件及其业务）→ wft05-eng（平台产品架构）→ **第2层交付**（设计规范/AI SKILL 形态交付至 EOS 设计空间）→ **第3层使用**（EOS 开发者使用交付物对 EOS 进行系统设计）。本 Skill 处于第1层中后段——承接系统需求概要展开为可被 pa-eng 消费的系统需求详细。

---

## 二、基本概念：从系统需求概要 srh-eng 到系统需求详细 srd-eng

**一句话**：本章讲**基本概念**——从系统需求概要 srh-eng 到系统需求详细 srd-eng 的核心产物是什么：功能组件业务（2.1）、布局组件与构件（2.2）、pa-eng 层收敛项（2.3）与承载关系（2.4）；转换总览先给整条设计主线（五步一条链 + 规则地图）。设计原理与判据见 §三，节点演进见 §四，操作步骤见 §五。

> **概要/详细分工叙述**：eng 链五个 Skill 以「概要定义/详细定义」重述（2026-08-21 定案）——wft01-eng 产出**业务流程概要定义**（01）、wft02-eng 产出**业务流程详细定义**（02）、wft03-eng 产出**系统需求概要定义**（03，末级=功能表单+概要说明）、本 Skill 产出**系统需求详细定义**（04，条目=**功能表单上各组件及其业务**，含 pa-eng 层收敛项标注）、wft05-eng 产出**平台产品架构**（05，末级=前后端组件+详细需求定义，07 pa-eng）。STR-E/BP/SR-F/PA 资产名已按 2026-08-24 更名清单迁移：STR-E→bph-eng、BP→bpd-eng、SR-F→srh-eng（03 系统需求概要）/srd-eng（04 系统需求详细）、PA→pa-eng。**03 写系统需求概要 srh-eng、04 写系统需求详细 srd-eng，状态机接力**：03 写功能表单概要→`可以详细定义`、04 写组件业务→`已详细定义`。**04 vs 03**：03 到区域/TAB/弹窗入口，04 到区域内具体组件+业务（布局组件/构件，功能级不写实现）。**04 vs 05**：04=功能组件业务（页面元素），05=产品组件（前端/后端/平台服务，07 pa-eng），**04 组件业务是 05 推断前后端组件的依据**。

### 转换总览：系统需求概要 srh-eng → 系统需求详细 srd-eng

**一句话**：本节给整条设计的路线图——转换方法（五步一条链）与规则地图（判据索引）；转换的"为什么"见 §三 3.1 转换原理。

#### 转换方法：五步一条链

**输入**：系统需求概要 srh-eng 节点（`05-*.md`，状态=`可以详细定义`/`待补充详细定义`）。
**输出**：系统需求详细 srd-eng 节点（`06-*.md`，系统需求），供 wft05-eng 展开为 pa-eng（前后端组件）。

| 步 | 做什么 | 判据 / 细则 |
|----|--------|-------------|
| ① 路由 | 组件候选生成 + Phase A 宿主匹配（归入已有系统需求详细条目 → Phase B 修订 / 待聚合池 → Phase C 新建） | §5.4.1 |
| ② 要素 | 布局组件判定 + 构件识别（基于页面编排概要 + 操作活动清单） | §3.2.2 / §3.2.3 |
| ③ 定义 | 组件业务定义 + pa-eng 层收敛项标注 | §3.3 / §5.5 |
| ④ 检查 | 覆盖自查 + 变更影响声明 | §3.4 / §5.6 |
| ⑤ 写回 | 资产落账（25 系统需求详细引用）+ 推进 srd-eng 状态 + 提交基线 | §5.7 |

> 读法：①~⑤ 是设计主线，具体判据在 §三 与 §五；可执行步骤见 §五 操作流程（Start/1/2/3/4/5/6/End）。

#### 规则地图：判据索引

| 判据 | 回答什么问题 | 位置 |
|------|-------------|------|
| 功能组件业务 | 区域内组件的业务形态与要素是什么 | §2.1 |
| 布局组件与构件 | 区域内有哪些布局组件/构件、如何分类 | §2.2 |
| pa-eng 层收敛项 | 哪些问题留待 pa-eng 判断 | §2.3 |
| 承载关系 | 组件业务承接哪些功能表单/操作活动 | §2.4 |
| 组件候选生成 | 从页面编排概要生成哪些组件候选 | §3.2.1 |
| 布局组件判定 | 区域内布局组件组合怎么判 | §3.2.2 |
| 构件识别 | 展示/交互/数据/反馈四类怎么识别 | §3.2.3 |
| 宿主匹配检查 | 复用/扩展/新建/冲突怎么分流 | §3.2.4 |
| 组件业务定义 | 组件业务怎么定义（功能级不写实现） | §3.3 |
| 完备性判据 | 系统需求详细是否完备 | §3.4 |
| 变更类型判定 | 增量/修订/结构，怎么影响下游 | §4.2 |
| 覆盖自查 | 设计输出是否漏做（Step 5 机械检查） | §5.6 |

**标注纪律**（框架全链一致，各 Skill 按域应用）：AI 设计产出中凡需人类确认/裁决的项，统一显式标注——①`[推断]`：AI 按规则推断、供人类确认或裁决（如布局组件/构件判定存疑）；②`[待下游推断]`：信息不足、跨 Skill 持久化待确认（本 Skill 承接已确认锚点，此类别少用）；③`供人类确认`：业务/权威锚点、必须由人类确认（本 Skill 对组件职责冲突标 `[需裁决]` 交人类裁决）。

---

### 2.1 功能组件业务

**一句话**：本节讲功能组件业务的概念——定义与要素（2.1.1）；完备性判据见 §三 3.4。

#### 2.1.1 定义与要素

功能组件业务是功能表单**区域内**具体组件及其业务的详细定义，是系统需求详细（04 层）的详细条目。**04 vs 03**：03 到区域/TAB/弹窗入口，04 到区域内具体组件+业务（布局组件/构件，功能级不写实现）。**04 vs 05**：04=功能组件业务（页面元素），05=产品组件（前端/后端/平台服务，07 pa-eng）。

**功能组件业务条目要素**（写入 `06-*.md` srd-eng 节点块）：

- 组件名称 / 所属功能表单 / 所在区域（TAB/弹窗入口）
- 布局组件类型（多级横版/列表/单据/辅助栏/弹窗/指标/看板）或构件类型（展示/交互/数据/反馈）
- 组件业务（功能级不写实现）——承载的操作活动清单（承接 03 操作活动清单）、数据来源、交互行为、状态反馈
- pa-eng 层收敛项（§2.3）
- 确认状态

> **边界**：本 Skill 定义组件**业务**（功能级）——布局/交互/数据/反馈的业务语义，不写实现（不写代码、不写数据库表、不写接口协议）；组件**实现**归 wft05-eng（07 pa-eng）与构件开发流水线。

---

### 2.2 布局组件与构件

#### 2.2.1 定义与正交关系

**布局组件**：多级横版、列表、单据、辅助栏、弹窗、指标页面、看板页面（树形表格是列表页面的呈现变体）通过布局组件组合表达功能表单**区域内**的承载方式。布局组件判定自 wft03-eng 收缩前 §2.2.1/§3.2.3 迁入。

**布局组件可选范围**：

| 布局组件 | 典型用途 |
|----------|----------|
| 多级横版 | 多层级配置对象、主从配置、流程/规则设计器 |
| 列表页面 | 配置对象集合、发布记录、版本记录、执行记录 |
| 单据页 | 单个配置对象编辑、参数维护、规则详情 |
| 辅助栏 | 详情预览、上下文摘要、引用关系、校验结果 |
| 弹窗页 | 选择、确认、局部参数编辑、冲突处理 |
| 指标页面 | 运行状态、质量指标、发布影响、异常趋势 |
| 看板页面 | 运行看板、任务看板、发布状态、异常汇总 |

**构件完整性分类**（四类须分别检查；无需求时写明"不适用"，不得只列按钮类构件）：

| 类别 | 典型构件 |
|------|----------|
| 展示类 | 列定义、字段布局、图表类型、卡片、树节点、阶段/里程碑 |
| 交互类 | 功能按钮、右键菜单、快捷键、下钻、行选中、TAB 切换 |
| 数据类 | 筛选器、排序器、分页器、导入、导出 |
| 反馈类 | 通知提示、进度条、状态标签、校验提示 |

> 布局组件与构件的详细定义见 [91 规范 §四](91-eos-biz-eng-spec.md#四布局组件构件与路径边界)，eng 路径分工见 [91 规范 §8.4](91-eos-biz-eng-spec.md#84-系统需求详细开发wft04-eng)。

---

### 2.3 pa-eng 层收敛项

**pa-eng 层收敛项是本 Skill 在组件业务条目中标注的「待 pa-eng 设计判断的问题线索」——pa-eng 本身（前后端组件）由 wft05-eng 设计**。本 Skill **不设计组件**，只把需要 wft05 判断的问题传递过去；不得预选同步/异步、存储归属或实现机制。

| 收敛项 | 示例 | 标注 |
|--------|------|------|
| 组件边界 | 发布按钮构件是否复用公共构件 | `pa-eng层收敛-组件边界` |
| 接口问题 | 发布动作是否存在跨组件交互待判定 | `pa-eng层收敛-接口` |
| 数据问题 | 配置版本、发布记录、审计记录需要持久化能力 | `pa-eng层收敛-数据` |
| 运行问题 | 装载、缓存刷新、回滚、重试等需要下游确定机制 | `pa-eng层收敛-运行` |
| NFR 问题 | 关联的 NFR 约束由 wft05-eng 消费约束包后决策 | `pa-eng层收敛-NFR` |

> **载件是 pa-eng 收敛项的主要来源**：wft02-eng 详设的配置信息组"系统处理·生效后响应"生成的载件（数据/配置/界面/构件/实例/支撑载件），经 03 操作活动清单承接后由 wft05-eng 判断如何由前后端组件承接——前端组件承接配置展示/交互，后端组件承接配置处理与业务逻辑。本 Skill 只标注问题线索，不设计载件的实现。

---

### 2.4 承载关系

**一句话**：本节讲组件业务的承接锚——系统需求详细承接系统需求概要（2.4.1）与页面编排概要（2.4.2）。

#### 2.4.1 承接系统需求概要

系统需求详细承接系统需求概要 srh-eng（03 产出）：

| 系统需求概要内容 | 系统需求详细承接 |
|-------------------|-------------|
| 功能表单清单（配置类+运行类） | 组件的所属功能表单（页面单位） |
| 结构判定级概要说明（页面职责/类别/使用角色/承载关系） | 组件的使用角色与承载配置信息组引用 |
| 操作活动清单（承接 02 标注入口） | 组件的承载操作活动清单（业务语义） |
| 页面编排概要（区域/TAB/弹窗入口） | 组件的所在区域（TAB/弹窗入口） |

#### 2.4.2 承接页面编排概要

页面编排概要（03 定义区域/TAB/弹窗入口）是组件判定的空间锚点——本 Skill 在**每个区域内**判定布局组件/构件并定义业务，不越过 03 的区域边界（组件不得跨区域定义，跨区域复用经宿主匹配处理）。

---

## 三、设计原理与判据：为什么这样设计、怎么判定

**一句话**：本章讲系统需求详细设计的逻辑——为什么用组件业务承接系统需求概要（3.1 转换原理）、怎么提取组件要素（3.2）、怎么定义组件业务与标注 pa-eng 收敛（3.3）、怎么判完备（3.4）；概念见 §二，节点演进见 §四，操作步骤见 §五。

### 3.1 转换原理：为什么用组件业务承接系统需求概要

**系统需求概要描述"这个功能表单如何编排、页面上有哪些可执行活动"，系统需求详细回答"区域内由哪些组件构成、每个组件的业务是什么"**。wft03-eng 产出的系统需求概要 srh-eng（功能表单清单+页面编排概要+操作活动清单）是组件业务的承接锚——本 Skill 承接该概要，判定区域内布局组件/构件并定义组件业务，供 wft05-eng 设计 pa-eng（前后端组件）。承接单位 = 功能组件业务条目（04 层详细条目）。

**04 组件业务是 05 推断前后端组件的依据**。wft05-eng 从组件业务推断前端容器组件/交互构件/状态反馈组件、后端服务组件——组件业务的完整性直接决定 pa-eng 可消费性（出口闸门）。

**04 不越权设计**。本 Skill 定义组件业务（功能级），不写实现（不写代码/数据库/接口协议）；pa-eng 组件设计归 wft05-eng，pa-eng 层收敛项只标注问题线索不设计。发现系统需求概要不足（组件无法在该区域内定义）→ 退回 `需wft03修订`；发现操作活动定义不足 → 经 03 退回 `需wft02修订`，不脑补。

**这套原理方法为什么可行**：它以**可验证性**为锚——组件业务以功能表单为边界（页面单位）承接系统需求概要，每个组件可被 wft05-eng 承接为 pa-eng 组件、追溯链（组件→功能表单→配置信息组→CU→bpd-eng→bph-eng→OR）完整、变更可传播；权威归信息最全的一方且只归其信息全的部分（系统需求概要在 03、组件业务在本 Skill、pa-eng 组件在 wft05）；判据全部可机械判定，操作全部有明确输入输出与执行模式（§5 Step 布局表），原理→判据→操作三层一一咬合，且与 91 规范、biz 路径同一套协议运转。

---

### 3.2 要素提取规则

**一句话**：本节讲从系统需求概要提取组件要素的规则——组件候选生成（3.2.1）、布局组件判定（3.2.2）、构件识别（3.2.3）、宿主匹配检查（3.2.4）。Phase B（修订已有系统需求详细）与 Phase C（新建系统需求详细）共用以下规则——先看 3.2.5 的 B/C 分工差异表，再读细节更顺。

#### 3.2.1 组件候选生成

对每个功能表单的每个区域（TAB/弹窗入口），按页面编排概要与操作活动清单生成组件候选：

| 页面编排概要 / 操作活动清单 | 组件候选 |
|----------------|---------------|
| 区域内有列表/版本记录/执行记录 | 列表布局组件（列表页面）+ 数据类构件（筛选器/排序器/分页器） |
| 区域内有单个配置对象编辑 | 单据布局组件（单据页）+ 交互类构件（按钮/右键菜单） |
| 区域内有主从/层级对象 | 多级横版布局组件 + 辅助栏（详情/上下文） |
| 区域内有临时选择/确认/冲突 | 弹窗布局组件 |
| 区域内有运行指标/异常趋势 | 指标/看板布局组件 |
| 操作活动清单有校验/发布/装载 | 反馈类构件（状态标签/校验提示/进度条） |

#### 3.2.2 布局组件判定

在页面编排概要基础上，独立判断区域内需要哪些布局组件：

| 判定条件 | 布局组件 |
|----------|----------|
| 需要展示对象集合、版本记录或执行记录 | 列表页面 |
| 需要编辑单个配置对象或参数组 | 单据页 |
| 需要并列展示多层主从对象 | 多级横版 |
| 需要上下文、详情、校验结果或引用关系 | 辅助栏 |
| 需要临时选择、确认、局部参数或冲突处理 | 弹窗页 |
| 需要展示运行指标、发布影响或异常趋势 | 指标页面 |
| 需要展示运行状态概览、任务分布或发布汇总 | 看板页面 |

#### 3.2.3 构件识别

在布局组件判定之后，按展示/交互/数据/反馈四类独立识别构件（四类定义见 §2.2.1）：

| 类别 | 识别线索 |
|----------|----------|
| 展示类 | 需展示哪些字段/图表/树/阶段 |
| 交互类 | 需哪些按钮/右键/快捷键/下钻/TAB |
| 数据类 | 需哪些筛选/排序/分页/导入/导出 |
| 反馈类 | 需哪些通知/进度/状态标签/校验提示 |

#### 3.2.4 宿主匹配检查

按 `引擎 + CU + 功能表单 + 区域内组件` 匹配已有系统需求详细条目：

| 结果 | 判定 | 动作 |
|------|------|------|
| 复用 | 组件类型和业务职责匹配 | 追加 bpd-eng/CU 来源，标记 `[确认]` |
| 扩展 | 组件类型一致，但业务或 pa-eng 收敛项扩展 | 全量重写该组件条目，保留历史引用，输出变更声明 |
| 新建 | 无可匹配的组件 | 新建系统需求详细条目 |
| 冲突 | 组件职责冲突或跨区域复用边界不清 | 标记 `[需裁决]` |

#### 3.2.5 Phase B（修订）与 Phase C（新建）的提取差异

| 维度 | Phase B（修订） | Phase C（新建） |
|------|----------------|-----------------|
| 判定范围 | 系统需求详细已承接全部 bpd-eng/CU + 本轮新 bpd-eng/CU | 待聚合池本组 bpd-eng/CU |
| 组件清单基线 | 已有清单为基线，仅扫描新功能表单是否引入新组件 | 从零构建 |
| 组件候选 | 增量生成（仅新功能表单/新区域触发） | 全量生成（§3.2.1） |
| 宿主匹配 | 执行匹配检查（§3.2.4），可能检出复用/扩展/新建/冲突 | 无需匹配，直接新建 |
| 组件业务 | 基线基础上增量合并（关系检测见 §3.3.2） | 全量定义 |
| 变更传播 | 输出变更影响声明（§4.2） | 写"首版，无变更" |

---

### 3.3 组件业务定义与 pa-eng 收敛项标注

**一句话**：本节讲组件业务定义的规则——组件业务要素（3.3.1）、Phase B 系统需求详细关系检测（3.3.2）。Phase B（修订已有系统需求详细）与 Phase C（新建系统需求详细）共用以下规则。

#### 3.3.1 组件业务要素

对每个布局组件/构件，按 §2.1.1 的条目要素定义组件业务（功能级不写实现）：

| 要素 | 定义口径 | 描述方式 |
|------|---------|---------|
| 组件名称 / 所属功能表单 / 所在区域 | 组件的定位 | 名称 + 所属功能表单 + 区域（TAB/弹窗入口） |
| 布局组件类型 / 构件类型 | 组件种类 | 布局组件（七种之一）或构件（展示/交互/数据/反馈） |
| 组件业务 | 组件做什么（功能级） | 承载的操作活动清单（承接 03）、数据来源、交互行为、状态反馈——**不写实现** |
| pa-eng 层收敛项 | 留待 wft05 判断的问题 | 组件边界/接口/数据/运行/NFR 标注（§2.3） |

**组件业务定义模板**：

> ### 组件：{组件名称}（{布局组件/构件类型}）· 所属 {功能表单} · 区域 {区域}
> **组件业务**：
> - 承载操作活动：{操作活动清单，承接 03 标注入口}
> - 数据来源：{展示/编辑哪些数据}
> - 交互行为：{用户如何交互}
> - 状态反馈：{结果/错误如何反馈}
> **pa-eng 层收敛项**：{`pa-eng层收敛-*`，无则"无"}

#### 3.3.2 Phase B 系统需求详细关系检测

宿主匹配通过后，将本轮生成的组件与系统需求详细已有清单比对。比对锚点为**组件类型 + 组件业务匹配**。

| 关系 | 判定 | 处理 |
|------|------|------|
| 确认 | 组件匹配 + 类型一致 + 来源升级（新 CU→已有组件） | 追加 bpd-eng/CU 来源 |
| 无新增 | 组件匹配 + 类型一致 + 所有来源已确认 | 增量合并新 CU 的细节（如有） |
| 组件业务扩展 | 组件匹配 + 类型一致 + 业务/pa-eng 收敛项边界扩展 | 全量重写，保留历史引用 |
| 补充 | 组件不匹配 | 新增系统需求详细条目 |
| 过时 | 基线已有组件**未被**本轮功能表单覆盖 | 保留在清单中，标注`过时`，由人类裁决移除 |

> 关系检测前先基于**全部功能表单**重新判定组件类型——新功能表单可能改变组件职责。`[调整]` 是与关系类型正交的叠加标记，用于组件业务/pa-eng 收敛项有变化时。

---

### 3.4 完备性判据

| 检查项 | 判据 | 缺口处理 |
|--------|------|----------|
| 区域覆盖 | 系统需求概要的每个区域（TAB/弹窗入口）均有组件承接 | 补充组件或退回 wft03-eng |
| 操作活动覆盖 | 操作活动清单的每个活动均有组件承接（组件业务承载） | 补充组件或退回 wft03-eng |
| 布局组件覆盖 | 区域内布局组件组合已判定，四类构件均已检查（无需求写明"不适用"） | 补充分类检查 |
| 组件业务完整 | 组件业务含承载操作活动/数据来源/交互行为/状态反馈（功能级不写实现） | 补充业务要素 |
| pa-eng 收敛标注 | 组件、接口、存储、运行机制、NFR 待定项均标注（无则写"无"） | 补充收敛项，不推进到可消费 |
| 追溯覆盖 | 组件 → 功能表单 → 配置信息组 → CU → bpd-eng → bph-eng 追溯完整 | 补充追溯引用 |
| 共享不重复 | 共享组件的系统需求详细只在主责链下定义；消费场景仅保留引用 | 删除重复组件，补共享引用 |
| 推断依据标注（缺口闭环） | 组件/pa-eng 收敛项的设计推断均标注 `[推断]` 及依据，无静默补判；承接 03 的承载操作活动不属推断 | 补标注依据；依据不足退回上游 |
| 25 关系清晰（资产对齐） | 25 中系统需求详细引用已有/待确认/缺失关系已标注 | 标注新类型设计记录或阶段性标记 |

---

## 四、srd-eng 详细节点演进：生命周期与变更传播

**一句话**：本章讲系统需求详细节点设计与确认后的演进机制——生命周期（4.1，节点状态四态主线 + 上游变更分流 + 本 Skill 写回范围）与变更传播（4.2，修订后写变更影响声明告诉下游 wft05-eng）。

### 4.1 系统需求详细节点生命周期

系统需求详细节点沿 [91 规范 §A.6](91-eos-biz-eng-spec.md#a6-节点生命周期模型) 的四态主线流转。状态名映射按 §A.6 通用六态自声明（见下方主线流转与写回范围）。**03/04 文档承载 srd-eng，状态机接力**：03 写功能表单概要→`可以详细定义`、04 写组件业务→`已详细定义`。并行推进规则（SR 线与 pa-eng 线互不阻塞）同 §A.6。

**主线流转**（wft03-eng → wft04-eng → wft05-eng）：

```
可以详细定义 ──wft04锁定──→ 在详细定义 ──本Skill完成──→ 已详细定义 ──wft05锁定──→ 在pa-eng设计 ──wft05完成──→ 已pa-eng架构
   （wft03 写入）                 │                     （wft05 写入）
                                  │
                                  └─ 内部：产出系统需求详细方案 → 待确认方案 ──人类确认──→（确认后 srd-eng 推进至 已详细定义）
```

下游退回：已pa-eng架构/已详细定义 ──wft05退回──→ 需wft04修订 ──本Skill修订+人类确认──→ 已详细定义

> 注：`可以详细定义` 状态由 wft03-eng 写入（03 写功能表单概要），本 Skill 承接后**锁定为 `在详细定义`**（消费展开期间），完成组件业务并人类确认后推进为 `已详细定义`（04 写组件业务）。

**上游变更**（新系统需求概要汇入触发系统需求详细修订）：

```
可以详细定义 ──新系统需求概要修订──→ 可以详细定义（版本递增，本 Skill 未消费）
在详细定义   ──新系统需求概要修订──→ 在详细定义（版本递增，本 Skill 增量处理）
已详细定义   ──新系统需求概要修订──→ 待补充详细定义 ──本 Skill 补全──→ 已详细定义
```

**本 Skill 写回范围**：`待确认方案` / `在详细定义` / `已详细定义` / `待补充详细定义` / `需wft04修订`。`可以详细定义` 由 wft03-eng 写入。

**下游退回处理**：读取 wft05-eng 缺失说明 → 按人类改进方案修订（走 Phase B）→ 人类确认 → srd-eng → `已详细定义`。根因在系统需求概要时退回 wft03-eng。

### 4.2 系统需求详细变更传播协议

通用定义（变更类型分类、判定优先级、声明模板、增量清单与变更影响声明的分工）见 [91 规范 §A.7](91-eos-biz-eng-spec.md#a7-变更传播协议)。

**本 Skill 变更影响声明格式**（每次修订后写入 srd-eng 节点末尾的 `06-*.md` 节点块内）：

```text
### 变更影响声明

srd-eng 详细版本：v{N}（本次修订前为 v{N-1}）
本轮变更类型：无变更 / 增量型 / 修订型 / 结构型

变更项：
- [新增] / [变更] / [确认] / [需裁决] ...
  说明字段中记录细分原因：组件新增 / 布局组件变更 / 构件变更 / 组件业务扩展 / 过时 / 冲突等

下游已消费此系统需求详细的 pa-eng 节点：
  <pa-eng节点ID列表>（如无则写"尚未被下游消费"）

对已消费 pa-eng 节点的影响：
  受影响节点：<pa-eng节点ID + 受影响原因>
  不受影响节点：<pa-eng节点ID>
  待新增 pa-eng 节点：<新组件对应的待展开组件说明>

建议 wft05-eng 动作：
  - 无变更：无需动作
  - 增量型：增量设计新增组件的 pa-eng，已有 pa-eng 不动
  - 修订型：修订受影响 pa-eng 节点，标记 [来自上游变更]
  - 结构型：输出影响范围，等人类裁决后处理；组件拆分/合并时 wft05-eng 需新建或重组 pa-eng 组件
```

---

## 五、操作流程

**一句话**：一次运行 = Step Start 入口路由 → Step 1 材料加载 → Step 2 反馈处理 → Step 3 系统需求详细设计 → Step 4 组件业务定义与 pa-eng 收敛项 → Step 5 覆盖自查与变更声明 → Step 6 资产写回 → Step End 输出与对话反馈。反馈走自然语言对话（§5.3），跨轮余留由 AI 标记持久化。

### Step 布局总览

| Step | 名称 | 职责 | 人类参与 | 执行模式 | 产出 | 写资产 |
|------|------|------|---------|---------|------|--------|
| Start | 前置校验与路由 | 变更感知 + 入口检测、输入校验、退回优先路由 | 无（AI 自动） | 统一 | 就绪 / 结束 | — |
| 1 | 设计材料加载 | 安全复查 + 读 AI 可以处理节点 + 加载系统需求概要 srh-eng/资产 | 无（AI 自动） | 统一 | srd-eng / 资产全景 | — |
| 2 | 方案反馈处理 | 读"待反馈处理"分节 + 对话处理人类修改意见 | **有**——对话反馈修改意见 / 整体确认 | 对话 | 调整后的系统需求详细 srd-eng | `06` |
| 3 | 系统需求详细设计 | Phase A 路由 / B 修订 / C 新建 + 布局组件判定/构件识别 | **有**——设计决策审核（布局组件/构件），对话反馈 | 统一（AI 产出带判据理由）+ 对话 | 区域内组件清单 + 布局组件 + 构件 | `06` |
| 4 | 组件业务定义与 pa-eng 收敛项 | 组件业务定义 + pa-eng 层收敛项标注 | **有**——设计决策审核（组件业务/pa-eng 收敛项），对话反馈 | 统一（AI 产出带判据理由）+ 对话 | 组件业务 + pa-eng 收敛项 | `06` |
| 5 | 覆盖自查与变更声明 | 覆盖自查 + 缺口处理 + 变更影响声明 | 无（AI 自动） | 统一 | 自查结论 + 变更影响声明 | `06` |
| 6 | 资产写回与落账 | 06 写回 + 25 系统需求详细引用 + 推进 srd-eng 状态 + 提交基线 | 无（AI 自动，写回已确认节点） | 统一 | 06/25 写回 + 状态推进 | `06/25` |
| Step End | 运行结束输出 | 三区摘要输出 + 等待人类反馈 | **有**——查看摘要、对话反馈 | 对话 | 人类下一步操作提示 | — |

> **执行模式白话**：统一 = AI 自动跑完这步，不需你介入；对话 = AI 输出后需要你说句话（反馈或确认）才继续。Step 1/5/6 为运行内自动步骤——其中 **Step 3/4 是设计步骤（人机协同交互密度最高处）**：AI 承接系统需求概要，基于对配置需求与已有资产的语义理解产出组件业务方案并附判据理由，人类在 Step End/Step 2 对组件设计决策（布局组件/构件/组件业务/pa-eng 收敛项）审核、思考、决策、反馈，AI 落实；Step 2 / Step End 为人机交互步骤（对话反馈，见 §5.3）。

**反馈机制一句话**：反馈走**自然语言对话**（AI 产出方案后不结束对话，§5.3）；跨轮余留反馈由 AI 标 `[修改]`/`[驳回]` 入"待反馈处理"分节（§5.3 状态机）；人类线下修订经 Step Start 变更感知（git diff，§5.1 变更感知）检出后纳入"待反馈处理"分节；AI 每轮写回提交基线（§5.7），保证下一轮 Step Start 的 `git diff <HEAD @上次 AI 运行>..HEAD` 只含人类变更。

---

### 5.1 Step Start · 前置校验与路由

> 入口条件与优先级判定遵循 [91 规范 §A.4](91-eos-biz-eng-spec.md#a4-入口条件与优先级step-start)。

**变更感知（检出人类线下修订，先于入口判定）**

读取目标文件前先检测自上次 AI 运行以来人类是否修改过文件（[§11.10](91-eos-biz-eng-spec.md#1110-ai-反馈检测规则)）：

1. 读取文件头 `**HEAD @上次 AI 运行**` 字段，获取上次提交 hash H
2. Bash("git diff H..HEAD -- 目标文件") → 返回 H 到当前 HEAD 之间的全部变更
3. 无变更 → 进入入口判定
   有变更 → 解析变更行号，读取变更行附近的节点块（上下 5 行）
   → 判定变更类型：结构化标注（`[同意]`/`[修改]`/`[驳回]`）→ 纳入待反馈；非标自由文本 → 标记 `[需确认]`；无关修改（格式/排版）→ 忽略
   → 将存在反馈的节点加入AI可以处理节点"待反馈处理"分节

> 变更感知必须先于入口判定——人类纯线下修订（不选新系统需求概要）时，只有 git diff 能发现该反馈并将其纳入"待反馈处理"分节，入口判定②才命中。若入口判定先于变更感知，纯线下修订会被误判"无待处理对象"退出。

#### 5.1.1 入口检测

读取产品数据中的 `## AI可以处理节点` 分节（[91 规范 §11.7](91-eos-biz-eng-spec.md#117-ai可以处理节点)），检测三类入口：

| # | 条件 | 检测方式 |
|---|------|---------|
| ① | **有新输入** | 「待 wft04-eng 处理」分节非空 |
| ② | **有待处理反馈** | 「待反馈处理」分节含本 Skill 产出节点 |
| ③ | **有下游退回** | 「待下游退回处理」分节含 `需wft04修订` 的节点 |

三类均不满足 → 输出"无待处理对象" → **退出**。

**退回门禁**（条件③时）：退回节点缺失说明中是否含人类改进方案（`[修改]`/`[同意]` 或下游 Skill 的具体指引）。已提供 → 通过。未提供 → 输出缺失说明，提示提供改进方案 → **退出**。

#### 5.1.2 输入校验

入口检测通过后进行校验。逻辑上不可能的条件为**硬退出**，数量/归属模糊条件为**软提示**：

| 校验条件 | 判定 | 动作 |
|---------|------|------|
| srh-eng 不是 eng 路径系统需求概要节点 | 类型不匹配（硬退出） | 输出类型分布，提示走 `wft03-biz` 或 nfr 路径 → **退出** |
| srh-eng 状态不是 `可以详细定义`/`待补充详细定义` | 状态不符（硬退出） | 输出不合格节点及当前状态 → **退出** |
| 系统需求概要缺少任一项（功能表单清单/操作活动清单/页面编排概要） | 要素缺失（硬退出） | srh-eng → `需wft03修订`，输出缺失说明 → **退出** |
| 选中 srh-eng >5 个 | 超建议阈值（软提示） | 输出超限提示 → 人类确认后继续或退出 |

> 软提示不强制退出，由人类选择继续或退出；硬退出为逻辑上不可能的条件，输出后退出（§A.4.5）。

**锁定写回**：输入校验通过后，将承接的 srh-eng 状态写为 `在详细定义`（锁定态，本 Skill 消费展开期间，对齐 91 §A.6 锁定态=下游写入 与 wft03 §4.1）；完成组件业务且人类确认后推进 `已详细定义`（§5.7）。

#### 5.1.3 分流处理

按优先级顺序执行（源于 [91 规范 §A.4.4](91-eos-biz-eng-spec.md#a44-规则三优先执行顺序)）：

**① 退回优先**：存在 `需wft04修订` 且门禁通过 → 读取缺失说明，按人类方案走 Phase B（§5.4.2），确认后推进为 `已详细定义`

**② 反馈处理**：存在 `待确认方案` + 未处理反馈 → 进入 Step 2（§5.3）。若全部条目已 `[同意]`+`[已处理]`，不阻塞新系统需求概要，Step End 时提示

**③ 新系统需求概要路由**：有未处理系统需求概要 → 进入 Step 1 → Step 3，逐节点执行 Phase A 宿主匹配（§5.4.1）

> **顺序约束**：Step 2（反馈）必须早于 Step 3（新增设计），因为反馈可能改变系统需求详细的组件清单/布局组件/构件/组件业务/pa-eng 收敛项。Phase A 匹配须基于 Step 2 修改后的最新 srd-eng 内容。

---

### 5.2 Step 1 · 设计材料加载

> 遵循 [91 规范 §A.3.3](91-eos-biz-eng-spec.md#a33-产品数据文档与资产文档读取与写回约定) 的统一读取协议。变更感知已在 Step Start 完成（§5.1）。

**步骤一：安全复查**

Step Start 已检出人类反馈并纳入"待反馈处理"分节（§5.1 变更感知）。此处按 §A.3.3 R0a 做加载前安全检查：`detect-changes.sh` 检出人类非预期修改时标记 `[需确认]`，不自动推进。

**步骤二：优先读取**（通过 `## AI可以处理节点` 定位本轮入口）

入口判定通过后，从 AI可以处理节点对应分节获取节点 ID 列表：
- 有新输入 → 读「待 wft04-eng 处理」
- 有反馈 → 读「待反馈处理」
- 有退回 → 读「待下游退回处理」

逐节点 Bash(`grep -n '@{节点ID}' 目标文件`) → 定位行号 → Read 节点块。

**步骤三：索引导航**（AI可以处理节点无待处理时，或按域浏览时）

1. Bash(`grep -n '索引行数：' 目标文件`) → 定位索引行号 L，提取 N
2. Read 业务域索引 → 按锚点 Read 域内索引 → 获取节点 ID 列表
3. 按需 Bash(`grep -n '@{节点ID}' 目标文件`) → 定位行号 → Read 节点

**加载清单**（按需扩大——仅在索引缺失、候选冲突或追溯断裂时扩大）：

| # | 文件 | 用途 |
|---|------|------|
| 1 | `05-*.md` — srh-eng 节点块 + AI可以处理节点 | 定位本轮系统需求概要 srh-eng 正文块（含功能表单清单/操作活动清单/页面编排概要） |
| 2 | `04-*.md` — bpd-eng 节点块 | 追溯来源 CU/bpd-eng |
| 3 | `23-eos-output-architecture.md` | 按 A1 产品路径筛选参考 |
| 4 | `25-eos-engine-models.md` | 按目标引擎/CU 筛选（只读） |
| 5 | `91-eos-biz-eng-spec.md` | 布局组件、构件、eng 路径分工 |

**按需扩大规则**（[91 规范 §11.12](91-eos-biz-eng-spec.md#1112-按需读取规则)）：
- srd-eng 正文块：按 ID 从索引定位
- 候选系统需求详细块：按同引擎/同 CU/同功能表单筛选，用于复用/扩展匹配
- 反馈重入：仅读取条目的确认状态字段及反馈指向块，不做全量重读
- 扩大触发条件：索引缺失 / 候选冲突 / 资产全貌不足 / 关系断裂

---

### 5.3 Step 2 · 方案反馈处理

**一句话**：处理人类反馈——对话内即时处理（修改 / 整体确认，§5.3.1），跨轮余留按 `[修改]`/`[驳回]` 标记处理（§5.3 状态机）。

人类反馈的通用协议见 [91 规范 §A.5](91-eos-biz-eng-spec.md#a5-反馈交互协议)（反馈通道双轨：对话 + 线下修订）。以下为本 Skill 的特化规则。

**反馈双轨**——人类可任选两种方式反馈，本 Skill 均支持：

- **AI 对话反馈**：Step End 后不结束对话，直接在对话中提出修改意见（自然语言）——AI 按 §5.3.1 即时处理（主通道）
- **线下文档修订**：直接打开 `06-*.md` 修订——在确认状态字段标注 `[同意]`/`[修改]`/`[驳回]`，或直接编辑方案内容（自由文本）——经 Step Start 变更感知（git diff，§5.1 变更感知）检出后按 §A.5 处理

两轨等价：对话反馈由 AI 即时落账（追 `[已处理]`），线下修订经 git diff 检出后同样落账；AI 每轮写回提交基线（§5.7），保证下一轮 diff 只含人类变更。

**本 Skill 的操作条目**：布局组件 + 构件 + 组件业务 + pa-eng 收敛项。各条目的 `确认状态` 字段写入 `06-*.md` srd-eng 节点块中。

**确认状态标记状态机**（本 Skill 反馈落账标记，定义单源；对话反馈写入者=AI，线下标注写入者=人类——经 git diff 检出后按 §A.5 处理）：

| 标记 | 含义 | 写入时机 | 转迁 |
|------|------|---------|------|
| `(空)` | 条目待人类反馈 | Step 3/4 产出时初始化 | 人类反馈后按情况转下 |
| `[修改]` | 人类提出修改意见、当轮未落地 | AI 在运行结束仍有余留修改意见时写入（跨轮持久化） | 下轮 Step 2 处理完追 `[已处理]` |
| `[驳回]` | 人类否决该条目方案、当轮未落地 | 同上 | 同上 |
| `[同意]` | 人类确认该条目 | "整体确认"时 | 随 `[已处理]` 一并追加 |
| `[已处理]` | 反馈已处理完毕（git 可追溯） | 处理完成后 | 默认跳过；满足清除条件（§5.3.1）时回到待处理 |

> 当轮对话内的反馈由 AI 即时处理（§5.3.1），不经 `[修改]`/`[驳回]` 态；`[修改]`/`[驳回]` 只在运行结束仍有余留反馈时出现，供下一轮 Step Start 检出（§5.1.1 入口②）。

**跨轮反馈持久化**：人类反馈（对话为主，§5.3.1）由 AI 当轮处理并追 `[已处理]`；若当轮对话结束仍有未落地的修改/驳回意见，AI 将对应条目标记 `[修改]`/`[驳回]` 并加入"待反馈处理"分节 → 下一轮 Step Start 检出（§5.1 变更感知）、Step 2 处理。AI 每轮写回后提交基线（§5.7），保证下一轮 Step Start 的 `git diff <HEAD @上次 AI 运行>..HEAD` 只含人类变更。

#### 5.3.1 AI 处理流程

按统一流程处理人类反馈：扫描待处理条目 → 收集整体确认 → 逐条处理 `[同意]`/`[修改]`/`[驳回]` → 级联检查（需求优先、资产对齐随后）→ 判定节点状态 → 确定写回时机 → 落反馈处理记录。

> **原则**：反馈处理不是对人类标注的机械执行，而是以系统需求概要为锚点的方案修订。先理解人类反馈在系统需求概要语境中的含义，执行修订后检查系统需求详细是否仍满足系统需求概要的需求。

按 [91 规范 §A.5.3](91-eos-biz-eng-spec.md#a53-ai-处理流程) 的流程处理人类自然语言反馈：

| 人类输入 | AI 动作 |
|---------|--------|
| 显式修改指令（"X 改为 Y"） | 定位条目 → 理解修改意图 → 执行修改 → 检查是否仍满足系统需求概要 → 追加 `[已处理]` → 输出修改摘要 → 继续等待 |
| 质疑/讨论（"为什么选 X？"） | 解释理由，不自动修改，继续等待决策 |
| 整体确认（"整体确认"/"可以"） | 全部条目追加 `[同意] [已处理]` → 总结反馈要点写入 `## AI最近变更` → 推进 srd-eng 至 `已详细定义` → **提交基线并更新 HEAD（机制见 §5.7，防下轮把 AI 自写当人类变更）** |
| 模糊意见（"粒度太粗"） | 追问具体化，引导可执行指令 |
| 线下修订检出（git diff 发现确认状态标注/自由文本编辑） | 定位条目 → Edit 修订 → 追加 `[已处理]` → 输出修改摘要 → 继续等待 |

**特化参数**：

| 参数 | 本 Skill 值 |
|------|-------------|
| **锚点需求** | 系统需求概要 srh-eng——不脱离系统需求概要自行修订 |
| **推进目标** | 全部条目含 `[同意]`+`[已处理]` → `已详细定义`。存在 `[修改]`/`[驳回]` 无 `[需重设计]` → 保持 `待确认方案`。存在 `[需重设计]` → 保持 `待确认方案`，需重做 Step 3~5 |
| **清除条件** | 已有 `[同意]`+`[已处理]` 的条目默认跳过。仅在新输入满足以下任一时，清除 `[已处理]`（保留 `[同意]`）回到待处理：<br>• 新输入是条目的直接上游节点（如新系统需求概要是系统需求详细来源）<br>• 新输入与条目同属一个架构层级且边界重叠（如新组件改变已有组件的类型或业务）<br>• 新输入导致条目被修订（变更影响声明标注修订型或结构型） |
| **级联触发条件** | 组件清单/布局组件/构件变更；组件业务新增/删除/类型变更；pa-eng 收敛项边界变化 → 触发需求级联检查 → 通过后触发资产对齐级联 |
| **资产对齐级联** | 方案变更影响 25 资产时更新对齐结论——系统需求详细变更 → 更新 25 系统需求详细引用（由 Step 6 维护） |

#### 5.3.2 确认状态格式示例

**AI 写入（Step 3~5 完成时）**——为每个条目初始化，状态为空：

```text
- 布局组件：节点配置区-列表（列表页面）+辅助栏 | 确认状态: (空)
- 构件：新建节点按钮（交互类）、节点状态标签（反馈类） | 确认状态: (空)
- 组件业务：新建节点按钮（承接操作活动·配置维护，节点配置区） | 确认状态: (空)
- pa-eng 层收敛项：节点元数据存储归属 [pa-eng层收敛-数据] | 确认状态: (空)
```

**人类标注**——三种形式：

```text
- 布局组件：节点配置区-列表（...） | 确认状态: [同意]
- 构件：新建节点按钮（...） | 确认状态: [修改]：应补充"批量删除"构件
- 组件业务：新建节点按钮（...） | 确认状态: [驳回]：业务应补充状态反馈
```

**AI 处理完成后**——追加 `[已处理]`：

```text
- 布局组件：节点配置区-列表（...） | 确认状态: [同意] [已处理]
- 构件：新建节点按钮（...） | 确认状态: [修改]：应补充"批量删除"构件 [已处理]
```

#### 5.3.3 反馈总结

人类「整体确认」后，AI 将本轮反馈要点总结为一句话，通过 `update-meta.sh add-recent-change` 写入 `06-*.md` 的 `## AI最近变更` 表：

```bash
bash ../scripts/update-meta.sh ../../80-pl4eos-2-eosdata/06-eos-system-requirement-detailed.md add-recent-change "wft04-eng" "反馈处理" "<srd-eng-ID>" "<AI总结的反馈要点>"
```

摘要示例：`构件调整：节点配置区补"批量删除"构件（交互类）；组件业务补状态反馈`

---

### 5.4 Step 3 · 系统需求详细设计

#### 5.4.1 Phase A：组件候选生成与宿主匹配

对每个功能表单的每个区域，按页面编排概要与操作活动清单生成组件候选（规则见 §3.2.1），然后逐组件做宿主匹配：

| 条件 | 路由 |
|------|------|
| 组件类型 + 组件业务落入已有系统需求详细条目 | 归入该 srd-eng → **Phase B** |
| 不满足 | 进入待聚合池 → **Phase C** |
| 组件职责冲突或跨区域复用边界不清 | 标记 `[需裁决]` |

待聚合池按 `{引擎, CU, 功能表单, 区域, 组件候选列表}` 分组。此阶段仅粗略分组，布局组件判定和完整要素提取在 Phase C 执行。

#### 5.4.2 Phase B：修订已有系统需求详细

1. **要素提取**：从本轮系统需求概要提取组件类型、布局组件线索和组件业务候选（§3.2.1、§3.3.1）。已有组件业务仅追加不重建
2. **宿主匹配检查**：确认系统需求概要仍在系统需求详细职责范围内（§3.2.4）。复用 → 追加来源；扩展 → 全量重写保留历史；冲突 → `[需裁决]`。Phase A 是候选筛选，Phase B 是精确匹配——职责扩展时不得回退 Phase A 重选宿主
3. **重判定与关系检测**：基于全部功能表单重判组件类型与布局组件（§3.2.2、§3.2.3），比对基线判定五类关系（§3.3.2）。新增/变更组件业务 → 定义要素；pa-eng 收敛项 → 写入标记
4. **变更影响声明**：判定变更类型（无变更/增量型/修订型/结构型），写入 srd-eng 节点末尾（§4.2）

#### 5.4.3 Phase C：新建系统需求详细

1. **判定布局组件** → 在页面编排概要基础上独立判断区域需要的布局组件组合（§3.2.2）
2. **识别构件** → 按展示/交互/数据/反馈四类独立识别（§3.2.3）
3. **定义组件业务** → 逐组件定义承载操作活动/数据来源/交互行为/状态反馈（§3.3.1，功能级不写实现）
4. **标注 pa-eng 层收敛项** → 组件边界/接口/存储/运行机制/NFR 均标注 `pa-eng层收敛-*`（§2.3）
5. **组装系统需求详细** → 写入 `06-*.md`：组件名称/所属功能表单/所在区域/布局组件或构件类型/组件业务/pa-eng 收敛项（完整要素见 §2.1.1）

---

### 5.5 Step 4 · 组件业务定义与 pa-eng 收敛项

> 按每个功能表单或强依赖功能表单组独立执行。新建 → 全量定义；修订 → 仅当系统需求概要变更或组件拓扑变化时重验。

**1. 组件业务定义**

对每个布局组件/构件，按 §3.3.1 的要素定义组件业务（功能级不写实现）：承载操作活动（承接 03 操作活动清单）、数据来源、交互行为、状态反馈。**承接 03 的承载操作活动不标 `[推断]`**（操作活动清单已在 wft03-eng 组织确认，经承接 03 引用即视为已确认）；**数据来源/交互行为/状态反馈等设计推断标 `[推断]`**，人类确认后方可落账。

**2. pa-eng 层收敛项标注**

逐组件检查（五类见 §2.3），存在则写入 `pa-eng层收敛-*` 标记（载件是主要来源——供 wft05 判断由前后端组件承接）：

- 组件边界待定 · 接口契约待定 · 数据存储待定 · 运行机制待定 · NFR 承接待定

**3. 要素机械检查**（逐组件，发现即标注 `[覆盖缺口]`，留待下一轮补全）

- [ ] 组件名称/所属功能表单/所在区域 **已定义**
- [ ] 布局组件类型/构件类型 **已判定**
- [ ] 组件业务（承载操作活动/数据来源/交互行为/状态反馈）**已定义**
- [ ] pa-eng 收敛项 **已标注**（无则写"无"）
- [ ] 所有区域、所有操作活动清单均被组件承接

---

### 5.6 Step 5 · 覆盖自查与变更声明

> 按每个 srd-eng 节点独立执行。新建 → 全量自查；修订 → 仅当组件清单或组件业务变更时重验。

**1. 覆盖自查**

按九项逐条检查（判据见 §3.4），发现遗漏标注 `[覆盖缺口]`，不回退：

- [ ] 区域覆盖 · 操作活动覆盖 · 布局组件覆盖
- [ ] 组件业务完整 · pa-eng 收敛标注 · 追溯覆盖
- [ ] 共享不重复 · 推断依据标注 · 25 关系清晰

**2. 缺口处理**

| 缺口类型 | 处理方式 |
|----------|----------|
| 区域或操作活动未覆盖 | 补充组件或退回 wft03-eng |
| 布局组件未判定 | 补充布局组件判定 |
| 组件业务不完整 | 补充业务要素（功能级不写实现） |
| pa-eng 必收敛项缺失 | 补充收敛项，不推进到可消费 |
| — | 不因缺口脑补节点——缺口就是缺口 |

**3. 要素机械检查**

- [ ] 九项覆盖自查已执行，遗漏项已标注
- [ ] 缺口已分流，处理方式明确
- [ ] srd-eng 版本号已递增（修订时），首版为 v1

**4. 变更影响声明**

新建或修订后写入声明（§4.2）。首版写"首版，无变更"。修订时判定变更类型，列出受影响组件、组件业务和下游建议。

---

### 5.7 Step 6 · 资产写回与落账

> 遵循 [91 规范 §11.11](91-eos-biz-eng-spec.md#1111-ai-写回规则) 的统一写回协议。仅写回人类已在 Step 2 确认的系统需求详细条目。不设二次确认。

**写回操作**：

1. `06` 写入或更新系统需求详细节点、状态队列、追溯引用
2. `25` 写入系统需求详细引用或待办，不固化组件设计（pa-eng 组件设计归 wft05-eng）
3. srd-eng → `已详细定义`（Step Start 已锁定为 `在详细定义`）
4. 写入资产写回记录（日期/来源 srd-eng/目标系统需求详细/动作/消费版本/下游状态）

**同步维护**：

| 操作 | 同步更新 |
|------|---------|
| 新增/修改节点 | 更新域产品索引 `节点数`；更新 `## AI可以处理节点` 对应分节 |
| 修改节点状态 | 更新 AI可以处理节点分节——状态符合消费条件时加入，不符合时移出 |
| 新增/删除子条目 | 更新父节点 `**子条目状态**` 汇总值 |
| 人类标注处理 | 更新父节点 `**子条目状态**` 和 `**确认状态**`；AI可以处理节点同步 |

**提交基线**（链级不变式，每次写回后必须执行；顺序对齐 wft02-eng §5.6）：

1. 在 `## AI最近变更` 表追加记录（[91 规范 §11.6](91-eos-biz-eng-spec.md#116-ai最近变更)），含时间/操作 Skill/操作类型/节点ID/变更摘要，操作类型列含 `[→](#节点ID)` 超链接
2. 保留 AI最近变更最近 30 条，超出时从最旧行滚动删除
3. `git add -A` + `git commit`（带 Co-Authored-By: Claude），把本轮 AI 产出（系统需求详细节点、25 写回、AI 可以处理节点维护、AI 最近变更）全部落基线——链级不变式：基线 = 最后 AI 提交，下一轮 Step Start 的 `git diff <HEAD @上次 AI 运行>..HEAD` 只含人类变更。反馈确认后的补充提交（§5.3.1 整体确认）同此机制
4. 更新文件头 `**文档版本**`（递增次版本号）、`**修订日期**`、`**HEAD @上次 AI 运行**`（=刚提交的基线 hash）

> **写回要求**：幂等去重、局部写回、全貌同步。所有操作在 Step 内完成，不允许"本次写节点、下次更新索引"。资产写回是独立收尾，不构成 wft05-eng 前置条件。

---

### 5.8 Step End · 运行结束输出

> 遵循 [91 规范 §A.3.2](91-eos-biz-eng-spec.md#a32-step-end-输出模板三区结构) 的三区输出模板。

本 Skill 实例化如下：

```text
当前状态：<待确认方案 / 已详细定义>
  本轮系统需求详细：<srd-eng-ID / 新建或修订 / 版本变化 / 来源系统需求概要>
资产落账：<未落账 / 已写回 06/25>

一、方案反馈
  直接回复修改意见即可，例如：
    - "节点配置区补一个'批量删除'构件"
    - "整体确认"（快捷同意全部未标注条目）
    - "组件业务补状态反馈段"
  审核锚点：布局组件/构件判定是否合理、组件业务定义是否完整（功能级不写实现）、pa-eng 收敛项标注是否到位
  可用性锚点：系统需求详细是否足以指导 wft05-eng 设计前后端组件？追溯链（组件→功能表单→配置信息组→CU→bpd-eng→bph-eng）是否可追踪？系统需求详细是否可理解？

二、下一步
  本Skill → 选择系统需求概要 srh-eng 节点（`可以详细定义` / `待补充详细定义`）或 `需wft04修订` 的 srd-eng 节点重新运行
  后续    → srd-eng-XXX（已详细定义）、srd-eng-YYY（待补充详细定义）→ wft05-eng
```

**输出后不要结束对话**，等待人类反馈——人类可直接用自然语言回复修改意见（§5.3.1）：

> 以上是本轮方案。直接回复修改意见即可：
> - "节点配置区补一个'批量删除'构件"
> - "整体确认"
> - "组件业务补状态反馈段"

---

## 六、AI 标记约定

四类增量标记（`[新增]`/`[变更]`/`[确认]`/`[需裁决]`）的定义及使用规则见 [91 规范 §A.3.1](91-eos-biz-eng-spec.md#a31-ai-增量标记)。

每次 Step 3 完成后，向人类输出增量清单，格式示例如下：

```text
=== srd-eng-ENG-FLOW-PAGE-001 系统需求详细 本轮更新清单 ===

[新增] 承接系统需求概要 srh-eng-ENG-FLOW-PAGE-001（03 产出）

区域内组件
  [新增] 布局组件：节点配置区-列表（列表页面）+辅助栏
  [新增] 构件：新建节点按钮（交互类）、节点状态标签（反馈类）
    组件业务：新建节点按钮（承接操作活动·配置维护，数据来源=FlowNodeConfig，交互=点击弹窗，反馈=校验提示）
    pa-eng 层收敛项：节点元数据存储 [pa-eng层收敛-数据]、发布装载接口 [pa-eng层收敛-接口]

完备性检查
  区域覆盖 ✓ / 操作活动覆盖 ✓ / 组件业务完整 ✓ / pa-eng 收敛 ⚠
```

---

## 七、自检清单

**一句话**：每次运行收尾前按此清单逐项自查——AI 按 Step 分组核对是否漏做、漏写、漏提交。

**前置与加载（Step Start / 1）**
- [ ] Step Start 已按 §A.4 完成入口决策（退回优先 → 反馈处理 → 新输入），无待处理对象已退出
- [ ] 输入校验通过：srh-eng 为 eng 路径系统需求概要节点、≤5 个、状态符合（`可以详细定义`/`待补充详细定义`）、系统需求概要完整（功能表单清单/操作活动清单/页面编排概要）
- [ ] Step Start 已变更感知（git diff，基线前提已满足）并判定变更类型，线下修订节点已纳入"待反馈处理"分节
- [ ] 已加载系统需求概要 srh-eng / 23/24 资产全貌 / 25 引擎清单，未默认加载全文件正文

**方案反馈处理（Step 2）**
- [ ] 对话反馈已按 §5.3 处理：修改指令 → 检查仍满足系统需求概要 → 追加 `[已处理]`；整体确认 → 推进 `已详细定义` + 提交基线
- [ ] 反馈双轨（对话 + 线下修订）均落账；跨轮余留反馈已按状态机标记 `[修改]`/`[驳回]` 并加入"待反馈处理"分节

**系统需求详细设计（Step 3）**
- [ ] Phase A 宿主匹配正确（落入已有系统需求详细 → Phase B；否则待聚合池 → Phase C）
- [ ] Phase B 宿主匹配检查已执行（复用 / 扩展 / 新建 / 冲突分流正确）
- [ ] 布局组件 + 构件已判定（基于页面编排概要 + 操作活动清单）
- [ ] 系统需求概要已承接（不重新定义功能表单、不重新定义操作活动清单）
- [ ] 变更影响声明已写入（Phase B）/ "首版，无变更"（Phase C）；增量清单已输出

**组件业务与 pa-eng 收敛（Step 4）**
- [ ] 组件业务已定义（承载操作活动/数据来源/交互行为/状态反馈，功能级不写实现）
- [ ] pa-eng 层收敛项已标注（载件为主要来源，供 wft05 判断由前后端组件承接）；本 Skill 未设计任何 pa-eng 组件

**覆盖自查与变更声明（Step 5）**
- [ ] 九项覆盖自查已执行（§3.4），遗漏项已标注 `[覆盖缺口]`
- [ ] 缺口已分流，处理方式明确；不因缺口脑补节点——缺口就是缺口
- [ ] 变更影响声明已写入（首版"首版，无变更"）

**资产写回与落账（Step 6）**
- [ ] 06 写入/更新系统需求详细节点、25 系统需求详细引用、srd-eng 状态推进（`已详细定义`）
- [ ] AI 最近变更表、AI 可以处理节点分节已维护
- [ ] 提交基线已执行（AI最近变更 → 滚动删除 → git commit → 更新文件头 HEAD=刚提交基线 hash）

**运行结束（Step End）**
- [ ] 三区摘要已输出，对话等待反馈；整体确认后已提交基线

---

## 八、变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-08-25 | v2.2 | 层名全对齐（人类裁决 2026-08-25，随 wft05-eng 深审）——§1.2 交付链条 / §二 概要/详细分工叙述「产品概要定义」→「平台产品架构」（wft05 输出层名，无「定义」后缀）。链级：wft05-eng v10.12 + 91 v5.43 同步。SKILL 无对应文本不 bump。 |
| 2026-08-24 | v1.9 | 标题全称正名（链级联动，人类逐项裁决 2026-08-24，对齐 wft01/wft02/wft03-eng 全称）——「系统需求概要（srh-eng）→ 系统需求详细（srd-eng）· 功能表单组件及其业务」→「**平台能力系统需求概要定义 → 平台能力系统需求详细定义 · 组件业务详细定义**」（镜像后缀模式：wft02「· A1 配置信息组详细定义」/wft03「· 功能表单概要定义」）。 |
| 2026-08-24 | v1.8 | 输出术语正名（链级联动，人类裁决 2026-08-24，对齐 wft01-biz v3.26）——业务概要定义→业务流程概要定义、业务详细定义→业务流程详细定义（成对一致），含缩写随迁；变更记录段保留历史旧词。 |
| 2026-08-24 | v1.7 | 加载清单措辞对齐 wft01-biz（链级联动，人类裁决 2026-08-24，同 wft01-eng v10.5）——§5.2 加载清单第 1/2 项「srd-eng 架构树画像 / bpd-eng 树画像 / 状态队列 / 节点索引」枚举 →「srd-eng 节点块 + AI可以处理节点」「bpd-eng 节点块」；头部版本号补落后（此前记录已到 v1.6）。SKILL 无对应文本不 bump。 |
| 2026-08-24 | v1.6 | 缩写更名（人类定案）——sfh→srh、sfd→srd（sr=系统需求）+ 中文层名统一（系统需求概要/详细定义）；资产名/节点ID/状态名/文件名随迁。 |
| 2026-08-24 | v1.5 | 资产名更名（人类定案）——SR-F→srh-eng（03 系统需求概要）/srd-eng（04 系统需求详细）、PA→pa-eng、STR-E→bph-eng、BP→bpd-eng（节点类型/节点ID/状态名），版本头同步递增 |
| 2026-08-24 | v1.3 | 文件名更名（人类定名 2026-08-24）——`eos-wft04-eng-sr2sr.md` → `eos-wft04-eng-srh2srd.md`（系统需求概要定义→系统需求详细定义，eng 链 04 层）；人类方案ID 与全库引用同步。 |
| 2026-08-22 | v1.2 | eng 链深审修正（人类逐项裁决 2026-08-22）：①§5.1.2 输入校验后补「锁定写回」段落（SR-F→`在详细定义`，对齐 wft02/wft03/wft05 同款——此前仅 §4.1 注描述锁定、SKILL 已实现，主文件为 5 链唯一缺者）；②§5.7 Step 6 写回补「Step Start 已锁定」注；③§4.1 上游变更补 `在详细定义` 锁定态分支（此前仅两分支，缺锁定态增量处理）；④Step 布局表 Step 4 名称改「组件业务定义与 PA 收敛项」（对齐 §5.5 节标题）。 |
| 2026-08-22 | v1.1 | SR-F 锁定态闭环 + 术语统一（人类裁决 E5 方案① + 机械项）：①**§4.1 补认 `在详细定义`**——主线流转改全链 `可以详细定义 ──wft04锁定──→ 在详细定义 ──本Skill完成──→ 已详细定义 ──wft05锁定──→ 在PA设计`（wft03 侧已画、本 Skill 补认；对齐 91 §A.6 锁定态=下游写入，与 `在PA设计` 对称），写回范围补 `在详细定义`，注改写「承接后锁定为在详细定义」；②**PA 层收敛项前缀统一**——§2.3 表/§5.4.3/§5.5/示例的 `PA层待处理-*` 全部改 `PA层收敛-*`（对齐 91 §8.4，消除本 Skill 内部自相矛盾）；③**布局组件定义删「树表」**——补注「树形表格是列表页面的呈现变体」。链级：wft05-eng v10.1（前缀同步）。 |
| 2026-08-21 | v1.0 | **eng 链「概要/详细」分工框架实施第④步（新增 Skill）**——系统需求详细定义（04 层）：承接系统需求概要 SR-F（功能表单清单+结构判定级概要说明+操作活动清单+页面编排概要），判定区域内布局组件/构件、定义组件业务（功能级不写实现）、标注 PA 层收敛项。收纳 wft03-eng v9.0 收缩时下移的内容（布局组件判定 §2.2/§3.2.2、构件识别 §2.2.1 四分类/§3.2.3、PA 层收敛项 §2.3，自 wft03-eng v8.0 迁入）。SR-F 状态机接力（03 写功能表单概要→`可以详细定义`、04 写组件业务→`已详细定义`）。04 组件业务是 wft05-eng 推断前后端组件的依据。文档承载同一 SR-F（05-*.md）。 |
