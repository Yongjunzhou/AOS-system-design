---
name: eos-wft01-biz-spr2bph
description: 规范化的相关方需求→业务流程概要· 业务流程概要（场景业务）设计与资产化。接收同角色 FR-BIZ 规范化的相关方需求（≤10条），
             完成规范化的相关方需求→业务流程概要（场景业务 + 输出文档序列 + Bn 组装：WBS归组/23一致性/缺口推断），
             将 Bn 组装结论写回 23 资产，形成可进入 wft02-biz 的业务流程概要节点。
---

# eos-wft01-biz · 规范化的相关方需求 → 业务流程概要

> **设计依据**：[eos-wft01-biz-spr2bph.md](../eos-wft01-biz-spr2bph.md)（人类方案——权威源）
> **运行时协议**：[91-eos-biz-eng-spec.md](../91-eos-biz-eng-spec.md) 附录A（入口条件/读取协议/反馈交互/Step End输出模板）
> **领域规范**：[91-eos-biz-eng-spec.md](../91-eos-biz-eng-spec.md)（仅在协议文件无法覆盖时查阅）

---

## 一、职责定位

### 负责与不负责

| 负责 | 不负责（路由指向） |
|------|-------------------|
| 接收同角色 FR-BIZ 规范化的相关方需求（≤10条），校验类型/数量/状态 | FR-ENG → `wft01-eng` |
| 按用户角色匹配已有业务流程概要 → 归入（Phase B）或待聚合池（Phase C） | NFR → `wft01-nfr` |
| 定义场景职责边界（= 主责角色的业务对象闭环）+ 业务链文档序列 + 引擎候选 + Bn 组装（WBS 归组/23 一致性/缺口推断） | 下游详细定义（字段级详设）→ `wft02-biz` |
| E2E 闭合检查 + 生成补充规范化需求材料 | 无规范化的相关方需求输入的治理业务流程概要不主动展开 |
| 人类确认后写回 23 资产 + 推进业务流程概要→`可以分解分配` + 规范化的相关方需求→`已分配` | 业务人员确认（人类线下活动） |
| 接收 wft02 退回的 `需wft01修订` 业务流程概要，按人类方案修订 | — |

### 上下游衔接

| 方向 | Skill | 交接内容 |
|------|-------|---------|
| 上游 | `ort03` | FR-BIZ 规范化的相关方需求写入 `01-*.md`，状态=`待处理`。通过 `## AI可以处理节点` 的"待 wft01-biz 处理"分节检测 |
| 下游 | `wft02-biz` | 消费 `可以分解分配`/`待补充分解分配` 的业务流程概要。缺陷时退回 `需wft01修订`+缺失说明 |
| 同级 | `wft01-eng`、`wft01-nfr` | 各自处理 FR-ENG / NFR，本 Skill 输入校验检出后退出并提示路由 |

---

## 二、操作流程

### Step Start · 前置校验与路由

**变更感知**（先于入口检测，检出人类线下修订）：

```bash
bash ../scripts/detect-changes.sh ../../../80-pl4eos-2-eosdata/01-eos-specified-requirements.md
bash ../scripts/detect-changes.sh ../../../80-pl4eos-2-eosdata/02-eos-business-summary-architecture.md
```

检出 `HAS_CHANGES=1` → AI 检查变更行，判定变更类型：结构化标注（`[同意]`/`[修改]`/`[驳回]`）→ 纳入"待反馈处理"分节；自由文本 → 标记 `[需确认]`；格式/排版 → 忽略。**先变更感知再入口判定**——纯线下修订若不先检出，会被误判"无待处理对象"退出（详见 human spec §5.1 变更感知）。

**入口检测**（按 [91 规范 §A.4](../91-eos-biz-eng-spec.md#a4-入口条件与优先级step-start)）：

```bash
# 1. 读取待处理节点
bash ../scripts/read-section.sh ../../../80-pl4eos-2-eosdata/01-eos-specified-requirements.md "AI可以处理节点"
bash ../scripts/read-section.sh ../../../80-pl4eos-2-eosdata/02-eos-business-summary-architecture.md "AI可以处理节点"
```

**输入校验**（按顺序，命中即退出）：

| 条件 | 判定 | 动作 |
|------|------|------|
| 规范化的相关方需求状态=`需退回重切` | 不可处理 | 输出退回记录 → **退出** |
| 规范化的相关方需求状态≠`待处理` | 不可处理 | 输出当前状态 → **退出** |
| 规范化的相关方需求分属多个角色 | 不可处理 | 输出角色分布 → **退出** |
| 规范化的相关方需求类型含 FR-ENG/NFR | 路由错误 | 输出类型分布 + 路由指引 → **退出** |
| 同角色规范化的相关方需求 >10 条 | 软提示 | 输出超限提示 → 人类确认 |
| 无任何待处理 | — | 输出"无待处理对象"→ **退出** |

**退回优先**：存在 `需wft01修订` 业务流程概要 → 检查人类是否提供改进方案（§A.4 退回门禁）。已提供 → 读取缺失说明，走 Phase B 修订。未提供 → 输出缺失说明 → **退出**。

---

### Step 1 · 设计材料加载

**1. 安全复查**（加载前检查；变更感知已在 Step Start 完成）：

```bash
bash ../scripts/detect-changes.sh ../../../80-pl4eos-2-eosdata/01-eos-specified-requirements.md
bash ../scripts/detect-changes.sh ../../../80-pl4eos-2-eosdata/02-eos-business-summary-architecture.md
```

检出非预期修改时标记 `[需确认]`，不自动推进（§A.3.3 R0a）。

**2. 加载本轮规范化的相关方需求条目**：

```bash
# 获取待处理规范化的相关方需求节点 ID 列表
bash ../scripts/read-section.sh <01-file> "待 wft01-biz 处理"

# 逐节点读取正文
bash ../scripts/read-node.sh <01-file> <规范化的相关方需求-ID>
```

**3. 加载已有业务流程概要节点**（用于宿主匹配）：

```bash
# 读取业务概要树画像和节点索引（对应 02 数据文件实际分节名）
bash ../scripts/read-section.sh <02-file> "0. 业务概要树画像"
bash ../scripts/read-section.sh <02-file> "2. 业务概要节点索引 / 分片索引"

# 按需读取候选业务流程概要节点块（同角色/同业务域）
bash ../scripts/read-node.sh <02-file> <业务流程概要-ID>
```

**4. 加载资产全貌**（用于 Bn 组装）：

```bash
# 23 资产：Bn 侧对齐锚点
bash ../scripts/read-section.sh ../../../80-pl4eos-2-eosdata/23-eos-output-architecture.md "业务域索引"

# 25 资产：功能引擎清单（运行时动态读取，不缓存）
bash ../scripts/read-section.sh ../../../80-pl4eos-2-eosdata/25-eos-engine-models.md "引擎全景"
```

**按需扩大原则**：仅在索引无法定位、候选冲突、追溯断裂或人类明确要求时扩大读取范围。禁止默认加载全文件正文。

---

### Step 2 · 方案反馈处理

**触发条件**：Step Start 检出 `需wft01修订` 业务流程概要，或上一轮 Step End 的反馈等待中人类提出修改意见。

**反馈双轨**（人类任选其一，详见人类方案 §5.3）：①AI 对话反馈——Step End 后不结束对话，直接回复修改意见，AI 即时处理；②线下文档修订——打开 `01/02-*.md` 在确认状态字段标注 `[同意]`/`[修改]`/`[驳回]` 或直接编辑方案内容（自由文本），经 Step Start 变更感知 git diff 检出后按 §A.5 处理。两轨等价均落账（追 `[已处理]`）。

**处理规则**：

1. 从当前轮次对话上下文或退回缺失说明中读取人类反馈意图
2. **逐条处理**（按人类反馈内容）：
   - AI 输出方案后人类直接回复修改意见 → 在当前轮次继续对话处理，不重新走 Step Start
   - wft02 退回的 `需wft01修订` → 下一轮 Skill 调用时走 Step Start 退回优先
3. 修改后检查是否仍满足该业务流程概要所承接的规范化的相关方需求
4. 修改导致结构性变更（边界/引擎/PL6-B 增删）→ 按需重入 Step 3
5. 全部条目获人类认可 → 业务流程概要 → `可以分解分配`

**本 Skill 特化参数**（[91 规范 §A.5.4](91-eos-biz-eng-spec.md#a54-其他规则) 要求各 Skill 定义）：

| 特化维度 | wft01-biz 值 |
|---------|-------------|
| 锚点需求 | 当前业务流程概要所承接的全部规范化的相关方需求条目（FR-BIZ）——人类反馈理解和修订均以规范化的相关方需求为最终锚点 |
| 操作条目 | 场景职责边界 + 业务链各 PL5 输出文档 + 按三独立展开的 PL6-B 正文条目 |
| 推进目标 | 人类「整体确认」→ 业务流程概要推进至 `可以分解分配` |
| 清除条件 | 已有 `[已处理]` 条目默认跳过；仅当本轮新输入满足任一条件时清除回待处理：直接上游节点 / 同层边界重叠 / 定义被修订 |
| 级联触发条件 | 修改导致结构性变更（边界/拆分合并/引擎变更/PL6-B 增删）→ 标记受影响条目 `[需重设计]` |
| 资产对齐级联 | 方案变更影响 23B 引用 → 更新受影响资产的 Q 判定和引用路径 |

**确认状态格式**（人类方案 §5.3.2）：AI 在 Step 3 产出时为每个条目初始化空的 `确认状态` 字段：

```text
- 场景职责边界 | 确认状态: (空)
- 采购申请单（业务输出文档，@engine-flow） [23B-Q2] | 确认状态: (空)
- 采购履约看板（看板，@engine-kanban） | 确认状态: (空)
```

人类反馈处理后 AI 追加 `[已处理]`；线下标注由人类写入 `[同意]`/`[修改]`/`[驳回]`、AI 经 git diff 检出后同样追加 `[已处理]`。

6. **元信息维护**：
   ```bash
   bash ../scripts/update-meta.sh <02-file> bump-version
   bash ../scripts/update-meta.sh <02-file> update-head
   ```

---

### Step 3 · 场景业务设计

#### Phase A — 规范化的相关方需求路由

对每条新规范化的相关方需求：
1. 从已加载的规范化的相关方需求节点块（Step 1 通过 `read-node.sh` 获取）中读取 `**所属角色**` 字段——角色已在需求规范化（ort03）时标注
2. 轻量提取：文档名称 + 触发动作（够路由即可）
3. 宿主匹配——规范化的相关方需求的文档名称 + 描述职责落入已有业务流程概要的场景职责边界 → Phase B；否则 → 待聚合池 → Phase C

待聚合池按**触发动作相似 + 文档名称重叠**分组。

#### Phase B — 修订已有业务流程概要

**执行顺序**：
1. **要素提取**——从本轮规范化的相关方需求提取文档名称和支撑引擎
2. **边界匹配检查**——确认规范化的相关方需求在业务流程概要边界内（[§3.2.3](../eos-wft01-biz-spr2bph.md#323-phase-b-边界匹配检查)）（详见 human spec）：
   - 边界内 → 进入关系检测
   - 边界扩展 → 更新边界陈述 + `[推断]`
   - 退回规范化 → 输出拆分建议，规范化的相关方需求→`需退回重切`
   - 宿主误匹配 → 退回待聚合池
3. **业务链关系检测**——比对已有基线（[§3.3.5](../eos-wft01-biz-spr2bph.md#335-phase-b-业务链关系检测)）（详见 human spec）：
   - 确认（文档匹配+引擎相同+来源非规范化的相关方需求明示）→ 来源升级
   - 无新增（文档匹配+来源已为规范化的相关方需求明示）→ 追加引用+增量合并
   - 引擎变更（文档匹配+引擎不同）→ 更新引擎+变更原因
   - 补充（文档不匹配）→ 新增到业务链
   - 过时（基线文档未被本轮覆盖）→ `[过时]`+保留不删
4. **变更影响声明**——判定变更类型（[91 规范 §A.7](../91-eos-biz-eng-spec.md#a7-变更传播协议)），按 [人类方案 §4.2](../eos-wft01-biz-spr2bph.md#42-业务流程概要变更传播协议) 实例化格式写入业务流程概要节点末尾
5. **Bn 组装重跑**——业务链拓扑变化时（新增/过时/引擎变更/边界扩展）按人类方案 §3.3.3 重跑 Bn 组装（WBS 归组 + 23 一致性 + 缺口推断，判据见附录 A.7），更新业务流程概要节点的 Bn 组装结论；结构性缺口入 Step 4 补充规范化的相关方需求回路

#### Phase C — 新建业务流程概要

**执行顺序**：

1. **确定业务域**——从已加载的规范化的相关方需求节点块的 `**所属角色**` 字段获取角色名。若 Step 1 已加载的角色索引中有该角色的业务域信息则直接引用；否则用 `bash ../scripts/read-node.sh ../../../80-pl4eos-2-eosdata/21-eos-stakeholder-roles.md ROLE-<ID>` 加载角色定义块查找。若 21 文件中无该角色或角色定义中无业务域字段，标记 `[推断]` 并基于角色职责语义匹配
2. **定义端到端边界**——识别触发→追踪自然终点→闭合验证→边界检查（[§3.2.2](../eos-wft01-biz-spr2bph.md#322-端到端边界定义)）（详见 human spec）
3. **判定业务流程概要类型**——D 或 PCA（判据见附录 A.1）
4. **逐文档要素提取**：
   - **a. 封面模式**——正文变更是否需要审批/发布/版本追溯？→ 是=正式 / 否=轻量
   - **b. 支撑引擎**——按正文数据类型匹配功能引擎候选，全量 `[推断]`（引擎清单从 25 资产动态读取）
   - **c. 任务类型**——零个或一个（计划/流程/工单）。规范化的相关方需求明示→直接捕获；规范化的相关方需求只提管理模式（如"需要审批"）→ 判定类型 + 标注 `[待wft02推断]`；规范化的相关方需求缺失活动节点描述→推断类型 + 标注 `[待wft02推断]`。**R4 约束**：场景业务中第一个有任务类型的文档，任务类型必然是计划（该文档=**开发/记录类文档**，闭环从它启动；PCA 场景另有计划清单文档，两者不同实例，[§2.2.1](../eos-wft01-biz-spr2bph.md#221-定义与生成方式)）
   - **d. 任务主要节点**——有任务类型时，捕获/推断任务主要节点（业务语言），**供人类确认业务流转关系**（谁审批/通过后到哪），作为 wft02 引擎化的业务锚点。正式文档节点附着封面、轻量文档附着正文实体；规范化的相关方需求明示则捕获，否则标注 `[待wft02推断]`
   - **e. PL6-B 正文条目**——先判定正文是否为集合容器，再按三独立（判据见附录 A.3）逐类判定；轻量文档原子正文行不重复展开。规范化的相关方需求只提管理模式（如"需要看板展示"）或信息不足→推断条目类型 + 条目名称标 `[待wft02推断]`
   - **f. 23B 对齐判定**（Bn 组装 ①）——逐文档 Q1/Q2/Q3 判定（判据见附录 A.4）
5. **确定文档序列**——按业务流转顺序排列
6. **组装业务流程概要**——写入 `02-*.md`，完整要素见 [§2.1.2](../eos-wft01-biz-spr2bph.md#212-业务流程概要节点要素与职责边界)（详见 human spec）
7. **Bn 组装**——按人类方案 §3.3.3 执行：① 23B Q1/Q2/Q3 判定（步骤 4.e 已逐文档完成）→ ② PL4 WBS 归组（数据依赖链硬判据 + 完成耦合，判据见附录 A.7）→ ③ 23 一致性检查（归属冲突/误新建/误引用）+ 缺口推断（按构件产品数据清单完整性做名称级比对，**不依赖任务类型/引擎**）。组装结论写入业务流程概要节点的 Bn 组装结论；结构性缺口标注 `[推断-待规范化的相关方需求确认]` 入 Step 4 补充规范化的相关方需求回路。Phase B 修订时业务链拓扑变化同样重跑（见 Phase B 步骤 5）

**写入业务流程概要节点时**，为每个条目初始化空的 `确认状态` 字段。

---

### Step 4 · 规范化的相关方需求增补

按每个业务流程概要独立执行。新建 → 全量检查；修订 → 仅当链拓扑变化时重验闭合。

**1. E2E 闭合检查**——D 闭合（需求提出→需求满足）；PCA 闭合按 [91 §2.7](../91-eos-biz-eng-spec.md#27-pca-场景的-pca)——业务链**必须包含计划清单输出文档**（正文条目三独立展开 PL6-B）+ 承载 C/A 的输出文档（指标/看板/风险/问题清单），**C/A 本身为任务链运行时结果、不作为规范化的相关方需求缺口**。缺口不脑补 → 生成补充规范化需求材料。

**2. 要素机械检查**——逐文档确认非空或有合理空置原因：文档名称/用途/封面模式/引擎/开发方式/任务类型/任务关系（信息不足标 `[待wft02推断]`）/PL6-B（信息不足标 `[待wft02推断]`）/生命周期/支持信息/交互信息/Bn 组装（Q 判定 + WBS 归组 + 缺口推断）。

**3. 补充规范化需求材料生成**——闭合缺口 + **Bn 组装结构性缺口**（构件产品数据清单未覆盖，附录 A.7 ③）时生成补充规范化需求材料，写入 `../../../80-pl4eos-2-eosdata/00-origin-requirement-materials/10-raw-files/<YYYYMMDD>-<场景业务名称>补充原始需求材料.md`，登记到材料状态表。

**4. 治理业务覆盖检查**——从 Step 1 已加载的树画像（`read-section.sh` 获取）中识别治理角色（业务架构管理员/数据治理员）的业务流程概要节点，判定当前批次是否触发治理需求。触发但缺规范化的相关方需求 → Step End 提示人类渐进增补。**治理业务流程概要业务链完备性判据**——按角色类型预定文档集核对（`21-eos-stakeholder-roles.md`）：业务链文档与预定产出文档集不一致 → 标注 `[治理缺口]` + Step End 提示人类渐进增补。

---

### Step 5 · 资产写回与落账

仅写回状态=`可以分解分配` 的业务流程概要节点。Step 3 新产出留待下一轮。

**写回操作**：
1. 遍历已确认业务流程概要的业务链文档，按 Bn 组装结果写回 23 资产——Q1 追加引用（去重），Q2 扩展已有块+扩展说明，Q3 新建块+定义+引用；**PL4 WBS 归组结论一并写回**（定位/扩展/新建 WBS 包）；**`[23归属冲突]` 矛盾项暂不写回**（待人类裁决后下一轮处理）
2. 对应规范化的相关方需求条目 → `已分配`
3. 记录资产写回记录到 `02-*.md`（日期/路径/动作/判定）
4. 记录引用链——规范化的相关方需求→业务流程概要→23B

**提交基线 + 元信息维护**：

```bash
# 追加 AI最近变更 记录 + 移出已完成节点（先于提交，随基线入库）
bash ../scripts/update-meta.sh <02-file> add-recent-change "wft01-biz" "资产写回" "<业务流程概要-ID>" "23B 引用写回"
bash ../scripts/update-meta.sh <01-file> move-node "<规范化的相关方需求-ID>" "wft01-biz" "done"
# 有未处理 `[修改]`/`[驳回]` 反馈的条目移入「待反馈处理」分节（对齐人类方案 §5.6 第6项）

# 提交本轮 AI 产出（链级不变式：基线 = 最后 AI 提交，git diff <HEAD @上次 AI 运行>..HEAD 只含人类变更）
git add -A && git commit -m "[AI] wft01-biz 资产写回（Co-Authored-By: Claude）"

# 更新文件头（HEAD = 刚提交的基线 hash）
bash ../scripts/update-meta.sh <01-file> bump-version
bash ../scripts/update-meta.sh <01-file> update-head
bash ../scripts/update-meta.sh <02-file> bump-version
bash ../scripts/update-meta.sh <02-file> update-head
bash ../scripts/update-meta.sh <23-file> bump-version
bash ../scripts/update-meta.sh <23-file> update-head
```

---

### Step End · 运行结束输出（含反馈等待）

按三区模板输出方案摘要：

```text
当前状态：<待确认方案 / 可以分解分配>
  修订业务流程概要-XXX（+N 规范化的相关方需求，v{N-1}→v{N}）/ 新建业务流程概要-YYY（聚合 K 条规范化的相关方需求，v1）
  退回规范化规范化的相关方需求-ZZZ（建议拆为子规范化的相关方需求后重新提交预处理链）
  待重新分组规范化的相关方需求-WWW（本轮宿主误匹配，留待下一轮）
资产落账：<23B 引用写回 / 未落账>
补充原始需求材料：<无 / YYYYMMDD-场景业务名-补充原始需求材料.md>（已登记材料状态表）

一、方案反馈
  A. 整体确认 → 回复「整体确认」，快捷同意全部未标注条目
  B. 局部修改 → 在确认状态中标注 [修改]：{修改内容}（如"第三个文档引擎改为 @engine-workorder"）
  C. 驳回重做 → 在确认状态中标注 [驳回]：{驳回理由}

二、下一步
  本Skill → 选择 FR-BIZ 规范化的相关方需求（含 `需退回重切` 的规范化的相关方需求拆分后重新提交）或 `需wft01修订` 的业务流程概要节点重新运行
  后续    → 业务流程概要-XXX（可以分解分配）、业务流程概要-ZZZ（待补充分解分配）→ wft02-biz
```

**输出后不要结束对话。** 等待人类反馈——人类可直接用自然语言回复修改意见：

> 以上是本轮方案。直接回复修改意见即可：
> - "第三个文档引擎改为 @engine-workorder"
> - "整体确认"
> - "采购申请单的 PL6-B 粒度太粗，拆分一下"

**AI 处理反馈规则**：

| 人类输入 | AI 动作 |
|---------|--------|
| 显式修改指令（"X 改为 Y"） | 定位条目 → Edit 修改 → 追加 `[已处理]` → 输出修改摘要 → 继续等待 |
| 质疑/讨论（"为什么选 X？"） | 解释理由，不自动修改，继续等待决策 |
| 整体确认（"可以"/"整体确认"） | 全部条目追加 `[同意] [已处理]` → **整理本次反馈总结**（见下）→ 推进状态 → Step 5 落账（含提交基线）→ 结束 |
| 模糊意见（"粒度太粗"） | 尝试具体化追问，引导可执行指令 |
| 线下修订检出（git diff 发现确认状态标注/自由文本编辑） | 定位条目 → Edit 修订 → 追加 `[已处理]` → 输出修改摘要 → 继续等待 |

**反馈总结**（仅在人类"整体确认"后执行）：

反馈互动结束后，AI 将本轮反馈要点总结为一句话，写入 `## AI最近变更`：

```bash
bash ../scripts/update-meta.sh <02-file> add-recent-change "wft01-biz" "反馈处理" "<业务流程概要-ID>" "<AI 总结的反馈要点>"
```

摘要示例：`引擎修正：@engine-flow→@engine-workorder；PL6-B"订单及时率"新增口径定义`

---

## 三、AI 标记约定

四类增量标记（`[新增]`/`[变更]`/`[确认]`/`[需裁决]`）的定义详见 [91 规范 §A.3.1](../91-eos-biz-eng-spec.md)。

每次 Step 3 完成后，向人类输出增量清单，格式如：

```text
=== 业务流程概要-XXXX 本轮更新清单 ===

[新增] 承接规范化的相关方需求-XXXX, 规范化的相关方需求-YYYY（共2条）

业务链
  [新增] "采购申请单"（@engine-flow）  资产对齐：23B=Q1
  [新增] "采购履约看板"（@engine-kanban）  [管理类业务流程概要]  资产对齐：23B=Q2
     [新增] PL6-B "订单及时率"  [三独立：是]

Bn 组装
  [新增] PL4 WBS：采购申请创建与维护    归组：采购申请单/审批记录
  [需裁决] 23 一致性：23B-Q3 建议新建"采购合同管理"，组装推导已有"采购合同"构件可承接 → 降为 Q2
  [推断-待规范化的相关方需求确认] 结构性缺口：构件产品数据清单含"供应商评估报告"，业务链未覆盖 → 补充规范化需求材料

完备性检查
  业务链端到端闭合 ⚠ / 业务流程概要类型：执行类
  → 补充原始需求材料: 20260717-采购执行补充原始需求材料.md
```

---

## 附录A：判据速查

### A.1 业务流程概要类型判定

**概念锚点**：场景业务（业务流程概要）= 一个主责角色的一个业务对象闭环——业务对象与主责角色一一对应（主推动者：提出需求 / 负责完成 / 确认结果），配合角色是闭环内流程环节、不单独成业务流程概要（[91 §1.4](../91-eos-biz-eng-spec.md#14-业务场景)）。D 与 PCA 两种类型：

| 类型 | 典型动作词 | 闭合规则 |
|------|-----------|---------|
| **执行类（D）** | 创建/提交/审批/交付/验收/退货/对账 | D 闭合——从需求提出到需求满足，文档间以输入-输出传递链为主 |
| **管理类（PCA）** | 策划/计划/监控/度量/分析/识别/纠正/改进 | PCA 闭合——业务链**必须包含计划清单输出文档**（正文条目三独立展开 PL6-B，首条目=策划）+ P 定义型文档（指标/看板清单）；C/A 为任务链运行时结果（指标度量→看板呈现→风险/问题条目） |

D 和 PCA 处理逻辑**流程骨架同构**（场景业务→文档序列→每文档封面/正文/任务/引用），差异在文档模式（PCA 含计划清单）与闭合形态（[§5.5](../eos-wft01-biz-spr2bph.md#55-step-4--规范化的相关方需求增补)）。类型仅影响引擎推断偏好（D 产品数据类 / PCA 业务管理类优先，计划引擎 @engine-task 两场景共用）——引擎权威校验以任务类型↔引擎映射为准（wft02 §3.2）。

### A.2 封面模式判定

问：该文档是否有独立于正文 CRUD 的生命周期（需要审批流转/版本追溯/派发验收/独立状态变迁）？
- **是** → 正式文档（有封面，DocumentCover 表一行 + 正文 1:1）
- **否** → 轻量文档（无封面，正文即文档）

### A.3 PL6-B 三独立判定

| 条件 | 判据 | 正例 | 反例 |
|------|------|------|------|
| ① 独立名称 | 在文档内有固有称谓 | ✓ "订单及时率"、"采购预警" | ✗ 物料行（"序号1"不是名称） |
| ② 独立定义要素 | 有独立业务约束/规则/格式 | ✓ 指标有口径/维度/目标值 | ✗ 物料行的编码/数量由文档字段结构定义 |
| ③ 独立生命周期 | 独立于父文档的状态变迁 | ✓ 指标需审批后发布 | ✗ 物料行随申请单创建/删除 |

三项全满足 → 展开 PL6-B。轻量文档原子正文行不重复判定。

### A.4 23B 对齐判定 Q1/Q2/Q3

| 判定 | 条件 | 动作 |
|------|------|------|
| **Q1** | E2E 项目业务 + 构件类型均在 23 资产中存在，职责范围未超出 | 直接引用已有路径 |
| **Q2** | E2E 存在但构件需扩展，或构件存在但归属需调整 | 引用+扩展建议 |
| **Q3** | E2E 或构件类型不存在 | 新建建议（名称/归属路径/定义） |

### A.5 Phase B 关系检测

| 关系 | 判定 | 处理 |
|------|------|------|
| 确认 | 文档匹配+引擎相同+来源非规范化的相关方需求明示 | 来源升级 |
| 无新增 | 文档匹配+引擎相同+全部已规范化的相关方需求明示 | 追加引用+增量合并 |
| 补充 | 文档不匹配 | 新增，来源=规范化的相关方需求明示 |
| 过时 | 基线文档未被本轮覆盖 | `[过时]`+保留不删 |
| 引擎变更 | 文档匹配+引擎不同 | 更新引擎+变更原因 |

### A.6 节点生命周期状态流转

```
待确认方案 ──人类确认──→ 可以分解分配 ──wft02锁定──→ 在分解分配 ──wft02完成──→ 已分解分配
     ↑                       ↑                        │
     │                       │              wft02退回   │
     │                       │                        ↓
     └── wft01修订 ──── 需wft01修订 ←────────────────┘

上游变更分流：
  可以分解分配 → 可以分解分配（版本递增）
  在分解分配   → 在分解分配（版本递增）
  已分解分配   → 待补充分解分配
```

### A.7 Bn 组装（WBS 归组 / 23 一致性 / 缺口推断）

本 Skill 末级（文档）完整性的双闸之一——A2 闭环（Step 4 闭合检查）+ **Bn 审查补漏**（本判据）。逐文档执行三步：

**① 23B 对齐判定**（Q1/Q2/Q3，见 A.4）——确定 PL2（E2E 项目业务）/PL3（构件类型）归属。

**② PL4 WBS 归组**——将同一构件类型（PL3）下的输出文档按业务模块归纳分组（PL3 与 PL5 之间），命名取文档名称/用途/支撑引擎共性。**硬判据 = 数据依赖链**：归入同一 WBS 的交付类文档，正文数据须在同一数据依赖链上（前一文档正文数据成为后一文档输入）→ 组内文档满足**完成耦合**；跨链不归组；管理类文档（指标/看板/风险/问题/计划清单）按其支撑的交付链归属。

**③ 23 一致性检查 + 缺口推断**——组装后校验 ① 与 ② 是否矛盾（检出而非静默覆盖）：

| 不一致类型 | 判定 | 动作 |
|-----------|------|------|
| 归属冲突 | Q2/Q3 建议归属与 WBS 推导的 PL2/PL3 不一致 | 标注 `[23归属冲突]` + 两者路径对比 → 提示人类裁决 |
| 误新建 | Q3 建议新建，组装上下文发现已有条目（同名/同义且职责覆盖）可承接 | 降为 Q2 + 记录降级原因和已有条目路径 |
| 误引用 | Q1 引用已有条目，组装后发现职责边界不能覆盖当前文档 | 升为 Q2/Q3 + 记录升级原因 |

矛盾项 Step 5 暂不写回 23（待人类裁决后下一轮处理）。

**缺口推断**：对每个构件类型，读取 23 资产 Bn 侧**产品数据清单**（该构件应有的输出文档集合，名称级），与业务流程概要业务链做**名称级比对**——清单文档已在业务链 → 视为已覆盖；未在 → **结构性缺口**，标注 `[推断-待规范化的相关方需求确认]`，由 Step 4 补充规范化需求材料补回。**不依赖任务类型/引擎**。

---

## 附录B：脚本接口速查

| 脚本 | 用法 | 说明 |
|------|------|------|
| `read-section.sh` | `bash ../scripts/read-section.sh <文件> <分节名>` | 提取 ##/### 分节完整内容 |
| `read-node.sh` | `bash ../scripts/read-node.sh <文件> <节点ID>` | 按节点ID提取节点块（支持规范化的相关方需求-NNN / ROLE-XXX / 业务流程概要-XXX / bph-nfr-XXX / @node-xxx / @engine-xxx / @nfr-NNN） |
| `detect-changes.sh` | `bash ../scripts/detect-changes.sh <文件>` | 检测人类自上次 AI 运行以来的文档变更 |
| `update-meta.sh` | `bash ../scripts/update-meta.sh <文件> <操作>` | 维护文件元信息（bump-version / update-head / add-recent-change / move-node） |

所有脚本路径相对于 `eos-wft01-biz-spr2bph/` 目录。

---

## 变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-08-28 | v1.32 | 层名更名（人类指示 2026-08-28）——业务流程概要定义→业务流程概要、业务流程详细定义→业务流程方案、系统需求概要定义→系统需求概要、系统需求详细定义→系统需求方案（nfr/eng 链同构，含短名/标题后缀）；历史变更记录保留旧词。 AI 执行规则语义不变 |
| 2026-08-27 | v1.31 | s 代号退役同步（人类指示 2026-08-27）——s01/s02/s03 移除，改用具体名称。AI 执行规则语义不变 |
| 2026-08-27 | v1.30 | 链级术语清扫同步（人类指示 2026-08-27）——规范化需求→规范化的相关方需求。AI 执行规则语义不变 |
| 2026-08-27 | v1.29 | 标题更名同步（人类指示 2026-08-27）——规范化需求 → 业务流程概要定义与资产化 → 规范化的相关方需求 → 业务流程概要定义。AI 执行规则语义不变 |
| 2026-08-25 | v1.28 | 链级机械修正（wft01-nfr 深审链级联动，人类裁决 2026-08-25）：①补充材料文件名统一「补充规范化需求材料.md」→「补充原始需求材料.md」（4 处，对齐 nfr/eng）；②数据文件相对路径修正 `../../80-pl4eos-2-eosdata/`→`../../../80-pl4eos-2-eosdata/`（自 skill 子目录上跳 3 级）。AI 执行规则语义不变 |
| 2026-08-24 | v1.27 | 树画像分节名修正（链级联动，人类裁决 2026-08-24）——Step 1/Step 4 的「STR树画像 / STR节点索引」→ 02 数据文件实际分节名「0. 业务概要树画像」「2. 业务概要节点索引 / 分片索引」（原 read-section 精确匹配失败；v1.20「还原」指向的分节名有误）。AI 执行规则语义不变 |
| 2026-08-24 | v1.26 | 材料状态表联动（wft03 单独轮 B4）——「规范化需求材料状态表」→「材料状态表」（2 处）。AI 执行规则语义不变 |
| 2026-08-24 | v1.25 | 人类方案同步（缩写更名 sfh→srh/sfd→srd + 中文层名统一）；AI 执行规则语义不变（术语随迁）。 |
| 2026-08-24 | v1.24 | 资产名更名补遗（人类方案 v3.28）——read-node 支持格式 STR-NFR-XXX→bph-nfr-XXX |
| 2026-08-24 | v1.22 | 标题正名同步（人类定名）——规范化需求 → 业务流程概要定义与资产化。AI 执行规则语义不变 |
| 2026-08-24 | v1.21 | 输出术语正名同步（人类纠正）——业务概要定义→业务流程概要定义（bph）、业务详细定义→业务流程详细定义（标题/描述/锚点随迁）。AI 执行规则语义不变 |
| 2026-08-24 | v1.20 | 内容层逻辑修正同步（人类指示"仔细看看"）——规范化需求需求复合词、需求规范化（ort03）、材料状态表、STR 树画像还原（资产分节名）。AI 执行规则语义不变 |
| 2026-08-24 | v1.19 | 全篇术语清扫同步（人类定名）——规范化需求（spr）/业务流程概要定义（bph）替换 OR/STR-F/原始需求/相关方需求/业务需求（含标题/描述/锚点改向）。AI 执行规则语义不变 |
| 2026-08-24 | v1.18 | 文件名更名同步（人类定名 2026-08-24）——`eos-wft01-biz-or2str` → `eos-wft01-biz-spr2bph`（name/frontmatter/设计依据/全库引用改向）。AI 执行规则语义不变 |
| 2026-08-24 | v1.17 | 深审第四轮修复——①引用源消歧（删 `wft01-runtime-protocols.md`，运行时协议改直引 91 附录A；入口检测/变更影响声明/§A.3.3 锚点随迁）；②主文档 v3.21 同步（Step 5 补写回记录/引用链/待反馈分节、Phase C 补任务主要节点 + `[待wft02推断]` 标注、要素机械检查补交互信息、治理覆盖补业务链完备性判据、确认状态格式示例、特化参数表）。AI 执行规则随迁 |
| 2026-08-22 | v1.16 | 人类方案 v3.21 同步（深审第三轮修复）——附录 A.1 引擎偏好句重写（交付类→产品数据类、@engine-task 两场景共用、权威校验以任务类型↔引擎映射为准）。AI 执行规则语义不变 |
| 2026-08-22 | v1.15 | 人类方案 v3.20 同步（取消 26 号文档资产联动）——主文件 §2.1.2「追溯/23/26 不迁」→「追溯/23」、§3.3.3「26 随 Bn 跟随」→「文档定义由 wft02 承载（04 + 25 §1.7）」；本 SKILL 无 26 引用，AI 执行规则语义不变 |
| 2026-08-22 | v1.14 | 人类方案 v3.17~v3.19 同步（深审 B 组）——Step End 方案反馈区补 A/B/C 三选项（整体确认/局部修改/驳回重做，对齐主文件 v3.18 与 91 §A.3.2）。AI 执行规则语义不变 |
| 2026-08-21 | v1.13 | 人类方案 v3.16 同步（biz 链「概要/详细」分工框架定案实施——本 Skill 定位为业务流程概要定义 s01）：description/负责表改「Bn 组装」；Step 3 Phase B 补步骤 5「Bn 组装重跑」（拓扑变化重跑）、Phase C 补步骤 7「Bn 组装」（①Q判定→②WBS 归组→③一致性+缺口推断，结构性缺口入 Step 4）；Step 4 补充规范化需求材料合并结构性缺口；Step 5 写回补 WBS 归组结论 + `[23归属冲突]` 暂不写回；新增附录 A.7「Bn 组装」判据速查；§三 增量清单补 Bn 组装段。AI 执行规则语义不变（Bn 组装为新执行步骤） |
| 2026-08-21 | v1.12 | 人类方案 v3.15 同步（Step 框架统一）——Step 2 内联处理表补「线下修订检出」行 + 整体确认补 `[同意]`、输入校验「判定\|动作」统一、Step End 双区合并单区、标注纪律框架。AI 执行规则随迁 |
| 2026-08-20 | v1.11 | 人类方案 v3.14 同步——Step Start 补「变更感知」（detect-changes.sh 先于入口检测，检出人类线下修订纳入"待反馈处理"分节）；Step 1「版本感知」改「安全复查」（仅标记 [需确认] 不自动推进，对齐 §A.3.3 R0a）。AI 执行规则语义不变 |
| 2026-08-19 | v1.10 | 提交基线顺序修正（对齐人类方案 §5.6 第 5~8 项 / wft01-eng v8.5 M1 先例）：Step 5 由「git commit → 文件头 → add-recent-change → move-node」改为「add-recent-change + move-node（先于提交、随基线入库）→ git commit → 文件头（HEAD=刚提交的基线 hash）」。原顺序使 AI最近变更 与 文件头 更新不进基线提交，违反「基线=最后 AI 提交」链级不变式。AI 执行规则语义不变 |
| 2026-08-19 | v1.9 | 反馈双轨落地（对齐人类方案 v3.13 / 91 §A.5 v5.20 / wft02 SKILL v1.8）：Step 2 补「反馈双轨」（AI 对话反馈主通道 + 线下文档修订经 git diff 检出，两轨等价）；Step End AI 处理反馈规则表补「线下修订检出」行；AI 执行规则语义不变 |
| 2026-08-19 | v1.8 | 人类方案原理与方法审视修复（v3.11）同步——Step 4 PCA 闭合检查补"必须包含计划清单文档 + C/A 为运行时结果"；附录 A.1 同构表述改为"流程骨架同构"+ PCA 闭合规则补计划清单要求；Phase C R4 补"开发/记录类文档 + 两个计划文档"注。AI 执行规则随迁 |
| 2026-08-19 | v1.7 | 人类方案知识层三章重构（§二 概念 / §三 原理与判据 / §四 节点演进）；SKILL 锚点迁移 #322/#323/#335（原 #232/#233/#245），AI 执行规则语义不变。对齐人类方案 v3.10 |
| 2026-08-19 | v1.6 | 人类方案转换总览去编号（2.0→无编号）；SKILL 无 2.0 引用、AI 执行规则语义不变。对齐人类方案 v3.9 |
| 2026-08-19 | v1.5 | 链级术语统一：正文转化引擎→支撑引擎（对齐人类方案 v3.8 全链统一）；AI 执行规则语义不变 |
| 2026-08-19 | v1.4 | 人类方案章节拆分（§二 拆为 §二 概念与规则 + §三 节点演进；操作流程 §三→§四）；SKILL 锚点 2.x 不变、AI 执行规则语义不变。对齐人类方案 v3.7 |
| 2026-08-19 | v1.3 | 人类方案 §2.1.2 标题「架构与属性」→「业务流程概要定义节点要素与职责边界」（"节点"指代明确 = 业务流程概要定义节点），SKILL 锚点同步；AI 执行规则语义不变。对齐人类方案 v3.6 |
| 2026-08-19 | v1.2 | 业务对象闭环概念同步（负责表 + A.1 概念锚点，对齐人类方案 v3.3 核心定义）+ 引擎术语统一（正文转化引擎）；对齐人类方案 v3.5 |
| 2026-08-19 | v1.1 | 提交基线（Step 5 写回 + 整体确认后 `git add`+`commit`+`update-head`，链级不变式）+ 确认状态标记口径同步（`[修改]`/`[驳回]`/`[同意]`/`[已处理]`）；对齐人类方案 v3.4（C1/C2） |
| 2026-07-17 | v1.0 | 初始版本——从人类方案文档提取 Skill，脚本化数据访问，协议精简嵌入 |
