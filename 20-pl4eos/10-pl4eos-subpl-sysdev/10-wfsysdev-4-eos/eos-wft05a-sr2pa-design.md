# SysReq → PA 架构 · Skill A · 设计开发
**Skill ID**: wft05a · **Phase 1: SysReq → PA Design**

> **概述**：每次执行处理**一个** SysReq 功能架构末级节点（IPO），执行创造性/分析性设计活动，产出 PA 组件方案。按 IPO 类型走不对称设计路径：业务定义类 IPO 为模式匹配（机械性），引擎配置类 IPO 为分解分配分析（创造性）。
>
> **输入 → 输出**：一个 SysReq 末级 IPO（`05-*.md`）→ PA 组件方案（`07-*.md`，状态=待确认方案）
>
> **适用场景**：瀑布式系统设计第5步 · Phase 1。设计方案经人类审阅通过后，由 Skill B（`eos-wft05b-sr2pa-settle.md`）执行资产落账和详细定义展开。
>
> **设计模式参考**：[91-doc-paired-skill-spec.md](91-doc-paired-skill-spec.md)
>
> **版本**：v4.3 | **修订**：2026-06-17

---

## 一、设计角色

本步在设计链中承担的角色：
- **输入**：`05-*.md` SysReq 状态表中人类选入的**一个** SysReq 功能架构末级节点（IPO）
- **产出**：PA 组件方案写入 `07-*.md`（状态=待确认方案），供人类审阅

本步按 IPO 类型生成两类 PA 末级组件：
- **业务定义类 IPO → 配置方案组件**
- **引擎配置类 IPO → 前后端组件**

### 与三条工程化设计原则的关系

| 原则 | 在本步中的体现 |
|------|--------------|
| **详概分离** | 本步产出 PA 层的**架构方案**（`07-*.md`）；详细定义（`06-*.md`）由 Skill B Phase 2 下半产出 |
| **文档结对** | 本步执行结对4（SysReq 详细定义末级 ↔ PA 架构）的设计思考——一次思考同时形成 06 和 07 的内容，正式落入由 Skill B 完成 |
| **基于资产** | 每次执行时读取已有 PA 资产，优先复用 > 改进 > 新增。单节点模式使后处理的节点总能看到前序已确认结果 |

### 单节点处理模式

每次执行只处理**一个** SysReq 架构末级节点（IPO）。这是全链路（wft01~wft05）的统一原则。

**成立的根本原因**——基于资产的设计原则使序列处理天然可行：

```
执行第1次：读已有资产 → 对 IPO-A 做设计 → 写回（状态=待确认方案）
执行第2次：读已有资产（含 A 结果）→ 对 IPO-B 做设计 → 写回
```

**无顺序约束**——人类选材时不需考虑 IPO 处理顺序：

1. **每个节点自描述**：四要素（I/P/O + 边界）已包含设计所需全部信息
2. **不依赖其他节点的设计结果**：只依赖自身四要素 + 已有资产状态
3. **承接关系是事后验证**：节点间 I/O 一致性由 Step 5 闭环检查保障

### 门禁 1 前的全局串行规则

为降低人类理解成本并保持"已有资产=已确认资产"的口径稳定，`wft05a` 采用**门禁 1 前全局串行**：

1. 任一 IPO 进入 `待设计开发`、`迭代中` 的执行过程后，直到该 IPO 完成门禁 1 前，**其他 IPO 不允许进入 Skill A**
2. 当前 IPO 到达 `待确认方案` 后，人类必须先给出 `[同意]` 或 `[修改]`
3. 人类给出 `[同意]` 后，节点状态转为 `待资产落账`，此时下一个 IPO 才允许进入 Skill A
4. 人类给出 `[修改]` 后，原节点状态转为 `迭代中`，仍由该节点继续占用 Skill A，直到再次完成门禁 1

该规则的目的不是表达节点间存在强顺序依赖，而是将 Skill A 明确定义为一个**串行设计工位**，避免后续节点把未确认方案误读为稳定资产。

### 两类 IPO 的不对称设计逻辑

```
业务定义类 IPO → 配置方案组件
  信息来源：配对引擎 IPO 的 I（已详细描述所需配置信息）
  生成方式：信息重组（模式匹配）

引擎配置类 IPO → 前后端组件
  信息来源：EOS 前后端组件资产 + IPO 四要素 + 系统需求
  生成方式：分解分配分析（创造性设计）
```

---

## 二、前置条件与状态机

### 文档结对关系

| 结对 | 需求方 | 方案方 | 本步角色 |
|------|--------|--------|---------|
| **结对4** | SysReq 详细定义条目 | PA 架构定义 | 设计思考在此完成，正式落入由 Skill B 执行 |

### 节点状态定义

| 状态 | 含义 | 所处阶段 |
|------|------|---------|
| **待设计开发** | 上游就绪 + 人类同意 | Skill A 入口（首次） |
| **迭代中** | 人类反馈修改意见 | Skill A 入口（重入） |
| **待确认方案** | Skill A 已完成，等待人类审阅 | 人类审阅门禁 1 |
| **待资产落账** | 方案已确认 | Skill B 入口 |
| **已资产落账** | Skill B 上半完成 | Skill B 下半入口 |
| **待确认细节** | Skill B 下半完成，等待人类审阅 | 人类审阅门禁 2 |
| **确认中** | 人类反馈修改详细定义 | Skill B 下半重入 |
| **已验证冻结** | 全链验证通过 | 终态 |
| **已废弃** | 停止使用 | 终态 |

### Step Start 路由逻辑

```
读取目标节点状态：
  ├─ 若存在其他 IPO 处于待确认方案 / 迭代中 → 提示"先完成该 IPO 的门禁 1"
  ├─ 待设计开发 → 执行 Skill A（首次）
  ├─ 迭代中 → 执行 Skill A（重入：加载反馈）
  ├─ 待资产落账 / 已资产落账 / 确认中 → 提示"请运行 Skill B"
  ├─ 待确认方案 / 待确认细节 → 提示"等待人类审阅"
  └─ 已验证冻结 / 已废弃 → 不可操作
```

---

## 三、操作流程

### Step 布局总览

| Step | 名称 | 执行者 | 写资产 |
|------|------|--------|--------|
| Start | 前置校验与路由 | AI | — |
| 1 | 资产加载 | AI | — |
| 2 | 分析活动 | AI | — |
| 3 | 设计活动（按 IPO 类型分流） | AI | — |
| 4 | 方案写入 | AI | `07` |
| 5 | 闭环检查 | AI | — |
| End | 输出交互（门禁 1） | 人类 | — |

### Step Start · 前置校验与路由

| # | 操作 | 说明 |
|---|------|------|
| 1 | 接收目标节点 | 人类指定的 SysReq 末级 IPO（节点 ID 或名称） |
| 2 | 检查串行占用 | 从 `05/07` 状态表检查是否已有其他 IPO 处于 `待确认方案` 或 `迭代中` |
| 3 | 读取节点状态 | 从 `05-*.md` SysReq 状态表或 `07-*.md` PA 状态表读取**当前状态** |
| 4 | 检查资产存在性 | 确认 `05-*.md` / `07-*.md` 存在可读 |
| 5 | 路由决策 | 串行占用存在 → 结束；待设计开发/迭代中 → 继续；其他 → 输出提示后结束 |

### Step 1 · 资产加载

| # | 操作 | 说明 |
|---|------|------|
| 1 | 加载目标 IPO 四要素 | I/P/O + 边界描述 |
| 2 | 加载 07 PA 相关分区资产 | PA 架构树、已有组件定义、`wft03` 建立的 PA 骨架 |
| 3 | 加载 NFR 约束包 | 读取 `wft04` 产出的分层约束表，按 `全局 + 所属业务域 + 目标 IPO` 三层装配 SysReq-NFR 约束包 |
| 4 | 加载 EOS 组件参考 | 前后端组件清单、配置方案组件清单 |
| 5 | 加载反馈（重入时） | 若状态=迭代中，读取 `07-*.md` 反馈区人类修改意见 |

### Step 2 · 分析活动

| # | 操作 | 说明 |
|---|------|------|
| 1 | 输入完整性验证 | IPO 四要素完整可用（I/P/O + 边界均非空） |
| 2 | 快速预检结论 | 汇总是否缺依附引擎配置类 IPO、阻断型 NFR、追溯链或必要资产 |
| 3 | 追溯链验证 | 目标 IPO 可追溯到 BA IPO 和上游 STR/SR 节点 |
| 4 | 与已有资产一致性 | 检查与已确认 PA 组件间是否存在边界冲突或重叠 |

### Step 3 · 设计活动

> 按目标 IPO 类型分流。这是结对4 的核心设计活动——一次思考同时产出 06 和 07 的**内容**（正式落入资产由 Skill B 完成）。

**3a · 业务定义类 IPO → 配置方案组件**

| # | 操作 | 说明 |
|---|------|------|
| 1 | 定位配对引擎 IPO | 找到与目标业务定义类 IPO 配对的引擎配置类 IPO |
| 2 | 提取配置信息需求 | 从配对引擎 IPO 的 Input 提取所需配置信息 |
| 3 | 组织组件结构 | 组织为主定义组件 + 7 类子组件结构 |
| 4 | 资产决策 | 对每个组件执行：复用已有 > 改进已有 > 新增 |

**3b · 引擎配置类 IPO → 前后端组件（分解分配分析）**

> **设计姿态**：基于已加载的资产全景，提出你认为最合理的分解分配方案。这是创造性设计，不存在唯一正确答案；方案将经人类审阅后迭代收敛。
>
> **选择依据**：引擎配置类 IPO 满足 91-doc-paired-skill-spec §4.1.1 的以下条件，归入直觉驱动模式（分解分配分析）——输出空间不可穷举（条件一）、无固定映射路径（条件二）、质量依赖人类判断边界合理性（条件三）、方案强依赖当前资产语境（条件四）。

| | 内容 |
|--|------|
| **看** | 已有 PA 资产全景、目标 IPO 四要素、引擎能力全景、EOS 组件参考 |
| **守** | 禁止跨类生成、复用 > 改进 > 新增、承接链闭合、新增组件须与现有资产风格一致（粒度/命名/边界模式/层级位置/依赖关系——参见 91-doc-paired-skill-spec §5.2） |
| **交** | 条目→组件映射、资产决策（复用/改进/新增）、承接关系 |
| **完** | 每条目有唯一宿主、粒度合理、边界清晰 |

**收敛指南**：

| 情况 | 合理倾向 |
|------|---------|
| IPO 的 I 字段直接对应已有引擎能力 | 优先分配，不做多余组件拆分 |
| IPO 的 I 字段覆盖多个引擎能力 | 按能力领域拆分为多个承接组件 |
| 前后端职责可明确分离 | 拆为前端组件 + 后端组件 |
| 前后端职责紧密耦合 | 合为一个能力主组件，不强行拆分 |
| 已有同类组件且可扩展 | 优先扩展，不新增 |
| 无同类组件可参考 | 按 IPO 语义分解，保持与现有资产近似的粒度 |

**迭代预期**：预期 2~3 轮收敛。收敛信号——组件边界稳定、无未闭合承接关系、NFR 约束已映射。

**人类在门禁 1 的审查焦点**：
1. 组件边界是否合理——各组件职责是否不重叠、不遗漏
2. 资产决策是否正确——复用/改进/新增的判断是否合理
3. 组件风格是否与现有资产协调——新增组件的粒度、命名、层级是否与同类一致
4. 承接关系是否闭合——所有需求条目有唯一宿主

质量兜底：门禁 1（人类审阅上述焦点）+ 迭代重入。

**3c · 非末级宿主生成（如需要）**

从当前末级组件集合反推稳定分类结构。总原则：先有末级组件，再生成非末级宿主。

### Step 4 · 方案写入

| # | 操作 | 说明 |
|---|------|------|
| 1 | 写入 PA 组件方案 | 写入 `07-*.md` 目标业务域分区（状态=待确认方案） |
| 2 | 记录 NFR 映射决策 | 将相关 NFR 约束映射到新生成/改进的 PA 组件 |
| 3 | 记录 SysReq→PA 映射 | 记录本次 IPO 到 PA 组件的映射关系 |

### Step 5 · 闭环检查

| # | 操作 | 说明 |
|---|------|------|
| 1 | 覆盖率 | 目标 IPO 的全部功能已有 PA 组件承接 |
| 2 | 1:1 承接 | 每条需求条目有唯一 PA 组件宿主 |
| 3 | 粒度与边界 | 组件边界清晰、可独立维护与验收 |
| 4 | NFR 完整性 | 相关 NFR 约束已映射 |
| 5 | 承接链一致性 | 与已有组件 I/O 关系无冲突（事后验证——发现不一致标记为迭代建议，不阻断） |

### Step End · 输出交互（门禁 1）

| # | 操作 | 执行者 | 说明 |
|---|------|--------|------|
| 1 | 方案摘要展示 | AI | 按固定审查包输出：IPO 类型、组件清单、资产决策、边界摘要 |
| 2 | 预检与闭环报告 | AI | Step 2 快速预检结果 + Step 5 结果 |
| 3 | 审查指引 | AI | 告知人类在 `07-*.md` 反馈区按固定反馈格式标注 |
| 4 | 唯一路由结论 | AI | 基于当前方案评估，明确给出预定路由：`重新运行 wft05a`或`运行 wft05b`，不把选择权留给人类自行判断 |

**人类操作**：

- 人类不自行判断后续流向，只按 AI 给出的`下一步路由`执行动作
- 当 AI 路由为 `运行 wft05b` 时：人类在反馈区标注 `[同意]`，AI 将状态更新为 `待资产落账`
- 当 AI 路由为 `重新运行 wft05a` 时：人类在反馈区标注 `[修改-*]` 或补充缺失信息，AI 将状态更新为 `迭代中`
- 若当前方案状态为 `阶段性方案`，则 `[同意]` 不构成有效放行，节点不得推进为 `待资产落账`

### 门禁 1 交互提效规范

为提高串行模式下的人机交互效率，AI 与人类按以下固定格式交互。

**AI 固定输出包**：

1. `方案状态`：`阶段性方案` / `可确认方案`
2. `下一步路由`：`重新运行 wft05a` / `运行 wft05b`
3. `路由原因`：一句话说明为什么进入该路由
4. `IPO 类型`：业务定义类 / 引擎配置类
5. `组件清单`：列出组件名称，并标明复用 / 改进 / 新增
6. `边界摘要`：每个组件一句话说明其职责边界
7. `关键映射`：IPO 条目 → PA 组件宿主
8. `阻断项清单`：列出阻断进入 `wft05b` 的具体原因；若无则写 `无`
9. `风险提示`：仅列出需要人类判断的 1~3 个关键点
10. `当前应执行动作`：只给当前路由对应的人类动作
11. `预检结果`：是否缺依附引擎配置类 IPO、阻断型 NFR、追溯链、必要资产

**人类固定反馈格式**：

- `[同意]`
- `[修改-边界]` + 意见
- `[修改-复用决策]` + 意见
- `[修改-组件粒度]` + 意见
- `[修改-NFR映射]` + 意见
- `[修改-其他]` + 意见

**重入输出要求**：

若节点状态为 `迭代中`，AI 不重复展开完整设计过程，只输出：

1. 上次人类意见摘要
2. 本次改动点
3. 改动原因
4. 仍需人类确认的剩余问题

### 唯一路由规则

Step End 必须输出**唯一下一步路由**。该路由是 AI 基于**当前方案评估**给出的预定路由，不把 `wft05a` 和 `wft05b` 同时作为待选项交给人类。

路由规则（方案状态的判定信号参见 91-doc-paired-skill-spec §4.4）：

1. 若当前方案状态为 `阶段性方案`，或当前仍存在阻断项、缺口项，则：
   - `下一步路由 = 重新运行 wft05a`
   - `当前应执行动作 = 在反馈区补充修改意见或缺失信息后，运行 wft05a [节点ID]`
2. 若当前方案状态为 `可确认方案`，则：
   - `下一步路由 = 运行 wft05b`
   - `当前应执行动作 = 在反馈区标注 [同意] 后，运行 wft05b [节点ID]`
3. 若人类在 Step End 后给出 `[修改-*]`，则节点状态更新为 `迭代中`，下次重入 `wft05a`
4. 若人类在 `阶段性方案` 下给出 `[同意]`，AI 必须拒绝放行，说明阻断项仍未解除，并将节点保持在 `迭代中`
5. AI 在任一轮 Step End 中，只输出当前唯一成立的路由，不同时展示两套待选后续动作

### 启动方式

> wft05a [节点名称/ID]

**典型序列**：

```
wft05a 节点X → 设计 → 写方案 → End（门禁1）
若方案状态=可确认方案，人类：[同意] → 状态变为待资产落账
人类：wft05b 节点X → 资产落账 → 详细定义 → End（门禁2）
```

**迭代**：

```
wft05a 节点X → End
人类：[修改] "边界不对" → 状态变为迭代中
wft05a 节点X → 加载反馈 → 调整方案 → End
若方案状态=可确认方案，人类：[同意] → wft05b 节点X
```

---

## 四、PA 组件生成规则

### 4.1 类型判定

| IPO 类型 | 判定标准 | PA 末级组件类型 |
|---------|---------|----------------|
| **业务定义类 IPO** | 目标是产出具体业务定义；需求落在"定义内容怎么配" | **配置方案组件** |
| **引擎配置类 IPO** | 目标是提供/增强引擎配置承接能力；需求落在"引擎如何接收、解析、运行配置" | **前后端组件** |

### 4.2 业务定义类 IPO → 配置方案组件

处理步骤：识别依附引擎 → 识别业务定义对象 → 展开配置方案信息 → 组织组件。

与引擎配置类 IPO 的关系：业务定义类 IPO 所生成的配置方案信息构成引擎配置类 IPO 的核心输入。承接关系一致性由闭环检查事后验证，不构成设计时顺序约束。

建议"**1 个主定义组件 + 7 类子组件**"结构：

| 组件层 | 组件名称 | 职责 |
|------|---------|------|
| 主组件 | **主定义组件** | 该业务定义的核心承载单元 |
| 子组件1 | **元数据组件** | 编号、名称、版本、状态、适用范围、归属域 |
| 子组件2 | **结构组件** | 字段结构、流程节点结构、指标维度结构等 |
| 子组件3 | **规则组件** | 业务规则、流转规则、计算规则、校验规则 |
| 子组件4 | **权限组件** | 查看、配置、发起、审批、维护等权限 |
| 子组件5 | **展示组件** | 页面布局、列表视图、字段展示、菜单挂接 |
| 子组件6 | **引用集成组件** | 依赖的其他业务定义、引擎能力、外部数据源 |
| 子组件7 | **校验发布组件** | 完整性检查、冲突检查、发布/生效条件 |

### 4.3 引擎配置类 IPO → 前后端组件

处理步骤：识别引擎配置承接类型 → 识别核心输入（含配置方案信息）→ 判断前端/后端/两者 → 拆为组件。

建议"**1 个能力主组件 + 5 类承接组件**"结构：

| 组件层 | 组件名称 | 职责 |
|------|---------|------|
| 主组件 | **能力主组件** | 该引擎配置类 IPO 的核心承接单元 |
| 子组件1 | **前端配置组件** | 配置页面、设计器、参数编辑器 |
| 子组件2 | **前端运行组件** | 运行期输入/操作/结果/查看页面 |
| 子组件3 | **后端定义处理组件** | 配置信息解析、校验、组装、版本化 |
| 子组件4 | **后端运行执行组件** | 定义解释、调度、执行、状态流转 |
| 子组件5 | **后端接口与集成组件** | API、外部集成、消息、存储、事件 |

### 4.4 禁止跨类生成

1. 业务定义类 IPO 不直接落前后端组件
2. 引擎配置类 IPO 不直接落配置方案组件
3. 存在引擎缺口时回 `wft02` 处理

### 4.5 PA 非末级节点生成规则

**总原则**：先有末级组件集合，再生成非末级分类宿主。

1. 先按组件对象类型分大类（配置方案/前端/后端）
2. 再按职责稳定性分中类
3. 仅在边界稳定时才抽象非末级宿主
4. 非末级同样遵循复用 > 改进 > 新增

### 4.6 `wft05a` 的到位粒度边界

`wft05a` 只负责形成 **PA 主结构方案**，不负责把主结构展开成开发级详细规格。判断边界的总原则是：

- 影响"谁来承接、怎么切边界、是否复用"的内容，属于 `wft05a`
- 不影响上述结构判断、只是把内容写细写全的，属于 `wft05b`

#### 业务定义类 IPO 的到位粒度

业务定义类 IPO 在 `wft05a` 中应细到 **组件类别级 + 职责边界级**，以便人类判断该业务定义应由哪些配置方案组件承接。

必须达到：

1. 已判定是否需要独立的主定义组件
2. 已判定 7 类子组件中哪些需要、哪些可合并、哪些暂不需要
3. 已给出各组件的一句话职责边界
4. 已完成上游需求条目到主组件/子组件宿主的分配

不在本步展开：

1. 结构组件中的字段明细
2. 规则组件中的规则项全集
3. 权限组件中的角色操作项明细
4. 展示组件中的页面与交互细节
5. 校验发布组件中的逐条校验规则

#### 引擎配置类 IPO 的到位粒度

引擎配置类 IPO 在 `wft05a` 中应细到 **承接链级 + 组件边界级**，以便人类判断配置如何被接收、处理、执行以及这些职责应落在哪些前后端组件。

必须达到：

1. 已判定需要哪些承接组件（前端配置/前端运行/后端定义处理/后端运行执行/后端接口与集成）
2. 已判定这些组件是否都需要，哪些可合并，哪些暂不需要
3. 已给出各组件的一句话职责边界
4. 已说明配置输入如何进入引擎、结果大致从哪里输出
5. 已完成关键需求条目到承接组件的分配

不在本步展开：

1. API 或消息的字段级接口规格
2. 配置解析流程的详细步骤
3. 执行引擎内部算法或时序细节
4. 存储结构、表结构、DTO 全量定义
5. 重试、补偿、异常处理等开发级机制细节

#### 门禁 1 的审查焦点

人类在门禁 1 审查的不是"方案是否已经足够详细"，而是"当前 PA 主结构方案是否成立，是否允许进入 `wft05b` 做资产落账和详细展开"。

因此门禁 1 重点审查：

1. IPO 类型判定是否正确
2. 组件类型和组件清单是否正确
3. 组件边界是否合理
4. 复用 / 改进 / 新增 决策是否合理
5. SysReq → PA 承接关系是否闭合
6. 当前方案状态是阶段性方案还是可确认方案

---

## 五、AI 执行指南

1. **单节点处理**：每次只处理一个 IPO，不跨节点批量操作
2. **门禁 1 前全局串行**：若存在其他 IPO 处于 `待确认方案` 或 `迭代中`，不得接收新 IPO
3. **先装配 NFR 约束包，再做预检**：从 `wft04` 分层约束表中按 `全局 + 业务域 + IPO` 读取约束，不要求人类手工指定
4. **先做快速预检，再进入设计**：缺少依附引擎配置类 IPO、阻断型 NFR、追溯链时，应先报缺口；允许形成阶段性方案，但不得误报为稳定确认
5. **先判 IPO 类型，再设计**：进入 Step 3 前必须明确 IPO 类型
6. **基于资产优先**：复用已有配置宿主/引擎构件
7. **分解分配分析 = 创造性设计**：基于资产全景提出方案，人类审核反馈，多轮迭代收敛。不是从输入推导唯一正确答案
8. **配置方案信息是引擎配置类 IPO 的核心输入**：验证在闭环检查中事后执行
9. **Skill A 不做落账**：只写方案区（状态=待确认方案），不写反向引用/详细定义
10. **输出必须短而可审**：优先给固定审查包，不要求人类重复阅读完整推理过程

### NFR 约束包读取规则

目标 IPO 的 NFR 约束包按以下公式装配：

`约束包 = 全局约束 + 所属业务域约束 + 目标 IPO 专属约束`

读取规则：

1. `全局约束`：读取分层约束表中 `约束范围=全局` 且 `约束对象=EOS` 的行
2. `业务域约束`：读取分层约束表中 `约束范围=业务域` 且 `约束对象=目标 IPO 所属业务域/配置能力类` 的行
3. `IPO 专属约束`：读取分层约束表中 `约束范围=IPO` 且 `约束对象=目标 IPO ID` 的行
4. 若同一 NFR 在三层同时命中，按 `IPO > 业务域 > 全局` 解释具体说明，但三层约束都保留在约束包中
5. 若约束包中存在 `影响级别=阻断型` 且该约束未明确或未完成，则本轮只允许形成阶段性方案，不得作为稳定方案推进门禁 1

---

## 六、质量门禁

- [ ] Step Start 路由正确（状态=待设计开发或迭代中）
- [ ] 目标 IPO 四要素完整可读
- [ ] IPO 类型判定有依据
- [ ] PA 组件方案已写入 07（状态=待确认方案）
- [ ] 闭环检查 5 项全部通过（或标注为迭代建议）
- [ ] 输出了人类可操作的审查指引

---

## 七、审查流程

### 审核要点

- [ ] IPO 类型判断正确
- [ ] 组件类型与 IPO 类型匹配（业务定义类→配置方案，引擎配置类→前后端）
- [ ] 每条需求条目分配到唯一 PA 组件
- [ ] 组件粒度合理、边界清晰
- [ ] NFR 约束映射完整
- [ ] 资产决策遵循"复用 > 改进 > 新增"

### 审查修复

人类在 `07-*.md` 反馈区按固定格式标注 `[修改-边界]` / `[修改-复用决策]` / `[修改-组件粒度]` / `[修改-NFR映射]` / `[修改-其他]` + 意见 → 状态变为迭代中 → 下次执行 Skill A 加载反馈并调整方案。

### 审查节奏建议

1. 人类优先审查 `组件边界` 与 `复用/改进/新增` 决策，不逐段重审设计推理
2. AI 每次只暴露最少必要信息，避免将串行等待时间浪费在人类重新理解上下文上
3. 同一节点重入时按"差异审查"进行，只审本轮修改点

---

## 八、产出物

| 文件 | 写入内容 | 路径 |
|------|---------|------|
| PA 架构定义 | 组件方案（状态=待确认方案） | `../../80-pl4eos-2-eosdata/07-eos-product-architecture.md` |

---

## 九、与 Skill B 的接口契约

| 字段 | 说明 |
|------|------|
| 门禁信号 | `07` PA 状态表：待确认方案 → 待资产落账（仅当方案状态=`可确认方案` 且人类 `[同意]` 后） |
| 传递内容 | Skill A 写入 07 的方案（组件树、定义、映射、NFR 决策） |
| Skill B 不重做设计 | 只做匹配/写回/格式化，不改变方案的设计内容 |

---

## 变更日志

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-06-17 | v4.3 | 修正门禁1路由时序：路由按当前方案评估输出；去除未定义的“待补充状态”；明确阶段性方案不得以 `[同意]` 放行；统一残留旧术语 |
| 2026-06-17 | v4.2 | 明确从 `wft04` 分层约束表装配 NFR 约束包，并区分阻断型 NFR 与阶段性方案 |
| 2026-06-17 | v4.1 | 增补门禁1前全局串行规则、快速预检、人机交互提效规范 |
| 2026-06-17 | v4.0 | 从统一文档拆出 Skill A：单节点处理 + 状态机路由 + 不对称设计逻辑 + 分解分配分析定位为设计框架 |
