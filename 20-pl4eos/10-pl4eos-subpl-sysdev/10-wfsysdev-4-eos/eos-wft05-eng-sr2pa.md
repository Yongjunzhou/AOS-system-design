# SR-F(A1) → PA · 引擎前后端组件设计
**Skill ID**: wft05-eng-sr2pa

> **概述**：接收引擎操作页面 SR-F，经前置校验、材料加载和反馈处理后，将操作页面、操作活动、操作页面组件/构件、NFR 约束和 CU 追溯展开为 PA 前后端组件方案。该 PA 以“前后端组件”为末级节点，回答“为了支撑这些操作页面、页面组件、构件和操作活动，需要哪些前端组件、后端组件及其架构关系”。
>
> **输入 → 输出**：SR-F 操作页面节点（`待产品架构` / `待补充PA设计`）/ 有反馈或退回的 PA 节点 → PA 前后端组件方案 + SR 详细定义 + SR-F 推进为 `已产品架构` + PA 组件进入 `待追溯验证`
>
> **适用场景**：瀑布式系统设计第5步 · 平台能力系统设计路径。
>
> **设计模式参考**：[91-eos-biz-eng-spec.md](91-eos-biz-eng-spec.md)
>
> **版本**：v4.2 | **修订**：2026-07-08

---

## 一、职责定位

### 1.1 本 Skill 目的与目标

> **主要目的**：取信于开发方——「能开工」。把已确认的引擎操作页面 SR-F 翻译为可实现、可追溯、可验证的前后端组件方案。

**目标**：

1. **基于操作页面 SR 及 NFR 推断平台架构**——判断组件属于引擎专属、页面组件扩展、公共构件、公共平台服务或运行支撑模块。
2. **基于操作页面及活动推断前端组件**——从页面类型、操作页面组件、公共构件、按钮/右键/辅助栏/弹窗等推断前端组件。
3. **基于操作活动推断后端组件**——从校验、保存、发布、装载、回滚、审计、运行处理等活动推断后端组件。
4. **推断前端组件架构**——定义前端组件职责、组合关系、状态管理、交互边界和复用边界。
5. **推断后端组件架构**——定义后端组件职责、服务边界、依赖关系、运行约束和 SR 详细定义。

本 Skill 负责：

1. **接收**——校验 SR-F 状态、类型、页面、操作活动、操作页面组件/构件线索、CU 追溯和 PA 层收敛项；缺失时退回 wft03-eng。
2. **归属**——确定组件的平台架构归属和复用层级。
3. **前端设计**——从操作页面组件与构件推断前端组件。
4. **后端设计**——从操作活动和系统处理要求推断后端组件。
5. **架构装配**——组织前后端组件关系、依赖链、接口边界、状态流和错误处理。
6. **NFR 装配**——消费适用于 eng 组件的性能、安全、权限、审计、可用性、可观测性等约束。
7. **SR 详细定义**——将已确认的组件需求写入 `06-*.md`，形成可验证需求条目。
8. **推进**——人类确认且满足冻结条件后，SR-F → `已产品架构`，PA 组件 → `待追溯验证`。
9. **回修**——接收验证或构件开发退回的 `需wft05修订` PA，按缺陷说明修订组件边界、依赖关系、NFR 承接或 SR 详细定义；根因在 SR-F 时退回 wft03-eng。

本 Skill 不负责：

1. STR-E 场景业务识别（→ `wft01-eng`）。
2. CU 配置能力定义、CU 拆分/合并和依赖重切（→ `wft02-eng`）。
3. 操作页面、页面类型、页面功能架构和操作活动设计（→ `wft03-eng`）。
4. 业务功能表单或业务应用组件设计（→ biz 路径或构件开发流水线）。
5. 代码实现、测试用例、数据库物理表或字段级接口协议。

### 1.2 与上下游的衔接

**上游 wft03-eng**：产出操作页面 SR-F，包含页面类型、操作页面组件、构件线索、操作活动、权限/状态约束、PA 层收敛项、CU 追溯和变更声明。本 Skill 消费这些内容推导 PA 组件。若 SR-F 缺少页面、活动、构件线索或 PA 层收敛项，本 Skill 不临时补写，退回 wft03-eng。

**下游验证与构件开发**：PA 组件和 SR 详细定义经确认后进入追溯验证；验证通过后由构件开发流水线消费。若验证或开发发现 SR-F 缺陷，回退到 wft03-eng；若发现组件边界、依赖、NFR 承接或 SR 详细定义缺陷，将 PA 标记为 `需wft05修订` 并回到本 Skill 修订。

---

## 二、前后端组件设计基础

> 本章定义 wft05-eng 的操作对象：平台架构归属、前端组件、后端组件、组件架构、SR 详细定义、NFR 装配和冻结条件。操作页面组件与构件定义见 [91 规范 §三](91-eos-biz-eng-spec.md#三-操作页面组件与构件)，eng 路径分工见 [91 规范 §7.4](91-eos-biz-eng-spec.md#74-产品架构开发wft05-eng)。操作流程见 §三。

### 2.1 操作页面组件与构件

91 规范 §3 定义了操作页面组件和构件：

- **操作页面组件**：平台 UI 的结构层次，如多级横版、列表、单据、弹窗、指标、辅助栏等。
- **构件**：安装在操作页面组件上的公共功能单元，可跨组件复用，如功能按钮、右键菜单项、快捷键绑定、列定义、字段布局、图表类型、钻取等。

wft05-eng 的组件设计必须从上游 SR-F 中的页面类型、操作页面组件、构件和操作活动出发，不重新定义页面需求。

### 2.2 平台架构归属

| 归属 | 判定 | 典型组件 |
|------|------|----------|
| 引擎专属组件 | 只服务某个引擎或 CU 的配置/运行能力 | 流程发布服务、看板指标聚合服务 |
| 页面组件扩展 | 扩展列表、单据、弹窗、指标、辅助栏等页面组件 | 树表配置容器、配置详情辅助栏 |
| 公共构件 | 功能按钮、右键、快捷键、列定义、字段布局、图表、钻取等可复用构件 | 发布按钮、回滚按钮、引用钻取 |
| 公共平台服务 | 权限、审计、版本、发布、调度、通知等跨引擎能力 | 审计服务、版本服务、任务调度服务 |
| 运行支撑模块 | 监控、回滚、日志、错误恢复、任务执行等运行期支撑 | 运行日志模块、错误恢复模块 |

组件若可跨引擎复用，应优先归入公共构件或公共平台服务；若承接特定 CU 语义，则归入引擎专属组件。

### 2.3 前端组件

前端组件承载显示、交互、页面状态和用户操作入口。

| 来源 | 前端组件类型 | 设计要点 |
|------|--------------|----------|
| 页面类型 | 列表容器、树表容器、单据容器、弹窗容器、辅助栏、指标容器 | 容器职责、状态、上下文 |
| 操作页面组件 | 筛选区、列定义、字段组、图表区、详情面板、上下文摘要 | 组件组合与复用边界 |
| 构件 | 按钮、右键菜单、快捷键、钻取、导入导出入口 | 入口、可见性、可操作性 |
| 操作活动 | 进度反馈、结果提示、错误提示、二次确认、状态刷新 | 活动体验与状态反馈 |

### 2.4 后端组件

后端组件承载页面操作背后的校验、保存、发布、运行处理和治理能力。

| 操作活动 | 后端组件类型 | 设计要点 |
|----------|--------------|----------|
| 校验 | 配置校验、依赖校验、权限校验、冲突检查 | 校验边界、错误返回 |
| 保存 | 配置存储、版本记录、草稿管理 | 幂等、版本、一致性 |
| 发布/装载 | 发布编排、运行态生效、缓存刷新、影响范围计算 | 同步/异步、回滚点 |
| 回滚/停用 | 历史版本恢复、停用控制、回滚审计 | 安全边界、影响范围 |
| 审计/追溯 | 变更记录、操作日志、引用关系、运行日志 | 可追溯、查询性能 |
| 运行支撑 | 状态机、规则执行、调度、指标聚合、错误恢复 | 可靠性、可观测性 |

### 2.5 组件匹配判定

| 判定 | 条件 | 动作 |
|------|------|------|
| Q1 复用 | 已有组件同归属、同职责、同承接对象，NFR 不冲突 | 复用并追加 SR-F 引用 |
| Q2 扩展 | 已有组件主体职责一致，但需新增活动、NFR 或接口边界 | 扩展已有组件，全量重写受影响定义 |
| Q3 新建 | 无可复用组件 | 新建 PA 组件 |
| 需裁决 | 组件可能公共化、拆分/合并或跨引擎归属不明 | 标记 `[需裁决]` |

### 2.6 NFR 装配

NFR 只消费适用于 `eng` 或 `共享` 的约束，并按组件类型落位：

| NFR 影响点 | 承接位置 |
|------------|----------|
| 性能/并发 | 后端发布、装载、运行支撑组件；前端状态反馈 |
| 安全/权限 | 前端可见/可操作规则；后端鉴权与权限校验 |
| 审计/追溯 | 后端审计组件；前端引用关系/日志查询入口 |
| 可用性/可靠性 | 后端重试、补偿、回滚、降级 |
| 可观测性 | 运行日志、指标、告警、追踪 |
| 兼容性/易用性 | 前端容器、构件交互、提示和校验体验 |

阻断型 NFR 必须改变组件边界、接口或运行机制；只写备注不得进入正式冻结。

### 2.7 PA 节点与 SR 详细定义要素

每个 PA 组件节点至少包含：

- PA ID / 名称
- 来源 SR-F / 操作页面 / 操作活动 / CU 追溯
- 组件归属与组件类型
- 组件职责边界
- 承接的前端页面组件/构件或后端操作活动
- 前后端依赖关系
- 接口、事件或状态边界摘要
- NFR 承接
- SR 详细定义引用
- 25 / 23 引用
- 确认状态
- 生命周期状态

每条 SR 详细定义至少包含：

- 来源 PA / 来源 SR-F
- 系统职责
- 输入/触发
- 系统处理
- 输出/结果
- 约束与 NFR
- 验收口径

### 2.8 正式冻结条件

PA 组件可正式推进的条件：

1. 来源 SR-F 已确认，状态为 `待产品架构` / `待补充PA设计`。
2. 操作页面和操作活动均有唯一组件承接。
3. PA 层必收敛项已闭合。
4. 阻断型 NFR 已影响组件边界、接口或运行机制，而不只是备注。
5. 前后端依赖关系闭合，无未裁决循环依赖。
6. SR 详细定义完整、可验证。
7. 人类已确认全部 PA 条目。

不满足时只能形成阶段性 PA，不推进 SR-F 为 `已产品架构`。

### 2.9 生命周期与变更传播

SR-F 生命周期引用 [91 规范 §8.6](91-eos-biz-eng-spec.md#86-节点生命周期模型)。PA 在本 Skill 中分为“PA 方案确认态”和“PA 组件验证态”：方案确认态用于人类确认设计草案，组件验证态用于已确认方案进入追溯验证后的状态流转。

| 对象 | 状态 |
|------|------|
| SR-F | `待产品架构` → `在PA设计` → `已产品架构` / `待补充PA设计` / `需wft03修订` |
| PA 方案 | `待确认方案` → `已确认方案` / `待补充方案` / `需裁决` |
| PA 组件 | `待追溯验证` → `验证中` → `已验证冻结` / `需wft05修订` / `已废弃` |

变更传播引用 [91 规范 §8.7](91-eos-biz-eng-spec.md#87-变更传播协议)。wft05-eng 的变更影响判定：

| 变更 | 类型 | 处理 |
|------|------|------|
| 仅追加 SR-F 来源或证据 | 无变更 | 无需调整 PA |
| 新增页面、构件或操作活动 | 增量型 | 新增组件或组件能力 |
| 修改页面职责、活动规则、NFR 或 PA 收敛项 | 修订型 | 修订受影响组件 |
| 页面架构重组、组件拆分/合并、公共化归属变化 | 结构型 | 等人类裁决 |
| 验证或开发退回 PA | 返工型 | PA → `需wft05修订`，优先重入本 Skill |

---

## 三、操作流程

### Step 布局总览

| Step | 名称 | 产出 | 写资产 |
|------|------|------|--------|
| Start | 前置校验与路由 | 就绪 / 退出 | — |
| 1 | 设计材料加载 | SR-F / PA / NFR / 23/25 / 91 线索 | — |
| 2 | 方案反馈处理 | 调整后的 PA | `07` |
| 3 | 平台架构归属 | 组件归属与复用层级 | `07` |
| 4 | 前端组件设计 | 前端组件方案 | `07` |
| 5 | 后端组件设计 | 后端组件方案 | `07` |
| 6 | 组件架构与 SR 详细定义 | 依赖关系 + `06` 详细需求 | `06/07` |
| 7 | 闭环检查与冻结判定 | 覆盖自查 + 冻结/阶段性结论 | `07` |
| 8 | 资产写回与落账 | 状态推进 + 引用写回 | `05/06/07/23/25` |
| End | 运行结束输出 | 增量清单 + 下一步提示 | — |

### 3.1 Step Start

执行顺序遵循 [91 规范 §8.1.0](91-eos-biz-eng-spec.md#810-step-start-入口条件)：退回优先 → 反馈处理 → 新输入路由。

| 校验条件 | 判定 | 动作 |
|----------|------|------|
| 存在 `需wft05修订` PA 且有缺陷说明/改进方案 | 退回入口 | 优先进入本 Skill 修订；定位受影响 PA/SRD/SR-F |
| 存在 `需wft05修订` PA 但无缺陷说明/改进方案 | 退回缺指令 | 输出缺少的验证证据或开发反馈并退出 |
| 存在待处理 PA 反馈 | 反馈入口 | 进入 Step 2 |
| SR-F 类型非 eng 操作页面 | 类型不符 | 退出；提示回到对应路径 |
| SR-F 状态非 `待产品架构` / `待补充PA设计` | 状态不符 | 退出；列出状态分布 |
| 缺页面、构件线索、操作活动、CU 追溯或 PA 收敛项 | 上游缺口 | SR-F → `需wft03修订` 并退出 |
| 阻断型 NFR 未加载 | 约束不足 | 提示补充 NFR 包；必要时退出 |
| 选中 SR-F >5 个 | 超批量 | 建议分批；人类确认后可继续 |

### 3.2 Step 1 · 设计材料加载

读取：

1. `05-*.md`：SR-F 操作页面、操作活动、PA 收敛项、状态和版本。
2. `06-*.md`：已有 SR 详细定义。
3. `07-*.md`：已有 PA 组件、状态、确认状态。
4. `23-eos-output-architecture.md`：A1 产品与组件索引归属。
5. `25-eos-engine-models.md`：引擎、CU、页面组件和构件参考。
6. `22-eos-nfr-taxonomy.md` / wft04 输出：适用于 eng 组件的 NFR 约束。
7. `91-eos-biz-eng-spec.md`：操作页面组件、构件和 wft05-eng 分工。

### 3.3 Step 2 · 方案反馈处理

反馈协议引用 [91 规范 §8.5](91-eos-biz-eng-spec.md#85-skill-反馈交互协议)。

本 Skill 的实例化参数：

| 参数 | 内容 |
|------|------|
| 锚点需求 | SR-F 操作页面 |
| 操作条目 | 平台架构归属、前端组件、后端组件、依赖关系、NFR 承接、SR 详细定义 |
| 推进目标 | SR-F=`已产品架构`，PA=`待追溯验证` |
| 需求级联触发 | 组件职责变化、接口边界变化、NFR 承接变化、组件拆分合并 |
| 资产对齐级联 | 更新 `07` PA、`06` SR 详细定义、`05` SR-F 状态、`23/25` 引用 |

确认状态字段位于 PA 节点各独立条目中。条目粒度包括：组件归属、前端组件、后端组件、依赖关系、NFR 承接、SR 详细定义。

若入口为 `需wft05修订` PA，本 Step 先按缺陷说明判定根因：

| 根因 | 处理 |
|------|------|
| 组件边界不清、依赖不闭合、NFR 承接不足、SRD 不可验证 | 留在本 Skill 修订 PA/SRD |
| 页面职责、操作活动、构件线索或 PA 收敛项缺失 | SR-F → `需wft03修订`，退出本 Skill |
| CU 配置能力或依赖来源错误 | 退回 wft02-eng，退出本 Skill |

### 3.4 Step 3 · 平台架构归属

#### Phase A · 候选组件生成

从 SR-F 提取：

1. 页面类型和操作页面组件 → 前端容器/页面组件候选。
2. 构件线索 → 公共构件候选。
3. 操作活动 → 前端入口构件和后端处理组件候选。
4. PA 层收敛项 → 必须决策的组件边界、接口、存储、运行机制候选。
5. NFR → 可能改变归属或拆分的约束。

#### Phase B · 归属判定

按 §2.2 判断组件归属。判定优先级：

1. 已有公共构件/公共平台服务可复用。
2. 页面组件扩展可承载。
3. 引擎专属组件承接。
4. 运行支撑模块承接。
5. 仍不明确则标记 `[需裁决]`。

#### Phase C · 匹配已有 PA

按 §2.5 执行 Q1/Q2/Q3/需裁决判定。

### 3.5 Step 4 · 前端组件设计

对每个操作页面：

1. 从页面类型推断容器组件。
2. 从操作页面组件推断布局、字段、辅助栏、弹窗、指标等组件。
3. 从构件线索推断按钮、右键、快捷键、列定义、图表、钻取等公共构件。
4. 从操作活动推断前端状态、进度反馈、结果提示、错误提示和二次确认。
5. 装配前端 NFR：权限可见性、易用性、可访问性、兼容性、响应时间。
6. 写明组件职责边界、复用范围、依赖关系和验收口径。

前端组件输出字段：

- 组件名称 / 归属 / 类型
- 来源页面 / 操作活动 / 构件线索
- 职责边界
- 状态与交互
- 依赖组件
- NFR 承接
- 确认状态

### 3.6 Step 5 · 后端组件设计

对每个操作活动：

1. 推断校验、保存、发布、装载、回滚、审计、运行支撑等后端组件。
2. 识别服务边界、事件边界、状态变化和错误处理。
3. 装配权限、安全、审计、性能、可靠性、可观测性等 NFR。
4. 保留 CU、页面、操作活动追溯。
5. 标注是否同步/异步、是否需要任务、是否需要回滚点。

后端组件输出字段：

- 组件名称 / 归属 / 类型
- 来源活动 / 来源 CU / 页面
- 职责边界
- 服务或事件边界摘要
- 状态变化与异常处理
- 依赖组件
- NFR 承接
- 确认状态

### 3.7 Step 6 · 组件架构与 SR 详细定义

#### 3.7.1 组件架构

1. 组织前端组件架构：页面容器、公共构件、状态管理、交互依赖。
2. 组织后端组件架构：服务边界、数据/事件流、运行支撑依赖。
3. 建立前后端协作关系：页面动作 → 后端服务 → 状态/结果反馈。
4. 标记共享组件、专属组件和运行支撑组件。
5. 检查循环依赖、重复承接和无人承接。

#### 3.7.2 SR 详细定义

将组件需求展开为 `06` SR 详细定义：

```text
SRD-XXX：<组件/能力名称>
来源：SR-F-XXX / PA-XXX
系统职责：<系统必须提供什么能力>
输入/触发：<页面动作、事件、状态或配置>
系统处理：<校验、保存、发布、查询、回滚等摘要>
输出/结果：<页面反馈、状态变化、记录、事件>
约束与 NFR：<性能、安全、审计、可靠性等>
验收口径：<如何验证该需求被承接>
```

### 3.8 Step 7 · 闭环检查与冻结判定

检查：

| 检查项 | 判据 |
|--------|------|
| 页面覆盖 | 每个操作页面有前端组件承接 |
| 活动覆盖 | 每个操作活动有前端入口和后端处理承接 |
| 构件覆盖 | 每个构件线索有复用/扩展/新建结论 |
| PA 收敛 | 所有 PA 层必收敛项已闭合 |
| NFR 承接 | 阻断型 NFR 已改变组件边界、接口或运行机制 |
| 依赖闭合 | 前后端组件依赖无未裁决断点 |
| SRD 完整 | 每个 PA 组件有 SR 详细定义或明确无需 SRD 的理由 |

结论：

| 结论 | 条件 | 状态动作 |
|------|------|----------|
| 正式 PA | 全部检查通过 + 人类确认 | SR-F → `已产品架构`，PA → `待追溯验证` |
| 阶段性 PA | 有未确认 25 模型、可延后收敛项或非阻断缺口 | 保持待确认/待补充，不进入冻结 |
| 退回 wft03 | 页面、活动、构件线索或 PA 收敛项缺失 | SR-F → `需wft03修订` |
| 退回本 Skill | 验证或开发发现组件边界、依赖、NFR 或 SRD 缺陷 | PA → `需wft05修订` |
| 待裁决 | 组件公共化、拆分合并、阻断 NFR 承接存在架构判断 | 保持 `待确认方案` |

### 3.9 Step 8 · 资产写回与落账

仅对已确认且满足正式冻结条件的 PA 执行。这里的“已确认”对应 PA 方案确认态，写回后 PA 组件进入验证态：

1. `07`：写入 PA 组件方案，PA 组件 → `待追溯验证`。
2. `06`：写入 SR 详细定义。
3. `05`：SR-F → `已产品架构`，记录消费版本。
4. `23`：更新 A1 产品组件索引。
5. `25`：更新引擎/CU/构件引用，不反向改写未确认模型。

阶段性 PA 只写待办和缺口，不推进 SR-F 为 `已产品架构`。

### 3.10 Step End · 运行结束输出

遵循 [91 规范 §8.8](91-eos-biz-eng-spec.md#88-skill-通用约定) 的三区模板。

```text
当前状态：<待确认方案 / 已产品架构 / 阶段性PA>
  PA：新增/修订前端组件 <N> 个、后端组件 <N> 个；SR 详细定义 <N> 条；待裁决 <N> 项。

一、方案反馈
  A. 整体确认 -> 回复「整体确认」，快捷同意全部未标注条目
  B. 局部修改 -> 在确认状态中标注 [修改]：...
  C. 驳回重做 -> 在确认状态中标注 [驳回]：...

二、下一步
  本Skill -> 存在未处理反馈 / 新 SR-F / 待补充PA设计 / 需wft05修订 PA 时重入
  后续    -> PA 组件进入追溯验证；验证通过后进入构件开发流水线
```

---

## 四、AI 标记约定

AI 增量标记引用 [91 规范 §8.8](91-eos-biz-eng-spec.md#88-skill-通用约定)。本 Skill 的增量清单示例：

```text
### wft05-eng 增量清单
- [新增] PA-FE-FLOW-NODE-TABLE：流程节点配置表格组件
  来源 SR-F：SR-F-ENG-FLOW-PAGE-001
  承接页面/活动：流程节点配置页 / 编辑节点
  归属：页面组件扩展
  NFR：权限可见、审计记录
  确认状态：(空)
- [新增] PA-BE-FLOW-PUBLISH-SERVICE：流程配置发布服务
  来源 SR-F：SR-F-ENG-FLOW-PAGE-002
  承接活动：发布流程配置
  归属：引擎专属组件
  NFR：幂等、回滚、审计、可靠性
  确认状态：(空)
- [需裁决] 发布服务是否抽象为公共平台发布服务
  原因：流程、菜单、权限配置均可能复用发布/回滚机制
```

---

## 五、变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-07-08 | v4.2 | 基于全链审核修复：补充验证/构件开发退回后的 wft05 修订入口，拆分 PA 方案确认态与 PA 组件验证态，消除确认后状态语义倒挂。 |
| 2026-07-08 | v4.1 | 精修 wft05-eng：补充组件匹配判定、NFR 装配、前端/后端组件输出字段、组件架构、SR 详细定义模板、闭环检查与冻结判定；明确从操作页面 SR-F 到 PA 组件的可执行流程。 |
| 2026-07-08 | v4.0 | 按 91 规范 v3.15 重构；明确 wft05-eng 末级节点为前后端组件，职责聚焦操作页面、操作活动、操作页面组件与构件到前后端组件的 PA 推导，不再使用旧版配置单元展开口径。 |
