# 配置页面 SR-F + 两类约束包 → PA · 引擎三类组件设计
**人类方案ID**: wft05-eng-sfd2pa

> **概述**：承接 wft04-eng 产出的功能详细 SR-F（区域内布局组件/构件 + 组件业务 + PA 层收敛项）、FR-BIZ 指标约束包和 wft04-nfr 产出的 wft05-eng NFR 约束包，将组件业务展开为 PA 组件需求定义。PA 末级节点包含前端组件、后端组件、平台服务组件三类；同时承接业务 IPO 指标和非功能质量指标。
>
> **输入 → 输出**：功能详细 SR-F（`已详细定义`）+ FR-BIZ 指标约束包 + wft04-nfr 产出的 wft05-eng NFR 约束包 / 有反馈或退回的 PA 节点 → 三类 PA 组件需求定义 + 两类指标承接 + 四类平台架构决策 + SR 详细定义 + PA 组件进入 `待追溯验证`
>
> **适用场景**：瀑布式系统设计第5步 · 平台能力系统设计路径。
>
> **设计模式参考**：[91-eos-biz-eng-spec.md](91-eos-biz-eng-spec.md)
>
> **版本**：v10.6 | **修订**：2026-08-24

---

## 一、职责定位

**一句话**：本章说明本 Skill 的定位——要达成什么（1.1 目的与目标）、与上下游如何衔接（1.2）。

### 1.1 本 Skill 目的与目标

> **主要目的**：取信于开发方——「能开工」。把已确认的功能详细 SR-F、FR-BIZ 指标和 NFR 约束翻译为可实现、可追溯、可验证的三类 PA 组件需求及平台架构决策，不预设具体技术栈。

> **三层定位（按 EOS 流水线系统设计规范 §8.1）**：本 Skill 属于第1层（流水线设计）活动——在 EOS 流水线设计层将 wft04-eng 产出的功能详细 SR-F 展开为 PA 三类组件需求；产出物经追溯验证后交付第2层（构件开发），最终由第3层 EOS 开发者（确认者/用户）使用。本 Skill 的执行分工按 EOS 流水线系统设计规范 §8.3：流水线开发者（主导者/审核者）审核 AI 的 PA 组件方案并决策变更路径，AI（执行者）执行标准操作流程，EOS 开发者（确认者/用户）确认交付物可用性。

**目标**——完成以下七项即为本 Skill 使命达成：

| # | 目标 | 说明 |
|:-:|------|------|
| 1 | **加载并映射两类约束包** | 消费 FR-BIZ 指标约束包和 wft05-eng NFR 约束包——NFR 包逐行判定范围与消费对象；FR-BIZ 包逐行承接映射到组件（§5.4） |
| 2 | **基于组件业务推断前端组件** | 组件业务（布局+构件）→容器、交互构件（前端组件推断规则见 §3.3.1） |
| 3 | **基于组件业务推断后端组件** | 组件业务承载的操作活动→后端服务（后端组件推断规则见 §3.3.2） |
| 4 | **推断前端组件架构** | 职责、组合、状态、交互、复用 |
| 5 | **推断后端组件架构与 SR 详细定义** | 职责、边界、依赖、SRD（要素见 §3.3.3） |
| 6 | **制定平台架构并推断平台服务组件** | 部署/通信/数据/安全 + 平台服务组件 |
| 7 | **展开 SR 详细定义** | 三类组件→可验证条目 |

**本 Skill 负责**：

| # | 职责 | 动作 |
|---|------|------|
| 1 | **接收** | 只接收所属路径=`eng`、节点类型=`功能详细` 的 SR-F（含 PA 层收敛项），以及 FR-BIZ 指标约束包和 NFR 约束包；校验状态、包版本、消费版本和变更声明 |
| 2 | **归属** | 按引擎专属/布局组件扩展/公共构件/公共平台服务/运行支撑模块五类判定每个组件的实现组件归属和复用层级，优先复用已有公共能力 |
| 3 | **前端设计** | 从组件业务（布局组件+构件类型）推断前端容器组件、布局组件、交互构件和状态反馈组件，定义职责边界、组合关系以及 FR-BIZ/NFR 指标承接 |
| 4 | **后端设计** | 从组件业务承载的操作活动和系统处理要求推断后端校验、保存、发布、装载、回滚、审计、运行支撑等服务组件 |
| 5 | **NFR 映射与平台架构决策** | 逐约束行判定范围和消费对象，形成部署/通信/数据/安全决策 |
| 6 | **平台服务组件设计** | 定义职责、来源约束行、实现策略、依赖和消费方 |
| 7 | **架构装配与 SR 详细定义** | 组织三类组件依赖并形成可验证需求条目 |
| 8 | **推进** | 人类确认且满足冻结条件后 PA 组件 → `待追溯验证` |
| 9 | **回修** | 组件/NFR/平台架构缺陷留在本 Skill；组件业务或 PA 收敛项缺失退回 wft04-eng；约束包缺陷退回 wft04-nfr |

**本 Skill 不负责**：

| 职责 | 归属 |
|------|------|
| STR-E 引擎场景业务识别 | → `wft01-eng` |
| CU 操作活动定义 | → `wft02-eng` |
| 功能概要（功能表单清单/概要说明/操作活动清单/页面编排概要） | → `wft03-eng` |
| 功能详细（布局组件/构件/组件业务/PA 层收敛项） | → `wft04-eng`（本 Skill 只承接组件业务设计组件，不重新定义） |
| 业务功能表单、窗口标签页或其他业务侧页面组件设计 | → biz 路径或构件开发流水线 |
| 代码实现、测试用例、数据库物理表或字段级接口协议 | → 构件开发流水线 |

### 1.2 与上下游的衔接

**上游 wft04-eng**：产出功能详细 SR-F，包含区域内布局组件/构件、组件业务（承载操作活动/数据来源/交互行为/状态反馈）、PA 层收敛项（五类标记 `PA层收敛-*`，载件为主要来源）和 CU 追溯。本 Skill 据此推导前后端组件。若 SR-F 缺少组件业务或 PA 收敛项，退回 wft04-eng。

**跨路径输入 wft03-biz**：提供 FR-BIZ 指标约束包，将业务页面 IPO 指标分配到既有引擎/CU。能力和指标已覆盖的行用于 PA 指标承接；Q2/Q3/Q4 缺口行须关联 FR-ENG 候选线索 ID（`FR-ENG-REQ-*`）或正式 FR-ENG OR 或待裁决结论，不得在本 Skill 中自行补造业务需求。

**前置输入 wft04-nfr**：wft04-nfr 产出的首版 NFR 约束包是本 Skill 首次启动的**必备前置输入**——未取得已确认的首版约束包前不得开始产品架构设计。首版被消费后约束包版本递增可触发本 Skill 增量重入。

**下游验证与构件开发**：PA 组件和 SR 详细定义经确认后进入追溯验证；验证通过后由构件开发流水线消费。若验证或开发发现组件缺陷，回退到 wft04-eng；若发现组件边界、依赖、NFR 承接或 SR 详细定义缺陷，将 PA 标记为 `需wft05修订` 并回到本 Skill 修订。

---

## 二、基本概念：从功能详细 SR-F 到 PA 组件

**一句话**：本章讲**基本概念**——从功能详细 SR-F 到 PA 的核心产物是什么：PA 组件架构（2.1）、平台架构归属（2.2）、三类组件与 NFR（2.3）与 PA 层收敛项承接锚（2.4）；转换总览先给整条设计主线（五步一条链 + 规则地图）。设计原理与判据见 §三，节点演进见 §四，操作步骤见 §五。

> **概要/详细分工叙述**：eng 链五个 Skill 以「概要定义/详细定义」重述（2026-08-21 定案）——本 Skill 产出**产品概要定义**（05，末级=前后端组件+详细需求定义，07 PA），承接 wft04-eng 的**功能详细定义**（04，组件业务+PA 层收敛项）+ NFR。STR-E/BP/SR-F/PA 资产名保留，不迁移。05 从组件业务推断三类 PA 组件——前端组件承接配置展示/交互、后端组件承接配置处理与业务逻辑、平台服务组件承接全局/共享/业务域级 NFR。

### 转换总览：SR-F → PA

**一句话**：本节给整条设计的路线图——转换方法（五步一条链）与规则地图（判据索引）；转换的"为什么"见 §三 3.1 转换原理。

#### 转换方法：五步一条链

**输入**：功能详细 SR-F（`05-*.md`，状态=`已详细定义`）+ FR-BIZ 指标约束包 + wft05-eng NFR 约束包。
**输出**：PA 组件需求定义（`07-*.md`，三类组件），供追溯验证 / 构件开发流水线。

| 步 | 做什么 | 判据 / 细则 |
|----|--------|-------------|
| ① 路由 | 候选组件生成 + Phase A 宿主匹配（归入已有 PA → Phase B 修订 / 待聚合池 → Phase C 新建） | §5.4.1 |
| ② 映射 | NFR 约束逐行映射 + 平台架构归属判定（五类）+ 四类平台架构决策 | §3.2.2 / §3.2.3 |
| ③ 推断 | 前端组件 + 后端组件 + 平台服务组件推断，展开 SR 详细定义 | §3.3 / §5.5 |
| ④ 检查 | 闭环检查 + 冻结判定 + 变更影响声明 | §3.4 / §5.8 |
| ⑤ 写回 | 资产落账（05/06/07/23/25）+ 推进 SR-F/PA 状态 + 提交基线 | §5.9 |

> 读法：①~⑤ 是设计主线，具体判据在 §三 与 §五；可执行步骤见 §五 操作流程（Start/1/2/3/4/5/6/7/8/End）。

#### 规则地图：判据索引

| 判据 | 回答什么问题 | 位置 |
|------|-------------|------|
| PA 组件架构 | PA 组件的形态与要素是什么 | §2.1 |
| 平台架构归属 | 组件复用层级与架构位置怎么定 | §2.2 |
| 三类组件与 NFR | 三类组件如何定义、NFR 如何装配 | §2.3 |
| PA 层收敛项承接锚 | 设计输入的承接锚是什么（五类标记 + 载件来源） | §2.4 |
| 转换原理 | 为什么用三类 PA 组件承接功能详细 SR-F | §3.1 |
| 候选组件生成 | 从功能详细 SR-F 生成哪些组件候选 | §3.2.1 |
| 平台架构归属判定 | 五类归属怎么判 | §3.2.2 |
| 宿主匹配检查 | 复用/扩展/新建/冲突怎么分流 | §3.2.3 |
| 组件推断 | 前端/后端/平台服务组件怎么推断 | §3.3.1~§3.3.2 |
| 完备性判据 | PA 是否完备（含正式冻结条件） | §3.4 |
| 变更类型判定 | 增量/修订/结构/返工，怎么影响下游 | §4.2 |
| 闭环检查 | 设计输出是否漏做（Step 7 机械检查） | §5.8 |

**标注纪律**（框架全链一致，各 Skill 按域应用）：AI 设计产出中凡需人类确认/裁决的项，统一显式标注——①`[推断]`：AI 按规则推断、供人类确认或裁决（如组件推断存疑、NFR 承接边界）；②`[待下游推断]`：信息不足、跨 Skill 持久化待确认（本 Skill 承接已确认锚点，此类别少用）；③`供人类确认`：业务/权威锚点、必须由人类确认（本 Skill 对组件公共化/拆分合并/跨引擎归属不明标 `[需裁决]` 交人类裁决）。

---

### 2.1 PA 组件架构

**一句话**：本节讲 PA 组件架构的概念——定义与层级（2.1.1）、架构与属性（2.1.2）；完备性判据见 §三 3.4。

#### 2.1.1 定义与层级

PA 是 A1 平台引擎产品的产品架构，末级节点为**前端组件、后端组件、平台服务组件**。PA 从功能详细 SR-F、FR-BIZ 指标约束包和 NFR 约束包推导，回答各组件业务由什么组件承接、业务 IPO 指标和非功能质量指标由什么软件能力保障。布局组件与构件定义见 [91 规范 §四](91-eos-biz-eng-spec.md#四布局组件构件与路径边界)，eng 路径分工见 [91 规范 §8.5](91-eos-biz-eng-spec.md#85-产品概要开发wft05-eng)。wft05-eng 的组件设计从它们出发，不重新定义页面需求：

- **布局组件**：平台 UI 的结构层次（多级横版、列表、单据、弹窗、指标、看板页面、辅助栏等）
- **构件**：安装在布局组件上的公共功能单元（功能按钮、右键菜单、快捷键、列定义、字段布局、图表类型、钻取等）

> **04 vs 05**：04=功能组件业务（页面元素，布局组件/构件 + 组件业务），05=产品组件（前端/后端/平台服务，07 PA），**04 组件业务是 05 推断前后端组件的依据**。

#### 2.1.2 架构与属性

PA 组件架构由三类组件、四类平台架构决策、组件依赖和 SR 详细定义构成。

每个 PA 组件节点至少包含（写入 `07-*.md` PA 节点块）：

- PA ID / 名称
- 来源 SR-F / 功能表单 / 组件业务 / CU 追溯
- 组件归属与组件类型
- 组件职责边界
- 组件类型（前端/后端/平台服务）及所承接的构件、活动或 NFR 约束行
- 前后端依赖关系
- 接口、事件或状态边界摘要
- NFR 约束行 ID、实现策略与消费版本
- 可用性子目标 / 响应时间设计预算（分解运算与组件级预算，如适用）
- FR-BIZ 指标行 ID、承接组件与消费版本
- SR 详细定义引用
- 23 / 25 引用
- 确认状态
- 生命周期状态

---

### 2.2 平台架构归属

**一句话**：本节讲组件的复用层级判定——定义与五类归属（2.2.1）。

#### 2.2.1 定义与五类归属

平台架构归属判定组件的复用层级和架构位置。判定遵循两条核心规则：

- **归属优先级**：可跨引擎复用 → 公共构件/公共平台服务；承接特定 CU 语义 → 引擎专属组件；其余归入布局组件扩展或运行支撑模块
- **共享能力追溯**：已有独立 STR-E/CU 的共享能力 → 组件追溯到该共享 STR-E，消费引擎只记录依赖。无独立配置/治理场景的技术能力 → 不补建 STR-E，直接形成公共构件或平台服务组件

| 归属 | 判定 | 典型组件 |
|------|------|----------|
| 引擎专属组件 | 只服务某个引擎或 CU 的配置/运行能力 | 流程发布服务、看板指标聚合服务 |
| 布局组件扩展 | 扩展列表、单据、弹窗、指标、看板页面、辅助栏等布局组件 | 树表配置容器、配置详情辅助栏 |
| 公共构件 | 功能按钮、右键、快捷键、列定义、字段布局、图表、钻取等可复用构件 | 发布按钮、回滚按钮、引用钻取 |
| 公共平台服务 | 权限、审计、版本、发布、调度、通知等跨引擎能力 | 审计服务、版本服务、任务调度服务 |
| 运行支撑模块 | 监控、回滚、日志、错误恢复、任务执行等运行期支撑 | 运行日志模块、错误恢复模块 |

---

### 2.3 三类组件与 NFR

**一句话**：本节讲三类组件定义与 NFR 装配——前端组件（2.3.1）、后端组件（2.3.2）、NFR 装配（2.3.3）、平台服务组件与四类平台架构（2.3.4）。组件推断规则见 §3.3。

三类组件在架构层级的定义见 [91 规范 §8.5](91-eos-biz-eng-spec.md#85-产品概要开发wft05-eng) 组件类型注解。本节在其定义锚点之上，以操作视角展开组件推断规则：

- **前端组件**——UI 层实现单元，承载显示、交互和页面状态。
- **后端组件**——服务层实现单元，承载校验、保存、发布、运行处理。
- **平台服务组件**——基础设施层部署单元，承载全局/共享/业务域级 NFR。

#### 2.3.1 前端组件定义

前端组件承载显示、交互、页面状态和用户操作入口。

| 来源 | 前端组件类型 | 设计要点 |
|------|--------------|----------|
| 页面类别 | 配置维护、配置管理、发布生效、运行支撑、治理追溯容器 | 容器职责、状态、上下文 |
| 布局组件 | 多级横版、列表页面、单据页、辅助栏、弹窗页、指标页面、看板页面 | 布局组合与复用边界 |
| 构件 | 按钮、右键菜单、快捷键、钻取、导入导出入口 | 入口、可见性、可操作性 |
| 操作活动 | 进度反馈、结果提示、错误提示、二次确认、状态刷新 | 活动体验与状态反馈 |

#### 2.3.2 后端组件定义

后端组件承载页面操作背后的校验、保存、发布、运行处理和治理能力。

| 操作活动 | 后端组件类型 | 设计要点 |
|----------|--------------|----------|
| 校验 | 配置校验、依赖校验、权限校验、冲突检查 | 校验边界、错误返回 |
| 保存 | 配置存储、版本记录、草稿管理 | 幂等、版本、一致性 |
| 发布/装载 | 发布编排、运行态生效、缓存刷新、影响范围计算 | 同步/异步、回滚点 |
| 回滚/停用 | 历史版本恢复、停用控制、回滚审计 | 安全边界、影响范围 |
| 审计/追溯 | 变更记录、操作日志、引用关系、运行日志 | 可追溯、查询性能 |
| 运行支撑 | 状态机、规则执行、调度、指标聚合、错误恢复 | 可靠性、可观测性 |

#### 2.3.3 NFR 装配

NFR 仅从 wft04-nfr 产出的 wft05-eng NFR 约束包消费。首次消费全量加载；包版本递增时按变更影响声明处理增量、修订或结构型变化。每条约束保留约束行 ID、消费版本、范围和目标对象。约束行可携带「分解运算 + 父指标引用」（四类运算规则见 wft04-nfr §2.4.1）：可用性子目标（组件类型级）绑定到对应组件并作为验收目标；响应时间类 E2E 目标由组件设计预算（记 PA/SRD 作为设计决策）共同保证，约束包不硬切分。消费完成后将本 Skill 消费的包版本与消费状态（已消费）回写约束包头（wft04-nfr 定义的单写者字段）。

FR-BIZ 性能指标仅从 wft03-biz 产出的 FR-BIZ 指标约束包消费。每条指标保留来源 FR-BIZ、来源业务功能表单/窗口标签页、目标引擎/CU、适用条件、统计口径、测量方法、包版本和消费版本。FR-BIZ 指标与 NFR 指标分别承接、分别验证，不得合并改类。

| NFR 影响点 | 承接位置 |
|------------|----------|
| 性能/并发 | 后端发布、装载、运行支撑组件；前端状态反馈 |
| 安全/权限 | 前端可见/可操作规则；后端鉴权与权限校验 |
| 审计/追溯 | 后端审计组件；前端引用关系/日志查询入口 |
| 可用性/可靠性 | 后端重试、补偿、回滚、降级 |
| 可观测性 | 运行日志、指标、告警、追踪 |
| 兼容性/易用性 | 前端容器、构件交互、提示和校验体验 |

阻断型 NFR 必须改变组件边界、接口或运行机制；只写备注不得进入正式冻结。

#### 2.3.4 平台服务组件与四类平台架构

平台服务组件承接全局、共享和业务域级 NFR，是与前端组件、后端组件并列的第三类 PA 末级节点。每个组件至少定义：组件名称、职责、来源 NFR 约束行 ID、实现策略（自研/外购/云服务）、依赖组件、消费方、验收口径。

NFR 映射必须形成四类平台架构决策：

| 架构 | 决策内容 |
|------|----------|
| 部署架构 | 节点、集群、扩缩容、容灾与故障域 |
| 通信架构 | 协议、同步/异步、消息、超时、重试与幂等 |
| 数据架构 | 持久化、缓存、一致性、备份恢复、数据生命周期 |
| 安全架构 | 身份、权限、加密、审计、密钥与边界防护 |

---

### 2.4 PA 层收敛项承接锚

**一句话**：本节讲设计输入的承接锚——PA 层收敛项的四级设计分工（2.4.1）与组件承接映射（2.4.2）。PA 层收敛项是 wft04-eng 标注的待本 Skill 判断的问题线索，本 Skill 承接它设计组件。

#### 2.4.1 四级设计分工

功能详细 SR-F 与 PA 组件的边界由四级设计分工锚定：

| 层 | 工作 | 产出 |
|----|------|------|
| **wft01-eng 业务概要** | 从 STR-E 分解分配，锚定配置信息组（概要说明）清单 + 概要说明 | 配置信息组清单 + 概要说明 |
| **wft02-eng 业务详细** | 展开配置信息组操作活动定义 | 配置操作 / 系统处理 / 运行期能力（步骤化三段式） |
| **wft03-eng 功能概要** | 承接操作活动定义组织为功能表单 | 功能概要 SR-F（功能表单+概要说明） |
| **wft04-eng 功能详细** | 承接功能概要，展开组件业务并标注 PA 层收敛项 | 功能详细 SR-F（组件业务 + PA 层收敛项） |
| **本 Skill（wft05-eng）组件设计** | 承接功能详细 SR-F 与 PA 层收敛项，设计三类组件 | PA 组件需求定义 |

**PA 是 wft05-eng 的设计产出**——前端组件承接配置展示/交互，后端组件承接配置处理与业务逻辑。**PA 层收敛项是 wft04-eng 在组件业务条目中标注的「待 PA 设计判断的问题线索」**，五类标记：

| 收敛项 | 标注 | 本 Skill 承接 |
|--------|------|--------------|
| 组件边界 | `PA层收敛-组件边界` | 判定组件归属与边界（§2.2） |
| 接口问题 | `PA层收敛-接口` | 定义组件间接口契约 |
| 数据问题 | `PA层收敛-数据` | 决定持久化归属与数据架构 |
| 运行问题 | `PA层收敛-运行` | 决定同步/异步、回滚、缓存等运行机制 |
| NFR 问题 | `PA层收敛-NFR` | 消费约束包后承接约束 |

#### 2.4.2 组件承接映射

**载件是 PA 收敛项的主要来源**——wft02-eng 详设的配置信息组"系统处理·生效后响应"生成的载件（数据/配置/界面/构件/实例/支撑载件），经 wft04-eng 标注为 PA 层收敛项后由本 Skill 判断如何由前后端组件承接：

| PA 层收敛项来源 | 组件承接 |
|-----------------|---------|
| 组件边界（构件复用/归属待定） | 前端/后端组件归属判定 + 复用决策 |
| 接口（跨组件交互待定） | 组件间接口、事件、状态边界定义 |
| 数据（持久化能力待定） | 数据架构决策 + 后端持久化组件 |
| 运行（装载/缓存/回滚/重试机制待定） | 后端运行支撑组件 + 运行机制设计 |
| NFR（约束承接待定） | 平台服务组件 + 四类平台架构决策 |

> **边界**：本 Skill 承接 PA 层收敛项设计组件，不重新标注收敛项（发现收敛项缺失退回 `需wft04修订`）；不重新定义功能详细（组件推断必须基于组件业务，不预设技术栈、不越权设计）。

---

## 三、设计原理与判据：为什么这样设计、怎么判定

**一句话**：本章讲组件设计的逻辑——为什么用三类 PA 组件承接功能详细 SR-F（3.1 转换原理）、怎么提取组件要素（3.2）、怎么推断组件（3.3）、怎么判完备（3.4）；概念见 §二，节点演进见 §四，操作步骤见 §五。

### 3.1 转换原理：为什么用三类 PA 组件承接功能详细 SR-F

**功能详细 SR-F 描述"区域内由哪些组件构成、每个组件的业务是什么"，PA 回答"为支撑这些，需要哪些软件组件、由什么组件承接组件业务与操作活动、业务 IPO 指标和非功能质量指标由什么软件能力保障"**。wft04-eng 产出的功能详细 SR-F 是组件设计的输入锚——本 Skill 承接该 SR-F（含 PA 层收敛项），展开为三类 PA 组件需求定义，供追溯验证与构件开发。承接单位 = PA 组件（末级节点），前端/后端/平台服务是其细分类型。

**PA 层收敛项是待 PA 设计判断的问题线索，本 Skill 只承接设计组件**。wft04-eng 在功能详细 SR-F 的组件业务条目中标注 PA 层收敛项（组件边界/接口/数据/运行/NFR 的待判断问题），不设计组件；本 Skill 承接这些收敛项，给出组件归属、接口、存储、运行机制与 NFR 承接的结论。**载件（wft02 详设的组系统处理产物，经 wft03 功能概要、wft04 功能详细逐级承接）是 PA 收敛项的主要来源**——由本 Skill 判断如何由前后端组件承接：前端组件承接配置展示/交互，后端组件承接配置处理与业务逻辑。

**三类组件各司其职**。前端组件（UI 层实现单元）承接页面显示、交互与页面状态；后端组件（服务层实现单元）承接校验、保存、发布、运行处理与治理能力；平台服务组件（基础设施层部署单元）承接全局/共享/业务域级 NFR。FR-BIZ 指标与 NFR 指标分别承接、分别验证。

**推断不是转录，也不越权设计**。本 Skill 不只复述功能详细 SR-F，还要在组件上下文推断组件形态（归属/类型/职责边界/依赖）、制定平台架构决策；但组件推断必须有组件业务依据——组件对应关系不明确或组件业务缺失时退回上游，不静默补判。本 Skill 只定义需求，不设计实现方案、不写接口协议、不规定数据库表结构（→ 构件开发流水线）。

**这套原理方法为什么可行**：它以**可验证性**为锚——每个 PA 组件可由追溯验证验证、追溯链（组件→功能详细→功能概要→配置信息组→CU→BP→STR-E→OR）完整、变更可传播；权威归信息最全的一方且只归其信息全的部分（功能概要在 wft03、功能详细在 wft04、组件设计在本 Skill、验证在追溯验证）；判据全部可机械判定，操作全部有明确输入输出与执行模式（§5 Step 布局表），原理→判据→操作三层一一咬合，且与 91 规范、biz/nfr 路径同一套协议运转。

---

### 3.2 要素提取规则

**一句话**：本节讲从功能详细 SR-F 提取组件要素的规则——候选组件生成（3.2.1）、平台架构归属判定（3.2.2）、Phase B 组件匹配检查（3.2.3）。Phase B（修订已有 PA）与 Phase C（新建 PA）共用以下规则——先看 3.2.4 的 B/C 分工差异表，再读细节更顺。

#### 3.2.1 候选组件生成

从功能详细 SR-F 提取组件候选：

| 来源 | 生成的候选 |
|------|-----------|
| 组件业务（布局组件/构件类型） | 前端容器/布局组件候选 |
| 组件业务（承载操作活动） | 前端入口构件 + 后端处理组件候选 |
| PA 层收敛项 | 必须决策的组件边界、接口、存储、运行机制候选 |
| NFR | 可能改变归属或拆分的约束 |
| FR-BIZ 指标行（Q1 已覆盖） | 指标展示 → 前端组件；指标计算/聚合 → 后端组件；共享指标基础 → 平台服务组件 |

#### 3.2.2 平台架构归属判定

按 §2.2 判断每个候选组件的归属，判定优先级：

1. 已有公共构件/公共平台服务可复用。
2. 布局组件扩展可承载。
3. 引擎专属组件承接。
4. 运行支撑模块承接。
5. 仍不明确则标记 `[需裁决]`。

#### 3.2.3 Phase B 组件匹配检查

Phase B 修订已有 PA 时，按 `组件归属 + 组件职责 + 承接对象` 匹配已有 PA 组件：

| 结果 | 判定 | 动作 |
|------|------|------|
| Q1 复用 | 已有组件同归属、同职责、同承接对象，NFR 不冲突 | 复用并追加 SR-F 引用，标记 `[确认]` |
| Q2 扩展 | 已有组件主体职责一致，但需新增活动、NFR 或接口边界 | 扩展已有组件，全量重写受影响定义，输出变更声明 |
| Q3 新建 | 无可复用组件 | 新建 PA 组件 |
| 需裁决 | 组件可能公共化、拆分/合并或跨引擎归属不明 | 标记 `[需裁决]` |

#### 3.2.4 Phase B（修订）与 Phase C（新建）的提取差异

| 维度 | Phase B 修订已有 PA | Phase C 新建 PA |
|------|-------------------|-----------------|
| 判定范围 | PA 已承接的全部功能详细 SR-F + 本轮新功能详细 SR-F | 待聚合池本组功能详细 SR-F |
| 组件清单基线 | 以已有组件清单为基线，仅扫描本轮新组件业务是否引入新组件 | 从零构建 |
| 候选生成 | 增量生成（仅新组件业务触发） | 全量生成（§3.2.1） |
| 归属判定 | 增量判定 + 已有组件归属重检 | 全量判定（§3.2.2） |
| 组件匹配 | 执行组件匹配检查（§3.2.3），可能检出 Q1/Q2/Q3/需裁决 | 无需匹配，直接新建 |
| NFR 装配 | 增量合并新 NFR + 重检已有 NFR 承接 | 全量装配 |
| 变更传播 | 需输出变更影响声明（§4.2） | 写"首版，无变更" |

---

### 3.3 组件推断规则

**一句话**：本节讲组件推断的规则——前端组件推断（3.3.1）、后端组件推断（3.3.2）、SR 详细定义要素（3.3.3）、Phase B 组件关系检测（3.3.4）。Phase B（修订已有 PA）与 Phase C（新建 PA）共用以下规则。

#### 3.3.1 前端组件推断

对每个功能详细 SR-F 的组件业务：

1. 从组件业务（布局组件/构件类型）推断多级横版、列表、单据、辅助栏、弹窗、指标等前端容器或区域组件。
2. 从组件业务（交互类构件）推断按钮、右键、快捷键、列定义、图表、钻取等公共构件。
3. 从组件业务（承载操作活动）推断前端状态、进度反馈、结果提示、错误提示和二次确认。
4. 装配前端 NFR：权限可见性、易用性、可访问性、兼容性、响应时间。
5. 写明组件职责边界、复用范围、依赖关系和验收口径。

前端组件输出字段：组件名称 / 归属 / 类型 / 来源功能详细 / 组件业务 / 职责边界 / 状态与交互 / 依赖组件 / NFR 承接 / 确认状态。

#### 3.3.2 后端组件推断

对每个组件业务承载的操作活动：

1. 推断校验、保存、发布、装载、回滚、审计、运行支撑等后端组件。
2. 识别服务边界、事件边界、状态变化和错误处理。
3. 装配权限、安全、审计、性能、可靠性、可观测性等 NFR。
4. 保留 CU、功能表单、组件业务、操作活动追溯。
5. 标注是否同步/异步、是否需要任务、是否需要回滚点。

后端组件输出字段：组件名称 / 归属 / 类型 / 来源组件业务 / 来源 CU / 功能表单 / 职责边界 / 服务或事件边界摘要 / 状态变化与异常处理 / 依赖组件 / NFR 承接 / 确认状态。

#### 3.3.3 SR 详细定义要素

将已确认的组件需求展开为 SR 详细定义，写入 `06-*.md`。每条 SR 详细定义至少包含：

| 要素 | 说明 |
|------|------|
| 系统职责 | 系统必须提供什么能力 |
| 输入/触发 | 页面动作、事件、状态或配置 |
| 系统处理 | 校验、保存、发布、查询、回滚等摘要 |
| 输出/结果 | 页面反馈、状态变化、记录、事件 |
| 约束与 NFR | 性能、安全、审计、可靠性等 |
| 验收口径 | 如何验证该需求被承接 |

格式模板：

```text
SRD-XXX：<组件/能力名称>
来源：SR-F-XXX / PA-XXX
系统职责：<系统必须提供什么能力>
输入/触发：<页面动作、事件、状态或配置>
系统处理：<校验、保存、发布、查询、回滚等摘要>
输出/结果：<页面反馈、状态变化、记录、事件>
约束与 NFR：<性能、安全、审计、可靠性等>
验收口径：<如何验证该需求被承接>
```

#### 3.3.4 Phase B 组件关系检测

Phase B 修订已有 PA 时，组件匹配通过后，将本轮生成的组件与 PA 已有组件清单比对。比对锚点为**组件归属+组件职责+承接对象匹配**。

| 关系 | 判定 | 处理 |
|------|------|------|
| 确认 | 组件匹配 + 归属/职责一致 + 来源升级（SR-F 引用→新活动确认） | 追加 SR-F 引用，内部细分结论=`确认` |
| 无新增 | 组件匹配 + 归属/职责一致 + 所有来源已确认 | 增量合并新 SR-F 中的活动细节/NFR（如有新内容），内部细分结论=`改进` |
| 组件职责扩展 | 组件匹配 + 归属一致 + 新增活动、NFR 或接口边界 | 扩展已有组件，全量重写受影响定义 + 内部细分结论=`组件职责扩展` + 变更原因 |
| 归属变更 | 组件匹配 + **平台架构归属不同**（公共化或专属化） | 更新归属 + 内部细分结论=`归属变更` + 变更原因 |
| 补充 | 组件不匹配 | 新增 PA 组件，内部细分结论=`新增` |
| 过时 | 基线已有组件 **未被**本轮 SR-F 覆盖（页面/活动移除导致组件不再被需要） | 内部细分结论=`过时` + 追加说明"对应页面/活动已不再需要此组件，请确认是否移除"。保留该组件在清单中（不删除）——移除是架构决策，由人类在反馈中触发 |

关系检测前先基于**全部 SR-F 重新判定归属**——新 SR-F 可能改变组件特征导致归属变更（表中 `归属变更` 行）。

`[调整]` 是与关系类型正交的叠加标记：已有组件的职责边界/接口/NFR 承接变化，在关系标记之外叠加 `[调整]`。

---

### 3.4 完备性判据

**一句话**：本节讲 PA 完备性判定——覆盖检查（正式冻结前的逐项检查）与正式冻结条件。

**覆盖检查**：

| 检查项 | 判据 | 缺口处理 |
|--------|------|----------|
| 组件业务覆盖 | 每个组件业务有前端组件承接 | 补充前端组件 |
| 操作活动覆盖 | 每个操作活动有前端入口和后端处理承接 | 补充组件或标注 PA 收敛 |
| 组件覆盖 | 每个布局组件/构件有复用/扩展/新建结论 | 补充组件结论 |
| PA 收敛 | 所有 PA 层必收敛项已闭合 | 补充收敛，不推进冻结 |
| NFR 承接 | 阻断型 NFR 已改变组件边界、接口或运行机制 | 重新设计组件边界，只写备注不得冻结 |
| FR-BIZ 承接 | 约束包 Q1 行均有组件承接结论（承接组件/消费版本） | 补充组件承接或标记待正式 OR |
| 平台架构 | 部署/通信/数据/安全四类决策完整并可追溯 | 补充平台架构决策 |
| 依赖闭合 | 三类组件依赖无未裁决断点 | 补充依赖或标记 `[需裁决]` |
| 平台服务覆盖 | 全局/共享/业务域级约束均有平台服务组件或平台架构决策承接 | 补充平台服务组件 |
| SRD 完整 | 每个 PA 组件有 SR 详细定义或明确无需 SRD 的理由 | 补充 SRD |

**正式冻结条件**（全部满足方可推进 PA 为 `待追溯验证`）：

1. 来源功能详细 SR-F 已确认，状态为 `已详细定义`。
2. 组件业务、构件和操作活动均有组件承接。
3. PA 层必收敛项已闭合。
4. 阻断型 NFR 已影响组件边界、接口或运行机制，而不只是备注。
5. FR-BIZ 指标约束包 Q1 行均有组件承接结论（承接组件/消费版本），Q2/Q3/Q4 缺口行已关联 FR-ENG 候选线索或正式 OR 或待裁决。
6. 三类组件依赖关系闭合，无未裁决循环依赖。
7. SR 详细定义完整、可验证。
8. 人类已确认全部 PA 条目。

不满足时只能形成阶段性 PA，不推进 SR-F 为 `已产品架构`。

---

## 四、SR-F / PA 节点演进：生命周期与变更传播

**一句话**：本章讲 PA 节点设计与确认后的演进机制——生命周期（4.1，SR-F 侧与 PA 侧双生命周期主线 + 上游变更分流 + 本 Skill 写回范围）与变更传播（4.2，修订后写变更影响声明告诉下游）。

### 4.1 SR-F / PA 节点生命周期

wft05-eng 同时管理两条生命周期线：SR-F（上游节点，本 Skill 消费并推进）和 PA（本 Skill 产出节点，分方案确认态与组件验证态）。通用模型见 [91 规范 §A.6](91-eos-biz-eng-spec.md#a6-节点生命周期模型)。状态名映射按 §A.6 通用六态自声明（见下方主线流转与写回范围）。

**SR-F 侧**（wft04-eng ←→ wft05-eng）：

```
已详细定义 ──wft05锁定──→ 在PA设计 ──wft05完成──→ 已产品架构
     ↑                        ↑                         │
     │                        │               wft05退回  │
     │                        │                         ↓
     └─── wft04修订 ───── 需wft04修订 ←───────────────┘
```

**PA 侧**（wft05-eng → 验证 → 构件开发）：

```
待确认方案 ──人类确认──→ 已确认方案 ──冻结──→ 待追溯验证 ──验证──→ 已验证冻结
  │                       ↑            │             │
  │  需人工裁决            │  验证/开发退回│             │
  ├──→ 需裁决 ──── 裁决完成┘            ↓             │
  │                               需wft05修订 ←────────┘
  │                                    ↑
  └──── wft05 修订 ────────────────────┘
```

**上游变更**（wft04-eng 修订功能详细 SR-F 触发本 Skill 重入）——分两条线分流：

**SR-F 侧**（按本 Skill 消费进度分流）：

```
已详细定义 ──新功能详细修订──→ 已详细定义（版本递增，本 Skill 未消费）
在PA设计   ──新功能详细修订──→ 在PA设计（版本递增，本 Skill 增量处理）
已产品架构 ──新功能详细修订──→ 待补充方案 ──wft05补全──→ 已确认方案
```

**PA 侧**（本 Skill 产出节点随上游修订重验）：

```
已确认方案 ──新功能详细修订──→ 已确认方案（版本递增）
待追溯验证 ──新功能详细修订──→ 待追溯验证（版本递增，增量验证）
已验证冻结 ──新功能详细修订──→ 待补充方案 ──wft05补全──→ 已确认方案
```

**本 Skill 的写回范围**：PA 方案侧 `待确认方案` / `已确认方案` / `需裁决` / `待补充方案`；PA 组件侧 `需wft05修订`。`待追溯验证` / `验证中` / `已验证冻结` / `已废弃` 由验证和构件开发流程写入和推进。SR-F 侧 `已详细定义` 由 wft04-eng 写入，`在PA设计`（Step Start 锁定）`/已产品架构`（Step 8 推进）由本 Skill 写入。

下游退回 `需wft05修订` 时：读取验证或开发的缺陷说明 → 判定根因（组件边界不清、依赖不闭合、NFR 承接不足、SRD 不可验证 → 留在本 Skill 修订；组件业务/PA 收敛项缺失 → 退回 wft04-eng）→ 按人类改进方案修订 PA → 人类确认 → 重新推进。

### 4.2 PA 变更传播协议

变更传播协议的通用定义（变更类型分类、判定优先级、声明格式模板、增量清单与变更影响声明的分工）见 [91 规范 §A.7](91-eos-biz-eng-spec.md#a7-变更传播协议)。

**本 Skill 的变更影响声明格式**，每次修订已有 PA 后写入 PA 节点末尾（`07-*.md` 中 PA 节点块内）：

```text
### 变更影响声明

PA 版本：v{N}（本次修订前为 v{N-1}）
本轮变更类型：无变更 / 增量型 / 修订型 / 结构型 / 返工型

变更项：
- [新增] / [变更] / [确认] / [需裁决] ...
  说明字段中记录细分原因：组件新增 / 归属变更 / 组件职责扩展 / 过时 / 需裁决等

下游已消费此 PA 的验证/开发记录：
  <记录ID列表>（如无则写"尚未被下游消费"）

对已消费验证/开发的影响：
  受影响条目：<ID + 受影响原因>
  不受影响条目：<ID>
  待新增验证条目：<新组件对应的待验证说明>

建议动作：
  - 无变更：无需任何动作
  - 增量型：增量验证新增组件即可，已有验证记录不动
  - 修订型：修订受影响组件的验证 → 标记 [来自上游变更]
  - 结构型：输出影响范围，等人类裁决后再处理
  - 返工型：验证或开发退回，PA → 需wft05修订，优先重入本 Skill
```

---

## 五、操作流程

**一句话**：一次运行 = Step Start 入口路由 → Step 1 材料加载 → Step 2 反馈处理 → Step 3 约束映射与平台架构决策 → Step 4 前端组件设计 → Step 5 后端组件设计 → Step 6 平台服务与三类组件架构 → Step 7 闭环检查与冻结判定 → Step 8 资产写回 → Step End 输出与对话反馈。反馈走自然语言对话（§5.3），跨轮余留由 AI 标记持久化。

### Step 布局总览

| Step | 名称 | 职责 | 人类参与 | 执行模式 | 产出 | 写资产 |
|------|------|------|---------|---------|------|--------|
| Start | 前置校验与路由 | 变更感知 + 入口检测、输入校验、退回优先路由 | 无（AI 自动） | 统一 | 就绪 / 结束 | — |
| 1 | 设计材料加载 | 安全复查 + 读 AI 可以处理节点 + 加载 SR-F/PA/约束包/资产 | 无（AI 自动） | 统一 | SR-F / PA / 约束包 / 资产全景 | — |
| 2 | 方案反馈处理 | 读"待反馈处理"分节 + 对话处理人类修改意见 | **有**——对话反馈修改意见 / 整体确认 | 对话 | 调整后的 PA | `07` |
| 3 | 约束映射与平台架构决策 | Phase A 路由 / B 修订 / C 新建 + NFR 逐行映射 + FR-BIZ 逐行承接映射 + 归属判定 + 四类平台架构决策 | **有**——设计决策审核（组件归属/平台架构决策），对话反馈 | 统一（AI 产出带判据理由）+ 对话 | 约束行映射 + 部署/通信/数据/安全架构 | `06/07` |
| 4 | 前端组件设计 | 前端组件推断（自组件业务）+ 前端 NFR 装配 | **有**——设计决策审核（前端组件方案），对话反馈 | 统一（AI 产出带判据理由）+ 对话 | 前端组件需求 | `07` |
| 5 | 后端组件设计 | 后端组件推断（自组件业务承载操作活动）+ 后端 NFR 装配 | **有**——设计决策审核（后端组件方案），对话反馈 | 统一（AI 产出带判据理由）+ 对话 | 后端组件需求 | `07` |
| 6 | 平台服务组件与三类组件架构 | 平台服务组件推断 + 三类组件架构装配 + SR 详细定义 | **有**——设计决策审核（平台服务/SRD），对话反馈 | 统一（AI 产出带判据理由）+ 对话 | 平台服务组件 + SRD + 三类组件依赖 | `06/07` |
| 7 | 闭环检查与冻结判定 | 闭环检查 + 冻结判定 + 变更影响声明 | 无（AI 自动） | 统一 | 三类组件/NFR 覆盖 + 冻结结论 | `07` |
| 8 | 资产写回与落账 | 05/06/07/23/25 写回 + 推进 SR-F/PA 状态 + 提交基线 | 无（AI 自动，写回已确认节点） | 统一 | 状态推进 + 引用写回 | `05/06/07/23/25` |
| Step End | 运行结束输出 | 三区摘要输出 + 等待人类反馈 | **有**——查看摘要、对话反馈 | 对话 | 人类下一步操作提示 | — |

> **执行模式白话**：统一 = AI 自动跑完这步，不需你介入；对话 = AI 输出后需要你说句话（反馈或确认）才继续。Step 1/7/8 为运行内自动步骤——其中 **Step 3/4/5/6 是设计步骤（人机协同交互密度最高处）**：AI 承接功能详细 SR-F 与 PA 层收敛项，基于对配置需求与已有资产的语义理解产出组件方案并附判据理由，人类在 Step End/Step 2 对组件设计决策（归属/类型/职责边界/依赖/NFR 承接/平台架构）审核、思考、决策、反馈，AI 落实；Step 2 / Step End 为人机交互步骤（对话反馈，见 §5.3）。

**反馈机制一句话**：反馈走**自然语言对话**（AI 产出方案后不结束对话，§5.3）；跨轮余留反馈由 AI 标 `[修改]`/`[驳回]` 入"待反馈处理"分节（§5.3 状态机）；人类线下修订经 Step Start 变更感知（git diff，§5.1 变更感知）检出后纳入"待反馈处理"分节；AI 每轮写回提交基线（§5.9），保证下一轮 Step Start 的 `git diff <HEAD @上次 AI 运行>..HEAD` 只含人类变更。

---

### 5.1 Step Start · 前置校验与路由

> 入口条件与优先级判定遵循 [91 规范 §A.4](91-eos-biz-eng-spec.md#a4-入口条件与优先级step-start)。

**变更感知（检出人类线下修订，先于入口判定）**

读取目标文件前先检测自上次 AI 运行以来人类是否修改过文件（[§11.10](91-eos-biz-eng-spec.md#1110-ai-反馈检测规则)）：

1. 读取文件头 `**HEAD @上次 AI 运行**` 字段，获取上次提交 hash H
2. Bash("git diff H..HEAD -- 目标文件") → 返回 H 到当前 HEAD 之间的全部变更
3. 无变更 → 进入入口判定
   有变更 → 解析变更行号，读取变更行附近的节点块（上下 5 行）
   → 判定变更类型：结构化标注（`[同意]`/`[修改]`/`[驳回]`）→ 纳入待反馈；非标自由文本 → 标记 `[需确认]`；无关修改（格式/排版）→ 忽略
   → 将存在反馈的节点加入AI可以处理节点"待反馈处理"分节

> 变更感知必须先于入口判定——人类纯线下修订（不选新 SR-F）时，只有 git diff 能发现该反馈并将其纳入"待反馈处理"分节，入口判定②才命中。若入口判定先于变更感知，纯线下修订会被误判"无待处理对象"退出。

#### 5.1.1 入口检测

读取产品数据文档中的 `## AI可以处理节点` 对应分节（[91 规范 §11.7](91-eos-biz-eng-spec.md#117-ai可以处理节点)），检测三类入口条件：

| # | 入口条件 | 检测方式 | 对应分节 |
|---|---------|---------|---------|
| ① | **有新输入** | AI可以处理节点「待 wft05-eng 处理」分节非空 | §11.7 |
| ② | **有待处理反馈** | AI可以处理节点「待反馈处理」分节含本 Skill 产出节点 | §11.7 |
| ③ | **有下游退回节点** | AI可以处理节点「待下游退回处理」分节含状态为 `需wft05修订` 的节点 | §11.7 |

三类均不满足 → 输出"无待处理对象"→ **退出**。

**退回门禁**（满足条件③时）：检查退回节点的缺失说明中是否包含人类改进方案（人类标注 `[修改]`/`[同意]` 或下游 Skill/验证流程产出的具体改进指引）。已提供 → 通过门禁。未提供 → 输出缺失说明 + 提示提供改进方案 → **退出**。

#### 5.1.2 输入校验

| 校验条件 | 判定 | 动作 |
|---------|------|------|
| SR-F 的所属路径不是 `eng`，或节点类型不是 `功能详细` | 类型不匹配（硬退出） | 输出类型分布；业务功能表单、窗口标签页等业务侧页面组件不得进入本 Skill → **退出** |
| SR-F 状态不是 `已详细定义` | 状态不符（硬退出） | 输出不合格节点及当前状态 → **退出** |
| SR-F 缺少组件业务、PA 收敛项或 CU 追溯中任一项 | 要素缺失（硬退出） | SR-F → `需wft04修订`，输出缺失说明 → **退出** |
| PA 状态为 `需wft05修订` 且人类未提供缺陷说明或改进方案 | 退回方案缺失（硬退出） | 输出缺少的验证证据或开发反馈 → **退出** |
| wft05-eng NFR 约束包缺失、版本不可判定或变更声明缺失 | 约束包缺失（硬退出） | 输出约束包缺失说明，退回 wft04-nfr 补齐 → **退出** |
| 本轮涉及的 FR-BIZ 指标约束包缺失、版本不可判定或变更声明缺失 | 约束包缺失（硬退出） | 输出缺失说明，退回 wft03-biz 补齐 → **退出** |
| 首版约束包未确认，或存在未完成目标绑定的阻断型约束 | 绑定未完成（硬退出） | 输出未绑定约束行和目标线索，退回 wft04-nfr 完成绑定 → **退出** |
| 约束包存在待确认行但不影响当前组件 | 待确认不阻断（软提示） | 列出待确认行及影响范围 → 人类确认后继续或退出 |
| 选中 SR-F >5 个 | 超建议阈值（软提示） | 输出超限提示 → 人类确认后继续或退出 |

> 软提示不强制退出，由人类选择继续或退出；硬退出为逻辑上不可能的条件，输出后退出（§A.4.5）。

**锁定写回**：输入校验通过后，将承接的 SR-F 状态写为 `在PA设计`（锁定态，本 Skill 消费展开期间，对齐 91 §A.6 锁定态=下游写入 与 §4.1 主线流转）；完成三类组件设计且正式冻结后推进 `已产品架构`（§5.8）。

#### 5.1.3 分流处理

按 [91 规范 §A.4.4](91-eos-biz-eng-spec.md#a44-规则三优先执行顺序) 定义的优先顺序执行（退回优先 → 反馈处理 → 新输入路由）：

1. **退回优先**：存在 `需wft05修订` 的 PA 节点（且门禁通过）→ 定位受影响 PA/SRD/SR-F，进入 Step 2 按 §5.3.1 根因分流表判定处理
2. **反馈处理**：存在 `待确认方案` + 有人类反馈未处理的 PA 节点 → 进入 Step 2 处理（§5.3）。若全部条目已 `[同意]`+`[已处理]`，不阻塞新 SR-F，Step End 时提示
3. **新 SR-F 路由**：有未处理的 SR-F 节点 → 进入 Step 1→Step 3，逐节点执行 Phase A 宿主匹配（§5.4.1），归入已有 PA 或进入待聚合池新建

**顺序约束**：Step 2（反馈处理）必须先于 Step 3（新增设计），因为反馈可能改变 PA 的组件归属/组件职责/依赖关系/NFR 承接。Step 3 的 Phase A 组件匹配必须基于 Step 2 修改后的最新 PA 内容，不得使用缓存旧状态。

### 5.2 Step 1 · 设计材料加载

> 遵循 [91 规范 §A.3.3](91-eos-biz-eng-spec.md#a33-产品数据文档与资产文档读取与写回约定) 的统一读取协议。变更感知已在 Step Start 完成（§5.1）。

**步骤一：安全复查**

Step Start 已检出人类反馈并纳入"待反馈处理"分节（§5.1 变更感知）。此处按 §A.3.3 R0a 做加载前安全检查：`detect-changes.sh` 检出人类非预期修改时标记 `[需确认]`，不自动推进。

**步骤二：优先读取**（通过 `## AI可以处理节点` 定位本轮入口）：

入口判定通过后（§5.1.1），从 `## AI可以处理节点` 对应分节获取待处理节点 ID 列表：
- 有新输入 → 读「待 wft05-eng 处理」分节
- 有待处理反馈 → 读「待反馈处理」分节
- 有下游退回 → 读「待下游退回处理」分节

逐节点：Bash("grep -n '@{节点ID}' 目标文件") 定位行号 → Read 节点块。

**步骤三：索引导航**（AI可以处理节点无待处理时，或按域浏览时）：

1. Bash("grep -n '索引行数：' 目标文件") → 定位索引行号 L
2. 从 L 提取 N → Read 业务域索引
3. 按锚点 Read 域内索引 → 获取节点 ID 列表
4. 按需：Bash("grep -n '@{节点ID}' 目标文件") → 定位行号 → Read 节点

**加载清单**（按需扩大 - 仅在索引缺失、候选冲突或追溯关系断裂时）：

1. `05-*.md` SR-F 架构树画像/状态队列/节点索引 — 只定位本轮 eng 功能详细 SR-F 正文块（含组件业务、PA 收敛项）
2. `06-*.md` SR 详细定义索引 — 定位已有 SR 详细定义条目，去重校验
3. `07-*.md` PA 树画像/状态队列/节点索引 — 定位已有 PA 组件节点，匹配已有 PA
4. `23-eos-output-architecture.md`（按 A1 产品/组件索引筛选）
5. `25-eos-engine-models.md`（按目标引擎/CU/构件筛选，只读）
6. wft03-biz 产出的 `FR-BIZ 指标约束包`（`10-eos-fr-biz-constraint-package.md`，跨路径正式输入；读取包版本、来源业务页面、目标引擎/CU、指标和覆盖结论）
7. wft04-nfr 产出的 `wft05-eng NFR 约束包`（`11-eos-nfr-constraint-package.md`，正式输入；读取包版本、变更影响声明、约束行引用）
8. `22-eos-nfr-taxonomy.md`（仅用于解释约束分类，不替代约束包）
9. `91-eos-biz-eng-spec.md`（布局组件、构件、eng 路径分工）

**按需扩大规则**（[91 规范 §11.12](91-eos-biz-eng-spec.md#1112-按需读取规则)）：

- SR-F / PA 正文块 — 按 ID 从索引定位
- 候选 PA 块 — 按同归属/同组件职责/同承接对象筛选，用于 Q1/Q2 复用匹配
- 反馈重入 — 仅读取条目的确认状态字段及反馈指向块，不做全量重读
- 扩大触发：索引缺失 / 候选冲突 / 资产全貌不足 / 关系断裂时

### 5.3 Step 2 · 方案反馈处理

**一句话**：处理人类反馈——对话内即时处理（修改 / 整体确认，§5.3.1），跨轮余留按 `[修改]`/`[驳回]` 标记处理（§5.3 状态机）。

人类反馈的通用协议见 [91 规范 §A.5](91-eos-biz-eng-spec.md#a5-反馈交互协议)（反馈通道双轨：对话 + 线下修订）。以下为本 Skill 的特化规则。

**反馈双轨**——人类可任选两种方式反馈，本 Skill 均支持：

- **AI 对话反馈**：Step End 后不结束对话，直接在对话中提出修改意见（自然语言）——AI 按 §5.3.1 即时处理（主通道）
- **线下文档修订**：直接打开 `07-*.md` 修订——在确认状态字段标注 `[同意]`/`[修改]`/`[驳回]`，或直接编辑方案内容（自由文本）——经 Step Start 变更感知（git diff，§5.1 变更感知）检出后按 §A.5 处理

两轨等价：对话反馈由 AI 即时落账（追 `[已处理]`），线下修订经 git diff 检出后同样落账；AI 每轮写回提交基线（§5.9），保证下一轮 diff 只含人类变更。

**本 Skill 的操作条目**：平台架构归属 + 前端组件 + 后端组件 + 依赖关系 + NFR 承接 + SR 详细定义。各条目的 `确认状态` 字段位于 `07-*.md` PA 节点块各条目正文中。

**确认状态标记状态机**（本 Skill 反馈落账标记，定义单源；对话反馈写入者=AI，线下标注写入者=人类——经 git diff 检出后按 §A.5 处理）：

| 标记 | 含义 | 写入时机 | 转迁 |
|------|------|---------|------|
| `(空)` | 条目待人类反馈 | Step 3~7 产出时初始化 | 人类反馈后按情况转下 |
| `[修改]` | 人类提出修改意见、当轮未落地 | AI 在运行结束仍有余留修改意见时写入（跨轮持久化） | 下轮 Step 2 处理完追 `[已处理]` |
| `[驳回]` | 人类否决该条目方案、当轮未落地 | 同上 | 同上 |
| `[同意]` | 人类确认该条目 | "整体确认"时 | 随 `[已处理]` 一并追加 |
| `[已处理]` | 反馈已处理完毕（git 可追溯） | 处理完成后 | 默认跳过；满足清除条件（§5.3.1）时回到待处理 |

> 当轮对话内的反馈由 AI 即时处理（§5.3.1），不经 `[修改]`/`[驳回]` 态；`[修改]`/`[驳回]` 只在运行结束仍有余留反馈时出现，供下一轮 Step Start 检出（§5.1.1 入口②）。

**跨轮反馈持久化**：人类反馈（对话为主，§5.3.1）由 AI 当轮处理并追 `[已处理]`；若当轮对话结束仍有未落地的修改/驳回意见，AI 将对应条目标记 `[修改]`/`[驳回]` 并加入"待反馈处理"分节 → 下一轮 Step Start 检出（§5.1 变更感知）、Step 2 处理。AI 每轮写回后提交基线（§5.9），保证下一轮 Step Start 的 `git diff <HEAD @上次 AI 运行>..HEAD` 只含人类变更。

#### 5.3.1 AI 处理流程

按 [91 规范 §A.5](91-eos-biz-eng-spec.md#a5-反馈交互协议) 的统一流程处理人类反馈：扫描待处理条目 → 收集整体确认 → 逐条处理 `[同意]`/`[修改]`/`[驳回]` → 级联检查（需求优先、资产对齐随后）→ 判定节点状态 → 确定写回时机 → 落反馈处理记录。

| 人类输入 | AI 动作 |
|---------|--------|
| 显式修改指令（"X 改为 Y"） | 定位条目 → 理解修改意图 → 执行修改 → 检查是否仍满足 SR-F 需求 → 追加 `[已处理]` → 输出修改摘要 → 继续等待 |
| 质疑/讨论（"为什么选 X？"） | 解释理由，不自动修改，继续等待决策 |
| 整体确认（"整体确认"/"可以"） | 全部条目追加 `[同意] [已处理]` → 总结反馈要点写入 `## AI最近变更` → 满足冻结条件则推进 PA → `待追溯验证` → **提交基线并更新 HEAD（机制见 §5.9，防下轮把 AI 自写当人类变更）** |
| 模糊意见（"粒度太粗"） | 追问具体化，引导可执行指令 |
| 线下修订检出（git diff 发现确认状态标注/自由文本编辑） | 定位条目 → Edit 修订 → 追加 `[已处理]` → 输出修改摘要 → 继续等待 |

> **原则**：反馈处理以功能详细 SR-F 为锚点。AI 先理解反馈在功能详细需求语境中的含义，再执行修订，最后检查修订后的 PA 是否仍满足功能详细 SR-F 需求。

**本 Skill 反馈处理特化参数**（[91 规范 §A.5.4](91-eos-biz-eng-spec.md#a54-其他规则) 要求各 Skill 定义以下参数）：

| 参数 | 本 Skill 值 |
|------|-------------|
| **锚点需求** | 本 Skill 的锚点是功能详细 SR-F 的组件业务和 PA 收敛项——反馈处理以组件业务→组件映射需求为锚，不脱离功能详细 SR-F 内容自行修订 |
| **操作条目** | 平台架构归属 + 前端组件 + 后端组件 + 依赖关系 + NFR 承接 + FR-BIZ 指标承接 + SR 详细定义。各条目的 `确认状态` 字段写入 `07-*.md` PA 节点的对应条目中 |
| **推进目标** | 全部条目含 `[同意]`+`[已处理]` + 满足正式冻结条件 → PA → `待追溯验证`，SR-F → `已产品架构`。存在未闭合项 → 保持待确认态 |
| **清除条件** | 已有 `[同意]`+`[已处理]` 的条目默认跳过扫描。仅当本轮新输入满足以下任一条件时，清除 `[已处理]`（保留 `[同意]`）使其回到待处理：（1）新输入为该条目的直接上游节点（如新 SR-F 节点是 PA 组件的来源）；（2）新输入与条目同属一个架构层级且边界重叠（如同属前端组件，新组件改变了已有组件的职责或归属）；（3）新输入导致条目定义被修订（变更影响声明标注为修订型或结构型，§A.5.3 Step 1） |
| **级联触发条件** | 平台架构归属变更、组件新增/删除/职责扩展、依赖关系重组、阻断型 NFR 边界变化 → 触发需求级联检查（重验修改后 PA 是否仍满足功能详细 SR-F 的组件业务需求）→ 通过后触发资产对齐级联 |
| **资产对齐级联** | 方案变更影响 23/25 资产时，更新对齐结论——组件新增 → 更新 23 组件索引；CU/构件引用变更 → 更新 25 引用。资产映射由 Step 8 维护 |

若入口为 `需wft05修订` PA，本 Step 先按缺陷说明判定根因：

| 根因 | 处理 |
|------|------|
| 组件边界不清、依赖不闭合、NFR 承接不足、SRD 不可验证 | 留在本 Skill 修订 PA/SRD |
| 组件业务、PA 收敛项缺失 | SR-F → `需wft04修订`，退出本 Skill |
| CU 配置能力或依赖来源错误 | 退回 wft02-eng，退出本 Skill |

#### 5.3.2 确认状态格式示例

**AI 写入（Step 3~7 完成时）** —— 为每个条目初始化 `确认状态` 字段，初始为空：

```text
- 平台架构归属：流程配置列表容器 → 布局组件扩展 | 确认状态: (空)
- 前端组件：流程节点配置表格（列表容器，布局组件扩展） | 确认状态: (空)
- 后端组件：流程配置发布服务（引擎专属组件） | 确认状态: (空)
- 依赖：发布服务 → 校验服务 → 版本服务 | 确认状态: (空)
- NFR 承接：发布幂等、回滚审计、可靠性（阻断型） | 确认状态: (空)
- FR-BIZ 指标承接：订单及时率（Q1，指标计算/聚合→后端发布服务，消费版本 v3） | 确认状态: (空)
- SR 详细定义：SRD-FLOW-001 流程配置发布 | 确认状态: (空)
```

**人类在条目 `确认状态` 字段标注** —— 三种标注形式：

```text
- 平台架构归属：流程配置列表容器 → 布局组件扩展 | 确认状态: [同意]
- 前端组件：流程节点配置表格（列表容器，布局组件扩展） | 确认状态: [修改]：应拆为树表容器+节点配置单据页
- 后端组件：流程配置发布服务（引擎专属组件） | 确认状态: [驳回]：应复用公共平台发布服务
- 依赖：发布服务 → 校验服务 → 版本服务 | 确认状态: [同意]
- NFR 承接：发布幂等、回滚审计、可靠性（阻断型） | 确认状态: [同意]
- FR-BIZ 指标承接：订单及时率（Q1，指标计算/聚合→后端发布服务，消费版本 v3） | 确认状态: [同意]
- SR 详细定义：SRD-FLOW-001 流程配置发布 | 确认状态: [修改]：验收口径应补充并发场景
```

**AI 处理完成后** —— 在人类标注末尾追加 `[已处理]`：

```text
- 平台架构归属：流程配置列表容器 → 布局组件扩展 | 确认状态: [同意] [已处理]
- 前端组件：流程节点配置表格（列表容器，布局组件扩展） | 确认状态: [修改]：应拆为树表容器+节点配置单据页 [已处理]
- 后端组件：流程配置发布服务（引擎专属组件） | 确认状态: [驳回]：应复用公共平台发布服务 [已处理]
- 依赖：发布服务 → 校验服务 → 版本服务 | 确认状态: [同意] [已处理]
- NFR 承接：发布幂等、回滚审计、可靠性（阻断型） | 确认状态: [同意] [已处理]
- FR-BIZ 指标承接：订单及时率（Q1，指标计算/聚合→后端发布服务，消费版本 v3） | 确认状态: [同意] [已处理]
- SR 详细定义：SRD-FLOW-001 流程配置发布 | 确认状态: [修改]：验收口径应补充并发场景 [已处理]
```

#### 5.3.3 反馈总结

人类「整体确认」后，AI 将本轮反馈要点总结为一句话，通过 `update-meta.sh add-recent-change` 写入 `07-*.md` 的 `## AI最近变更` 表：

```bash
bash ../scripts/update-meta.sh ../../80-pl4eos-2-eosdata/07-eos-product-architecture.md add-recent-change "wft05-eng" "反馈处理" "<PA-ID>" "<AI总结的反馈要点>"
```

摘要示例：`组件归属调整：流程配置发布服务从引擎专属组件调整为公共平台发布服务；发布动作 NFR 承接补幂等与回滚审计`

### 5.4 Step 3 · 约束映射与平台架构决策

先加载 wft05-eng NFR 约束包并按消费版本分流：首次消费全量加载；版本递增时读取变更影响声明，仅处理新增、修订或结构型影响行。逐约束行判定约束范围和消费目标：

- CU 级约束 → 对应 CU 的前后端组件设计参数。
- PA 组件级约束 → 对应组件的并发、缓存、持久化等架构参数。
- 全局/共享/业务域级约束 → 平台架构决策和平台服务组件候选。
- 功能详细 SR-F 约束 → 对应前端、后端组件及交互边界。

输出部署、通信、数据、安全四类平台架构决策，并记录每项决策承接的 NFR 约束行 ID。对携带「分解运算」的约束行按类型处理：可用性子目标（乘法）绑定到对应组件类型并作为验收目标；响应时间类（加法）不硬切分，由组件设计预算（记 PA/SRD 作为设计决策）共同保证 E2E 目标。约束映射完成后，将本 Skill 消费的 NFR 约束包版本与消费状态（已消费）回写约束包头。

随后加载 FR-BIZ 指标约束包并按消费版本分流，逐行承接映射：Q1 已覆盖行判定承接组件归属（指标展示 → 前端组件、指标计算/聚合 → 后端组件、共享指标基础 → 平台服务组件），记录承接组件及包版本/消费版本到 PA 节点（§2.1.2）；Q2/Q3/Q4 缺口行标记为待正式 FR-ENG OR（或待裁决结论），不承接。完成两类约束映射后，再执行组件归属与候选匹配。

#### 5.4.1 Phase A：候选组件生成与宿主匹配

对每个功能详细 SR-F，从组件业务/PA 层收敛项/两类约束包提取组件候选（详见 §3.2.1），然后逐组件做宿主匹配：

| 条件 | 路由 |
|------|------|
| 组件归属 + 组件职责 + 承接对象落入已有 PA 组件 | 归入该 PA → Phase B |
| 不满足 | 进入待聚合池 → Phase C |
| 组件可能公共化、拆分/合并或跨引擎归属不明 | 标记 `[需裁决]` |

待聚合池按**同一归属 + 同一承接对象类型**分组（`{归属, 承接对象类型, 候选组件列表}`）。此阶段仅粗略分组，归属判定和完整要素提取在 Phase C 执行。

#### 5.4.2 Phase B：修订已有 PA

1. **要素提取**：从本轮功能详细 SR-F 的组件业务/PA 层收敛项/NFR 提取组件候选（详见 §3.2.1）。已有组件职责仅追加不重建
2. **组件匹配检查**（详见 §3.2.3）：确认 SR-F 需求仍在 PA 组件职责范围内

   | 匹配结果 | 动作 |
   |---------|------|
   | Q1 复用 | 追加 SR-F 引用 |
   | Q2 扩展 | 全量重写受影响定义 + 变更声明 |
   | 需裁决 | 标记 `[需裁决]` |
   | 职责扩展（Phase A 已匹配，但职责需扩展）| 不回退重选宿主，继续 Phase B + 标记 `[推断]` |

3. **归属/组件/NFR 重判定与关系检测**：基于全部 SR-F 和当前 NFR 约束包重新判定组件归属。新增/变更前端组件、后端组件按构件和活动展开；全局/共享/业务域约束映射到平台服务组件和四类平台架构决策；所有组件保留约束行追溯
4. **变更影响声明**：判定变更类型（无变更/增量型/修订型/结构型/返工型），写入 PA 节点末尾（详见 §4.2）

#### 5.4.3 Phase C：新建 PA

1. **判定平台架构归属** → 引擎专属/布局组件扩展/公共构件/公共平台服务/运行支撑模块五类（详见 §2.2、§3.2.2）。优先复用已有公共能力
2. **推断前端组件** → 组件业务（布局组件/构件类型）→ 多级横版/列表/单据/辅助栏/弹窗/指标 → 组件业务（交互类构件）→ 按钮/右键/快捷键/图表/钻取 → 组件业务（承载操作活动）→ 状态/反馈/提示（详见 §3.3.1）
3. **推断后端组件** → 校验/保存/发布/装载/回滚/审计/运行支撑 → 服务边界/事件边界/状态变化/同步异步/回滚点
4. **制定平台架构决策** → 部署/通信/数据/安全架构，每项引用来源约束行
5. **推断平台服务组件** → 职责/约束行 ID/实现策略/依赖/消费方/验收条件
6. **装配三类组件架构** → 前端组件组合 → 后端服务边界 → 平台服务依赖 → 循环依赖与无人承接检查
7. **展开 SR 详细定义并组装 PA** → 三类组件均写入 `06/07`，保留 NFR 消费版本和约束行追溯

### 5.5 Step 4 · 前端组件设计

Step 4 按每个功能详细 SR-F 实例独立执行。新建组件业务 → 全量设计；修订已有组件业务 → 仅当组件业务或 PA 收敛项变更时重验组件推断，要素机械检查始终针对变更组件业务执行。

**1. 前端组件推断**

基于 Step 3 确定的平台架构归属，对每个功能详细 SR-F 的组件业务逐层推断前端组件（详见 §3.3.1）：

- 页面级容器：从页面类别推断配置维护、配置管理、发布生效、运行支撑或治理追溯容器职责
- 布局组件实现：从多级横版、列表页面、单据页、辅助栏、弹窗页、指标页面、看板页面等布局组件需求推断前端容器与区域组件
- 交互构件：从组件业务（交互类构件）推断按钮、右键、快捷键、列定义、图表、钻取等公共构件
- 状态反馈：从操作活动推断前端状态（编辑中/校验中/提交中/已完成/异常）、进度反馈、结果提示、错误提示和二次确认
- 每个组件写明职责边界、复用范围、依赖关系和验收口径

**2. 前端 NFR 装配**

消费 wft04-nfr 产出的 eng 组件级 NFR 约束，逐组件装配（详见 §2.3.3）：

- 权限可见性：字段/按钮/操作的可见与可操作依据
- 易用性：操作步骤数、引导提示、帮助文案
- 可访问性：键盘导航、屏幕阅读器兼容
- 兼容性：浏览器/分辨率/语言
- 响应时间：前端交互反馈时限

阻断型 NFR 必须改变组件边界或交互设计，只写备注不得进入正式冻结（§5.8）。

**3. 组件定义要素**

每个前端组件写入以下完整要素（详见 §3.3.1）：

- 组件名称 / 归属 / 类型
- 来源功能详细 / 组件业务
- 职责边界
- 状态与交互
- 依赖组件
- NFR 承接
- 确认状态

所有组件标注 `[推断]`，经由人类确认后方可进入冻结判定（§5.8）。

**4. 要素机械检查**（逐页面，发现即标注——在对应条目中写入 `[覆盖缺口]` 及缺失说明，留待下一轮补全）

- [ ] 每个组件业务的容器组件 **已推断**
- [ ] 布局组件（布局/字段/辅助栏/弹窗/指标）**已逐项映射**到前端组件
- [ ] 组件业务（交互类构件）**已逐项映射**到交互构件
- [ ] 每个操作活动的前端状态和反馈 **已定义**
- [ ] 前端 NFR **已逐组件装配**，阻断型 NFR **已改变设计**（非仅备注）
- [ ] 每个组件的七项定义要素 **齐全**

### 5.6 Step 5 · 后端组件设计

Step 5 按每个组件业务承载的操作活动实例独立执行。新建活动 → 全量设计；修订已有活动 → 仅当系统处理要求或操作活动变更时重验组件推断，要素机械检查始终针对变更活动执行。

**1. 后端组件推断**

基于 Step 3 确定的平台架构归属，对每个操作活动逐类推断后端组件（详见 §3.3.2）：

- 校验组件：参数校验、业务规则校验、冲突检查、依赖检查
- 持久化组件：保存、更新、删除、版本管理
- 发布组件：发布、装载、生效确认、回滚
- 审计组件：操作记录、变更日志、合规追溯
- 运行支撑组件：异步任务、定时任务、缓存刷新、重试、异常恢复
- 服务边界与事件边界：识别每个组件的服务边界、事件边界、状态变化和错误处理
- 同步/异步标注：标注是否同步/异步、是否需要任务队列、是否需要回滚点
- 追溯保留：保留 CU、页面、操作活动追溯链

**2. 后端 NFR 装配**

消费 wft04-nfr 产出的 eng 组件级 NFR 约束，逐组件装配（详见 §2.3.3）：

- 权限：数据行级/字段级权限校验
- 安全：数据加密、注入防护、敏感信息脱敏
- 审计：操作可追溯、变更可审计、日志完整性
- 性能：响应时间、吞吐量、并发控制
- 可靠性：事务边界、幂等性、重试策略、故障恢复
- 可观测性：日志、指标、追踪、告警

阻断型 NFR 必须改变组件边界或服务接口，只写备注不得进入正式冻结（§5.8）。

**3. 组件定义要素**

每个后端组件写入以下完整要素（详见 §3.3.2）：

- 组件名称 / 归属 / 类型
- 来源活动 / 来源 CU / 页面
- 职责边界
- 服务或事件边界摘要
- 状态变化与异常处理
- 依赖组件
- NFR 承接
- 确认状态

所有组件标注 `[推断]`，经由人类确认后方可进入冻结判定（§5.8）。

**4. 要素机械检查**（逐活动，发现即标注——在对应条目中写入 `[覆盖缺口]` 及缺失说明，留待下一轮补全）

- [ ] 每个操作活动的校验/持久化/发布/审计/运行支撑组件 **已推断**
- [ ] 服务边界、事件边界、状态变化和错误处理 **已识别**
- [ ] 同步/异步、任务队列、回滚点 **已标注**
- [ ] CU→页面→操作活动→后端组件追溯链 **完整**
- [ ] 后端 NFR **已逐组件装配**，阻断型 NFR **已改变设计**（非仅备注）
- [ ] 每个组件的八项定义要素 **齐全**

### 5.7 Step 6 · 平台服务组件与三类组件架构

两个子活动依次执行：（1）推断平台服务组件；（2）装配三类组件架构。

**平台服务组件推断**：逐项消费 Step 3 中映射到全局/共享/业务域的约束。每个组件写明：名称、职责、来源约束行 ID、实现策略（自研/外购/云服务）、依赖、消费方、量化验收条件。无候选时必须给出"全部约束已由既有平台服务承接"的逐行引用，不得留空。

**三类组件架构装配**：按每个 PA 节点组织 Step 4~6 产出的组件。新建 PA → 全量装配；修订已有 PA → 组件清单、NFR 约束版本或依赖关系变化均触发重验。

**1. 组件架构组织**

将 Step 4~6 产出的前端、后端、平台服务组件组织为统一组件架构：

- 前端组件架构：页面容器→公共构件→状态管理→交互依赖的层次关系
- 后端组件架构：服务边界→数据/事件流→运行支撑依赖的拓扑关系
- 三类组件协作关系：页面动作 → 后端服务 → 平台服务 → 状态/结果反馈的端到端链路
- 组件分类标记：共享组件（跨 CU/页面复用）、专属组件（单 CU/页面专用）、运行支撑组件（不与页面直接耦合）
- 架构校验：检查循环依赖、重复承接和无人承接

**2. SR 详细定义**

将已确认的组件需求展开为 `06-*.md` SR 详细定义条目，每条按六要素格式写入（详见 §3.3.3）：

| 要素 | 说明 |
|------|------|
| 系统职责 | 系统必须提供什么能力 |
| 输入/触发 | 页面动作、事件、状态变化或配置变更 |
| 系统处理 | 校验、保存、发布、查询、回滚、审计等处理摘要 |
| 输出/结果 | 页面反馈、状态变化、持久化记录、事件通知 |
| 约束与 NFR | 性能、安全、审计、可靠性等约束的承接说明 |
| 验收口径 | 如何验证该需求被组件承接（可观察/可度量） |

每个 SRD 条目标注来源（SR-F ID / PA 组件 ID），确认状态与 PA 组件同步。只定义需求——不设计实现方案、不写接口协议、不规定数据库表结构。

**3. 要素机械检查**（逐 PA，发现即标注——在对应条目中写入 `[覆盖缺口]` 及缺失说明，留待下一轮补全）

- [ ] 三类组件架构层次关系 **已组织**，共享/专属/平台服务组件 **已分类**
- [ ] 三类组件协作链路（页面动作→后端服务→平台服务→状态反馈）**完整**
- [ ] 每个平台服务组件的 NFR 约束行 ID、实现策略、依赖和消费方 **完整**
- [ ] 循环依赖、重复承接、无人承接 **已检查**，问题项已标记
- [ ] 每个需 SRD 的 PA 组件 **已展开**为六要素 SR 详细定义条目
- [ ] 每个 SRD 条目来源追溯（SR-F ID / PA 组件 ID）**完整**
- [ ] 确认状态与 PA 组件 **同步**

### 5.8 Step 7 · 闭环检查与冻结判定

Step 7 按每个 PA 节点实例独立执行。新建 PA → 全量检查；修订已有 PA → 仅当组件清单、依赖关系或 SRD 条目变更时重验闭环，要素机械检查始终针对变更节点执行。

**1. 闭环检查**

对每个 PA 节点，按十项逐条检查（判据见 §3.4），发现遗漏仅标注、不回退——在对应条目中写入 `[覆盖缺口]` 及缺失说明，留待下一轮补全：

- [ ] 组件业务覆盖：每个组件业务有前端组件承接
- [ ] 活动覆盖：每个操作活动有前端入口和后端处理承接
- [ ] 组件覆盖：每个布局组件/构件有复用/扩展/新建结论
- [ ] PA 收敛：所有 PA 层必收敛项已闭合（组件边界/接口契约/数据存储/运行机制/NFR 承接）
- [ ] NFR 承接：约束包全部行均有消费结论，阻断型 NFR 已改变组件边界、接口或运行机制
- [ ] FR-BIZ 承接：约束包 Q1 行均有组件承接结论（承接组件/消费版本），缺口行已关联 FR-ENG 候选线索或正式 OR 或待裁决
- [ ] 平台架构：部署/通信/数据/安全四类决策完整并可追溯
- [ ] 平台服务：全局/共享/业务域约束均有平台服务组件或既有能力引用承接
- [ ] 依赖闭合：三类组件依赖无未裁决断点
- [ ] SRD 完整：每个 PA 组件有 SR 详细定义或明确无需 SRD 的理由

**2. 冻结判定**

闭环检查完成后，按以下条件分流。条件互斥，命中即止：

| 条件 | 动作 | 后续 |
|------|------|------|
| 全部检查通过 + 人类确认（**正式冻结**） | SR-F → `已产品架构`（Step Start 已锁定为 `在PA设计`），PA → `待追溯验证` | §5.9 资产写回 |
| 有未确认 25 模型、可延后收敛项或非阻断缺口（**阶段性冻结**） | 保持待确认/待补充 | 不推进冻结 |
| 组件业务/PA 收敛项缺失，根因在上游 | SR-F → `需wft04修订`（携带缺失说明） | 退回 wft04-eng |
| 验证/开发发现组件边界、依赖、NFR 或 SRD 缺陷 | PA → `需wft05修订` | 退回本 Skill |
| 组件公共化/拆分合并/阻断 NFR 承接需人类裁决 | PA → `需裁决`（等待人类标注 `[同意]`/`[修改]`/`[驳回]`） | 裁决后进入已确认方案 |

**3. 要素机械检查**（逐 PA，发现即标注——在对应条目中写入 `[覆盖缺口]` 及缺失说明，留待下一轮补全）

- [ ] 九项闭环检查 **已执行**，遗漏项已标注 `[覆盖缺口]`
- [ ] 冻结判定 **已分流**，无未裁决的边界情况
- [ ] PA 版本号 **已递增**（修订时），首版为 v1

**4. 变更影响声明**

每次新建或修订 PA 后写入变更影响声明；首版写"首版，无变更"。修订时按 §4.2 判定变更类型（无变更/增量型/修订型/结构型），列出受影响组件、SRD 条目和下游验证/构件开发建议动作。格式见 §4.2。

### 5.9 Step 8 · 资产写回与落账

> 遵循 [91 规范 §11.11](91-eos-biz-eng-spec.md#1111-ai-写回规则) 的统一写回协议。仅写回已确认且满足正式冻结条件的 PA 组件。不设二次确认。

**写回资产操作**：

1. `07` 写入 PA 组件方案，PA 组件 → `待追溯验证`
2. `06` 写入 SR 详细定义
3. `05` SR-F 节点写入 `SR-F 消费版本：v{N}`，状态推进为 `已产品架构`（Step Start 已锁定为 `在PA设计`）
4. `23` 更新 A1 产品组件索引
5. `25` 更新引擎/CU/构件引用，不反向改写未确认模型
6. 写入资产写回记录（日期/来源 SR-F/目标 PA 组件/动作/消费版本/下游状态）

**写回时的同步维护**（[91 规范 §11.11](91-eos-biz-eng-spec.md#1111-ai-写回规则) 要求每次写回同时维护以下内容）：

| 操作 | 同步更新 |
|------|---------|
| 新增/修改节点 | 更新域产品索引的 `节点数`；更新 `## AI可以处理节点` 对应分节——节点状态变更时移入或移出 |
| 修改节点状态 | 更新AI可以处理节点对应分节：状态符合消费条件时加入，不符合时移出 |
| 新增/删除子条目 | 更新父节点 `**子条目状态**` 汇总值 |
| 人类标注处理 | 同步更新父节点 `**子条目状态**`（子条目标注时）和 `**确认状态**`（父节点标注时）；AI可以处理节点"待反馈处理"同步 |

**提交基线**（链级不变式，每次写回后必须执行；顺序对齐 wft03-eng §5.7）：

1. 在 `## AI最近变更` 表追加记录（[91 规范 §11.6](91-eos-biz-eng-spec.md#116-ai最近变更)），含时间/操作 Skill/操作类型/节点ID/变更摘要，操作类型列含 `[→](#节点ID)` 超链接
2. 保留 AI最近变更最近 30 条，超出时从最旧行滚动删除
3. `git add -A` + `git commit`（带 Co-Authored-By: Claude），把本轮 AI 产出（PA 节点、SRD、05/23/25 写回、AI 可以处理节点维护、AI 最近变更）全部落基线——链级不变式：基线 = 最后 AI 提交，下一轮 Step Start 的 `git diff <HEAD @上次 AI 运行>..HEAD` 只含人类变更。反馈确认后的补充提交（§5.3.1 整体确认）同此机制
4. 更新文件头 `**文档版本**`（递增次版本号）、`**修订日期**`、`**HEAD @上次 AI 运行**`（=刚提交的基线 hash）

> **写回要求**：幂等去重、局部写回、全貌同步。所有操作在本 Step 内完成，不允许"本次写节点、下次更新索引"。

说明：阶段性 PA 只写待办和缺口，不推进 SR-F 为 `已产品架构`。资产写回是独立收尾，不构成下游验证或构件开发的前置条件。

### 5.10 Step End · 运行结束输出

> 遵循 [91 规范 §A.3.2](91-eos-biz-eng-spec.md#a32-step-end-输出模板三区结构) 的三区输出模板。

本 Skill 实例化如下：

```text
当前状态：<待确认方案 / 需裁决 / 已确认方案 / 阶段性PA>
  本轮 PA：<PA-ID / 新建或修订 / 版本变化 / 来源 SR-F>
冻结判定：<正式冻结（SR-F→已产品架构）/ 阶段性冻结（待办 N 项）/ 未冻结>
资产落账：<未落账 / 已写回 05/06/07/23/25>

一、方案反馈
  直接回复修改意见即可，例如：
    - "发布服务应复用公共平台发布服务"
    - "整体确认"（快捷同意全部未标注条目）
    - "前端组件方案调整交互结构"
  审核锚点：组件归属判定是否合理、三类组件职责/依赖是否完整、PA 层收敛项是否全部承接、阻断型 NFR 是否已改变设计
  可用性锚点：三类 PA 组件是否足以指导构件开发流水线？追溯链（组件→SR-F→配置信息组→CU→BP→STR-E）是否可追踪？SR 详细定义是否可验证？

二、下一步
  本Skill → 选择功能详细 SR-F 节点（`已详细定义`）或 `需wft05修订` 的 PA 节点重新运行
  后续    → PA-XXX（已确认方案）冻结后进入追溯验证 → 构件开发流水线
```

**输出后不要结束对话**，等待人类反馈——人类可直接用自然语言回复修改意见（§5.3.1）：

> 以上是本轮方案。直接回复修改意见即可：
> - "发布服务应复用公共平台发布服务"
> - "整体确认"
> - "前端容器组件拆分一下职责"

---

## 六、AI 标记约定

四类增量标记（`[新增]`/`[变更]`/`[确认]`/`[需裁决]`）的定义及使用规则见 [91 规范 §A.3.1](91-eos-biz-eng-spec.md#a31-ai-增量标记)。

每次 Step 3 完成后，向人类输出一份增量清单，格式示例如下：

```text
=== PA-FLOW-001 本轮更新清单 ===

[新增] 承接 SR-F-ENG-FLOW-PAGE-001, SR-F-ENG-FLOW-PAGE-002

前端组件
  [新增] "流程节点配置表格"（列表容器，布局组件扩展）  承接：流程节点配置页 / 编辑节点  NFR：权限可见、审计记录
  [新增] "流程节点树表"（树表容器，布局组件扩展）  承接：流程节点配置页 / 节点树导航

后端组件
  [新增] "流程配置发布服务"（引擎专属组件）  承接：发布流程配置  NFR：幂等、回滚、审计、可靠性

完备性检查
  页面覆盖 ✓ / 活动覆盖 ✓ / 构件覆盖 ⚠ / PA 收敛 ⚠ / NFR 承接 ✓
  → [需裁决] 发布服务是否抽象为公共平台发布服务（流程、菜单、权限均可能复用发布/回滚机制）
```

---

## 七、自检清单

**一句话**：每次运行收尾前按此清单逐项自查——AI 按 Step 分组核对是否漏做、漏写、漏提交。

**前置与加载（Step Start / 1）**
- [ ] Step Start 已按 §A.4 完成入口决策（退回优先 → 反馈处理 → 新输入），无待处理对象已退出
- [ ] 输入校验通过：SR-F 均为 eng 功能详细、≤5 个、状态符合（`已详细定义`）、含组件业务/PA 收敛项/CU 追溯
- [ ] Step Start 已变更感知（git diff，基线前提已满足）并判定变更类型，线下修订节点已纳入"待反馈处理"分节
- [ ] 已加载 SR-F / PA / FR-BIZ 指标约束包 / NFR 约束包 / 23/25/22/91，未默认加载全文件正文

**方案反馈处理（Step 2）**
- [ ] 对话反馈已按 §5.3 处理：修改指令 → 检查仍满足 SR-F 需求 → 追加 `[已处理]`；整体确认 → 推进 + 提交基线
- [ ] 反馈双轨（对话 + 线下修订）均落账；跨轮余留反馈已按状态机标记 `[修改]`/`[驳回]` 并加入"待反馈处理"分节
- [ ] 根因分流正确：组件边界/NFR/SRD 缺陷留本 Skill；组件业务/PA 收敛项缺失退回 wft04-eng；CU 来源错误退回 wft02-eng

**NFR 映射与组件设计（Step 3~6）**
- [ ] Phase A 宿主匹配正确（落入已有 PA → Phase B；否则待聚合池 → Phase C）
- [ ] NFR 约束已逐行映射，四类平台架构决策已输出并引用来源约束行
- [ ] 平台架构归属已判定（五类），PA 层收敛项已承接（载件为主要来源，不重新标注）
- [ ] 前端组件（组件业务布局/构件/承载操作活动三来源）已推断，七项定义要素齐全
- [ ] 后端组件（校验/保存/发布/装载/回滚/审计/运行支撑）已推断，八项定义要素齐全
- [ ] 平台服务组件与三类组件架构已装配，循环依赖/重复承接/无人承接已检查
- [ ] SR 详细定义已展开（六要素），来源追溯完整，与 PA 组件确认状态同步

**闭环检查与冻结（Step 7）**
- [ ] 九项闭环检查已执行（§3.4），遗漏项已标注 `[覆盖缺口]`
- [ ] 冻结判定已分流（正式冻结 / 阶段性冻结 / 退回 / 需裁决），PA 版本号已递增
- [ ] 变更影响声明已写入（首版"首版，无变更"）

**资产写回与落账（Step 8）**
- [ ] 07/06/05/23/25 已写回，SR-F/PA 状态已推进，消费版本已记录
- [ ] AI 最近变更表、AI 可以处理节点分节已维护
- [ ] 提交基线已执行（AI最近变更 → 滚动删除 → git commit → 更新文件头 HEAD=刚提交基线 hash）

**运行结束（Step End）**
- [ ] 三区摘要已输出（设计审核/可用性确认/下一步），对话等待反馈；整体确认后已提交基线

---

## 八、变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-08-24 | v10.6 | 文件名更名（人类定名 2026-08-24）——`eos-wft05-eng-sr2pa.md` → `eos-wft05-eng-sfd2pa.md`（功能详细定义→产品架构，eng 链 05 层）；人类方案ID 与全库引用同步。 |
| 2026-08-22 | v10.5 | eng 链第四轮深审修复（人类逐项裁决 2026-08-22）：①**A1** §3.4 覆盖检查表补「FR-BIZ 承接」「平台架构」两行、正式冻结条件补「FR-BIZ 指标约束包 Q1 行均有组件承接结论、Q2/Q3/Q4 缺口行已关联候选线索或正式 OR 或待裁决」、§5.8 Step 7 闭环标题「按九项」→「按十项」（与 §3.4 判据表对齐）；②**B1** §5.3.1 操作条目补「FR-BIZ 指标承接」、§5.3.2 确认状态示例补 FR-BIZ 条目（对齐 SKILL v1.7）；③**约束包设计**（人类逐项裁决 2026-08-22）：§2.3.3 补 NFR 约束包「分解运算+父指标引用」处理（可用性子目标绑定为验收目标 / 响应时间 E2E 由组件设计预算共同保证）+ 消费完成后回写消费版本与消费状态到约束包头（补 wft04-nfr 定义的单写者字段）、§5.4 Step 3 补同口径、§2.1.2 PA 节点字段补「可用性子目标/响应时间设计预算」、§5.2 加载清单补两约束包文件路径（`10-eos-fr-biz-constraint-package.md` / `11-eos-nfr-constraint-package.md`）。 |
| 2026-08-22 | v10.4 | eng 链第三轮深审修复（人类逐项裁决）：①**A3 FR-BIZ 指标约束包消费落地**——Step 3 补 FR-BIZ 指标行→组件承接映射（§5.4，Q1 行判定承接组件归属、Q2/Q3/Q4 缺口行标记待正式 OR）、PA 组件节点字段补「FR-BIZ 指标行 ID/承接组件/消费版本」（§2.1.2）、Step 7 闭环检查补「FR-BIZ 承接」项、§3.2.1 候选组件生成表补 FR-BIZ 来源行；②B6 §1.2 缺口行锚点放宽（候选线索 ID `FR-ENG-REQ-*`/正式 OR/待裁决 三可）；③C7 目标#1 拆分表述、Step 3 标题改「约束映射与平台架构决策」；④C4 概述/输入输出 NFR 约束包统一「wft05-eng NFR 约束包」。SKILL v1.7 同步。 |
| 2026-08-22 | v10.3 | eng 链深审修正（人类逐项裁决 2026-08-22）：①§4.1 上游变更补 **SR-F 侧**三分支分流（`已详细定义` 未消费版本递增 / `在PA设计` 锁定态增量处理 / `已产品架构`→`待补充方案`）——此前仅 PA 侧，SR-F 侧上游修订缺处理规则；②Step 布局表 Step 6 名称改「平台服务组件与三类组件架构」（对齐 §5.7 节标题）。 |
| 2026-08-22 | v10.2 | SR-F 锁定态闭环 + D2 口径（人类裁决 2026-08-22，对齐 91 §A.6 锁定态=下游写入，与 wft04 E5 模式对称）：①§4.1 写回范围补 SR-F 侧 `在PA设计`（Step Start 锁定）`/已产品架构`（Step 8 推进）由本 Skill 写入、§5.1 Step Start 补「锁定写回」（SR-F→`在PA设计`）、§5.8 冻结判定/§5.9 写回补锁定注；②§1.2「Q2/Q3 缺口行」→「Q2/Q3/Q4 缺口行」（wft03-biz 约束包含 Q4 行）。 |
| 2026-08-22 | v10.1 | 术语统一（机械项）：PA 层收敛项前缀 `PA层待处理-*`→`PA层收敛-*`（§2.4 承接表/上游衔接，对齐 91 §8.4 统一口径，与 wft04-eng v1.1 同步）。 |
| 2026-08-21 | v10.0 | **eng 链「概要/详细」分工框架实施第⑤步**（人类裁决：标题不动，镜像 biz；定案见记忆 [[eng-chain-five-layer-framework]]）——本 Skill 定位为**产品概要定义**（eng 链 05 层），**输入变**（SR-F → 功能详细+NFR）：①**§1 定位**——概述/输入→输出/§1.1 目的/三层定位/目标/负责/不负责：输入改「功能详细 SR-F（`已详细定义`）」（wft04-eng 组件业务是推断前后端组件的依据）、上游 wft03-eng 改 wft04-eng、§1.2 删「eng 路径无独立 wft04-eng」结构说明（04 已独立）、回修根因组件业务/PA 收敛项缺失退回 wft04-eng；②**§2 概念章**——章首补「概要/详细分工叙述」（05 承接 04 功能详细+NFR）、§2.1.1 补「04 vs 05」注、§2.4 PA 层收敛项承接锚改**四级设计分工**（wft03 功能概要/wft04 功能详细[组件业务+PA 收敛项]/wft05 组件设计）、PA 层收敛项由 wft04-eng 标注（自 wft03 下移 04）、边界退回改 `需wft04修订`；③**§3 原理判据**——3.1 转换原理改「承接功能详细 SR-F（组件业务）」、3.2.1 候选组件生成改「自组件业务（布局/构件类型+承载操作活动）」、3.3.1/3.3.2 组件推断改「对组件业务/组件业务承载的操作活动」、3.4 正式冻结条件 1 改来源状态 `已详细定义`；④**§4 生命周期**——SR-F 侧主线 `已详细定义 ──wft05锁定──→ 在PA设计`、上游变更/写回范围/退回根因同步；⑤**§5 操作**——Step 布局表 Step 4/5 补「自组件业务」、输入校验类型改 `功能详细`/状态改 `已详细定义`/要素缺失退回 `需wft04修订`、Step 3 Phase A/B/C 组件候选改自组件业务、Step 4/5 描述同步、闭环检查改组件业务覆盖/组件覆盖、冻结判定退回改 wft04-eng、Step End 入口状态改 `已详细定义`；⑥**§7 自检清单**同步。SKILL 同步（v1.4）。**v10.0 补充修正（2026-08-21 收尾联动核查）**：§2.4.1 四级设计分工表「锚定配置信息组结构」→「锚定配置信息组（概要说明）清单」1 处，对齐 v9.3 正名口径 |
| 2026-08-21 | v9.0 | 知识层三章重构 + 原理与方法审视 + PA 边界对齐（对标 wft03-eng v8.0，人类确认）——①**知识层三章**：§二 基本概念（转换总览：转换方法五步一条链 + 规则地图判据索引；概念 2.1 PA 组件架构 / 2.2 平台架构归属 / 2.3 三类组件与 NFR / 新增 2.4 PA 层收敛项承接锚）、新 §三 设计原理与判据（3.1 转换原理新增 / 3.2 要素提取规则[原 §2.4] / 3.3 组件推断规则[原 §2.5] / 3.4 完备性判据[原 §2.1.3 + 正式冻结条件]）、新 §四 SR-F/PA 节点演进（原 §2.6/§2.7）、原 §三 操作流程 → §五（3.x→5.x 重编号）、新 §七 自检清单；②**PA 边界对齐**：PA = 本 Skill 设计的三类组件（前端承接配置展示/交互、后端承接配置处理与业务逻辑），PA 层收敛项 = wft03-eng 标注的待本 Skill 判断的问题线索（五类标记，载件为主要来源），本 Skill 承接收敛项设计组件、不重新标注（§1.1 不负责表补 PA 层收敛项标注 → wft03-eng；§1.2 上游产出补 PA 层收敛项五类标记；§5.1.2 输入校验/§5.2 加载清单补 PA 收敛项消费）；③**方法层对齐**：Step 布局表补人类参与/执行模式列 + 白话注 + 反馈机制一句话 + 三角色分工表（按 EOS 流水线系统设计规范 §8.3）、§1.1 补三层定位（按 EOS 流水线系统设计规范 §8.1）、Step 2 补反馈双轨/确认状态标记状态机/跨轮反馈持久化/反馈总结（§5.3.3）、Step 8 提交基线顺序修正（AI最近变更→滚动删除→git commit→更新文件头 HEAD=刚提交基线，原顺序违反链级不变式）、Step End 补三角色四区（EOS 开发者可用性确认）、新增 §七 自检清单。链级：SKILL wft05 v1.1→v1.2 同步 |
| 2026-08-20 | v8.2 | 反馈检出前置到 Step Start（链级推广，人类确认六个 Skill 一起修）：①**Step Start 增加变更感知引导段**（无编号，先于入口判定）——git diff H..HEAD 检出人类线下修订，判定变更类型（结构化标注→纳入待反馈 / 自由文本→[需确认] / 格式→忽略）后加入"待反馈处理"分节，对齐 91 §11.10；②**Step 1 步骤一改安全复查**——变更感知已上移 Step Start，此处仅按 §A.3.3 R0a 做加载前安全检查（detect-changes.sh 检出非预期修改标记 [需确认]） |
| 2026-07-13 | v8.0 | 对齐 91 页面概念体系：输入统一为配置页面 SR-F；前端推导改按页面类别+布局组件+构件；平台架构归属中的页面组件扩展改为布局组件扩展。 |
| 2026-07-13 | v7.1 | 对齐 91 规范 v5.2：独立共享 STR-E 组件保持上游追溯；无独立配置场景的技术共享能力直接形成公共构件或平台服务组件。 |
| 2026-07-13 | v7.0 | 对齐 91 规范 v5.1：输入限定为 eng 平台操作页面 SR-F；新增 FR-BIZ 指标约束包，与 NFR 约束包分类型、分版本承接。 |
| 2026-07-12 | v6.1 | 对齐 91 规范 v5.0：首次启动前校验首版约束包已确认，且不存在未完成目标绑定的阻断型约束。 |
| 2026-07-12 | v6.0 | 依据 91 规范 v4.8 增加平台服务组件、NFR 约束包版本消费、逐行映射和部署/通信/数据/安全架构决策。 |
| 2026-07-12 | v5.3 | §3.3.1 反馈处理特化参数表新增清除条件（§A.5.3 Step 1）。 |
| 2026-07-12 | v5.2 | 对齐 91 规范 v4.8：纠正交叉引用（§三→§四 操作页面组件与构件、§7.4→§8.4 eng 路径分工）；§8.x→§A.x 引用体系迁移（§2.6→§A.6 生命周期、§2.7→§A.7 变更传播、§3.3→§A.5 反馈协议、§3.10→§A.3.2 Step End 模板、§四→§A.3.1 AI 标记）；§3.1 对齐 §A.4 入口决策流程（三类入口+AI可以处理节点检测+退回门禁+软硬退出分类）；§3.2 对齐 §A.3.3 读取协议（变更感知→AI可以处理节点优先→索引导航）；§3.3 新增本 Skill 反馈处理特化参数表（原 §8.5 实例化参数表移入 Skill 文档）；§3.9 对齐 §11.11 写回协议（同步维护 AI最近变更/AI可以处理节点/子条目状态/文件头字段+提交规范） |
| 2026-07-09 | v5.0 | 分工链对齐——与 91 规范 v3.18 / wft03-eng v5.0 同步：wft05-eng 输入从"操作页面 SR-F"转型为 **构件 SR-F**——构件是功能设计概念，前后端组件是实现设计概念。概述新增"构件→构件开发阶段"分界线说明。影响：概述（输入=构件清单）、§1.1 目的（构件→前端+后端组件）、§1.1 目标#2~#3（基于构件推断前端/后端）、§1.1 负责项#3~#4（构件类型→前端组件/后端组件）、§1.1 不负责项（构件识别→wft03-eng）、§1.2 上游（wft03-eng 产出构件清单） |
| 2026-07-09 | v4.8 | §3.10 Step End 风格对齐 wft02-biz：补"Step End"引导句+三区说明"（结果→方案反馈→下一步）。本 Skill 实例化如下："、当前状态修正为 PA 侧生命周期状态（已产品架构→已确认方案）+ 从 `<N>` 占位概括改为逐项结果枚举行（本轮 PA）+ 新增冻结判定行（wft05-eng 独有——Step 7 产出，正式冻结/阶段性冻结/未冻结）+ 新增资产落账行（05/06/07/23/25）、方案反馈 `->`→`→`、下一步从泛化描述改为具体 PA-ID + 状态 + SR-F 入口条件 + 冻结→验证→构件开发路径 |
| 2026-07-09 | v4.7 | §3.4 风格对齐 wft01-biz：三 Phase（A~C）→ 三编号子节（§3.4.1~§3.4.3）；Phase A 合并候选生成+归属判定+匹配为散文+路由表+聚合池（移除5项候选列表——见 §2.4.1、5级优先级列表——见 §2.4.2）；Phase B 新增为四步编号列表+`→`动作链+`（详见 §x.x）`引用（要素提取/组件匹配检查/重判定与关系检测/变更影响声明）；Phase C 新增为六步编号列表+`→`链+`（详见 §x.x）`引用（归属判定/前端推断/后端推断/架构装配/SRD展开/组装PA）；原 Phase C 一行引用 `按 §2.5 执行` 扩展为完整修订/新建双路径 |
| 2026-07-09 | v4.6 | §3.3 风格对齐 wft01-biz/wft01~03-eng：拆为 §3.3.1（AI 处理流程 + 根因分流表）+ §3.3.2（确认状态格式示例），实例化参数表移至 91 规范引用，新增 wft05-eng 特化示例（归属/前端/后端/依赖/NFR/SRD六类条目），根因分流表保留在 §3.3.1 中 |
| 2026-07-09 | v4.5 | §3.2 风格对齐 wft03-eng v4.5：开头改为"每次运行加载以下资产"、`—` 替换 `：`、05/07 补"树画像/状态队列/节点索引"模式+动作导向目的描述、06 补"SR 详细定义索引"标注+去重校验目的、23/25/22/91 改为括号标注筛选/只读模式（核心资产 vs 参考资产分层）、新增"按需扩大"段落（正文块/候选 PA/反馈重入/扩大触发）、简化 NFR 来源为 22 单一来源移除 wft04 or-条件 |
| 2026-07-09 | v4.4 | §二 风格对齐 wft01-biz/wft01~03-eng 第二章：移除章节引导段、拆为 7 节 16 子节（§2.1.1~§2.7）；定义与提取规则分离（§2.1~§2.3 定义"是什么"→ §2.4~§2.5 定义"怎么做"）；新增 Phase B/C 提取差异表（§2.4.4）+ 候选组件生成映射表（§2.4.1）+ Phase B 组件关系检测六种判定（§2.5.4）；原 §2.9 拆为 §2.6（双生命周期 ASCII 流转图——SR-F 侧+PA 侧+上游变更）和 §2.7（变更传播格式模板含返工型）；完备性判据从 §2.8 冻结条件+Step7 检查表合并为 §2.1.3；原 §2.1 操作页面组件与构件归入 §2.1.1；原 §2.7 SR 详细定义要素归入 §2.5.3；NFR 装配从 §2.6 归入 §2.3.3；归属判定优先级从 §3.4 Phase B 提取到 §2.4.2 |
| 2026-07-09 | v4.3 | §1.1 风格对齐 wft01-biz：主要目的改为"不仅…更…"递进式修辞、目标#5 合并后端架构与 SR 详细定义、负责项展开叙事细节（归属五分类/前端组件四来源/后端组件六活动/NFR 阻断规则/回修根因分流）、不负责项去句号 |
| 2026-07-08 | v4.2 | 基于全链审核修复：补充验证/构件开发退回后的 wft05 修订入口，拆分 PA 方案确认态与 PA 组件验证态，消除确认后状态语义倒挂。 |
| 2026-07-08 | v4.1 | 精修 wft05-eng：补充组件匹配判定、NFR 装配、前端/后端组件输出字段、组件架构、SR 详细定义模板、闭环检查与冻结判定；明确从操作页面 SR-F 到 PA 组件的可执行流程。 |
| 2026-07-08 | v4.0 | 按 91 规范 v3.15 重构；明确 wft05-eng 末级节点为前后端组件，职责聚焦操作页面、操作活动、操作页面组件与构件到前后端组件的 PA 推导，不再使用旧版配置单元展开口径。 |
