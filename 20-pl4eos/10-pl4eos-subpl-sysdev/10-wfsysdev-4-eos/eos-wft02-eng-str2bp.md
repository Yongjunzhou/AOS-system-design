# STR-E → BP · A1 配置单元详细定义
**人类方案ID**: wft02-eng-str2bp

> **概述**：接收 STR-E 平台能力场景业务，经前置校验、材料加载和反馈处理后，承接 wft01-eng 已分配的 CU，展开为 A1 配置单元 BP **业务详细定义**（eng 链 02 层）——操作活动定义。STR-E 主责能力域可以是引擎或平台共享能力域；共享 CU 只在其主责 STR-E 下定义，消费方通过依赖关系复用。本 Skill 的职责是**业务详细定义**——承接已分配 CU（配置信息组清单+结构判定级概要说明），展开为**操作活动定义**（配置操作/系统处理/运行期能力，步骤化三段式）+ 依赖 + 状态迁移；分解分配设计（概要末级→配置信息组（概要说明）→CU 承接，含收敛判定）由 wft01-eng（业务概要定义）完成。
>
> **输入 → 输出**：STR-E 节点（`可以详细定义` / `待补充详细定义`）/ 有反馈的 BP 节点 → A1 BP 方案（配置信息组 + 操作活动定义 + 依赖 + 状态迁移 + 详设缺口补需）+ STR-E 推进为 `已详细定义` + BP 推进为 `可以SR设计`
>
> **适用场景**：瀑布式系统设计第2步 · 平台能力系统设计路径。
>
> **设计模式参考**：[91-eos-biz-eng-spec.md](91-eos-biz-eng-spec.md)
>
> **版本**：v10.0 | **修订**：2026-08-21 | **说明**：eng 链「概要/详细」分工框架实施第②步（人类裁决：表格结构保留，三行升级步骤化）——本 Skill 定位为**业务详细定义**（eng 链 02 层，操作活动定义）：§2.2.1 详设形态对齐 **PDF v1.5.2** 步骤化基准（配置操作=完整步骤序列每步含对象+约束+联动、系统处理=**编辑时处理**（校验→写载件→阻断）+生效后响应（装载→界面反馈→运行期语义载件显式标注）、运行期能力=操作处理分支规则显式化、配置信息组标题=组名+字段全集含枚举），补**统一保存机制**（设计会话+全局保存+三类粒度：标量写会话/子表行行级保存/版本级动作全量校验）与系统标识生成规则；概念章补「概要/详细分工叙述」；下游引用随 03/04 改名联动核查

---

## 一、职责定位

**一句话**：本章说明本 Skill 的定位——要达成什么（1.1 目的与目标）、与上下游如何衔接（1.2）。

### 1.1 本 Skill 目的与目标

> **主要目的**：取信于平台配置方——「这个 CU 让配置人员配置什么、系统如何处理、运行期产出什么能力、需要达到什么指标」。
>
> 本 Skill 承接 wft01-eng 已分配的 CU，将其展开为 A1 配置单元 BP **业务详细定义**（eng 链 02 层）——**操作活动定义**：定义每个 CU 的配置信息组（配置操作/系统处理/运行期能力，步骤化三段式）、配置入口、依赖关系与状态迁移，识别详设层缺口并补需，为 wft03-eng 功能概要（功能表单）提供完整输入。

> **三层定位（按 EOS 流水线系统设计规范 §8.1）**：本 Skill 属于第1层（流水线设计）活动——在 EOS 流水线设计层将 STR-E 已分配 CU 展开为 A1 配置单元 BP 方案；产出物经 wft03-eng→wft05-eng 完整链条后，以设计规范/AI SKILL 形态交付第2层（构件开发），最终由第3层 EOS 开发者（确认者/用户）使用。本 Skill 的执行分工按 EOS 流水线系统设计规范 §8.3：流水线开发者（主导者/审核者）审核 AI 的 BP 方案并决策变更路径，AI（执行者）执行标准操作流程，EOS 开发者（确认者/用户）确认交付物可用性。

**目标**——完成以下四项即为本 Skill 使命达成：

| # | 目标 | 说明 |
|:-:|------|------|
| 1 | **承接 STR-E 的 A2 上下文 + 展开 A1 装配** | A2 侧：承接 wft01-eng 装配的业务配置人员→STR-E→配置活动上下文；A1 侧：流程与IT域→主责能力域→能力场景→已分配 CU 层级 |
| 2 | **详细定义已分配的 CU** | 承接 wft01 的配置信息组清单+结构判定级概要说明，展开每个 CU 的操作活动定义（配置操作/系统处理/运行期能力，步骤化三段式，基准=PDF v1.5.2）、配置入口、FR-ENG 性能指标与引擎状态迁移。配置入口只描述职责与对象，不设计配置页面类型、布局组件或构件 |
| 3 | **定义并校验依赖与闭合性** | 定义引擎内依赖、跨引擎依赖和治理依赖，确保详设层闭合 |
| 4 | **详设层缺口补需** | 发现配置能力、依赖或状态迁移缺口时，形成补充 FR-ENG 或退回 wft01-eng 修订承接 |

**本 Skill 负责以下操作**：

| # | 操作 | 说明 |
|:-:|------|------|
| 1 | **接收** | 接收人类选中的一批 STR-E 节点（≤5 个），校验状态、主责类型、唯一主责能力域、独立验收结果和已分配 CU 结构完整（CU 承接关系 + 配置信息组结构）；缺失时退回 wft01-eng |
| 2 | **匹配** | 按主责能力域和能力场景匹配已有 BP 节点；同域不同场景不得强行合并 |
| 3 | **承接已分配 CU** | 读取 STR-E 的 CU 承接关系（wft01-eng 已判定收敛/骨架/引擎归属），不重新收敛、不重新判定引擎归属 |
| 4 | **定义 CU 详细设计（操作活动定义）** | 配置信息组（承接 wft01 的清单+结构判定级概要说明，展开每组的配置操作/系统处理/运行期能力——步骤化三段式）、配置入口、系统处理（编辑时处理+生效后响应，统一保存机制）、运行期产出、FR-ENG 性能指标及引擎状态迁移 |
| 5 | **保留配置页面推导线索** | 记录配置角色、配置入口、活动顺序和运行期支撑需求，供 wft03-eng 功能概要（功能表单）组织页面——不形成页面设计结论 |
| 6 | **定义并校验依赖** | 定义引擎内依赖、跨引擎依赖和治理依赖（发布/版本/审计/回滚/监控），校验详设层闭合 |
| 7 | **详设层缺口补需** | 将配置能力、依赖或状态迁移缺口整理为补充 FR-ENG 材料（登记到 OR 原料状态表）；承接缺陷（CU 承接关系/配置信息组结构无法在该 CU 下定义）退回 wft01-eng（§5.5 1）；详设层变更（配置能力扩展）以 `[调整]` 标注（§3.3.3） |
| 8 | **推进** | 人类确认后：BP → `可以SR设计`，STR-E → `已详细定义`，记录消费版本 |
| 9 | **回修** | 接收 wft03-eng 退回的 `需wft02修订` BP，读取缺失说明，按人类方案修订，确认后重新推进为 `可以SR设计` |

**本 Skill 不负责**（由上下游承担）：

| # | 职责归属 | 承担方 |
|:-:|---------|--------|
| 1 | STR-E 的概要定义（场景边界/能力链）、分解（概要末级→配置信息组（概要说明））、分配（配置信息组→CU 承接，含收敛判定/骨架判定）、A2 上下文装配 | `wft01-eng` |
| 2 | A2/Bn 业务流程输出文档的完整设计 | `wft02-biz` |
| 3 | 配置页面、布局组件、构件、页面功能架构和操作活动设计 | 功能概要（功能表单+概要说明）→ `wft03-eng`；功能详细（功能表单组件及其业务）→ `wft04-eng` |
| 4 | 前后端组件、接口、数据库、服务边界设计 | `wft05-eng` |
| 5 | 将业务实例配置写入 25（25 是类型/模型事实源，本 Skill 的 BP 是场景化设计方案） | — |

### 1.2 与上下游的衔接

**上游 wft01-eng**：产出 STR-E 场景业务（**业务概要定义**，eng 链 01 层），包含主责类型、主责能力域、配置/治理目标、独立验收结果、能力链、概要末级节点、配置信息组（概要说明，结构判定级）、CU 承接关系（含收敛判定）和依赖 STR-E。本 Skill 承接已分配 CU，展开操作活动定义（配置操作/系统处理/运行期能力）/配置入口/依赖/状态迁移详细定义。若 CU 承接关系或配置信息组清单缺失，退回 wft01-eng。

**下游 wft03-eng**：消费 `可以SR设计` 的 BP 节点，承接配置操作/运行期能力组织**功能概要**（功能表单+概要说明），再经 wft04-eng 功能详细（功能表单组件及其业务）→ wft05-eng 产品概要（前后端组件）。若 CU 定义不足以组织功能表单，将 BP 退回为 `需wft02修订`。

**交付链条**（按 EOS 流水线系统设计规范 §8.1 三层框架）：本 Skill 产出（**业务详细定义**——A1 配置单元 BP 方案）→ wft03-eng（功能概要定义）→ wft04-eng（功能详细定义）→ wft05-eng（产品概要定义）→ **第2层交付**（设计规范/AI SKILL 形态交付至 EOS 设计空间）→ **第3层使用**（EOS 开发者使用交付物对 EOS 进行系统设计）。本 Skill 处于第1层中段——承接 STR-E 已分配 CU，展开为可被下游消费的配置单元 BP。

---

## 二、基本概念：从 STR-E 到 A1 配置单元 BP

**一句话**：本章讲**基本概念**——从 STR-E 到 BP 的核心产物是什么：A1 配置单元架构（2.1）、CU 配置能力（2.2）、A2 上下文与触发追溯（2.3）与 CU 依赖关系（2.4）；转换总览先给整条设计主线（五步一条链 + 规则地图）。设计原理与判据见 §三，节点演进见 §四，操作步骤见 §五。

> **概要/详细分工叙述**：eng 链五个 Skill 以「概要定义/详细定义」重述（2026-08-21 定案）——wft01-eng 产出**业务概要定义**（01，末级=配置信息组+概要说明，结构判定级）、本 Skill 产出**业务详细定义**（02，详细条目=**操作活动定义**：配置操作/系统处理/运行期能力，步骤化三段式，基准=PDF v1.5.2）、wft03-eng 产出**功能概要定义**（03，末级=功能表单+概要说明）、wft04-eng 产出**功能详细定义**（04，条目=功能表单上各组件及其业务）、wft05-eng 产出**产品概要定义**（05，末级=前后端组件+详细需求定义，07 PA）。STR-E/BP/SR-F/PA 资产名保留，不迁移。02 承接 01 的配置信息组清单+结构判定级概要说明，展开操作活动定义；03 承接 02 的操作活动组织功能表单，04 承接组件业务，05 承接前后端组件。操作活动定义对齐 biz s02「活动/正文条目/表单使用顺序」（配置字段清单=正文条目、操作步骤=活动、步骤顺序=使用顺序）。

### 转换总览：STR-E → BP

**一句话**：本节给整条设计的路线图——转换方法（五步一条链）与规则地图（判据索引）；转换的"为什么"见 §三 3.1 转换原理。

#### 转换方法：五步一条链

**输入**：STR-E 节点（`02-*.md`，状态=`可以详细定义`/`待补充详细定义`）。
**输出**：A1 BP 节点（`04-*.md`，业务流程架构），供 wft03-eng 展开为功能概要（功能表单）。

| 步 | 做什么 | 判据 / 细则 |
|----|--------|-------------|
| ① 路由 | 把 STR-E 归入已有 BP（→ Phase B 修订）或待聚合池（→ Phase C 新建） | §5.4.1 Phase A 宿主匹配 |
| ② 边界 | 确定 A1 路径与 A2 上下文——新建走能力域确定 + 承接 STR-E 的 A2 上下文，修订走宿主匹配检查 | §3.2.1 / §3.2.2 / §3.2.3 |
| ③ 要素 | 承接已分配 CU + 展开操作活动定义（配置操作/系统处理/运行期能力，步骤化）+ 定义并校验依赖 + 状态迁移 | §3.3 |
| ④ 检查 | 完备性：操作活动定义完整 + 配置入口/FR-ENG 指标 + 依赖闭合 + 详设缺口补需 | §3.4 / §5.5 |
| ⑤ 写回 | 资产落账（23/25）+ 推进 BP/STR-E 状态 + 提交基线 | §5.6 |

> 读法：①~⑤ 是设计主线，具体判据在 §三 与 §五；可执行步骤见 §五 操作流程（Start/1/2/3/4/5/End）。

#### 规则地图：判据索引

| 判据 | 回答什么问题 | 位置 |
|------|-------------|------|
| A1 配置单元架构 | BP 的形态与层级是什么 | §2.1.1 |
| 操作活动定义 | 每个 CU 定义什么（配置操作/系统处理/运行期能力，含配置信息组与 FR-ENG 指标） | §2.2.1 |
| CU 承接读取 | 已分配 CU 怎么承接、校验什么 | §3.3.1 |
| 操作活动定义 | 承接后怎么定义各组内容 | §3.3.2 |
| 详设变更处理 | 配置能力扩展/承接关系变更怎么处理 | §3.3.3 |
| 依赖校验四步 | 依赖怎么校验闭合 | §3.3.4 |
| 完备性判据 | BP 是否完备（含出口闸门） | §3.4 |
| 变更类型判定 | 增量/修订/结构，怎么影响下游 | §4.2 |
| 覆盖自查 | 设计输出是否漏做（Step 4 末尾机械检查） | §5.5 |

**标注纪律**（框架全链一致，各 Skill 按域应用）：AI 设计产出中凡需人类确认/裁决的项，统一显式标注——①`[推断]`：AI 按规则推断、供人类确认或裁决（如配置信息组/配置能力定义存疑）；②`[待下游推断]`：信息不足、跨 Skill 持久化待确认（本 Skill 承接已确认锚点，此类别少用）；③`供人类确认`：业务/权威锚点、必须由人类确认（本 Skill 对 CU 争用/承接冲突标 `[需裁决]`，配置能力扩展标 `[调整]` 交人类确认）。

**① 设计产物——A1 配置单元 BP 与 CU**

### 2.1 A1 配置单元架构（BP）

**一句话**：本节讲 A1 配置单元 BP 的概念——定义与层级（2.1.1）、节点要素与职责边界（2.1.2）；完备性判据见 §三 3.4。

#### 2.1.1 定义与层级

A1 是平台引擎产品，归属流程与 IT 域。构件为引擎，末级节点为 CU（配置单元）。A1 产品定义见 [91 规范 §5.1](91-eos-biz-eng-spec.md#51-a1产品)，CU 功能本质见 [91 规范 §3.2](91-eos-biz-eng-spec.md#32-引擎及层级) 与 [§4.3](91-eos-biz-eng-spec.md#43-cu-与布局组件及构件的对应关系)，eng 路径分工见 [91 规范 §8.2](91-eos-biz-eng-spec.md#82-业务流程开发wft02-eng)。

```text
流程与 IT 域
  → A1 平台引擎产品
    → 引擎（@engine-xxx）
      → CU（配置单元）
```

CU 是引擎内可独立选配的配置单元。按 91 规范，CU 定义一组可配置的 IPO 能力：配置人员通过后续功能表单维护配置信息；系统校验并保存；运行时，引擎按 CU 定义生成或渲染制品、构件及其功能。本 Skill 承接已分配 CU，定义操作活动定义（配置操作/系统处理/运行期能力），不设计具体页面。

#### 2.1.2 节点要素与职责边界

**一句话**：本节分两块看——**职责边界**（wft02-eng 切到什么粒度 / 不做什么）与 **BP 节点完整要素**（写入 `04-*.md` 的清单）。

> **"节点"指代**：本节"节点" = **BP 节点**（A1 配置单元 BP，架构树的末级节点，写入 `04-*.md` BP 节点块）——架构框架中"节点"是通用词（还有 CU 节点、依赖 STR-E 节点），此处特指 BP 节点，不泛指。

BP 节点的架构（CU 清单 + 配置能力 + 依赖关系）见 §2.2~§2.4 与 §3.3。

> **边界约定**：本 Skill 承接 STR-E 已分配 CU，定义每个 CU 的操作活动定义（配置操作/系统处理/运行期能力，步骤化三段式）、配置入口职责、FR-ENG 性能指标、依赖与状态迁移，不设计配置页面类型、布局组件或构件（§1.1 不负责）。

BP 节点完整要素（写入 `04-*.md` BP 节点块）：

- BP ID / 名称 / A1 产品路径 / 目标引擎 / 能力域
- 来源 STR-E 与消费版本
- A2 产品业务流程上下文（承接自 STR-E）
- 触发追溯
- CU 清单（承接自 STR-E 的 CU 承接关系）
- 每个 CU 的配置信息组（操作活动定义）、配置入口、FR-ENG 性能指标
- CU 依赖关系
- 25 对齐与模型状态
- 详设缺口与补充 FR-ENG 材料
- 变更影响声明
- 版本号 / 确认状态

---

### 2.2 CU 配置能力

**一句话**：本节讲 CU 配置能力的概念——定义与详细设计要素（2.2.1）：CU 承接自 STR-E 的配置信息组清单+结构判定级概要说明（wft01-eng 业务概要定义产出，每个 = 一个可以运行的能力，§3.1），本 Skill 展开每组的操作活动定义（配置操作/系统处理/运行期能力，步骤化三段式），配置入口与 FR-ENG 性能指标留 CU 级；末尾给出引擎状态迁移定义。

#### 2.2.1 定义与详细设计要素（操作活动定义）

每个 CU 用以下要素定义其配置能力。**CU 的配置信息组清单+结构判定级概要说明由 wft01-eng 在概要设计时产出**（每个配置信息组 = **某组配置字段，这组字段共同提供一个可以运行的能力**），本 Skill 承接该清单+概要说明并**展开每组为操作活动定义**——配置操作、系统处理、运行期能力均以配置信息组为单位、按**步骤化三段式**说明（基准=PDF v1.5.2）；配置入口与 FR-ENG 性能指标留在 CU 级。叙事链为”配置人员操作 → 系统处理 → 业务人员操作”。

**CU 级要素**：

| 要素 | 定义口径 | 描述方式 |
|------|---------|---------|
| 配置入口 | 配置人员在哪个配置页面维护该 CU（一个 CU 对应一个页面；复合页内的子页面即完整页面） | 页面职责、配置对象、配置类别；不设计具体 UI 布局 |
| FR-ENG 性能指标 | CU 配置后运行期需达到的性能指标 | 以可验证方式描述（定量指标或二元判定）；供 wft03-eng 功能概要、wft05-eng 产品概要承接指标输入；不设计实现 |

**配置信息组级要素——操作活动定义**（每个配置信息组按以下三方面、步骤化说明）：

| 要素 | 定义口径 | 描述方式 |
|------|---------|---------|
| 配置操作 | 配置人员如何编辑该组配置信息 | **以步骤序列说明**——从配置入口到提交的完整步骤序列，每步含**操作对象+约束+联动**（如”在画布选中节点→填名称/类型→校验→点确定”）；**配置信息组标题=组名+字段全集（含枚举）** |
| 系统处理 | 系统内部如何处理该组配置信息 | ① **编辑时处理**——设计会话中编辑该组时系统如何**校验→写载件→阻断**（对齐统一保存机制，§2.2.1 注）；② **生效后响应**——该组配置信息随版本发布装载后系统应①…②…③…：装载→界面反馈→运行期语义，**载件类型显式标注**（配置/数据/界面/构件/实例/支撑）；不写实现算法 |
| 运行期能力 | 系统基于该组配置信息为业务人员提供的能力 | **以业务人员操作过程说明**（操作页面 + 操作处理，**分支规则显式化**——如”通过→…；驳回→…；事件→…”）；**提供业务人员可操作能力的组必填**，以系统处理为主的组显式标注 |

**主语判据**：配置操作主语=配置人员、系统处理主语=系统、运行期能力主语=系统对业务人员交付。系统处理与运行期能力的分水岭在**对内/对外**——系统处理是系统内部行为（校验、事件触发、超时调度、状态维护），运行期能力是系统对外交付、业务人员可感知可操作的能力。

> **统一保存机制**（对齐 PDF v1.5.2，2026-08-19 定，全 BP 适用）：流程设计采用”设计会话+全局保存”模型，配置保存分三类粒度——**标量/父级配置**（节点基本信息、办理人、阅知人、征集/子流程、超时、任务标题、全局变量等）：编辑即时写入当前设计会话，由画布顶部全局”保存/保存&模拟/保存&发布”统一落库（增量持久化，仅写脏数据）；**子表行配置**（节点事件、操作按钮、子业务校验、表单校验规则、必传文档清单等）：每行独立保存（行级 CRUD 落库为子表行，落库即时生效于当前版本配置、不阻塞后续编辑），行内校验即时，同一子表类型接口跨宿主复用；**版本级动作**：保存=增量落库+版本级汇总校验（秒级），保存&模拟/保存&发布=全量校验+发布装载，重型校验可异步执行。**因此本 Skill 中”配置操作”的”保存”统一指：标量组=确认写入设计会话；子表行组=确认保存该行。真正的落库/装载由统一机制处理，不逐组重复描述”落库 XX 表”。**

> **系统标识生成规则**（对齐 PDF v1.5.2，2026-08-19 定）：节点/连接线/网关/按钮标识由系统自动生成、只读显示、不可手动修改——节点 key={类别短码}{版本内递增序号}（审批→appr01、征集→col01、子流程→sub01、开始→start、结束→end）；网关 key={类型短码}{序号}（排他→xgw01、并行→pgw01、包容→bgw01）；连接线 key=line{序号}；按钮 key=btn_{类型短码}{序号}（通过→btn_pass01、驳回→btn_reject01、事件→btn_evt01）。约束：仅字母/数字/下划线、字母开头（BPMN XML NCName 合法）；不含 ${ / # /空格/中文；版本内唯一；类别变更时 key 自动重生成并同步引用（按钮目标节点/连线/事件参数）；序号含已删除不复用。

**配置信息组说明模板**（步骤化三段式）：

> ### 配置信息组：{组名}（{该组包含的配置项全集，含枚举}）
> **配置操作**：
> 步骤1：……（操作对象+约束）；
> 步骤2：……（操作对象+约束）；
> 步骤N：点击确定，写入设计会话 / 子表行保存。
> **系统处理**：
> - 编辑时处理：校验…… → 写入设计会话（配置载件）；阻断条件……
> - 生效后响应：随版本发布装载后，系统应：① ……（载件类型）；② ……（载件类型）
> **运行期能力**（提供业务人员可操作能力的组写，以业务人员操作过程说明；以系统处理为主的组不填此项）：
> - 操作页面：……
> - 操作处理：步骤1：……（分支：通过→…；驳回→…；事件→…）

**三条规则**：① 组的分界按**可以运行的能力**——由 wft01-eng 在概要设计时确定（分组判据见 [wft01-eng §3.3.1](eos-wft01-eng-or2str.md)），本 Skill 承接配置信息组清单+结构判定级概要说明；详设时若发现分组与 CU 承接冲突：组粒度可调整（合并/拆分组）→ 标注 `[调整]`（不改 CU 承接关系，§3.3.3）；结构冲突（某组无法在该 CU 下展开）→ 退回 wft01-eng（§3.3.3）；② 运行期能力非必填——某组只提供系统处理、不提供业务互动能力时，显式标注”以系统处理为主”，不硬凑运行期能力；③ **步骤化纪律**——配置操作以步骤序列写（每步含对象+约束+联动）、编辑时处理以”校验→写载件→阻断”写、生效后响应一律编号式（”随版本发布装载后，系统应①…②…③…”）且**载件类型显式标注**。

**载件分类**（系统处理·生效后响应的生成物，承载配置所定义的能力）：数据载件（物理表/字段）、配置载件（配置记录，如 FlowNodeConfig 行）、界面载件（功能表单/窗口标签页/菜单入口）、构件载件（操作按钮/状态标签/指标图示/看板视图）、实例载件（流程实例/流程任务/工单/计划任务/指标结果记录）、支撑载件（定时任务/通知/催办消息/审计日志）。实体引擎 CU 以数据载件为主（生成表、字段），功能引擎 CU 生成其余各类载件。

**示例：@flow-cu-node-config**（流程节点配置单元，按操作活动定义说明；步骤化形态=PDF v1.5.2 样板）：

**CU 级**：
- 配置入口：配置页面=流程设计器·节点配置面板（wft03-eng 据此组织功能表单，本 Skill 只定职责）
- 统一保存机制：节点配置采用”设计会话+全局保存”（标量组写入设计会话、子表行组行级保存、版本级动作全量校验，§2.2.1 注），下列示例的”点击确定”即写入设计会话 / 保存子表行

**配置信息组（操作活动定义）**：

> ### 5.1 节点基本信息/类型组（节点名称、节点key、节点类别[审批/征集/子流程]、节点类型[单人/多人并行]）
> **配置操作**：
> 步骤1：在 BPMN 画布中选中任务节点，右侧打开节点配置面板；
> 步骤2：填写节点名称（必填，同一版本内不重名）、节点key（系统自动生成只读，规则见上）；
> 步骤3：选择节点类别（审批/征集/子流程），面板字段随类别切换；
> 步骤4：选择节点类型（单人/多人并行）；
> 步骤5：点击确定，写入设计会话。
> **系统处理**：
> - 编辑时处理：校验节点名称不冲突、节点类别合法；节点 key 按规则自动生成，类别变更时自动重生成并同步所有引用；更新设计会话中该节点配置（配置载件）
> - 生效后响应：随版本发布装载后，系统应：① 节点类别/节点类型装载（配置载件）；② 节点背景色按配置完整度更新（灰=新建/白=完整/黄=缺必填项，构件载件）；③ 运行期按节点类别决定该节点的办理形态
> **运行期能力**：
> - 操作页面：该节点办理单据页（渲染绑定表单）
> - 操作处理：实例流转到该节点，系统按办理人配置生成待办并分派；业务人员打开办理单据页，看到该节点的办理形态（审批/征集/子流程）

> ### 5.4 操作项/办理意见组（通过/驳回/事件按钮、目标节点、意见配置、应用条件、驳回后重提策略）
> **配置操作**：
> 步骤1：在节点配置面板的”审批按钮”区勾选本节点的操作项按钮集（默认：通过一个；驳回/事件可选添加，同类按钮节点内不重名）；
> 步骤2：单击按钮行单元格→行内编辑修改属性；右键”编辑”→打开按钮编辑弹窗（按钮名称/类型/目标节点/意见/事件/条件/重提策略）；
> 步骤3：通过/驳回类按钮配目标节点（通过→后续节点或跳转指定节点；驳回→前节点/发起人/指定节点）；
> 步骤4：配置意见（意见可编辑=是→必填/可空+默认文案；否→锁定默认文案，默认意见必填）；
> 步骤5：配置按钮事件（选填）——校验/前置/后置 3 槽位，各配同步/异步与异常处理；
> 步骤6：配置应用条件（条件表达式，选填，满足才显示该按钮）；
> 步骤7：配置驳回后重提策略（全部失效/仅驳回节点重审/指定重审起点）；
> 步骤8：子表行保存——每个按钮为子表行，点”确定”即保存该行（行级落库）。
> **系统处理**：
> - 编辑时处理（行级保存）：校验按钮名称节点内不重名、目标节点有效、意见配置联动合法、应用条件可解析；删除唯一通过按钮时提示（不阻断行级保存，版本级校验在全局保存/发布时阻断）；校验按钮事件绑定服务已注册、同步后置事件可回滚
> - 生效后响应：随版本发布装载后，系统应：运行期在办理单据页按配置渲染按钮构件（构件载件）；业务人员点击按钮后，按配置驱动该节点办理结果（写 task_result）
> **运行期能力**：
> - 操作页面：该节点办理单据页 + 办理弹窗
> - 操作处理：步骤1：打开办理单据页，看到按配置渲染的通过/驳回/事件按钮（或列表右键菜单）；步骤2：点击”通过”→触发表单/子业务/引用校验 + VALIDATE 校验→记录 task_result 并流转；步骤3：点击”驳回”→不触发表单/VALIDATE 校验，仅校验意见与驳回目标有效→记录驳回 task_result→流转到驳回目标节点；步骤4：点击”事件”→仅执行按钮绑定事件，不记录 task_result、不完成任务、不流转；步骤5：其余办理操作——移交（意见必填）、加签/减签（多实例节点）、传阅（生成待阅）、取回（下一步未审批时）

> ### 5.7 超时流转组（是否超时处理、超时时长(天)、超时审批意见、超时异常通知人）
> **配置操作**：
> 步骤1：在节点配置面板开启”是否超时处理”（默认否；开启后显示超时相关字段）；
> 步骤2：填写超时时长（天，正整数，必填）；
> 步骤3：填写超时审批意见（多行文本，系统自动提交时使用）；
> 步骤4：选择超时异常通知人（默认=流程管理员）；
> 步骤5：点击确定，写入设计会话。
> **系统处理**：
> - 编辑时处理：校验超时时长合法（开启时必填）
> - 生效后响应：随版本发布装载后，系统应：① 定时任务扫描超时待办，到期自动提交（办理人=系统管理员，意见=配置意见）；② 超时审批逐条执行（各自留超时记录/日志）；③ 节点表单必填未填报→不允许触发超时（标记”超时失败待处理”，向办理人+管理员推送）
> **运行期能力**（以系统处理为主）：超时未办时业务人员收到催办/超时通知；超时自动审批——多人任意随机选一、多人全部逐个提交

并单独定义**引擎状态迁移**：配置草稿、已校验、已发布、已装载、已停用、已回滚等状态及其触发条件；不得仅隐含在”系统处理”描述中。

---

### 2.3 A2 上下文与触发追溯

**一句话**：本节讲 A2 上下文的概念——定义与承接规则（2.3.1）：wft02-eng 同时保留 A2 与 A1 两个视角，其中 A2 上下文由 wft01-eng 装配、本 Skill **承接**，回答"CU 为什么需要"；A2 是追溯/上下文支撑视角（非设计视角），A1 是唯一设计视角，两视角双向可追溯、A2→A1 单向设计支撑。

#### 2.3.1 定义与承接规则

wft02-eng 必须同时保留 A2 和 A1 两条核心视角，其中 A2 上下文同时承载触发追溯信息：

| 视角 | 归属 | 作用 | 输出 |
|------|------|------|------|
| **A2 产品业务流程上下文**（含触发追溯） | A2 | 说明 CU 服务于哪个业务运行对象、配置用户和业务场景，以及 CU 为什么需要（触发追溯） | A2 产品/场景引用、业务运行对象、配置或支撑角色、触发事件、状态影响；触发追溯含治理对象、构件缺口、布局组件缺口、横切能力阶段 |
| **A1 产品装配** | A1 | 说明 CU 属于哪个平台引擎产品 | A1 产品路径、目标引擎、CU 清单、依赖关系 |

> 触发追溯是 A2 上下文的核心内容（回答"CU 为什么需要"），不是独立于 A2/A1 之外的第三视角。§8.2 只定义 A2 和 A1 两个视角，本表中触发追溯归入 A2 上下文以保持与 91 规范一致。构件缺口、布局组件缺口等追溯信息服务于 CU 来源判定（wft01-eng §3.3.3），不构成独立的设计视角。

**A2 承接的性质**：A2 上下文由 wft01-eng 装配（从 OR 触发来源与 23/24 资产摘取、结构化，[wft01-eng §2.5/§3.2.2](eos-wft01-eng-or2str.md)），本 Skill 从 STR-E 节点**承接**该上下文——校验字段完整后写入 BP，不重新装配、不重写业务链 BP 文档、不把 Bn 业务子产品展开到 eng 链。A2 是"追溯/上下文支撑视角"而非设计视角——A2 的完整设计归 wft02-biz，CU 推导依赖 A2 的触发追溯证据；两视角关系 = **双向可追溯（追溯矩阵保证）、单向设计支撑（A2→A1）**。A2 的完备性判据是"能否支撑 CU 来源判定与追溯"（§5.5 要素机械检查），不是 A2 业务流自身闭环。

> 若 A2 上下文缺失到无法判断 CU 来源，应退回 wft01-eng（A2 装配缺失）或生成补充 FR-ENG。

**A2 上下文最低字段要求**：

| 字段 | 输出要求 |
|------|----------|
| A2 产品/场景引用 | 指向业务链已有 A2/场景节点；缺失时标注来源 STR-E |
| 业务运行对象 | 被配置、支撑或治理的业务对象 |
| 配置或支撑角色 | 配置人员、业务管理员、运行支撑人员或外部系统 |
| 触发事件 | 业务配置、发布、运行、异常、治理或构件缺口触发 |
| 状态影响 | CU 生效后影响的业务状态、运行状态或治理状态 |

---

### 2.4 CU 依赖关系

**一句话**：本节讲 CU 依赖关系的概念——四类依赖的分类（2.4.1）；依赖校验流程见 §三 3.3.4。

#### 2.4.1 依赖关系分类

CU 依赖分为四类：

| 依赖类型 | 示例 | 处理 |
|----------|------|------|
| 引擎内依赖 | 流转规则配置依赖流程节点配置 | 在 BP 中写明先后关系 |
| 跨引擎依赖 | 流程配置依赖实体字段、权限规则 | 引用依赖引擎/CU，不改主归属 |
| 布局组件/构件依赖 | 流程 CU 运行期需要按钮构件、节点构件 | 说明被渲染的布局组件/构件 |
| 治理依赖 | 发布、版本、审计、回滚、监控 | 若缺失则生成补充 FR-ENG 或设计新治理配置面（记录进 25） |

---

## 三、设计原理与判据：为什么这样设计、怎么判定

**一句话**：本章讲详设的逻辑——为什么用 A1 配置单元承接 STR-E（3.1 转换原理）、怎么提取要素（3.2）、怎么详设 CU 与校验依赖（3.3）、怎么判完备（3.4）；概念见 §二，节点演进见 §四，操作步骤见 §五。

### 3.1 转换原理：为什么用 A1 配置单元承接 STR-E

**STR-E 描述"需要什么平台能力"，BP 回答"用哪些配置单元怎么实现"**。STR-E 是平台能力场景的业务定义（配置/治理目标 + 能力链 + 概要末级节点 + 配置信息组（概要说明） + CU 承接关系，wft01-eng 产出）；BP 把这些已分配的 CU 展开为可被系统需求（SR）承接的 A1 配置单元 BP——详细定义每个 CU 的配置信息组（操作活动定义：配置操作/系统处理/运行期能力 + 引擎状态迁移）、建立依赖关系。承接单位 = A1 配置单元 BP，CU 是其细分节点（与 STR-F 下辖输出文档的层级关系完全对称，[91 规范 §8.2](91-eos-biz-eng-spec.md#82-业务流程开发wft02-eng)）。

**承接已分配 CU 做业务详细定义，不是重新设计**。wft01-eng 已完成设计——分解（概要末级节点→配置信息组（概要说明，结构判定级））、分配（配置信息组→CU 承接，含收敛判定复用/新增/拆分/合并 + 对照 25 盘存的骨架判定）、分析（完备性）；本 Skill 承接其产出，只做**业务详细定义**：读取 CU 承接关系（含引擎归属），展开操作活动定义（配置操作/系统处理/运行期能力，步骤化三段式）、配置入口、依赖与状态迁移。本 Skill **不重复收敛**（不重新判定 CU 新增/拆分/合并）、**不重复判定引擎归属**（承接 wft01 的设计结论）——承接关系是**设计结论**（wft01 权威），详设时若发现承接缺陷（配置信息组无法在该 CU 下展开、引擎归属错判、承接缺失）退回 `需wft01修订`，不静默改。

**配置信息组清单+结构判定级概要说明承接自 wft01 的概要设计，本 Skill 展开为操作活动定义**。wft01-eng 在概要设计时按"可以运行的能力"把概要末级拆成配置信息组（概要说明，[wft01-eng §3.3.1](eos-wft01-eng-or2str.md)）；本 Skill 承接该清单+概要说明，为每个配置信息组定义操作活动定义——配置操作（步骤序列）、系统处理（**编辑时处理 + 生效后响应**，统一保存机制）、运行期能力（§2.2.1），并定义 CU 级配置入口、FR-ENG 性能指标与引擎状态迁移。CU=**页面单位**（操作/维护边界）、配置信息组=**能力单位**（运行期边界）的两级分工由 wft01 概要设计确定；本 Skill 详设时若发现分组与 CU 承接冲突：组粒度可调整 → 标注 `[调整]`（§3.3.3）；结构冲突（某组无法在该 CU 下展开）→ 退回 wft01-eng（§3.3.3）。

**详设层缺口经补充 FR-ENG 或退回 wft01 处理，不脑补承接**。已分配 CU 的**操作活动定义、依赖或状态迁移缺口**（配置入口缺失、配置操作步骤缺失、系统处理缺段、运行期能力不可定义、依赖断点）由本 Skill 识别——能形成补充需求文本的生成补充 FR-ENG 材料（§5.5 3），需人类补充业务配置意图的标记 `[需裁决]`；**承接缺陷**（CU 承接关系/配置信息组清单无法在该 CU 下展开）属 wft01 设计层问题，退回 `需wft01修订`（§5.5 1），不脑补 CU 承接。**25 盘存是资产，不是需求源**——本 Skill 详设新增配置信息组/载件时对照 25 盘存判定复用/改进/新增（骨架判定，对齐通用设计规范 §2.3 文档结对骨架），但承接关系本身已在 wft01 确定。

**详设层变更以 `[调整]` 标注**。承接关系确定后，本 Skill 详设过程中对 CU 的**操作活动定义内容（配置操作步骤/系统处理/运行期能力）**的扩展（配置能力扩展）属详设层变更——以 `[调整]` 标注并写变更原因（§3.3.3），不改变 CU 承接关系；CU 承接关系本身的变更（新增/拆分/合并/归属变更）退回 wft01-eng 重新分配，本 Skill 不静默改承接。新增配置信息组/载件的粒度设计（§2.2.1/§3.3）同属详设内容——落地走文档结对骨架（对照 25 盘存判定复用/改进/新增），由 wft03-eng 承接验证可消费性。

**这套原理方法为什么可行**：它以**可验证性**为锚——每个设计决策都追问"下游能否验证、追溯是否完整、变更能否传播"。承接关系（wft01 设计结论）与详设内容（本 Skill 定义）分离，保证"设计决策与设计过程不分离"（决策在 wft01、详设在 wft02）；操作活动定义（配置操作/系统处理/运行期能力，步骤化三段式）是"配置→系统处理→运行期生效"的微闭环，缺一段即不可配置或不可验证；"运行期能力可消费"出口闸门（运行期制品/构件/功能、指标和配置入口足以让 wft03-eng 组织功能概要，§3.4）把可消费性落为硬判据，不足则保持待确认方案；**追溯完整性永远优先**——承接关系必须可追溯 STR-E 与 OR，承接缺陷退回 wft01 而非脑补；权威归信息最全的一方且只归其信息全的部分（设计→wft01-eng，详设→wft02-eng，功能概要→wft03-eng）。判据全部可机械判定，操作全部有明确输入输出与执行模式（§5 Step 布局表），原理→判据→操作三层一一咬合，且与 91 规范、biz 路径同一套协议运转。

---

### 3.2 要素提取规则

**一句话**：本节讲从 STR-E 提取 BP 要素的规则——目标引擎与能力域确定（3.2.1）、A2 上下文承接（3.2.2）、Phase B 宿主匹配检查（3.2.3）。Phase B（修订已有 BP）与 Phase C（新建 BP）共用以下规则——先看 3.2.4 的 B/C 分工差异表，再读细节更顺。

#### 3.2.1 目标引擎与能力域确定

从 STR-E 读取并确认：

| 提取项 | 说明 |
|--------|------|
| 主责类型 | `引擎专属` / `平台共享` |
| 主责能力域 | STR-E 唯一主责引擎或共享能力域 |
| 能力域 | STR-E 场景的配置生成/构件扩展/布局组件扩展/横切治理/运行支撑类型 |
| 依赖 STR-E | 本场景消费的其他引擎专属或平台共享能力场景 |

路由规则：

| 情况 | 处理 |
|------|------|
| STR-E 主责能力域唯一且场景边界与 BP 一致 | 进入宿主匹配（Phase B）或新建（Phase C） |
| 主责能力域相同但独立场景不同 | 匹配其他 BP 或新建，不按同域强行合并 |
| STR-E 主责能力域不明确或能力域混杂 | 标记 `[推断]`，必要时退回 wft01-eng |
| CU 属于依赖的共享 STR-E | 不在当前 BP 重复定义，建立依赖引用 |
| STR-E 缺少能力链或 CU 承接关系 | 退回 wft01-eng，不进入 BP 设计 |

#### 3.2.2 A2 上下文承接

Phase C 新建 BP 时，从 STR-E 节点承接 wft01-eng 已装配的 A2 上下文（概念见 §2.3）：

1. **读取 A2 产品/场景**：从 STR-E 的 A2 上下文块读取业务运行对象/治理对象及 A2 产品/场景引用。
2. **读取配置/支撑角色**：从 STR-E 的场景边界陈述中读取配置人员或支撑角色。
3. **读取触发事件与状态影响**：从 STR-E 的触发追溯与能力链”运行期可用性确认”段分别读取。
4. **校验触发追溯链**：`A2/Bn 业务运行对象/治理对象/构件缺口/布局组件缺口/横切阶段 → STR-E → A1 BP → CU` 的追溯链字段完整。

若 A2 上下文信息不足，标注 `[推断]` 并记录缺失项；若缺失到无法判断 CU 来源，退回 wft01-eng（A2 装配缺失）。

#### 3.2.3 Phase B 宿主匹配检查

Phase B 修订已有 BP 时，按目标引擎和能力域匹配已有 BP：

按以下顺序匹配：

1. 同一 A1 产品路径（流程与 IT 域 → 同一引擎）。
2. 同一目标引擎（@engine-xxx）。
3. 同一能力域或互补能力域。
4. CU 清单可归入同一 BP。

匹配结果：

| 结果 | 判定 | 动作 |
|------|------|------|
| Q1 直接归入 | 同一主责能力域、同一能力场景、边界一致 | 追加 STR-E 来源，标记 `[确认]` |
| Q2 扩展归入 | 同一能力场景，但 CU 能力边界扩展 | 扩展 BP，写变更声明 |
| Q3 新建 | 无可用 BP 宿主 | 新建 A1 BP 节点 |
| 共享依赖 | CU 已由平台共享 STR-E 定义 | 只记录依赖和消费关系，不复制 CU |
| 冲突 | 同一 CU 被多个主引擎争用或 A1 路径不一致 | 标记 `[需裁决]` |

#### 3.2.4 Phase B（修订）与 Phase C（新建）的提取差异

| 维度 | Phase B 修订已有 BP | Phase C 新建 BP |
|------|-------------------|-----------------|
| 判定范围 | BP 已承接的全部 STR-E + 本轮新 STR-E | 待聚合池本组 STR-E |
| CU 承接基线 | 以已有 CU 承接为基线，仅扫描本轮新 STR-E 的已分配 CU | 从零承接 |
| A2 上下文 | 增量合并新 STR-E 中的 A2 信息，已有上下文仅追加不重建 | 全量承接（§3.2.2） |
| 宿主匹配 | 执行宿主匹配检查（§3.2.3），可能检出 Q1/Q2/Q3/冲突 | 无需匹配，直接新建 |
| 详设处理 | 承接新 CU + 定义操作活动定义（详设变更见 §3.3.3） | 全量详设（§3.3.2） |
| 依赖重识别 | 合并重识别可能导致已有依赖关系变更——标记 `[依赖变更]` 并附变更原因 | 全量识别，无此问题 |
| 变更传播 | 需输出变更影响声明（§4.2） | 写”首版，无变更” |

---

### 3.3 CU 详细定义与依赖校验规则

**一句话**：本节讲逐 CU 的详设规则——CU 承接读取（3.3.1）、配置信息组与操作活动定义（3.3.2）、详设变更处理（3.3.3）、依赖定义与闭合校验（3.3.4）。Phase B（修订已有 BP）与 Phase C（新建 BP）共用以下规则。CU 收敛判定、来源类型与关系检测已上移 wft01-eng（设计归属重构 DP5），本 Skill 不重复执行。

#### 3.3.1 CU 承接读取

**读取 CU 承接关系**：从当前操作范围内的 STR-E 读取 CU 承接关系（wft01-eng 分配产物，含收敛判定/骨架判定/引擎归属，[wft01-eng §2.4](eos-wft01-eng-or2str.md)），去重后形成 CU 清单，每个 CU → BP 中一个配置单元节点。多个 STR-E 的 CU 承接指向同一 CU 时归入同一节点（要素收敛）。

**校验承接结构完整**：每条 CU 承接须含承接的配置信息组（概要说明）清单与引擎归属；缺失时退回 wft01-eng。本 Skill **不重新收敛**、**不重新判定引擎归属**——承接关系是 wft01 设计结论（§3.1），只校验详设可行性。

#### 3.3.2 配置信息组与操作活动定义

对每个已分配 CU，承接 wft01 的配置信息组清单+结构判定级概要说明（§2.2.1），逐配置信息组展开操作活动定义：

| 定义项 | 口径 | 说明 |
|--------|------|------|
| 操作活动定义 | 承接 wft01 的配置信息组清单+概要说明，逐组展开配置操作（步骤序列）/系统处理（编辑时处理+生效后响应）/运行期能力（分支显式化） | 组清单与 wft01 承接保持一致；详设时可标注 `[调整]`（合并/拆分组粒度），不改 CU 承接关系 |
| 配置入口 | 配置人员在哪个配置页面维护该 CU | CU 级，页面职责/配置对象/配置类别，不设计 UI |
| FR-ENG 性能指标 | CU 配置后运行期需达到的性能指标 | CU 级，定量指标或二元判定；供 wft03-eng 功能概要、wft05-eng 产品概要承接指标输入 |
| 系统处理 | 编辑时处理（校验→写载件→阻断）+ 生效后响应（生成载件，载件类型显式标注） | 逐组定义，对齐统一保存机制 |
| 运行期能力 | 系统对业务人员交付的能力 | 以业务人员操作过程说明，分支规则显式化；以系统处理为主的组显式标注 |
| 引擎状态迁移 | 配置草稿/已校验/已发布/已装载/已停用/已回滚等状态及触发条件 | CU 级，单独定义 |

详设新增配置信息组/载件时对照 [25 资产 §1.1](../../80-pl4eos-2-eosdata/25-eos-engine-models.md#11-引擎全景) 盘存判定复用/改进/新增（骨架判定，人机协同设计决策点——AI 附判据理由，流水线开发者审核决策，对齐通用设计规范 §2.3 文档结对骨架）。

#### 3.3.3 详设变更处理

承接关系确定后，BP 修订时处理两类详设层变更：

| 变更类型 | 触发 | 处理 |
|----------|------|------|
| **配置能力扩展** | 已有 CU 的操作活动定义内容（配置操作步骤/系统处理/运行期能力）扩展 | 修订 CU 操作活动定义，叠加 `[调整]` 标注 + 写变更原因。不新增/不拆分 CU（CU 承接关系不变） |
| **承接关系变更** | 新 STR-E 携带的已分配 CU 与已有承接冲突（承接缺失/引擎归属错判/配置信息组无法在该 CU 下定义） | 不静默改——退回 `需wft01修订`，携带缺失说明，由 wft01-eng 重新分配（wft01-eng §3.3.4 关系检测） |

> 上表与 wft01-eng 的收敛判定/关系检测是两个层面：wft01 管**承接关系**（CU 新增/拆分/合并/归属变更），本 Skill 管**承接内容**（详设定义变更）。配置能力扩展不改变承接关系，由本 Skill `[调整]` 处理。

#### 3.3.4 依赖定义与闭合校验

依赖概念与分类见 §2.4。对 BP 的依赖关系定义并执行四步闭合校验：

1. **引擎内**：定义 CU 间先后配置关系，缺失前置 CU 时标记依赖断点。
2. **跨引擎**：定义引用引擎/CU（wft01 已判定归属），校验在 25 或已有 BP 中存在，不存在时标记 `[推断]` 依赖。
3. **布局组件/构件**：定义运行期能力中的构件/组件，校验可追溯到来源 CU 或 25 中的布局组件/构件定义。
4. **治理**：定义发布、版本、审计、权限、回滚等横切依赖，校验在 BP 或 25 中有覆盖，缺失时生成补充 FR-ENG（§5.5 3）。

---

### 3.4 完备性判据

| 检查项 | 判据 | 缺口处理 |
|--------|------|----------|
| 承接完整 | 已分配 CU 全部承接且结构完整（CU 承接关系 + 配置信息组清单可展开操作活动定义） | 承接缺失退回 wft01-eng（§5.5 1） |
| 配置能力自洽 | 配置入口/配置操作（步骤序列）能被系统处理，并形成运行期制品或功能 | 标记 `[需裁决]` 或补充 FR-ENG |
| 依赖闭合 | CU 依赖的实体、表单、权限、发布、构件等能力有来源 | 形成依赖缺口，生成补充 FR-ENG |
| 运行期能力可消费（出口闸门·DP4） | 运行期制品/构件/功能、指标和配置入口足以让 wft03-eng 组织功能概要（功能表单） | 不足则保持待确认方案 |
| 25 关系清晰 | 25 中已有、待确认或缺失的模型关系已标注 | 标注新类型设计记录或阶段性标记 |

---

## 四、CU BP 节点演进：生命周期与变更传播

**一句话**：本章讲 BP 节点设计与确认后的演进机制——生命周期（4.1，节点状态四态主线 + 上游变更分流 + 本 Skill 写回范围）与变更传播（4.2，修订后写变更影响声明告诉下游 wft03-eng）。

### 4.1 BP 节点生命周期

BP 节点沿 [91 规范 §A.6](91-eos-biz-eng-spec.md#a6-节点生命周期模型) 的四态主线流转及上游变更三条分流规则运行。本 Skill 的状态名映射按 §A.6 通用六态自声明：BP 侧（`待确认方案`/`可以SR设计`/`待补充SR设计`/`需wft02修订`）见下方主线流转与写回范围，STR-E 侧（`可以详细定义`/`待补充详细定义`/`已详细定义`）见 §1.1 与 §5.6。并行推进规则（资产线与 SR 线互不阻塞）见 91 规范 §A.6。

**主线流转**（wft02-eng ←→ wft03-eng）：

```
待确认方案 ──人类确认──→ 可以SR设计 ──wft03锁定──→ 在SR设计 ──wft03完成──→ 已SR设计
     ↑                        ↑                         │
     │                        │               wft03退回  │
     │                        │                         ↓
     └─── wft02修订 ────── 需wft02修订 ←───────────────┘
```

**上游变更**（新 STR-E 汇入触发 BP 修订）：

```
可以SR设计 ──新STR-E修订──→ 可以SR设计（版本递增，wft03 未消费）
在SR设计   ──新STR-E修订──→ 在SR设计（版本递增，wft03 增量处理）
已SR设计   ──新STR-E修订──→ 待补充SR设计 ──wft03补全──→ 已SR设计
```

**本 Skill 的写回范围**：`待确认方案` / `可以SR设计` / `待补充SR设计` / `需wft02修订`。`在SR设计` 和 `已SR设计` 由 wft03-eng 写入和推进。

下游退回 `需wft02修订` 时：读取 wft03-eng 的缺失说明 → 按人类改进方案修订 BP（走 Phase B 修订流程）→ 人类确认 → BP → `可以SR设计`。

### 4.2 BP 变更传播协议

变更传播协议的通用定义（变更类型分类、判定优先级、声明格式模板、增量清单与变更影响声明的分工）见 [91 规范 §A.7](91-eos-biz-eng-spec.md#a7-变更传播协议)。

**本 Skill 的变更影响声明格式**，每次修订已有 BP 后写入 BP 节点末尾（`04-*.md` 中 BP 节点块内）：

```text
### 变更影响声明

BP 版本：v{N}（本次修订前为 v{N-1}）
本轮变更类型：无变更 / 增量型 / 修订型 / 结构型

变更项：
- [新增] / [变更] / [确认] / [需裁决] ...
  说明字段中记录细分原因：CU 新增 / 配置能力扩展 / 引擎归属变更 / 过时 / 宿主冲突等

下游已消费此 BP 的 SR 节点：
  <SR节点ID列表>（如无则写”尚未被下游消费”）

对已消费 SR 节点的影响：
  受影响节点：<SR节点ID + 受影响原因>
  不受影响节点：<SR节点ID>
  待新增 SR 节点：<新CU对应的待组织功能表单说明>

建议 wft03-eng 动作：
  - 无变更：无需任何动作
  - 增量型：由 wft03-eng 增量组织新增 CU 的功能概要（功能表单），已有 SR 节点不动
  - 修订型：修订受影响的 SR 节点 → 标记 [来自上游变更]
  - 结构型：输出影响范围，等人类裁决后再处理；含 CU 拆分/合并时 wft03-eng 需新建或重组功能概要
```

---

## 五、操作流程

**一句话**：一次运行 = Step Start 入口路由 → Step 1 材料加载 → Step 2 反馈处理 → Step 3 A1/CU BP 设计 → Step 4 引擎需求增补 → Step 5 资产写回 → Step End 输出与对话反馈。反馈走自然语言对话（§5.3），跨轮余留由 AI 标记持久化。

### Step 布局总览

| Step | 名称 | 职责 | 人类参与 | 执行模式 | 产出 | 写资产 |
|------|------|------|---------|---------|------|--------|
| Start | 前置校验与路由 | 变更感知 + 入口检测、输入校验、退回优先路由 | 无（AI 自动） | 统一 | 就绪 / 结束 | — |
| 1 | 设计材料加载 | 安全复查 + 读 AI 可以处理节点 + 加载 STR-E/BP/资产 | 无（AI 自动） | 统一 | STR-E / BP / 资产全景 | — |
| 2 | 方案反馈处理 | 读"待反馈处理"分节 + 对话处理人类修改意见 | **有**——对话反馈修改意见 / 整体确认 | 对话 | 调整后的 BP 方案 | `02/04` |
| 3 | A1/CU BP 设计 | Phase A 路由 / B 修订 / C 新建 + 承接已分配 CU + 操作活动定义（配置操作/系统处理/运行期能力）/依赖/状态迁移定义 | **有**——设计决策审核（操作活动定义、详设变更 [调整]），对话反馈 | 统一（AI 产出带判据理由）+ 对话 | A1 路径 + 已分配 CU + 操作活动定义 + 依赖 + 追溯 | `04` |
| 4 | 引擎需求增补 | 详设缺口识别 + 要素机械检查 + 补充 FR-ENG/设计产物记录检查 | 无（AI 自动） | 统一 | 详设缺口说明 + 补充材料 + 新类型设计记录 | `01/04` |
| 5 | 资产写回与落账 | 23/25 写回 + 推进 BP/STR-E + 提交基线 | 无（AI 自动，写回已确认节点） | 统一 | 23 引擎引用 + 25 CU 引用 + 状态推进 | `23/25/02/04` |
| Step End | 运行结束输出 | 三区摘要输出 + 等待人类反馈 | **有**——查看摘要、对话反馈 | 对话 | 人类下一步操作提示 | — |

> **执行模式白话**：统一 = AI 自动跑完这步，不需你介入；对话 = AI 输出后需要你说句话（反馈或确认）才继续。Step 1/3/4/5 为运行内自动步骤——其中 **Step 3 是详设步骤（人机协同交互密度最高处）**：AI 承接已分配 CU，基于对配置需求与已有资产（25 盘存）的语义理解产出详设方案（操作活动定义）并附判据理由，人类在 Step End/Step 2 对详设决策（操作活动定义、详设变更 [调整]）审核、思考、决策、反馈，AI 落实；Step 2 / Step End 为人机交互步骤（对话反馈，见 §5.3）。

**反馈机制一句话**：反馈走**自然语言对话**（AI 产出方案后不结束对话，§5.3）；跨轮余留反馈由 AI 标 `[修改]`/`[驳回]` 入"待反馈处理"分节（§5.3 状态机）；人类线下修订经 Step Start 变更感知（git diff，§5.1 变更感知）检出后纳入"待反馈处理"分节；AI 每轮写回提交基线（§5.6），保证下一轮 Step Start 的 `git diff <HEAD @上次 AI 运行>..HEAD` 只含人类变更。

---

### 5.1 Step Start

**一句话**：先变更感知（检出人类线下修订并纳入待反馈分节），再按 [§A.4](91-eos-biz-eng-spec.md#a4-入口条件与优先级step-start) 入口决策路由（退回优先 → 反馈处理 → 新输入），无待处理对象即退出。

**变更感知（检出人类线下修订，先于入口判定）**

读取目标文件前先检测自上次 AI 运行以来人类是否修改过文件（[§11.10](91-eos-biz-eng-spec.md#1110-ai-反馈检测规则)）：

1. 读取文件头 `**HEAD @上次 AI 运行**` 字段，获取上次提交 hash H
2. Bash("git diff H..HEAD -- 目标文件") → 返回 H 到当前 HEAD 之间的全部变更
3. 无变更 → 进入入口判定
   有变更 → 解析变更行号，读取变更行附近的节点块（上下 5 行）
   → 判定变更类型：结构化标注（`[同意]`/`[修改]`/`[驳回]`）→ 纳入待反馈；非标自由文本 → 标记 `[需确认]`；无关修改（格式/排版）→ 忽略
   → 将存在反馈的节点加入AI可以处理节点"待反馈处理"分节

> 变更感知必须先于入口判定——人类纯线下修订（不选新 STR-E）时，只有 git diff 能发现该反馈并将其纳入"待反馈处理"分节，入口判定②才命中。若入口判定先于变更感知，纯线下修订会被误判"无待处理对象"退出。

**入口判定**（在变更感知后执行）：读取 `02-*.md`/`04-*.md` 的 `## AI可以处理节点` 分节（[§11.7](91-eos-biz-eng-spec.md#117-ai可以处理节点)），获取三类入口的节点列表，再按以下顺序处理：

| # | 入口条件 | 检测方式 | 对应分节 |
|---|---------|---------|---------|
| ① | **有新输入** | AI可以处理节点「待 wft02-eng 处理」分节非空 | §11.7 |
| ② | **有待处理反馈** | AI可以处理节点「待反馈处理」分节含本 Skill 产出节点 | §11.7 |
| ③ | **有下游退回节点** | AI可以处理节点「待下游退回处理」分节含状态为 `需wft02修订` 的节点 | §11.7 |

三类均不满足 → 输出"无待处理对象"→ **退出**。

#### 5.1.1 输入校验

| 校验条件 | 判定 | 动作 |
|---------|------|------|
| STR 类型不是 STR-E | 类型不匹配（硬退出） | 输出类型分布，提示走 `wft02-biz` 或 nfr 路径 → **退出** |
| STR-E 状态不是 `可以详细定义` / `待补充详细定义` | 状态不符（硬退出） | 输出不合格节点及当前状态 → **退出** |
| STR-E 缺少场景边界、能力链或触发追溯 | 要素缺失（硬退出） | STR-E → `需wft01修订`，输出缺失说明 → **退出** |
| STR-E 无已分配 CU 或承接结构不完整（缺配置信息组结构） | 承接不完整（硬退出） | 输出"输入不足"提示 → STR-E → `需wft01修订`，输出缺失说明 → **退出** |
| BP 状态为 `需wft02修订` 且人类未提供改进方案 | 退回方案缺失（硬退出） | 输出 wft03-eng 的缺失说明，提示提供改进方案 → **退出** |
| 选中 STR-E >5 个 | 超建议阈值（软提示） | 输出超限提示 → 人类确认后继续或退出 |

> 软提示不强制退出，由人类选择继续或退出；硬退出为逻辑上不可能的条件，输出后退出（§A.4.5）。

#### 5.1.2 退回优先（§A.4.3 门禁）

存在 `需wft02修订` 的 BP 节点（来自"待下游退回处理"分节）：
- 检查人类是否已提供改进方案（读取退回节点的缺失说明中是否含人类标注 `[修改]`/`[同意]` 或下游 Skill 产出的具体改进指引）
- 已提供 → 通过门禁，读取 wft03-eng 的缺失说明，按人类方案走 Phase B 修订（§5.4.2），人类确认后推进为 `可以SR设计`
- 未提供 → 输出 wft03-eng 的缺失说明 + 提示提供改进方案 → **退出**

#### 5.1.3 反馈处理

存在"待反馈处理"分节中的 BP 节点（条目确认状态 = `[修改]`/`[驳回]` 且无 `[已处理]`，标记定义与写入者见 §5.3）→ 进入 Step 2 处理（§5.3）。若全部条目已 `[同意]`+`[已处理]`，不阻塞新 STR-E，Step End 时提示。

#### 5.1.4 新 STR-E 路由

有未处理的 STR-E 节点（来自"待 wft02-eng 处理"分节）→ 进入 Step 1→Step 3，逐节点执行 Phase A 宿主匹配（§5.4.1），归入已有 BP（→Phase B）或待聚合池（→Phase C）。

**执行顺序约束**：Step 2（反馈处理）必须先于 Step 3（新增设计），因为反馈可能改变 BP 的 A1 路径/CU 清单/配置能力/依赖关系。Step 3 的 Phase A 宿主匹配必须基于 Step 2 修改后的最新 BP 内容，不得使用缓存旧状态。此顺序与 §A.4.4 规则三一致（退回优先 → 反馈处理 → 新输入路由）。

---

### 5.2 Step 1 · 设计材料加载

加载产品数据与资产文档遵循 [91 规范 §A.3.3](91-eos-biz-eng-spec.md#a33-产品数据文档与资产文档读取与写回约定) 的统一读取协议：AI可以处理节点优先 → 索引导航。变更感知已在 Step Start 完成（§5.1），此处仅做加载前安全复查。

**步骤一：安全复查（检出非预期修改）**

Step Start 已检出人类反馈并纳入"待反馈处理"分节（§5.1 变更感知）。此处按 [§A.3.3](91-eos-biz-eng-spec.md#a33-产品数据文档与资产文档读取与写回约定) R0a 做加载前安全检查：`detect-changes.sh` 检出人类非预期修改时标记 `[需确认]`，不自动推进。

**步骤二：优先读取**（通过 `## AI可以处理节点` 定位本轮入口）：

入口判定通过后（§5.1），从 `## AI可以处理节点` 对应分节获取待处理节点 ID 列表：
- 有新输入 → 读「待 wft02-eng 处理」分节
- 有待处理反馈 → 读「待反馈处理」分节
- 有下游退回 → 读「待下游退回处理」分节

逐节点：Bash("grep -n '@{节点ID}' 目标文件") 定位行号 → Read 节点块。

**步骤三：索引导航**（AI可以处理节点无待处理时，或按域浏览时）：

1. Bash("grep -n '索引行数：' 目标文件") → 定位索引行号 L
2. 从 L 提取 N → Read 业务域索引
3. 按锚点 Read 域内索引 → 获取节点 ID 列表
4. 按需：Bash("grep -n '@{节点ID}' 目标文件") → 定位行号 → Read 节点

**加载清单**（按需扩大 - 仅在索引缺失、候选冲突或追溯关系断裂时）：

1. `02-*.md` STR 树画像 / 状态队列 / 节点索引 — 定位本轮 STR-E 正文块（含场景边界、能力链、CU 承接关系/配置信息组（概要说明）清单）
2. `04-*.md` BP 树画像 / 状态队列 / 节点索引 — 定位本轮 A1 BP 节点块，匹配已有 BP
3. `23-eos-output-architecture.md` 资产全貌层 — A1 产品路径锚点
4. `24-eos-business-assets.md` 资产全貌层 — A2/Bn/治理触发追溯线索
5. `25-eos-engine-models.md` §1.1 — 引擎/CU 类型清单（运行时动态读取，不缓存；详设新增配置信息组/载件时对照盘存做骨架判定）
6. `91-eos-biz-eng-spec.md` — CU 功能本质、页面组件与构件关系、eng 路径分工

**按需扩大规则**（[91 规范 §11.12](91-eos-biz-eng-spec.md#1112-按需读取规则)）：

- 候选 BP 块 — 按同目标引擎/同能力域筛选，用于 Q1/Q2 归入匹配
- 候选 25 CU 类型 — 按 CU 名称/目标引擎匹配，用于详设骨架判定（新增配置信息组/载件时对照盘存）
- 反馈重入 — 仅读取条目的确认状态字段及反馈指向块，不做全量重读
- 扩大触发：索引缺失 / 候选冲突 / 资产全貌不足以判断边界 / 追溯关系断裂时

---

### 5.3 Step 2 · 方案反馈处理

**一句话**：处理人类反馈——对话内即时处理（修改 / 整体确认，§5.3.1），跨轮余留按 `[修改]`/`[驳回]` 标记处理（§5.3 状态机）。

人类反馈的通用协议见 [91 规范 §A.5](91-eos-biz-eng-spec.md#a5-反馈交互协议)（反馈通道双轨：对话 + 线下修订）。以下为本 Skill 的特化规则。

**反馈双轨**——人类可任选两种方式反馈，本 Skill 均支持：

- **AI 对话反馈**：Step End 后不结束对话，直接在对话中提出修改意见（自然语言）——AI 按 §5.3.1 即时处理（主通道）
- **线下文档修订**：直接打开 `02-*.md`/`04-*.md` 修订——在确认状态字段标注 `[同意]`/`[修改]`/`[驳回]`，或直接编辑方案内容（自由文本）——经 Step Start 变更感知（git diff，§5.1 变更感知）检出后按 §A.5 处理

两轨等价：对话反馈由 AI 即时落账（追 `[已处理]`），线下修订经 git diff 检出后同样落账；AI 每轮写回提交基线（§5.6），保证下一轮 diff 只含人类变更。

**本 Skill 的条目**：A1 产品路径 + 每个 CU（含配置入口/操作活动定义/FR-ENG 指标）+ 每条依赖 + 每条缺口补需。各条目的 `确认状态` 字段位于 `04-*.md` BP 节点块各条目正文中。

**确认状态标记状态机**（本 Skill 反馈落账标记，定义单源；对话反馈写入者=AI，线下标注写入者=人类——经 git diff 检出后按 §A.5 处理）：

| 标记 | 含义 | 写入时机 | 转迁 |
|------|------|---------|------|
| `(空)` | 条目待人类反馈 | Step 3 产出时初始化 | 人类反馈后按情况转下 |
| `[修改]` | 人类提出修改意见、当轮未落地 | AI 在运行结束仍有余留修改意见时写入（跨轮持久化） | 下轮 Step 2 处理完追 `[已处理]` |
| `[驳回]` | 人类否决该条目方案、当轮未落地 | 同上 | 同上 |
| `[同意]` | 人类确认该条目 | "整体确认"时 | 随 `[已处理]` 一并追加 |
| `[已处理]` | 反馈已处理完毕（git 可追溯） | 处理完成后 | 默认跳过；满足清除条件（§5.3.1）时回到待处理 |

> 当轮对话内的反馈由 AI 即时处理（§5.3.1），不经 `[修改]`/`[驳回]` 态；`[修改]`/`[驳回]` 只在运行结束仍有余留反馈时出现，供下一轮 Step Start 检出（§5.1.3）。

**跨轮反馈持久化**：人类反馈（对话为主，§5.3.1）由 AI 当轮处理并追 `[已处理]`；若当轮对话结束仍有未落地的修改/驳回意见，AI 将对应条目标记 `[修改]`/`[驳回]` 并加入"待反馈处理"分节 → 下一轮 Step Start 检出（§5.1 变更感知）、Step 2 处理。AI 每轮写回后提交基线（§5.6），保证下一轮 Step Start 的 `git diff <HEAD @上次 AI 运行>..HEAD` 只含人类变更。

#### 5.3.1 AI 处理流程

**触发条件**：Step Start 检出 `需wft02修订` BP，或上一轮 Step End 的反馈等待中人类提出修改意见。

**处理规则**：

1. 从当前轮次对话上下文或退回缺失说明中读取人类反馈意图
2. **逐条处理**（按人类反馈内容）：
   - AI 输出方案后人类直接回复修改意见 → 在当前轮次继续对话处理，不重新走 Step Start
   - wft03 退回的 `需wft02修订` → 下一轮 Skill 调用时走 Step Start 退回优先
3. 修改后检查是否仍满足该 BP 所承接的 STR-E 需求
4. 修改导致结构性变更（A1 路径/CU 清单/配置能力/依赖重组）→ 按需重入 Step 3
5. 全部条目获流水线开发者认可 → BP → `可以SR设计`

按 [91 规范 §A.5.3](91-eos-biz-eng-spec.md#a53-ai-处理流程) 的流程处理人类自然语言反馈：

| 人类输入 | AI 动作 |
|---------|--------|
| 显式修改指令（"X 改为 Y"） | 定位条目 → 理解修改意图 → 执行修改 → 检查是否仍满足 STR-E 需求 → 追加 `[已处理]` → 输出修改摘要 → 继续等待 |
| 质疑/讨论（"为什么选 X？"） | 解释理由，不自动修改，继续等待决策 |
| 整体确认（"整体确认"/"可以"） | 全部条目追加 `[同意] [已处理]` → 总结反馈要点写入 `## AI最近变更` → 推进 BP 至 `可以SR设计` → **提交基线并更新 HEAD（机制见 §5.6，防下轮把 AI 自写当人类变更）** |
| 模糊意见（"粒度太粗"） | 追问具体化，引导可执行指令 |
| 线下修订检出（git diff 发现确认状态标注/自由文本编辑） | 定位条目 → Edit 修订 → 追加 `[已处理]` → 输出修改摘要 → 继续等待 |

> **原则**：反馈处理不是对人类标注的机械执行，而是以 STR-E 需求理解为锚点的方案修订。AI 在 Step 2 中与 Step 3 同为设计者——先理解人类反馈在 STR-E 需求语境中的含义，再执行修订，最后检查修订后的 BP 是否仍满足 STR-E 需求。

**本 Skill 反馈处理特化参数**（[91 规范 §A.5.4](91-eos-biz-eng-spec.md#a54-其他规则) 要求各 Skill 定义以下参数）：

**锚点需求**：STR-E 节点——反馈处理以 STR-E 场景业务需求（引擎归属、能力链、CU 承接关系）为锚，不脱离 STR-E 内容自行修订。

**操作条目**（写入 `04-*.md` BP 节点对应条目的 `确认状态` 字段）：
- A1 产品路径
- 每个 CU（含配置入口/操作活动定义/FR-ENG 指标）
- 每条依赖
- 每条缺口补需

**推进目标**：
- 全部条目 `[同意]`+`[已处理]` → BP → `可以SR设计`
- 存在 `[修改]`/`[驳回]` 且无 `[需重设计]` → 保持 `待确认方案`
- 存在 `[需重设计]` → 保持 `待确认方案`，须进入 Step 3 重做

**清除条件**：已有 `[同意]`+`[已处理]` 的条目默认跳过。仅当本轮新输入满足以下任一条件时，清除 `[已处理]`（保留 `[同意]`）使其回到待处理：
1. 新输入为该条目的直接上游节点（如新 STR-E 是 BP 节点的来源）
2. 新输入与条目同属一个架构层级且边界重叠（如同属 CU，新 CU 改变了已有 CU 的范围）
3. 新输入导致条目定义被修订（变更影响声明标注为修订型或结构型，§A.5.3 Step 1）

**级联触发条件**：A1 产品路径或 CU 详设变更（配置能力扩展 `[调整]`、承接冲突）、依赖关系变更 → 触发需求级联检查（重验 BP 是否仍满足 STR-E 需求）→ 通过后触发资产对齐级联。

**资产对齐级联**：方案变更影响 23/25 资产时更新对齐结论——CU 定义变更 → 更新 25 引用；A1 路径变更 → 更新 23 引用。资产映射由 Step 5 维护。

**元信息维护**：

```bash
bash ../scripts/update-meta.sh ../../80-pl4eos-2-eosdata/04-eos-business-architecture.md bump-version
bash ../scripts/update-meta.sh ../../80-pl4eos-2-eosdata/04-eos-business-architecture.md update-head
```

#### 5.3.2 确认状态格式示例

**AI 写入（Step 3 完成时）** —— 为每个条目初始化 `确认状态` 字段，初始为空：

```text
- A1 产品路径：流程与 IT 域 → @engine-flow → 流程配置 | 确认状态: (空)
- CU：流程节点配置 | 确认状态: (空)
- CU：流转规则配置 | 确认状态: (空)
- 依赖：流转规则配置 → 流程节点配置（引擎内） | 确认状态: (空)
- 缺口补需：缺少发布后回滚审计 CU | 确认状态: (空)
```

**人类在条目 `确认状态` 字段标注** —— 三种标注形式：

```text
- A1 产品路径：流程与 IT 域 → @engine-flow → 流程配置 | 确认状态: [同意]
- CU：流程节点配置 | 确认状态: [修改]：配置入口应补充"流程模板管理"子页面
- CU：流转规则配置 | 确认状态: [驳回]：已在 BP-ENG-FLOW-002 中覆盖
- 依赖：流转规则配置 → 流程节点配置（引擎内） | 确认状态: [同意]
- 缺口补需：缺少发布后回滚审计 CU | 确认状态: [同意]
```

**AI 处理完成后** —— 在人类标注末尾追加 `[已处理]`：

```text
- A1 产品路径：流程与 IT 域 → @engine-flow → 流程配置 | 确认状态: [同意] [已处理]
- CU：流程节点配置 | 确认状态: [修改]：配置入口应补充"流程模板管理"子页面 [已处理]
- CU：流转规则配置 | 确认状态: [驳回]：已在 BP-ENG-FLOW-002 中覆盖 [已处理]
- 依赖：流转规则配置 → 流程节点配置（引擎内） | 确认状态: [同意] [已处理]
- 缺口补需：缺少发布后回滚审计 CU | 确认状态: [同意] [已处理]
```

> `确认状态` 字段由 AI 在方案产出时初始化（空）；对话反馈由 AI 追加 `[已处理]`，线下标注由人类写入、AI 检出后同样追加 `[已处理]`（反馈双轨等价，§5.3），作为 git 可追溯的处理标记。

#### 5.3.3 反馈总结

人类「整体确认」后，AI 将本轮反馈要点总结为一句话，通过 `update-meta.sh add-recent-change` 写入 `04-*.md` 的 `## AI最近变更` 表：

```bash
bash ../scripts/update-meta.sh ../../80-pl4eos-2-eosdata/04-eos-business-architecture.md add-recent-change "wft02-eng" "反馈处理" "<BP-ID>" "<AI总结的反馈要点>"
```

摘要示例：`CU 引擎归属修正：@engine-flow→@engine-entity；CU"节点基本信息"粒度过粗，建议拆分`

---

### 5.4 Step 3 · A1/CU BP 设计

#### 5.4.1 Phase A：STR-E 宿主匹配

对每个 STR-E 节点，按目标引擎和能力域匹配已有 BP（详见 §3.2.3），逐节点做宿主匹配：

| 条件 | 路由 |
|------|------|
| 同一目标引擎 + 能力域一致或互补，落入已有 BP 的 A1 路径 | 归入该 BP → Phase B |
| 不满足 | 进入待聚合池 → Phase C |
| 同一 CU 被多个主引擎争用或 A1 路径不一致 | 标记 `[需裁决]` |
| CU 属于依赖的共享 STR-E | 不进入本 BP（共享能力按独立 BP 建模，消费方只记录依赖，见 §3.2.1 路由规则） |

待聚合池按**同一目标引擎 + 同一能力域**分组（`{引擎, 能力域类型, STR-E列表}`）。此阶段仅粗略分组，CU 去重和完整要素提取在 Phase C 执行。

#### 5.4.2 Phase B：修订已有 BP

1. **要素提取**：从本轮 STR-E 增量合并 A2 上下文（产品/场景引用、业务运行对象、配置/支撑角色、触发事件、状态影响）和 CU 承接关系（详见 §2.3.1、§3.3.1）。已有上下文仅追加不重建
2. **宿主匹配检查**：确认 STR-E 仍在 BP 宿主范围内（详见 §3.2.3）。Q1 直接归入→追加来源；Q2 扩展归入→扩展 BP + 变更声明；冲突→标记 `[需裁决]`。Phase A 匹配是候选筛选、Phase B 是精确匹配——宿主扩展时不得回退 Phase A 重选宿主，继续完成 Phase B 全部处理并标记 `[推断]` 供人类裁决
3. **CU 详设与变更处理**：承接已分配 CU，定义操作活动定义（配置操作/系统处理/运行期能力）、配置入口、FR-ENG 指标和引擎状态迁移（详见 §3.3.2）；执行详设变更处理——配置能力扩展 `[调整]`、承接关系冲突退回 `需wft01修订`（详见 §3.3.3）；不设计页面
4. **变更影响声明**：判定变更类型（无变更/增量型/修订型/结构型），写入 BP 节点末尾（详见 §4.2）

#### 5.4.3 Phase C：新建 BP

1. **确定 A1 产品路径** → 从 STR-E 读取目标引擎和能力域 → 流程与 IT 域 → 引擎 → CU（详见 §3.2.1）
2. **承接 A2 产品业务流程上下文** → 读取 wft01-eng 装配的 A2 上下文（业务配置人员 → STR-E → 配置活动序列 → 运行期能力可用；A2 产品/场景引用、触发事件和状态影响），校验字段完整（详见 §3.2.2）
3. **承接已分配 CU** → 读取 STR-E 的 CU 承接关系 + 配置信息组（概要说明）清单，校验清单完整（详见 §3.3.1）
4. **定义 CU 详细设计（操作活动定义）** → 配置信息组（配置操作/系统处理/运行期能力，步骤化三段式）/配置入口/FR-ENG 指标/引擎状态迁移（详见 §3.3.2）
5. **输出页面推导线索** → 为每个 CU 记录配置角色、入口职责、配置对象、活动顺序和运行支撑需求，交由 wft03-eng 组织功能概要（功能表单）
6. **校验依赖并组装 BP** → A1 路径 + A2 页面序列 + CU 详细设计 + 依赖 + 缺口材料，写入 `04-*.md`

---

### 5.5 Step 4 · 引擎需求增补

Step 4 按每个 BP 实例独立执行。新建 BP → 全量检查；修订已有 BP → 仅当详设内容变化（配置能力扩展/承接冲突）时重验闭合，要素机械检查始终针对变更节点执行。变更影响声明在 Step 3 Phase B 产出（§5.4.2 / §4.2），本节负责详设缺口识别与补需。

**1. 缺口识别与分类**

逐 BP 检查四类缺口，按来源分流：

- **上游缺口**（退回 wft01-eng）：STR-E 场景边界缺失、能力链不闭合 → 退回 `需wft01修订`，携带缺失说明
- **承接缺陷**（退回 wft01-eng）：CU 承接关系或配置信息组清单无法在该 CU 下展开（承接缺失、引擎归属错判、分组与 CU 承接冲突，§3.1）→ 不静默改，退回 `需wft01修订`，由 wft01-eng 重新分配
- **配置能力缺口**（生成补充材料）：配置入口缺失（不知道配置人员从哪里配置）→ 生成补充 FR-ENG；配置操作步骤缺失（缺少关键步骤/对象/约束）→ 标记 `[需裁决]`，需要人类补充业务配置意图；系统处理缺失（缺少编辑时处理/生效后响应）→ 生成补充 FR-ENG；运行期能力缺失（缺少制品/构件/功能）→ 生成补充 FR-ENG；FR-ENG 性能指标缺失（缺少运行期需达标的指标）→ 生成补充 FR-ENG
- **依赖缺口**（生成补充材料或新模型设计）：CU 间依赖断点 → 标记依赖断点并生成补需；跨引擎依赖无对应引擎模型 → 设计新模型记录（走 25 类型模板，`[推断]` 供流水线开发者审核决策），不直接固化
- 缺口 → 进入「补充 FR-ENG 材料生成」。不因缺口脑补节点——缺口就是缺口

**2. 要素机械检查**（逐 BP，发现即标注——在对应条目中写入 `[覆盖缺口]` 及缺失说明，留待下一轮补全）

| 检查项 | 判据 | 标注方式 |
|-------|------|---------|
| A2 上下文 | 产品/场景引用 / 业务运行对象 / 配置角色 / 触发事件 / 状态影响 **完整**（承接自 STR-E） | 缺失项写入 `[覆盖缺口]` |
| A1 产品路径 | 流程与 IT 域 → 引擎 → CU **到位** | 同上 |
| CU 承接 | **非空**，每条承接含配置信息组（概要说明）清单（承接自 STR-E） | 漏标时补充 |
| CU 操作活动定义 | 每个需详设 CU 的配置信息组（配置操作/系统处理/运行期能力）/配置入口/FR-ENG 指标 **已定义** | 不足时标注 |
| CU 依赖关系 | 引擎内 / 跨引擎 / 布局组件与构件 / 治理 **已定义并校验**，断点已标记 | 断点标注 |
| 变更影响声明 | **已写入**（首版写”首版，无变更”；格式见 §4.2） | 缺失时补充 |

**3. 补充 FR-ENG 材料生成**

闭合缺口时生成，写入独立材料文件：

```
路径：../../80-pl4eos-2-eosdata/00-origin-requirement-materials/10-raw-files/
文件名：<YYYYMMDD>-<BP名称>补充原始需求材料.md
```

每个材料文件包含以下字段：

| 字段 | 说明 |
|------|------|
| 材料编号 / 来源 BP / 来源 STR-E | 追溯锚点 |
| 缺口类型 | 上游缺口 / 承接缺陷 / 配置能力缺口 / 依赖缺口 |
| 补充需求文本 / 推断依据 | 详细说明 |
| 建议类型 | `补充OR` / `澄清项` |（承接缺陷退回 wft01-eng 不走补充材料；新配置信息组/载件设计不走补充材料——由 §3.3.2 设计判定 + 设计产物记录检查处理） |
| 确认状态 | 初始为空，人类确认后填写 |

材料生成后登记到 `01-eos-sysdev-status.md` 表 A（生命周期=`已登记`），文件引用写入 BP 节点的完备性检查结果。补充 FR-ENG 材料与 wft03-biz 缺口线索走**同一管道**：经流水线开发者确认 + 完整通过 ort00→ort03 转为正式 FR-ENG OR 后进入本 Skill——能力链缺口发现的补充需求不豁免预处理链。

**4. 设计产物记录检查**

从 25 盘存比对当前 BP 详设引用的引擎/CU 模型：若 BP 详设引用了 25 中不存在的配置信息组/载件类型（详设新增的复用/改进/新增判定，§3.3.2），检查设计产物是否完整到可落账——按 25 类型模板要素（对应系统实体/配置页面/Input/Process/Output/依赖/实例化规则）核对。完整 → 记录进 25（**25 是记录层，承接设计产物**）；不完整 → 保持 `[推断]`，作为人机协同设计决策点在 Step End 中提示流水线开发者审核决策。不生成补充 OR 材料、不登记 OR 状态表、不自动补（详设缺口仍走补充 FR-ENG 回路或退回 wft01-eng，§3.1）。

---

### 5.6 Step 5 · 资产写回与落账

遵循 [91 规范 §11.11](91-eos-biz-eng-spec.md#1111-ai-写回规则) 的统一写回协议。仅写回状态=`可以SR设计` 的 BP 节点。人类已在 Step 2 确认方案（含 CU 配置能力与引擎判定结果），Step 5 不设二次确认。

**写回资产操作**：

1. 遍历已确认 BP 节点，按引擎判定结果写回 23 资产：
   - 已有引擎 → 定位已有块，追加 A1 产品路径引用（去重）
   - 新引擎候选 → 主责能力域设计判定落账（wft01-eng 已判定，本 Skill 记录承接引用）
2. 遍历已确认 BP 节点的 CU 详设，写回 25 资产：
   - 已详设 CU → 写入操作活动定义/依赖/状态迁移引用
   - 详设待确认（`[调整]`/`[推断]`）→ 仅保留设计记录，不固化
3. 记录资产写回记录到 `04`（日期/路径/原子单元/目标资产块/动作/判定）
4. `02` 对应 STR-E 节点推进为 `已详细定义`（记录 `STR-E 消费版本：v{N}`）

**写回时的同步维护**（[91 规范 §11.11](91-eos-biz-eng-spec.md#1111-ai-写回规则) 要求每次写回同时维护以下内容）：

| 操作 | 同步更新 |
|------|---------|
| 新增/修改节点 | 更新域产品索引的 `节点数`；更新 `## AI可以处理节点` 对应分节——节点状态变更时移入或移出 |
| 修改节点状态 | 更新AI可以处理节点对应分节：状态符合消费条件时加入，不符合时移出 |
| 新增/删除子条目 | 更新父节点 `**子条目状态**` 汇总值 |
| 人类标注处理 | 同步更新父节点 `**子条目状态**`（子条目标注时）和 `**确认状态**`（父节点标注时）；AI可以处理节点"待反馈处理"同步 |

**提交基线**（链级不变式，每次写回后必须执行；顺序对齐 wft01-eng §5.6，AI 最近变更 随基线入库）：

1. 在 `## AI最近变更` 表追加一条记录（[91 规范 §11.6](91-eos-biz-eng-spec.md#116-ai最近变更)），含时间/操作 Skill/操作类型/节点ID/变更摘要，操作类型列含 `[→](#节点ID)` 超链接
2. 保留 AI最近变更最近 30 条，超出时从最旧行滚动删除
3. `git add -A` + `git commit`（带 Co-Authored-By: Claude），把本轮 AI 产出（BP 节点、23/25 写回、AI 可以处理节点维护、AI 最近变更）全部落基线——链级不变式：基线 = 最后 AI 提交，下一轮 Step Start 的 `git diff <HEAD @上次 AI 运行>..HEAD` 只含人类变更。反馈确认后的补充提交（§5.3.1 整体确认）同此机制
4. 更新文件头 `**文档版本**`（递增次版本号）、`**修订日期**`、`**HEAD @上次 AI 运行**`（=刚提交的基线 hash）

**写回要求**：幂等去重、局部写回、全貌同步。所有操作在本 Step 内完成，不允许"本次写节点、下次更新索引"。

说明：Step 5 不改变 BP 状态。资产写回是独立收尾，不构成 wft03-eng 前置条件。

---

### 5.7 Step End · 运行结束输出

Step End 遵循 [91 规范 §A.3.2](91-eos-biz-eng-spec.md#a32-step-end-输出模板三区结构) 的三区输出模板（结果→方案反馈→下一步）。本 Skill 实例化如下：

```text
当前状态：<待确认方案 / 可以SR设计>
  本轮 BP：<BP-ID / 新建或修订 / 版本变化 / 来源 STR-E>
资产落账：<未落账 / 已写回 02/04/23/25>
补充原始需求材料：<无 / YYYYMMDD-wft02-eng-批次ID-引擎需求增补.md>（已登记 OR 原料状态表）
新类型设计记录：<无 / 已详设 N 个配置信息组/载件，记录进 25>

一、方案反馈
  直接回复修改意见即可，例如：
    - "节点基本信息组的操作活动定义补充校验规则"
    - "整体确认"（快捷同意全部未标注条目）
    - "流程节点配置的配置信息组粒度太粗，调整一下"
  审核锚点：操作活动定义是否自洽、依赖是否闭合、详设变更 [调整] 是否合理、承接冲突是否已退回 wft01-eng
  可用性锚点：CU 操作活动定义是否足以指导 wft03-eng 组织功能概要（功能表单）？追溯链（STR-E→BP→CU）是否可追踪？CU 定义是否可理解？

二、下一步
  本Skill → 选择 STR-E 节点（`可以详细定义` / `待补充详细定义`）或 `需wft02修订` 的 BP 节点重新运行
  后续    → BP-XXX（可以SR设计）、BP-YYY（待补充SR设计）→ wft03-eng
```

**输出后不要结束对话**，等待人类反馈——人类可直接用自然语言回复修改意见（§5.3.1）：

> 以上是本轮方案。直接回复修改意见即可：
> - "节点基本信息组的操作活动定义补充校验规则"
> - "整体确认"
> - "流程节点配置的配置信息组粒度太粗，调整一下"

---

## 六、AI 标记约定

四类增量标记（`[新增]`/`[变更]`/`[确认]`/`[需裁决]`）的定义及使用规则见 [91 规范 §A.3.1](91-eos-biz-eng-spec.md#a31-ai-增量标记)。

每次 Step 3 完成后，向人类输出一份增量清单，格式示例如下：

```text
=== BP-ENG-FLOW-001 本轮更新清单 ===

[新增] 承接 STR-E-ENG-FLOW-001 v1

已分配 CU 详设
  [新增] “流程节点配置”（@engine-flow）  承接自 STR-E-ENG-FLOW-001（wft01 已分配）
    - 配置信息组：节点基本信息/类型组、关联表单组、办理人/传阅人组、操作项/办理意见组、子业务校验规则组、节点事件组、超时流转规则组
    - 引擎状态迁移：配置草稿 → 已校验 → 已发布 → 已装载
  [新增] “发布配置”（@engine-flow）  承接自 STR-E-ENG-FLOW-001
    - 配置信息组：发布配置组（[调整] 新增回滚审计载件）

完备性检查
  承接完整 ✓ / 依赖闭合检查通过 / 运行期能力可消费（出口闸门）✓
  → 详设缺口: 缺少回滚审计载件 — 已生成补充 FR-ENG 材料
```

---

## 七、自检清单

**一句话**：每次运行收尾前按此清单逐项自查——AI 按 Step 分组核对是否漏做、漏写、漏提交。

**前置与加载（Step Start / 1）**
- [ ] Step Start 已按 §A.4 完成入口决策（退回优先 → 反馈处理 → 新输入），无待处理对象已退出
- [ ] 输入校验通过：STR 类型均为 STR-E、≤5 个、状态符合（`可以详细定义`/`待补充详细定义`）、主责能力域唯一、已分配 CU 结构完整
- [ ] Step Start 已变更感知（git diff，基线前提已满足）并判定变更类型，线下修订节点已纳入"待反馈处理"分节
- [ ] 已加载 STR-E / BP / 23/24 资产全貌 / 25 引擎清单，未默认加载全文件正文
- [ ] "待反馈处理"分节条目已处理（`[修改]`/`[驳回]` 无 `[已处理]`）

**方案反馈处理（Step 2）**
- [ ] 对话反馈已按 §5.3 处理：修改指令 → 检查仍满足 STR-E → 追加 `[已处理]`；整体确认 → 推进 `可以SR设计` + 提交基线
- [ ] 跨轮余留反馈已按状态机标记 `[修改]`/`[驳回]` 并加入"待反馈处理"分节

**A1/CU BP 设计（Step 3）**
- [ ] Phase A 宿主匹配正确（落入已有 BP → Phase B；否则待聚合池 → Phase C）
- [ ] Phase B 宿主匹配检查已执行（Q1 直接归入 / Q2 扩展归入 / 冲突分流正确）
- [ ] CU 承接已读取（承接自 STR-E 的 CU 承接关系 + 配置信息组结构），未重复收敛/未重复判定引擎归属
- [ ] 详设变更处理已执行（配置能力扩展 `[调整]` / 承接关系冲突退回 `需wft01修订`）
- [ ] CU 操作活动定义已定义（配置信息组/配置操作（步骤序列）/系统处理（编辑时+生效后）/运行期能力/FR-ENG 指标 + 引擎状态迁移）
- [ ] 依赖定义与闭合校验已执行（引擎内/跨引擎/布局组件构件/治理四步）
- [ ] 变更影响声明已写入（Phase B）/ "首版，无变更"（Phase C）；增量清单已输出

**引擎需求增补（Step 4）**
- [ ] 缺口识别与分类已执行（上游/承接缺陷/配置能力/依赖四类，分流正确）
- [ ] 要素机械检查已逐 BP 执行（A2 上下文/A1 路径/CU 承接/CU 配置能力/依赖/变更声明）
- [ ] 补充 FR-ENG 材料已生成（不脑补）+ 登记 OR 原料状态表 + 与 wft03-biz 同管道
- [ ] 设计产物记录检查已执行（新配置信息组/载件设计完整到可落账 / 保持 `[推断]` 待决策）

**资产写回与落账（Step 5）**
- [ ] 23/25 资产写回按引擎判定/已详设 CU 分流执行，幂等去重
- [ ] BP/STR-E 状态已推进（STR-E → `已详细定义`），引用链已记录
- [ ] AI 最近变更表、AI 可以处理节点分节已维护
- [ ] 提交基线已执行（AI最近变更 → 滚动删除 → git commit → update-head）

**运行结束（Step End）**
- [ ] 三区摘要已输出，对话等待反馈；整体确认后已提交基线

---

## 八、变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-08-21 | v10.0 | **eng 链「概要/详细」分工框架实施第②步**（人类裁决：表格结构保留、三行升级步骤化；基准=PDF v1.5.2，人类方案见记忆 [[eng-chain-five-layer-framework]]）——本 Skill 定位为**业务详细定义**（eng 链 02 层，操作活动定义）：①**§2.2.1 详设形态对齐 PDF v1.5.2**——配置信息组标题=组名+字段全集（含枚举）、配置操作改**步骤序列**（每步含操作对象+约束+联动）、系统处理改**编辑时处理（校验→写载件→阻断）+生效后响应（装载→界面反馈→运行期语义，载件类型显式标注）**、运行期能力补**分支规则显式化**；补**统一保存机制**（设计会话+全局保存+三类粒度：标量写会话/子表行行级保存/版本级动作全量校验）与**系统标识生成规则**；三条规则补「步骤化纪律」；模板改步骤化三段式；示例升级为 PDF 3.1.5 步骤化样板（节点基本信息/操作项/超时）;②**定位重述**——概述/§1.1 补「业务详细定义（02 层）操作活动定义」、概念章补「概要/详细分工叙述」（02 承接 01 清单+结构判定级概要说明、03 功能概要/04 功能详细/05 产品概要 + 对齐 biz s02 活动/正文条目/表单使用顺序）；③**§3.1/§3.3.2/§3.4/§5 全链术语统一**——「配置能力五要素」→「操作活动定义」、「保存时处理」→「编辑时处理」、「推断配置页面」→「组织功能概要（功能表单）」；④**下游引用随 03/04 改名联动核查**——§1.2 交付链条补 5 层、不负责 #3 拆 03/04、出口闸门改「组织功能概要」、Step End 锚点同步。SKILL 同步（v1.10）。 |
| 2026-08-21 | v9.8 | 承接对象随 wft01-eng v9.3 正名同步（链级待办 #1，依据 wft03-eng v8.0 配置信息组三级分工定案，人类确认）：①**§2.2.1 承接对象**——"CU 的配置信息组结构由 wft01-eng 在分解时按'可以运行的能力'分组确定、本 Skill 承接该结构"→"CU 的配置信息组清单+概要说明由 wft01-eng 在概要设计时产出、本 Skill 承接该清单+概要说明并展开每组内容"；规则①"由 wft01-eng 在分解时确定...承接分组结构"→"在概要设计时确定...承接配置信息组清单+概要说明"（结构冲突改"无法在该 CU 下展开"）；②**§3.1 转换原理**——承接段改"承接 wft01 的配置信息组清单+概要说明，展开每组内容"，两级分工改"由 wft01 概要设计确定"；③**§3.3.1 承接读取**——校验承接结构完整改"每条 CU 承接须含承接的配置信息组（概要说明）清单"；④**§3.3.2**——"承接 wft01 分组结构（§2.2.1）"→"承接 wft01 的配置信息组清单+概要说明（§2.2.1）"，配置信息组内容行同步；⑤**§1.1 目标/负责/不负责、§1.2 上游产出清单、§2.2 一句话、§2.1.2、§3.4 承接完整、§5.2 加载清单、§5.4.3 Phase C、§5.5 承接缺陷/机械检查**同步（「详细需求条目」→「配置信息组（概要说明）」、配置信息组结构→清单）。三段式命名（配置操作/系统处理/运行期能力）保留（人类裁决）。链级：wft01-eng v9.3 + 91 v5.24 + 两 SKILL v1.9/v1.8 同步。 |
| 2026-08-20 | v9.7 | v9.6 一致性通读修复（新读者视角，人类确认）——①**FR-ENG 性能指标补 CU 级要素落点**：§2.2.1 CU 级要素表增指标行（口径=定量或二元判定）、§3.3.2 定义表增指标行、Phase C 步骤4 补齐（对齐 Phase B）、负责4/目标2/§2.1.2 节点要素与边界约定/§5.3 条目/§5.5 要素机械检查/§七 自检清单同步补指标，「配置能力四要素」改「五要素」（概述/§3.1/规则地图）；§5.5 缺口识别补「FR-ENG 性能指标缺失→生成补充 FR-ENG」；②**悬空引用修复**：§2.4 依赖校验流程 3.3.5→3.3.4、§1.1 负责7 详设变更 §3.3.2→§3.3.3、§5.5 删除「（§1.2 上游门禁）」；③**§4.1 状态名映射**改按 §A.6 通用六态自声明（删悬空「状态名映射表」引用；状态名本体与 91 §10.6 对齐不变）；④**§5.1.1 承接缺陷**退回由「标记 `[需裁决]`」改「STR-E→`需wft01修订`」（对齐 §5.5 承接缺陷与 wft01-eng §1.2）；⑤**分组冲突处置**补判据分界——组粒度调整（合并/拆分）→`[调整]`（不改承接关系）、结构冲突（无法在该 CU 下定义）→退回 `需wft01修订`（§2.2.1 规则①/§3.1 与 §3.3.2/§3.3.3 口径对齐）；⑥**目标表合并**：目标2（详细定义 CU，含配置入口+FR-ENG 指标）与目标3（配置能力与指标）合并为「详细定义已分配的 CU」，五项改四项；Step 4 名称保留「引擎需求增补」（链级对齐 wft01-eng「FR-ENG 需求增补」，§5.5 开头已说明本节职责为详设缺口识别与补需）。链级：wft03-eng v7.5（出口闸门承接核查）+ 两 SKILL v1.7/v1.2 同步。 |
| 2026-08-20 | v9.6 | eng 链设计归属重构第二步（wft01-eng v9.2 对偶实施，人类裁决 DP1~DP5）：**本 Skill 收缩为详细定义**——①**§1.1 目的/目标/负责**——目的改"承接已分配 CU，展开配置信息组/配置能力/依赖/状态迁移详细定义"；移除"收敛 CU"负责项（改承接已分配 CU，读取 wft01 承接关系不重新收敛）；补需改详设层缺口（配置能力/依赖/状态迁移缺口 + 承接缺陷退回 wft01）；接收校验改"已分配 CU 结构完整"；②**§2.2 定位拆分→定义**——配置信息组结构承接自 wft01 分解分组（规则①拆分判据迁 wft01 §3.3.1），本 Skill 定义每组内容；③**§2.3 A2 装配改承接**——A2 上下文由 wft01-eng 装配、本 Skill 承接校验（DP3），移除"本 Skill A2 装配性质/为什么 A2 是支撑视角"段（迁 wft01）；④**§3.1 回修**——"设计=分解分配分析"回修为"承接已分配 CU 做详细定义"（承接关系是 wft01 设计结论、不重复收敛/不重复判引擎归属）；移除缺口锚定边界/分组原理/引擎权威两层段（迁 wft01）；补详设缺口处理（配置能力/依赖/状态迁移缺口 + 承接缺陷）+ 详设变更 [调整] 段；⑤**§3.3 收缩为详设规则**——3.3.1 CU 承接读取 / 3.3.2 配置信息组与配置能力定义 / 3.3.3 详设变更处理（配置能力扩展 [调整]、承接关系变更退回）/ 3.3.4 依赖定义与闭合校验；移除收敛七判定/来源类型七类/Phase B 关系检测六关系（迁 wft01 §3.3）；⑥**§3.4 完备性判据**——"CU 覆盖"改"承接完整"（承接缺失退回 wft01）；**出口闸门（运行期能力可消费）正式确立（DP4）**；⑦**DP1 状态改名**——STR-E 输入状态 `可以分解分配`/`待补充分解分配`→`可以详细定义`/`待补充详细定义`，STR-E 推进 `已分解分配`→`已详细定义`；⑧**§5 操作**——Step 3 Phase B 改 CU 详设与变更处理、Phase C 承接 A2/已分配 CU；Step 4 缺口分类改四类（上游/承接缺陷/配置能力/依赖）、要素机械检查改 CU 承接；Step 5 写 25 改"已详设 CU"、STR-E 推进 `已详细定义`；Step End 审核锚点/增量清单同步；⑨§六/§七 同步。链级：91 v5.23（§8.1/8.2/CU 设计权/接力链表/§10.6）+ 两 SKILL v1.8/v1.6 同步。 |
| 2026-08-20 | v9.4 | 张力点③ A2/A1 关系定案（讨论-裁决-落地）：①**§2.3 一句话扩展**——A2 是追溯/上下文支撑视角（非设计视角）、A1 是唯一设计视角，两视角双向可追溯、A2→A1 单向设计支撑；②**§2.3.1 补两段**——(a) **"为什么 A2 是支撑视角而非设计视角"**：STR-E 自带 A2 身份（91 §5.1 双重身份，A2 信息随 STR-E 进入非设计出来）/ A2 完整设计归 wft02-biz（§1.1 不负责，再设计即双权威冲突）/ CU 推导必须用 A2（来源判定证据 §3.3.2 + 追溯链前端锚点 §3.2.2），"已经存在+不能设计+推导需要它"锁死 A2 只能摘取/结构化/支撑；(b) **与 biz A2/Bn 同异对照**——同：双视角/同一上游保一致/缺口从一侧入 OR 回路；异：biz 是双设计视角（各自闭环互为投影、交汇点=输出文档→功能表单），eng 的 A2 是上下文装配视角、A1 是唯一设计视角；关系=**双向可追溯+单向设计支撑（A2→A1）**；A2 完备判据=能否支撑 CU 来源判定与追溯（§5.5 要素机械检查），非 A2 业务流自身闭环。链级：91 §8.2 已为权威口径（A2 只做上下文装配），无需改动。SKILL 无需改动（负责表已含 A2 上下文装配）。 |
| 2026-08-20 | v9.3 | 张力点②配置信息组二级结构原理落地（人类纠正模型：一个 CU 对应一个配置页面，页面内几个字段成组提供可以运行的能力→讨论-裁决-落地）：①**§3.1 补原理段**——CU=**页面单位**（操作/维护边界，回答"管什么"）vs 配置信息组=**能力单位**（运行期边界，回答"运行期产出什么"），边界依据不同（页面 vs 运行期能力）、粒度不同（一页常含多组能力）所以需两级；"CU 可独立选配"落地为"开这个页面配这个 CU"；复合页内的子页面即完整页面、不改变"一 CU 一页面"；分组锚定"可以运行的能力"而非字段技术关系——组须回答"这组字段运行期产出/驱动什么"、运行形态经生效后响应生成的载件具体化；单字段即可提供一项能力则单字段成组、能力不可独立则合并；可运行能力含业务人员可操作能力与系统侧运行行为两类（后者标"以系统处理为主"，运行期同样跑起来只是无用户操作面）；**可独立验证是组结构的下游属性**（组先能运行才谈得上被验证/被消费）；②**§2.2.1 同步**——定义口径"这组字段能够提供一个相对独立的业务能力"→"这组字段共同提供一个可以运行的能力"；规则①同款改措辞（删"可独立交付/验证"、改"可以运行的能力"）；配置入口补"一个 CU 对应一个页面（复合页内的子页面即完整页面）"；说明模板运行期能力括号改"提供业务人员可操作能力的组写，以系统处理为主的组不填此项"。链级：91 规范 §3.3 同口径对齐。SKILL 同步。 |
| 2026-08-20 | v9.2 | 新增/改进引擎、CU 落地为设计内容（人类提出"新增或改进引擎、CU和配置信息组，这些工作正是设计的内容，既要理解设计的内涵和落地思路，也要基于已有资产"→讨论-裁决-落地）：①**§3.1 补设计原理段**——新增/改进引擎、CU、配置信息组是设计内容（AI 语义理解提方案 → 流水线开发者审核/思考/决策/反馈 → AI 落实，人机协同），锚定 25 盘存（含组织已有归档资产；**25 是记录层不裁决**），落地走文档结对骨架（对照盘存判定复用/改进/新增，对齐通用设计规范 §2.3）；"缺口必须回 STR"补两轴区分（业务身份锚 OR/STR，方案设计锚 25 盘存）；新增引擎判定源头在 wft01-eng 主责能力域，本 Skill 匹配不上引擎→退回需wft01修订而非新引擎；②**§3.3.3 新增 CU 改写**——盘存有承接类型→实例化；盘存无承接→按内涵判据设计新 CU 类型（走 25 类型模板，标 `[推断]`，人机协同设计决策点）；③**§5.5 Step 4 改名"设计产物记录检查"**——"25 模型建议（不直接固化）"改为"检查设计产物完整到可落账→记录进 25；不完整→保持 `[推断]` 待决策"；④**"25 模型建议"表述全链去除**——§1.1 目标 5/负责 7/§2.4.1 治理依赖/§5.5 依赖缺口与补充材料建议类型/Step 布局行/Step End 模板/自检清单；⑤**Step 3 定为设计决策点**——人类参与列"无（AI 产出）"→"有（设计决策审核）"，执行模式白话补 Step 3 人机协同说明，三角色分工同步。链级：wft01-eng v9.0 同款（骨架判定 + 模型建议去除）。SKILL 同步。 |
| 2026-08-20 | v9.1 | 反馈检出前置到 Step Start（人类提出"分析 step 框架是否支持两种反馈方式，思路应与 wft01-eng/wft02-biz 一致"→审查发现链级缺陷后修复）：①**Step Start 增加变更感知**（先于入口判定）——git diff H..HEAD 检出人类线下修订，判定变更类型（结构化标注→纳入待反馈 / 自由文本→[需确认] / 格式→忽略）后加入"待反馈处理"分节，**先于入口判定**；补注记说明"先变更感知再入口判定，否则纯线下修订会被误判无待处理对象退出"（对齐 91 §11.10「AI 在 Step Start 中通过 git diff 检测」权威口径）；②**Step 1 步骤一改安全复查**——变更感知已上移 Step Start，此处仅按 §A.3.3 R0a 做加载前安全检查（detect-changes.sh 检出非预期修改标记 [需确认]），职责分离消除混叠；③同步更新：Step 布局表（Start 加变更感知 / Step 1 改安全复查）、反馈机制一句话（git diff 位置改 Step Start）、§5.3 反馈双轨线下修订表述（经 Step Start 变更感知检出）、跨轮反馈持久化（下一轮 Step Start 检出）、§5.6 提交基线（下一轮 Step Start diff）、§七 自检清单。链级推广：wft01-eng v8.9 / wft02-biz v9.0 同款修复（人类确认三个 Skill 一起修） |
| 2026-08-20 | v9.0 | 以 wft02-biz v8.9 + wft01-eng v8.8 为标杆知识层三章重构 + 原理方法审视 + Step 框架对齐：①§二 改为**基本概念**——转换总览（转换方法五步一条链 + 规则地图判据索引，无语言层级——eng 路径严格镜像 wft01-eng）+ 2.1~2.4 概念（2.1 A1 配置单元架构[定义层级+节点要素与职责边界]、2.2 CU 配置能力[定义与详细设计要素]、2.3 A2 上下文与触发追溯、2.4 CU 依赖关系[分类]）；②新 §三 **设计原理与判据**——3.1 转换原理（新增：为什么用 A1 配置单元承接 STR-E）+ 3.2 要素提取规则（原 §2.4，3.2.1~3.2.4）+ 3.3 CU 收敛与依赖推断规则（原 §2.5，3.3.1~3.3.5）+ 3.4 完备性判据（原 §2.1.3 移入）；③新 §四 **CU BP 节点演进**——4.1 生命周期（原 §2.6）+ 4.2 变更传播（原 §2.7）；④原 §三 操作流程 → §五（3.x→5.x 重编号，Step 布局补「人类参与/执行模式」列 + 白话注 + 反馈机制一句话 + 三角色分工表）；⑤Step Start 拆四子节（5.1 入口读取+三类入口表 / 5.1.1 输入校验 / 5.1.2 退回优先 / 5.1.3 反馈处理 / 5.1.4 新 STR-E 路由，对齐 wft01-eng v8.8 D1）；⑥Step 2 补**反馈双轨**（AI 对话反馈主通道 + 线下文档修订经 git diff 检出，两轨等价均落账）+ 确认状态标记状态机 + 跨轮反馈持久化 + 5.3.3 反馈总结（自原 Step End 移入）；⑦Step 4 改名**「引擎需求增补」**（原「缺口补需与变更声明」名不符实——变更声明实际在 Step 3 Phase B 产出，对齐 wft01-eng v8.8 D2「FR-ENG 需求增补」命名模式；章首补一句话说明）；⑧Step 5 补**提交基线顺序修正**——由「AI最近变更→文件头→滚动删除→git commit」改为「AI最近变更→滚动删除→git commit→文件头（HEAD=刚提交基线 hash）」，对齐 wft01-eng v8.5 M1 链级不变式（原顺序使 AI最近变更/文件头 不进基线提交）；⑨Step End 改**三角色四区**——设计审核（流水线开发者）+ 可用性确认（EOS 开发者）+ 下一步，对齐 wft01-eng v8.8 D3；⑩新 §七 **自检清单**（按 Step 分组）；⑪AI 标记 §四→§六、变更记录 §五→§八；⑫正文全部 § 引用迁移（§2.1.3→§3.4、§2.4.1→§3.2.1、§2.4.2→§3.2.2、§2.4.3→§3.2.3、§2.5.1~2→§3.3.1~2、§2.5.3→§3.3.3、§2.5.4→§3.3.4、§2.5.5→§2.4.1+§3.3.5、§2.6→§4.1、§2.7→§4.2、操作 3.x→5.x）。SKILL 同步（v1.1，锚点迁移 + Step 2 反馈双轨 + Step 4 改名 + Step 5 提交基线） |
| 2026-08-14 | v8.6 | §2.2.1 配置信息组补能力锚定判据（对齐 91 规范 v5.13）：配置信息组=某组配置字段，这组字段能够提供相对独立的业务能力；规则① 由"有关系的配置信息"升级为"相对独立的业务能力"（单字段可独立成组、能力不可独立时合并） |
| 2026-08-14 | v8.5 | §2.2.1 CU 定义口径升级为 CU→配置信息组二级结构（对齐 91 规范 v5.12）：配置操作/系统处理/运行期能力以配置信息组为单位说明、配置入口留 CU 级；补组说明模板、三条规则（有关系的配置信息分组/运行期能力非必填/生效后响应编号式）与 @flow-cu-node-config 按组完整示例 |
| 2026-08-14 | v8.4 | §2.2.1 四要素口径精修（对齐 91 规范 v5.11）：配置要素以配置人员操作过程方式说明；系统处理分"保存时处理（逐组校验/落库）+ 生效后响应（生成载件）"两段；运行期能力以业务人员操作过程方式说明（操作界面+操作处理）；新增主语判据、载件分类（数据/配置/界面/构件/实例/支撑）与 @flow-cu-node-config 四要素完整示例 |
| 2026-07-13 | v8.2 | 可读性全面修订：§1.1 目标/负责/不负责从列表改为表格（信息密度降低、跳读更快）；§2.3.1 长段断句（A2装配的性质 + 最低字段要求独立成段）；§2.5.4 Phase B 关系检测表重构（"处理"列分拆为"何时触发"+"AI处理方式"两列 + 前置检查独立成段）；§3.3.1 参数表改为定义列表（消除单列长值表）；§3.5 要素检查清单改为表格 + 补充材料段落结构化拆为字段表。 |
| 2026-07-13 | v8.1 | 基于 §8.2 审视优化三项：§2.3.1 三视角表重构（触发追溯归入A2，保持与91规范 A2/A1 双视角定义一致）+ §2.5.2 补充推断标记规则（非 `STR-E明示` 来源均标 `[推断]`）+ §3.4.1 Phase A 路由表补充共享依赖行（共享能力独立建模，消费方只记录依赖） |
| 2026-07-13 | v7.1 | 对齐 91 规范 v5.2：支持引擎专属与平台共享 STR-E；共享 CU 只在主责场景定义，消费场景仅建立依赖。 |
| 2026-07-13 | v7.0 | 对齐 91 规范 v5.1：撤销本 Skill 的页面设计职责，聚焦 CU IPO、FR-ENG 指标和依赖；平台操作页面由 wft03-eng 推断。 |
| 2026-07-12 | v6.2 | 依据 91 规范 v4.8 补齐 A2+A1 双视角、操作页面序列和引擎状态迁移。 |
| 2026-07-12 | v6.1 | §3.3.1 反馈处理特化参数表新增清除条件（§A.5.3 Step 1）。 |
| 2026-07-12 | v6.0 | 对齐 91 规范 v4.8：§8.x→§A.x 引用体系迁移（§2.6→§A.6 生命周期、§2.7→§A.7 变更传播、§3.3→§A.5 反馈协议、§3.7→§A.3.2 Step End 模板、§四→§A.3.1 AI 标记）；§3.1 对齐 §A.4 入口决策流程（三类入口+AI可以处理节点检测+退回门禁+软硬退出分类）；§3.2 对齐 §A.3.3 读取协议（变更感知→AI可以处理节点优先→索引导航）+ 读取/写回约定引用从"文档编写约定 §十四/§十五"迁移至 91 规范 §A.3.3；§3.3 新增本 Skill 反馈处理特化参数表（原 §8.5 实例化参数表移入 Skill 文档）；§3.6 对齐 §11.11 写回协议（同步维护 AI最近变更/AI可以处理节点/子条目状态/文件头字段+提交规范） |
| 2026-07-09 | v5.0 | 分工链对齐——与 91 规范 v3.18 / wft01-eng v5.0 同步：wft02-eng 从"CU 配置能力定义"扩展为 **CU IPO 详细设计 + 操作页面设计**（配置页+业务页），两类页面从 wft03-eng 下放至此。影响：概述（末级节点 CU、操作页面为新产出）、§1.1 目的（新增操作页面设计目标）、§1.1 负责项#5（定义 CU IPO→设计操作页面）、§1.1 不负责项#3（CU 操作页面→操作页面上的构件识别）、§1.2 下游（wft03-eng 接收操作页面→识别构件）、§2.1.3 判据（运行期能力可消费→让 wft03-eng 识别构件） |
| 2026-07-09 | v4.8 | §3.7 Step End 风格对齐 wft02-biz：补"Step End"引导句+三区说明"（结果→方案反馈→下一步）。本 Skill 实例化如下："、当前状态从 `<N>` 占位概括改为逐项结果枚举行（本轮 BP）+ 新增资产落账行（02/04/23/25）+ 补充原始需求材料行（引擎需求增补）+ 新增 25 模型建议行（eng 独有——Step 4 §3.5 item 4 触发）、方案反馈 `->`→`→`、下一步从泛化描述改为具体 BP-ID + 状态 + STR-E 入口条件引用 |
| 2026-07-09 | v4.7 | §3.4 风格对齐 wft01-biz：五 Phase（A~E）→ 三编号子节（§3.4.1~§3.4.3）；Phase A 宿主匹配改为散文+路由表+聚合池（移除 Q1/Q2/Q3 结果表——内容见 §2.4.3）；Phase B 改为四步编号列表+`→`动作链+`（详见 §x.x）`引用（合并原 B/C/D/E 为步骤3"CU 收敛与关系检测"）；Phase C 改为六步编号列表+`→`链+`（详见 §x.x）`引用；移除冗余 ASCII 追溯图（§2.3.1）/A2 字段表（§2.3.1）/CU 配置能力表（§2.2.1）/BP 节点要素清单（§2.1.2） |
| 2026-07-09 | v4.6 | §3.3 风格对齐 wft01-biz/wft01-eng：拆为 §3.3.1（AI 处理流程）+ §3.3.2（确认状态格式示例），实例化参数表移至 91 规范引用，新增 wft02-eng 特化示例（A1 路径/CU/依赖/缺口补需） |
| 2026-07-09 | v4.5 | §3.2 风格对齐 wft02-biz：开头改为"每次运行加载以下资产"、`—` 替换 `：`、资产用途改为动作导向描述、02/04 补"树画像/状态队列/节点索引"模式、23/24 补"资产全貌层"标注、25 限定 §1.1 + 补"运行时动态读取不缓存"、新增"按需扩大"段落（候选 BP/候选 25 CU 类型/反馈重入/扩大触发）+ 读写约定引用 |
| 2026-07-09 | v4.4 | §二 风格对齐 wft01-biz/wft01-eng 第二章：移除章节引导段、拆为 7 节 18 子节（§2.1.1~§2.7）；定义与提取规则分离（§2.1~§2.3 定义"是什么"→ §2.4~§2.5 定义"怎么做"）；新增 Phase B/C 提取差异表（§2.4.4）+ CU 清单提取规则（§2.5.1）+ Phase B CU 关系检测六种判定（§2.5.4）+ 依赖校验四步流程（§2.5.5）；原 §2.8 拆为 §2.6（生命周期 ASCII 流转图）和 §2.7（变更传播格式模板）独立成节；CU 来源类型表补全示例释义（§2.5.2）；A2 上下文装配独立为 §2.3.1 并补全字段要求 |
| 2026-07-09 | v4.3 | §1.1 风格对齐 wft01-biz：主要目的改为"不仅…更…"递进式修辞、目标统一格式去句号、负责项展开叙事细节（收敛判定七种枚举/四类配置能力拆解/依赖四分类/消费版本记录）、不负责项去句号 |
| 2026-07-08 | v4.2 | 基于全链审核修复：将 A2 从触发追溯补强为 eng 链所需的产品业务流程上下文装配，明确其服务于 CU 推导且不替代业务链输出文档。 |
| 2026-07-08 | v4.1 | 精修 wft02-eng：补充 CU 收敛判定、CU 配置能力定义、依赖关系分类、Phase A~E 设计流程、缺口补需规则和 BP 节点要素；继续保持 91 规范的 CU 口径。 |
| 2026-07-08 | v4.0 | 按 91 规范 v3.15 重构；明确 wft02-eng 末级节点为配置单元，职责聚焦 A2 触发追溯、A1 引擎产品装配、CU 配置能力定义、依赖链和补充 FR-ENG，不再承担操作页面或组件设计。 |
