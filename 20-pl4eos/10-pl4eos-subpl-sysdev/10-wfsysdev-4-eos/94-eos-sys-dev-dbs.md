# 94-eos-sys-dev-dbs · EOS 系统设计产品数据树结构

> 本文定义 EOS 系统设计各阶段输出产品数据文档的树形结构。下列章节为框架，各节结构树逐节填充。

## 一、相关方需求

### 1.1 相关方功能需求

```
场景业务 ───── 业务对象闭环（属性：所属业务域、相关方角色＝主责角色·业务对象生命周期主推动者）
└── 业务输出文档 ──── ① 业务主对象
    └── 文档开发任务节点 ─ ②
        └── 用例场景类操作 ─ ③
```

### 1.2 相关方非功能需求

（结构树待定义）

## 二、业务流程

### 2.1 角色履职业务流程

```
角色履职业务
└── 业务域 ──────── ① 归属域
    └── 相关方角色 ─── ② 主责角色（业务对象生命周期主推动者）
        └── 角色职责 ─ ③ 角色承担的一类职责范围
            └── E2E场景业务 ─ ④ 一个业务对象闭环（D 执行 / PCA 管理）
                └── 业务输出文档 ─ ⑤ 个人级的端到端，流程/工单/计划/看板/进展
                    └── 文档开发任务节点 ─ ⑥ 任务包括多个活动，每个活动对应一个功能表单及其功能
                        └── 用例场景类操作 ─ ⑦ 任务节点上的用例场景类操作，相当于用例场景
```

### 2.2 产品开发业务流程

```
输出产品业务
└── 业务域 ──────────── ① 归属域
    └── 产品类型 ─────── ② 部门级的端到端业务输出
        └── 产品构件类型 ─ ③ 科室级的端到端业务输出
            └── 构件开发WBS（包）─ ④ 文档序列
                └── 构件产品数据（= 业务输出文档）─ ⑤ 个人级的端到端，流程/工单/计划/看板/进展
                    └── 产品数据开发任务节点 ─ ⑥ 任务包括多个活动，每个活动对应一个功能表单及其功能
                        └── 用例场景类操作 ─ ⑦ 任务节点上的用例场景类操作，相当于用例场景
```

### 2.3 引擎开发业务流程

```
BP 产品树（单分支——只有配置单元）
└── 业务域 ──────── ① L1 业务域（IT与信息化）
    └── 产品类型 ────── ② L2 产品类型（EOS平台）
        └── 构件 ────────── ③ L3 构件（引擎，如 流程引擎 @engine-flow）
            └─ WBS ────────── ④ L4 WBS（目录）
                └─ 配置单元 ──── ⑤ L5 配置单元（CU，@xx-cu-zzz）
                    └─ 配置信息组 ── ⑥ L6 枝梢节点
                        └─ 用例场景类操作 ──── ⑦ L7 叶节点
```

## 三、系统需求

### 3.1 系统功能需求

```
SR 系统功能需求树
└── 系统用例功能簇 ─ ① 对系统用例功能类的再聚类
    └── 系统用例功能类 ─ ② 按照某个维度对系统用例功能的聚类
        └── 系统用例功能 ─ ③ 实现同一功能的不同实现方式；
            └── 场景功能 ─ ④ 对应操作项
                └── IPO功能 ─ ⑤ 系统对输入的响应
```

### 3.2 系统非功能需求

（结构树待定义）

## 四、平台构成

```
PB 产品树
└── 业务域 ────── ① L1 业务域（IT与信息化）
    └── 产品类型 ── ② L2 产品类型（EOS平台）
        └── 模型 ────── ③ L3 模型（目录性质：对应代码的文件夹）
            └── 前后端组件 ── ④ L4 枝梢节点（前端组件 / 后端组件）
```

## 五、配置方案

（结构树待定义）

## 六、前后端组件方案

### 6.1 前端组件方案

（结构树待定义）

### 6.2 后端组件方案

（结构树待定义）

