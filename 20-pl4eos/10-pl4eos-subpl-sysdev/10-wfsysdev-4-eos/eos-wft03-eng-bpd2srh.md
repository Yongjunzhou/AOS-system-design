# 业务详细（bpd-eng）→ 系统需求概要（srh-eng）· 系统需求概要设计
**人类方案ID**: wft03-eng-bpd2srh

> **概述**：承接 wft02-eng 详设的配置信息组操作活动定义（配置操作/系统处理/运行期能力，步骤化三段式）与配置入口、FR-ENG 指标，组织为可进入 wft04-eng 的**系统需求概要定义**（eng 链 03 层）——配置功能表单+概要说明（结构判定级）。本 Skill 只组织页面：判定页面职责/页面类别/使用角色/承载关系，输出操作活动清单（承接 02 标注入口，不重复步骤）与页面编排概要（区域/TAB/弹窗入口）；**布局组件/构件与 pa-eng 层收敛项下移 wft04-eng**。该 srh-eng 以功能表单为末级节点，供 wft04-eng 展开系统需求详细、wft05-eng 设计 pa-eng（前后端组件）。
>
> **输入 → 输出**：A1 bpd-eng 节点（`可以srh设计` / `待补充srh设计`）/ 有反馈的 srh-eng 节点 → 系统需求概要 srh-eng（功能表单清单 + 概要说明[页面职责/页面类别/使用角色/承载关系] + 操作活动清单 + 页面编排概要）+ bpd-eng 推进为 `已srh设计` + srh-eng 推进为 `可以详细定义`
>
> **适用场景**：瀑布式系统设计第3步 · 平台能力系统设计路径。
>
> **设计模式参考**：[91-eos-biz-eng-spec.md](91-eos-biz-eng-spec.md)
>
> **版本**：v9.6 | **修订**：2026-08-24

---

## 一、职责定位

**一句话**：本章说明本 Skill 的定位——要达成什么（1.1 目的与目标）、与上下游如何衔接（1.2）。

### 1.1 本 Skill 目的与目标

> **主要目的**：取信于平台开发人员——「这个配置信息组操作活动以什么功能表单呈现、页面如何编排」。承接 wft02-eng 详设的配置信息组操作活动定义，组织为可进入 wft04-eng 的**系统需求概要** srh-eng（功能表单+概要说明），为 wft04-eng 展开组件业务、wft05-eng 设计前后端组件提供输入。

> **三层定位（按 EOS 流水线系统设计规范 §8.1）**：本 Skill 属于第1层（流水线设计）活动——在 EOS 流水线设计层将 wft02-eng 详设的 A1 配置单元 bpd-eng 组织为系统需求概要 srh-eng；产出物经 wft04-eng（系统需求详细）→wft05-eng（产品概要）展开为 pa-eng 后，以设计规范/AI SKILL 形态交付第2层（构件开发），最终由第3层 EOS 开发者（确认者/用户）使用。本 Skill 的执行分工按 EOS 流水线系统设计规范 §8.3：流水线开发者（主导者/审核者）审核 AI 的系统需求概要方案并决策变更路径，AI（执行者）执行标准操作流程，EOS 开发者（确认者/用户）确认交付物可用性。

**目标**——完成以下五项即为本 Skill 使命达成：

| # | 目标 | 说明 |
|:-:|------|------|
| 1 | **承接配置信息组操作活动定义** | 读取 CU 操作活动定义（配置操作/系统处理/运行期能力，步骤化三段式）+ 配置入口 + FR-ENG 指标，校验完整；不重新定义 CU 或组操作活动 |
| 2 | **组织功能表单** | 基于 CU 操作活动定义确定五类页面类别之一，页面以 CU 为边界；功能表单清单含配置类（来自 CU 配置入口）与运行类（来自运行期能力操作页面）两类 |
| 3 | **承接操作活动清单** | 将配置信息组操作活动挂载为页面操作活动清单（配置维护/校验处理/发布生效/运行支撑/治理追溯），逐活动标注承接 02 的标注入口，不重复步骤 |
| 4 | **保留完整追溯** | 功能表单 → 配置信息组 → 来源 CU → bpd-eng → bph-eng 的追溯链完整可验证 |
| 5 | **执行覆盖自查** | 检查每个 CU 的配置入口、配置信息组操作活动定义均被页面承接 |

**本 Skill 负责**：

| # | 职责 | 动作 |
|---|------|------|
| 1 | **接收** | 人类选中的 ≤5 个 A1 bpd-eng 节点 → 校验状态 `可以srh设计`/`待补充srh设计`，检查配置信息组操作活动定义完整（配置操作/系统处理/运行期能力）与 FR-ENG 指标；缺失时退回 wft02-eng |
| 2 | **匹配** | 按 `引擎 + CU + 功能表单职责` 匹配已有 srh-eng → 职责一致则复用，职责扩展则全量重写，无可匹配则新建，冲突标记 `[需裁决]` |
| 3 | **页面组织** | 判定功能表单类别（五类）+ 页面编排概要（区域/TAB/弹窗入口），写页面职责/使用角色/承载关系 |
| 4 | **操作活动清单** | 承接配置信息组操作活动挂载为页面操作活动清单（配置维护/校验处理/发布生效/运行支撑/治理追溯五类），逐活动标注承接 02 的标注入口 |
| 5 | **覆盖自查** | 检查配置信息组操作活动 → 功能表单的承接链完整 |
| 6 | **推进** | 人类确认后：srh-eng → `可以详细定义`，bpd-eng → `已srh设计`，记录消费版本 |
| 7 | **回修** | 接收 wft04-eng 退回的 `需wft03修订` srh-eng，读取缺失说明，按人类方案修订；根因在 bpd-eng 时退回 wft02-eng |

**本 Skill 不负责**：

| 职责 | 归属 |
|------|------|
| 配置信息组操作活动定义（配置操作/系统处理/运行期能力） | → `wft02-eng`（本 Skill 只承接，不重新定义） |
| 功能表单上各组件及其业务（布局组件/构件） | → `wft04-eng` |
| pa-eng 层收敛项标注（组件边界/接口/数据/运行/NFR） | → `wft04-eng` |
| CU 拆分/合并（收敛判定/骨架判定） | → `wft01-eng` |
| 功能表单和窗口标签页设计 | → `wft03-biz` |
| pa-eng 前后端组件设计 | → `wft05-eng` |
| NFR 量化 | 本 Skill 只保留需要 wft04-eng/wft05-eng 消费的 NFR 约束引用 |

### 1.2 与上下游的衔接

**上游 wft02-eng**：产出 A1 bpd-eng，包含配置信息组操作活动定义（配置操作/系统处理/运行期能力，步骤化三段式）、配置入口、FR-ENG 指标和依赖。本 Skill 承接该操作活动定义组织为功能表单并输出操作活动清单，不重新定义 CU 或组操作活动。

**下游 wft04-eng**：消费 `可以详细定义` 的系统需求概要 srh-eng（功能表单清单/概要说明/操作活动清单/页面编排概要），展开功能表单上各组件及其业务（系统需求详细），并标注 pa-eng 层收敛项。若发现页面、操作活动清单或承载关系缺失，将 srh-eng 退回为 `需wft03修订`。

**下游 wft05-eng**：消费 wft04-eng 产出的系统需求详细 srd-eng（`已详细定义`），结合 FR-BIZ 指标约束包和 NFR 约束包设计 pa-eng 三类组件（前端/后端/平台服务）。

---

## 二、基本概念：从 A1 bpd-eng 到系统需求概要 srh-eng

**一句话**：本章讲**基本概念**——从 bpd-eng 到系统需求概要 srh-eng 的核心产物是什么：功能表单 srh-eng（2.1）、页面类别（2.2）、页面编排概要（2.3）与承载关系（2.4）；转换总览先给整条设计主线（五步一条链 + 规则地图）。设计原理与判据见 §三，节点演进见 §四，操作步骤见 §五。

> **概要/详细分工叙述**：eng 链五个 Skill 以「概要定义/详细定义」重述（2026-08-21 定案）——wft01-eng 产出**业务概要定义**（01，末级节点=配置信息组[PL5，A1 产品树末级]）、wft02-eng 产出**业务详细定义**（02，条目=操作活动定义）、本 Skill 产出**系统需求概要定义**（03，末级=**功能表单+概要说明**，结构判定级——页面职责/页面类别/使用角色/承载关系/操作活动清单/页面编排概要）、wft04-eng 产出**系统需求详细定义**（04，条目=功能表单上各组件及其业务，含 pa-eng 层收敛项标注）、wft05-eng 产出**产品概要定义**（05，末级=前后端组件+详细需求定义，07 pa-eng）。STR-E/BP/SR-F/PA 资产名已按 2026-08-24 更名清单迁移：STR-E→bph-eng、BP→bpd-eng、SR-F→srh-eng（03 系统需求概要）/srd-eng（04 系统需求详细）、PA→pa-eng。**03 写系统需求概要 srh-eng、04 写系统需求详细 srd-eng，状态机接力**：03 写功能表单概要→`可以详细定义`、04 写组件业务→`已详细定义`。**03 纯概要**——02 是操作主线定义（操作活动定义步骤化），03 只组织页面（页面视角聚合+编排+类别/角色判定），操作活动清单**承接 02 标注入口、不重复步骤**；**布局组件/构件与 pa-eng 层收敛项下移 04**。

### 转换总览：bpd-eng → srh-eng

**一句话**：本节给整条设计的路线图——转换方法（五步一条链）与规则地图（判据索引）；转换的"为什么"见 §三 3.1 转换原理。

#### 转换方法：五步一条链

**输入**：A1 bpd-eng 节点（`04-*.md`，状态=`可以srh设计`/`待补充srh设计`）。
**输出**：系统需求概要 srh-eng 节点（`05-*.md`，系统需求），供 wft04-eng 展开为系统需求详细（组件业务）、wft05-eng 展开为 pa-eng（前后端组件）。

| 步 | 做什么 | 判据 / 细则 |
|----|--------|-------------|
| ① 路由 | 页面候选生成 + Phase A 宿主匹配（归入已有 srh-eng → Phase B 修订 / 待聚合池 → Phase C 新建） | §5.4.1 |
| ② 要素 | 页面类别判定 + 承载关系（基于 CU 配置入口 + 配置信息组操作活动定义） | §3.2.2 |
| ③ 组织 | 页面编排概要（区域/TAB/弹窗入口）+ 操作活动清单（承接 02 标注入口） | §3.3 / §5.5 |
| ④ 检查 | 覆盖自查 + 变更影响声明 | §3.4 / §5.6 |
| ⑤ 写回 | 资产落账（25 功能表单能力引用）+ 推进 bpd-eng/srh-eng 状态 + 提交基线 | §5.7 |

> 读法：①~⑤ 是设计主线，具体判据在 §三 与 §五；可执行步骤见 §五 操作流程（Start/1/2/3/4/5/6/End）。

#### 规则地图：判据索引

| 判据 | 回答什么问题 | 位置 |
|------|-------------|------|
| 功能表单 srh-eng | 功能表单的形态与要素是什么 | §2.1 |
| 页面类别 | 功能表单承担哪种配置职责 | §2.2 |
| 页面编排概要 | 功能表单如何组织（区域/TAB/弹窗入口） | §2.3 |
| 承载关系 | 功能表单承接哪些配置信息组 | §2.4 |
| 页面候选生成 | 从 CU 配置入口生成哪些功能表单候选 | §3.2.1 |
| 页面类别判定 | 页面类别怎么判（五类） | §3.2.2 |
| 宿主匹配检查 | 复用/扩展/新建/冲突怎么分流 | §3.2.4 |
| 操作活动清单承接 | 页面动作如何承接组操作活动（标注入口） | §3.3 |
| 完备性判据 | srh-eng 是否完备（含推断依据标注/25 关系清晰） | §3.4 |
| 变更类型判定 | 增量/修订/结构，怎么影响下游 | §4.2 |
| 覆盖自查 | 设计输出是否漏做（Step 5 机械检查） | §5.6 |

**标注纪律**（框架全链一致，各 Skill 按域应用）：AI 设计产出中凡需人类确认/裁决的项，统一显式标注——①`[推断]`：AI 按规则推断、供人类确认或裁决（如页面候选、页面类别存疑）；②`[待下游推断]`：信息不足、跨 Skill 持久化待确认（本 Skill 承接已确认锚点，此类别少用）；③`供人类确认`：业务/权威锚点、必须由人类确认（本 Skill 对页面职责冲突/同一 CU 争用标 `[需裁决]` 交人类裁决）。

---

### 2.1 功能表单 srh-eng

**一句话**：本节讲系统需求概要 srh-eng 的概念——定义与分类（2.1.1）、架构与属性（2.1.2）；完备性判据见 §三 3.4。

#### 2.1.1 定义与分类

功能表单是配置、治理或运行支撑人员直接使用的页面单元，用来维护 CU 配置、触发引擎处理、查看发布/运行结果或处理异常。它**不同于** biz 路径面向业务角色的功能表单和窗口标签页（eng 功能表单承接配置操作+配置能力运行的操作页面，两链**同名同层、内容异质、非共享资产**）。

**功能表单清单含两类**（03 输出）：

| 功能表单类别 | 来源 | 说明 |
|----------|------|------|
| **配置类** | 02 CU 配置入口 | 配置人员在哪个页面维护该 CU（一个 CU 对应一个页面；复合页内的子页面即完整页面；流程设计器等多功能表单的**编排容器**，非单个复合功能表单） |
| **运行类** | 02 运行期能力操作页面 | 办理单据页 / 待办清单 / 征集待办 / 待阅清单等 |

功能表单按职责分为五类（由 AI 根据 CU 操作活动定义判定）：

| 页面类别 | 用途 | 典型来源 |
|----------|------|----------|
| 配置维护页 | 维护 CU 配置要素 | 配置入口、配置操作 |
| 配置管理页 | 管理配置版本、状态、引用关系 | 系统处理、治理依赖 |
| 发布生效页 | 校验、发布、装载、回滚、停用 | 系统处理 |
| 运行支撑页 | 查看运行期能力、刷新、重试、处理异常 | 运行期能力 |
| 治理追溯页 | 审计、影响范围、引用关系、操作日志 | 横切治理依赖 |

> 布局组件与构件的详细定义见 [91 规范 §四](91-eos-biz-eng-spec.md#四布局组件构件与路径边界)，eng 路径分工见 [91 规范 §8.3](91-eos-biz-eng-spec.md#83-系统需求概要开发wft03-eng)。

#### 2.1.2 架构与属性

系统需求概要设计涉及两个**相互独立**的维度，不能互相替代（布局组件判定下移 wft04-eng，本 Skill 只到区域/TAB/弹窗入口级别）：

| 维度 | 来源 | 回答的问题 | 进入 srh-eng 的字段 |
|------|------|------------|------------------|
| 页面类型 | 91 规范 §1.1.1 | 是否属于硬代码开发的功能表单？ | `页面类型=功能表单` |
| 页面类别 | 91 规范 §8.3 | 承担哪种配置职责？ | `页面类别` |

> 页面类别判定规则见 §3.2，页面编排概要见 §2.3.1，操作活动清单见 §2.3.2。

**srh-eng 节点完整要素**（写入 `05-*.md` srh-eng 节点块）：

- 页面 ID / 名称 / 页面类型（固定为功能表单）/ 页面类别 / 使用角色（来源 bpd-eng A2 上下文）
- 来源 bpd-eng / 来源 CU / 承载配置信息组清单（承载关系，§2.4）
- 页面职责 · 页面编排概要（区域/TAB/弹窗入口）· 页面字段或配置对象摘要
- 操作活动清单（承接 02 标注入口）· 权限、状态、版本、发布约束
- 确认状态

---

### 2.2 页面类型与页面类别

#### 2.2.1 定义与正交关系

**页面类型**：本 Skill 的末级节点固定为**功能表单**。配置维护页、配置管理页、发布生效页、运行支撑页、治理追溯页是**页面类别**（职责分类），不是新的页面类型。

每个 srh-eng 必须同时写明：**`页面类型=功能表单`** + **页面类别**。

**功能表单粒度**：子页面即完整页面；流程设计器等多功能表单是**编排容器**（承载多个 CU 配置信息组的页面编排，非单个复合功能表单）。布局组件组合与构件识别下移 wft04-eng（[91 规范 §四](91-eos-biz-eng-spec.md#四布局组件构件与路径边界) 定义）。

---

### 2.3 页面编排概要

#### 2.3.1 页面编排概要

页面编排概要回答：一个 CU 或一组强依赖 CU 的功能表单如何组合使用。**它组织的正是 CU 的配置信息组操作活动定义——CU 操作活动是其所有配置信息组操作活动的集成，页面编排概要把该集成组织为页面层的组合方式**。

**要素**（03 到区域/TAB/弹窗入口级别，区域内具体组件下移 04）：
- 功能表单清单
- 区域 / TAB / 弹窗入口（页面内编排级别）
- 页面间跳转和上下文传递
- 权限、状态、版本和发布依赖
- 与来源 CU、承载配置信息组清单的对应关系

#### 2.3.2 操作活动清单（承接 02 标注入口）

操作活动是用户在功能表单上执行的可验证动作，**承接自配置信息组操作活动定义**。本 Skill 输出**操作活动清单**——逐活动标注**承接 02 的标注入口**（配置操作/系统处理/运行期能力），**不重复步骤**（步骤化三段式已在 wft02-eng 详设）：

| 字段 | 说明 |
|------|------|
| 活动名称 | 如新建配置、校验配置、发布配置 |
| 承接 02 标注入口 | 承接配置操作 / 系统处理 / 运行期能力的引用，不重复步骤 |
| 适用页面类别 | 配置维护页 / 配置管理页 / 发布生效页 / 运行支撑页 / 治理追溯页 |

操作活动清单承接配置信息组操作活动定义（三类对应）：

| 操作活动类型 | 承接 02 的操作活动段 | 典型动作 | 适用页面类别 |
|----------|------------------|----------|-------------|
| 配置维护 | 配置操作 | 新建、编辑、复制、删除、导入、导出 | 配置维护页 |
| 校验处理 | 系统处理（编辑时处理） | 校验、预览、冲突检查、依赖检查 | 配置维护/管理页 |
| 发布生效 | 系统处理（生效后响应） | 保存、提交、发布、装载、回滚、停用 | 发布生效页 |
| 运行支撑 | 运行期能力 | 查看生效结果、刷新缓存、重试、异常处理 | 运行支撑页 |
| 治理追溯 | 系统处理（治理） | 查看版本、审计、引用关系、影响范围 | 治理追溯页 |

> **布局组件/构件与 pa-eng 层收敛项下移 wft04-eng**：区域内具体组件+业务（布局组件/构件，功能级不写实现）与 pa-eng 层收敛项标注（组件边界/接口/数据/运行/NFR）由 wft04-eng 承接，本 Skill 不涉及。

---

### 2.4 承载关系（承接锚）

**一句话**：本节讲功能表单内容的承接锚——三级设计分工（2.4.1）与承载关系（2.4.2）。配置信息组是 A1 产品树的末级节点（PL5，能力单元），本 Skill 承接其操作活动定义组织为功能表单。

#### 2.4.1 三级设计分工

配置信息组是 A1 产品树的末级节点（PL5，能力单元），其三级设计分工：

| 层 | 工作 | 产出 |
|----|------|------|
| **wft01-eng 业务概要** | 锚定配置信息组（概要说明）清单 + 结构判定级概要说明（配置什么、运行期提供什么） | 配置信息组清单 + 结构判定级概要说明 |
| **wft02-eng 业务详细** | 承接配置信息组，展开操作活动定义 | 配置操作 / 系统处理 / 运行期能力（步骤化三段式） |
| **本 Skill（wft03-eng）系统需求概要** | 承接 wft02 详设后的操作活动定义，组织为功能表单 | 系统需求概要 srh-eng（功能表单+概要说明） |
| **wft04-eng 系统需求详细** | 承接系统需求概要，展开功能表单上各组件及其业务 | 组件业务 + pa-eng 层收敛项 |

**CU 操作活动 = 其所有配置信息组操作活动的集成**。功能表单以 CU 为边界（页面单位），页面编排概要组织该集成；配置信息组是能力单位（运行期边界），其操作活动定义是功能表单内容的承接锚。

#### 2.4.2 承载关系

| 承载项 | 系统需求概要承接 |
|-------------------|-------------|
| 配置信息组清单（结构判定级） | 功能表单承载的配置信息组清单（来源 CU + 承载配置信息组清单） |
| 配置操作 | 操作活动清单·配置维护类（标注承接 02 入口） |
| 系统处理 | 操作活动清单·校验/发布生效类（标注承接 02 入口） |
| 运行期能力 | 操作活动清单·运行支撑类 + 运行类功能表单（办理单据页/待办清单/征集待办/待阅清单） |

> **边界**：本 Skill 承接操作活动定义组织功能表单，不重新定义操作活动（发现操作活动不足退回 `需wft02修订`）；不重新定义/不拆分组结构（组结构由 wft01-eng 分解确定，发现结构缺陷经 wft02 退回 `需wft01修订`）；布局组件/构件与 pa-eng 收敛项下移 wft04-eng。

---

## 三、设计原理与判据：为什么这样设计、怎么判定

**一句话**：本章讲系统需求概要设计的逻辑——为什么用功能表单承接 CU 操作活动定义（3.1 转换原理）、怎么提取页面要素（3.2）、怎么承接操作活动清单（3.3）、怎么判完备（3.4）；概念见 §二，节点演进见 §四，操作步骤见 §五。

### 3.1 转换原理：为什么用功能表单承接 CU 操作活动定义

**CU 操作活动定义描述"这个配置单元配置什么、系统怎么处理、运行期产出什么"，系统需求概要 srh-eng 回答"为支撑这些，需要哪些功能表单、每页如何编排（区域/TAB/弹窗入口）、页面上有哪些可执行活动"**。wft02-eng 详设的配置信息组操作活动定义是功能表单内容的承接锚——本 Skill 承接该定义，组织为页面形态的系统需求（系统需求概要 srh-eng），供 wft04-eng 展开组件业务、wft05-eng 设计 pa-eng（前后端组件）。承接单位 = 功能表单（末级节点，概要说明为结构判定级）。

**配置信息组是 A1 产品树的末级节点（PL5，能力单元），设计分工各司其职**。wft01-eng 业务概要锚定配置信息组（概要说明）清单并给出结构判定级概要说明（配置什么、运行期提供什么）；wft02-eng 业务详细承接后展开操作活动定义（配置操作/系统处理/运行期能力，步骤化三段式）；本 Skill 系统需求概要承接 wft02 详设后的操作活动定义组织为功能表单。**CU 操作活动 = 其所有配置信息组操作活动的集成**——功能表单以 CU 为边界，页面编排概要组织的正是该集成（若按组分页，集成关系就碎了）。

**操作活动清单是承接，不是再定义业务**。本 Skill 把配置信息组操作活动挂载为页面操作活动清单——配置操作 → 配置维护活动、系统处理 → 校验/发布生效活动、运行期能力 → 运行支撑活动。每个活动**标注承接 02 的标注入口、不重复步骤**（步骤化三段式已在 wft02-eng 详设）。**发现组操作活动不足 → 退回 `需wft02修订`；发现组结构/概要缺陷 → 经 wft02 退回 `需wft01修订`**，不脑补。

**03 纯概要，只组织页面**。本 Skill 只做**结构判定级**页面组织——页面职责/页面类别/使用角色/承载关系/操作活动清单/页面编排概要；**布局组件/构件与 pa-eng 层收敛项下移 wft04-eng**（系统需求详细），不在本 Skill 判定。pa-eng 是 wft05-eng 的设计产出，wft04-eng 标注 pa-eng 层收敛项作为 wft05 设计 pa-eng 的输入。

**推断不是转录，也不越权设计**。本 Skill 不只复述配置信息组操作活动定义，还要在页面上下文推断页面形态（页面类别/页面编排概要）；但页面推断必须有操作活动依据——CU 未承载但页面必需的，标注依据不足退回上游，不静默补判。

**这套原理方法为什么可行**：它以**可验证性**为锚——功能表单以 CU 为边界（页面单位）承接集成操作活动，每个功能表单可被 wft04-eng 承接为组件业务、追溯链（功能表单→配置信息组→CU→bpd-eng→bph-eng→OR）完整、变更可传播；权威归信息最全的一方且只归其信息全的部分（操作活动定义在 wft02、系统需求概要在本 Skill、组件业务在 wft04、组件设计在 wft05）；判据全部可机械判定，操作全部有明确输入输出与执行模式（§5 Step 布局表），原理→判据→操作三层一一咬合，且与 91 规范、biz 路径同一套协议运转。

---

### 3.2 要素提取规则

**一句话**：本节讲从 bpd-eng 提取功能表单要素的规则——页面候选生成（3.2.1）、页面类别判定（3.2.2）、宿主匹配检查（3.2.3）。Phase B（修订已有 srh-eng）与 Phase C（新建 srh-eng）共用以下规则——先看 3.2.4 的 B/C 分工差异表，再读细节更顺。

#### 3.2.1 页面候选生成

对每个非占位 CU，按操作活动定义生成页面候选（页面以 CU 为边界，内容以配置信息组操作活动为线索）：

| CU 操作活动定义 | 页面/活动映射 |
|----------------|---------------|
| 配置入口 | 配置类功能表单：配置维护页、配置列表、配置详情、选择弹窗 |
| 配置操作（组级） | 字段/规则编辑区、关系维护区、辅助栏详情、选择器 |
| 系统处理（组级） | 校验、保存、发布、装载、回滚、停用、异常处理活动，以及执行记录页 |
| 运行期能力（组级） | 运行类功能表单：能力预览页、运行支撑页、结果查看页、异常处理页、办理单据页、待办清单；业务运行页面本体仍由 biz 路径承接 |

候选覆盖五类：配置维护页、配置管理页、发布生效页、运行支撑页、治理追溯页。

#### 3.2.2 页面类别判定

**页面类型**固定为`功能表单`。按配置职责确定页面类别：

| 判定条件 | 页面类别 |
|----------|----------|
| 多条配置对象或版本记录维护 | 配置维护/管理页 |
| 层级对象维护 | 配置维护页 |
| 单个配置对象编辑 | 配置维护页 |
| 主配置与子规则强相关 | 配置维护/管理页 |
| 发布、装载、回滚或运行状态处理 | 发布生效/运行支撑页 |
| 审计、引用关系或影响范围追溯 | 治理追溯页 |
| 临时选择、确认、局部参数或冲突处理 | 从属父页面类别 |

> 布局组件组合判定下移 wft04-eng（本 Skill 只到页面类别与页面编排概要级别）。

#### 3.2.3 宿主匹配检查

按 `引擎 + CU + 页面类别 + 页面职责` 匹配已有功能表单：

| 结果 | 判定 | 动作 |
|------|------|------|
| 复用 | 页面类别和页面职责匹配 | 追加 bpd-eng/CU 来源，标记 `[确认]` |
| 扩展 | 页面类别和职责一致，但操作活动清单或页面编排概要扩展 | 全量重写该功能表单 srh-eng，保留历史引用，输出变更声明 |
| 新建 | 无可匹配的功能表单 | 新建功能表单 srh-eng |
| 冲突 | 页面职责冲突或同一 CU 被多个功能表单争用 | 标记 `[需裁决]` |

#### 3.2.4 Phase B（修订）与 Phase C（新建）的提取差异

| 维度 | Phase B（修订） | Phase C（新建） |
|------|----------------|-----------------|
| 判定范围 | srh-eng 已承接全部 bpd-eng/CU + 本轮新 bpd-eng/CU | 待聚合池本组 bpd-eng/CU |
| 页面清单基线 | 已有清单为基线，仅扫描新 CU 是否引入新页面 | 从零构建 |
| 页面候选 | 增量生成（仅新 CU 触发） | 全量生成（§3.2.1） |
| 宿主匹配 | 执行匹配检查（§3.2.3），可能检出复用/扩展/新建/冲突 | 无需匹配，直接新建 |
| 操作活动清单 | 基线基础上增量合并（关系检测见 §3.3.2） | 全量承接 |
| 变更传播 | 输出变更影响声明（§4.2） | 写"首版，无变更" |

---

### 3.3 操作活动清单承接规则

**一句话**：本节讲操作活动清单承接的规则——活动类型与标注（3.3.1）、Phase B 功能表单关系检测（3.3.2）。Phase B（修订已有 srh-eng）与 Phase C（新建 srh-eng）共用以下规则。

#### 3.3.1 活动类型与标注

对每个功能表单的操作活动，按 §2.3.2 的五类活动类型逐项标注（**承接 02 标注入口，不重复步骤**）。活动类型由页面类别和 CU 操作活动定义共同决定，操作活动清单承接配置信息组操作活动（承接映射见 §2.4.2）：

| 页面类别 | 主导活动类型 | 辅助活动类型 |
|----------|-------------|-------------|
| 配置维护页 | 配置维护 | 校验处理 |
| 配置管理页 | 配置维护 | 治理追溯 |
| 发布生效页 | 发布生效 | 校验处理、运行支撑 |
| 运行支撑页 | 运行支撑 | 治理追溯 |
| 治理追溯页 | 治理追溯 | — |

每个操作活动写入：活动名称、承接 02 标注入口（配置操作/系统处理/运行期能力引用）、适用页面类别（§2.3.2 清单字段）。

#### 3.3.2 Phase B 功能表单关系检测

宿主匹配通过后，将本轮生成的功能表单与 srh-eng 已有清单比对。比对锚点为**页面类别 + 页面职责匹配**。

| 关系 | 判定 | 处理 |
|------|------|------|
| 确认 | 页面匹配 + 类别一致 + 来源升级（新 CU→已有页面） | 追加 bpd-eng/CU 来源 |
| 无新增 | 页面匹配 + 类别一致 + 所有来源已确认 | 增量合并新 CU 的细节（如有） |
| 页面职责扩展 | 页面匹配 + 类别一致 + 操作活动清单/页面编排概要边界扩展 | 全量重写，保留历史引用 |
| 补充 | 页面不匹配 | 新增功能表单 srh-eng |
| 过时 | 基线已有页面**未被**本轮 CU 覆盖 | 保留在清单中，标注`过时`，由人类裁决移除 |

> 关系检测前先基于**全部 CU** 重新判定页面类别——新 CU 可能改变页面职责（如类别变更）。`[调整]` 是与关系类型正交的叠加标记，用于页面职责/操作活动清单有变化时。

---

### 3.4 完备性判据

| 检查项 | 判据 | 缺口处理 |
|--------|------|----------|
| CU 覆盖 | 每个非占位 CU 均有功能表单承接 | 补充功能表单或退回 wft02-eng |
| 配置入口覆盖 | 每类配置入口都有功能表单承接 | 补充功能表单或退回 wft02-eng |
| 配置操作覆盖 | 各组配置操作有操作活动清单承接（标注 02 入口） | 补充操作活动清单或退回 wft02-eng |
| 系统处理覆盖 | 各组系统处理（编辑时处理/生效后响应）有操作活动承接，或写明不适用理由 | 补充操作活动清单或退回 wft02-eng |
| 运行期能力覆盖 | 各组运行期能力有运行类功能表单承接（查看/预览/触发/支撑），或说明由 biz 页面承接 | 补充运行支撑功能表单或说明承接方 |
| 追溯覆盖 | 功能表单 → 配置信息组 → CU → bpd-eng → bph-eng 追溯完整 | 补充追溯引用 |
| 共享不重复 | 共享 CU 的功能表单只在主责链下定义；消费场景仅保留入口或依赖引用 | 删除重复功能表单，补共享依赖 |
| 推断依据标注（缺口闭环） | 功能表单/操作活动清单的推断均标注 `[推断]` 及依据，无静默补判 | 补标注依据；依据不足退回上游 |
| 25 关系清晰（资产对齐） | 25 中功能表单能力引用已有/待确认/缺失关系已标注 | 标注新类型设计记录或阶段性标记 |

---

## 四、srh-eng 节点演进：生命周期与变更传播

**一句话**：本章讲 srh-eng 节点设计与确认后的演进机制——生命周期（4.1，节点状态四态主线 + 上游变更分流 + 本 Skill 写回范围）与变更传播（4.2，修订后写变更影响声明告诉下游 wft04-eng）。

### 4.1 srh-eng 节点生命周期

srh-eng 节点沿 [91 规范 §A.6](91-eos-biz-eng-spec.md#a6-节点生命周期模型) 的四态主线流转。状态名映射按 §A.6 通用六态自声明（见下方主线流转与写回范围）。**03 写系统需求概要 srh-eng、04 写系统需求详细 srd-eng，状态机接力**：03 写功能表单概要→`可以详细定义`、04 写组件业务→`已详细定义`。并行推进规则（srh-eng 线与 pa-eng 线互不阻塞）同 §A.6。

**主线流转**（wft03-eng ↔ wft04-eng）：

```
待确认方案 ──人类确认──→ 可以详细定义 ──wft04锁定──→ 在详细定义 ──wft04完成──→ 已详细定义
     ↑                        ↑                         │
     │                        │               wft04退回  │
     │                        │                         ↓
     └─── wft03修订 ────── 需wft03修订 ←───────────────┘
```

**上游变更**（新 bpd-eng/CU 汇入触发 srh-eng 修订）：

```
可以详细定义 ──新bpd-eng/CU修订──→ 可以详细定义（版本递增，wft04 未消费）
在详细定义   ──新bpd-eng/CU修订──→ 在详细定义（版本递增，wft04 增量处理）
已详细定义   ──新bpd-eng/CU修订──→ 待补充详细定义 ──wft04补全──→ 已详细定义
```

**本 Skill 写回范围**：`待确认方案` / `可以详细定义` / `待补充详细定义` / `需wft03修订`；`在详细定义` 和 `已详细定义` 由 wft04-eng 写入和推进；bpd-eng 侧 `在srh设计`（Step Start 锁定）`/已srh设计`（Step 6 推进）由本 Skill 写入（对齐 wft02 §4.1 与 91 §A.6 锁定态=下游写入）。

**下游退回处理**：读取 wft04-eng 缺失说明 → 按人类改进方案修订（走 Phase B）→ 人类确认 → srh-eng → `可以详细定义`。根因在 bpd-eng 时退回 wft02-eng。

### 4.2 srh-eng 变更传播协议

通用定义（变更类型分类、判定优先级、声明模板、增量清单与变更影响声明的分工）见 [91 规范 §A.7](91-eos-biz-eng-spec.md#a7-变更传播协议)。

**本 Skill 变更影响声明格式**（每次修订后写入 srh-eng 节点末尾的 `05-*.md` 节点块内）：

```text
### 变更影响声明

srh-eng 版本：v{N}（本次修订前为 v{N-1}）
本轮变更类型：无变更 / 增量型 / 修订型 / 结构型

变更项：
- [新增] / [变更] / [确认] / [需裁决] ...
  说明字段中记录细分原因：功能表单新增 / 页面类别变更 / 页面职责扩展 / 过时 / 冲突等

下游已消费此 srh-eng 的 srd-eng 详细节点：
  <wft04-eng 系统需求详细节点ID列表>（如无则写"尚未被下游消费"）

对已消费详细节点的影响：
  受影响节点：<节点ID + 受影响原因>
  不受影响节点：<节点ID>
  待新增节点：<新功能表单对应的待展开组件业务说明>

建议 wft04-eng 动作：
  - 无变更：无需动作
  - 增量型：增量展开新增页面/活动的组件业务，已有详细节点不动
  - 修订型：修订受影响详细节点，标记 [来自上游变更]
  - 结构型：输出影响范围，等人类裁决后处理；页面拆分/合并时 wft04-eng 需新建或重组组件业务
```

---

## 五、操作流程

**一句话**：一次运行 = Step Start 入口路由 → Step 1 材料加载 → Step 2 反馈处理 → Step 3 系统需求概要设计 → Step 4 页面编排与操作活动清单 → Step 5 覆盖自查与变更声明 → Step 6 资产写回 → Step End 输出与对话反馈。反馈走自然语言对话（§5.3），跨轮余留由 AI 标记持久化。

### Step 布局总览

| Step | 名称 | 职责 | 人类参与 | 执行模式 | 产出 | 写资产 |
|------|------|------|---------|---------|------|--------|
| Start | 前置校验与路由 | 变更感知 + 入口检测、输入校验、退回优先路由 | 无（AI 自动） | 统一 | 就绪 / 结束 | — |
| 1 | 设计材料加载 | 安全复查 + 读 AI 可以处理节点 + 加载 bpd-eng/srh-eng/资产 | 无（AI 自动） | 统一 | bpd-eng / srh-eng / 资产全景 | — |
| 2 | 方案反馈处理 | 读"待反馈处理"分节 + 对话处理人类修改意见 | **有**——对话反馈修改意见 / 整体确认 | 对话 | 调整后的 srh-eng | `05` |
| 3 | 系统需求概要设计 | Phase A 路由 / B 修订 / C 新建 + 页面类别判定 | **有**——设计决策审核（页面类别/页面职责），对话反馈 | 统一（AI 产出带判据理由）+ 对话 | 功能表单清单 + 类别 + 页面职责 + 承载关系 | `05` |
| 4 | 页面编排与操作活动清单 | 页面编排概要（区域/TAB/弹窗入口）+ 操作活动清单（承接 02 标注入口） | **有**——设计决策审核（页面编排概要/操作活动清单），对话反馈 | 统一（AI 产出带判据理由）+ 对话 | 页面编排概要 + 操作活动清单 | `05` |
| 5 | 覆盖自查与变更声明 | 覆盖自查 + 缺口处理 + 变更影响声明 | 无（AI 自动） | 统一 | 自查结论 + 变更影响声明 | `05` |
| 6 | 资产写回与落账 | 05 写回 + 25 功能表单能力引用 + 推进 bpd-eng/srh-eng 状态 + 提交基线 | 无（AI 自动，写回已确认节点） | 统一 | 04/05/25 写回 + 状态推进 | `04/05/25` |
| Step End | 运行结束输出 | 三区摘要输出 + 等待人类反馈 | **有**——查看摘要、对话反馈 | 对话 | 人类下一步操作提示 | — |

> **执行模式白话**：统一 = AI 自动跑完这步，不需你介入；对话 = AI 输出后需要你说句话（反馈或确认）才继续。Step 1/5/6 为运行内自动步骤——其中 **Step 3/4 是设计步骤（人机协同交互密度最高处）**：AI 承接配置信息组操作活动定义，基于对配置需求与已有资产的语义理解产出系统需求概要方案并附判据理由，人类在 Step End/Step 2 对系统需求概要设计决策（页面类别/页面编排概要/操作活动清单）审核、思考、决策、反馈，AI 落实；Step 2 / Step End 为人机交互步骤（对话反馈，见 §5.3）。

**反馈机制一句话**：反馈走**自然语言对话**（AI 产出方案后不结束对话，§5.3）；跨轮余留反馈由 AI 标 `[修改]`/`[驳回]` 入"待反馈处理"分节（§5.3 状态机）；人类线下修订经 Step Start 变更感知（git diff，§5.1 变更感知）检出后纳入"待反馈处理"分节；AI 每轮写回提交基线（§5.7），保证下一轮 Step Start 的 `git diff <HEAD @上次 AI 运行>..HEAD` 只含人类变更。

---

### 5.1 Step Start · 前置校验与路由

> 入口条件与优先级判定遵循 [91 规范 §A.4](91-eos-biz-eng-spec.md#a4-入口条件与优先级step-start)。

**变更感知（检出人类线下修订，先于入口判定）**

读取目标文件前先检测自上次 AI 运行以来人类是否修改过文件（[§11.10](91-eos-biz-eng-spec.md#1110-ai-反馈检测规则)）：

1. 读取文件头 `**HEAD @上次 AI 运行**` 字段，获取上次提交 hash H
2. Bash("git diff H..HEAD -- 目标文件") → 返回 H 到当前 HEAD 之间的全部变更
3. 无变更 → 进入入口判定
   有变更 → 解析变更行号，读取变更行附近的节点块（上下 5 行）
   → 判定变更类型：结构化标注（`[同意]`/`[修改]`/`[驳回]`）→ 纳入待反馈；非标自由文本 → 标记 `[需确认]`；无关修改（格式/排版）→ 忽略
   → 将存在反馈的节点加入AI可以处理节点"待反馈处理"分节

> 变更感知必须先于入口判定——人类纯线下修订（不选新 bpd-eng）时，只有 git diff 能发现该反馈并将其纳入"待反馈处理"分节，入口判定②才命中。若入口判定先于变更感知，纯线下修订会被误判"无待处理对象"退出。

#### 5.1.1 入口检测

读取产品数据中的 `## AI可以处理节点` 分节（[91 规范 §11.7](91-eos-biz-eng-spec.md#117-ai可以处理节点)），检测三类入口：

| # | 条件 | 检测方式 |
|---|------|---------|
| ① | **有新输入** | 「待 wft03-eng 处理」分节非空 |
| ② | **有待处理反馈** | 「待反馈处理」分节含本 Skill 产出节点 |
| ③ | **有下游退回** | 「待下游退回处理」分节含 `需wft03修订` 的节点 |

三类均不满足 → 输出"无待处理对象" → **退出**。

**退回门禁**（条件③时）：退回节点缺失说明中是否含人类改进方案（`[修改]`/`[同意]` 或下游 Skill 的具体指引）。已提供 → 通过。未提供 → 输出缺失说明，提示提供改进方案 → **退出**。

#### 5.1.2 输入校验

入口检测通过后进行校验。逻辑上不可能的条件为**硬退出**，数量/归属模糊条件为**软提示**：

> **消费前提（出口闸门再验证）**：bpd-eng 推进为 `可以srh设计` 前已通过 wft02-eng 出口闸门（[wft02-eng §3.4](eos-wft02-eng-bph2bpd.md#34-完备性判据)——运行期制品/构件/功能、指标和配置入口足以组织系统需求概要）。此处输入校验对该判据做再验证，配置信息组操作活动定义须含配置操作、系统处理、运行期能力与 FR-ENG 指标。

| 校验条件 | 判定 | 动作 |
|---------|------|------|
| bpd-eng 不是 A1 类型 | 类型不匹配（硬退出） | 输出类型分布，提示走 `wft03-biz` → **退出** |
| bpd-eng 状态不是 `可以srh设计`/`待补充srh设计` | 状态不符（硬退出） | 输出不合格节点及当前状态 → **退出** |
| 配置信息组操作活动定义缺少任一项（配置操作/系统处理/运行期能力/FR-ENG 指标） | 要素缺失（硬退出） | bpd-eng → `需wft02修订`，输出缺失说明 → **退出** |
| CU 依赖不闭合且 AI 无法阶段性说明 | 依赖断点（硬退出） | bpd-eng → `需wft02修订`，输出依赖断点 → **退出** |
| srh-eng 为 `需wft03修订` 且人类未提供改进方案 | 退回方案缺失（硬退出） | 输出 wft04-eng 缺失说明，提示提供改进方案 → **退出** |
| 选中 bpd-eng >5 个 | 超建议阈值（软提示） | 输出超限提示 → 人类确认后继续或退出 |

> 软提示不强制退出，由人类选择继续或退出；硬退出为逻辑上不可能的条件，输出后退出（§A.4.5）。

**锁定写回**：输入校验通过后，将承接的 bpd-eng 状态写为 `在srh设计`（锁定态，本 Skill 消费展开期间，对齐 91 §A.6 锁定态=下游写入 与 wft02 §4.1）；完成系统需求概要且人类确认后推进 `已srh设计`（§5.7）。

#### 5.1.3 分流处理

按优先级顺序执行（源于 [91 规范 §A.4.4](91-eos-biz-eng-spec.md#a44-规则三优先执行顺序)）：

**① 退回优先**：存在 `需wft03修订` 且门禁通过 → 读取缺失说明，按人类方案走 Phase B（§5.4.2），确认后推进为 `可以详细定义`

**② 反馈处理**：存在 `待确认方案` + 未处理反馈 → 进入 Step 2（§5.3）。若全部条目已 `[同意]`+`[已处理]`，不阻塞新 bpd-eng，Step End 时提示

**③ 新 bpd-eng 路由**：有未处理 bpd-eng → 进入 Step 1 → Step 3，逐节点执行 Phase A 宿主匹配（§5.4.1）

> **顺序约束**：Step 2（反馈）必须早于 Step 3（新增设计），因为反馈可能改变 srh-eng 的页面清单/类别/页面编排/操作活动清单。Phase A 匹配须基于 Step 2 修改后的最新 srh-eng 内容。

---

### 5.2 Step 1 · 设计材料加载

> 遵循 [91 规范 §A.3.3](91-eos-biz-eng-spec.md#a33-产品数据文档与资产文档读取与写回约定) 的统一读取协议。变更感知已在 Step Start 完成（§5.1）。

**步骤一：安全复查**

Step Start 已检出人类反馈并纳入"待反馈处理"分节（§5.1 变更感知）。此处按 §A.3.3 R0a 做加载前安全检查：`detect-changes.sh` 检出人类非预期修改时标记 `[需确认]`，不自动推进。

**步骤二：优先读取**（通过 `## AI可以处理节点` 定位本轮入口）

入口判定通过后，从 AI可以处理节点对应分节获取节点 ID 列表：
- 有新输入 → 读「待 wft03-eng 处理」
- 有反馈 → 读「待反馈处理」
- 有退回 → 读「待下游退回处理」

逐节点 Bash(`grep -n '@{节点ID}' 目标文件`) → 定位行号 → Read 节点块。

**步骤三：索引导航**（AI可以处理节点无待处理时，或按域浏览时）

1. Bash(`grep -n '索引行数：' 目标文件`) → 定位索引行号 L，提取 N
2. Read 业务域索引 → 按锚点 Read 域内索引 → 获取节点 ID 列表
3. 按需 Bash(`grep -n '@{节点ID}' 目标文件`) → 定位行号 → Read 节点

**加载清单**（按需扩大——仅在索引缺失、候选冲突或追溯断裂时扩大）：

| # | 文件 | 用途 |
|---|------|------|
| 1 | `04-*.md` bpd-eng 树画像/状态队列/节点索引 | 定位本轮 A1 bpd-eng 正文块（含配置信息组操作活动定义、依赖、追溯） |
| 2 | `05-*.md` srh-eng 架构树画像/状态队列/节点索引 | 定位候选功能表单 srh-eng 节点 |
| 3 | `23-eos-output-architecture.md` | 按 A1 产品路径筛选参考 |
| 4 | `25-eos-engine-models.md` | 按目标引擎/CU 筛选（只读） |
| 5 | `22-eos-nfr-taxonomy.md` | 按功能表单相关 NFR 约束筛选（只读） |
| 6 | `91-eos-biz-eng-spec.md` | 页面类型、布局组件与构件、eng 路径分工 |

**按需扩大规则**（[91 规范 §11.12](91-eos-biz-eng-spec.md#1112-按需读取规则)）：
- bpd-eng / srh-eng 正文块：按 ID 从索引定位
- 候选 srh-eng 块：按同引擎/同 CU/同页面类别筛选，用于复用/扩展匹配
- 反馈重入：仅读取条目的确认状态字段及反馈指向块，不做全量重读
- 扩大触发条件：索引缺失 / 候选冲突 / 资产全貌不足 / 关系断裂

---

### 5.3 Step 2 · 方案反馈处理

**一句话**：处理人类反馈——对话内即时处理（修改 / 整体确认，§5.3.1），跨轮余留按 `[修改]`/`[驳回]` 标记处理（§5.3 状态机）。

人类反馈的通用协议见 [91 规范 §A.5](91-eos-biz-eng-spec.md#a5-反馈交互协议)（反馈通道双轨：对话 + 线下修订）。以下为本 Skill 的特化规则。

**反馈双轨**——人类可任选两种方式反馈，本 Skill 均支持：

- **AI 对话反馈**：Step End 后不结束对话，直接在对话中提出修改意见（自然语言）——AI 按 §5.3.1 即时处理（主通道）
- **线下文档修订**：直接打开 `05-*.md` 修订——在确认状态字段标注 `[同意]`/`[修改]`/`[驳回]`，或直接编辑方案内容（自由文本）——经 Step Start 变更感知（git diff，§5.1 变更感知）检出后按 §A.5 处理

两轨等价：对话反馈由 AI 即时落账（追 `[已处理]`），线下修订经 git diff 检出后同样落账；AI 每轮写回提交基线（§5.7），保证下一轮 diff 只含人类变更。

**本 Skill 的操作条目**：功能表单 + 页面编排概要 + 操作活动清单 + 承载关系。各条目的 `确认状态` 字段写入 `05-*.md` srh-eng 节点块中。

**确认状态标记状态机**（本 Skill 反馈落账标记，定义单源；对话反馈写入者=AI，线下标注写入者=人类——经 git diff 检出后按 §A.5 处理）：

| 标记 | 含义 | 写入时机 | 转迁 |
|------|------|---------|------|
| `(空)` | 条目待人类反馈 | Step 3/4 产出时初始化 | 人类反馈后按情况转下 |
| `[修改]` | 人类提出修改意见、当轮未落地 | AI 在运行结束仍有余留修改意见时写入（跨轮持久化） | 下轮 Step 2 处理完追 `[已处理]` |
| `[驳回]` | 人类否决该条目方案、当轮未落地 | 同上 | 同上 |
| `[同意]` | 人类确认该条目 | "整体确认"时 | 随 `[已处理]` 一并追加 |
| `[已处理]` | 反馈已处理完毕（git 可追溯） | 处理完成后 | 默认跳过；满足清除条件（§5.3.1）时回到待处理 |

> 当轮对话内的反馈由 AI 即时处理（§5.3.1），不经 `[修改]`/`[驳回]` 态；`[修改]`/`[驳回]` 只在运行结束仍有余留反馈时出现，供下一轮 Step Start 检出（§5.1.1 入口②）。

**跨轮反馈持久化**：人类反馈（对话为主，§5.3.1）由 AI 当轮处理并追 `[已处理]`；若当轮对话结束仍有未落地的修改/驳回意见，AI 将对应条目标记 `[修改]`/`[驳回]` 并加入"待反馈处理"分节 → 下一轮 Step Start 检出（§5.1 变更感知）、Step 2 处理。AI 每轮写回后提交基线（§5.7），保证下一轮 Step Start 的 `git diff <HEAD @上次 AI 运行>..HEAD` 只含人类变更。

#### 5.3.1 AI 处理流程

按统一流程处理人类反馈：扫描待处理条目 → 收集整体确认 → 逐条处理 `[同意]`/`[修改]`/`[驳回]` → 级联检查（需求优先、资产对齐随后）→ 判定节点状态 → 确定写回时机 → 落反馈处理记录。

> **原则**：反馈处理不是对人类标注的机械执行，而是以配置信息组操作活动定义为锚点的方案修订。先理解人类反馈在 CU 操作活动定义语境中的含义，执行修订后检查 srh-eng 是否仍满足配置信息组操作活动定义的需求。

按 [91 规范 §A.5.3](91-eos-biz-eng-spec.md#a53-ai-处理流程) 的流程处理人类自然语言反馈：

| 人类输入 | AI 动作 |
|---------|--------|
| 显式修改指令（"X 改为 Y"） | 定位条目 → 理解修改意图 → 执行修改 → 检查是否仍满足配置信息组操作活动定义 → 追加 `[已处理]` → 输出修改摘要 → 继续等待 |
| 质疑/讨论（"为什么选 X？"） | 解释理由，不自动修改，继续等待决策 |
| 整体确认（"整体确认"/"可以"） | 全部条目追加 `[同意] [已处理]` → 总结反馈要点写入 `## AI最近变更` → 推进 srh-eng 至 `可以详细定义` → **提交基线并更新 HEAD（机制见 §5.7，防下轮把 AI 自写当人类变更）** |
| 模糊意见（"粒度太粗"） | 追问具体化，引导可执行指令 |
| 线下修订检出（git diff 发现确认状态标注/自由文本编辑） | 定位条目 → Edit 修订 → 追加 `[已处理]` → 输出修改摘要 → 继续等待 |

**特化参数**：

| 参数 | 本 Skill 值 |
|------|-------------|
| **锚点需求** | 配置信息组操作活动定义——不脱离操作活动定义自行修订 |
| **推进目标** | 全部条目含 `[同意]`+`[已处理]` → `可以详细定义`。存在 `[修改]`/`[驳回]` 无 `[需重设计]` → 保持 `待确认方案`。存在 `[需重设计]` → 保持 `待确认方案`，需重做 Step 3~5 |
| **清除条件** | 已有 `[同意]`+`[已处理]` 的条目默认跳过。仅在新输入满足以下任一时，清除 `[已处理]`（保留 `[同意]`）回到待处理：<br>• 新输入是条目的直接上游节点（如新 bpd-eng 是 srh-eng 来源）<br>• 新输入与条目同属一个架构层级且边界重叠（如新功能表单改变已有功能表单的类别或页面编排）<br>• 新输入导致条目被修订（变更影响声明标注修订型或结构型） |
| **级联触发条件** | 功能表单清单、类别或页面编排概要变更；操作活动清单新增/删除/类型变更 → 触发需求级联检查 → 通过后触发资产对齐级联 |
| **资产对齐级联** | 方案变更影响 25 资产时更新对齐结论——功能表单变更 → 更新 25 功能表单能力引用（由 Step 6 维护） |

#### 5.3.2 确认状态格式示例

**AI 写入（Step 3~5 完成时）**——为每个条目初始化，状态为空：

```text
- 功能表单：流程节点配置页（类型=功能表单；类别=配置维护页；编排=区域/TAB 划分） | 确认状态: (空)
- 功能表单：流程发布管理页（类型=功能表单；类别=发布生效页） | 确认状态: (空)
- 页面编排概要：节点配置页 → 发布管理页（TAB切换） | 确认状态: (空)
- 操作活动清单：新建流程节点（配置维护，承接配置操作·节点基本信息/类型组，节点配置页） | 确认状态: (空)
- 承载关系：流程节点配置页 承载 配置信息组[节点基本信息/类型、办理人、操作项/办理意见] | 确认状态: (空)
```

**人类标注**——三种形式：

```text
- 功能表单：流程节点配置页（...） | 确认状态: [同意]
- 功能表单：流程发布管理页（...） | 确认状态: [修改]：应补充"回滚"操作活动
- 操作活动清单：新建流程节点（...） | 确认状态: [驳回]：应在右键菜单而非按钮入口
```

**AI 处理完成后**——追加 `[已处理]`：

```text
- 功能表单：流程节点配置页（...） | 确认状态: [同意] [已处理]
- 功能表单：流程发布管理页（...） | 确认状态: [修改]：应补充"回滚"操作活动 [已处理]
```

#### 5.3.3 反馈总结

人类「整体确认」后，AI 将本轮反馈要点总结为一句话，通过 `update-meta.sh add-recent-change` 写入 `05-*.md` 的 `## AI最近变更` 表：

```bash
bash ../scripts/update-meta.sh ../../80-pl4eos-2-eosdata/05-eos-system-requirement-summary-architecture.md add-recent-change "wft03-eng" "反馈处理" "<srh-eng-ID>" "<AI总结的反馈要点>"
```

摘要示例：`页面类别调整：流程节点配置页从配置维护页调整为发布生效页；操作活动清单补"回滚"（承接 02 系统处理·生效后响应）`

---

### 5.4 Step 3 · 系统需求概要设计

#### 5.4.1 Phase A：页面候选生成与宿主匹配

对每个非占位 CU，按操作活动定义生成五类功能表单候选（规则见 §3.2.1），然后逐功能表单做宿主匹配：

| 条件 | 路由 |
|------|------|
| 页面类别 + 页面职责落入已有 srh-eng 功能表单 | 归入该 srh-eng → **Phase B** |
| 不满足 | 进入待聚合池 → **Phase C** |
| 页面职责冲突或同一 CU 被多个功能表单争用 | 标记 `[需裁决]` |

待聚合池按 `{引擎, CU, 页面类别, 页面候选列表}` 分组。此阶段仅粗略分组，页面类别判定和完整要素提取在 Phase C 执行。

#### 5.4.2 Phase B：修订已有 srh-eng

1. **要素提取**：从本轮配置信息组操作活动定义提取页面职责、页面编排线索和操作活动候选（§3.2.1、§3.3.1）。已有页面职责仅追加不重建
2. **宿主匹配检查**：确认配置信息组操作活动定义仍在 srh-eng 职责范围内（§3.2.3）。复用 → 追加来源；扩展 → 全量重写保留历史；冲突 → `[需裁决]`。Phase A 是候选筛选，Phase B 是精确匹配——职责扩展时不得回退 Phase A 重选宿主
3. **重判定与关系检测**：基于全部 CU 重判页面类别与页面编排概要（§3.2.2、§2.3.1），比对基线判定五类关系（§3.3.2）。新增/变更操作活动 → 标注操作活动清单
4. **变更影响声明**：判定变更类型（无变更/增量型/修订型/结构型），写入 srh-eng 节点末尾（§4.2）

#### 5.4.3 Phase C：新建 srh-eng

1. **确定页面类型与类别** → 页面类型固定为功能表单；按职责判定五类页面类别之一（§3.2.2）
2. **定义页面职责** → 页面类别 + 页面用途 + 配置对象 + 使用角色（使用角色承接自 bpd-eng 的 A2 上下文，配置或支撑角色）
3. **定义承载关系** → 来源 CU + 承载配置信息组清单（§2.4.2）
4. **承接操作活动清单** → 逐活动标注承接 02 的标注入口（§3.3.1），不重复步骤
5. **组装 srh-eng** → 写入 `05-*.md`：页面 ID/名称/类型/类别 + 使用角色 + 来源 bpd-eng/CU + 页面职责 + 承载关系 + 操作活动清单 + 页面编排概要（完整要素见 §2.1.2）

---

### 5.5 Step 4 · 页面编排与操作活动清单

> 按每个 CU 或强依赖 CU 组独立执行。新建 → 全量设计；修订 → 仅当配置信息组操作活动定义变更或页面拓扑变化时重验编排。

**1. 页面编排概要设计**

基于 Step 3 确定的功能表单清单、类别，组织页面编排概要（其本质是 CU 集成操作活动的页面编排，§2.3.1，03 到区域/TAB/弹窗入口级别）：

- **区域 / TAB / 弹窗入口**：页面内编排级别（区域内具体组件下移 04）
- **页面间跳转与上下文传递**：跳转路径 + CU ID、配置版本、引擎实例等上下文
- **权限、状态、版本、发布依赖**：页面受哪些引擎治理能力约束
- **追溯矩阵**：每个页面 TAB/区块 → 配置信息组 → CU → 操作活动定义

**2. 操作活动清单承接**

对每个功能表单，承接配置信息组操作活动定义标注操作活动清单（§2.4.2：配置操作→配置维护、系统处理→校验/发布生效、运行期能力→运行支撑）。活动类型按 §2.3.2 五类归组。每个活动写入清单字段（活动名称/承接 02 标注入口/适用页面类别，§3.3.1），**标注承接引用、不重复步骤**。所有活动标注 `[推断]`，人类确认后方可落账。

**3. 要素机械检查**（逐功能表单，发现即标注 `[覆盖缺口]`，留待下一轮补全）

- [ ] 区域/TAB/弹窗入口 **已定义**
- [ ] 权限、状态、版本、发布依赖 **已标注**
- [ ] 功能表单→配置信息组→CU→操作活动定义追溯矩阵 **完整**
- [ ] 操作活动清单 **已承接**，标注承接 02 入口
- [ ] 所有非占位 CU 的操作活动定义均被功能表单承接

---

### 5.6 Step 5 · 覆盖自查与变更声明

> 按每个 srh-eng 节点独立执行。新建 → 全量自查；修订 → 仅当功能表单清单或操作活动清单变更时重验。

**1. 覆盖自查**

按九项逐条检查（判据见 §3.4），发现遗漏标注 `[覆盖缺口]`，不回退：

- [ ] CU 覆盖 · 配置入口覆盖 · 配置操作覆盖 · 系统处理覆盖
- [ ] 运行期能力覆盖 · 追溯覆盖 · 共享不重复
- [ ] 推断依据标注 · 25 关系清晰

**2. 缺口处理**

| 缺口类型 | 处理方式 |
|----------|----------|
| 配置入口或配置操作未覆盖 | 补充功能表单或退回 wft02-eng |
| 系统处理未覆盖 | 补充操作活动清单或退回 wft02-eng |
| 运行期能力未覆盖 | 补充运行类功能表单或说明由 biz 承接 |
| 布局组件/构件缺失 | 下移 wft04-eng（本 Skill 不判定，见 §2.3.2） |
| — | 不因缺口脑补节点——缺口就是缺口 |

**3. 要素机械检查**

- [ ] 九项覆盖自查已执行，遗漏项已标注
- [ ] 缺口已分流，处理方式明确
- [ ] srh-eng 版本号已递增（修订时），首版为 v1

**4. 变更影响声明**

新建或修订后写入声明（§4.2）。首版写"首版，无变更"。修订时判定变更类型，列出受影响功能表单、操作活动清单和下游建议。

---

### 5.7 Step 6 · 资产写回与落账

> 遵循 [91 规范 §11.11](91-eos-biz-eng-spec.md#1111-ai-写回规则) 的统一写回协议。仅写回人类已在 Step 2 确认的 srh-eng 条目。不设二次确认。

**写回操作**：

1. `05` 写入或更新 srh-eng 架构节点、状态队列、追溯引用
2. `25` 写入功能表单能力引用或待办，不固化组件设计（组件下移 wft04-eng）
3. `04` bpd-eng 节点写入 `bpd-eng 消费版本：v{N}`，状态推进为 `已srh设计`（Step Start 已锁定为 `在srh设计`）
4. srh-eng → `可以详细定义`
5. 写入资产写回记录（日期/来源 bpd-eng/目标 srh-eng/动作/消费版本/下游状态）

**同步维护**：

| 操作 | 同步更新 |
|------|---------|
| 新增/修改节点 | 更新域产品索引 `节点数`；更新 `## AI可以处理节点` 对应分节 |
| 修改节点状态 | 更新 AI可以处理节点分节——状态符合消费条件时加入，不符合时移出 |
| 新增/删除子条目 | 更新父节点 `**子条目状态**` 汇总值 |
| 人类标注处理 | 更新父节点 `**子条目状态**` 和 `**确认状态**`；AI可以处理节点同步 |

**提交基线**（链级不变式，每次写回后必须执行；顺序对齐 wft02-eng §5.6）：

1. 在 `## AI最近变更` 表追加记录（[91 规范 §11.6](91-eos-biz-eng-spec.md#116-ai最近变更)），含时间/操作 Skill/操作类型/节点ID/变更摘要，操作类型列含 `[→](#节点ID)` 超链接
2. 保留 AI最近变更最近 30 条，超出时从最旧行滚动删除
3. `git add -A` + `git commit`（带 Co-Authored-By: Claude），把本轮 AI 产出（srh-eng 节点、25 写回、AI 可以处理节点维护、AI 最近变更）全部落基线——链级不变式：基线 = 最后 AI 提交，下一轮 Step Start 的 `git diff <HEAD @上次 AI 运行>..HEAD` 只含人类变更。反馈确认后的补充提交（§5.3.1 整体确认）同此机制
4. 更新文件头 `**文档版本**`（递增次版本号）、`**修订日期**`、`**HEAD @上次 AI 运行**`（=刚提交的基线 hash）

> **写回要求**：幂等去重、局部写回、全貌同步。所有操作在 Step 内完成，不允许"本次写节点、下次更新索引"。资产写回是独立收尾，不构成 wft04-eng 前置条件。

---

### 5.8 Step End · 运行结束输出

> 遵循 [91 规范 §A.3.2](91-eos-biz-eng-spec.md#a32-step-end-输出模板三区结构) 的三区输出模板。

本 Skill 实例化如下：

```text
当前状态：<待确认方案 / 可以详细定义>
  本轮 srh-eng：<srh-eng-ID / 新建或修订 / 版本变化 / 来源 bpd-eng>
资产落账：<未落账 / 已写回 04/05/25>

一、方案反馈
  直接回复修改意见即可，例如：
    - "页面类别改为主子表单页"
    - "整体确认"（快捷同意全部未标注条目）
    - "操作活动清单补发布生效段"
  审核锚点：页面类别判定是否合理、操作活动清单承接是否完整（配置信息组操作活动是否全部标注）、承载关系是否完整
  可用性锚点：系统需求概要是否足以指导 wft04-eng 展开组件业务？追溯链（功能表单→配置信息组→CU→bpd-eng→bph-eng）是否可追踪？srh-eng 是否可理解？

二、下一步
  本Skill → 选择 bpd-eng 节点（`可以srh设计` / `待补充srh设计`）或 `需wft03修订` 的 srh-eng 节点重新运行
  后续    → srh-eng-XXX（可以详细定义）、srh-eng-YYY（待补充详细定义）→ wft04-eng
```

**输出后不要结束对话**，等待人类反馈——人类可直接用自然语言回复修改意见（§5.3.1）：

> 以上是本轮方案。直接回复修改意见即可：
> - "流程节点配置页补充'回滚'操作活动"
> - "整体确认"
> - "发布生效页的页面编排调整一下"

---

## 六、AI 标记约定

四类增量标记（`[新增]`/`[变更]`/`[确认]`/`[需裁决]`）的定义及使用规则见 [91 规范 §A.3.1](91-eos-biz-eng-spec.md#a31-ai-增量标记)。

每次 Step 3 完成后，向人类输出增量清单，格式示例如下：

```text
=== srh-eng-ENG-FLOW-PAGE-001 本轮更新清单 ===

[新增] 承接 bpd-eng-ENG-FLOW-001 / CU-流程节点配置

功能表单
  [新增] "流程节点配置页"（类型=功能表单；类别=配置维护页；编排=区域/TAB 划分）
    操作活动清单：新建节点、编辑节点、校验节点、发布节点（承接 02 配置操作/系统处理标注入口）
    承载关系：流程节点配置页 承载 配置信息组[节点基本信息/类型、办理人、操作项/办理意见]

完备性检查
  CU 覆盖 ✓ / 配置入口覆盖 ✓ / 系统处理覆盖 ✓
  → [需裁决] 发布记录页是否与版本记录页合并（操作对象相近，但审计职责可能独立）
```

---

## 七、自检清单

**一句话**：每次运行收尾前按此清单逐项自查——AI 按 Step 分组核对是否漏做、漏写、漏提交。

**前置与加载（Step Start / 1）**
- [ ] Step Start 已按 §A.4 完成入口决策（退回优先 → 反馈处理 → 新输入），无待处理对象已退出
- [ ] 输入校验通过：bpd-eng 均为 A1、≤5 个、状态符合（`可以srh设计`/`待补充srh设计`）、配置信息组操作活动定义完整（配置操作/系统处理/运行期能力/FR-ENG 指标）
- [ ] Step Start 已变更感知（git diff，基线前提已满足）并判定变更类型，线下修订节点已纳入"待反馈处理"分节
- [ ] 已加载 bpd-eng / srh-eng / 23/24 资产全貌 / 25 引擎清单 / 22 NFR，未默认加载全文件正文

**方案反馈处理（Step 2）**
- [ ] 对话反馈已按 §5.3 处理：修改指令 → 检查仍满足配置信息组操作活动定义 → 追加 `[已处理]`；整体确认 → 推进 `可以详细定义` + 提交基线
- [ ] 反馈双轨（对话 + 线下修订）均落账；跨轮余留反馈已按状态机标记 `[修改]`/`[驳回]` 并加入"待反馈处理"分节

**系统需求概要设计（Step 3）**
- [ ] Phase A 宿主匹配正确（落入已有 srh-eng → Phase B；否则待聚合池 → Phase C）
- [ ] Phase B 宿主匹配检查已执行（复用 / 扩展 / 新建 / 冲突分流正确）
- [ ] 页面类别已判定（基于 CU 操作活动定义，五类）；布局组件/构件未判定（下移 04）
- [ ] 配置信息组操作活动定义已承接（不重新定义、不拆分组结构）
- [ ] 承载关系已定义（来源 CU + 承载配置信息组清单）
- [ ] 变更影响声明已写入（Phase B）/ "首版，无变更"（Phase C）；增量清单已输出

**页面编排与操作活动清单（Step 4）**
- [ ] 页面编排概要已组织（区域/TAB/弹窗入口/跳转/上下文/权限），追溯矩阵完整
- [ ] 操作活动清单已承接（配置操作→配置维护、系统处理→校验/发布生效、运行期能力→运行支撑），标注承接 02 入口、不重复步骤
- [ ] 布局组件/构件与 pa-eng 层收敛项未涉及（下移 wft04-eng，本 Skill 不设计任何组件）

**覆盖自查与变更声明（Step 5）**
- [ ] 九项覆盖自查已执行（§3.4），遗漏项已标注 `[覆盖缺口]`
- [ ] 缺口已分流，处理方式明确；不因缺口脑补节点——缺口就是缺口
- [ ] 变更影响声明已写入（首版"首版，无变更"）

**资产写回与落账（Step 6）**
- [ ] 05 写入/更新 srh-eng 节点、25 功能表单能力引用、04 bpd-eng 状态推进（`已srh设计`）
- [ ] AI 最近变更表、AI 可以处理节点分节已维护
- [ ] 提交基线已执行（AI最近变更 → 滚动删除 → git commit → 更新文件头 HEAD=刚提交基线 hash）

**运行结束（Step End）**
- [ ] 三区摘要已输出，对话等待反馈；整体确认后已提交基线

---

## 八、变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-08-24 | v9.7 | 缩写更名（人类定案）——sfh→srh、sfd→srd（sr=系统需求）+ 中文层名统一（系统需求概要/详细定义）；资产名/节点ID/状态名/文件名随迁。 |
| 2026-08-24 | v9.6 | 资产名更名（人类定案）——SR-F→srh-eng、PA→pa-eng（节点类型/节点ID/状态名），版本头同步递增 |
| 2026-08-24 | v9.5 | 资产名更名（人类定案）——SR-F→srh-eng（03 系统需求概要）/srd-eng（04 系统需求详细）、PA→pa-eng（节点类型/节点ID/状态名），版本头同步递增 |
| 2026-08-24 | v9.4 | 文件名更名（人类定名 2026-08-24）——`eos-wft03-eng-bp2sr.md` → `eos-wft03-eng-bpd2srh.md`（业务详细定义→系统需求概要定义，eng 链 03 层）；人类方案ID 与全库引用同步。 |
| 2026-08-22 | v9.3 | eng 链第三轮深审修复（人类逐项裁决）：B15 §1.1「页面类型」来源 91 §1.2→§1.1.1（§1.2 为平台交互模式，页面类型定义在 §1.1.1）。SKILL v1.8 同步（清除条件行 + 材料表 25 读/写）。 |
| 2026-08-22 | v9.2 | A1 树 4 层架构升级 + BP 锁定态闭环（人类裁决 2026-08-22，对齐 91 v5.31）：①blockquote 末级节点口径统一为「配置信息组（PL5，A1 产品树末级）」、§2.4.1「配置信息组是 eng 链的末级节点」→「配置信息组是 A1 产品树的末级节点（PL5，能力单元）」；②A1 锁定态：§5.1 Step Start 补「锁定写回」（BP→`在SR设计`）、§5.7 推进顺序补「Step Start 已锁定」、§4.1 写回范围补 BP 侧 `在SR设计`/`已SR设计` 由本 Skill 写入。 |
| 2026-08-22 | v9.1 | 文档标题修正（机械项）：「BP(A1) → SR-F · **配置页面设计**」→「BP(A1) → SR-F · **系统需求概要设计**」（配置页面是 §1.2 七种页面类型之一=硬代码页面，与系统需求概要不同义，对齐 91 §8.3 章名；wft05-eng 标题「配置页面 SR-F」系人类裁决保留，不在此列）。 |
| 2026-08-21 | v9.0 | **eng 链「概要/详细」分工框架实施第③步**（人类裁决：彻底收缩为纯概要；定案见记忆 [[eng-chain-five-layer-framework]]）——本 Skill 定位为**系统需求概要定义**（eng 链 03 层，功能表单+概要说明，结构判定级）：①**§2 概念章收缩**——2.1 配置页面改**功能表单**（清单含配置类[来自 CU 配置入口]+运行类[办理单据页/待办清单/征集待办/待阅清单]，粒度=子页面即完整页面、流程设计器=编排容器）、2.2 收缩为页面类型与页面类别（去布局组件判定，注明下移 04）、2.3 改**页面编排概要（区域/TAB/弹窗入口）+操作活动清单（承接 02 标注入口，不重复步骤）**、2.4 改**承载关系（来源 CU+承载配置信息组清单）**；删除 PA 层收敛项（§2.3.3，下移 04）；②**§3 原理判据收缩**——3.1 转换原理改「03 纯概要只组织页面」、3.2 去布局组件判定（3.2.3 布局组件判定删除、3.2.2 改页面类别判定、3.2.3 宿主匹配、3.2.4 差异表）、3.3 改**操作活动清单承接**（去 PA 收敛项标注）、3.4 完备性判据 11→9 项（去构件线索覆盖/PA 收敛）；③**§4 生命周期**——SR-F 状态机接力（03 写功能表单概要→`可以详细定义`、04 写组件业务→`已详细定义`），主链 wft03↔wft04、`待产品架构`→`可以详细定义`、写回范围/变更声明下游改 wft04-eng；④**§5 操作**——Step 3 改系统需求概要设计（Phase A/B/C：页面类别/页面职责/承载关系/操作活动清单）、Step 4 改页面编排与操作活动清单、Step 5 覆盖自查 9 项、Step 6 SR-F→`可以详细定义`、Step End 锚点/增量清单同步；⑤**§1 定位重述**——概述/§1.1 补「系统需求概要定义（03 层）」、不负责补「功能表单上各组件及其业务/PA 层收敛项→ wft04-eng」、§1.2 下游改 wft04-eng+补 wft05；⑥§二 概念章补「概要/详细分工叙述」（03/04 承载 SR-F 状态机接力）。SKILL 同步（v1.5）。**v9.0 补充修正（2026-08-21 收尾联动核查）**：§2.4.1 三级设计分工表 + §3.1 原理「锚定配置信息组结构」→「锚定配置信息组（概要说明）清单」2 处，对齐 v9.3 正名口径 |
| 2026-08-21 | v8.0 | 知识层三章重构 + 原理与方法审视（对标 wft02-eng v9.7 / wft01-eng v9.2，人类确认）——①**知识层三章**：§二 基本概念（转换总览：转换方法五步一条链 + 规则地图判据索引；概念 2.1 配置页面 SR-F / 2.2 页面类型类别布局组件 / 2.3 页面功能架构与操作活动 / 新增 2.4 配置信息组业务过程承接锚）、新 §三 设计原理与判据（3.1 转换原理新增 / 3.2 要素提取规则[原 §2.4] / 3.3 操作活动承接与推断规则[原 §2.5] / 3.4 完备性判据[原 §2.1.3 + 补两层]）、新 §四 SR-F 节点演进（原 §2.6/§2.7）、原 §三 操作流程 → §五（3.x→5.x 重编号）、新 §七 自检清单；②**配置信息组分工定案**：wft01 概要锚定组结构 + 概要说明 / wft02 三段式详设（配置过程/系统处理及载件/业务人员操作）/ 本 Skill 承接映射为页面（CU 业务过程 = 配置信息组业务过程集成，页面功能架构组织集成；操作活动=承接不是再定义，系统处理摘要标注承接引用）；③**PA 边界**：PA = wft05 前后端组件，PA 层收敛项 = 待 PA 判断的问题线索（载件是主要来源）；④**完备性判据 9→11 项**：补「推断依据标注（缺口闭环）」「25 关系清晰（资产对齐）」；⑤**方法层对齐**：Step 布局表补人类参与/执行模式列 + 白话注 + 反馈机制一句话 + 三角色分工表、Step 2 补反馈双轨/确认状态标记状态机/跨轮反馈持久化/反馈总结、Step 6 提交基线顺序修正（AI最近变更→滚动删除→git commit→更新文件头）、Step End 补三角色四区（EOS 开发者可用性确认）、新增 §七 自检清单；⑥**术语修正**：负责3 构件识别措辞（来源 91 §四，非"§8.3 任务 1 细化"）、Phase C 使用角色来源补（BP A2 上下文）、§1.1 补三层定位（按 EOS 流水线系统设计规范 §8.1）。链级：SKILL wft03 v1.2→v1.3 同步；wft01-eng「详细需求条目」正名「配置信息组（概要说明）」记入链级待办。 |
| 2026-08-20 | v7.5 | 可消费性承接核查（对齐 wft02-eng v9.7，人类确认）：①**出口闸门承接**——§3.1.2 输入校验补「消费前提（出口闸门再验证）」注记（引用 wft02-eng §3.4：运行期制品/构件/功能、指标和配置入口足以推断配置页面），CU 配置能力校验补 FR-ENG 指标项（对齐负责1「检查指标完整」，此前 §3.1.2 漏查指标）；②**归属修正**——不负责表「CU 拆分/合并 → wft02-eng」改「CU 配置能力定义 → wft02-eng；CU 拆分/合并（收敛判定/骨架判定）→ wft01-eng」（收敛判定已上移 wft01，DP5/91 §8.1 任务2）；③**术语统一**——目标4/负责6「CU IPO」改「CU 配置能力」、负责1「入口线索」改「配置入口」，对齐 wft02/91 §8.3 口径；④**§2.6 状态名映射**改按 §A.6 通用六态自声明（删悬空「§A.6 表（wft03-eng 列）」引用，同 wft02 §4.1 修复）。链级：wft02-eng v9.7 + 两 SKILL v1.7/v1.2 同步 |
| 2026-08-20 | v7.4 | 反馈检出前置到 Step Start（链级推广，人类确认六个 Skill 一起修）：①**Step Start 增加变更感知引导段**（无编号，先于入口判定）——git diff H..HEAD 检出人类线下修订，判定变更类型（结构化标注→纳入待反馈 / 自由文本→[需确认] / 格式→忽略）后加入"待反馈处理"分节，对齐 91 §11.10；②**Step 1 步骤一改安全复查**——变更感知已上移 Step Start，此处仅按 §A.3.3 R0a 做加载前安全检查（detect-changes.sh 检出非预期修改标记 [需确认]） |
| 2026-07-13 | v7.1 | §8.3 对齐审查后修复（目标#3/负责项#3/目标#1）+ 全文可读性优化：§1.1 负责/不负责项改为表格；§2.2.1 拆分混合段落；§2.3.2 活动类型表合并到一处；§2.5.3 关系检测描述精简；§3 各 Step 去除重复引用引导段，合并冗余检查表，密集参数表压缩 |
| 2026-07-13 | v7.0 | 对齐 91 页面概念体系：末级节点统一为配置页面；配置维护等五类改为页面类别；以布局组件组合替代旧六种页面形态，关系检测改按页面类别+布局组合+页面职责。 |
| 2026-07-13 | v6.1 | 对齐 91 规范 v5.2：平台操作页面继承 STR-E 主责能力域；共享 CU 页面只在共享链定义，消费场景仅保留入口或依赖引用。 |
| 2026-07-13 | v6.0 | 对齐 91 规范 v5.1：重新承担由 CU 配置能力推断平台操作页面的职责；统一平台操作页面术语并与 biz 业务应用页面分离。 |
| 2026-07-12 | v5.4 | 依据 91 规范 v4.8 以展示/交互/数据/反馈四类构件组织主判据，并将 PA 内容收敛为下游待处理线索。 |
| 2026-07-12 | v5.3 | §3.3.1 反馈处理特化参数表新增清除条件（§A.5.3 Step 1）。 |
| 2026-07-12 | v5.2 | 对齐 91 规范 v4.8；§8.x→§A.x 引用体系迁移；§3.1 对齐 §A.4；§3.2 对齐 §A.3.3；§3.3 新增特化参数表。 |
| 2026-07-09 | v5.0 | 分工链对齐：wft03-eng 从"为 CU 推断操作页面"转型为"操作页面→构件识别"。 |
| 2026-07-09 | v4.8 | §3.8 Step End 风格对齐 wft02-biz：三区说明+资产落账行。 |
| 2026-07-09 | v4.7 | §3.4 风格对齐 wft01-biz：四 Phase → 三编号子节。 |
| 2026-07-09 | v4.6 | §3.3 风格对齐 wft01-biz：拆为 §3.3.1+§3.3.2。 |
| 2026-07-09 | v4.5 | §3.2 风格对齐 wft03-biz：开头重写+按需扩大。 |
| 2026-07-09 | v4.4 | §二 风格对齐：定义与提取规则分离；拆为 7 节 17 子节。 |
| 2026-07-09 | v4.3 | §1.1 风格对齐 wft01-biz：目的递进式修辞+负责项展开。 |
| 2026-07-08 | v4.2 | 基于全链审核修复：页面类型与组件正交。 |
| 2026-07-08 | v4.1 | 补充页面类别、判定规则和覆盖自查。 |
| 2026-07-08 | v4.0 | 按 91 规范 v3.15 重构；末级节点明确为操作页面。 |
