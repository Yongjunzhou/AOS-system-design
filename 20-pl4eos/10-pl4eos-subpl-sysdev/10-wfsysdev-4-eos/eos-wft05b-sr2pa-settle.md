# SysReq → PA 架构 · Skill B · 资产落账
**Skill ID**: wft05b · **Phase 2: SysReq → PA Settlement**

> **概述**：对 Skill A 已确认的 PA 组件方案执行资产落账（Phase 2 上半）和详细定义展开（Phase 2 下半）。上半为机械性操作（跨域匹配、写回、反向引用），无需人类审阅；下半产出 SysReq 详细定义和 PA 组件需求规格，经人类审阅后冻结。
>
> **输入 → 输出**：已确认 PA 方案（`07-*.md`，状态=待资产落账）→ 资产落账 + SysReq 详细定义（`06-*.md`）+ PA 组件需求规格（`07-*.md`）
>
> **适用场景**：瀑布式系统设计第5步 · Phase 2。Skill A（`eos-wft05a-sr2pa-design.md`）方案通过门禁 1 后运行本 Skill。
>
> **设计模式参考**：[90-design-split-pattern.md](90-design-split-pattern.md)
>
> **版本**：v4.0 | **修订**：2026-06-17

---

## 一、设计角色

- **输入**：`07-*.md` PA 状态表中**当前状态=待资产落账**（进入上半）或**已资产落账/确认中**（进入下半）的目标节点
- **产出1**：资产落账——跨域匹配结果写回 `07-*.md`，反向引用更新 `05/22/23`
- **产出2**：SysReq 详细定义（`06-*.md`）——消费方产出规律：wft05 作为 SR 的消费方产出 SR 详细定义
- **产出3**：PA 组件需求规格——功能清单、接口、数据、验收标准写入 `07-*.md`

### "消费方产出"规律

```
wft03 生产 SR 架构    ← 不知道下游粒度
                    ↓
wft05 消费 SR → 设计 PA  ← 知道 SR 多细才能映射到 PA
  Phase 2 下半：写回 SR 详细定义（06-*.md）+ PA 组件需求规格
```

### Skill B 的本质

Skill B **不做设计决策**——方案在 Skill A 中已确认。Skill B 的操作全部是：
- 匹配（Q1/Q2/Q3 跨域资产归并）
- 写回（将方案正式落入资产文档体系）
- 格式化（将设计思考结果组织为标准化详细定义和规格文档）

---

## 二、前置条件与状态机

### Step Start 路由逻辑

```
读取目标节点状态：
  ├─ 待资产落账 → Phase 2 上半（Step A→F）→ 自动进入 Phase 2 下半
  ├─ 已资产落账 → Phase 2 下半（Step G→H→End）
  ├─ 确认中 → Phase 2 下半（重入：加载反馈）
  └─ 其他 → 不可操作（提示运行 Skill A 或等待人类审阅）
```

### 节点状态（Skill B 管理的范围）

| 状态 | 含义 | 转迁 |
|------|------|------|
| **待资产落账** | Skill A 方案已通过门禁 1 | → 已资产落账（Phase 2 上半完成后） |
| **已资产落账** | 上半完成，等待下半 | → 待确认细节（Phase 2 下半完成后） |
| **待确认细节** | 下半完成，等待人类审阅 | 人类 [同意] → 已验证冻结 |
| **确认中** | 人类反馈修改 | → 待确认细节（重入下半后） |
| **已验证冻结** | 终态 | — |

---

## 三、操作流程

### Step 布局总览

| 阶段 | Step | 名称 | 写资产 |
|------|------|------|--------|
| **Phase 2 上半** | Start | 前置校验与路由 | — |
| | A | 资产环境加载 | — |
| | B | 实体提取 | — |
| | C | Q1/Q2/Q3 跨域匹配 | — |
| | D | 写回主资产 | `07` |
| | E | 反向引用更新 | `05/22/23` |
| | F | 状态推进 | `07` |
| **Phase 2 下半** | G | SysReq 详细定义写入 | `06` |
| | H | PA 组件需求规格 | `07` |
| | End | 输出交互（门禁 2） | — |

> Phase 2 上半完成后**自动**进入 Phase 2 下半（无需人类介入）。若目标节点状态已为"已资产落账"，则 Step Start 直接路由到 Phase 2 下半。

### Step Start · 前置校验与路由

| # | 操作 | 说明 |
|---|------|------|
| 1 | 接收目标节点 | 人类指定的节点 ID/名称 |
| 2 | 读取节点状态 | 从 `07-*.md` PA 状态表读取当前状态 |
| 3 | 检查资产存在性 | 确认 `05/06/07` 存在可读 |
| 4 | 路由决策 | 待资产落账→上半；已资产落账/确认中→下半；其他→结束 |

---

### Phase 2 上半 · 资产落账

> 机械性操作，不需要人类审阅。

#### Step A · 资产环境加载

| # | 操作 | 说明 |
|---|------|------|
| 1 | 加载 07 主资产 | PA 架构树 + 目标组件已确认方案 |
| 2 | 加载 05 SysReq 状态表 | 用于更新反向引用 |
| 3 | 加载 22/23/25 | NFR 引用、PA 引用、引擎模型引用 |

#### Step B · 实体提取

| # | 操作 | 说明 |
|---|------|------|
| 1 | 提取引擎引用 | 从已确认方案中提取依附的引擎实体 |
| 2 | 提取组件类型引用 | 配置方案/前端/后端分类 |
| 3 | 提取业务域归属 | 确定组件所属业务域分区 |

#### Step C · Q1/Q2/Q3 跨域匹配

| # | 操作 | 说明 |
|---|------|------|
| 1 | Q1: 域内复用 | 同一业务域内是否已有可复用组件 |
| 2 | Q2: 跨域归并 | 其他业务域是否有同功能组件需归并 |
| 3 | Q3: 共享决策 | 是否需要抽取为跨域共享组件 |

#### Step D · 写回主资产

| # | 操作 | 说明 |
|---|------|------|
| 1 | 完善组件定义 | 补充 Q1/Q2/Q3 发现的跨域信息 |
| 2 | 确认最终位置 | 确定组件在 PA 架构树中的最终归属 |
| 3 | 写入 07 | 更新 `07-*.md` 组件定义和位置 |

#### Step E · 反向引用更新

| # | 操作 | 说明 |
|---|------|------|
| 1 | 更新 05 SysReq 状态表 | 标记目标 IPO 为"已产品架构" |
| 2 | 更新 22 NFR 引用 | 将 PA 组件引用写入 NFR 引用列表 |
| 3 | 更新 23 PA 引用 | 更新 PA 组件索引 |

#### Step F · 状态推进

| # | 操作 | 说明 |
|---|------|------|
| 1 | 状态 → 已资产落账 | 更新 `07-*.md` PA 状态表 |
| 2 | 自动继续 | 进入 Phase 2 下半（无需人类介入） |

---

### Phase 2 下半 · 详细定义展开

> 入口条件：已资产落账（首次）或 确认中（重入）

#### Step G · SysReq 详细定义写入

| # | 操作 | 说明 |
|---|------|------|
| 1 | 确定展开方式 | 业务定义类：从配对引擎 IPO 的 I 格式化为配置项条目；业务引擎能力类：将 Skill A 分解结果正式记录为能力承接条目 |
| 2 | 写入 06-*.md | 将详细定义条目写入 SysReq 详细定义文档 |
| 3 | 建立 06→07 映射 | 每条详细定义条目与 PA 组件的对应关系 |

#### Step H · PA 组件需求规格

| # | 操作 | 说明 |
|---|------|------|
| 1 | 功能清单 | 组件承接的全部功能点 |
| 2 | 接口定义 | API、消息、回调、存储关系 |
| 3 | 数据定义 | 输入/输出数据结构 |
| 4 | 验收标准 | 可测试的验收条件 |
| 5 | 写入 07 | 更新 `07-*.md` 组件需求规格 |

#### PA 末级节点内容要求

**配置方案组件**应达到"**可开发的定义宿主规格**"粒度，至少包含：组件定义、承接上游条目、配置方案结构（元数据/结构/规则/权限/展示/引用/校验发布）、输入输出、依附引擎、约束与校验、符合性分析、开发/配置指导。

**前后端组件**应达到"**开发可执行定义**"粒度，至少包含：组件定义（前端配置/前端运行/后端定义处理/后端运行执行/接口与集成）、承接上游条目、职责边界、输入（配置方案信息/运行上下文/外部事件）、输出、内部处理逻辑、对外接口、NFR 约束、开发指导。

#### Step End · 输出交互（门禁 2）

| # | 操作 | 执行者 | 说明 |
|---|------|--------|------|
| 1 | 详细定义摘要 | AI | 展示 06 中写入的条目和 07 中的组件规格 |
| 2 | 审查指引 | AI | 告知人类在 `06-*.md` 反馈区标注 [同意] / [修改] |
| 3 | 后续行动建议 | AI | 可进入 t06 / 处理下一个 IPO |

**人类操作**：

- `[同意]`：AI 将状态更新为 `已验证冻结`，该节点可进入 t06
- `[修改]`+"意见"：AI 将状态更新为 `确认中` → 下次重入 Phase 2 下半

### 启动方式

> wft05b [节点名称/ID]

**典型序列**：

```
wft05b 节点X → Step Start（状态=待资产落账）
  → Phase 2 上半（A→F）→ 状态变为已资产落账
  → Phase 2 下半（G→H→End）→ 门禁2
人类：[同意] → 已验证冻结
```

**仅下半重入**：

```
wft05b 节点X → Step Start（状态=确认中）
  → Phase 2 下半（加载反馈 → 调整详细定义 → End）→ 门禁2
```

---

## 四、AI 执行指南

1. **不重做设计**：Skill B 不改变 Skill A 已确认的设计方案
2. **Phase 2 上半全自动**：无需人类介入，完成后直接进入下半
3. **详细定义按 IPO 类型展开**：业务定义类=格式化（从引擎 IPO 的 I 推导），业务引擎能力类=正式记录（Skill A 分解结果）
4. **内容要求必须达标**：组件规格必须达到"可开发"粒度，不能只有命名和一句定义

---

## 五、质量门禁

### Phase 2 上半

- [ ] Q1/Q2/Q3 匹配完成
- [ ] 07 主资产已更新
- [ ] 反向引用（05/22/23）已更新
- [ ] 状态已推进为已资产落账

### Phase 2 下半

- [ ] SysReq 详细定义已写入 06
- [ ] 每条详细条目与 PA 组件有明确对应
- [ ] PA 组件需求规格已写入 07
- [ ] 需求规格达到"可开发"粒度
- [ ] 输出了人类可操作的审查指引

---

## 六、产出物

| 文件 | 写入内容 | 路径 |
|------|---------|------|
| SysReq 详细定义 | SR 详细条目 | `../../80-pl4eos-2-eosdata/06-eos-system-requirements-detailed.md` |
| PA 架构定义 | 落账后组件 + 需求规格 | `../../80-pl4eos-2-eosdata/07-eos-product-architecture.md` |
| SysReq 状态表 | 反向引用更新 | `../../80-pl4eos-2-eosdata/05-eos-system-requirements-architecture.md` |

---

## 七、与 Skill A 的接口契约

| 字段 | 说明 |
|------|------|
| 门禁信号 | `07` PA 状态表：待确认方案 → 待资产落账（人类 [同意] 后） |
| 传递内容 | Skill A 写入 07 的方案（组件树、定义、映射、NFR 决策） |
| Skill B 不重做设计 | 只做匹配/写回/格式化 |

---

## 变更日志

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-06-17 | v4.0 | 从统一文档拆出 Skill B：Phase 2 上半（资产落账）+ Phase 2 下半（详细定义展开 + 组件需求规格） |
