# wft01~05 Design / Settle 拆分统一规范

> **概述**：规定 `wft01~wft05` 在瀑布式系统设计中的统一拆分方式——将每个 Skill 拆分为 `design`（设计开发）与 `settle`（资产落账）两个子 Skill，并以两道人类审阅门禁串联状态机、回退边界和人机协作规则。
>
> **定位**：本文件是 EOS 设计线中 `design / settle` 拆分方法的权威规范；各具体 Skill（如 `wft05a/05b`）是本规范的实例化实现。
>
> **版本**：v1.5 | **修订**：2026-06-18

---

## 一、总体流程

### 核心流程

```
┌──────────────────────────────────────────────────┐
│ Phase 1 · 设计开发（创造性/分析性）                │
│                                                    │
│  入口：上游就绪节点（状态=待设计开发 + 人类同意）    │
│  核心：逐条做映射、分解、定义、完善                │
│  产出：设计方案 → 写入目标资产文档                 │
│        （节点状态=待确认方案）                     │
│                                                    │
│  ── 人类审阅门禁 1 ──                              │
│  [同意]（仅可确认方案）→ 状态→待资产落账            │
│  [修改] → 状态→迭代中（重入 Phase 1）              │
│  [驳回] → 重做方案                                │
│                                                    │
├──────────────────────────────────────────────────┤
│ Phase 2 上半 · 资产维护（匹配/写回/记录）          │
│                                                    │
│  步骤A 资产环境加载          ← 加载共享资产当前状态 │
│  步骤B 实体提取              ← 从设计方案中提取引用 │
│  步骤C 跨域资产匹配          ← Q1/Q2/Q3 找宿主     │
│  步骤D 写回主资产            ← 写入目标资产文档     │
│  步骤E 反向引用更新          ← 写回上游资产引用列表 │
│  步骤F 状态推进              ← 状态→已资产落账     │
│                                                    │
│  ──（机械性操作，不需要人类审阅）──                  │
│                                                    │
├──────────────────────────────────────────────────┤
│ Phase 2 下半 · 详细定义展开                        │
│                                                    │
│  步骤G 详细条目展开         ← 架构末级→详细条目    │
│  产出：详细定义子文档草案（状态=待确认细节）         │
│                                                    │
│  ── 人类审阅门禁 2 ──                              │
│  [同意] → 状态→已验证冻结 → 可进入下游             │
│  [修改]/[驳回] → 重入 Phase 2 下半                 │
└──────────────────────────────────────────────────┘
```

### 核心原则

1. Skill A 每次仅处理一个上游架构末级节点。
2. Skill A 负责主结构方案形成；Skill B 负责已确认方案的正式落账与详细展开。
3. 门禁 1 仅放行 `可确认方案`；`阶段性方案` 不得进入 Skill B。
4. Skill B 仅处理规格问题，不得重做主结构设计；凡属结构问题，必须回退 Skill A。
5. 详细定义应遵循”消费方产出”规律，由消费该层架构的下游 Skill 在 Phase 2 下半写回。
6. **STR 架构末级节点（STR-F）指代的是场景定义**——每个 STR-F 节点对应一个角色的端到端职责闭环，与 wft02 中该角色的场景定义形成 1:1 映射。场景定义集合是 EOS 系统功能菜单的唯一设计来源。
7. **五类信息链是设计分析框架，不是场景定义的内容结构**：执行业务链、管理业务链、个人绩效链、组织绩效链、资源需求链（待后续扩展）是 AI 在 wft01a 中识别信息载体时的分析维度，引导全面扫描。场景定义的内容是一个扁平的信息载体清单，每个载体标注归属链。
8. **业务定义与场景定义**：**业务定义**（不变）指单次业务操作，有业务产出，归属输出产品路径的各构件，分为直接产出类和管理控制类。**场景定义**（新增）指业务定义的有序序列集，不产生产出，归属角色场景产品类型。场景定义的子节点是业务定义，不能是其他场景定义。

---

## 二、统一状态机

### 节点状态

| 统一状态 | 含义 | 管理阶段 |
|---------|------|---------|
| **待设计开发** | 上游就绪，等待进入 Phase 1 | Phase 1 入口 |
| **待确认方案** | Phase 1 已完成，等待人类审阅 | 人类审阅门禁 1（仅可确认方案可放行） |
| **迭代中** | 人类反馈修改意见，等待重新处理 | Phase 1 重入 |
| **待资产落账** | 方案已确认，等待进入 Phase 2 | Phase 2 入口 |
| **已资产落账** | Phase 2 上半完成，进入 Phase 2 下半 | Phase 2 下半入口 |
| **待确认细节** | Phase 2 下半已完成，等待人类审阅详细定义 | 人类审阅门禁 2 |
| **确认中** | 人类反馈修改意见，等待重新处理详细定义 | Phase 2 下半重入 |
| **已验证冻结** | 全链验证通过 | 终态 |
| **已废弃** | 节点停止使用 | 终态 |

### 人类操作类型

| 人类操作 | 作用 | 发生位置 |
|---------|------|-----------|
| 同意设计开发 | 将节点选入 Phase 1 | 上游状态表 |
| `[同意] / [修改] / [驳回]`（门禁 1） | 决定当前节点进入 Skill B，或留在 Skill A 重入 | 目标资产文档的反馈区 |
| `[同意] / [修改] / [驳回]`（门禁 2） | 决定当前节点冻结完成，或留在 Skill B 重入 | 详细定义/规格文档的反馈区 |
| 同意验证 | 将节点选入下游验证 | 下游状态表或入口文档 |

---

## 三、全链路映射

| 步骤 | Skill A 处理对象 | Skill B 上半 | Skill B 下半 | 详细定义写入 |
|------|------|------|------|------|
| `wft01` | OR 末级需求 → STR 节点（即场景定义），识别广义文档，标注归属链，形成扁平信息载体清单 | 引用匹配与写回 | 不适用 | 由 `wft02` 产出 |
| `wft02` | STR 架构末级（场景定义及信息载体清单）→ 展开为完整业务定义 + 场景定义编排 | 匹配、写回、派生回写 | STR 详细条目展开 | `03-*.md` |
| `wft03` | BA 末级 IPO → SysReq 节点 | 分类宿主匹配与写回 | 不适用 | 由 `wft05` 产出 |
| `wft04` | NFR 末级条目 → SysReq-NFR | 指标匹配与写回 | 不适用 | 内嵌于架构末级 |
| `wft05` | SysReq 末级 IPO → PA 组件 | 跨域匹配、落账、反向引用 | SR 详细定义 + PA 组件规格 | `06-*.md` / `07-*.md` |

规范说明：

1. `wft01 / wft03 / wft04` 无 Phase 2 下半。
2. `wft02 / wft05` 是消费方，因此负责在 Phase 2 下半写回详细定义。
3. 当前完整实例是 `wft05a/05b`，可作为 `wft01~04` 的直接参照。

推导依据：

1. `STR` 架构由 `wft01` 产出，但 `STR` 详细定义只有在 `wft02` 消费 `STR` 去设计 `BA` 时，才能确定应细化到什么粒度，因此由 `wft02` 写回。
2. `SR` 架构由 `wft03` 产出，但 `SR` 详细定义只有在 `wft05` 消费 `SR` 去设计 `PA` 时，才能确定应细化到什么粒度，因此由 `wft05` 写回。
3. `wft04` 对应的 `NFR` 详细内容不独立拆分为下游详细定义子文档，而是内嵌于架构末级节点，因此不设置 Phase 2 下半。

**STR 架构与业务定义体系的说明**：

1. **STR-F 末级节点 = 场景定义**。每个 STR-F 节点指代一个角色的端到端职责闭环，与 wft02 中该角色的场景定义形成 1:1 映射。STR 树的角色组织方式直接对应系统功能菜单的结构。

2. **wft01a 的职责范围**：
   - 从 OR 语义中识别场景定义需要哪些广义文档（业务文档、表单、指标报表、绩效数据、资源视图等）
   - 以五条信息链为分析框架，引导全面扫描，确保不遗漏
   - 匹配 `26-eos-document-definitions.md`（文档资产清单），确定文档的已有/扩展/待确认状态
   - 每个载体标注归属链（执行业务链/管理业务链/个人绩效链/组织绩效链），形成扁平的信息载体清单
   - wft01a 不展开业务定义的 I/P/O——那是 wft02 的工作

3. **wft02 的两条路径分工不同**：
   - **3A 输出产品路径（直觉驱动）**：接收 wft01a 的信息载体清单（含归属链标签），匹配 24 资产业务定义，展开完整 I/P/O，定位到业务域→产品类型→构件。归属链标签指引 3A 路由：执行业务链→对应构件、管理业务链→项目管理构件、绩效链→绩效管理构件
   - **3B 角色场景路径（规则驱动）**：基于 STR-F 节点的功能说明，将 3A 已产出的业务定义按自然流向编排为场景定义的四条链。场景定义是业务定义的有序序列集，子节点不能是其他场景定义

4. **五类信息链作为设计分析框架**：
   - **执行业务链**：引导 AI 扫描 OR 中与业务操作直接相关的文档/表单——这些最终会展开为直接产出类业务定义，归各业务构件
   - **管理业务链**：引导 AI 扫描 OR 中与指标/绩效/进展监控相关的报表——展开为管理控制类业务定义，归项目管理构件
   - **个人绩效链**：引导 AI 扫描与个人绩效评估相关的信息载体
   - **组织绩效链**：引导 AI 扫描与部门/组织绩效评估相关的信息载体
   - **资源需求链**：引导 AI 扫描与资源申请/查看相关的信息载体（暂不展开，待后续扩展）
   - 以上五链是 wft01a 的分析维度，不是场景定义的内容结构。场景定义的内容是扁平的，每个信息载体标注其归属链标签。

---

## 四、人机交互

### 各阶段的人类操作

| 阶段 | 人类操作 | 在哪个文档操作 | 操作内容 |
|------|---------|--------------|---------|
| **Phase 1 前** | 选材 | 上游状态表（如 05-*.md SysReq 状态表） | 设 人类决策=同意设计开发 |
| **Phase 1 后** | 审阅方案 | 目标资产文档（如 07-*.md §X.6 反馈区） | 写 [同意]/[修改]/[驳回] |
| **Phase 2 下半前** | — | AI 自动执行 Phase 2 上半（无须人类介入） | — |
| **Phase 2 下半后** | 审阅详细定义 | 详细定义文档（如 06-*.md / 03-*.md） | 写 [同意]/[修改]/[驳回] |
| **全链路完成后** | 选入下一站 | 目标资产文档 §1.1 状态表 | 按下游 Skill 的入口规则设置对应人类决策 |

### Phase 1 [修改]/[驳回] 后

若当前节点收到 `[修改]` 或 `[驳回]`，该节点应进入 Skill A 重入，不得改为重新选入其他节点。仅当当前节点完成门禁 1 放行后，后续节点方可进入 Skill A。

### 交互约定

- 人类反馈应按**组件/节点级别**标注；同一批次中不同节点可具有不同反馈。
- AI 应在 Step Start 中检测反馈区内容，并自动同步状态表；人类不直接编辑状态表。
- Phase 2 上半为机械性资产归并操作，不设置人类审阅门禁。

---

## 五、wft01~05 Design / Settle 拆分统一规范

本节规定 `wft01~05` 拆分为 `design / settle` 后的统一交互与状态处理规则。凡后续 `wft01a/b ~ wft05a/b` 的实现、修订与评审，均应优先遵守本节。

### 1. 适用范围

本规范适用于瀑布式系统设计各步骤在拆分为两个 Skill 后的统一交互与状态处理方式：

- `Skill A = design`
- `Skill B = settle`

其中：

- `Skill A` 负责方案形成、设计判断、人类门禁 1
- `Skill B` 负责资产落账、详细展开、人类门禁 2

### 2. 统一职责边界

#### 2.1 Skill A（design）

Skill A 负责当前步骤目标节点的主结构方案形成，不负责正式资产落账及开发级详细展开。

其核心职责是：

1. 识别目标节点
2. 加载上游承接链与相关约束资产
3. 形成方案
4. 判断方案状态
5. 提交人类进行门禁 1 审查

#### 2.2 Skill B（settle）

Skill B 负责将 Skill A 已确认的主结构方案正式落入资产体系，并在需要时展开为详细定义或需求规格。

其核心职责是：

1. 接收已确认方案
2. 做资产匹配、写回、反向引用更新
3. 在需要时展开详细定义和规格
4. 提交人类进行门禁 2 审查

#### 2.3 总边界

1. Skill A 负责决定“谁来承接、如何切分边界、是否复用”。
2. Skill B 负责将已确认的承接结构正式落账并展开细化。
3. Skill B 不得重做 Skill A 已确认的主结构设计。
4. Skill B 发现结构问题时，必须回退 Skill A。

### 3. Skill A 的统一输入处理

#### 3.1 输入处理步骤

每个 Skill A 应按以下顺序处理输入：

1. 接收一个目标节点
2. 检查该节点状态是否允许进入 Skill A
3. 检查是否存在其他节点占用当前门禁前工位
4. 加载目标节点自身内容
5. 加载上游追溯链
6. 加载与本步相关的约束资产
7. 加载已有目标资产
8. 若为重入，加载反馈区的人类意见
9. 执行输入预检并形成处理前结论

#### 3.2 输入包定义

Skill A 的标准输入包为：

1. 目标节点
2. 上游追溯链
3. 相关约束资产
4. 已有目标资产
5. 重入反馈

#### 3.3 输入预检要求

Skill A 在进入正式设计前，必须先判断：

1. 输入是否完整
2. 上游追溯链是否成立
3. 是否存在阻断项
4. 本轮是否只能形成阶段性方案
5. 本轮是否具备进入 Skill B 的前提

补充要求：

1. 输入预检发现缺口时，应先报告缺口，再进入正式设计。
2. 常见缺口至少包括：依附关系缺口、关键约束缺口、追溯链缺口、必要资产缺口。

### 4. 设计判断

> 本节描述 Skill A 在形成方案时的核心思维过程——它回答的是"输入处理之后、方案写入之前，设计判断如何运作"的问题。这个过程不是线性步骤，而是同时在多个维度上进行的判断循环。
>
> 本节与 §2（职责边界）配合：§2 规定 Skill A 负责"形成方案"和"判断方案状态"，本节展开这个职责内部的思维过程。与 §11（审查口径）配合：门禁 1 所审的"主结构是否成立"可引用本节 4.4 和 §5 的内容作为判断支撑。

#### 4.1 设计的两种工作方式

Skill A 进入设计活动后，存在两种本质上不同的工作方式：

| 方面 | 规则驱动（模式匹配） | 直觉驱动（分解分配分析） |
|------|---------------------|------------------------|
| 决定关系 | 输入 → 规则 → 输出 | 输入 + 资产 + 理解 → 方案 |
| 确定性 | 确定性：不同执行者结果趋于一致 | 非确定性：存在多种合理方案 |
| 可自动化程度 | 高 | 低 |
| 人类参与模式 | **审核确认**：人类审方案是否按规则执行完整、映射是否正确 | **深度参与**：人类判断边界是否合理、风格是否一致、方案是否收敛，需多轮迭代 |
| 迭代次数预期 | **单轮**：AI 产出 → 人类审核 → 确认/微调 → 放行 | **多轮**：AI 初案 → 人类反馈 → AI 调整 → 人类再判断 → 直到收敛 |
| 典型实例 | 业务定义类 IPO → 1+7 组件结构 | 引擎配置类 IPO → 前后端组件划分 |

说明：§3 的输入预检完成后，Skill A 进入上述两种方式之一。规则驱动部分按既有模式执行即可；直觉驱动部分遵循 4.2~4.4 的描述。Skill 编制者在设计 Step 3 时，应先按 4.1.1 的条件判断当前设计活动应归入哪种方式，再按相应方式编写。

##### 4.1.1 工作方式的选择条件

> **适用对象**：Skill 编制者。在编写某个 Skill 的 Step 3（设计活动）时，用以下条件判断"这个设计活动应该写成规则驱动的模式匹配，还是需要写成直觉驱动的分解分配分析框架"。

**条件一：输出空间是否已知**

| 规则倾向 | 直觉倾向 |
|---------|---------|
| 你能提前列出所有可能的输出类型/结构 | 你无法穷举输出可能长什么样 |

判断方法：问自己"如果做 10 次，输出结构是否大致相同？"——是则规则驱动，否则直觉驱动。

**条件二：从输入到输出是否存在固定映射路径**

| 规则倾向 | 直觉倾向 |
|---------|---------|
| 存在确定的翻译/转换规则：输入的 X 字段对应输出的 Y 结构 | 不存在逐字段映射，需要理解问题域后重新组织 |

判断方法：问自己"能否用一张映射表说明输入字段和输出结构的关系？"——能则规则驱动，不能则直觉驱动。

**条件三：方案质量是否可规则验证**

| 规则倾向 | 直觉倾向 |
|---------|---------|
| 可以用检查清单验证"做得对不对"（字段齐全、格式正确） | 质量依赖于人类判断"合不合理"（边界恰当、风格一致） |

判断方法：问自己"能否编写一份完整的通过/不通过检查清单来验证方案正确性？"——能则规则驱动，不能则直觉驱动。

**条件四：方案是否强依赖资产语境**

| 规则倾向 | 直觉倾向 |
|---------|---------|
| 方案内容主要由输入决定，资产仅作为引用参考 | 方案内容必须结合当前资产状态来决定——已有组件长什么样直接影响本次设计 |

判断方法：问自己"如果换一套资产，同样的输入是否会产出不同的方案？"——不会则规则驱动，会则直觉驱动。

**综合判断方法**：采取简单多数原则——四个条件中 ≥2 条指向直觉驱动，则该设计活动应按直觉驱动方式编制；≤1 条指向直觉驱动，可按规则驱动方式编制。最终决定因素是条件一——如果输出空间根本不可穷举，则必然为直觉驱动。

**选择结果对 Skill 结构的影响**：

| 差异项 | 规则驱动 | 直觉驱动 |
|--------|---------|---------|
| Step 3 的写法 | 写为确定的步骤 + 映射规则 | 写为判断框架 + 收敛方向指引 |
| 判断框架模板 | 不适用 | **四维框架：看/守/交/完**。各 Skill 填充各自的"看什么、守什么、交什么、完标准" |
| 人类交互设计 | 人类审"是否按规则执行完整" | 人类审"边界、风格、方案是否合理" |
| 迭代设计 | 不单独设计迭代路径，走统一门禁 1 重入即可 | 需主动设计迭代循环的预期次数和收敛条件 |
| 尾部提示重点 | "映射完整，可进入 B" | "方案已形成，人类请判断以下关键点……" |
| 编制风险 | 模式误用：把直觉题当规则题做，导致方案僵硬 | 过度发散：没有收敛锚点，迭代无法结束 |

#### 4.2 分解分配分析的判断结构

分解分配分析的核心特征是**并行判断循环**——分解方案和分配方案同时形成、互相影响，不存在固定的先后顺序：

```
上游末级节点
     │
     ├── 分解判断：按什么维度拆？拆成几个部分？
     │       ↕  同时进行，互相约束
     ├── 分配/匹配判断：已有宿主？可扩展宿主？需新增？
     │
     └── 资产感知判断：新增组件是否与现有资产风格一致？
```

三个判断维度的具体含义和牵制关系：

| 判断维度 | 核心问题 | 牵制关系 |
|---------|---------|---------|
| **分解判断** | 拆成哪些条目？划分逻辑是什么？ | 受限于宿主是否存在、可扩展性——无宿主的条目需要调整分解方案 |
| **分配/匹配判断** | 每条需求由哪个组件承接？复用 / 改进 / 新增？ | 受限于分解方案——不同分解方式产生不同的分配路径 |
| **资产感知判断** | 新增/扩展的组件是否与现有资产协调？ | 影响分解方式和分配方案——避免产生与现有资产脱节的组件 |

三者不是先 A 后 B 再 C 的关系。设计者实际上是在三者间快速跳跃：试探一个分解方案 → 检查是否有宿主 → 发现没有合适宿主或宿主风格不匹配 → 调整分解方案 → 再匹配 → 直到三者收敛为一个可接受的平衡点。

#### 4.3 收敛方向判断

分解分配分析的终点不是"唯一正确答案"——流程设计不存在唯一解，而是在**多个可行方案中选取一个合理的收敛点**。

典型情况下的判断倾向：

| 情况 | 合理倾向 |
|------|---------|
| 有明确宿主可承接 | 优先分配，不做多余分解 |
| 有可扩展的已有宿主 | 检查扩展边界是否合理，合理则扩展后承接；不合理则回到分解判断 |
| 需要新增组件 | 参考同层同类组件的风格，保持资产一致性 |
| 多组件可承接同一条目 | 检查边界是否重叠，调整分配方向 |
| 条目无任何宿主 | 回到分解判断，重新考虑条目划分逻辑 |

#### 4.4 方案状态的形成逻辑

§2 和 §6 规定了"方案状态"（阶段性方案 / 可确认方案）影响路由和门禁放行，但方案状态不是方案的自有属性——它是**设计者对当前判断信心的综合表达**。

**判定为"阶段性方案"的典型信号：**

1. 存在未闭合的承接关系（部分条目无宿主或扩展路径不明确）
2. 存在阻断型 NFR 约束未满足或未完成映射
3. 缺少关键约束资产，无法做出准确判断
4. 新增组件与现有资产风格差异较大，无法确认是否合理
5. 方案刚形成，尚未经过任何迭代检视

**判定为"可确认方案"的典型信号：**

1. 所有需求条目有明确宿主或可验证的扩展路径
2. NFR 约束已映射完成或已明确标记为"可接受风险"
3. 必要约束资产已加载并纳入设计考量
4. 组件风格与现有资产协调，无不合理处
5. 方案经过了至少一轮内部检视或迭代验证

**信号不一致时的处理规则**：当上述信号不一致时（例如承接关系闭合但 NFR 存疑），设计者应取保守判断——标记为阶段性方案，将缺口作为阻断项一并提交人类审阅。

### 5. 资产感知与风格一致性

> 本节与 §2.3（总边界第3项"Skill A 负责决定是否复用"）配合。是否复用的判断不仅取决于功能匹配度，还取决于新增组件是否能与现有资产自然融合。这种融合判断的能力称为资产感知。

#### 5.1 资产感知的定义

**资产感知**是指设计者对已有资产体系的理解程度，体现在新增或扩展节点时能自然融入现有结构，而不是以孤立或不协调的方式存在。

#### 5.2 风格一致性的检查维度

资产感知无法用规则穷举，但可以转化为以下检查维度作为判断框架：

| 维度 | 判断问题 |
|------|---------|
| **粒度** | 新增组件的粒度是否与同层同类组件一致？（不过粗也不过细） |
| **命名** | 组件名称的命名模式是否与同类组件一致？ |
| **边界模式** | 职责边界的划分逻辑是否与同域组件一致？ |
| **层级位置** | 新增组件是否放在正确的架构层级？ |
| **依赖关系** | 新增组件对上下游的依赖方式是否与同类一致？ |

#### 5.3 资产感知在设计判断中的贯穿作用

资产感知不是独立的设计步骤，而是贯穿 §4.2 中三个判断维度的底层能力：

- **在分解判断中**：了解资产中同类节点的分解深度和惯用方式，使新节点的分解方案与现有资产自然对齐
- **在分配/匹配判断中**：感知资产的组件风格偏好，新增节点时自然模仿同层同类组件的形式和规格
- **在门禁 1 审查中**：作为"主结构是否成立"的软性判断维度——结构成立不仅是逻辑正确，还包括资产体系的内在协调一致性

### 6. Skill A 的统一尾部提示

#### 4.1 尾部提示目标

Skill A 尾部必须明确告知人类：

1. 当前方案是什么状态
2. 为什么是这个状态
3. 下一步预定进入哪个 Skill
4. 人类现在该做什么

#### 4.2 固定输出项

每个 Skill A 在 Step End 必须输出：

1. 方案状态
2. 下一步路由
3. 路由原因
4. 关键方案摘要
5. 阻断项清单
6. 当前应执行动作
7. 重点查看位置

#### 4.3 路由规则

Skill A 尾部必须输出唯一预定路由：

1. `重新运行 wftXa`
2. `运行 wftXb`

其中：

1. 若方案仍存在阻断项，或当前仅为阶段性方案，则预定路由为 `重新运行 wftXa`。
2. 若方案已达到可确认状态，则预定路由为 `运行 wftXb`。

#### 4.4 放行限制

仅当满足以下条件时，Skill A 才允许进入 Skill B：

1. 当前方案状态为 `可确认方案`
2. 人类给出 `[同意]`

若当前方案状态为 `阶段性方案`，则 `[同意]` 不构成有效放行。

### 7. Skill B 的统一输入处理

#### 5.1 输入处理步骤

每个 Skill B 应按以下顺序处理输入：

1. 接收一个目标节点
2. 检查该节点状态是否允许进入 Skill B
3. 加载 Skill A 已确认的主结构方案
4. 将该方案视为冻结输入
5. 加载当前资产环境
6. 按上半 / 下半进行路由
7. 若为重入，加载反馈区的人类意见

#### 5.2 输入包定义

Skill B 的标准输入包为：

1. 已确认主结构方案
2. 当前资产环境
3. 重入反馈

#### 5.3 冻结原则

Skill B 输入中的主结构方案视为冻结内容，至少包括：

1. 组件清单
2. 组件类型
3. 组件边界
4. 宿主分配
5. 复用 / 改进 / 新增 决策
6. 主承接关系

### 8. Skill B 的统一尾部提示

#### 6.1 尾部提示目标

Skill B 尾部必须明确告知人类：

1. 本轮结果是什么
2. 当前节点该回 A、留在 B，还是已完成
3. 人类现在该做什么
4. 应重点看哪份文档的哪一部分

#### 6.2 三类标准结果

每个 Skill B 尾部都应归入以下三类之一：

1. `已回退 Skill A`
2. `待确认细节`
3. `已验证冻结`

#### 6.3 固定输出项

每个 Skill B 在 Step End 必须输出：

1. 当前处理结论
2. 当前节点路由
3. 路由原因
4. 当前应执行动作
5. 重点查看位置

#### 6.4 路由规则

1. 若发现结构问题，应回退 Skill A。
2. 若主结构成立但细节仍待确认，应重入 Skill B。
3. 若详细定义与规格均通过确认，则当前节点完成。

#### 6.5 节点完成后的可选动作

仅当节点已 `已验证冻结` 时，才允许在流程层面提示多个后续可选动作，例如：

1. 进入下一步
2. 回到 Skill A 选择下一个节点

### 9. 统一回退规则

#### 7.1 回退触发条件

Skill B 一旦发现以下问题触及主结构，必须回退 Skill A：

1. 组件是否存在需要改
2. 组件类型需要改
3. 组件边界需要重切
4. 宿主分配需要重做
5. 复用 / 改进 / 新增 决策需要推翻
6. 主依附关系不成立
7. 阻断型约束导致主结构失效

#### 7.2 回退动作

回退时必须执行：

1. 停止当前 Skill B
2. 不继续进入下半或后续门禁
3. 状态回退到 Skill A 重入状态
4. 写入回退记录
5. 输出唯一回退路由

补充要求：

1. 若结构问题在 Skill B 已正式写入主资产后才被发现，必须先撤销本轮正式落账结果，再执行回退。
2. 撤销范围至少包括：主资产正式写入、状态成功推进、反向引用正式更新。

#### 7.3 回退记录位置

回退原因的权威记录位置统一为：

目标主资产文档中对应节点的反馈区 / 审查区。

状态变化写入状态表，详细原因写入反馈区。

### 10. 统一状态原则

#### 8.1 Skill A 状态原则

Skill A 至少应覆盖：

1. 首次进入状态
2. 重入状态
3. 待门禁 1 审查状态
4. 已放行进入 Skill B 状态

#### 8.2 Skill B 状态原则

Skill B 至少应覆盖：

1. 上半入口状态
2. 上半完成状态
3. 待门禁 2 审查状态
4. 下半重入状态
5. 已验证冻结状态
6. 回退 Skill A 状态

#### 8.3 状态与路由一致性

任何新增交互规则都必须满足：

1. 状态表中有对应状态
2. 路由逻辑中有对应入口
3. 人类操作后有明确状态转迁
4. 不允许出现“文案里有动作，但状态机里无承接状态”的情况

### 11. 统一审查口径

#### 9.1 门禁 1

门禁 1 审查的是：

1. 主结构方案是否成立
2. 是否允许进入资产落账与详细展开
3. 不审开发级细节

#### 9.2 门禁 2

门禁 2 审查的是：

1. 详细定义和规格是否达到可开发、可验证、可冻结粒度
2. 不重审主结构

### 12. 统一复用建议

在 `wft01~04` 拆分时，仅替换以下内容：

1. 本步主处理节点对象
2. 本步依赖的约束资产
3. 本步是否存在下半详细展开
4. 本步的具体写入资产位置

其余输入处理、尾部提示、状态原则、回退机制与门禁口径，均应优先复用本统一规范。


---

## 六、附录A：wft05 实例

`wft05a/05b` 是当前最完整的实例，其关键特征如下：

1. `wft05a` 处理 `SysReq 末级 IPO → PA 组件方案`，并区分 `业务定义类 IPO` 与 `引擎配置类 IPO`。
2. `wft05b` 以上半完成主资产落账、以下半完成 `SR` 详细定义与 `PA` 组件规格。
3. `wft05` 体现了本规范的完整边界：阶段性方案/可确认方案、冻结输入、结构问题回退、规格问题留在 B。

若需具体实例细节，应以 [eos-wft05a-sr2pa-design.md](/Users/iproyongjunzhou/claude/AOS-system-design/20-pl4eos/10-pl4eos-subpl-sysdev/10-wfsysdev-4-eos/eos-wft05a-sr2pa-design.md) 与 [eos-wft05b-sr2pa-settle.md](/Users/iproyongjunzhou/claude/AOS-system-design/20-pl4eos/10-pl4eos-subpl-sysdev/10-wfsysdev-4-eos/eos-wft05b-sr2pa-settle.md) 为准。

---

## 七、附录B：适用说明

1. 当前完整实例为 `wft05a/05b`。
2. `wft01~04` 后续拆分时，应优先复用本规范正文，仅替换主处理节点对象、约束资产、详细展开方式与具体写入位置。
3. 若后续实例化落地发现更稳定的通用规则，应优先修订本规范，再修订各具体 Skill。

## 八、附录C：变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-06-18 | v1.5 | 信息链降级：核心原则 §7 从"每个场景定义包含五类信息链"改为"五类信息链是设计分析框架，不是场景定义的内容结构"；wft01a 职责范围更新为"信息载体+归属链标签"而非"四链节点骨架"；wft02 3A/3B 描述同步更新；§4 场景定义四链结构说明改为"五类信息链作为设计分析框架"；全链路映射表 wft01/wft02 描述同步更新 |
| 2026-06-18 | v1.4 | STR 架构重新定位：STR-F 末级节点 = 场景定义，场景定义是业务定义的有序序列集（非其他场景定义），与 wft02 场景定义 1:1 映射；场景定义包含五类信息链（执行业务/管理业务/个人绩效/组织绩效/资源需求），资源需求链暂不展开；废弃"原子业务定义"和"编排定义"术语。全链路映射表同步更新，核心原则新增 §7~§9 |
| 2026-06-18 | v1.4 | wft01a 范围明确：识别广义文档（业务文档/表单/指标/绩效/资源），匹配 26 文档资产，构建四链节点骨架（不展开业务定义 I/P/O）；wft02 接手展开完整业务定义并匹配 24 资产。全链路映射表及说明同步更新 |
| 2026-06-18 | v1.4 | 结构重组：原 §4 设计判断（含选择条件、并行判断结构、方案状态形成逻辑）和 §5 资产感知移至 §3（输入处理）之后，顺延后级编号；§五 逻辑流变为：输入处理 → 设计判断 → 资产感知 → 尾部提示A/B → 统一机制 |
| 2026-06-18 | v1.4 | 新增 §5.11 设计判断（分解分配分析的并行判断结构、方案状态形成逻辑）和 §5.12 资产感知与风格一致性（检查维度、贯穿作用）；§5.11.1 对比表补充人类参与模式与迭代预期两行；新增 §5.11.1.1 工作方式选择条件（四条件 + 综合判断 + 对Skill结构的影响表，含看/守/交/完四维模板）——填补 Skill A 方案形成过程的设计直觉缺口 |
| 2026-06-17 | v1.3 | 精简 `91` 正文：保留总体流程、统一状态机、统一规范正文、全链路映射与简化实例附录；移除重复的长版 `wft05` 详述、消费方产出展开说明和大文档说明，压缩变更历史 |
| 2026-06-17 | v1.2 | 将 `91` 中的统一规范章节收口为“wft01~05 Design / Settle 拆分统一规范”正式结构：按适用范围、职责边界、A/B 输入处理、A/B 尾部提示、统一回退规则、统一状态原则、统一审查口径、统一复用建议重组，并保留前期已验证的阶段性方案、冻结输入、撤销落账等规则；同时将后半段试点表述收口为“实例与推广”口径 |
| 2026-06-17 | v1.1 | 在 `91` 中补入“wft01~05 Design / Settle 拆分统一规范草案”的核心内容：统一输入处理、方案成熟度、唯一路由、Step End 提示、结构/规格边界、回退记录与撤销规则；将 `wft05a/05b` 已验证规则上提为全链路复用规范 |
