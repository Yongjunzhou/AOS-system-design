# 业务需求 → STR-F · 场景定义设计与资产化
**Skill ID**: wft01-biz-or2str

> **概述**：每次执行处理一个角色的 FR-BIZ 类型 OR 条目（≤10 条），STR 反馈优先于新 OR 处理。完成 OR→STR-F 场景定义设计、23/26/22 资产对准与写回，形成可进入 wft02a 的 STR-F 节点。三步递进：确定场景定义 → 确定所需信息载体 → 确定每个载体所属产品类型/构件类型并写回 23（产品架构）+ 26（文档定义）+ 22（场景指标）。
>
> **输入 → 输出**：一个角色的 FR-BIZ OR 条目（≤10 条）/ 有反馈的 STR-F 节点 → STR-F 节点方案 + 23/26/22 资产对准与写回（状态=`待分解分配`）+ OR 条目推进为 `已分配`
>
> **适用场景**：瀑布式系统设计第1步 · 业务需求路径。
>
> **设计模式参考**：[91-doc-paired-skill-spec.md](91-doc-paired-skill-spec.md)
>
> **版本**：v1.0 | **修订**：2026-06-23

---

## 一、职责定位

本 Skill 负责：

1. 接收人类选中的一批同角色 FR-BIZ OR 条目（≤10 条）
2. 将 OR 分配到已有 STR-F 节点（Q1/Q2）或聚合为新的端到端场景（Q3）
3. 对已有 STR-F 节点的修订，执行多 OR 收敛检测——四关系判定 + 来源升级 + 矛盾记录
4. 形成 STR-F 场景定义方案（含信息载体清单 + 场景指标 + 推理依据）
5. 对信息载体执行 23-Q1/Q2/Q3 资产对准（十类节点归属规则）
6. 全 Q1 时自动写回 23/26/22 资产；Q2/Q3 时形成资产建议等人类确认后写回
7. 写回完成后推进 STR-F 节点到 `待分解分配`，对应 OR 到 `已分配`

本 Skill 不负责：

1. 引擎需求（FR-ENG → `wft01-eng-or2str`）
2. 非功能需求（NFR → `wft01-nfr-or2str`）
3. 下游 wft02a 的详细分解展开
4. 业务人员确认——这是人类线下活动

---

## 二、输入与路由

### 标准输入包

1. 本轮处理对象（同角色 FR-BIZ OR 条目 ≤10 条 / 有反馈的 STR-F 节点）
2. 上游追溯链（`01-*.md` 树画像 / 状态队列 / 节点索引）
3. 已有 STR-F 架构资产（`02-*.md` 树画像 / 状态队列 / 节点索引）
4. 相关约束资产（`23` 产品架构 + `26` 文档定义 + `22` NFR 分类体系）
5. 重入反馈

### Step Start 路由

#### 第一层：入口容错

| 条件 | 路由 |
|------|------|
| 人类选中的 OR 分属多个角色 | 输出角色分布，要求只选一个角色 → **退出** |
| 同角色 OR >10 条 | 输出超限提示，要求分批 → **退出** |
| OR 类型含 FR-ENG 或 NFR | 输出类型分布，要求分路径 → **退出** |
| 无 OR 选中 + 无待处理反馈的 STR-F 节点 | 输出"无待处理对象"→ **退出** |

#### 第二层：反馈优先

| 条件 | 路由 |
|------|------|
| 存在 `待确认方案` + 反馈区有 `[同意]/[修改]/[驳回]` 的 STR-F 节点 | 本轮先处理全部有反馈的 STR-F 节点 |
| 存在 `待确认方案` + 反馈区为空的 STR-F 节点 | 不阻塞新 OR；Run End 时提示 |

#### 第三层：新 OR 路由

STR-F 反馈处理完后，逐条处理新 OR → Phase A 宿主匹配。

---

## 三、文档加载规则

读取遵守 [文档编写约定 §十四](../../../../00-generalspec/00-doc-conventions.md)：读全景层（建立感知+确定可处理对象）→ 读索引层（定位目标块）→ 读目标正文块+追溯引用块 → 扩大读取仅在索引缺失/关系断裂/一致性检查时触发。

写回遵守 [文档编写约定 §十五](../../../../00-generalspec/00-doc-conventions.md)：幂等去重、局部写回、全貌同步（树画像 / 分片索引 / 状态队列 / 追溯引用）。

### 全景加载

每次运行先加载基础全景层：

1. `01-*.md` 树画像 / 状态队列 / 节点索引，定位本轮 OR 条目正文块
2. `02-*.md` STR 树画像 / 状态队列 / 节点索引，定位本轮 STR-F 节点块
3. `23-eos-output-architecture.md` 资产全貌层
4. `26-eos-document-definitions.md` 资产全貌层
5. `22-eos-nfr-taxonomy.md` 资产全貌层（场景指标链）

### 按需加载线索

- **对象线索**：按 OR 条目 ID 或 STR-F 节点 ID 在索引中定位必须读取的正文块
- **追溯线索**：按追溯关系读取上下游相关块，不复制对方正文
- **结构线索**：按同角色/同业务域/同层级筛选候选 STR-F 块，用于 Q1/Q2/Q3 宿主匹配
- **语义线索**：按 OR 中的角色/动作/业务对象/流程阶段/约束条件/异常条件/指标口径匹配资产候选分片
- **信息链线索**：按十链扫描法识别潜在信息载体和场景指标，定位 `26` 文档定义分片
- **反馈线索**：重入路径只读取反馈区及反馈指向的相关块，不重新做全量候选读取
- **扩大读取触发**：索引缺失/候选冲突/资产全貌不足以判断边界/追溯关系断裂时，说明原因后扩大

---

## 四、操作流程

### Step 布局总览

| Step | 名称 | 产出 | 写资产 |
|------|------|------|--------|
| Start | 前置校验与路由 | 就绪 / 结束 | — |
| 1 | 设计上下文加载 | OR / STR / 资产全景 | — |
| 2 | 方案反馈处理 | 调整后的 STR-F 方案 | `01/02` |
| 3 | 新增场景定义设计 | STR-F 节点方案（含 23/26/22 对准） | `01/02` |
| 4 | 资产写回 | 23/26/22 引用写回 | `23/26/22/02` |
| 5 | 资产建议反馈 | Q2/Q3 确认后的写回 | `23/26/22/02` |
| 6 | 状态推进 | OR→已分配，STR→待分解分配 | `01/02` |
| End | 运行结束输出 | 人类下一步操作提示 | — |

### STR-F 节点生命周期

| 状态 | 含义 |
|------|------|
| `待确认方案` | 方案已生成或修订，等待人类反馈 |
| `可以资产化` | 人类已整体确认同意（全 Q1），自动进入写回 |
| `待分解分配` | 资产写回完成，等待进入 wft02a |

---

### Step 1 · 设计上下文加载

按 §三 加载全景层和本轮正文块。

---

### Step 2 · 方案反馈处理

若本轮存在有反馈的 STR-F 节点（`待确认方案` + 反馈区有效），逐个处理：

| 操作 | 行为 |
|------|------|
| `[同意]` 或整体确认 | 归档反馈 → 生成空反馈区 → 全 Q1 则自动进入 Step 4 写回，有 Q2/Q3 则进入 Step 5 |
| `[修改]` | 按反馈调整方案 → 重新对准 → 更新增量清单 → 回写 `待确认方案` |
| `[驳回]` | 重做 OR→STR-F 设计 → 更新方案和对准 → 回写 `待确认方案` |

---

### Step 3 · 新增场景定义设计

分三个阶段。

#### Phase A：OR → 目标 STR-F 分组

1. 沿用 `#role-list` 中已确认角色归属
2. 对每条 OR 独立执行 **Q1/Q2/Q3 宿主匹配**：
   - Q1（直接匹配）：OR 的功能描述与已有场景定义语义一致
   - Q2（扩展匹配）：OR 是已有场景的子集或超集边缘
   - Q3（新增）：无可用宿主
3. 分组结果：Q1/Q2 → 归入已有 STR-F；Q3 → 进入待聚合池
4. 待聚合池按**业务域 + 触发相似性**分组

#### Phase B：修订已有 STR-F（四关系检测）

**边界匹配检查**：先检查 OR 是否在 STR-F 节点边界内（边界内/超出/边缘）。

**逐载体四关系检测**：比对锚点"角色+业务对象+职责"三元组。

| 关系 | 判定 | 处理 |
|------|------|------|
| 确认 | 新 OR 与既有载体一致 | 来源升级（`OR隐含`→`OR明示`），增量标记 `[确认]` |
| 补充 | 新 OR 提到未识别载体 | 新增到清单，来源=`OR明示`，增量标记 `[新增]` |
| 矛盾 | 载体形态/范围与既有不同 | 追加矛盾记录，增量标记 `[矛盾]`，等人类裁决 |
| 无新增 | 完全被覆盖 | 仅追加 OR 引用 |

来源类型升级规则（只升不降）：`OR隐含`→`OR明示`、`同类场景推断`→`OR明示`、`资产线索推断`→`OR明示`。

矛盾记录追加式不覆盖，存在未解决矛盾时不得推进到写回。

#### Phase C：新建 STR-F

**1. 确定业务域**：从角色类型映射 23 资产业务域。

**2. 三步判断法定义端到端业务闭环**：

2a. **识别触发**：提取"角色 + 动作 + 业务对象"。组内触发不匹配时移除或标注为内部补充。

2b. **追踪自然终点**：端到端约束——OR 语义覆盖的终点即追踪终点，不得在终点之后扩展。跨角色/跨域截断并标注交接点。

2c. **边界检查**：完整性（触发到终点完成一件完整的事）/ 一致性（同角色同业务域）/ 紧凑性（不混多个不连续职责段→拆分）/ 粒度匹配（与同层已有一致）。

输出**场景职责边界陈述**：`「<角色>」在「<业务域>」中「从触发到终点的职责概括」`。

**3. 推理依据**：业务常识推理 / 系统设计推理 / 企业治理推理，编号引用。

**4. 派生场景定义名称**：与同层已有节点粒度和命名风格一致。

**5. 完善 STR-F 要素与资产对准**：

信息载体标注字段：
- 载体名称 / 归属链 / 文档性质 / 产生方式 / 一句话职责 / 来源 OR / 来源类型 / 确认状态
- **26 资产方案级判断**：`26-Q1` / `26-Q2` / `26-Q3` / `未检查`
- **23 产品类型** / **23 构件类型** / **23 判定**（23-Q1/Q2/Q3）/ **23 资产建议**（Q2/Q3时）
- **关系对象**（按信息载体类型记录）

**十类信息载体归属对准规则**（AI 判定 23-Q 的依据）：

| 节点类型 | 归属锚点 | 归属规则 |
|------|---------|---------|
| 执行业务链节点 | 被执行/创建/处理/流转的产品数据 | 归属到该产品数据所在产品类型和构件类型；归属锚点是业务对象 |
| 管理业务链节点 | 被管理的业务对象/过程/控制对象 | 归属到主产品类型下的项目管理构件，归入管理业务 WBS |
| 个人绩效链节点 | 个人对业务/项目的贡献/达成/质量/效率 | 归属到项目管理构件，作为个人绩效 WBS |
| 组织绩效链节点 | 组织/团队/部门/项目的贡献/达成/质量/效率 | 归属到项目管理构件，作为组织绩效 WBS |
| 规则知识链节点 | 所附属的执行业务链节点 | 继承所附属执行节点的产品类型和构件类型 |
| 资源需求链节点 | 支持维资源提供方 | 按资源类型归属到资源提供类产品类型和构件类型 |
| 风险合规链节点 | 所附属的执行业务链节点 | 继承所附属执行节点，内容为事先/事中/事后规则 |
| 状态事件链节点 | 所记录的执行业务链节点 | 继承所附属执行节点；跨多业务对象的状态汇总转入管理业务链 |
| 协同交互链节点 | 公共协同或目标承接方 | 优先归属到公共协同类产品类型及构件类型 |
| 场景指标链节点 | 被度量对象 | 按被度量对象确定产品类型和构件类型 |

**信息载体关系记录要求**：

| 节点类型 | 必须记录的关系 |
|------|---------------|
| 执行业务链节点 | 被执行产品数据 / 业务对象 |
| 管理业务链节点 | 被管理对象、项目管理构件、管理业务 WBS |
| 个人绩效链节点 | 评价的个人/角色、项目管理构件、个人绩效 WBS |
| 组织绩效链节点 | 评价的组织对象、项目管理构件、组织绩效 WBS |
| 规则知识链节点 | 所附属执行业务链节点、附属文档类型 |
| 资源需求链节点 | 需求发起执行节点、资源类型、资源提供产品类型/构件类型 |
| 风险合规链节点 | 所附属执行业务链节点、事先/事中/事后规则类型 |
| 状态事件链节点 | 所记录执行业务链节点、状态/事件/轨迹/日志类型 |
| 协同交互链节点 | 来源执行节点、公共协同或目标承接方、请求/通知/待办/回执/接口消息类型 |
| 场景指标链节点 | 被度量对象、指标口径、数据来源 |

场景指标标注字段：
- 指标名称 / 指标类别 / 指标含义 / 主要数据来源 / 来源 OR / 来源类型 / 确认状态
- **22 一级类目** / **22 分类维度** / **22 判定**（22-Q1/Q2/Q3）/ **22 资产建议**（Q2/Q3时）

STR-F 完整要素：
- 定义 / 业务域 / 产品类型 / 用户角色 / 视角类型=`场景` / 功能说明
- 信息载体清单（含 26/23 对准） / 场景指标（含 22 对准）
- 推理依据与补全说明 / 十链覆盖自查 / 承接的 OR 末级
- 符合性分析（`待 wft02 生成`）/ 设计指导（`待 wft02 生成`）

**6. 十链覆盖自查**：逐链确认每条链是否至少有一个信息载体或标注合理空置原因。自查结果写入 STR-F 节点。

---

### Step 4 · 资产写回

全 Q1 时自动写回，无需人类介入。

#### 写回 23（产品架构资产）

- Q1：定位产品类型/构件类型正文块 → 追加文档引用（去重）→ 记录追溯引用
- Q2/Q3 → 进入 Step 5

#### 写回 26（文档定义资产）

- Q1：定位文档定义块 → 更新业务域/产品类型/构件类型/来源/追溯引用
- Q2/Q3 → 进入 Step 5

#### 写回 22（NFR 分类体系——场景指标）

- Q1：定位分类维度块 → 追加指标引用（含名称/类别/含义/数据来源）→ 记录追溯引用
- Q2/Q3 → 进入 Step 5

写回要求：幂等去重、局部写回、全貌同步（树画像/资产全貌/分片索引/状态队列/追溯引用）。

---

### Step 5 · 资产建议反馈

存在 Q2 或 Q3 时，在 `### 资产建议反馈区` 形成正式建议：

```text
[操作] 同意 / 修改 / 驳回

[23 资产建议] / [26 资产建议] / [22 资产建议]
```

| 操作 | 行为 |
|------|------|
| `[同意]` | 执行写回 → 进入 Step 6 |
| `[修改]` | 调整建议 → 重新等待 |
| `[驳回]` | 归属另有目标 → 重新定位；原子单元有问题 → 回退 Phase B/C 重做 |

---

### Step 6 · 状态推进

全部写回完成后：

- `02` 记录 `### 资产写回记录`（日期/路径/原子单元/目标资产块/动作/判定）
- `02` STR-F 节点推进为 `待分解分配`
- `01` 对应 OR 条目推进为 `已分配`
- OR→STR-F→23/26/22 引用链记录
- 清空反馈区待办信号

---

## 五、增量标记输出

| 标记 | 含义 | 使用场景 |
|------|------|---------|
| `[新增]` | 本次新增的内容 | 新 OR、新增信息载体、新增指标 |
| `[改进]` | 对既有内容的修改 | 功能说明补充、口径调整、边界澄清 |
| `[确认]` | 脑补载体被新 OR 证实 | 来源类型升级（`OR隐含`→`OR明示`等） |
| `[矛盾]` | 新 OR 与既有载体存在分歧 | 需人类裁决 |
| `[推断]` | 匹配基于待确认边界 | Phase A Q1/Q2 宿主边界未确认 |
| `[边界扩展]` | OR 处于宿主边界边缘 | Phase B 边界匹配检查 |
| `[跨场景]` | 拆分出的剩余候选场景线索 | 紧凑性检查拆分 |
| `[23-Q1/23-Q2/23-Q3]` | 信息载体对 23 资产的对准结果 | Phase C 对准 |
| `[26-Q1/26-Q2/26-Q3]` | 信息载体对 26 资产的对准结果 | Phase C 对准 |
| `[22-Q1/22-Q2/22-Q3]` | 场景指标对 22 资产的对准结果 | Phase C 对准 |

增量清单格式（新建 STR-F）：

```text
=== STR-F-XXXX 本轮更新清单 ===

[新增] 承接 OR-XXXX, OR-YYYY（共2条）
[新增] IC-XXX "载体名称" [23-Q1] → @comp-采购管理:采购申请 [26-Q2]
[新增] KP-XXX "指标名称" [22-Q1]

=== 以上为本次全部新内容 ===

请逐项确认，或回复"整体确认同意"。
对有 [23-Q3] 的项，请确认资产新增建议。
```

---

## 六、Run End 输出

### 单 ID 最小待办原则

1. 每次 Run End 只提示人类处理一个 ID
2. 当前 ID 未完成时，下次继续提示同一 ID
3. 只有当前 ID 完成/切换后，才选择下一个

### Run End 提示

```text
当前状态：完成 [角色名] 的 FR-BIZ OR 处理
  修订 STR-F-XXX（+N 条 OR）/ 新建 STR-F-YYY（聚合 K 条 OR）

你可以：
  A. 增加新 OR → 选择 FR-BIZ OR 条目重新运行
  B. 确认方案 → 回复「整体确认同意」/「修改：…」/「驳回：…」
     - STR-F-XXX（待确认方案）
  C. 确认资产建议 → 回复「同意」/「修改」/「驳回」
     - 资产建议 ID：AS-XXX
  D. 进入 wft02a → 可用的待分解分配 STR-F 节点：
     - STR-F-WWW
```

---

## 七、回退规则

| 条件 | 处理 |
|------|------|
| STR-F 节点是否存在需要重判 | 回退 Phase A |
| 节点边界/OR→STR 宿主分配需要重做 | 回退 Phase A/B |
| 信息载体需要新增/删除/改名/改边界 | 回退 Phase B/C |
| 资产块在资产文件中不存在（Q1 失效） | 降级为 Q3，形成新增建议 |

---

## 八、与原 wft01 的关系

1. 本文件从 `eos-wft01a-or2str-design.md` + `eos-wft01b-or2str-settle.md` 中提取 FR-BIZ 路径自闭环
2. 原文件保留作为历史参考，后续迁移至 `90-hold/`
3. FR-ENG 路径 → `eos-wft01-eng-or2str.md`，NFR 路径 → `eos-wft01-nfr-or2str.md`

---

## 九、变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-06-23 | v1.0 | 初版：从 wft01a Step 3 + wft01b STR-F/23/26/22 写回路径合并自闭环；FR-BIZ OR→STR-F 场景定义设计→23/26/22 资产对准→写回 全流程覆盖 |
