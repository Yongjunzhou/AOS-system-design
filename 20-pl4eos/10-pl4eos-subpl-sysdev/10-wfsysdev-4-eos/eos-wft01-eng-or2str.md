# 引擎需求 → STR-E · 引擎能力定义与资产对齐
**Skill ID**: wft01-eng-or2str

> **概述**：每次执行处理已进入 `01-*.md` 的正式 FR-ENG（功能需求-引擎类）类型 OR（原始需求）条目（≤10 条），STR-E 反馈优先于新 OR 处理。完成 OR→STR-E（相关方需求-引擎）引擎需求结构化声明：从"业务层级/业务对象需要什么运行能力"追溯到目标 EOS 引擎 → 识别 OR 触及的 CU（配置单元）类型（名称级别，不做完整 IPO（输入-处理-输出）设计）→ 判定引擎能力边界（顺序1/2/3），形成可进入 wft02-eng 的 STR-E 节点。引擎匹配即确定 23（产品架构资产）产品类型/构件类型归属（引擎=构件类型）。来自 wft02-biz 的引擎能力缺口线索若尚未被人类确认并转化为正式 FR-ENG OR，本 Skill 不直接接收。
>
> **输入 → 输出**：已进入 `01-*.md` 的正式 FR-ENG OR 条目（≤10 条）/ 有反馈的 STR-E 节点 → STR-E 节点方案（含 CU 类型清单 + 引擎能力边界陈述 + 23 对齐）+ 23 资产对齐与写回（状态=`可以分解分配`）+ OR 条目推进为 `已分配`
>
> **适用场景**：瀑布式系统设计第1步 · 引擎需求路径。
>
> **设计模式参考**：[91-doc-paired-skill-spec.md](91-doc-paired-skill-spec.md)
>
> **版本**：v2.14 | **修订**：2026-07-01

---

## 一、职责定位

### 1.1 本 Skill 目的与目标

> **主要目的**：取信于相关方——「你听懂了我的需求」。把非结构化的 FR-ENG OR 转化为结构化的 STR-E 声明——识别该需求源自哪类业务生成/支撑需要、触及哪些 CU 类型、归属哪个 EOS 引擎、引擎能力边界在哪里。
>
> **目标**：名称级识别，不脑补。AI 推断标注 `[推断]`，架构决策（更换引擎/拆分 STR）提交人类。STR-E 的 CU 类型清单、业务支撑来源和边界陈述是 wft02-eng 设计展开的输入——wft01-eng 不做 CU IPO 设计。

> **引擎与业务关系**：引擎是业务生成器，eng 线所有设计对象都必须保留 `业务层级/业务对象 → 业务运行能力 → 支撑引擎 → CU` 的追溯。对表单、流程、规则等主生成能力，应追溯到明确业务层级/业务对象/业务运行能力；对版本、发布、回滚、审计、运行监控、错误恢复等公共引擎基础能力，可追溯到“横切支撑的业务运行能力类别 / 引擎生命周期阶段 / 受影响业务对象范围”。若完全无法说明某项引擎能力支撑或生成哪类业务，该能力不得作为正式 STR-E 推进，只能标注为需澄清或候选线索。

本 Skill 负责：

1. **接收**——接收人类选中的、已写入 `01-*.md` 的正式 FR-ENG OR 条目（≤10 条），校验类型/数量/内容适用性（含单引擎门禁），不合格则说明原因并退出；不直接接收 wft02-biz 产出的引擎能力缺口线索
2. **溯源**——从 OR 语义还原业务支撑来源：哪个业务层级/业务对象/业务运行能力触发了本引擎能力需求；若为公共引擎基础能力，则还原其横切支撑范围和引擎生命周期阶段。引擎需求不明确时标注"需澄清"，并将该 OR 推进为 `需澄清`
3. **匹配**——基于 OR 引擎能力需求语义匹配 25（平台引擎资产）资产中的 EOS 引擎（引擎=构件类型→确定 23 归属），再以引擎为键匹配已有 STR-E 节点——同引擎归入同一 STR-E（Q1=直接引用/Q2=扩展归入），无已有则新建（Q3=新建）
4. **识别**——基于 25 资产的 CU（配置单元）类型清单，逐项判定 OR 触及了哪些 CU（明确触及/隐含触及/未触及），仅标注名称、配置页面/子页面线索和能力需求的一句话说明，完整 CU IPO 设计由 wft02-eng 负责
5. **对齐**——EOS 引擎 → 23 产品类型/构件类型对齐（23-Q1/Q2/Q3）
6. **定界**——逐触及 CU 分析 25 资产匹配情况，判定引擎能力边界——顺序 1=25 已有匹配可直接设计、顺序 2=25 有近似需扩展推断设计、顺序 3=25 无匹配需形成模型扩展候选
7. **写回**——人类确认方案后统一写回 23 资产；将 CU 类型清单引用写入 25（名称级别，不含 CU IPO 细节），顺序 3 只登记为 25 资产新增建议 / 候选 CU 类型，不视为正式模型能力
8. **推进**——人类确认时 STR-E 节点推进为 `可以分解分配`（wft02-eng 可即时进入）；资产落账后对应 OR 推进为 `已分配`
9. **承接下游退回**——接收 wft02-eng 退回的 `需wft01修订` STR-E 节点，读取缺失说明，按人类改进方案修订 STR-E（走 Phase B 修订流程），人类确认后重新推进为 `可以分解分配`

本 Skill 不负责：

1. 业务需求（FR-BIZ，功能需求-业务类 → `wft01-biz-or2str`）
2. 非功能需求（NFR，非功能需求 → `wft01-nfr-or2str`）
3. 配置单元的 CU IPO 设计和 25-Q 逐单元对齐——由 `wft02-eng-str2ba` 负责
4. 25 资产中 EOS 引擎的正式定义——那是 25 资产自身的设计产出

### 1.2 与 25 号资产的关系

25 号资产是 eng 线的 EOS 引擎 / CU 类型事实源，不是业务实例库。本 Skill 读取 25 的引擎层次、配置单元类型清单、依赖关系、状态处理队列和索引，用于判断 FR-ENG OR 应归属哪个引擎、触及哪些 CU 类型、是否存在模型缺口。

| 25 状态 | wft01-eng 处理 |
|---------|----------------|
| `已确认` | 可作为顺序 1 候选，进入 STR-E 正式方案 |
| `待确认` | 可作为候选类型读取，但 STR-E 必须标注 `待模型确认` / `[推断]`，不得作为最终 25 能力或开发强依赖 |
| `待填充` / 不存在 | 只能形成顺序 3 模型扩展候选，写入新增建议或待补齐队列，不固化为正式 CU 类型 |

因此，wft01-eng 只写入名称级引擎 / CU 类型引用和新增建议；不把具体业务场景、业务对象的 CU 实例写入 25，也不在本环节做 CU IPO。

### 方法论成熟度说明

引擎定位和配置单元类型识别当前依赖对业界通用做法和引擎能力常识的推断，尚未总结出稳定的自动化判定规则。本节标注方法论成熟度分级，供 AI 执行和人类审阅参考：

| 层次 | 说明 | 动作 |
|------|------|------|
| **确定性规则** | 可严格按规则执行，不需推断 | OR 类型校验、EOS 引擎匹配（25 资产索引）、CU 类型从 25 清单选取 |
| **启发式推断** | AI 基于引擎能力常识推断，标注 `[推断]`，人类审阅兜底 | 触发识别（从 OR 语义还原业务驱动源）、OR 触及哪些 CU 类型、引擎需求不明确判定（标记需澄清）、能力边界判定 |
| **待方法论成熟** | 当前不具备抽象规则的条件，暂不做 | OR→CU 类型的自动化判定规则、引擎间依赖自动推导、跨引擎 CU 协调设计 |

## 二、输入与路由

### 2.1 标准输入包

1. 本轮处理对象（已进入 `01-*.md`（原始需求文档）的正式 FR-ENG OR 条目 ≤10 条 / 有反馈的 STR-E 节点 / `需wft01修订` 的 STR-E 节点）
2. 上游追溯链（`01-*.md`（原始需求文档））
3. 已有 STR-E 架构资产（`02-*.md`（相关方需求架构文档））
4. 相关约束资产（`25`（平台引擎资产）EOS 引擎 + `23`（产品架构资产）产品架构 + `24`（场景组件资产）业务资产）
5. 重入反馈

### 2.2 Step Start 路由

#### 2.2.1 第一层：入口容错

| 条件 | 路由 |
|------|------|
| OR 条目状态为 `需澄清` | 输出澄清项清单，提示人类澄清 OR 的引擎需求后重新提交 → **退出** |
| OR 条目状态不是 `待处理` | 输出"本 Skill 仅处理 `待处理` 状态的 FR-ENG OR"，并列出本轮 OR 状态分布 → **退出** |
| OR 类型含 FR-BIZ / NFR / 其他非 FR-ENG 类型 | 输出"本 Skill 仅处理 FR-ENG 类型"，并列出类型分布；提示 FR-BIZ 走 `wft01-biz`，NFR 走 `wft01-nfr` → **退出** |
| 输入对象只是 wft02-biz 引擎能力缺口线索，尚未转化为正式 FR-ENG OR | 输出"该对象仍是派生线索，不属于 wft01-eng 输入；请先由人类确认并转化为 01 文档中的 FR-ENG OR"→ **退出** |
| FR-ENG OR >10 条 | 输出"本轮 N 条 FR-ENG OR，建议分批"，提示 OR 条目 ID 列表 → **退出** |
| OR 不能追溯到明确的业务层级/业务对象/业务运行能力，且也不能说明横切支撑范围 / 引擎生命周期阶段 | 输出"缺少业务支撑来源，无法进入 eng 链" + 需澄清项 → **退出** |
| OR 触及多个无依赖关系的引擎 | 输出"请退回 OR 预处理按引擎拆分" + 涉及引擎列表 → **退出** |
| OR 内容不适合配置单元法展开 | 输出 `类型疑义` + 疑义原因，供人类确认 → 人类确认后继续或退出 |
| 无 OR 选中 + 无待处理反馈的 STR-E 节点 + 无 `需wft01修订` 的 STR-E 节点 | 输出"无待处理对象"→ **退出** |

入口容错只判断"能否进入本 Skill"。凡因业务支撑来源缺失、类型疑义、多引擎混杂被拦截的 OR，不得在本轮临时改写为 STR-E；需先由人类澄清、拆分或确认后再以 `待处理` 的正式 FR-ENG OR 重新进入。

#### 2.2.2 第二层：STR-E 重入优先

| 条件 | 路由 |
|------|------|
| 存在 `需wft01修订` 的 STR-E 节点 | 读取 wft02-eng 的缺失说明 → 按人类改进方案修订 STR-E（走 Phase B 修订流程）→ 人类确认 → STR-E → `可以分解分配` |
| 存在 `待确认方案` + 条目 `确认状态` 字段中有人类标注且无 `[已处理]` 的 STR-E 节点 | 本轮先处理全部有反馈的 STR-E 节点 |
| 存在 `待确认方案` + 所有条目均已 `[同意]` + `[已处理]` 的 STR-E 节点 | 不阻塞新 OR；Step End 时提示 |

#### 2.2.3 第三层：新 OR 路由

STR-E 重入处理完后，逐 OR 处理 → Phase A 触发识别。

#### 2.2.4 同次运行顺序约束

Step 2（反馈处理）先于 Step 3（新增设计）执行。Step 2 中 `[修改]` / `[驳回]` 可能改变 EOS 引擎匹配结果或 CU 类型清单，因此 Step 3 设计时**必须基于 Step 2 修改后的 STR-E 节点当前内容**，不得使用 Step 1 加载时的缓存旧状态。

若同次运行既有反馈处理又有新 OR：先完成全部 STR-E 反馈处理；被确认的 STR-E 可进入 Step 4 写回队列，新 OR 则继续进入 Step 3 形成 `待确认方案`。Step 4 只写已确认 STR-E，不写本轮新生成但尚未确认的 STR-E。

---

## 三、文档加载规则

读取遵守 [文档编写约定 §十四](../../../../00-generalspec/00-doc-conventions.md)：读全景层→读索引层→读目标正文块+追溯→按需扩大。

写回遵守 [文档编写约定 §十五](../../../../00-generalspec/00-doc-conventions.md)：先写目标正文块，再同步索引/追溯/状态字段；写回必须局部、幂等、可追溯，不得重排无关资产块。

### 3.1 全景加载

每次进入本 Skill 后，先读取以下全景层：

1. `01-*.md`（原始需求文档）：读取本轮 FR-ENG OR 条目、状态、类型、原文和追溯线索
2. `02-*.md`（相关方需求架构文档）：读取 STR-E 架构全景、候选 STR-E 节点、状态、确认字段和反馈处理记录
3. `25-eos-engine-models.md`（平台引擎资产）：读取 EOS 引擎、CU 类型清单、已有能力边界和候选缺口登记区
4. `23-eos-output-architecture.md`（产品架构资产）：读取产品类型/构件类型归属，用于 EOS 引擎注册和 23-Q1/Q2/Q3 判定
5. `24-eos-business-assets.md`（场景组件资产）：读取业务层级/业务对象/业务运行能力线索，用于验证 FR-ENG 的业务支撑来源

### 3.2 按需加载线索

- **对象线索**：按 OR 条目 ID 或 STR-E 节点 ID 在索引中定位必须读取的正文块
- **追溯线索**：按追溯关系读取上下游相关块，不复制对方正文
- **业务支撑线索**：按 OR 中的业务层级、业务对象、业务运行页面、业务运行能力追溯 24 业务资产和 23 输出产品架构
- **结构线索**：按同 EOS 引擎筛选候选 STR-E 块，用于 Q1/Q2/Q3 宿主匹配
- **语义线索**：按 OR 中的引擎能力需求匹配 25 EOS 引擎候选分片和配置单元类型
- **引擎线索**：按 OR 涉及的引擎类型在 25 资产中定位候选 EOS 引擎和配置单元类型块
- **反馈线索**：重入路径只读取条目的 `确认状态` 字段及反馈指向的相关块，不重新做全量候选读取
- **扩大读取触发**：索引缺失/候选冲突/资产全貌不足以判断边界/追溯关系断裂时，说明原因后扩大

---

## 四、引擎需求设计基础

### 4.1 25 号资产读取原则

读取 25 时按“引擎层级 → 引擎依赖 → CU 类型 → 状态队列”顺序理解，不得只按关键词命中。`@engine-scene` 面向 L4 业务场景组合；`@engine-menu` 只做末级菜单映射和可访问入口，不生成业务内容；`@engine-form` / `@engine-flow` / `@engine-indicator` / `@engine-workorder` 面向 L5 业务功能或部件；`@engine-entity` 面向 L6 业务对象定义；`@engine-permission` 是横切权限能力。

### 4.2 引擎归属判定规则

STR-E 归属先看业务层级和业务对象，再看 25 中的引擎层次与依赖关系。表单依赖实体、流程依赖表单与实体、工单依赖表单/流程/实体、指标从表单/流程/实体采集、菜单只映射到场景入口。若 OR 只是“某业务页面需要入口”，优先判断为菜单/场景访问线索；只有 OR 明确要求生成或编排业务场景时才归入 `@engine-scene`。

权限类需求作为横切能力处理：若权限只是附着在表单、流程、菜单等能力上，主引擎保持原业务生成引擎，同时记录 `@engine-permission` 依赖；只有 OR 的主要目标是角色、属性组、授权关系或权限管理时，才以权限引擎作为主归属。

### 4.3 25 状态与顺序判定

顺序 1 只适用于 25 中已确认且语义匹配的引擎 / CU 类型。顺序 2 适用于已有相近类型、但需要扩展说明或状态仍为 `待确认` 的情况，STR-E 必须保留 `待模型确认` 标记。顺序 3 适用于 25 缺类型、类型待填充或依赖链无法闭合的情况，只形成模型扩展候选，不允许把候选引擎直接作为后续 PA 或开发强依赖。

## 五、操作流程

### Step 布局总览

| Step | 名称 | 产出 | 写资产 |
|------|------|------|--------|
| Start | 前置校验与路由 | 就绪 / 结束 | — |
| 1 | 设计材料加载 | OR / STR-E / 25+23 全景 | — |
| 2 | 方案反馈处理 | 调整后的 STR-E 方案 | `01/02` |
| 3 | 引擎需求设计 | STR-E 节点方案（含 CU 类型清单 + 引擎能力边界陈述 + 23 对齐） | `01/02` |
| 4 | 资产写回与落账 | 23 引用写回 + 25 CU 类型引用 + OR→已分配 + 资产写回记录 | `23/25/01/02` |
| Step End | 运行结束输出 | 人类下一步操作提示 | — |

### STR-E 节点生命周期

#### 推进态（按顺序）

| 状态 | 含义 | 触发条件 |
|------|------|---------|
| `待确认方案` | 方案已生成或修订，等待人类反馈 | Step 3 完成 |
| `可以分解分配` | 方案已确认，wft02-eng 可启动 BA 展开 | 全部条目 `[同意]` + `[已处理]` |
| `在分解分配` | wft02-eng 已锁定此 STR-E，正在展开 BA | wft02-eng 写入活动锁 |
| `已分解分配` | BA 展开完成，STR-E 消费版本与当前版本一致 | wft02-eng 处理完成且未检测到上游新版本 |

人类确认后，两条线并行推进、互不阻塞：
- **资产线**（本 Skill）：Step 4，将 EOS 引擎引用写回 23 资产文件，将 CU 类型清单引用写入 25。资产写回完成与否不改变 STR-E 状态。
- **BA 线**（wft02-eng）：读 `可以分解分配`，展开 A1（平台引擎产品）CU IPO 设计。不依赖 23/25 资产文件中的写回结果。

`在分解分配` 和 `已分解分配` 由 wft02-eng 写入和推进。本 Skill（wft01-eng）写入的范围为 `待确认方案` / `可以分解分配` / `待补充分解分配` / `需wft01修订`，并在修订时更新版本号。

#### 回退态（上游修订触发）

上游修订 = 本 Skill 因新 OR 对已有 STR-E 执行 Step 3 Phase B。修订后版本号递增，状态按原状态分流：

| 原状态 | 修订后状态 | 含义 |
|--------|----------|------|
| `可以分解分配` | `可以分解分配`（版本递增） | 未被下游消费过，版本升级不影响任何 BA |
| `在分解分配` | `在分解分配`（版本递增） | wft02-eng 锁仍有效，重入时检测版本差，按变更类型增量处理 |
| `已分解分配` | **`待补充分解分配`** | BA 已不完整，需 wft02-eng 重新介入补全 |

- **`待补充分解分配`**：上游 STR-E 修订后，已消费的 BA 可能不完整。wft02-eng 进入后按变更影响声明处理，处理完成后若版本一致 → 推进回 `已分解分配`

#### 下游退回态（wft02-eng 退回触发）

下游退回 = wft02-eng 在 BA 展开中发现 STR-E 方案存在缺陷（如缺少业务支撑来源），无法继续 BA 设计，将 STR-E 退回本 Skill 修订。

| 状态 | 含义 | 触发条件 |
|------|------|---------|
| **`需wft01修订`** | STR-E 方案存在缺陷，需本 Skill 修订后重新交付 | wft02-eng 写入，附带缺失说明 |

本 Skill 重入 `需wft01修订` 的 STR-E 时：读取 wft02-eng 的缺失说明 → 按人类改进方案修订 STR-E（走 Phase B 修订流程）→ 人类确认 → STR-E → `可以分解分配`

---

### Step 1 · 设计材料加载

按 §三 加载全景层和本轮正文块。

---

### Step 2 · 方案反馈处理

#### 人类审阅规则

人类直接在 STR-E 节点条目上标注意见——在条目的 `确认状态` 字段写入 `[同意]` / `[修改]`：修改内容 / `[驳回]`：驳回理由。AI 处理完后在该字段末尾追加 `[已处理]`——是否已有 `[已处理]` 是 AI 判断"本轮是否需要处理"的唯一依据。

1. **条目级反馈**（权威）：对 STR-E 节点的独立决策单元（引擎能力边界陈述、各 CU 类型、23 对齐项）逐条标注。已 `[同意]` + `[已处理]` 的条目在后续轮次中默认跳过——人类若改变主意，直接写入新的 `[修改]` / `[驳回]` 即覆盖旧标注，覆盖后 `[已处理]` 自然消失，条目回到待处理状态
2. **整体确认**（快捷）：人类在对话中说「整体确认」→ AI 对**无条目级标注的条目**写入 `[同意]`（标注来源=`整体确认`），随后对这批条目追加 `[已处理]`——与条目级 `[同意]` 等效

**冲突消解**：条目级 > 整体确认。两者并存时，条目级标注保留原结论不受影响，整体确认仅补未标注的剩余条目。

#### AI处理规则

若本轮存在 `待确认方案` 的 STR-E 节点，AI 扫描各条目的 `确认状态` 字段——有人类标注但无 `[已处理]` 的条目为本轮待处理对象：

1. **扫描待处理条目**——遍历 STR-E 节点各条目（引擎能力边界陈述 + 各 CU 类型 + 23 对齐项），筛选 `确认状态` 中有人类标记（`[同意]` / `[修改]` / `[驳回]`）但无 `[已处理]` 的条目
2. **收集整体确认**——若人类说了「整体确认」，对**无条目级标注的剩余条目**写入 `[同意]`（标注来源=`整体确认`），随后为该批条目追加 `[已处理]`
3. **执行**——逐条目按人类意见处理：

| 人类意见 | AI处理 |
|---------|------|
| `[同意]`（明示或整体确认补全） | 条目维持不变 → 在 `确认状态` 字段末尾追加 `[已处理]` |
| `[修改]`：修改内容 | 按反馈调整该条目 → 重新对齐 → 追加 `[已处理]` |
| `[驳回]`：驳回理由 | 重做该条目 → 追加 `[已处理]` |

4. **判定 STR-E 状态**：
   - 全部条目 `确认状态` 含 `[同意]` + `[已处理]` → STR-E → `可以分解分配` → 进入 Step 4 写回
   - 任一条目含 `[修改]` 或 `[驳回]`（不论是否已追加 `[已处理]`）→ STR-E 保持 `待确认方案`
5. 写入 `### 反馈处理记录`（含本轮各条目人类意见+来源+处理动作+时间戳）

**多轮迭代**：`[同意]` + `[已处理]` 双标记完整的条目本轮默认跳过（已确认且已执行）。但新 OR 的修订可能改变已有条目的定义/归属——AI 在处理修订时先清除受影响条目的 `[已处理]` 标记（保留 `[同意]`），使条目回到待处理状态，供人类重新审阅。上游 OR 变更优先于人类确认。

**写回时机**：若本轮仅有反馈处理（无新 OR），直接进入 Step 4 写回 23/25 资产；23-Q1/Q2/Q3 与 25 顺序 1/2/3 均按已确认方案处理，不需人类二次确认。若本轮同时有新 OR，先执行 Step 3 完成新增或修订设计；Step 4 仍只写本轮已确认的 STR-E，新产出的 `待确认方案` 留待下一轮人类确认后再写资产。

---

### Step 3 · 引擎需求设计

#### Phase A：OR → STR-E 归属判定

**1. 需求溯源**

从 OR 语义提取"哪个业务层级/业务对象/业务运行能力需要启动这项引擎能力需求"。触发 = "业务配置人员 + 为了生成/支撑 ZZ 业务运行能力 + 需要让 XX 引擎具备 YY 配置能力"。

若 OR 属于公共引擎基础能力，触发可表达为："为保障一类或多类业务运行能力在配置、发布、运行、回滚、审计等生命周期阶段稳定可用，需要让 XX 引擎具备 YY 基础能力"。此类 OR 不强制绑定单个业务对象，但必须说明横切支撑范围，不得退化为纯平台内部自嗨能力。

FR-ENG OR 条目应在 ort03 阶段已完成引擎拆分（ort03 规则 #11），每条 OR 仅涉及一个 EOS 引擎。若 OR 触及多个引擎，输出"OR-XXX 涉及多个 EOS 引擎，请退回 ort03 按引擎拆分"→ **退出**。引擎需求不明确时标注"需澄清"，在 `01-*.md` 中将该 OR 推进为 `需澄清`，AI 不做强行匹配。

**2. 节点匹配**

以 OR 的引擎能力语义为锚点，直接匹配已有 STR-E 节点——STR-E 节点本身就是 EOS 引擎能力声明（含引擎能力边界陈述和 CU 类型清单）。

| 判定 | 条件 | 动作 |
|------|------|------|
| **Q1（复用）** | OR 的引擎能力需求与已有 STR-E 的引擎定义语义一致，且 CU 类型清单可覆盖 | 归入已有 STR-E → 从 25 资产加载该引擎的 CU 类型清单供 Phase B 使用 |
| **Q2（扩展）** | OR 触及的引擎与已有 STR-E 相同，但能力需求超出当前 CU 类型清单范围 | 归入已有 STR-E → 从 25 资产加载清单 + 标注需扩展的 CU 类型方向 |
| **Q3（新建）** | 无已有 STR-E 对应此引擎能力需求 | 标记待新建 → 基于 OR 语义提出新引擎的候选 CU 类型（名称级别），全部标记 `[推断]` + `归属待定` |

状态敏感匹配：`已确认`→正常；`待确认`→标注 `[推断]`；`疑似`→标注 `[推断]` + `归属待定`；`废弃`→不使用。

STR-E 宿主匹配完成后输出宿主分配结论（含 Q 级别/匹配到的 STR-E ID/新建时的建议 ID）。

**归属结论**：Q1/Q2 → 归入已有 STR-E，进入 Phase B（修订已有 STR-E）；Q3 → 进入待聚合池 → Phase C（新建 STR-E）。

**Q3 待聚合池分组**：多条 Q3 OR 按引擎能力语义聚合——触及同一引擎能力方向的 OR 归入同一新建 STR-E。分组完成后每组进入 Phase C 独立处理。

#### Phase B：修订已有 STR-E

> 本 Phase 仅在 Phase A 结论为 Q1 或 Q2 时执行。对已有 STR-E 节点执行增量修订——引擎能力边界检查 → 多 OR 收敛检测 → 增量 CU 识别 → 验证输出更新。

**1. 引擎能力边界检查**

先检查 OR 是否在已有 STR-E 的引擎能力边界内。比对锚点为 STR-E 的引擎能力边界陈述：

| 结果 | 判定 | 处理 |
|------|------|------|
| 边界内 | OR 的引擎能力需求未超出当前 STR-E 能力边界陈述的范围 | 正常进入六关系检测 |
| 边缘 | OR 处于能力边界边缘，部分需求与当前边界相邻但不完全超出 | 增量标记 `[边界扩展]`，更新引擎能力边界陈述，重新执行跨 CU 检查（按 Phase C 步骤 3 的检查项） |
| 超出 | OR 的引擎能力需求明确超出当前能力边界 | 增量标记 `[边界扩展]`，更新引擎能力边界陈述 + 按 Phase C 步骤 1 追踪引擎能力自然边界，重新执行跨 CU 检查；若边界扩展导致与原 STR-E 粒度不一致，标记 `[推断]` 供人类裁决是否拆分 |

**Phase A Q2 与 Phase B 边界结果不一致时的处理**：Phase A 的 Q2 是候选筛选（"大概率归这个 STR-E"），Phase B 的边界检查是精确匹配。当边界检查检出"超出"比 Q2 预期的更严重时，AI 不得自行回退 Phase A 重选宿主——继续完成 Phase B 全部处理，标记 `[推断]`，将边界冲突显性化提交人类方案审阅。原因：拆分引擎是架构决策，不应由 AI 单方面判断。

**2. OR 与已有 CU 比对**

从 25 资产加载该 EOS 引擎的 CU 清单，将新 OR 的能力需求与已有 CU 逐一比对，判定属于以下哪种关系：

| 关系 | 判定 | 处理 |
|------|------|------|
| 确认 | 新 OR 触及的 CU 与既有一致 | 来源升级（`OR隐含`→`OR明示`），增量标记 `[确认]` |
| 补充 | 新 OR 为既有 CU 补充了新的能力需求说明 | 追加简要说明，来源=`OR明示`，增量标记 `[新增]` |
| 矛盾 | 新 OR 对同一 CU 的能力需求与既有不同 | 追加矛盾记录，增量标记 `[矛盾]`，等人类裁决 |
| 无新增 | OR 触及的 CU 完全被既有覆盖 | 仅追加 OR 引用 |
| 未触及 | OR 未涉及该 CU | 保持既有内容不变 |
| 占位→实体 | 引擎预定义 CU 被新 OR 首次触及 | 来源升级（`引擎内置`→`OR明示`），增量标记 `[确认]` |

来源类型升级规则（只升不降）：`OR隐含`→`OR明示`、`引擎内置`→`OR明示`、`推断`→`OR明示`。矛盾未解决时不得推进到写回。

CU 排序（按 25 资产顺序/依赖拓扑/OR 触及程度三级优先级排列）。

比对完成后执行跨 CU 验证（检查项同 Phase C 步骤 3：覆盖完整性/粒度自洽/无重叠/集中性/依赖完整性），并更新引擎能力边界陈述（格式见 Phase C 步骤 1），反映本轮修订后的能力覆盖变化。

**3. 精简覆盖自查**（机械检查，不重新评估设计质量）：仅检查**本轮变更涉及的 CU 类型**——关系=`补充` 的新触及 CU，关系=`占位→实体` 的来源升级 CU，以及关系=`矛盾` 的双边 CU。走 `确认`/`无新增`/`未触及` 的 CU 默认跳过（已在 Phase C 新建时通过覆盖自查，或此前已通过增量自查）。逐变更 CU 确认：触及方式（明确触及/隐含触及/未触及）已标注；能力需求说明 **非空**；来源类型/增量标记/确认状态 已写入。逐项打勾，结果追加在关系检测输出之后。发现遗漏时当场补齐——覆盖自查是机械门禁，补齐不影响设计质量判断。

**4. 下游变更影响声明**

按本轮 OR 对 STR-E 的实际变更范围判定变更类型——仅新增 CU 触及且引擎能力边界未变 → `增量型`；已有 CU 的触及方式/能力需求说明被修改且边界未变 → `修订型`；能力边界变化或引擎匹配改变 → `结构型`。将变更影响声明写入 STR-E 节点末尾（格式与完整规则见 §5.8 变更传播协议）。

---

#### Phase C：新建 STR-E

> 本 Phase 仅在 Phase A 结论为 Q3 时执行。从零开始新建 STR-E 节点，完成引擎能力边界定义、CU 首次识别、资产对齐和完整要素输出。EOS 引擎统一归属流程与IT域，无需额外确定引擎域。

**1. 引擎能力边界定义**

定义新引擎的能力范围——从触发到终点覆盖了哪些配置能力。

- 识别触发：明确启动该引擎能力需求的配置场景
- 自然边界：以 OR 语义覆盖的能力范围为边界，不在 OR 未覆盖的方向扩展
- 边界检查：完整性（覆盖一件完整工作）/ 一致性（同一引擎类型）/ 紧凑性（不混多个不连续能力段）

输出边界陈述：`XX EOS 引擎` 为生成/支撑 `哪些业务运行能力` 需要具备 `哪些配置能力`。CU 覆盖分析在步骤 3 完成后回填，每个触及的 CU 按以下三级判定：

- **顺序 1**：25 中已有匹配 CU，wft02-eng 可直接展开 CU IPO 设计
- **顺序 2**：25 中有近似 CU 需扩展，wft02-eng 在已有 CU 基础上推断设计 + 标注 `[推断]`
- **顺序 3**：25 中无匹配 CU，进入 wft02-eng 时只能形成候选 CU IPO 草案 + 25 新增建议，标注 `[推断]` + `[模型需扩展]` + `待确认`；若人类不允许推断设计，则待 25 资产补齐后重入

**2. 推理依据**

为每个设计决策标注推理依据，按主导角度归类、编号引用。

- **配置常识推理**：基于业务配置的一般规律推断。如"任何审批都需要驳回选项""表单字段应有默认值"
- **引擎机制推理**：基于引擎的运行机制推断。如"表单引擎通过 CU 绑定字段→需要字段定义 CU""流程引擎通过状态机驱动→需要状态定义 CU"
- **运行体验推理**：基于配置人员的使用体验推断。如"配置页面应所见即所得""字段可编辑状态应在运行时实时反馈"

**3. CU 识别**

从 25 资产加载该 EOS 引擎的 CU 清单，逐一判定 OR 是否触及：

| 触及判定 | 处理 |
|---------|------|
| CU 在 25 内，OR 明确触及 | 标注触及 + 简要说明能力需求，来源=`OR明示`，标记 `[新增]` |
| CU 在 25 内，OR 隐含触及 | 标注触及 + 推断依据 + 标注 `[推断]`，来源=`OR隐含`，标记 `[新增]`+`[推断]` |
| CU 在 25 内，OR 未触及 | 占位保留，来源=`引擎内置`，标记 `[待触发]`，待后续 OR 填充 |
| CU 不在 25 内，OR 触及 | 标注触及 + 25 资产扩展建议，来源=`推断`，标记 `[新增]`+`[推断]`+`模型需扩展` |

> 仅标注"该 CU 是否被 OR 触及 + 能力需求的一句话说明"，不设计三栏。完整设计由 wft02-eng 负责。

CU 排序：按 25 资产顺序、依赖拓扑、OR 触及程度三级优先级排列。

跨 CU 验证：

| 检查项 | 内容 |
|--------|------|
| 覆盖完整性 | OR 的全部引擎能力需求是否均被 CU 覆盖 |
| 粒度自洽 | 触及的 CU 之间粒度是否一致 |
| 无重叠 | 两个 CU 的能力边界是否清晰 |
| 集中性 | 全部 CU 是否归属同一 EOS 引擎 |
| 依赖完整性 | CU 间的依赖链是否闭环（前置 CU 均在清单中） |

**4. 引擎注册**

将 EOS 引擎注册到 23 产品架构，归属流程与 IT 域 → 平台引擎类构件类型：

| 情形 | 处理 |
|------|------|
| 23 中已有对应的产品类型/构件类型 | 23-Q1 + 产品类型 ID + 构件类型 ID |
| 23 中有相近的可扩展 | 23-Q2 + 扩展建议 |
| 23 中无匹配 | 23-Q3 + 新增建议 |

**5. 覆盖自查**：逐项确认 STR-E 完整要素是否齐备——

| 检查项 | 内容 |
|--------|------|
| 业务支撑来源 | 是否已从 OR 语义还原"哪个业务层级/业务对象/业务运行能力需要该引擎具备什么能力" |
| EOS 引擎 | 是否已引用 25 资产引擎 ID + 标注来源类型/增量标记/确认状态 |
| 23 产品架构对齐 | 是否已写 23-Q 判定 + 产品类型/构件类型/资产建议 |
| 引擎能力边界陈述 | 是否每个触及的 CU 均有顺序 1 / 顺序 2 / 顺序 3 判定 |
| CU 清单 | 是否每个 CU 标注了触及方式/能力需求说明/来源类型/增量标记/确认状态 |
| 推理依据 | 是否已标注主导推理角度 |
| 变更影响声明 | 写"首版，无变更" |

自查结果写入 STR-E 节点末尾。

**STR-E 完整要素**（Phase C 产出，Phase B 修订时同名产出）：

STR-E 节点包含以下要素：

- **定义**
- **业务支撑来源**：OR 需求一句话概括——"哪个业务层级/业务对象/业务运行能力需要该引擎具备什么能力"；公共引擎基础能力写明"横切支撑范围 + 引擎生命周期阶段 + 受影响业务能力类别"
- **EOS 引擎**：25 资产引用，含引擎 ID + 来源类型/增量标记/确认状态
- **23 产品架构对齐**：23 产品类型/构件类型/23-Q判定/23资产建议
- **引擎能力边界陈述**：含 CU 覆盖分析 + 顺序 1/2/3 判定
- **CU 清单**：名称级，每个标注触及方式/能力需求说明/来源类型/增量标记/确认状态
- **推理依据** / 承接的 OR 末级
- **符合性分析**（`待 wft02-eng 生成`）/ **设计指导**（`待 wft02-eng 生成`）
- **STR-E 版本号**（初始 v1，每次因 OR 修订递增）/ **变更影响声明**
- **CU 清单变更摘要**

**Phase A 节点匹配与 Phase C 识别结果不一致时的处理**：Phase A 的节点匹配是候选匹配（基于 OR 语义推断），Phase C 的步骤 3 是精确 CU 识别。当步骤 3 发现 OR 触及的 CU 实质上属于另一个 EOS 引擎时，AI 不得自行回退 Phase A 重选引擎——继续完成 Phase C 全部处理，在受影响的 CU 上标记 `[推断]` + `归属待定`，将引擎冲突显性化提交人类方案审阅。原因：更换 EOS 引擎是架构决策，不应由 AI 单方面判断。

---

### Step 4 · 资产写回与落账

Step 4 仅写回 Step 2 中已确认的 STR-E 节点（状态=`可以分解分配`）所关联的 23/25 资产。Step 3 新产出的 STR-E（状态=`待确认方案`，尚未经人类确认）不触发 23/25 写回。

人类已在 Step 2 中确认方案（全部条目 `[同意]` + `[已处理]`），因此 Step 4 不再二次征询确认。23-Q1/Q2/Q3 与 25 顺序 1/2/3 均按确认方案执行；但 25 顺序 3 只能写入资产新增建议 / 候选 CU 类型，不得在本步固化为正式 CU 模型能力。

#### 资产写回

**写回 23（产品架构资产——EOS 引擎注册）**，按 EOS 引擎的 23 判定：
- 23-Q1：定位产品类型/构件类型 → 追加引擎引用（含引擎名称/定义/25 引擎ID）→ 记录追溯引用
- 23-Q2：扩展已有产品类型/构件类型 → 追加引擎引用 + 扩展说明 → 记录追溯引用
- 23-Q3：新建产品类型/构件类型块 → 写入定义 + 引用 → 记录追溯引用

**写回 25（EOS 引擎资产——CU 类型清单引用）**：
- 定位 EOS 引擎块 → 在"支持的业务"或对应区域写入本 STR-E 涉及的 CU 类型清单引用（名称级别，含能力需求的一句话说明）
- 若存在顺序 3 缺口（25 无匹配的 CU 类型），形成 25 资产新增建议（CU 类型名称/所属引擎/建议的 Input/Process/Output 方向/建议状态=`待确认`）

写回 25 时仅写入 CU 类型名称级引用，**不含三栏设计细节**——完整的 Input/Process/Output 设计由 wft02-eng 负责并写回。

写回要求：幂等去重、局部写回、全貌同步。

#### 资产落账

写回完成后：
- `02` 记录 `### 资产写回记录`
- `01` 对应 OR 条目推进为 `已分配`
- OR→STR-E→23/25 引用链记录

说明：Step 4 不改变 STR-E 节点状态——状态已在 Step 2 设为 `可以分解分配`。资产写回是独立的收尾工作，不构成 wft02-eng 的前置条件。若 STR-E 因上游修订处于 `待补充分解分配`，本轮只记录变更影响声明和待 wft02-eng 补全提示，不把未重新确认的修订内容写入 23/25。

---

### Step End · 运行结束输出

#### Step End 提示

```text
当前状态：完成 FR-ENG OR 处理
  修订 STR-E-XXX（+N 条 OR，版本 v{N-1}→v{N}）/ 新建 STR-E-YYY（聚合 K 条 OR，版本 v1）
  已确认并可写回 STR-E-AAA（状态=可以分解分配，23/25 写回已完成/待执行）
  待确认 STR-E-BBB（状态=待确认方案，本轮不写 23/25）
  需澄清 OR-ZZZ（引擎需求不明确，已推进为 需澄清 状态）
  待退回预处理 OR-YYY（触及多个无依赖关系的引擎，需按引擎拆分后重新提交）
  引擎能力边界：顺序 1（N 个 CU 类型）/ 顺序 2（N 个 CU 类型，需推断设计）/ 顺序 3（N 个缺口，待 25 补齐）

你可以：
  A. 增加新 OR → 选择 FR-ENG OR 条目重新运行
  B. 确认方案 → 在条目的 `确认状态` 字段标注意见，或整体确认：
     - STR-E-XXX（待确认方案）：
       逐条标注：在引擎能力边界陈述 / 各 CU 类型 / 23 对齐项的 `确认状态` 字段写入「[同意]」/「[修改]：修改内容」/「[驳回]：驳回理由」
       快捷：回复「整体确认」= 所有未标注的条目自动写入 [同意]
       AI 处理后在标注末尾追加 [已处理]——已处理条目在后续轮次中默认跳过
  C. 处理需澄清的 OR → 澄清 OR-ZZZ 的引擎需求后，将 OR 状态从 需澄清 改回 待处理 重新提交
  D. 退回预处理拆分 OR → 将 OR-YYY 按目标 EOS 引擎拆分为多个正式 FR-ENG OR 后重新进入本 Skill
  E. 进入 wft02-eng → 可用的 STR-E 节点：
     - STR-E-WWW（可以分解分配，首次进入）
     - STR-E-ZZZ（待补充分解分配，上游已修订，需 wft02-eng 补全）
  F. 补齐 25 资产缺口 → 顺序 3 的 CU 类型待 25 资产新增后重新运行 wft01-eng
```

---

### 5.8 STR-E 变更传播协议

下游 wft02-eng 消费 STR-E 后生成 A1 配置单元节点。当 STR-E 因新 OR 修订而变更时，已生成的 BA 节点面临同步问题。本协议定义 STR-E → BA 的变更分类、通知格式和消费追溯。

#### 变更类型分类

| 变更类型 | 判定标准 | 实例 | 对下游影响 |
|---------|---------|------|----------|
| **增量型** | 仅新增 CU 类型触及，已有 CU 类型的触及方式/能力需求说明均未改动；EOS 引擎匹配未变 | 新 OR 触及了新的 CU 类型 | 已有 A1 BA 节点不受影响，仅需展开新 CU |
| **修订型** | 已有 CU 类型的触及方式/能力需求说明被修改；EOS 引擎匹配未变 | 某 CU 类型的能力需求说明从 X 变为 Y | 对应 BA 节点需局部调整 |
| **结构型** | EOS 引擎匹配改变、引擎能力边界变化 | 新 OR 要求更换 EOS 引擎 | BA 结构可能重塑，需人类裁决后方可处理 |

判定优先级：结构型 > 修订型 > 增量型。

#### 变更影响声明格式

每次修订已有 STR-E 后，在 STR-E 节点末尾写入（`02-*.md` 中 STR-E 节点块内）：

变更影响声明包含以下内容：

- **STR-E 版本**：v{N}（本次修订前为 v{N-1}）
- **本轮变更类型**：增量型 / 修订型 / 结构型
- **变更项**：列出本轮 [新增] / [改进] / [矛盾] / [模型需扩展] ... 等标记项
- **下游已消费此 STR-E 的 BA 节点**：列出 BA 节点 ID 列表（如无则写"尚未被下游消费"）
- **对已消费 BA 节点的影响**：区分受影响节点（含原因）、不受影响节点、待新增 BA 节点
- **建议 wft02-eng 动作**：
  - 增量型：增量展开新增 CU 即可，已有 BA 节点不动
  - 修订型：修订受影响的 BA 节点 → 标记 [来自上游变更]
  - 结构型：输出影响范围，等人类裁决后再处理

#### 版本标记与消费追溯

- STR-E 节点初次生成时版本 = `v1`，变更影响声明写"首版，无变更"
- 每次因新 OR 触发 Step 3 Phase B 修订，版本号递增
- 版本号写入 STR-E 节点头部（与节点 ID 同级）
- wft02-eng 首次消费 STR-E 时，在 `04-*.md` 对应 BA 节点中记录 `STR-E 消费版本：v{N}`
- wft02-eng 重入同一 STR-E 时比对版本：若 STR-E 当前版本 > 已消费版本 → 触发变更感知

#### 增量清单与变更影响声明的关系

| 维度 | 增量清单（§五） | 变更影响声明 |
|------|----------------|------------|
| 面向读者 | 人类 | 下游 Skill（wft02-eng） |
| 目的 | 告知"本轮我做了什么，请逐项审阅" | 告知"STR-E 变了什么，你需要做什么" |
| 内容 | 本轮新增/改进/确认/矛盾的逐项列表 | 变更类型 + 对已消费 BA 的影响 + 建议动作 |
| 写入位置 | STR-E 节点末尾（供人类参照 `确认状态` 字段逐条审阅） | STR-E 节点末尾（独立小节） |
| 生成时机 | 每次 Step 3 产出方案时 | 仅修订已有 STR-E 时；新建 STR-E 写"首版，无变更" |

---

## 六、增量清单输出

STR-E 路径的标记：

| 标记 | 含义 | 使用场景 |
|------|------|---------|
| `[新增]` | 本次新增的内容 | 新 OR、新触及的 CU 类型 |
| `[改进]` | 对既有内容的修改 | 人类反馈驱动的 CU 类型识别修订 |
| `[确认]` | 推断内容被新 OR 证实 | 来源类型升级（`OR隐含`→`OR明示`等） |
| `[矛盾]` | 新 OR 与既有 CU 类型识别存在分歧 | 需人类裁决 |
| `[推断]` | 匹配基于待确认边界或信息不足 | 候选 EOS 引擎/CU 类型触及推断 |
| `[边界扩展]` | OR 处于引擎能力边界边缘或超出 | Phase B 边界匹配检查 |
| `[待触发]` | 引擎预定义 CU 类型，当前 OR 未触及 | 占位标记，待后续 OR 填充 |
| `[粒度调整]` | CU 类型建议拆分或合并 | 跨 CU 类型检查发现粒度不一致 |
| `[边界模糊]` | 两个 CU 类型职责边界不清 | 跨 CU 类型检查发现重叠 |
| `[模型需扩展]` | 超出已知 CU 类型 | OR 触及 25 未覆盖的能力 |
| `[阶段性]` | CU 类型信息不足，即使推断也无法完成完整识别 | 不得推进到写回 |
| `[23-Q1/23-Q2/23-Q3]` | EOS 引擎对 23 资产的对齐结果 | Phase A 对齐 |

增量清单格式（新建 STR-E）：

```text
=== STR-E-XXXX 本轮更新清单 ===

[新增] 承接 OR-XXXX, OR-YYYY（共2条）
[新增] CU 类型 @form-cu-field 被 OR 明确触及——需要"驳回原因"字段定义能力
[推断] CU 类型 @form-cu-behavior 被 OR 隐含触及——推断需要字段可编辑状态控制能力

=== 以上为本次全部新内容 ===

请逐项确认，或回复"整体确认同意"。
```

---

## 七、回退规则

| 条件 | 处理 |
|------|------|
| STR-E 宿主分配需要重做 | 回退 Phase A 步骤 2 |
| 23 资产对齐需重做 | 回退 Phase C 步骤 4 |
| CU 类型识别需重做 | 回退 Phase B（Q1/Q2）/ Phase C 步骤 3（Q3） |
| 资产块在资产文件中不存在（Q1 失效） | 降级为 Q3，形成新增建议 |

---

## 八、与原 wft01 的关系

1. 本文件从 `eos-wft01a-or2str-design.md` Step 4 + `eos-wft01b-or2str-settle.md` STR-E 写回路径合并自闭环
2. FR-BIZ 路径 → `eos-wft01-biz-or2str.md`，NFR 路径 → `eos-wft01-nfr-or2str.md`

---

## 九、变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-07-01 | v2.14 | 按 wft01-biz 风格补充“职责定位 / 设计基础 / 操作流程 / 增量清单”布局；新增 25 号资产关系说明，明确 25 是引擎/CU 类型事实源而非业务实例库；补充引擎层级、菜单/权限横切、待确认状态与顺序1/2/3判定规则，防止把 25-Q3 候选固化为正式模型能力 |
| 2026-07-01 | v2.13 | 对齐 wft01-biz 的入口与出口规则：§二拆为标准输入包/Step Start 路由；入口容错新增 `待处理` 状态门禁、类型分布提示、无对象三条件退出；同次运行明确反馈优先与已确认 STR-E 写回队列；§三改为全景加载/按需加载结构；Step 2 新增写回时机；Step 4 明确仅写已确认 STR-E，未确认的新 STR-E 不写 23/25；Step End 增加待确认/待退回预处理提示 |
| 2026-06-29 | v2.12 | Phase B 新增步骤 3"精简覆盖自查"——六关系检测后对变更涉及的 CU 类型（补充/占位→实体/矛盾）执行机械检查门禁；原步骤 3→4（下游变更影响声明） |
| 2026-06-29 | v2.11 | §一 职责补充：第2点增加"引擎需求不明确时推进 OR 为 `需澄清`"；新增第9条"承接下游退回"职责；标准输入包增加 `需wft01修订` STR-E 节点 |
| 2026-06-29 | v2.10 | STR-E 生命周期新增下游退回态 `需wft01修订`（wft02-eng 退回触发）；Step Start 第二层重命名为"STR-E 重入优先"+ 新增 `需wft01修订` 路由行；本 Skill 写入状态范围同步更新 |
| 2026-06-29 | v2.9 | 引擎需求不明确的 OR 推进为 `需澄清` 状态——Phase A 标记"需澄清"时同步推进 OR 状态；Step Start 新增 `需澄清` 入口拦截行；Step End 新增处理选项 C |
| 2026-06-29 | v2.8 | Step 2 反馈模型重构为两层——条目级反馈（权威留痕）+ 整体确认（快捷补全）；冲突消解：条目级 > 整体确认；STR-E→`可以分解分配` 条件收紧为全部条目 `[同意]`；多轮迭代写保护；Step End 提示同步更新 |
| 2026-06-29 | v2.7 | 明确 eng 线以业务生成/支撑为牵引：STR-E 必须保留 `业务层级/业务对象→业务运行能力→支撑引擎→CU` 追溯；公共引擎基础能力允许以横切支撑范围和生命周期阶段追溯；wft01-eng 只做 CU 名称级识别和配置页面/子页面线索标注，不做 CU IPO；顺序3只登记为 25 新增建议/候选 CU 类型，不固化正式模型；入口容错新增业务支撑来源缺失和多引擎拆分门禁；术语收敛为 EOS 引擎 |
| 2026-06-28 | v2.6 | 增加入口门禁：wft01-eng 只处理已写入 `01-*.md` 的正式 FR-ENG OR；来自 wft02-biz 的引擎能力缺口线索若尚未被人类确认并转化为正式 OR，不得直接作为本 Skill 输入 |
| 2026-06-26 | v2.5 | §一 设计原则 → 主要目的与目标：简述——取信于相关方，名称级识别不脑补。全链路统一格式 |
| 2026-06-26 | v2.4 | §一 新增设计原则（五条）——名称级识别不越界设计 / 能力边界驱动 / Phase A/B/C 三阶段同构 / 收敛检测与来源升级 / AI 推断透明化。含目的与价值声明，回答"做什么、做到什么程度为止、判断依据是什么"，定义 wft01-eng 身份：AI 是高效的识别者，不是自主的决策者 |
| 2026-06-25 | v2.3 | 与 wft01-biz 全量结构对齐：Phase A 重命名为 OR→STR-E 归属判定，子步骤改为 1.需求溯源 / 2.节点匹配，删除 A.3（23对齐移入 Phase C）；Phase B 重命名"修订已有 STR-E"，精简为 3 步（边界检查→OR与已有CU比对→变更影响声明），与 biz 对称；Phase C 重命名"新建 STR-E"，精简为 5 步（边界定义→推理依据→CU识别→引擎注册→覆盖自查），CU识别表统一为四行（25内外×触及与否）；术语引擎模型→业务引擎，CU类型→CU；顺序 1/4/5→1/2/3；代码块→Markdown格式；单引擎OR门禁（ort03规则#11兜底） |
| 2026-06-25 | v2.2 | Step 3 结构精化：Phase B 新增内部分流表（Q1/Q2 完整四步 vs Q3 跳过 B.1/B.2）；B.1/B.2 标注"仅 Q1/Q2"，B.3/B.4 标注"Q1/Q2/Q3 均执行"；B.2 末尾新增变更影响声明写入触发（对齐 wft01-biz Phase B）；B.2 末尾新增 Phase A 业务引擎匹配与 Phase B 识别结果不一致处理规则（不得自行回退，标记 [推断]+归属待定）；Phase C 新增 C.5 覆盖自查（含新建 STR-E 写"首版，无变更"）；方法论成熟度表"启发式推断"行扩展（触发识别、引擎需求不明确判定） |
| 2026-06-25 | v2.1 | 与 wft02-eng 分工重定义——wft01-eng 降级为 CU 类型识别（名称级别，不做三栏设计）；删除 A.3 抽象业务定义类型（伪概念）；Phase B 从"配置单元展开"改为"配置单元类型识别"；Phase C.2 引擎能力边界陈述扩展为结构化顺序 1/2/3 判定表；25-Q 逐单元对齐移入 wft02-eng；§二 改为三层路由（对齐 wft02-biz）；§一 新增方法论成熟度分级说明；Step End 新增选项 D（25 资产补齐） |
| 2026-06-23 | v2.0 | 对齐 wft01-biz v1.12 全量结构：生命周期 3→5+1 态（可以资产化→可以分解分配 + 在分解分配/已分解分配 + 待补充分解分配回退态）；Step 4+5+6 合并为 Step 4"资产写回与落账"；Run End→Step End 移入 §四；新增 §四·附 STR-E 变更传播协议；Phase B 新增下游变更影响声明；STR-E 完整要素增加版本号；§六~§九顺移为 §六~§八 |
| 2026-06-23 | v1.7 | 点Q/R：§五标记表新增 [改进]（对既有内容的修改）；A.2 状态敏感匹配新增 `疑似`→标注 [推断]+归属待定 |
| 2026-06-23 | v1.6 | 点L/M/N/O/P：路由新增"反馈区为空→不阻塞"；§三按需加载线索从名称列表扩展为逐条描述；Step 6 新增清空反馈区待办信号；§五新增 [阶段性] 标记定义+增量清单格式示例 |
| 2026-06-23 | v1.5 | 点I/J/K：补全缺失的 Step 1 设计上下文加载；B.2 六类关系从名称列表扩展为完整判定+处理表（含来源升级规则+矛盾阻断）；§六 Run End 输出扩展为完整格式（单 ID 原则+最小待办格式+提示模板） |
| 2026-06-23 | v1.4 | 点F/G/H：补全缺失的 Step 2 方案反馈处理（同意/修改/驳回）；§五通用标记从"同通用定义"改为完整定义表；§二新增同次运行顺序约束，与 wft01-biz v1.5 对齐 |
| 2026-06-23 | v1.3 | 点D：A.3 抽象业务定义类型新增粒度锚点规则——以业务引擎支撑能力边界为锚点，通用引擎可较粗、领域引擎应较细，人类方案审阅兜底 |
| 2026-06-23 | v1.2 | 点C：回退规则新增"资产块在资产文件中不存在（Q1 失效）→ 降级为 Q3，形成新增建议"，与 wft01-biz 对齐 |
| 2026-06-23 | v1.1 | 点A：新增 STR-E 节点生命周期表（待确认方案→可以资产化→待分解分配），与 wft01-biz 对齐 |
| 2026-06-23 | v1.0 | 初版：从 wft01a Step 4（配置单元法）+ wft01b STR-E/23+25 写回路径合并自闭环；FR-ENG OR→STR-E 引擎定义设计→23/25 资产对齐→写回 全流程覆盖 |
