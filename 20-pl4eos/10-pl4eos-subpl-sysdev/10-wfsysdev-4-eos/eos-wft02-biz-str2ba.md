# STR-F → BA · 业务架构设计与资产化
**Skill ID**: wft02-biz-str2ba

> **概述**：接收 ≥1 个 STR-F 节点（`可以分解分配` / `待补充分解分配`），BA 反馈优先。将 STR-F 的场景业务展开为 BA 方案——A2 角色场景视角 + Bn 输出产品视角，为流程和工单类业务定义其活动节点与文档状态。设计过程中识别的业务缺口统一生成 FR-BIZ OR 材料，经 OR 预处理链确认为正式 OR 后才进入下游路径。完成 23/24/26/27 资产对齐写回。
>
> **输入 → 输出**：STR-F 节点 / 有反馈的 BA 节点 → BA 方案 + 23/24/26/27 资产写回（状态=`可以SR设计`）+ STR-F→`已分解分配` + 原始需求材料文档（如有）
>
> **适用场景**：瀑布式系统设计第2步 · 业务架构路径。
>
> **设计模式参考**：[91-eos-biz-eng-spec.md](91-eos-biz-eng-spec.md)
>
> **版本**：v4.0 | **修订**：2026-07-07

---

## 一、职责定位

### 1.1 本 Skill 目的与目标

> **主要目的**：取信于业务方——「你给出的业务方案是对的」。把 STR-F 的场景业务展开为 BA 方案——A2 角色场景视角 + Bn 输出产品视角，为流程和工单类业务定义其活动节点与文档状态，为下游系统需求设计（wft03-biz）提供可展开的输入。
>
> **目标**：
> 1. **装配 A2 产品业务架构**——以 STR-F 场景职责边界为锚，将场景业务展开为 SL3~SL5 角色场景 BA（角色类型/角色职责/场景业务），定义功能表单的编排序列和全场景指标
> 2. **装配 Bn 产品业务架构**——将功能表单定位到 Bn 侧的 PL1~PL5 路径（业务域/E2E项目/构件类型/WBS/输出文档），展开每个输出文档的设计
> 3. **推断 Bn 构件输出文档**——检查 26 资产文档定义覆盖，按构件类型检查产品数据完备性，识别业务缺口
> 4. **基于 Bn 补充业务需求**——汇聚业务缺口，统一生成 FR-BIZ OR 材料并登记 OR 原料状态表
> 5. **为流程和工单类业务定义活动节点与文档状态**——对流程类和工单类输出文档，定义其节点活动序列（流转步骤/处理动作）和文档状态变迁（如草稿→已提交→审批中→已通过/已驳回）

> 以上为 wft02-biz 完成的设计任务的**逻辑罗列**，非严格执行顺序。实际操作中，A2 装配（1）和 Bn 装配（2）分别在 Step 3 和 Step 4 中执行，输出文档推断（3）在 Step 4 的文档定义检查和完备性检查中完成，业务需求增补（4）在 Step 5 统一处理，活动节点定义（5）分布在 Step 3（部件编排序列）和 Step 4（输出文档生命周期）中。推断功能表单由 wft03-biz 负责（91 规范 §5.3）。各 Skill 的 Step 布局由各自的设计逻辑决定，Step 数量可按需增减（91 规范 §5.1）。

本 Skill 负责：

1. **接收**——接收人类选中的 ≥1 个 STR-F 节点（`可以分解分配` / `待补充分解分配`），校验状态/类型/数量/跨域，不合格则说明原因并退出
2. **匹配**——按产品路径+用户角色匹配已有 BA 节点——同路径同角色归入同一 BA（Q1 直接归入/Q2 扩展归入），无已有则新建（Q3）
3. **收敛**——将本轮新 STR-F 的信息载体与 BA 已有内容逐项比对，判定关系并执行对应动作——确认则升级来源、补充则进入展开（全量重新生成，非增量追加）、矛盾则记录待人类裁决、无新增则仅加引用
4. **设计角色场景视角**（Step 3）——生成 SL3~SL5 角色场景 BA + 全场景指标 + 功能表单编排序列（流程/工单类在此定义其活动节点与文档状态）
5. **设计输出产品视角**（Step 4）——定位 PL1~PL5 路径，展开每个输出文档的设计、生命周期（流程/工单类在此定义其活动节点与文档状态）和数据权限，检查文档定义与产品数据完备性，识别业务缺口
6. **增补业务需求**（Step 5）——汇聚业务缺口，AI 推断补充 + `[推断-待OR确认]` 标注，统一生成 FR-BIZ OR 材料并登记 OR 原料状态表
7. **写回**（Step 6）——人类确认方案后统一写回 23/24/26/27 资产（Q1/Q2/Q3 均直接写入，不需二次确认）
8. **推进**——人类确认时 BA 节点推进为 `可以SR设计`（wft03 可即时进入）；资产落账后对应 STR-F 推进为 `已分解分配`
9. **回修**——接收 wft03 退回的 `需wft02修订` BA 节点，读取缺失说明，按人类改进方案修订 BA（走 Step 3~5 设计流程），人类确认后重新推进为 `可以SR设计`

本 Skill 不负责：

1. 引擎配置需求（STR-E，相关方需求-引擎类 → `wft02-eng-str2ba`）
2. 非功能需求（STR-NFR，非功能相关方需求 → `wft04a`）
3. A1（平台引擎产品）的 BA 设计（→ `wft02-eng-str2ba`）
4. 下游 wft03 的 SR 架构设计展开

### 1.2 与上下游的衔接

**上游 wft01-biz**：已在名称级完成输出文档的候选支撑引擎链判定（91 规范 §5.1），产出 STR-F 节点。本 Skill 消费 STR-F，展开为 BA 方案。

**下游 wft03-biz**：消费 BA 方案，推断功能表单并展开为 SR-F 架构（91 规范 §5.3）。wft03 在 SR-F 展开中发现 BA 方案缺陷时，将 BA 节点退回为 `需wft02修订`，本 Skill 按人类改进方案修订后重新交付。

---

## 二、BA 设计基础

> 本章定义 wft02-biz 操作的领域对象——业务架构（BA）、A2/Bn 两类产品及其末级节点、功能引擎与输出文档的关系、输出文档的活动节点。操作流程见 §三。层级体系的权威定义见 [91 规范 §2.1](91-eos-biz-eng-spec.md#21-业务及层级) 和 [25 资产 §1](../../80-pl4eos-2-eosdata/25-eos-engine-models.md)。

### 2.1 业务架构（BA）

#### 2.1.1 定义

BA 是五层设计链的第三层（OR→STR→**BA**→SR→PA），将 STR-F 的场景业务展开为可被系统需求（SR-F）承接的业务方案。BA 的权威定义见 [通用设计规范 §7.4](../../../00-generalspec/01-generalspec-sysdev.md)。

#### 2.1.2 双视角模型

EOS 的 BA 包含两个并存互补的视角：

- **A2 角色场景视角**（§2.2）：回答"谁在哪个菜单入口、按什么顺序使用哪些功能表单、完成端到端的职责闭环"
- **Bn 输出产品视角**（§2.3）：回答"某类输出文档属于哪个业务域/构件类型、需要哪些前序文档和业务规则才能生成"

两个视角互相校验完备性：A2 侧覆盖场景已提到的业务，Bn 侧完备性检查暴露输出文档缺失。A2/Bn 的定义见 [91 规范 §3.2~§3.3](91-eos-biz-eng-spec.md#32-a2产品)。

#### 2.1.3 A2与Bn的交汇点

两个视角通过**功能表单**交汇——功能表单在 SL 侧为 SL6（功能表单/主子表单）、在 PL 侧为 PL5（业务输出文档），是双视角的连接点。双视角映射即在此完成。

对于流程类和工单类输出文档，BA 还需定义其**活动节点与文档状态**（详见 §2.5）。

### 2.2 A2产品：角色场景视角

#### 2.2.1 定义

A2 是**各业务域的角色场景**。定义见 [91 规范 §3.2](91-eos-biz-eng-spec.md#32-a2产品)。构件为角色（如采购员、库存管理员），末级节点为场景业务（SL5）。

#### 2.2.2 SL层级

沿角色场景视角（SL）展开：

| 层级 | 名称 | 说明 |
|------|------|------|
| SL0 | 平台 | EOS 平台 |
| SL1 | 业务域 | 如采购管理、生产管理 |
| SL2 | A2产品 | 角色场景产品 |
| SL3 | 角色类型 | 如采购员、库存管理员 |
| SL4 | 角色职责 | 归纳层，可选占级 |
| SL5 | 场景业务 | **末级节点**，与 STR-F 1:1 映射 |
| SL6 | 功能表单/主子表单 | = PL5 输出文档，双视角交汇点 |

**场景业务（SL5）**：STR-F 末级节点，定义端到端职责闭环——用户角色、场景入口条件、可用功能表单集、跨部件上下文传递和全场景指标。由菜单引擎（`@engine-menu`）的末级节点编排支撑。分为执行类（D）和管理类（PCA），分类定义见 [91 规范 §1.3](91-eos-biz-eng-spec.md#13-业务场景)。

#### 2.2.3 本Skill在A2侧的职责

**装配 A2 产品业务架构**——将 STR-F 的场景业务展开为 SL3~SL5 角色场景 BA（角色类型/角色职责/场景业务）和全场景指标，定义功能表单的编排序列。其中流程/工单类功能表单引用 Step 4 定义的活动节点来决定出现顺序和触发关系。

### 2.3 Bn产品：输出产品视角

#### 2.3.1 定义

Bn 是**各业务域的输出产品**。定义见 [91 规范 §3.3](91-eos-biz-eng-spec.md#33-bn产品)。构件为产品构件（如单据、流程、工单），末级节点为构件的产品数据——即**输出文档**（PL5）。这也是 wft02-biz 的末级节点（[91 规范 §5.2](91-eos-biz-eng-spec.md#52-业务架构开发wft02-biz)）。

#### 2.3.2 PL层级

沿产出视角（PL）展开：

| 层级 | 名称 | 说明 |
|------|------|------|
| PL0 | 平台 | EOS 平台 |
| PL1 | 业务域 | 如采购管理、生产管理 |
| PL2 | E2E 项目业务 | 如采购申请管理 |
| PL3 | 构件类型 | 如单据、流程、工单、指标、报表 |
| PL4 | WBS | 归纳层，可选占级 |
| PL5 | 业务输出文档 | **末级节点**，= SL6 功能表单 |

#### 2.3.3 输出文档分类

输出文档按生成方式分为两类（[91 规范 §2.1](91-eos-biz-eng-spec.md#21-业务及层级)）：

- **内置 CAX**：内容由 EOS 功能引擎直接生成，沿三层引擎生成链逐级生成——物理表（实体引擎）→功能表单→主子表单
- **外置 CAX**：由平台外部工具生成，以文件名及附件形式回传至 EOS 平台，由计划引擎管理任务/状态/版本/附件/评审/归档

每个输出文档在引擎层对应一个**主子表单**——由功能引擎将一组功能表单按主子业务关系（1+N，N≥0）组装而成。wft01-biz 在名称级识别输出文档及其候选支撑引擎链，wft02-biz 在 BA 模式级展开输出文档设计。

#### 2.3.4 本Skill在Bn侧的职责

**装配 Bn 产品业务架构** + **推断 Bn 构件输出文档**——定位输出文档的 PL1~PL5 路径，展开每个输出文档的设计，补充 Bn 产品文档并检查产品数据完备性，定义输出文档的权限。其中流程/工单类输出文档还需定义其生命周期（即活动节点序列与文档状态变迁，详见 §2.5）。

### 2.4 功能引擎与输出文档的关系

#### 2.4.1 三层引擎生成链

EOS 平台通过三层引擎（EL1~EL3）逐级生成业务（[91 规范 §2.2~§2.3](91-eos-biz-eng-spec.md#22-引擎及层级)）。权威资产定义见 [25 资产 §1.1](../../80-pl4eos-2-eosdata/25-eos-engine-models.md#11-引擎全景)。

```
实体引擎（EL3）──生成──→ 物理表
                           ↓ 被消费
功能引擎（EL2）  ──生成──→ 功能表单 / 主子表单
                           ↓ 被消费
菜单引擎（EL1）  ──生成──→ 菜单树 + 场景业务
```

各引擎为生成器：下级引擎生成制品，上级引擎消费该制品后生成自身制品，逐级递进。

**功能引擎**是 EL2 层级，包含 10 个具体引擎（产品数据类：需求/流程/工单；业务管理类：项目/WBS/计划/看板/指标/进展/定时任务），各引擎将物理表转换为功能表单并按主子关系组装。

#### 2.4.2 输出文档推导路径

自顶向下推导（[91 规范 §2.3](91-eos-biz-eng-spec.md#23-业务组装)）：

- **内置 CAX**：输出文档条目化内容 → 实体引擎（`@engine-entity`）生成物理表 → 功能引擎转换为功能表单 → 按主子关系（1+N）组装为主子表单 → 菜单引擎（`@engine-menu`）编排为场景业务
- **外置 CAX**：输出文档 → 计划引擎（`@engine-task`）管理任务/状态/附件/评审/归档，附件能力由实体引擎提供，N=0

wft01-biz 已在名称级判定每个输出文档的候选支撑引擎链。wft02-biz 在 BA 模式级展开输出文档设计。引擎支撑的详细判定由 wft03-biz 在 SR-F 展开时执行（91 规范 §5.3）。

### 2.5 输出文档的活动节点

#### 2.5.1 定义与适用范围

对于流程类和工单类输出文档，BA 还需定义其**活动节点与文档状态**——即该文档从创建到完结的完整状态变迁路径。非流程/非工单类输出文档（如物料主数据、指标看板）无状态机，无需此项。

#### 2.5.2 活动节点结构

**活动节点** = 一个处理步骤，包含：
- **处理动作**：谁在这一步做什么（如提交申请、审批通过、驳回重审）
- **文档状态**：该步骤执行后的文档所处状态（如草稿、已提交、审批中、已通过、已驳回）

**活动节点序列** = 文档的状态变迁路径，即该输出文档的生命周期。例如采购申请单：

```
创建申请 ──→ 提交 ──→ 审批 ──→ 通过
  │                    │           │
  └─ [草稿]            └─ [审批中]   └─ [已通过]
                                   │
                        驳回 ────→ ┘
                          │
                          └─ [已驳回] → 重新提交 → ...
```

活动节点在 **Step 4（Bn 侧）**定义——作为输出文档的生命周期（状态变迁路径）。Step 3（A2 侧）的编排序列引用这些状态来决定功能表单的出现顺序和触发关系，不重复定义活动节点本身。

wft03-biz 消费活动节点与文档状态，为每个节点推断其功能表单结构——包括字段可编辑性（如草稿态全部可编辑、审批态仅审批意见可编辑）、按钮可见性（如提交按钮仅在草稿态可见、审批按钮仅在审批态可见）和校验规则。

### 2.6 BA 节点生命周期

#### 2.6.1 推进态（按顺序）

| 状态 | 含义 | 触发条件 |
|------|------|---------|
| `待确认方案` | 方案已生成或修订，等待人类反馈 | Step 3~5 方案与缺口出口生成完成后 |
| `可以SR设计` | 方案已确认，wft03 可启动 SR 架构设计 | 全部条目 `[同意]` + `[已处理]` |
| `在SR设计` | wft03 已锁定此 BA，正在展开 SR-F | wft03 写入活动锁 |
| `已SR设计` | SR-F 展开完成，BA 消费版本与当前版本一致 | wft03 处理完成且未检测到上游新版本 |

人类确认方案后，BA 节点推进为 `可以SR设计`，此时 wft03 即可启动 SR-F 展开——wft03 直接从 BA 节点（`04-*.md`）读取设计内容，不依赖 23/24/26/27 资产文件的写回结果。资产写回（Step 6）与 SR-F 展开（wft03）并行推进、互不阻塞。

BA 节点状态分工：wft02-biz 写入 `待确认方案` / `可以SR设计` / `待补充SR设计` / `需wft02修订`，并在修订时更新版本号；wft03 写入 `在SR设计` / `已SR设计`。

#### 2.6.2 上游变更态（STR-F 汇入触发 BA 修订）

当新 STR-F 汇入已有 BA 触发修订时，BA 内容变更、版本号递增——但下游 wft03 可能已基于旧版本生成了 SR-F。修订后 BA 状态按 wft03 的消费进度分流：

| 修订前状态 | 修订后 | 说明 |
|-----------|--------|------|
| `可以SR设计` | 保持，版本递增 | wft03 尚未消费，不受影响 |
| `在SR设计` | 保持，版本递增 | wft03 正在处理中，重入时检测版本差异，按变更影响声明增量处理 |
| `已SR设计` | 降为 **`待补充SR设计`** | wft03 之前生成的 SR-F 基于旧版本 BA，需 wft03 重新介入补全 |

- **`待补充SR设计`**：wft03 之前已消费的 SR-F 节点不动（它们仍然有效），但 BA 新增了业务或功能表单需要生成新的 SR-F。wft03 进入后按 BA 的变更影响声明确定增量映射范围，处理完成后若消费版本追上 BA 当前版本 → 推进回 `已SR设计`

#### 2.6.3 下游退回态（wft03 发现缺陷退回修订）

| 状态 | 含义 | 触发条件 |
|------|------|---------|
| **`需wft02修订`** | BA 方案存在缺陷，需本 Skill 修订后重新交付 | wft03 写入，附带缺失说明 |

本 Skill 重入 `需wft02修订` 的 BA 时：读取 wft03 的缺失说明 → 按人类改进方案修订 BA（走 Step 3 修订流程）→ 人类确认 → BA → `可以SR设计`

#### 2.6.4 版本标记与消费追溯

- BA 节点初次生成时版本 = `v1`，变更影响声明写"首版，无变更"
- 每次因新 STR-F 触发 Step 3 修订，版本号递增
- 版本号写入 BA 节点头部（与节点 ID 同级）
- wft03 首次消费 BA 时，在对应 SR-F 节点中记录 `BA 消费版本：v{N}`
- wft03 重入同一 BA 时比对版本：若 BA 当前版本 > 已消费版本 → 触发变更感知

### 2.7 BA 变更传播协议

下游 wft03 消费 BA 后生成 SR-F 节点。当 BA 因上游 STR-F 修订而变更时，已生成的 SR-F 节点面临同步问题。本协议定义 BA → SR-F 的变更分类、通知格式和消费追溯。变更传播协议的通用定义（变更类型分类、判定优先级、版本标记）见 [91 规范 §7.7](91-eos-biz-eng-spec.md#77-变更传播协议)。

#### 2.7.1 变更类型分类

| 变更类型 | 判定标准 | 对下游影响 |
|---------|---------|----------|
| **增量型** | 仅新增业务/场景要素，已有业务/产品路径/功能表单结构均未改动 | 已有 SR-F 节点不受影响，仅需映射新业务 |
| **修订型** | 已有业务/场景要素被修改；产品路径与功能表单结构未变 | 对应 SR-F 节点需局部调整 |
| **结构型** | 产品路径需新增 SL2/PL2 节点或跨业务域重组、菜单新增/合并/拆分、BA 结构需重塑 | SR-F 结构可能重塑，需人类裁决后方可处理 |

> 注：Q2 在已有路径下扩展子节点属「修订型」，不触发「结构型」。判定优先级：结构型 > 修订型 > 增量型。

#### 2.7.2 变更影响声明格式

每次修订已有 BA 后，在 BA 节点末尾写入（`04-*.md` 中 BA 节点块内）：

```text
### 变更影响声明

BA 版本：v{N}（本次修订前为 v{N-1}）
本轮变更类型：增量型 / 修订型 / 结构型

变更项：
- [新增] / [变更] / [确认] / [需裁决] ...

下游已消费此 BA 的 SR-F 节点：
  <SR-F节点ID列表>（如无则写"尚未被下游消费"）

对已消费 SR-F 节点的影响：
  受影响节点：<SR-F节点ID + 受影响原因>
  不受影响节点：<SR-F节点ID>
  待新增 SR-F 映射：<新业务/新功能表单/权限需求对应的待映射说明>

建议 wft03 动作：
  - 增量型：增量映射新增 PL5/权限需求即可，已有 SR-F 节点不动
  - 修订型：修订受影响的 SR-F 节点 → 标记 [来自上游变更]
  - 结构型：输出影响范围，等人类裁决后再处理
```

#### 2.7.3 增量清单与变更影响声明的关系

| 维度 | 增量清单（§四） | 变更影响声明 |
|------|----------------|------------|
| 面向读者 | 人类 | 下游 Skill（wft03） |
| 目的 | 告知"本轮我做了什么，请逐项审阅" | 告知"BA 变了什么，你需要做什么" |
| 写入位置 | BA 节点末尾（供人类参照 `确认状态` 字段逐条审阅） | BA 节点末尾（独立小节） |
| 生成时机 | 每次 Step 3 产出方案时 | 仅修订已有 BA 时；新建 BA 写"首版，无变更" |

### 2.8 BA 设计规则

> Step 3（角色场景视角）与 Step 4（输出产品视角）共用的设计规则。领域概念（双视角模型、层级体系等）见 §2.1~§2.5。

**BA 节点完整要素**：

- 角色场景视角（A2产品）BA 方案——SL3~SL5（角色类型/角色职责/场景业务，SL6 功能表单由 Bn 侧 PL5 覆盖）+ 全场景指标
- 输出产品视角（Bn产品）BA 方案——PL3~PL5（构件类型/PL4 WBS/PL5 业务输出文档）+ 输出文档路径 + 数据权限 + 文档补充与完备性检查
- 资产对齐判定（23/24/26/27 Q1/Q2/Q3）
- 变更影响声明（变更类型 + 下游影响 + 建议动作，Step 4 判定变更类型 + Step 5 统一写入）
- BA 版本号（初始 v1，每次上游 STR-F 修订递增）

**资产对齐范围**：

| 资产 | 内容 | Q1/Q2/Q3 含义 |
|------|------|--------------|
| **23** 产品架构 | A2产品/Bn产品产品路径（含 A2 菜单可达性路径）。A2产品侧对齐记为 `23A`，Bn产品侧对齐记为 `23B` | Q1=直接引用已有路径 / Q2=扩展已有 / Q3=新建 |
| **24** 业务资产 | SL4 角色职责、SL5 场景业务、全场景指标 | Q1=已有资产复用 / Q2=扩展已有业务 / Q3=新建业务块 |
| **26** 文档定义 | 输出文档对应功能表单的定义与归属 | Q1=同名同义文档已存在 / Q2=相似文档需扩展 / Q3=无匹配需新建 |
| **27** 资源资产 | 支持信息中的 CAX 工具/AI 工具/模板/专业能力/作业指南 | 已有资源追加引用；缺失资源形成建议 |

#### 2.8.1 Q1/Q2/Q3 对齐判定

对每个设计对象，按资产对齐范围执行对齐判定。Q1/Q2/Q3 描述当前设计对象与已有资产的匹配程度——**Q1（复用）**：已有资产完全匹配，直接引用；**Q2（扩展）**：已有资产部分匹配，需扩展边界；**Q3（新建）**：无匹配资产，需新建。

| 资产 | Q1 复用 | Q2 扩展 | Q3 新建 |
|------|---------|---------|---------|
| **23** 产品架构 | A2/Bn 产品路径存在且职责边界一致，直接引用已有路径 | 路径存在但需扩展构件类型、子节点或菜单可达性边界，引用路径并写扩展建议 | 路径不存在，写新建路径建议（名称/归属/边界） |
| **24** 业务资产 | 已有同名/同义业务块且职责范围一致，直接引用 | 存在相似业务块但职责或全场景指标需扩展，引用并写扩展建议 | 无匹配业务块，写新建业务块建议（名称/职责/归属路径） |
| **26** 文档定义 | 已有同名/同义文档定义且业务域、构件类型一致，直接引用 | 存在相似文档定义但名称、归属或范围需扩展，引用并写扩展建议 | 无匹配文档定义，写新建文档定义建议（名称/职责/归属业务域） |

**27 资源资产对齐判定**：27 不使用 Q1/Q2/Q3。已有资源直接追加 BA 引用（去重）；缺失资源形成资源资产建议（名称/类型/用途，建议状态=`待确认`）。

#### 2.8.2 设计心法链

Step 3~4 共享以下推理链：

```
读上游输入（STR-F 节点 + 输出文档清单 + 支持信息 / 交互信息）
  → 查 25 引擎能力（确认目标引擎能力边界）
  → 展开 SL5 场景业务设计 / PL5 输出文档设计
  → （引擎能力缺口由 wft03-biz 在 SR-F 展开时处理）
  → 按资产对齐范围执行 Q1/Q2/Q3
```

#### 2.8.3 SL4/PL4 归纳分组

SL4 角色职责从 SL5 场景业务的职责边界归纳分组，PL4 WBS 从 PL5 输出文档的业务模块归属归纳分组；命名取共性特征。角色仅一个 SL5 时 SL4 不独立占级（菜单深度降为 3 级），PL4 同理。已有 SL4/PL4 可扩展边界容纳新 L5 节点（Q2），无法归入时新建（Q3）。

#### 2.8.4 覆盖自查清单

Step 5 末尾使用，机械检查，逐项打勾，发现遗漏当场补齐：

1. SL3~SL5 角色场景 BA 方案（含全场景指标）
2. PL3~PL5 产出产品 BA 方案（含输出文档路径 / 数据权限 / 文档补充 / 完备性检查）
3. Step 4 执行完整性——逐 STR-F 功能表单核对 Step 4 已生成 PL3~PL5 路径和输出文档设计（无遗漏）
4. 23 对齐判定（A2/Bn 产品路径 / 菜单可达性路径）
5. 24 对齐判定（角色职责 / 场景业务 / 全场景指标）
6. 26 对齐判定（文档定义）
7. 27 对齐判定（资源资产引用）
8. 变更影响声明
9. BA 版本号

---

## 三、操作流程

> 本章定义 wft02-biz 的完整操作流程。领域概念定义见 §二。

### Step 布局总览

| Step | 名称 | 产出 | 写资产 |
|------|------|------|--------|
| Start | 前置校验与路由 | 就绪 / 结束 | — |
| 1 | 设计材料加载 | STR-F / BA / 资产全景 | — |
| 2 | 方案反馈处理 | 调整后的 BA 方案 | `02/04` |
| 3 | 角色场景视角 BA 设计 | SL3~SL5 角色场景 BA 方案 + 全场景指标 + 部件编排序列 + 23/24 对齐判定 | `02/04` |
| 4 | 输出产品视角 BA 设计 | PL3~PL5 产出产品 BA 方案 + 文档补充与完备性检查 + 输出文档设计 + 数据权限 + 23/24/26/27 对齐判定 + 业务缺口识别 | `02/04` |
| 5 | 业务需求增补 | 业务缺口 → BA 缺口说明 + FR-BIZ OR 材料 + OR 原料状态表登记 | `04` |
| 6 | 资产写回与落账 | 23/24/26/27 引用写回 + STR-F→已分解分配 + 资产写回记录 | `23/24/26/27/02/04` |
| Step End | 运行结束输出 | 增量清单 + 人类下一步操作提示 | — |

---

### 3.1 Step Start

运行时 AI 按以下顺序执行：

1. **输入校验**：
   - 无 STR-F 选中 + 无待处理反馈的 BA 节点 → 输出"无待处理对象"→ **退出**
   - STR-F 节点状态不是 `可以分解分配` / `待补充分解分配` → 输出状态分布 → **退出**
   - 选中节点含 STR-E / STR-NFR 类型 → 输出类型分布（STR-E → `wft02-eng`，STR-NFR → `wft04-nfr-str2sr`）→ **退出**
   - 选中的 STR-F 分属多个业务域且无已确认关联 → 输出业务域分布，提示可能存在跨域设计 → 人类确认后继续或分批
   - 选中 STR-F >5 个 → 输出"本轮 N 个 STR-F，建议分批"，提示节点 ID 列表 → 人类确认后继续，不强制退出
2. **退回优先**：存在 `需wft02修订` 的 BA 节点 → 读取 wft03 的缺失说明，按人类改进方案修订 BA，人类确认后推进为 `可以SR设计`
3. **反馈处理**：存在 `待确认方案` + 有人类标注且无 `[已处理]` 的 BA 节点 → 进入 Step 2 处理。若全部条目已 `[同意]`+`[已处理]`，不阻塞新 STR-F，Step End 时提示
4. **新 STR-F 路由**：有未处理的 STR-F 节点 → 进入 Step 3，按产品路径+用户角色匹配已有 BA（Q1 归入/Q2 扩展归入/Q3 新建），逐节点展开设计

**顺序约束**：Step 2 必须先于 Step 3，因为反馈可能改变 BA 节点的产品路径、业务清单或权限结论。Step 3 必须基于 Step 2 修改后的最新 BA 节点内容，不得使用 Step 1 加载时的缓存旧状态。

---

### 3.2 Step 1 · 设计材料加载

每次运行加载以下资产：

1. `02-*.md` STR-F 树画像 / 状态队列 / 节点索引 — 定位本轮 STR-F 正文块（含输出文档清单、支撑引擎链、Bn 对齐方案）
2. `04-*.md` BA 树画像 / 状态队列 / 节点索引 — 定位本轮 BA 节点块，匹配已有 BA
3. `23-eos-output-architecture.md` 资产全貌层 — A2/Bn 路径对齐锚点
4. `24-eos-business-assets.md` 资产全貌层 — 可复用业务资产匹配
5. `26-eos-document-definitions.md` 资产全貌层 — 文档定义查重与引用
6. `25-eos-engine-models.md` §1.1 — 引擎清单（运行时动态读取，不缓存），仅用于业务支撑链校验

读取约定见 [文档编写约定 §十四](../../../../00-generalspec/00-doc-conventions.md)。写回约定见 [文档编写约定 §十五](../../../../00-generalspec/00-doc-conventions.md)。

按需扩大：
- 候选 BA 块 — 按同业务域/同角色/同产品类型筛选，用于 Q1/Q2 归入匹配
- 候选 24 业务资产 — 按功能表单名称/输出文档性质/主实体匹配，用于检查可复用定义
- 候选 26 文档定义 — 按功能表单的输出文档名称/构件类型匹配，用于文档定义查重
- 反馈重入 — 仅读取条目的确认状态字段及反馈指向块，不做全量重读
- 扩大触发：索引缺失 / 候选冲突 / 资产全貌不足以判断边界 / 追溯关系断裂时

---

### 3.3 Step 2 · 方案反馈处理

人类审阅与 AI 处理的通用协议见 [91 规范 §7.5](91-eos-biz-eng-spec.md#75-skill-反馈交互协议)。以下为本 Skill 的特化规则。

**本 Skill 的条目**：BA 节点的 A2 方案 / Bn 方案 / 输出文档设计 / 数据权限 / 资产对齐结论。各条目的 `确认状态` 字段位于 `04-*.md` BA 节点块各条目正文中。

#### 3.3.1 AI 处理流程

本轮存在 `待确认方案` 的 BA 节点时：

1. **扫描**：遍历各条目的 `确认状态` 字段，筛选有人类标注（`[同意]`/`[修改]`/`[驳回]`）但无 `[已处理]` 的条目。已有 `[同意]`+`[已处理]` 的默认跳过——除非本轮新 STR-F 修订触及其定义/归属，此时先清除 `[已处理]`（保留 `[同意]`），使其回到待处理状态
2. **收集整体确认**：人类说「整体确认」→ 对无条目级标注的剩余条目写入 `[同意]` + `[已处理]`
3. **逐条执行**：
   - `[同意]` → 不变，追加 `[已处理]`
   - `[修改]` → 按反馈调整该条目，追加 `[已处理]`
   - `[驳回]` → 移除被驳回对象及关联对齐结论，追加 `[已处理]`
4. **级联处理**：局部修改触发级联时（产品路径/业务拆分合并/构件类型归属变更），完成后重新执行受影响对象的对齐检查
5. **判定状态**：全部条目含 `[同意]`+`[已处理]` → BA → `可以SR设计` → 进入 Step 6 写回；任一条目含 `[修改]`/`[驳回]` → 保持 `待确认方案`
6. **写回时机**：仅有反馈处理（无新 STR-F）→ 直接进入 Step 6 写回资产；同时有新 STR-F → 待 Step 3~5 完成后统一写回
7. **落记录**：写入 `### 反馈处理记录`（含本轮各条目人类意见+来源+处理动作+时间戳）

#### 3.3.2 确认状态格式示例

**AI 写入（Step 3~5 完成时）** —— 为每个条目初始化 `确认状态` 字段，初始为空：

```text
- A2-场景业务-采购员执行采购申请 | 确认状态: (空)
- 采购申请单（输出文档，@engine-flow + @engine-entity） [23B-Q2] | 确认状态: (空)
- 采购申请合格率（全场景指标） | 确认状态: (空)
```

**人类在条目 `确认状态` 字段标注** —— 三种标注形式：

```text
- A2-场景业务-采购员执行采购申请 | 确认状态: [同意]
- 采购申请单（输出文档，@engine-flow + @engine-entity） [23B-Q2] | 确认状态: [修改]：引擎应改为 @engine-flow + @engine-entity
- 采购申请合格率（全场景指标） | 确认状态: [驳回]：该指标已有 BA-YYY 覆盖，不重复
```

**AI 处理完成后** —— 在人类标注末尾追加 `[已处理]`：

```text
- A2-场景业务-采购员执行采购申请 | 确认状态: [同意] [已处理]
- 采购申请单（输出文档，@engine-flow + @engine-entity） [23B-Q2] | 确认状态: [修改]：引擎应改为 @engine-flow + @engine-entity [已处理]
- 采购申请合格率（全场景指标） | 确认状态: [驳回]：该指标已有 BA-YYY 覆盖，不重复 [已处理]
```

人类改写 `确认状态` 即覆盖旧内容，`[已处理]` 自然消失，条目回到待处理状态。完整历史追溯通过 BA 版本号 + 变更记录实现。

---



### 3.4 Step 3 · 角色场景视角 BA 设计

**设计线第一步**。以 STR-F 场景职责边界为锚，生成 SL5 场景业务 IPO 和 输出文档编排序列，归纳 SL4 角色职责和 SL3 角色类型归属，产出全场景指标，执行 23/24 对齐判定。Step 3 产出的部件编排序列是 Step 4 展开 PL5 IPO 的 Input 来源。

#### 5.4.1 设计过程

从 STR-F 末级场景业务出发，落成角色场景视角 BA。STR-F 已经说明"某角色的端到端职责闭环"，因此 A2产品是 BA 设计的第一主线，而不是输出产品视角设计之后的组装结果。

沿 §3.4.1·3 设计心法链第①③⑥步执行——读上游输入（STR-F 节点与输出文档清单）→ 生成 SL5 场景业务 IPO → 执行 Q1/Q2/Q3 判定。

**1. 确定场景路径**

| 方案项 | 设计内容 |
|--------|----------|
| 用户角色 | STR-F 中的用户角色，如采购员、库存管理员 |
| 场景职责边界 | STR-F 已确认的端到端职责闭环 |
| A2产品业务运行系统路径 | 业务域 / 角色工作台 / 场景组 / SL5 场景业务 |
| SL5 场景业务 | 与 STR-F 末级节点 1:1 映射的场景业务 |
| 场景入口条件 | 角色进入该场景的触发条件、前置上下文、可见性约束 |
| 可用 输出文档集 | 来自 STR-F 输出文档清单，每个 PL5 对应一个输出文档或业务指标 |

**2. 归纳 SL4 角色职责**

从 SL5 场景业务的职责边界归纳 SL4 角色职责。归纳规则见 §3.4.1·4——取 SL5 职责的共性特征命名，角色仅一个 SL5 时 SL4 不独立占级。已有 SL4 可扩展边界容纳新 SL5（Q2），无法归入时新建（Q3）。

**3. 生成场景业务 IPO**

展开 SL5 场景业务设计。Process 列分两层呈现：先以文字描述@engine-menu 的末级节点编排编排的通用能力（TAB/窗口顺序、默认进入部件、跨部件上下文传递、状态衔接、异常路径），再以**部件编排序列**展开本次场景下 输出文档的实际编排顺序与触发关系（格式见 §3.5.2）。

**4. 生成全场景指标**

全场景指标 = 输出文档指标聚合 + 场景特有指标。若 wft01-biz 已给出全场景指标产出物，则作为 PL5 指标功能表单承接；若仅有全场景度量语义，则在 SL5 场景业务下生成全场景指标业务。

**5. 确定资产归属**

按 §3.5 资产对齐范围 + §3.4.1·2 Q1/Q2/Q3 判定细则（23 产品架构 / 24 业务资产），对 Step 3 产出执行 Q1/Q2/Q3 判定并标注对齐结论。

**6. BA 节点写入结构**

#### 5.4.2 角色场景视角（A2产品）BA 方案

**SL5 场景业务 IPO**：按 §二 IPO 模型 SL5 行格式写入。本步特有：Process 列附加下方部件编排序列。

**部件编排序列**：

```text
SL5 {场景业务}
  → PL5 {功能表单A}（输出文档或业务指标）
      → 触发动作 / 状态事件 / 外部调用 / 消息约束
  → PL5 {功能表单B}（输出文档或业务指标）
      → 触发动作 / 状态事件 / 外部调用 / 消息约束
```

**全场景指标**：

| 指标 | 来源 | 24-Q |
|------|------|------|
| {指标名称} | 输出文档指标聚合 / 场景特有 | {判定} |

> 23A-Q 判定写入资产对齐字段（如 `资产对齐：23A=Q1`），24-Q 判定在全场景指标表内以 `24=Qx` 形式标注。SL3 角色类型、SL4 角色职责为路径定位信息，从 STR-F 直接映射，不重复写入。

**向 Step 4 传递**：Step 3 产出的每个 SL5 场景业务的部件编排序列（含部件顺序和上下文传递点）是 Step 4 展开 PL5 IPO 的 Input 来源。传递内容：SL5 场景业务 ID → 部件编排序列（输出文档列表，按顺序，标注触发动作/状态事件/外部调用/消息约束）。

---

### 3.5 Step 4 · 输出产品视角 BA 设计

**设计线第二步**。从功能表单的 Bn 侧归属出发，以 Step 3 传递的部件编排序列为参考，展开每个 PL5 输出文档的设计，补充 Bn 产品文档并检查产品数据完备性，识别业务缺口（不在此步生成 OR 材料——交由 Step 5 统一处理），执行 23/24/26/27 对齐判定。PL1~PL3 由 STR-F 23B 判定提供，Step 4 聚焦 PL4 WBS 归纳、产品文档补充和输出文档设计——使 Bn 产品架构的构件类型下具备完整的业务输出文档定义。

#### 3.10.1 设计过程

沿 §3.4.1 设计心法链执行——读上游输入（Step 3 部件编排序列 + STR-F 输出文档清单含 23B 判定）→ 查 25 引擎能力 → 展开输出文档设计 → 执行 Q1/Q2/Q3 判定。

**1. 确定输出文档路径**

| 方案项 | 设计内容 |
|--------|----------|
| 业务域 | 从 STR-F `业务域`字段读取 |
| E2E 项目业务 | 从 STR-F 功能表单 23B 判定读取 |
| 构件类型 | 从 STR-F 功能表单 23B 判定读取 |

> **上游信息源说明**：PL2（E2E 项目业务）和 PL3（构件类型）优先从 STR-F 23B 判定读取。若 wft01-biz 尚未产出 Bn 侧 23-Q 判定（当前 STR-F 仅携带 A2 侧产品类型/构件类型），AI 基于功能表单的输出文档性质、支撑引擎类型和 23 资产 Bn 构件类型清单推断 PL2/PL3 归属，并标注 `[推断]`。Bn 侧 23-Q 的正式判定在 wft01-biz 补齐后回填确认。

**2. 归纳 PL4 WBS**

从功能表单的输出文档名称/用途/支撑引擎归纳 PL4 WBS。归纳规则见 §3.4.1·4——取共性特征命名，已有 WBS 可扩展边界容纳新功能表单（Q2），无法归入时新建（Q3）。

**3. 补充 Bn 产品文档**

以 STR-F 23B 判定的构件类型为锚点，分两步检查：

**3a. 逐功能表单检查文档定义**：对每个功能表单检查 26 资产中是否已有对应的文档定义。有则引用（26-Q1），需扩展则标注（26-Q2），无则形成新建建议（26-Q3）。输出格式见 §3.5.2 文档定义检查表。

**3b. 按构件类型检查产品数据完备性**：以 23 构件类型行引用的 26 文档定义清单为基线，聚合该构件类型的应有产品数据，再由 AI 基于工程经验/同类构件模式补全候选清单，与 STR-F 功能表单输出文档清单比对。

应有产品数据清单的来源：

| 清单来源 | 含义 | 使用要求 |
|---------|------|---------|
| 资产已定义 | 26 中已有与该构件类型关联的文档定义（23 构件类型行引用 @doc-* ID） | 作为正式覆盖检查依据 |
| AI 经验推断 | AI 基于工程经验、行业知识、同类构件模式推断的应有产品数据 | 写入 BA 节点缺口说明，标注 `[推断-待OR确认]`（置信度默认≤中）；不进入步骤 4 PL5 IPO；由 Step 5 统一生成 OR 材料，后续经 OR→STR-F→BA 管道确认后再进入资产写回 |
| 人类指定补充 | 人类在反馈中明确指出的 | 同 AI 经验推断——写入 BA 节点缺口说明，标注 `[推断-待OR确认]`；不进入步骤 4 PL5 IPO；由 Step 5 生成 OR 材料 |
| STR-F 输出文档清单 | wft01-biz 已确认的功能表单 | 正式产品数据，进入步骤 4 展开输出文档设计，无特殊标注 |

输出格式见 §3.5.2 产品数据完备性检查表。

	**4. 展开输出文档设计**

对每个输出文档，按 I-P-O 三段描述其设计：
- **Input**：前序输出文档 + 支持信息（规则知识/风险合规/资源工具匹配 27 资产）
- **Process**：根据支撑引擎和开发方式的处理模式——内置 CAX=引擎界面/校验/行为，外置 CAX=规则/知识/资源支撑/附件回传。交互信息吸收到 Process 中（触发动作/状态变化/外部调用/消息约束），不生成独立业务
- **Output**：输出文档 + 生命周期

**生命周期**分两种情况：
- **流程/工单类输出文档**：按 §2.5 定义活动节点序列——逐节点标注处理动作和文档状态，画出状态变迁路径（如草稿→已提交→审批中→已通过/已驳回）。活动节点序列即该文档的生命周期

  **活动节点推断依据**：
  1. OR 语义——原始需求中明示或隐含的处理动作（"提交""审批""驳回"）和状态词（"草稿""已提交""审批中"）
  2. 支撑引擎的能力边界——wft01-biz 已判定该文档的支撑引擎（如 `@engine-flow`），引擎的能力类型决定了可用的状态模板
  3. 同类文档模式——同业务域/同引擎类型的已有输出文档的活动节点模式
  4. AI 经验推断——基于工程经验和行业知识的合理补全，标注 `[推断]` 供人类确认

- **非流程/非工单类输出文档**：仅写简单生命周期（如"创建→使用中→归档"），无需活动节点

输出格式见 §3.5.2 输出文档设计。
**5. 确定资产归属**

按 §3.5 资产对齐范围 + §3.4.1 Q1/Q2/Q3 判定细则（23 产品路径 / 24 业务资产 / 26 文档定义），27 资源资产按 §3.4.1 二值判定（已有→引用，缺失→建议），对本步产出执行对齐判定并标注结论。

**6. 推导数据权限**

对每个 PL5 输出文档，基于 STR-F 的 `用户角色` 和角色在业务链中的位置，按四维度推导数据权限——字段可见性、字段可编辑性、行级数据范围、按钮操作权限。完整推导规则与维度定义见 §3.5.3。

#### 3.10.2 输出格式

BA 节点写入结构：

**文档定义检查表**：

| 功能表单 | 输出文档 | 26 匹配 | 判定 | 建议动作 |
|---------|---------|---------|------|---------|
| {功能表单} | {输出文档} | {已有文档定义块或"无匹配"} | 26-Q1/Q2/Q3 | {引用/扩展建议/新建建议} |

**产品数据完备性检查表**：

构件类型应有产品数据清单：

| 构件类型 | 应有产品数据 | 来源 | 置信度 |
|---------|------------|------|-------|
| {构件类型} | {产品数据清单} | 资产已定义 / AI推断 / 人类补充 | 高/中/低 |

覆盖结论：

| 已覆盖（来自 STR-F） | 缺失（补充产品数据） | 缺口处理 |
|----------------------|------|---------|
| {功能表单列表} | {缺口清单} | 逐缺口标注 `[推断-待OR确认]`，由 Step 5 汇聚写入独立缺口说明段并生成 OR 材料 |

**输出文档设计**：按 I-P-O 三段描述每个 PL5 输出文档——Input（前序输出文档 + 支持信息）、Process（根据支撑引擎和开发方式的处理模式，附加交互信息：触发动作/状态变化/外部调用/消息约束）、Output（输出文档），表外附加生命周期行。生命周期：{状态流转路径，如 草稿 → 已提交 → 审批中 → 已通过 / 已驳回}

#### 3.10.3 数据权限

对每个 输出文档，基于 STR-F 的 `用户角色` 和角色在业务链中的位置，推导数据权限。

| 数据权限维度 | 推导来源 | 示例（采购申请单 / 采购员） |
|-------------|---------|--------------------------|
| 字段可见性 | 角色职责范围、风险合规约束 | 审批意见字段对采购员只读 |
| 字段可编辑性 | 角色在业务链中的位置：录入者 / 审批者 / 查询者 | 草稿态字段全部可编辑 |
| 行级数据范围 | 角色组织归属 | 仅可见本人创建的申请单 |
| 按钮操作权限 | 角色可执行动作 | 提交/撤回可操作，审批不可操作 |

写入 输出文档描述的数据权限段。

#### 5.5.4 设计示例：STR-F → BA Step 3~4 贯穿

场景：STR-F 来自 OR「采购员根据采购需求创建采购申请单，审批通过后通知申请人和采购主管」。

**wft01-biz 已标注输入**：
- 业务域：采购管理
- 用户角色：采购员
- 场景职责边界：采购员接收采购需求，创建采购申请单，提交审批
- 功能表单：采购申请单（输出文档=采购申请单，候选支撑链=@engine-flow + @engine-entity，业务方式=内置CAX）
- 支持信息：物料主数据引用规则、供应商选择合规要求、供应商评估模板
- 交互信息：审批通过后通知申请人和采购主管

**Step 3 角色场景视角 BA 设计摘要**：

```text
SL5 场景业务：采购员执行采购申请
  Input：采购员职责、采购需求入口、采购申请单功能表单、物料/供应商上下文
  Process：@engine-menu 的末级节点编排打开采购申请单功能表单；在审批状态变化后保持采购申请上下文并触发后续状态/消息约束
  Output：可运行的采购申请场景业务
  全场景指标：采购申请合格率、申请转化时效、需求覆盖率
```

**Step 4 输出产品视角 BA 设计摘要**：

```text
输出文档路径：采购申请单 → 采购管理 / 采购申请管理 / 采购申请单据 / 采购申请创建与维护
  PL1~PL3←STR-F 23B（Q1），PL4 WBS AI 归纳

补充 Bn 产品文档：
  逐部件：采购申请单 → 26-Q3（新建文档定义）
  完备性检查：
    构件类型=采购申请单据，应有=采购申请单/审批记录/状态记录
    清单来源：采购申请单=STR-F已覆盖；审批记录/状态记录=AI经验推断（置信度=中）
    覆盖结论：部分覆盖
    缺口处理：[推断-待OR确认] 审批记录、[推断-待OR确认] 状态记录 → 写入 BA 缺口说明 + 生成 OR 材料

输出文档设计：采购申请单
  Input：采购需求确认 + 物料主数据引用规则 + 供应商选择合规要求 + 供应商评估模板
  Process：采购员录入申请信息 → 表单校验 → 提交 → 状态进入审批中；审批完成后触发通知动作
  Output：采购申请单
  生命周期：草稿 → 已提交 → 审批中 → 已通过 / 已驳回

引擎能力校验：
  - 实体对象：采购申请
  - 明细实体：采购申请物料明细
  - 实体关系：采购申请 → 物料主数据
  - 状态事件：采购申请已提交、采购申请审批通过
  - 业务动作：提交采购申请、撤回采购申请
  - 校验规则：申请数量必须大于 0

数据权限（采购员）：
  - 字段可见：审批意见字段只读
  - 字段可编辑：草稿态字段全部可编辑
  - 行级范围：仅可见本人创建的申请单
  - 按钮权限：提交/撤回可操作，审批不可操作
```

**BA 节点汇总**：1 个角色场景视角 BA 方案 + 1 个输出产品视角（Bn产品） BA 方案 + 1 个 SL5 场景业务 + 1 个 输出文档 + 若干 引擎能力校验 + 数据权限；无独立交互业务。

#### 5.5.5 变更类型判定

按 §2.7 判定本轮变更类型（增量型 / 修订型 / 结构型），准备变更项清单。完整变更影响声明在 Step 5 覆盖自查通过后统一写入——此时 BA 方案、业务缺口和 OR 材料引用均已完备。

---


### 3.6 Step 5 · 业务需求增补

**增补线第一步（FR-BIZ 出口）**。汇聚 Step 4 识别的业务缺口（产品数据完备性检查中 AI 推断的补充产品数据），统一执行 `[推断-待OR确认]` 标注、生成 FR-BIZ OR 材料并登记 OR 原料状态表。指标缺口已由 Step 3（SL5 级）和 Step 4（PL5 级）分别覆盖，不再由本 Skill 作为独立缺口类型处理。

> **原则**：Step 3/4 只做设计 + 识别缺口，Step 5 统一处理业务侧缺口 → OR 材料。缺口不进入 PL5 IPO，不直接写入 24/26 资产；必须经后续 OR→STR-F→BA 管道确认后，才作为正式 BA 内容进入资产写回。

#### 5.6.1 缺口来源

| 来源 | 缺口形态 | 发现点 | OR 类型 |
|------|---------|--------|---------|
| 业务缺口 | 构件类型应有产品数据缺少业务承接 | Step 4 产品数据完备性检查 | FR-BIZ |

#### 5.6.2 处理流程

对每条缺口：

1. **汇聚并写入独立缺口说明段**：在 BA 节点中创建 `### 业务缺口说明`，逐条列出 Step 4 已标注 `[推断-待OR确认]` 的缺口，附加置信度和 OR 材料引用，不写入 24/26 资产正文
2. **聚合**：同类型缺口按构件类型分组，合并为 OR 材料条目
3. **生成 OR 材料**：写入 `20-pl4eos/80-pl4eos-2-eosdata/00-origin-requirement-materials/10-raw-files/{YYYYMMDD}-wft02-biz-{批次ID}-业务需求增补.md`
4. **登记 OR 原料状态表**：更新 `01-eos-sysdev-status.md` 表A，生命周期=`已登记`

#### 5.6.3 OR 材料模板

材料至少包含：

| 字段 / 章节 | 内容要求 |
|-------------|----------|
| 材料编号 | `RAW-{YYYYMMDD}-{seq}` |
| 创建日期 | 本轮运行日期 |
| 来源 | `wft02-biz` Step 5、来源 BA 节点 ID、来源 STR-F 节点 ID、BA 版本 |
| 材料状态 | `已登记，待文本化 / 切分 / 规范化` |
| 适用范围 | `EOS 业务需求增补类 FR-BIZ OR 材料` |
| 背景与设计判断 | 说明从哪些 Step 4 完备性检查中引出 |
| 业务缺口清单 | 按构件类型分组列出缺失产品数据、推断依据、置信度 |
| 边界说明 | 该材料不是正式 OR，不写入 `01-original-requirements.md`，不直接进入 wft01-biz |
| 建议预处理方向 | 建议 OR 预处理链如何归并、切片、澄清、规范化 |

---

### 3.7 Step 6 · 资产写回与落账

人类已在 Step 2 中确认方案（全部条目 `[同意]` + `[已处理]`），Step 6 统一执行写回与落账，不区分 Q 级别——Q1/Q2/Q3 均直接写入，不需人类二次确认。

写回要求：所有写回统一遵循幂等去重、局部写回、全貌同步（树画像/资产全貌/分片索引/状态队列/追溯引用）；不改变 BA 节点状态（状态已在 Step 2 设为 `可以SR设计`，或因上游修订按生命周期规则设为 `待补充SR设计`）；不构成 wft03 的前置条件。

**23/24/26/27 资产写回动作**：按 §3.4.1·2 的资产对齐判定执行，写回时只记录动作、依据和追溯引用，不重复展开判定理由。

| 资产 | Q1 / 已有 | Q2 / 扩展 | Q3 / 缺失 |
|------|-----------|-----------|-----------|
| **23** 产品架构资产 | 定位对应节点块，追加 BA 引用并记录追溯引用 | 扩展已有节点块，追加 BA 引用、扩展说明和追溯引用 | 新建节点块，写入定义、BA 引用和追溯引用 |
| **24** 业务资产 | 定位业务块，追加 BA 引用（业务名称/一句话职责/来源 STR-F） | 扩展已有业务，追加引用和扩展说明 | 新建业务块，写入定义和追溯引用 |
| **26** 文档定义资产 | 定位文档定义块，更新业务域/产品类型/构件类型/来源 BA/来源 STR-F/追溯引用 | 扩展已有文档定义，更新引用和扩展说明 | 新建文档定义块，写入定义和追溯引用 |
| **27** 资源工具资产 | 已有资源追加 BA 引用（去重） | — | 缺失资源形成资源资产建议（名称/类型/用途/建议状态=`待确认`） |

**落账操作**（资产写回的自然延续）：
- `04` 记录 `### 资产写回记录`（日期/路径/原子单元/目标资产块/动作/判定）
- `02` 对应 STR-F 节点推进为 `已分解分配`（记录 `STR-F 消费版本：v{N}`）
- 清空待办信号

---

### 3.8 Step End · 运行结束输出

#### 5.9.1 Step End 提示

```text
当前状态：<待确认方案 / 可以SR设计 / 已分解分配>
本轮 BA：<BA-ID / 新建或修订 / 版本变化 / 来源 STR-F>
方案摘要：详见 §四 增量清单输出
原始需求材料：<无 / FR-BIZ 材料>，状态见 OR 原料状态表
资产落账：<未落账 / 已写回 23/24/26/27>

下一步：
  A. 确认或修订方案：按 §3.7.1 写入条目级确认状态，或整体确认
  B. 进入 wft03：选择状态为 `可以SR设计` 或 `待补充SR设计` 的 BA
  C. 处理原始需求材料（如有）：选择对应 ORT 进入 OR 预处理链
  D. 继续追加 STR-F：选择新 STR-F 节点重新运行
```

---

## 四、增量清单输出

增量清单面向人类审阅，只暴露少量高层标记。Step 3~5 内部仍可使用 A2 方案、Bn 路径、文档补充、完备性缺口、权限等细分结论，但这些细分结论写入条目的 `说明` / `判定依据` / `变更影响声明`，不作为人类逐项审阅时必须理解的主标记。

### 4.1 人类审阅标记

| 标记 | 人类需要判断什么 | 覆盖的内部情形 |
|------|------------------|----------------|
| `[新增]` | 是否同意新增这个 BA 对象 | 新 STR-F 承接、A2 方案、Bn 路径、输出文档、输出文档、资源引用 |
| `[变更]` | 是否同意修改已有 BA 内容 | 产品路径扩展、IPO 调整、权限调整、文档定义扩展、引擎重判、资产对齐方案变化 |
| `[确认]` | 是否认可本轮只是复用资产或补强证据 | Q1 复用、追加 STR-F 引用、来源升级、无结构变化的说明改进 |
| `[需裁决]` | 需要人类做架构判断或回到上游处理 | 产品/场景边界存疑、跨场景候选、矛盾、业务缺口是否转 OR |

使用规则：

1. 每个清单条目最多一个主标记，避免叠加多个方括号。
2. A2 / Bn / 权限 / 指标等类别通过所在小节或说明字段表达，不再使用 `[A2方案新增]` / `[Bn路径新增]` / `[输出文档新增]` 等作为主标记。
3. `23/24/26/27` 的 Q 级别是资产对齐结果，不是增量标记；写成 `资产对齐：23=Q1，24=Q2，26=Q3，27=已有引用/资源建议`。
4. `[推断]` / `[推断-待OR确认]` 不作为清单主标记；AI 推断写入 `判定依据`。只有需要人类决策时，清单主标记才写 `[需裁决]`。
5. 业务缺口不直接等同“新增需求”；清单用 `[需裁决]` 标记，并说明建议生成的原始需求材料路径。

### 4.2 内部细分结论与人类标记映射

| 内部细分结论 | 清单主标记 | 输出方式 |
|--------------|------------|----------|
| 新建 BA / A2 方案 / Bn 路径 / PL5 / 资源引用 | `[新增]` | 写入对象名称、来源 STR-F、资产对齐结果 |
| Q1 复用 / 追加来源 / 无新增 | `[确认]` | 写入复用资产路径和证据补强说明 |
| IPO 调整 / 路径扩展 / 权限调整 / 文档定义扩展 | `[变更]` | 写入调整前后差异和判定依据 |
| Bn 完备性缺口 / 指标缺口 | `[需裁决]` | 写入缺口说明、原始需求材料路径或建议处理方向 |
| 矛盾 / 跨场景 / 边界存疑 | `[需裁决]` | 写入裁决问题和可选处理路径 |

增量清单覆盖 Step 3~5 的全部增量，在 Step End 输出。增量清单是 **AI 在对话中的输出**，供人类阅读和反馈，不写入 `04-*.md` 或其他资产文档。格式如下（新建 BA）：

```text
=== BA-XXXX 本轮更新清单 ===

[新增] 承接 STR-F-XXXX, STR-F-YYYY（共2个）

角色场景视角（A2产品）
  [新增] A2-场景业务-XXX "采购员执行采购"
    资产对齐：23=Q1，24=Q1

输出产品视角（Bn产品）
  [新增] 采购申请单 → 采购管理 / 采购申请管理 / 采购申请单据 / 采购申请创建与维护
    资产对齐：23=Q1，26=Q3
  [需裁决] 完备性缺口：审批记录、状态记录是否进入 OR 预处理链
    建议材料：{YYYYMMDD}-wft02-biz-{批次ID}-业务需求增补.md

PL5
  [新增] PL5-PART-XXX "采购申请单"
    资产对齐：23=Q1，24=Q1，26=Q3
  [新增] 
    资产对齐：24=Q1

权限
  [变更] 采购员对采购申请单的字段/按钮权限方案

资源
  [新增] 27 资源引用 CAX-XXX "CAD工具"

=== 以上为本次全部新内容 ===

请逐项确认，或回复"整体确认同意"。
```

---

## 五、回退规则

| 条件 | 处理 |
|------|------|
| BA 节点是否存在需要重判 | 回退 Step 3 |
| A2产品/Bn产品产品路径需要重做 | 回退 Step 3/4 |
| 输出文档或 输出文档需要新增/删除/改名/改边界 | 回退 Step 3/4 |
| 权限需要重判 | 回退 Step 3/4 |
| 资产块在资产文件中不存在（Q1 失效） | 降级为 Q3，形成新增建议 |

---

## 六、变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-07-07 | v4.0 | 基于 91 规范 v3.3 结构性重写：(1) §一 目的与目标对齐 91 规范 §5.2 五任务清单，引擎需求增补职责移至 wft03-biz，Step 布局 7→6 步；(2) §二 重构为「BA 设计基础」四节（业务架构/A2/Bn/功能引擎与输出文档的关系），IPO 降级为操作技法，功能元件（PL6）概念去除；(3) §三 操作流程重组（输入路由+文档加载并入前置子节，BA 生命周期→§3.4，设计规则→§3.5）；(4) 引擎命名对齐 25 资产 12 引擎权威清单（`@engine-场景引擎`→`@engine-menu`，`@engine-表单引擎`→具体功能引擎引用，`@engine-permission` 删除）；(5) 交互协议引用 91 规范 §7.5~§7.7；(6) 全文 PL 层级止于 PL5；(7) 章节重编号（§六→§四、§七→§五、§八→§六） |
| 2026-07-01 | v3.21 | §5.7.7 字段精简 15→9——删除线索类型/来源 BA/所需能力/影响对象/边界说明/建议处理方向（冗余/固定值/上下文已知/下游决策），所需能力合并入线索表述 |
| 2026-07-01 | v3.20 | 判定链顺序重编号——旧(0/1/1C/4/5)→新(1/2/3/4/5)，删除非连续编号说明；联动更新流程图/字段口径/触发条件/汇聚范围全部引用 |
| 2026-07-01 | v3.19 | F5 审视修复——§3.8.6 回填范围「各线索」→「所有被聚合到此材料的线索」，与按引擎分组的聚合逻辑一致 |
| 2026-07-01 | v3.18 | F4 审视修复——§5.7.7 `原始材料文件` 字段示例与 §四 增量清单示例中的文件名模板对齐 §3.8.6（`{BA节点ID}`→`{批次ID}`，`引擎能力缺口原始需求材料`→`引擎需求增补`） |
| 2026-07-01 | v3.17 | F3 审视修复——§5.7.1 新增预判信息消费段（读取 Step 4 `[引擎缺口]` 标记作为优先核查线索，一致引用/不一致说明差异）；§3.8.8 覆盖自查新增 Step 6 专项核查（逐条核对 Step 4 缺口标记均已处理） |
| 2026-07-01 | v3.16 | F2 审视修复——§5.7.1 判定链顶部新增前置判断「能否确定该对象的目标引擎？」→ 顺序 0；顺序表新增顺序 0 行；§3.8.5 新增顺序 0 触发条件；§3.8.6 汇聚范围纳入顺序 0；§5.7.4 字段口径补充顺序 0 填写规则 |
| 2026-07-01 | v3.15 | F1 审视修复——删除 §5.7.2 横切对象与支撑能力子节（菜单引擎在治理 STR-F 的 wft02-biz 走标准判定链，权限归入 PL5 表单/流程引擎能力边界判定，基础集成归入 PL5 Process 吸收），联动删除 §5.7.4 支撑信息表横切行、§3.8.5 第4条、§5.7.7 来源 BA 对象字段横切引用 |
| 2026-07-01 | v3.14 | Step 6 命名简化——「引擎支撑判定与需求增补」→「引擎需求增补」，与 Step 5「业务需求增补」对称；判定是手段，需求增补是目的，标题聚焦产出 |
| 2026-07-01 | v3.13 | 全文冗余瘦身——资产对齐判定细则收敛为一张表，Step 7 写回动作引用统一判定口径；Step End 改为状态/摘要/下一步，不再重复 Step 3~5 产出；变更记录仅保留近期条目，历史记录折叠为归档说明 |
| 2026-07-01 | v3.12 | 压缩 §5.7 冗余内容——概述改为三件事边界说明；删除缺口线索示例块；缺口触发条件从示例表改为规则清单；合并“覆盖自查门禁”和“状态推进”为 §3.8.8；交叉引用从 §5.7.9 更新为 §3.8.8 |
| 2026-07-01 | v3.11 | 优化 §5.7 子节标题可读性——标题从名词型标签改为“判定链 / 判定对象 / 差异处理 / 写入格式 / 触发条件 / 材料生成 / 字段规范 / 门禁 / 状态推进”的过程型目录；同步将“横切支撑判定”小标题改为“横切对象与支撑能力” |
| 2026-07-01 | v3.10 | 收紧 §5.7 引擎支撑判定边界——BA 判定对象改为“BA 末级对象及其 输出文档”；新增 25 资产状态门禁和 `候选覆盖-待确认` 状态，避免将待确认引擎作为强依赖；补充 ML 菜单可达性、权限与基础集成横切支撑判定；明确可引用 CU 类型作为能力边界证据但不绑定 CU 实例、不展开 CU IPO；缺口线索目标引擎不得临时固化 25 中不存在的新引擎 ID |
| 2026-07-01 | v3.9 | 冗余精简 R1~R3——R1：§5.5 intro 删除与 §3.5.1 重复的设计心法链句；R2：§3.5.2 SL5 IPO 表 + §3.5.2 PL5 IPO 表压缩为引用 §二 + 步骤特有补充行；R3：§3.4.1·1 IPO 模式生成删除 §二 IPO 表的散文重述段 |
| 2026-07-01 | v3.8 | 审视修复 F1~F7——F1：§3.7.1 「Step 3~4」→「Step 3~5」；F2：§二 变更影响声明归属「Step 3」→「Step 4+6」；F3：§5.5 Step 4 设计过程结构重组（6 步骤集中至 §3.5.1 设计过程，§3.5.2 输出格式，§3.5.3~§3.5.5 重编号）+ 交叉引用联动更新；F4：§二 23 资产行新增 `23A`/`23B` 缩写定义；F5：§3.5.1 新增第 7 步「推导数据权限」纳入编号设计过程；F6：§3.5.3 完备性检查表缺口处理列 + §5.6.2 标注→汇聚，明确 Step 4 逐缺口标注 / Step 5 汇聚写入独立缺口说明段的分工；F7：§3.15.1 结构型判定标准「边界扩展」→「新增 SL2/PL2 或跨域重组」+ Q2 不触发结构型注记 |
| 2026-07-01 | v3.7 | 审视修复 F1/F2/F3/F7/F8/F9/F10——F1：指标术语对齐 wft01-biz v1.60（共 15 处）；F2：`菜单可见角色` 改为从 STR-F `用户角色` 推导（§2.1/§5.4.3/§3.5.5 共 3 处）；F3：§3.15.1 "边缘/超出"→"边界扩展"；F7：§5.5 子节编号补全（§5.5.6~§5.5.8→§3.5.5~§5.5.7）；F8：§5.7.2 删除"Bn产品原子业务操作"行，判定重点并入 PL5 行；F9：§3.5.2 IPO 表 Process 单元格简化；F10：§二 BA 资产层级表新增跨层映射注释（SL6=PL5） |
| 2026-07-01 | v3.6 | 删除 Step 5（平台菜单架构设计）并将内容重新分配——菜单可达性移入 Step 3 为 SL5 属性（§5.4.3 菜单路径）、权限推导移入 Step 4 为 PL5 属性（§5.5.6 数据权限）、上层指标方案不再作为 wft02-biz 独立产出（SL5/PL5 级指标已由 Step 3/4 分别覆盖，聚合由治理 STR-F 承接）、治理能力缺口识别已由 Step 6 覆盖。设计示例和变更类型判定重定位至 Step 4 末尾（§5.5.7/§5.5.8）。Step 全量重编号（旧 6→5、7→6、8→7、End→8、§5.11→§5.9）。§5.1 布局表 / §一 职责重写 / §二 要素列表 / §3.4.1·5 覆盖自查（12→10 项）/ §七 回退规则 / 全文交叉引用联动更新 |
| 2026-07-01 | v3.5 | 删除「附加产出」表及全部引用——业务菜单方案/上层指标方案回归为治理 STR-F 的正常 输出文档，治理能力缺口线索归入 Step 7 引擎缺口判定。§二 表→注记；BA 节点完整要素移除「附加产出」行；覆盖自查清单移除治理缺口线索项（13→12 项）；增量清单移除「或附加产出」措辞 |
| 2026-06-24~2026-07-01 | v1.0~v3.4 | 历史变更记录已折叠，详见 Git 历史或旧版文档。 |
