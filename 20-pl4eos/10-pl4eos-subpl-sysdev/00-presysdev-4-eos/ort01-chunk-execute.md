# 文本化的原始需求材料切分执行与迭代

**Skill ID**: ort01-chunk-execute · **Step ort01: Textualized Original Requirement Chunk Execution and Iteration**

> **概述**：读取文本化原始需求文档正文区中的切分方案块和人类反馈文本，按反馈执行切分或重新生成方案。本步可迭代——人类在反馈区输入意见后重启即可。
>
> **输入 → 输出**：文本化原始需求文档（`{原始文件名}.md`，正文区：原文 + 切分方案块 + 反馈提示区）+ OR 状态表（含切分状态表）→ 切分文件（`{原始文件名}-sub-{seq}.md`）+ OR 状态表更新
>
> **接口设计**：本步不依赖变更记录表+切分标记。直接读取正文区切分方案块和人类反馈文本。读到"同意执行"时按方案块行号范围执行切分；读到修改建议时重新生成方案块并归档旧版。下游 ort02 通过 OR 状态表生命周期=已登记读取切分文件。
>
> **适用场景**：接续 ort00 输出的文本化原始需求文档（含切分标记）或人类自行编写的切分标记文档。切分文件产出后供下游 ort02-clarify-propose 进行首轮模糊标注。
>
> **版本**：v1.2 | **修订**：2026-06-07

---

## 一、设计角色

本步在设计链中承担的角色：
- **输入**：`../../80-pl4eos-2-eosdata/_inputs/10-source-materials/` 目录中的文本化原始需求文档（`{原始文件名}.md`，含切分标记。.md 格式源材料对应 `{原始文件名}.textualized.md`）+ `../../80-pl4eos-2-eosdata/_inputs/01-eos-or-lifecycle.md`
- **产出**：`../../80-pl4eos-2-eosdata/_inputs/10-source-materials/` 目录中的切分文件（每片一个 `{原始文件名}-sub-{seq}.md`）+ OR 状态表更新（登记表主表：各切分独立记录；切分状态表同步更新）+ 文本化文档管理区追加切分迭代记录

### 与三条工程化设计原则的关系

| 原则 | 在本步中的体现 |
|------|--------------|
| **详概分离** | 本步仅执行语义切分，不涉及需求内容的理解或分析。切分是纯粹的文档结构重组 |
| **文档结对** | 本步不产生文档结对关系。本步产出的切分文件是 ort02-clarify-propose（首轮模糊标注）的输入 |
| **基于资产** | 切分文件是原始文档的派生资产，通过文件名关联（`-sub-{seq}`）保持追溯。OR 状态表中通过 parent_file 字段维护切分与原始文件的关系 |

---

## 二、前置条件

| 条件 | 说明 |
|------|------|
| 输入 | `../../80-pl4eos-2-eosdata/_inputs/10-source-materials/` 目录中存在文本化原始需求文档（`{原始文件名}.md`，正文区：原文 + 切分方案块 + 反馈提示区） |
| 参考 | 通用系统设计规范 §7.3（原始需求分析——分批处理原则）+ ort00-chunk-propose（切分提议+文本化） |
| 前提 | 正文区切分方案块已经 ort00 写入。人类在反馈区输入意见后启动本步。AI 读取反馈文本决定执行/重新生成路径 |

---

## 三、操作流程

核心驱动逻辑：读取 OR 状态表中切分状态表的当前状态和正文区切分方案块，扫描人类反馈文本——读到"同意执行"时按方案块行号范围执行切分；读到修改建议时重新生成方案块并归档旧版；当前无反馈时跳过等待。

本步按"扫描分流 → 反馈检测 → 执行/重新生成 → 追加记录 → 行动清单"的线性序列执行。扫描分流→反馈检测→执行/重新生成→追加记录为 AI 独立执行段。

操作流程概览：

```
[前置校验] 检查上游前置条件
  ├─ 10-source-materials/ 不存在 → 输出"请先运行 ort00-chunk-propose 初始化目录" → 结束
  ├─ 01-eos-or-lifecycle.md 不存在 → 输出"请先运行 ort00-chunk-propose 初始化 OR 状态表" → 结束
  ├─ 无文本化 .md 文档（不含 .pdf 等非文本格式） → 输出"请先运行 ort00-chunk-propose 完成文本化和切分建议" → 结束
  └─ 全部就绪 → [扫描分流] ↓

[扫描分流] 扫描文本化文档 + OR 状态表（切分状态表）
  │
  ├─ "待确认切分方案" → 读取正文区切分方案块 + 反馈文本
  │    ├─ [同意执行] → [执行流程] ↓
  │    ├─ [修改边界] → [重新生成] ↓
  │    ├─ [不切分] → [写入]（输出单片） ↓
  │    └─ 无反馈文本 → 跳过（等待人类操作）
  │
  ├─ "切分已执行"（迭代重入）
  │    ├─ 检测方案块有变更 + 人类反馈 → 按反馈执行/重切
  │    └─ 无变更/无反馈 → 跳过
  │
  └─ 其他状态 → 跳过

[执行流程]
  ├─ [阶段一] 读取方案块（行号范围、语义标签）
  ├─ [阶段二] 按行号范围分割原文 → 输出切分文件
  │   · 常规切分→输出多片；不切分→输出单片（total=1）
  ├─ [写入] 状态摘要→管理区、更新 OR 状态表、追加迭代记录、追加决策记录
  └─ [清理] 删除正文区切分方案块 + 反馈区，正文区回归纯原文
    ↓

[重新生成]
  ├─ 解析人类修改建议（如"002 范围扩到 L500"）
  ├─ 调整方案块中的边界
  ├─ 归档：旧方案块 + 人类反馈原文 → 历史区
  └─ 写入：新方案块到正文区（含新反馈提示区）
    ↓

[追加记录]
  ├─ 追加切分迭代记录（§5.4）
  └─ 追加决策记录（§六）
    ↓

[行动清单] 输出线下行动指南
    ↓

[人机交互] 询问人类是否打开登记表
  ├─ 回答"是"→ 输出登记表路径 + 建议关注行
  └─ 回答"否"→ 跳过

→ 本轮结束
```

### 入口：扫描分流

AI 读取 OR 状态表中的切分状态表，按当前状态分流：

| 当前切分状态表状态 | AI 操作 | 说明 |
|-------------------|---------|------|
| **待确认切分方案** | 读取正文区切分方案块，扫描末尾反馈文本。**读到"同意执行"** → 进入执行流程；**读到修改边界描述** → 进入重新生成；**读到"不切分"** → 输出单片；**无反馈文本** → 跳过 | 首次进入 |
| **切分已执行** | 检测方案块是否有变更 + 人类反馈 → 有则按反馈执行/重切；无则跳过 | 迭代重入 |
| **其他状态** | 跳过 | 非本步职责 |

> **统一输出原则**：所有文档在 ort01 后均输出 `-sub-*.md` 文件（切分文档输出多片，不切分文档输出单片 total=1），ort02 统一处理全部切分文件，不再区分来源。

### 阶段一：读取方案块与反馈

| # | 操作 | 执行者 | 说明 |
|---|------|--------|------|
| 1 | 读取切分方案块 | AI | 从正文区读取切分方案块，获取每片的行号范围、语义标签 |
| 2 | 读取人类反馈文本 | AI | 扫描方案块末尾"您的反馈："之后的内容，识别操作意图 |
| 3 | 校验方案完整性 | AI | 检查行号范围无重叠、无遗漏覆盖区域、语义标签完整 |

### 阶段二：执行切分

| # | 操作 | 执行者 | 说明 |
|---|------|--------|------|
| 1 | 按行号范围分割内容 | AI | 根据方案块中的行号范围，将原文按行分割为独立切分 |
| 2 | 为每片附加元数据 | AI | 每片开头插入 YAML frontmatter，记录来源（source/original_source）、序号（seq/total）、语义标签（label）、范围（range），状态初始化为 `status: raw` |

**切分文件内容结构**（YAML frontmatter 模板见 §5.1）。

**切分文件的目录与命名**：

与原始文件同目录，命名格式：`{原始文件名}-sub-{seq}.md`

```
10-source-materials/
├── 01-市场调研报告.pdf              ← 原始文件（只读）
├── 01-市场调研报告.md                ← 文本化文档（含切分标记）
├── 01-市场调研报告-sub-001.md      ← 切分输出
├── 01-市场调研报告-sub-002.md      ← 切分输出
└── 01-市场调研报告-sub-003.md      ← 切分输出
```

### 写入：输出切分文件 + 更新状态表 + 追加记录

| # | 操作 | 执行者 | 说明 |
|---|------|--------|------|
| 1 | 输出切分文件 | AI | 按行号范围分割原文，每片写入独立文件。常规切分→输出多片；不切分→输出单片（total=1） |
| 2 | 写入/更新状态摘要 | AI | 在切分文件管理区顶部 + 文本化文档管理区顶部写入状态摘要 |
| 3 | 生成执行批次号 | AI | 格式 `YYYYMMDD-v{序号}`，本次执行的所有切分共享同一批次号 |
| 4 | 更新 OR 状态表 | AI | 每片注册为独立记录（首次→已登记；重切→旧片废弃、新片注册）。原始文档生命周期→已切分 |
| 5 | 更新切分状态表 | AI | 状态→"切分已执行"（不切分场景→"不切分已确认"） |

### 清理：正文区方案块

| # | 操作 | 执行者 | 说明 |
|---|------|--------|------|
| 6 | 清理方案块 | AI | 删除正文区切分方案块 + 反馈区。仅在执行分支触发，重新生成分支不清理 |

### 追加归档

| # | 操作 | 执行者 | 说明 |
|---|------|--------|------|
| 7 | 追加切分迭代记录 | AI | 在管理区追加执行记录块，含批次号、各片摘要 |
| 8 | 追加决策记录 | AI | 根据本次处理结果填充决策记录模板（§6），追加到管理区 |

OR 状态表记录格式定义见 §5.3。切分迭代记录格式见 §5.4。

### 行动清单输出

```
══════════════════════════════════════════
  ort01-chunk-execute 执行完成 · 行动清单
══════════════════════════════════════════

下方操作项按材料状态分组列出。操作完成后启动 ort02-clarify-propose 进入首轮模糊标注。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 切分文件确认
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[文本化文档] — 已输出 [N] 个切分文件：
├─ [切分文件]（[语义标签]）
├─ [切分文件]（[语义标签]）
└─ [切分文件]（[语义标签]）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️ 孤儿文件（重切·片数减少时出现。如无，请忽略）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
以下切分文件在本次重切后不再对应任何新边界：
├─ [文件]（原标签：[标签]）
相关记录已标记为"已废弃"，不会被下游拾取。
请审阅后决定删除或归档，不影响系统处理。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 方案块已处理
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[文件] — 切分方案块已执行（清理或归档），
同时追加了切分迭代记录（含批次号）。
如需调整边界，修改方案块后写入反馈重启即可迭代。
```

▲ AI 独立执行段结束

切分状态表和登记表已更新，切分文件已输出，方案块已处理。

需要打开登记表查看详情吗？
（回答"是"→ 直接打开；"否"→ 结束本轮）

人类回答"是"后，AI 直接提供可打开的文件路径：

登记表路径：../../80-pl4eos-2-eosdata/_inputs/01-eos-or-lifecycle.md

建议关注行：
├─ 记录ID 行 → 内容摘要、切分结果
└─ 切分状态表 → 切分处理状态、操作指引

---

## 四、AI 执行指南

### 4.1 切分边界处理

| 边界情况 | 处理方式 |
|---------|---------|
| 标记边界在段落中间 | 按标记边界切，被截断的段落完整归入起始片。在行动清单中标注"段落被边界截断" |
| 连续两片之间有空白行 | 空白行归入前一片 |
| 切分内容为空（标记之间无内容） | 不输出空文件，视为无效标记，在行动清单中报告 |
| 切分文件与已有文件重名（重切场景） | 覆盖旧文件。旧切分在 OR 状态表中标记为"已废弃" |

### 4.2 重切时的变更检测

AI 通过比对文本化文档的 **last_size** 与切分状态表中记录的上次执行时的文件大小来判断是否需要重切（由入口扫描分流触发，详见 §3·入口：扫描分流）。last_size 在每次切分/重切执行后更新：

| 条件 | 处理 |
|------|------|
| 文本化文档文件未变化（last_size 一致） | 跳过，复用已有切分文件 |
| 文本化文档中的切分标记已变化 | 按新标记重新切分。新注册的切分文件覆盖旧文件，旧切分标记为"已废弃" |
| 新增了切分（原 3 片变 5 片） | 新增片注册为"已登记" |
| 减少了切分（原 5 片变 3 片） | 减少的片标记为"已废弃"。遗留的切分文件作为孤儿文件在行动清单中列出供人类审阅（见§3·行动清单"孤儿文件提醒"） |
| 切分序号重新排列 | 按新序号输出，旧序号标记为"已废弃" |

### 4.3 人类指令与标记

#### 4.3.1 人类对切分标记的调整方式

人类在文本化文档中可直接：

| 操作 | 方式 | AI 处理 |
|------|------|---------|
| 确认边界 | 保留 `[切分建议]` 标记不变 | 按标记执行 |
| 移动边界 | 将 `[切分建议]` 或 `[切分结束]` 标记移到其他位置 | 按新边界切分 |
| 新增切分 | 插入新的 `[切分建议: {新序号} · {标签}]` ... `[切分结束: {新序号}]` | 识别新标记，增加切分 |
| 合并切分 | 删除两片之间的边界标记 | 两片合并为一片 |
| 取消切分 | 删除所有切分标记，添加 `[切分建议: 不切分]` | 输出单一切分文件（整份为一片） |

#### 4.3.2 标记约定

切分标记使用与 ort00 相同的格式，人类可在此基础上调整：

```
[切分建议: 001 · 市场部业务需求]    ← 人类可移动此行位置
...内容...
[切分结束: 001]                      ← 人类可移动此行位置
```

#### 4.3.3 变更记录表的操作方式

本步使用统一变更记录表替代传统确认区。变更记录表有两种形态，分别对应首次进入时（由 ort00 写入）和执行后（由 ort01 刷新）。

**形态① — 由 ort00 写入（首次进入）：**

首次进入时，文本化文档管理区由 ort00 写入变更记录表（位于状态摘要之后），包含两行建议行（状态=待确认）。人类删除不选择的行，保留一行，然后启动本步。AI 检测到仅剩一行"待确认"（情形A）后自动执行。

切分建议下的变更记录表示例：

```
## 变更记录

| 日期 | 操作者 | 操作 | 说明 | 状态 |
|------|--------|------|------|------|
| {日期} | AI | 切分建议 | 确认切分 → 按当前切分标记执行切分（N片：{标签}） | 待确认 |
| {日期} | AI | 切分建议 | 确认不切分 → 文档不宜切分，输出单一切分文件 | 待确认 |

⚠️ **操作提示**：删除你不选择的行，保留下你想要的行。只保留一行后启动本步。
```

不切分建议下的变更记录表示例：

```
## 变更记录

| 日期 | 操作者 | 操作 | 说明 | 状态 |
|------|--------|------|------|------|
| {日期} | AI | 切分建议 | 确认不切分 → 无需切分，输出单一切分文件 | 待确认 |
| {日期} | AI | 切分建议 | 确认切分 → 文档应切分，按标记执行切分 | 待确认 |

⚠️ **操作提示**：删除你不选择的行，保留下你想要的行。只保留一行后启动本步。
```

**形态② — 由 ort01 执行后刷新（迭代入口）：**

本步执行完成后，将变更记录表刷新为执行记录 + 重切选项行（状态=待确认）。人类可编辑后重启本步触发重切。

```
---

## 变更记录

| 日期 | 操作者 | 操作 | 说明 | 状态 |
|------|--------|------|------|------|
| {日期} | AI | 切分建议 | 建议切分 {N} 片（{标签}） | 已完成 |
| {日期} | AI | 切分执行 | 已按切分标记执行切分，共 {N} 片，批次 {批次号} | 已完成 |
| ... | ... | ... | ... | ... |
| {日期} | AI | 重切 | 如已调整切分标记，保留此行使 AI 按新边界重新切分 | 待确认 |

⚠️ **操作提示**：如需重切，调整切分标记后保留"重切"行；如结果满意，删除"重切"行。
```

**入口检测规则**（AI 在入口分流中的判断逻辑）：

| 场景 | 变更记录表行状态 | AI 检测 | AI 操作 |
|------|----------------|--------|---------|
| 人类同意切分建议 | 仅剩 1 行"待确认"（结果行） | 情形A（删行） | 将此行更新为"已确认"→ 执行切分 |
| 人类否决切分建议 | 仅剩 1 行"待确认"（反建议行） | 情形A（删行） | 将此行更新为"已确认"→ 输出单一切分文件 |
| 人类追加确认行 | 出现 1 行"已确认" | 情形B（追加） | 校验后执行对应操作 |
| 人类追加重切行 | 出现 1 行"重新切分" | 情形C（重切） | last_size 变更→ 执行重切 |
| 人类满意切分结果 | 0 行"待确认" | 情形D（无法判定） | 跳过，文件就绪 |
| 无法判定 | ≥2 行"待确认" | 情形D（无法判定） | 跳过，等待人类清理 |

> **注意**：所有文档在 ort01 后均输出 `-sub-*.md` 切分文件（切分文档输出多片，不切分文档输出单片 total=1），ort02 统一处理全部切分文件，不再区分来源。检测规则由 AI 在入口分流中自动执行，无需人类手动编辑状态表。
>
> **情形B 校验说明**：AI 对追加的"已确认"行执行格式校验——(1) 行格式符合 5 列规范；(2) 操作列值为可识别的操作名。校验通过后执行对应操作；不通过时视为情形D（跳过）。

### 4.4 启动方式

#### 人类启动指令

> 开始文本化的原始需求材料切分迭代（或 ort01-chunk-execute）

#### AI 动作序列

0. **前置校验**：检查上游前置条件。**逐级报错，首次即停**——只报告最早的未满足条件。
   - `../../80-pl4eos-2-eosdata/_inputs/10-source-materials/` 不存在 → 输出"请先运行 **ort00-chunk-propose** 初始化目录结构" → **结束**
   - `../../80-pl4eos-2-eosdata/_inputs/01-eos-or-lifecycle.md` 不存在 → 输出"请先运行 **ort00-chunk-propose** 初始化 OR 状态表" → **结束**
   - `10-source-materials/` 中无文本化 `.md` 文档（不含 .pdf 等非文本格式） → 输出"请先运行 **ort00-chunk-propose** 完成文本化和切分建议" → **结束**
   - 全部就绪 → 进入步骤 1
1. **扫描分流**：读取 OR 状态表中切分状态表的当前状态
   - **状态="待确认切分方案"**：读取正文区切分方案块+反馈文本。**读到"同意执行"** → 进入步骤 2；**读到修改边界** → 进入步骤 5；**读到"不切分"** → 输出单片后跳至步骤 6；**无反馈文本** → 跳过
   - **状态="切分已执行"**：检测方案块变更+反馈 → 有则按反馈执行/重切；无则跳过
   - **其他状态** → 跳过
2. **阶段一**：读取切分方案块（行号范围、语义标签）→ 校验完整性
3. **阶段二 执行**：按行号范围分割原文 → 输出切分文件 → 更新 OR 状态表
4. **清理**：删除正文区方案块+反馈区 → 跳至步骤 6
5. **重新生成**：解析修改建议 → 调整边界 → 归档旧方案块+反馈到历史区 → 写入新方案块+新反馈提示
6. **追加记录**：切分迭代记录 + 决策记录
7. **输出行动清单**
8. **人机交互**：向人类提供登记表路径和关注指引，询问是否需要打开查看

### 4.5 与 OR 状态表（含切分状态表）的字段联动

| 字段 | 操作 | 说明 |
|------|------|------|
| 文件 | 写入 | 切分文件名称（如 `01-市场调研报告-sub-001.md`）；不切分场景同样写入（单片 total=1） |
| parent_file | 写入 | 原始文件名称；不切分场景同样写入 |
| chunk_label | 写入 | 语义标签+范围；不切分场景写入"不切分（整份文档）" |
| 生命周期状态 | 写入（切分片）→ 已登记；更新（原始文档行）→ **已切分**（所有文档在 ort01 后原始行标记为"已切分"，不再被下游拾取）；更新（重切·旧片）→ 已废弃 |
| 切分状态表·状态 | 更新 | 常规切分（变更记录表·情形A→"确认切分"行幸存）：切分已确认 → 切分已执行；不切分（情形A→"确认不切分"行幸存）：入口分流设"不切分已确认"→ 写入阶段保持该状态不变（跳过阶段一/二，输出单片）；迭代重切：保持"切分已执行"不变（不降级） |
| 切分状态表·操作指引 | 更新 | 切分迭代后写入"切分已完成，进入切分各自的登记表流程"；不切分场景同样写入（输出单片，启动 ort02 进入标注处理） |
| 切分状态表·last_size | 更新 | 每次切分/重切执行后更新为当前文本化文档的文件大小。入口扫描分流据此检测标记是否变更 |
| 切分状态表·chunk_batch | 写入 | 每次切分/重切执行后更新为本次生成的批次号（格式 YYYYMMDD-v{序号}），本次执行的所有切分共享。序号=已有执行记录最大序号+1，从 1 起始 |
| AI 建议 | 写入 | 切分完成 / 整份文档未切分 |
| 人类决策 | 不操作 | 保留，由人类在确认后修改 |

---

## 五、切分文件模板与格式

### 5.1 切分文件模板

```markdown
---
chunk:
  source: {原始文件名}.md
  original_source: {原始文件名}.{ext}
  seq: {序号}
  total: {总片数}
  label: {语义标签}
  range: {原文范围}
status: raw
---

（切分正文内容）
```

| 字段 | 说明 |
|------|------|
| source | 文本化文档文件名 |
| original_source | 原始文件文件名（只读档案） |
| seq | 本片序号（三位数字） |
| total | 本次切分总片数 |
| label | 语义标签，概括本片主题 |
| range | 原文中的范围描述（章节/段落/行号范围等） |
| status | 处理状态，初始化为 `raw`；ort02 更新为 `annotated`，ort03 更新为 `clarified` |

### 5.2 状态摘要模板

状态摘要写在切分文件管理区顶部，同步更新文本化文档顶部的状态摘要。AI 在写入阶段写入，每次完全覆盖旧摘要。切分完成后统一写入以下内容：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
文件状态
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
阶段：切分完成
AI建议：—
待处理：无
操作指引：运行 ort02-clarify-propose 进行首轮模糊标注
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

> **文本化文档同步更新**：同一轮执行中，文本化文档顶部的状态摘要也同步覆盖更新为上述内容（替换 ort00 写入的"切分建议"阶段状态）。

---

### 5.3 OR 状态表切分记录格式

OR 状态表在现有字段基础上扩展以下字段（仅切分文件的行使用）：

| 字段 | 示例 | 说明 |
|------|------|------|
| 文件 | 01-市场调研报告-sub-001.md | 切分文件名称 |
| parent_file | 01-市场调研报告.pdf | 原始文件名称 |
| chunk_label | 市场部业务需求(§2.1~§2.5) | 语义标签+范围 |
| chunk_batch | 20260606-v1 | 执行批次号，同一轮执行的所有切分共享同一批次号 |

非切分文件（普通原始材料）的 parent_file、chunk_label 和 chunk_batch 字段为空。

### 5.4 切分迭代记录格式

切分迭代记录追加到文本化文档管理区（变更记录表之前）。首次执行时写入第一块记录；重切时在已有记录之前插入新记录块（最新记录最上）：

```markdown
## 切分迭代记录

### 执行 #2 · 2026-06-07 · 重切 · 批次 20260607-v2
| 片序号 | 切分文件 | 语义标签 | 范围 |
|--------|---------|---------|------|
| 001 | 01-市场调研报告-sub-001.md | 市场部业务需求 | §2.1~§2.3 |
| 002 | 01-市场调研报告-sub-002.md | 研发+售后综合需求 | §2.4~§3.8 |

### 执行 #1 · 2026-06-06 · 首次切分 · 批次 20260606-v1
| 片序号 | 切分文件 | 语义标签 | 范围 |
|--------|---------|---------|------|
| 001 | 01-市场调研报告-sub-001.md | 市场部业务需求 | §2.1~§2.5 |
| 002 | 01-市场调研报告-sub-002.md | 研发部开发流程 | §3.1~§3.4 |
| 003 | 01-市场调研报告-sub-003.md | 售后部工单管理 | §3.5~§3.8 |
```

每个执行记录块的结构：`### 执行 #{序号} · {日期} · {切分类型} · 批次 {批次号}` 作为摘要行，下方为片序号→切分文件→语义标签→范围 的表格。

## 六、决策记录模板

AI 在写入阶段（§3·写入 Step 8）追加决策记录到文本化文档管理区（变更记录表之后），基于本次处理结果填充本模板并输出。

```
══════════════════════════════════════════
  切分决策记录（供追溯）
══════════════════════════════════════════

源文件：[文件名]
切分类型：[首次切分 / 重切]
执行日期：[YYYY-MM-DD]

切分总览：
├─ 总片数：[N 片]
├─ 执行批次：[批次号]
├─ 覆盖范围：[文档章节/段落范围]
└─ 语义标签：[标签1 / 标签2 / ...]

重切记录（仅重切场景）：
├─ 前次切分总片数：[N]
├─ 变更类型：[新增片 / 减少片 / 边界调整 / 重排]
└─ 已废弃切分：[旧切分文件列表]

边界异常记录：
└─ [如有段落被边界截断、重叠标记等问题，在此记录]
```

---

## 七、线下审查指引

本指引供人类在 ort01 执行完成后审查切分产出，确保切分质量符合下游处理要求。

### 7.1 审查内容

| 审查项 | 说明 | 通过标准 |
|--------|------|---------|
| 边界合理性 | 每片是否为语义完整的独立段落 | 单片内容围绕单一主题，无生硬截断 |
| 覆盖完整性 | 全部标记范围均已输出为切分文件 | 无遗漏段落，无空切分文件 |
| 标签准确性 | YAML frontmatter 中的 label 与内容匹配 | 标签概括了该片核心主题 |
| 命名一致性 | 文件名遵循 `-sub-{seq}` 格式 | 序号连续无跳号，与标记对应 |
| 不切分标记 | `[切分建议: 不切分]` 标记的材料是否确为整份文档无需细分 | 单一切分文件内容完整，label 标注合理 |

### 7.2 调整方式

审查发现问题时，打开文本化文档调整切分标记后重启本步：

> **标记调整后注意事项**：调整切分标记会触发 `last_size` 变更检测。请确保文本化文档管理区的变更记录表中保留"重切"行（形态②已自带），或将切分状态表手动改为"切分已确认"以走中断恢复路径。否则入口分流将因无有效重切行而跳过该文件。

| 问题 | 调整方式 |
|------|---------|
| 边界偏前/偏后 | 移动 `[切分建议]` 或 `[切分结束]` 标记位置 |
| 漏切了一段 | 插入一对新标记包围遗漏内容 |
| 某片语义不连贯 | 在该片中间插入切分标记拆分为两片 |
| 两片应合并 | 删除两片之间的边界标记 |
| 整份不应切分 | 删除所有切分标记，添加 `[切分建议: 不切分]` |

---

## 八、质量门禁

### 8.1 过程正确性

- [ ] 入口扫描分流已按切分状态表当前状态正确分流（§3·入口：扫描分流）
- [ ] 人类反馈文本已正确解析（同意执行/修改边界/不切分）
- [ ] 方案块行号范围已校验（无重叠、无遗漏）
- [ ] 重切确认标记已正确检测（变更记录表中出现"重切"行且状态=待确认）
- [ ] 状态不降级原则已遵守（迭代重切不降级"切分已执行"回前态，§3·入口分流）
- [ ] 切分状态表已同步更新（切分已确认→切分已执行 / 不切分已确认，§4.5）
- [ ] 切分标记完整性已校验（成对、无重叠，§3·阶段一 Step 3）
- [ ] 执行批次号已正确生成（格式 YYYYMMDD-v{序号}，序号递增，§3·写入 Step 3）
- [ ] 状态摘要已写入/更新——切分文件顶部状态摘要已写入，文本化文档顶部旧摘要已覆盖（§3·写入 Step 2）
- [ ] 旧切分记录已标记为"已废弃"（仅重切场景，§3·写入 Step 4）
- [ ] 变更记录表已刷新为形态②（原行→已完成，追加执行记录行+重切行[待确认]，§3·写入 Step 7）
- [ ] 切分迭代记录已追加到文本化文档（含批次号、各片摘要；重切时在已有记录之前插入，§3·写入 Step 6）
- [ ] 决策记录已追加到文本化文档（§3·写入 Step 8 + §6）
- [ ] 孤儿文件已汇总并在行动清单中提醒（仅重切·片数减少场景，§3·行动清单）
- [ ] 不切分场景已正确输出单一切分文件（total=1，label="不切分（整份文档）"，§3·写入 Step 1）

### 8.2 产出验证

- [ ] 所有切分文件命名前缀与原始文件一致（`{原始文件名}-sub-{seq}`，§3·写入 Step 1）
- [ ] 每个切分文件包含完整的 YAML frontmatter（source/original_source/seq/total/label/range/status，§5.1）
- [ ] 每个切分文件顶部已写入状态摘要（阶段=切分完成，操作指引指向 ort02，§5.2）
- [ ] 文本化文档顶部的状态摘要已同步更新（覆盖 ort00 旧摘要，§3·写入 Step 2）
- [ ] 切分文件中已移除 `[切分建议]` / `[切分结束]` 标记行（§3·阶段二 Step 3）
- [ ] 各切分内容覆盖全部标记范围，无遗漏、无空文件
- [ ] 切分序号连续，无跳号（§3·阶段一 Step 3）
- [ ] OR 状态表中各切分行已正确注册（含 parent_file + chunk_label + chunk_batch，§3·写入 Step 4 + §5.3）
- [ ] 切分状态表状态已正确更新——常规切分→"切分已执行"；不切分→保持"不切分已确认"（§3·写入 Step 5）
- [ ] 切分状态表 last_size 已更新为当前文本化文档的文件大小（§4.5）
- [ ] 切分状态表 chunk_batch 已更新为本次执行批次号（§4.5）
- [ ] 文本化文档中已追加切分迭代记录（含批次号、各片摘要；最新记录最上，§3·写入 Step 6 + §5.4）
- [ ] 文本化文档管理区的变更记录表已刷新为形态②（执行记录行[已完成]+重切行[待确认]，§3·写入 Step 7）
- [ ] 文本化文档正文区切分标记已清理（正文区回归纯原文，§3·写入 Step 9）
- [ ] 决策记录已追加到文本化文档（§3·写入 Step 8 + §6）
- [ ] 已废弃的旧切分不再被下游步骤拾取

---

## 九、产出物

| 文件 | 说明 | 路径 |
|------|------|------|
| 切分文件 | 按行号范围拆分的 `.md` 文件，含 YAML frontmatter | `../../80-pl4eos-2-eosdata/_inputs/10-source-materials/{原始文件名}-sub-{seq}.md` |
| OR 状态表（登记表主表） | 各切分注册为独立记录（生命周期=已登记） | `../../80-pl4eos-2-eosdata/_inputs/01-eos-or-lifecycle.md` |
| OR 状态表（切分状态表） | 切分处理状态更新 | `../../80-pl4eos-2-eosdata/_inputs/01-eos-or-lifecycle.md` |
| 切分迭代记录 | 追加到文本化文档管理区 | `{文本化文档}.md`（管理区） |
| 决策记录 | 追加到文本化文档管理区 | `{文本化文档}.md`（管理区） |
| 文本化文档正文区 | 执行分支→方案块已清理；重切分支→新版方案块已写入 | `{文本化文档}.md`（正文区） |
| 历史区 | 重切分支：旧方案块+人类反馈原文已归档 | `{文本化文档}.md`（历史区） |

> **不切分场景**：当文本化文档标记为 `[切分建议: 不切分]` 时，本步同样输出单一切分文件（`{原始文件名}-sub-001.md`，total=1，label="不切分（整份文档）"），切分状态表更新为"不切分已确认"，原始文档行生命周期更新为"已切分"。ort02 统一处理所有切分文件，不再区分来源。

---

## 十、变更日志

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-06-06 18:20 | v1.0 | **预处理链统一模型实施**：① `-chunk-`→`-sub-` 重命名——输出文件命名模式全域替换，涉及命名规则/示例/质检项约 30 处；② `-clarified.md` 合并到 sub 文件——frontmatter 追加 `status: raw`，标注记录直接写入 sub 文件（不再创建独立标注文件） |
| 2026-06-07 | v1.1 | **两区结构适配**：变更记录表/迭代记录/决策记录从文本化文档末尾移至管理区；新增清理步骤（写入后删除正文区切分标记，仅状态转迁至"切分已执行"时执行） |
| 2026-06-07 | v1.2 | **三区模型 + 方案块简化**：移除变更记录表检测和行内切分标记；改为扫描切分方案块+反馈文本（同意执行/修改边界/不切分）；新增重新生成分支（解析反馈→调整边界→归档旧版+反馈→写入新版）；新增清理方案块步骤；简化质量门禁/产出物 |
| 2026-06-06 16:30 | v0.9 | **全文档逐节审查 H3 级优化 + 预处理链模式对齐**：模式对齐（①②③④⑤⑥）；逐节审查——§1 产出清单明示切分状态表、§2 前提条件对齐入口分流、§3 分流表追加"切分已确认（中断恢复）"行 + 流程图入口节点措辞对齐 + Step 2 序号描述精简 + 行动清单引用修正、§6 决策记录模板追加执行批次号、§7 7.2 追加标记调整后注意事项、§8 8.1 第2项措辞对齐分流表术语、§9/§10 复核无异常 |
| 2026-06-06 14:43 | v0.7 | **迭代模型重构**：§3·入口重构为[扫描分流]（按切分状态表状态分流 + last_size 变更检测 + 状态不降级原则）。§3·写入新增 Step 4（刷新确认区）。§4.3.3 确认区从两种变体(A/B)重构为三种形态(形态①/①'/②)，移除 [返回分析]/[仍需切分] 分支。§4.2 变更检测从"内容签名"改为 last_size。§4.4 AI 动作序列同步对齐。§8 质量门禁追加 last_size/确认区刷新/状态不降级检查项。§4.5 追加 last_size 字段。ort00 切分确认机制章节同步更新 |
| 2026-06-06 14:21 | v0.5 | §3 追加核心驱动逻辑段落 + 执行模式说明（AI 独立执行段/人类交接点），对齐 ort04 模式；新增 §6 产出物（切分文件/登记表/切分状态表/行动清单），明示不切分场景的产出差异 |
| 2026-06-06 11:56 | v0.4 | 流程概览图重构为 [阶段二]→[写入]→[行动清单]→[人机交互] 四段结构，[写入]节点仅保留 ort01 实际产出（切分文件/切分状态表/登记表注册）；流程描述和 AI 动作序列同步对齐；行动清单末尾追加"需要打开登记表查看详情吗？"交互段 |
| 2026-06-06 11:53 | v0.3 | 补充变体 B 确认流程：流程图/入口检测/AI 动作序列增加 [确认不切分]/[仍需切分] 分支；4.3.3 补充变体 B 格式说明；4.5 字段联动表覆盖"不切分已确认"场景；自检清单新增变体 B 检查项；文本化文档命名加注 .md 源差异 |
| 2026-06-06 11:44 | v0.2 | 全域术语统一"切片"→"切分"，对齐 ort00 v0.3 术语体系 |
| 2026-06-06 10:11 | v0.1 | 初始构建——从原预处理链中拆分出切片执行步骤，作为预处理链第二步（ort01） |
