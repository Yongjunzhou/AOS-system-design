# 原始需求澄清

**Skill ID**: eos-ort02-clarify · **Step ort02-clarify: Requirement Clarification**

> **概述**：对已切分的原始需求材料执行澄清迭代。每次运行时检测正文区状态——无模糊标注时执行首轮全量标注；有 `[✓已澄清]` 注释时按注释生成新正文并归档旧版，对新正文执行三项条件评估；有未解决标注但无新注释时输出迭代报告等待人类操作。本步是人类-in-the-loop 步骤，人类通过在正文区追加 `[✓已澄清]` 注释驱动流程。
>
> **输入 → 输出**：切分文件（`-sub-{seq}.md`，status=raw）+ OR 状态表 → 已澄清切分文件（同文件，正文区为最新版本，status=clarified）+ OR 状态表状态更新（生命周期→已澄清 / 保持澄清中 / 已废弃）
>
> **接口设计**：本步靠正文区状态驱动——无标注时走首轮标注路径；有 `[✓已澄清]` 时走迭代路径；有 `[⨯]` 但无 `[✓已澄清]` 时等待。上游 eos-ort01-chunk 产出切分文件（status=raw）。下游 eos-ort03-norm 读取"生命周期=已澄清"的记录。
>
> **版本**：v0.1 | **修订**：2026-06-07

---

## 一、设计角色

本步在设计链中承担的角色：
- **输入**：`../../80-pl4eos-2-eosdata/_inputs/10-source-materials/` 目录中的切分文件（`-sub-{seq}.md`）+ `../../80-pl4eos-2-eosdata/_inputs/01-eos-or-lifecycle.md`
- **产出**：已澄清的切分文件（同文件，正文区为最新纯净版本，status=clarified）+ OR 状态表（生命周期→已澄清 / 保持澄清中 / 已废弃）

### 与三条工程化设计原则的关系

| 原则 | 在本步中的体现 |
|------|--------------|
| **详概分离** | 本步仅做需求澄清，不涉及任何分解或设计决策 |
| **文档结对** | 本步不产生文档结对关系 |
| **基于资产** | 所有迭代历史和旧版正文保留在历史区，可追溯每轮处理 |

### 三区结构说明

本步操作的文件（切分文件）的三区：

| 区域 | 内容 | 本步操作 |
|------|------|---------|
| **管理区** | 状态摘要、澄清决策记录 | 更新状态摘要（首轮标注完成/已澄清/保持澄清中/已废弃）；追加决策记录 |
| **正文区** | 原文 + 模糊标注 `[⨯/✓]`（临时）+ 模糊标注报告（临时） | 首轮标注：追加 `[⨯]` 标记+报告；迭代：生成新正文（纯原文），旧版归档至历史区；已澄清：正文区为纯原文（无标注） |
| **历史区** | 旧版正文 + 标注标记（迭代归档时写入） | 每次迭代归档旧版正文+全部标注标记 |

---

## 二、前置条件

| 条件 | 说明 |
|------|------|
| 输入 | `10-source-materials/` 目录中存在切分文件（`-sub-{seq}.md`）；OR 状态表已就绪 |
| 参考 | 通用系统设计规范 §7.3 阶段一（原始需求澄清——模糊子类型分类、已澄清判定标准）|
| 前提 | 输入材料必须已通过 eos-ort01-chunk 完成切分（切分文件 status=raw 或已标记的文件）。新材料应先运行 eos-ort01-chunk 处理 |

---

## 三、操作流程

核心驱动逻辑：读取 OR 状态表中生命周期=已登记/澄清中的记录，检测正文区状态——无模糊标注时执行首轮全量标注（五种子类型），写入 `[⨯]` 标记和模糊标注报告；检测到 `[✓已澄清]` 注释时根据注释生成新正文，将旧正文+全部标注标记转入历史区，对新正文执行三项条件评估；无 `[✓已澄清]` 但仍有 `[⨯]` 时输出迭代报告等待人类操作。

本步按"扫描分流 → 正文区检测 → 首轮标注/迭代生成+评估 → 写入 → 行动清单"的线性序列执行。

操作流程概览：

```
[前置校验] 检查上游前置条件
  ├─ 01-eos-or-lifecycle.md 不存在 → 输出"请先运行 eos-ort00-textualize" → 结束
  ├─ 10-source-materials/ 中无 `-sub-*.md` 切分文件 → 输出"请先运行 eos-ort01-chunk" → 结束
  └─ 全部就绪 → [扫描分流] ↓

[扫描分流] 读取 OR 状态表 + 切分文件
  ├─ 人类决策=已终止 → 更新生命周期=已废弃，追加废弃说明，跳过
  ├─ 生命周期=已登记（status=raw）→ [首轮标注] ↓
  ├─ 生命周期=澄清中 → [正文区检测] ↓
  ├─ 生命周期=已澄清 → [批次选材] ↓（§3.4）
  └─ 其他 → 跳过

[正文区检测] 扫描正文区标注标记
  │
  ├─ 无任何标注标记（首轮进入）→ [首轮标注] ↓
  │
  ├─ 发现 [✓已澄清] 注释 → [迭代生成+评估] ↓
  │   ├─ [归档] 旧正文 + 所有标注标记 → 历史区（版本 #N）
  │   ├─ [生成] 按注释更新正文为新版本（纯原文）
  │   └─ [评估] 三项条件判定 ↓
  │        ├─ 清晰 → 状态→已澄清，status→clarified
  │        └─ 仍有模糊 → 追加新 [⨯] 标记 → 保持澄清中
  │
  ├─ 有 [⨯] 未解决，无 [✓已澄清] → 输出迭代报告，保持澄清中
  │
  └─ 无任何标记（异常）→ 检查 status，如=clarified 跳过

[写入] 更新管理区 + OR 状态表
  状态摘要、frontmatter status、OR 状态表

[批次选材] 扫描已澄清材料 → 输出摘要供人类确认（§3.4）

[行动清单] 按状态分组输出行动项
    ↓

[人机交互] 询问人类是否打开登记表
  ├─ 回答"是"→ 输出路径
  └─ 回答"否"→ 跳过

→ 本轮结束
```

### 3.1 首轮标注

AI 通读切分文件正文区，全量标注模糊项。

| # | 操作 | 说明 |
|---|------|------|
| 1 | 通读完整文档 | 从头到尾通读，在完整语境中判断哪些部分需要澄清 |
| 2 | 全量模糊标注 | 按五种模糊子类型标注全部发现。每种标注包含：子类型、具体问题描述、建议澄清方向 |
| 3 | 输出模糊标注报告 | 按模板输出，列出本轮全部新增模糊项、三项条件判定结果 |
| 4 | 更新 frontmatter status | status=raw → status=annotated |

**五种模糊子类型**：

| 子类型 | 适用场景 | 标注示例 |
|--------|---------|---------|
| **术语模糊** | 非量化表述需明确度量方式 | `[⨯术语模糊: 高效→需量化处理能力指标]` |
| **主体/客体不明** | 角色或对象不可识别 | `[⨯主体不明]提出该需求` |
| **边界模糊** | 条件或指标缺少边界 | `[⨯边界模糊: 实时→需明确具体延迟上限]` |
| **信息缺失** | 缺少元信息 | `[⨯信息缺失→缺来源日期和提出方]` |
| **多重模糊** | 同处存在两种以上类型，按优先级分层 | `[⨯多重模糊: 术语+边界→非量化+缺时限]` |

**标注格式**：

```
[⨯子类型: 原文片段→问题说明]
```

- 状态：`⨯` 未解决 / `✓` 已解决
- 子类型：必填
- 原文片段：推荐保留
- 问题说明：推荐保留，类型自明时可省

**首轮标注后状态摘要**：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
文件状态
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
阶段：首轮标注完成
AI建议：需澄清
待处理：N 项模糊待解决（见行内 [⨯] 标记）
操作指引：追加 [✓已澄清: ...] 注释后重启 eos-ort02-clarify
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

无模糊项时（AI建议=可确认）：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
文件状态
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
阶段：首轮标注完成
AI建议：可确认
待处理：无
操作指引：运行 eos-ort03-norm 进行规范化
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 3.2 迭代生成+评估（检测到 `[✓已澄清]` 时）

| # | 操作 | 说明 |
|---|------|------|
| 1 | 读取所有 `[✓已澄清]` 注释 | 扫描正文区，收集所有 `[✓已澄清: ...]` 注释内容 |
| 2 | 生成新正文 | 根据注释内容将模糊表述替换为精确值，生成新版本正文（纯原文，无标注） |
| 3 | 归档旧版本 | 旧正文 + 全部标注标记（`[⨯]`/`[✓]`）→ 历史区（版本 #N） |
| 4 | 更新正文区 | 替换为新版本（纯原文） |
| 5 | 三项条件评估 | 通读新正文，判定三项条件 |

**三项条件**（引用通用规范 §7.3 阶段一(d)）：

1. 正文内容清晰完整（所有非量化表述已替换为精确值）
2. 所有历史标注的模糊项已在归档前的正文中有对应的 `[✓已澄清]` 注释
3. 无新增的模糊标注产生

**评估分支**：

| 条件 | 操作 |
|------|------|
| 三项全满足 | 状态→已澄清，frontmatter status→clarified，输出完成报告 |
| 仍有未清晰项 | 在新正文对应位置追加 `[⨯...]` 标注，生命周期保持澄清中，输出迭代报告 |

**归档格式**（历史区，倒序，最新在最上）：

```
## 历史版本

### 版本 #2 · 2026-06-07 · 第2轮澄清
- 当次正文：（全文）
- 当次标注：[⨯...] / [✓...] 列表
- 人类注释：[✓已澄清: ...] 内容

### 版本 #1 · 2026-06-06 · 首轮标注
- 当次正文：（全文）
- 当次标注：[⨯...] 列表
```

### 3.3 写入：更新管理区 + OR 状态表

| # | 操作 | 说明 |
|---|------|------|
| 1 | 更新状态摘要 | 按分支写入：已澄清/已废弃/保持澄清中 |
| 2 | 更新 frontmatter status | 仅已澄清时 status→clarified |
| 3 | 更新 OR 状态表 | 生命周期转迁 + AI 建议 + last_size + iteration_count + unresolved_count |
| 4 | 追加决策记录 | 文档达到已澄清或已废弃时追加 |

**已澄清状态摘要**：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
文件状态
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
阶段：已澄清
AI建议：可确认
待处理：无
操作指引：在 OR 状态表中设人类决策=已确认后运行 eos-ort03-norm
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**保持澄清中（需澄清）状态摘要**：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
文件状态
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
阶段：澄清中
AI建议：需澄清
待处理：N 项模糊未解决（见行内 [⨯] 标记）
操作指引：追加 [✓已澄清: ...] 注释后重启 eos-ort02-clarify
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**已废弃状态摘要**：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
文件状态
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
阶段：已废弃
AI建议：建议终止
待处理：无
操作指引：该文件不再进入下游处理
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 3.4 批次选材

扫描所有"生命周期=已澄清"且"人类决策=空"的记录，输出批次选材摘要供人类到 OR 状态表中确认：

```
══════════════════════════════════════════
  批次选材摘要 · 供 eos-ort03-norm 规范化
══════════════════════════════════════════

已澄清材料（可送入规范化）：

| # | 源文件 | 摘要 | 状态 |
|---|-------|------|------|
| 1 | [文件] | [核心内容一句话] | 候选纳入 |

➡️ 请到 OR 状态表中审查以上材料：
   1. 决定纳入本批的 → 设 人类决策=已确认
   2. 决定暂不入本批的 → 保持 人类决策=空（留待下批）
   3. 决定废弃的 → 设 人类决策=已终止
   4. 完成后启动 eos-ort03-norm
```

### 3.5 行动清单模板

```
══════════════════════════════════════════
  eos-ort02-clarify 本轮完成 · 行动清单
══════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 需追加 [✓已澄清] 注释
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
在以下文档的 [⨯] 标注旁追加 [✓已澄清: ...] 注释，然后重启 eos-ort02-clarify：

├─ [文档A] — N 项模糊待解决
└─ [文档B] — N 项模糊待解决

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 候选纳入规范化
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
到 OR 状态表中将以下材料的 人类决策 设为 已确认，然后启动 eos-ort03-norm：

└─ [文档C] — 已澄清

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 未解决督促（≥3 轮）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
└─ [文档D] — 已迭代 N 轮，未解决 M 项，最后更新 YYYY-MM-DD
```

AI 随后发起对话交互：

```
▲ AI 独立执行段结束

OR 状态表已更新，每份材料已输出本轮迭代结果或状态转迁。

需要打开登记表查看详情吗？
（回答"是"→ 直接打开；"否"→ 结束本轮）
```

人类回答"是"后，AI 直接提供可打开的文件路径：

```
登记表路径：../../80-pl4eos-2-eosdata/_inputs/01-eos-or-lifecycle.md

建议关注行：
├─ 记录ID 行 → 生命周期状态、AI 建议、迭代轮次
└─ 切分状态表 → 当前切分处理状态（仅切分材料）
```

---

## 四、AI 执行指南

### 4.1 正文区检测规则

AI 在入口分流中检测正文区状态：

| 正文区状态 | 判定 |
|-----------|------|
| 无任何 `[⨯]` 或 `[✓]` 标注标记 | 首轮标注（frontmatter.status=raw 或无标注记录） |
| 存在 `[✓已澄清: ...]` 注释 | 迭代路径 → 归档→生成→评估 |
| 存在 `[⨯]` 标记，无 `[✓已澄清]` 注释 | 输出迭代报告，等待人类 |
| 无任何标注标记且 status=clarified | 跳过（已澄清终态） |

### 4.2 标注规范

**标注纪律**：

1. **标"哪里不清楚"，不标"应该怎么做"**：即使发现明显内部矛盾，也只标注矛盾位置和双方表述
2. **先通读后标注**：通读全文后再逐处标注，避免局部视角导致重复或遗漏
3. **一条只标一个点**：同一处不同维度的模糊合并为一条多重模糊
4. **报告与内联一致**：标注报告中列出的每项对应文档中的一处内联标注

**省略规则**：

| 省略内容 | 条件 | 示例 |
|---------|------|------|
| 原文片段 | 模糊位置自明（如独立成行的信息缺失） | `[⨯信息缺失→缺提出方]` |
| 问题说明 | 子类型+原文片段已足够自明 | `[⨯术语模糊: 智能化]` |
| 不可省略状态和子类型 | — | — |

### 4.3 启动方式

#### 人类启动指令

> 开始原始需求澄清（或 eos-ort02-clarify）

#### AI 动作序列

0. **前置校验**：检查上游前置条件。**逐级报错，首次即停**：
   - `../../80-pl4eos-2-eosdata/_inputs/01-eos-or-lifecycle.md` 不存在 → 输出"请先运行 **eos-ort00-textualize**" → **结束**
   - `10-source-materials/` 中无 `-sub-*.md` 切分文件 → 输出"请先运行 **eos-ort01-chunk**" → **结束**
   - 全部就绪 → 进入步骤 1
1. **扫描分流**：读取 OR 状态表 + 切分文件 frontmatter status
2. **正文区检测**：扫描行内标注标记
   - **无标注且生命周期=已登记** → 步骤 3（首轮标注）
   - **有 `[✓已澄清]`** → 步骤 4（迭代生成+评估）
   - **有 `[⨯]` 无 `[✓已澄清]`** → 输出迭代报告，跳至步骤 7
   - **无标记且 status=clarified** → 跳至步骤 6（批次选材）
   - **人类决策=已终止** → 废弃处理
3. **首轮标注**：通读全文 → 全量模糊标注 → 写入 `[⨯]` 标记 → 输出模糊标注报告 → 更新 status=annotated → 跳至步骤 5
4. **迭代生成+评估**：读取 `[✓已澄清]` 注释 → 生成新正文 → 归档旧版+标注到历史区 → 更新正文区 → 三项条件评估 → 清晰→已澄清；模糊→追加新 `[⨯]`
5. **写入**：更新状态摘要 → 更新 frontmatter status → 更新 OR 状态表（生命周期/AI 建议/last_size/iteration_count/unresolved_count）
6. **批次选材**：扫描已澄清材料，输出摘要
7. **输出行动清单**
8. **人机交互**：询问人类是否打开登记表

---

## 五、模板与格式

### 5.1 首轮标注报告模板

```
══════════════════════════════════════════
  模糊标注报告（供人类审查）
══════════════════════════════════════════

文档：[文件名]
本轮标注日期：[YYYY-MM-DD]
本轮 AI 建议：[需澄清 / 可确认 / 建议终止]

本轮发现模糊项（N 项）：
├─ 术语模糊：[清单]
├─ 主体/客体不明：[清单]
├─ 边界模糊：[清单]
├─ 信息缺失：[清单]
└─ 多重模糊：[清单]

三项条件判定（首轮）：
├─ 条件1（文本化完成）：✅
├─ 条件2（旧模糊已澄清）：不适用（首轮）
└─ 条件3（无新增模糊）：✅ / ❌
```

### 5.2 迭代报告模板

```
══════════════════════════════════════════
  澄清迭代报告（供人类审查）
══════════════════════════════════════════

文档：[文件名]
本轮标注日期：[YYYY-MM-DD]
本轮 AI 建议：[需澄清 / 可确认 / 建议终止]
迭代轮次：第 [N] 轮

本轮新增模糊项（N 项）：
├─ 术语模糊：[清单]
└─ ...

历史遗留未解决模糊项（N 项）：
├─ [清单]

三项条件判定：
├─ 条件1（正文清晰完整）：✅ / ❌
├─ 条件2（旧模糊已澄清）：✅ / ❌
└─ 条件3（无新增模糊）：✅ / ❌
```

### 5.3 废弃说明块

```
> **【废弃说明】**
> | 项目 | 内容 |
> |------|------|
> | **废弃时间** | [YYYY-MM-DD] |
> | **决策人** | [姓名] |
> | **废弃原因** | [说明] |
```

### 5.4 自检清单

- [ ] 正文区检测已正确执行（无标注→首轮标注 / 有 [✓已澄清]→迭代 / 有 [⨯] 无 [✓已澄清]→报告）
- [ ] 首轮标注覆盖了所有模糊项，标注子类型分类正确
- [ ] 标注纪律已遵守（标不清楚不标方案 / 一条只标一个点）
- [ ] 迭代生成：已正确读取 `[✓已澄清]` 注释并生成新正文
- [ ] 迭代归档：旧版正文+全部标注标记已完整转入历史区
- [ ] 三项条件已逐项判定
- [ ] 状态摘要已按分支正确更新（已澄清/已废弃/保持澄清中）
- [ ] OR 状态表已更新（iteration_count++，unresolved_count）
- [ ] 批次选材已执行（仅标记已澄清+人类决策为空）
- [ ] 督促信息已列出（如有≥3 轮记录）
- [ ] 人机交互已执行（输出登记表路径及建议关注行，询问是否打开）

---

## 六、产出物

| 文件 | 说明 | 路径 |
|------|------|------|
| 已澄清切分文件（原地更新） | 正文区最新纯净版本，status=clarified | `../../80-pl4eos-2-eosdata/_inputs/10-source-materials/{原始文件名}-sub-{seq}.md` |
| OR 状态表更新 | 生命周期转迁 + 批次选材标记 | `../../80-pl4eos-2-eosdata/_inputs/01-eos-or-lifecycle.md` |

---

## 七、变更日志

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-06-07 | v0.1 | 初始构建——合并原 ort02（首轮标注）和 ort03（澄清迭代）；采用三区模型 + 统一循环（检测正文区状态决定路径）；无变更记录表，改为正文区标注标记 `[⨯/✓已澄清]` 驱动；旧的 [确认通过]/[确认终止] 选项改为 OR 状态表人类决策字段 |
