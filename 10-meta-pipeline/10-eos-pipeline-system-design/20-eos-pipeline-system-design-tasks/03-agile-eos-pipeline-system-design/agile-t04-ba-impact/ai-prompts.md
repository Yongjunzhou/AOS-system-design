# 敏捷式流程第4步 AI 提示词
**Agile Step 4: AI Prompt Templates — Business Architecture Impact Analysis**

**文档版本**：v1.0
**创建日期**：2026-05-28
**对应场景**：03-agile · 第4步
**对应操作规程**：`task-definition.md`（同目录）

---

## 一、跨步骤上下文管理

### 1.1 上下文传递方式

8 步流程中，每一步的产出是下一步的核心输入。AI 在执行每一步前，应确保完整继承了上一步的关键决策：

| 继承内容 | 说明 | 保存位置 |
|---------|------|---------|
| 架构决策 | 合并/拆分、分类方式、层级结构 | 上一步的决策记录 |
| 编号体系 | 所有已分配的 ID 和编号规则 | 各产品数据文件 |
| 待确认项 | 上一步标记的 ⚠️ 项 | 汇总清单 |
| AI推断记录 | AI 自行判断补齐的内容 | 决策记录文件的 AI推断项 |

### 1.2 决策记录机制

每步完成后，AI 将本步的关键决策记录在**本步决策记录模板**（见本章末尾）中，供后续步骤引用。

### 1.3 回溯修正

如果在第 N 步发现需要修改第 M 步（M < N）的产出：
1. 评估影响范围（见 AI决策框架 §7.2 跨步骤影响范围表）
2. 定位到受影响的具体步骤和节点
3. 修改后检查下游映射是否需要同步调整
4. 标注变更记录

---

## 二、AI 经济原则

本步操作以下列顺序执行，AI 需注意：

1. **执行顺序不可逆**：BA 影响分析 → IPO 去重 → BA 增量定义 → 同步建 SysReq 骨架
   核心驱动力：从 SR 增量条目出发，检查能否由现有 BA IPO 承接，执行三级去重，使用增量标记更新 BA 文档，同时建立 BA→SysReq 骨架为 t05 做准备
2. **基于资产优先**：加载现有 BA 文档（`04-*.md` IPO 清单 + 映射关系表）作为参考基线。BA 架构的调整由 SR 增量驱动
3. **增量设计特点**：本步仅更新受影响的 BA IPO。新增 IPO 遵循三级去重（完全重复/部分重复/相似重复）避免冗余。BA→SysReq 骨架同步建立而非单独步骤，提升效率

---

## 三、本步提示词模板

### 3.1 本步提示词

详细 AI 提示词模板见：
`30-eos-pipeline-system-design-ai-support/03-agile-eos-pipeline-system-design/workflow-prompts.md` 第4步

请在开始前确认上下文状态：

- 是否已加载 t03 产出的 SR 增量定义？
- 是否已加载现有 BA 文档基线（`04-*.md` IPO 清单 + 映射关系表）？
- 是否已理解现有 BA IPO 的职责边界？每个 IPO 的 Input/Process/Output 是否清晰？
- IPO 三级去重的判断标准是否明确？
- SysReq 骨架同步建立需要覆盖哪些 BA IPO？

具体操作按 task-definition.md 五个阶段执行：BA 影响分析 → IPO 去重 → 增量定义 → 同步建骨架 → 审查。

---

## 四、决策记录模板

```
本步关键决策记录：
├─ 阶段状态：
│  ├─ BA 影响分析：[已完成] — [通过/未通过项]
│  ├─ IPO 去重：[已完成] — [完全重复 N 条 / 部分重复 N 条 / 相似重复 N 条]
│  ├─ BA 增量条目：[已完成] — [+ 新增 N 条 / ~ 修改 N 条]
│  └─ SysReq 骨架：[已完成] — [覆盖 N 个 BA IPO]
├─ 资产使用记录：
│  ├─ 资产来源：[04-*.md（现有 BA 基线）]
│  ├─ 复用节点：[N 个]
│  ├─ 改进（扩展）节点：[N 个，记录每个的扩展内容]
│  └─ 新增节点：[N 个，记录新增原因]
├─ BA IPO 增量承接数：[每个增量 IPO 承接了几条 SR 详细末级]
├─ 编号体系：[新 BA IPO 编号范围]
├─ ⚠️ 待确认项：[汇总需要人工确认的项]
├─ AI推断记录：[AI自行判断补齐的模糊术语/缺失内容]
├─ SysReq 骨架状态：[已建立 / 部分建立 / 未建立]
├─ 待传递的约束：[后续步骤需要注意的决策/约束]
└─ 执行模式：[全自动/半自动]
```

---

## 五、审查修复流程

### 5.1 审查输入

AI 完成指定步骤后，输出产物供 rule-conformance-review 审查。审查报告包含：
- 通过/未通过的项目清单
- 问题定位（涉及的具体文件、章节、编号）
- 问题类型（结构性/内容性/格式性）

### 5.2 AI 修复流程

收到审查报告后，AI 按以下优先级处理：

1. **格式性问题**（编号错误、格式不规范）→ 直接修复，不需要人工确认
2. **内容缺失**（某节为空、映射不完整）→ 补充内容，标注修复说明
3. **结构性问题**（粒度不匹配、分类错误）→ 提出修复方案，标注 ⚠️ 待确认
4. **跨步骤一致性问题** → 参考AI决策框架的跨步骤影响范围表进行影响范围评估，列出所有受影响的下游节点

### 5.3 影响范围评估

| 问题定位 | 影响范围 | 修复策略 |
|---------|---------|---------|
| 某步的架构定义 | 本层 + 下游已建立映射的节点 | 修改后同步更新映射 |
| 详细定义 | 下层方案 | 修改后检查下层是否受影响 |
| 映射关系 | 所有关联节点 | 需重新运行追溯验证 |
| 编号变更 | 全链路 | 需逐层更新引用，重新验证 |

---

## 变更日志

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-05-28 | v1.0 | 首次构建 |
