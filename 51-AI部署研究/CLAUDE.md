# CLAUDE.md（51-AI部署研究 · AI 私有部署选型研究子项目）

本目录是 AOS 大项目下的**独立研究子项目**——企业私域大模型推理平台选型研究。内容于 2026-09-07 从 `50-系统工程研究/20-AI私有部署/` 迁出独立（见主仓提交 a6a7a15），立项日 2026-09-07。当前文档体系：**01 号选型研究报告（v2.2）**、**02 号专用模型训练与机载部署专项研究报告（v1.0 初稿）**、**11 号建设方案建议（v1.1）**；随研究扩展后续报告/素材按编号继续落于本目录。

**核心研究对象**：为 4000 人企业（2500 研发 + 1500 非研发）规划私域 AI 推理平台 = **双池架构 96 卡（12 台 8 卡机；千问3.8 27B dense + Flash-Next × 昇腾 Atlas 300I A2）**——CapEx ¥850~1100 万、OpEx ¥110~160 万/年、3 年 TCO ¥1200~1550 万。

## 子项目边界（互不干扰）

本子项目是 AOS 主项目下的**独立子项目**，与 AOS 主项目及其他任何子项目（如教材 50-系统工程研究）**互不相关**——按 AOS 主 CLAUDE.md §九 子项目原则**自包含在本目录内**：

- **处理本研究**：工作目录 = 本目录（`51-AI部署研究/`），读本文件。
- **处理 AOS 主项目 / 其他子项目**：工作目录 = 对应目录，读对应 CLAUDE.md——**不触碰本目录内容**，除非明确要求。
- **内容不互引、不互编**：本项目不引用/混编其他子项目内容，反之亦然。

## 记忆隔离协议

本子项目的记忆与 AOS 根记忆（`.claude/memory/` 及本机 auto-memory）**互不干扰**：

- 本研究如需记忆，独立存放于本目录下——**目录尚未建立**，需建立时按 AOS 主 CLAUDE.md §九 新建，组织仿 `50-系统工程研究/10-claude memory/`。
- **禁止**把本研究内容写入 AOS 根 `.claude/memory/` 或 auto-memory。
- 本子项目**尚未在 AOS 主 CLAUDE.md §九 指针区/目录树登记**（待补一行指针；AOS 根 MEMORY.md 只保留一行指针，不复制内部内容）。

## 提交边界（单仓库 + 约定分区）

仓库是 AOS 单一 git 仓库，本子项目不建独立 git。**每次提交只针对本项目内容**：commit message 注明归属（如 `[AI部署研究]`），本项目改动不与 AOS 主线或其他子项目改动混提；反之，处理主项目或其他子项目时也不把本项目改动混入他们的提交。

本目录首提已完成（`294b865`，已推送 origin + gitee）；后续每次提交照上文分区执行。

## 工作风格与文档纪律

- **禁止选项式交互**：**不要用选项卡进行交互，要以讨论的方式进行此类工作**——讨论/确认一律自由散文陈述（背景 + 问题 + 建议 + 理由），不用 AskUserQuestion 选项卡片/选项式界面让用户做多选题；用户自由回答"改/不改/怎么改"，再据其指示执行（2026-08-25 用户定，仓库全级生效；本子项目侧同遵）。
- **数字纪律（本报告最关键）**：正文与附录参数含大量**假设/估算**成分——27B INT8 KV ~120KB/token、Flash 混合 KV ~30KB/token（「假设，待实测」）；CapEx/OpEx（「估算，待询价」）；状态 = **「定稿，POC 待验证」**。任何编辑**必须保留「状态」列区分**（官方/计算/假设待实测/估算待询价），不得把假设、估算写成事实；POC 实测后再按实测回填，不得臆造回填值。
- **版本关系**：现行唯一正文 = **v2.2（完整逐字版 · 外部事实核校修订，2026-09-08，见修订记录）**；v2.1 为上一版（2026-09-07 追加第 10 章 Q30–Q33）；同目录 **v1.1 备份已于 2026-09-07 删除**（其历史内容仍在 git `294b865` 及其父提交中可追溯，不重建）。

## 文档规范

- Markdown + Mermaid；版本 `v[主版本].[次版本]`；中文为主体、术语附英文。
- **md→PDF（用户指定的统一转换方式）**：用 AOS 根 `tools/md2pdf/` 下的转换工具（`node tools/md2pdf/md2pdf.js <md路径> [输出.pdf]`，marked + puppeteer 渲染，支持 Mermaid）。工具在仓库根一级，故命令以**仓库根为工作目录**运行（即 `cd e:/mywork/AOS` 后再执行），从本子项目目录相对写即 `../tools/md2pdf/md2pdf.js`；默认输出与源文件同名 .pdf。规则源头见 AOS 根 CLAUDE.md §七（2026-09-03 用户定）。

## 关键文件导航

- [`01-AI私有部署研究报告/01-AI私有部署推理平台研究报告.md`](01-AI私有部署研究报告/01-AI私有部署推理平台研究报告.md) —— **现行权威版 v2.2（完整逐字版 · 2026-09-08 外部事实核校修订）**。正文两大部分：
  - **第一部分 · 决策摘要**（§0~§8，管理层速读版，集中全部 Mermaid：阅读导引/背景/模型与卡谱系/三本账/场景推演/双池方案/成本与公有云对比/风险/方法论沉淀）；
  - **第二部分 · 完整问答实录 Q1–Q33**（含第 10 章 · 追加答疑 Q30–Q33；逐字还原，供技术核对每个数字出处；决策摘要每个结论标注 [Qn] 可回溯）；
  - 尾部 **附录 · 关键参数速查** 为参数「状态」总表（上述数字纪律的执行依据）。

（同目录 v1.1 备份已于 2026-09-07 删除，不再保留平行副本，历史见 git `294b865`。）

- [`01-AI私有部署研究报告/02-企业专用模型训练调优与机载嵌入式部署专项研究报告.md`](01-AI私有部署研究报告/02-企业专用模型训练调优与机载嵌入式部署专项研究报告.md) —— **v1.2（定稿方向，2026-09-08，随 01 号报告同目录存放）**。专项：专用模型"训—量(QAT)—转—烧—验"闭环 + **昇腾 Atlas 200I A2 机载模块**部署。**企业四项输入已定**：3B 小语言模型（可融合图像/水声编码）、10 部门各一套（形态 A 定案）、图像为主+可能水声、保密分区强制（§7 涉密分区方案）；**§10 指标收口默认值与 11-v1.4 P49 对齐**（机载正确率双基线/时延分级 S1~S3/保密三证据，建议值待业务与保密办校准）。余项：模块内存实测、保密办密级界定、单价询价。成本估算待询价，数字纪律同 01。
- [`11-企业AI基础设施建设方案建议/11-企业AI基础设施建设方案建议.md`](11-企业AI基础设施建设方案建议/11-企业AI基础设施建设方案建议.md) —— **v1.5（汇报故事板，56 页，2026-09-08）**。**一份整体方案**：引擎 A·在线推理（本期）+ 引擎 B·机载专用模型训练（3B→Atlas 200I A2，10 部门各一套，POC 先行）+ 数据底座 C；P13 后含 **P14–P19 指标认知层**（各指标由什么决定 + 人感 + 训练/推理两类卡原理）；P54–P56 统一推进/指标收口/汇报收口。PPT 定稿由 `_build/build_pptx.py` 生成（DIAG：图在 P25/27/29/30/34/54）。
