---
name: ch4-ch5-rename-and-abr-ch4-rebuild
description: 定案：SKILL 指南第四五章章名成对改名（输入到输出的过程及规则／执行步骤及检验规则）＋ biz01abr 第四章重建成「对应—过程—并入」三节，六步成小节（2026-09-18）
metadata:
  type: project
---

**人类 2026-09-18 逐轮定**。落笔 = 93 第 64 版 ＋ biz01abr 第 64 版／abp 第 11 版／cr 第 10 版；提交 `55010300`，github 与 gitee 两仓已推。

## 一、章名成对改名

| 章 | 旧 | 新 |
|----|----|----|
| 四 | 输出与输入转换规则 | **输入到输出的过程及规则** |
| 五 | 转换流程及检验规则 | **执行步骤及检验规则** |

两条理由：**词序反**（"输出与输入"读着逆着 X→Y 的方向）；**「转换规则」看不出这一章讲的是"X 怎么一路变成 Y"**。而「过程」这个字原被第五章占着（93 §3.5 定位首句「设计过程，即本任务的 Step 序列」），故**成对改**——第五章那个「转换流程」本来也不准，它讲的不是转换的流，是一次运行的序（93 §2.1 章序逻辑自己写的是"五章给'执行与验收'"）。

落点六处（93 §2.1 骨架表两行／§2.2 组织原理一句（"流程及检验"→"执行步骤及检验"）／§3.4 节题／§3.5 节题／§5.1 映射表两行）＋ 三试点章题。正文里「归第四章／第五章」的引用只写号不写名，一处没动。

## 二、93 §3.4 的禁含收窄（这是本轮的关键解锁）

旧禁含：「**流程步骤顺序，属第五章**」——正是这条把"转换过程"也挡在第四章外，逼得第四章只剩一堆互不相干的判定。

新禁含：「**一次运行的 Step 序列**，即载入、反馈处理、写回、Start 与 End 两端，属第五章」。

并补应含第一项：「**转换过程**：X 走到 Y 的步骤顺序——分几步、每步做什么、每步在 Y 的哪一级上落，把随后各段规则串成一条路。**步骤止于 Y 落成**；一次运行的 Step 序列属第五章。」

## 三、biz01abr 第四章：三节

```
4.1 X 与 Y 的对应      表4-1（X 三级 ↔ Y 七级）＋两注
4.2 转换过程           六个小节 4.2.1~4.2.6（与第五章 #### 5.1.N Step N 同层级）
                       表4-2 干枝由属性落地、表4-3 末三级的落位 就地嵌在第二、四步
4.3 并入已有 Y         表4-4 边界匹配检查、表4-5 业务链关系检测
```

六步：取根上的三个属性／长干枝／聚出 ③ 角色职责／把末三级挂到第五级／整这条 E2E职责／并不完时出缺口。

原四段（归拢与长枝规则、要素生成判定、末三级落位规则、缺口规则）**全部并入六步**——要素生成判定表那八行本各是某一步的产出（E2E职责类型归第二步、任务类型与用例场景归第四步、封面模式/正文实体类型/支撑引擎/文档清单归第五步），单列一张表就是同一批事实说两遍。

## 四、这一轮推出来的"过程"实质

- **X 三级、Y 七级，错开四级**：X 的**根**（业务文档）带三个 R 侧属性 → 落 Y 的 ① ② ④；X 的三级节点 → 落 Y 的 ⑤ ⑥ ⑦；Y 的 ③ 由 ④ 聚类，X 里没有。**"X 只给值，不给干枝"**。
- **单件输入**：一次运行 = 一张业务文档及其下级节点。依据是第二章图2-1 里业务文档那行标着「**① 根**」——X 的树就是一张业务文档为根的树。故 §5.2 按此收口：删「同一批业务文档的主责角色不唯一」行、「本批」全改「本次」。
- **三章三套话收口**：94 §1.4「落位说」（整段搬入、只判落位）／表3-2「判档说」（合并或新建、复用→改进→新增）／§1.3「重整理说」（经合并或变更后重新整理全部下级）——**新建时是整段搬入（判档退化为全新增），撞上 Y 上已有同名文档时才逐节点判**。三说各对应一种情形，不是打架。
- **④ 是本任务的新建与修订单位 ＋ 人类确认挂点**（归拢条目、职责边界、收敛自查三项确认挂 ④ 上）。
- **③ 的时序在 ④ 之后**：树上位于 ② 与 ④ 之间，却要等 ④ 落定才聚得出；只有"AI 给聚类方案 ＋ 人类反馈修订建议或确认"一档，不进资产优先阶梯。

## 五、术语裁决（同轮）

- **坐标＝属性**：94 §2.2 同段里既说"五个坐标"又说"由同一批属性落地"，两词同义。第四章一律写"属性"。
- **清除自造词**：三跳、判档、新起（一张）、比对集、比对锚点——后四个纯我造，「比对锚点」还是从退役的 wft01 传下来的。
- 表4-1 三行一字不差的「同指，整段搬入」是常量，按"列不留常量、进正文"收进表下的注说全。

## 未落（等人类一句话或下一轮）

1. **表3-2 与第四章将双写"处置"**——表3-2 ⑥ 说"逐节点定复用→改进→新增"，第四章也说。干净的分法：**表3-2 管"这个节点在 Y 里怎么来、写到什么深度"（来历＋形态边界＋构建规则），第四章管"从 X 推它的判定动作"（判据＋比对范围＋处置档位）**。要动第三章。
2. **93 §3.4 其余五项仍是旧骨架**——「单承接，可 N:1 不可 1:N」「切不动则走横切式承接」对组装型本任务整项不适用（93 §3.5 自己就写着"叶由上游直接给出的步不设此项"）；「对齐规则：Y→关联产品数据的判定动作，如 Q 判定」也不适用（本任务 §6 已声明 R 树本身就是资产）。改完 93 §3.4 才算对上链上三种任务。
3. **「同一份业务输出的产品数据」的判据缺家**——表4-3 第三行是全章最吃重的判定，判据栏只能写到"语义与业务逻辑分析"。线索：它可能就是 X 侧「业务文档」这个节点本身的身份（表2-1 ① 的定义），本任务只是比对、不重判。
4. **「内联稳定标签」无家**——传出承诺第 3 条、三下游追溯、三份数据文件都靠它，没有单源说它长什么样、怎么保证"稳定"。
5. **91 §7.1 词汇过期**——本任务任务清单（任务 1~7）的出处仍是旧词汇（场景业务／bpl-biz／规范化的相关方需求）。是 [[pending-91-align-to-94]] 的第五处偏差，且恰是本任务的判据出处。
6. **abp／cr 的第四章结构未跟**——abr 的收法依赖逐条判断（abp 的 §4.4 是 91 §5.3.1 对齐判据、cr 的是引擎能力对照），不宜照搬。
7. **X 侧两级未建**（[[pending-01-align-to-94]]）——本章的输入单位就是"一张业务文档及其下级节点"，两级不建则 §5.2 末尾那句"按条目正文语义归拢、标 `[待复核]`"与单件输入相抵。

关联 [[ch3-y-tree-tables-and-rules]]、[[ch2-ch3-tabling-2026-09-18]]、[[ch2-structure-only-verify-in-ch5]]、[[skill-guide-ch1-writing-criteria]]、[[feedback-code-whitelist-2026-09-16]]、[[eos-design-landing-spec-93-refactor]]、[[pending-91-align-to-94]]、[[pending-01-align-to-94]]。
