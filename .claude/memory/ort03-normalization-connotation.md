---
name: ort03-normalization-connotation
description: ort03 拆分/质量规则已定案——类型先行按类型分治（FR-BIZ 场景为锚一场景可多条 OR / FR-ENG 引擎能力 / NFR 四元组原子），资产判定只到 23 输出产品；开放项 #1~#5 已处理，剩余链级衔接项（FR-ENG 同构/跨块/派生FR）
metadata:
  type: project
---

2026-08-17 新会话重点讨论 ort03「规范化」概念层——拆分规则与质量规则已落定（v0.40~v0.44），开放项逐一处理。

**已定案（ort03 人类方案 v0.41 基础 + v0.42~v0.44 开放项处理）**：
1. **类型先行、按类型分治**——先判 FR-BIZ/FR-ENG/NFR，再按各类型边界拆分：
   - FR-BIZ：**场景为锚、一场景可多条 OR**——场景需求可能被多个用户分别提出，成条依据 = 提出者 × 单一可确认性；**取消「能力单元」中间层**（因"能力"太抽象放弃，也避免场景内强制细分），场景内不设中间单元
   - FR-ENG：对一个平台引擎的一项能力诉求（25 资产锚定）
   - NFR：**四元组原子**——单一关注角色 × 单一分类维度（22 @nfr-xxx）× 单一适用对象线索，对齐 [91 §九 STR-NFR 归组四元组]
2. **资产判定只到 23 输出产品及其部件**（产品类型/组部件 Q1/Q2/Q3）——不再匹配 26 文档（输出文档在 STR 层由 wft01 确定）；22 从"候选线索"升级为"NFR 分类维度判定"（判 @nfr-xxx，无匹配标候选待写，**不写回 22**）
3. **五项标准按类型解读**（§2.2）：NFR 边界可判定 = 数值+单位+条件（应已在 ort02 澄清中由相关方确认，缺则标 `待确认` 退回 ort02）、可测试性 = 目标值可验证，统计口径/测量方法/验收条件由 wft04-nfr 展开
4. **NFR 链全链贯通**：ort01 v0.55 识别 NFR 适用对象线索（文本归属）；ort02 v0.27 澄清 NFR 目标值+属性/适用对象（缺少定量指标第六子类型，废除不量化）；ort03 v0.45 判四元组 + 完整性检查（缺则待确认退回）；wft01-nfr v5.3 归组记录相关方确认的目标值；wft04-nfr v5.3 承接目标值做系统侧指标设计

**开放项 #1~#5 处理结果（2026-08-17 逐一讨论定案）**：
- #1 **优先级 P0~P3 判定依据** → 已定案（v0.42/v2.10）：四档语义 + 顺序判定判据（P0 合规/安全强制或基础能力 / P1 核心闭环必要可分期 / P2 常规增强 / P3 可延后待定），边界声明只用于分批、不作交付承诺
- #2 **冲突检测层 D** → 已定案（v0.43/v2.11）：不做独立层 D，改 23 匹配加 `23-D` 语义抵触标注（OR 主张 vs 已确认资产矛盾，与 Q 并列追加，只检出不裁决，改资产触发下游重审）
- #3 **待确认/退回 ort02 反向路由** → 已定案（v0.44/v2.12 + ort02 v0.24/v2.9，ort03 v0.45 补「定量指标在原文层缺失」也是退回触发）：待确认分两类（条目不完整→ort03 自解 / 原文缺事实→退回 ort02）；表 B `人类决策=需退回澄清`、生命周期 `待确认规范方案→澄清中`、frontmatter.status→`raw`、清理 OR 基线 `#body-{seq}`，仅方案期；ort02 加重入入口（`澄清中`+`需退回澄清` 重新首轮标注）
- #4 **22 资产契约不一致** → 已定案（22 资产 v1.1，方案 A）：ort03 从「写候选」改「读 + 判 + 标注候选待写、不写回 22」——初步判定（`[推断]`）不写资产，进 22 的内容须经 wft01 评审；22 契约改 ort03 只读、wft01 承接评审写回；wft01-nfr v5.2 补承接句
- #5 **重复/唯一性检测** → **不做**（已评估边界）：语义查重模糊、误报成本高，由 ort00 逐字查重 + wft01 关系检测（无新增→增量合并）兜底；若将来 OR 基线出现明显重复困扰，按 ort00 记忆指引加"AI 语义初筛→人类确认"
- #6 **FR-ENG/STR-E 同构扩展** → **暂缓**（记边界）：FR-ENG 大多落在配置类场景（业务定义配置人员的业务对象闭环，ort01 现有闭环切分已覆盖）；无归属独立引擎描述少、走 ort01 结构锚点兜底；不像 NFR 有系统级误并危害；将来材料出现困扰再按 NFR 同构补（扫引擎关键词判适用对象→系统级独立成片）
- #7 **跨块诉求归属** → **暂缓**（记边界）：同一条需求跨两个切分块是 ort01 切分缺陷，靠 ort01 切分方案人类复核兜底；ort03 有隐式路径（检出→待确认→人类触发 ort01 `[重新切分]`）；跨块检测模糊不值得自动化，若实际困扰再补显式 `待确认（跨块诉求）` 标注
- #8 **NFR→FR 派生衔接** → **记为 wft01-nfr 后续增强**（ort03 不动）：通用规范把派生FR 归到 OR 分解（§3.1.1，wft01 层）；ort03 的 22 分类维度判定已提供维度锚点；将来做 wft01-nfr 时在其 §2.3 22 对齐加"维度有派生FR 时按附录B 生成 FR 条目路由 biz"
- C1 **92-srf 悬空引用 wft03a** → **已解决**（92-srf v1.1）：wft03a 旧模型已废弃（归档 legacy-a-b-pairs），三处引用改现行 `eos-wft03-biz-bpd2sfh` / `eos-wft03-eng-bpd2sfh`

**仍开放（C 组记忆待办）**：
- C2 记忆库 ba-* 术语待更新（pending-memory-ba-bp-term）

**How to apply:** ort03 v0.45/v2.13、ort01 v0.55/v2.37、ort02 v0.27/v2.12、22 资产 v1.1、wft01-nfr v5.3、wft04-nfr v5.3、91 规范 v5.18 已落地。相关：[[ort-review-style-pattern]] [[ort01-chunk-align-scene-business]] [[ort02-clarify-question-list]] [[ort00-dedup-design-decisions]]
