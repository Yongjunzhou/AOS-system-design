---
name: pending-01-align-to-94
description: 挂账：01 相关方需求（功能需求部分）需按 94 §2.1.1 重建——补三级树、五个属性、状态取值；人类定"后续要调整"（2026-09-17）
metadata:
  type: project
---

**待办（人类定 2026-09-17：「后续要调整相关方需求中的功能需求部分，以符合当前的定义」）**——`20-pl4eos/80-pl4eos-2-eosdata/01-eos-specified-requirements.md` 需按 [94 §2.1.1](../../20-pl4eos/10-pl4eos-subpl-sysdev/10-wfsysdev-4-eos/94-eos-sys-dev-dbs.md) 重建。**启动须人类点头。**

## 现行与目标的落差

现行文件（v3.2）的形态是**角色分片 → spr-NNN 正文块**，块内字段为 类型／优先级／所属角色／当前状态／来源材料／原始表述／下游映射／确认状态。

目标形态（94 §2.1.1）：

```
相关方功能需求
└── 业务文档 ─ ① 根　所属业务域 × 所属主责角色 × 所属E2E职责 × 所属产品类型 × 所属产品构件（五个坐标作属性）
    └── 任务节点 ─ ② 该业务文档开发所对应任务之步骤
        └── 用例场景 ─ ③（需求条目 spr-XXX）
```

三件要补：① 树位 **① 业务文档 与 ② 任务节点** 两级（现行只有 ③ 条目）；② 业务文档上的**五个属性**；③ **条目状态取值**对齐 91 §10.6／§11.7 的 `待处理`——现行文件的状态说明表写的是 待分解映射／待确认方案／迭代中／已确认／已验证冻结／已废弃，与 91 的「待 wft01-biz 处理 = FR-BIZ 且状态 = `待处理`」对不上。

**开放点**：域级索引轴——现行用**角色索引**，91 §11.2 立的是**业务域索引**；五个属性里含所属业务域，重建时可一并定。

## 连带

- `biz01abr`（`eos-biz01abr-stfr2rbpl.md`）§2.1 的「**落位现状**」引块（"现行物理文件以角色分片直接挂条目，尚未含业务文档与任务节点两级；本任务读时按条目正文语义归拢，标 `[待复核]`"）是**该落差的临时交代**——01 重建后应随之删除，`[待复核]` 一并用不上（`02` 数据文件 §3 标注纪律里的 `[待复核]` 定义同此）。
- 同族挂账：[[pending-91-align-to-94]]（91 对齐 94 树权威，人类定"不是现在"）；`04` 数据文件 §4.1 模板重建（见 [[eos-design-landing-spec-93-refactor]]）。

关联 [[94-eos-sys-dev-dbs-tree-authority]]、[[eos-design-landing-spec-93-refactor]]、[[biz-chain-renumbering-2026-09-16]]。
