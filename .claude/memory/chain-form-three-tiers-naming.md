---
name: chain-form-three-tiers-naming
description: 三形态定案（框架/概要/定义）＋ 任务编号记号（做哪几级 + 视角）＋ 全链更名链（概要→框架、概详→框概、方案→概要、概要设计→框架设计；角色履职→职责；前缀 bph→bpl·bpd 字母不动·srh→srb·srd→srl）＋ 未决（系统需求侧任务 ID、biz01abr 的 ID 串 stfr 化石、93 仍写角色履职）＋ biz01abr 深审落笔（93 第 51 版／94 v1.31／试点第 36 版）
metadata:
  node_type: memory
  type: project
---

**2026-09-17 人类逐轮定案。这一条是现行命名体系的权威记录。**

## 一、三形态（94 v1.28 §1.3）

每份产品数据是**同一棵树**，按定义到多深分三级：

| 形态 | 覆盖 | 含义 |
|---|---|---|
| **框架** | 根 → **枝梢** | 叶只以枝梢分解源判据的**展开项**露名字（名称级） |
| **概要** | 根 → **叶** | 各级节点只到**说明**，不写详细定义 |
| **定义** | 概要 ＋ **各节点的详细定义** | 叶在概要是说明级树节点，到这里才拿到详细定义 |

**级的三级设计动作**：**框架设计**（a）／**概要设计**（b）／**详细定义**（c）。相邻两级可**合做**——一个设计动作一次交出两级（ab／bc／abc）；也可**省略或裁剪**——某份产品数据不必每一级都停。**「合」≠「省」**：合是一手做完，省是那一级没做。

**叶的三种身份**：框架里是枝梢上的一条展开项 → 概要里成为说明级树节点 → 定义里带详细定义。

**「方案」是伞词**（2026-09-17 人类再次明确）：框架／概要／定义**三者都叫方案**，只是详细程度不同。故「需求-方案对」「方案迭代」「`待确认方案`」里的"方案"是伞义，不指某一形态；`配置方案`／`前后端组件方案` 名称不含形态标记同此理。

**框架有阶（高阶框架）**：**枝梢取哪一级由框架设计定**，取越浅的级框架阶越高。业务流程树例——**构件级高阶框架**（枝梢＝构件）、**文档级高阶框架**（枝梢＝⑤ 业务文档）、**任务节点级**（枝梢＝⑥，基准）。旧 `02-eos-business-summary-architecture.md`（bpl-biz）即**文档级高阶框架**：其注写明「枝梢＝节点内业务链的输出文档（PL5）…PL6 叶在完整态 04 定义，02 不含叶」——这正是 biz01abr 里"分解源判据挂在业务文档⑤"残迹的出处。

## 二、任务编号记号（93 第 49 版 §1.2）

`{路径}{需求-方案对序号}{做哪几级}{视角}`

- **字母位记"这一步做哪几级"**：`a`＝框架设计、`b`＝概要设计、`c`＝详细定义；**连写＝合做**
- **末位记视角**：`r`＝角色履职视角、`p`＝产品开发视角（两侧都带，不靠"不带就是 R"的默认读法）
- **任务位号取需求-方案对的序号**

本链实例：`biz01abr`（做 ab，R 侧）→ `02`；`biz01abp`（做 ab，P 侧）→ `03`；`biz01cr`（做 c，R 侧）→ `04`。

## 三、各产品数据取了哪几级

| 产品数据 | 记号 | 现存文档 |
|---|---|---|
| 角色履职视角业务流程 | **0xbc**（框架设计被穿过，未停） | `02` 业务流程**概要**、`04` 业务流程**定义** |
| 产品开发视角业务流程 | **0xb**（框架穿过、详细定义裁剪） | `03` 产品开发视角业务流程**概要** |
| 系统需求 | **0xa** 后接 **0xb**（分开两步） | `05` 系统需求**框架**、`06` 系统需求**概要** |
| 平台构成 | **0xa** | `07` 平台构成**框架** |

## 四、同一日的前序更名（同属一条链）

| 词 | 改动 |
|---|---|
| 概要（旧义＝根→枝梢） | → **框架** |
| 概要设计 | → **框架设计** |
| 概详 | → **框概** |
| 方案（形态名义） | → **概要**（一般义如「需求-方案对」「修复方案」不动；`配置方案`／`前后端组件方案` 按 94 §1.1「名称不含形态标记」不动） |
| 英文 | Summary → **Framework** |
| 资产前缀 | `bph`→**`bpl`**（概要）、`bpd`→**`bpd`**（字母不变，转指定义）、`srh`→**`srb`**（框架）、`srd`→**`srl`**（概要） |

## 五、未决（挂账）

1. ~~**资产前缀与链上任务 ID 串按新形态重推**~~ —— **已落**。推法（依 02＝概要、04＝定义）：业务流程一侧 `bph-*`→`bpl-*`、`bpd-*` 字母不动（转指定义）；系统需求一侧 `srh-*`→`srb-*`、`srd-*`→`srl-*`。链 ID 串现为 `spr2bpl`（相关方需求→业务流程概要）、`bpl2bpd`（概要→定义）、`bpd2srb`（定义→系统需求框架）、`srb2srl`、`srl2pa`；六个 wft 目录与文件已 `git mv`。
2. **`完整态` 204 处**——三形态下这个词没有了，逐处判归**概要**（"根→叶"那层意思）还是**定义**（"写到最实"那层意思）。
3. **系统需求／平台构成侧的任务 ID 未定**——原提议 `biz02ar`／`biz02br`／`biz03ar`，但**路径前缀有问题**：产 05／06 的任务现是 `wft03-eng`／`wft04-eng`（eng 路径），`biz` 前缀放不上；待定的是「三路径各一套 01→07，任务 ID 的路径位取什么」。
4. ~~**`高阶框架` 重新启用**~~ —— **已落**：94 §1.3 补「**框架有阶**」条（枝梢取哪一级由该次框架设计定，取越浅的级框架阶越高；构件级＝枝梢取产品构件、文档级＝枝梢取业务文档）；93 §3.3 应含项改「**形态边界**」并注明"停在框架的另写明枝梢停在哪个阶"。
5. ~~**分解源判据的归属**~~ —— **已落**：94 §1.3 补「**分解源判据附着于本次形态的最深一级**」条——框架停在哪阶就挂哪一级，**到概要叶已是树节点、判据的内容即叶本身、不另设展开清单**；93 §3.3 同改；`biz01abr` 三处打架的落点已合并。**挂账**：`02` 数据文件 §4.1 模板仍把 ⑦ 与"分解源判据"并列写两遍，随 `02`／`04` 分装题一起改。
6. ~~**`02` 与 `04` 的分装**~~ —— **已落**：按字面口径即「**`04` 复述 `02` 的整棵树，再给各级节点补详细定义**」（04 ⊇ 02），biz01cr 原就是这么写的，Node 编号沿用 02 的；94 §1.1 的并列两行不动。
7. ~~**`03` 的形态名**~~ —— **已落**：按证据定为**概要**（94 §1.1 ＋ `03` 数据文件树画像标 ⑥ 枝梢／⑦ 叶 ＋ biz01abp 的树图与传出承诺都是末三级同名视图），**94 §2.2「产品开发视角止于框架（`03`）」已改「止于概要」**；产品开发视角**没有定义形态**。
8. **状态值两处不一致**——`02` 用 `需biz01修订`、`03` 用 `需biz01cr修订`，同一退回动作两个名字。
9. **`落叶型` 这个名字成了化石**（2026-09-17）——叶在**概要**里已树节点化，`02→04` 的活是"补详细定义"不是"把展开项落成叶"。93 §3.2 的描述已按"深化步"改，**类型名是否也改（如 `深化型`）待人类定**；三份试点正文里的动词已统一为"深化"。

## 六、进度与下一开工点

**三份试点已全部审完并落笔**（2026-09-17）：`biz01abr` 第 33→34 版、`biz01abp` 第 3→4 版、`biz01cr` 第 2→3 版。改法同一套——术语落后一代（`框架`／`完整态`／`展开项`／`骨架` 全清）、同文自相矛盾（§3.2 与 §3.4 两处「方案边界」合为一处 ×3）、P 侧令牌 `[归属冲突]` 从 R 侧剔除、措辞对齐 94。

**两份的实质差异**：`biz01abp` 的 Y 由"止于框架"改为**止于概要**（P 侧没有定义形态）；`biz01cr` 整篇由"X 是框架、把分解源判据展开项落成叶"重述为"**X 是概要、叶已是说明级树节点，本任务补各级详细定义**"——这一条是 `biz01cr` 全文的骨架，改了它之后 `biz01abp`／`biz01cr` 的受理门槛、复核项、关口、传出承诺才和 94 §1.3 对得上。

**配套**：94 v1.28→v1.29（§1.1 伞词、§1.3 框架有阶＋判据归属、§2.2 四处去「完整态」并定 `03`＝概要）、93 第 49→50 版（§3.1 去二选一、§3.2 三类语义按三形态重述、§3.3 三处）。

**数据文件侧已落**（2026-09-17）：`02` v1.1（§4.1 模板降到**概要**级、与 biz01abr §3.5 对齐——封面模式／正文实体类型／支撑引擎候选／名称级内联标签，去掉数据权限与分解源判据；`[待复核]` 口径落在标注纪律）、`03` v1.1（产出者更正为 `biz01abp`、`需biz01cr修订`→`需biz01修订`、完整态→概要）、`04` v3.3（定为"**复述 `02` 的整棵树＋各级详细定义**"，§3.1 去双视角、§4.1 按"表单标签页序列＋叶定义"分列，原 PL6-A／PL6-B 口径退役）；三份的目录链接（`subpd`→`subpl`）、任务代号、树画像／索引标题一并随迁。

**下一开工点**：① `03` 的**节点 ID 前缀【待定】**——`bpd-biz-*` 会与 `02` 撞号，且两侧干枝不是同一批节点；② `04` 的**资产区**（§1.2 资产写回队列／§3.3 关联资产／§6.2 资产建议）与 biz01cr §6「R 树本身就是资产、不另读资产文档」相冲，去留取决于 21／23／24／25／27／28 的存档裁定；③ 三份数据文件的**英文副标题**（`— Framework` 现指框架，概要该用哪个词待定）。

**`完整态`／`框架态` 在 93／94 与三份试点已清零**；全仓尚余 177 处，分布＝memory 正文（不改）、91 与 95 报告（91 挂起）、通用规范、废弃档（`02-summary-architecture`／`03-summary-detailed`）、05 数据文件、教材子项目与 20 号报告（子项目独立，主项目不碰）。

## 七、`biz01abr` 深审与双向修订（2026-09-17，第二轮）

上一节那轮是**机械对齐**（换词、合重、删令牌）；这一轮是**语义深审**——拿 93／94 当尺子逐句量，不符处**双向**出结论：落在试点的改试点，落在权威层的反推改权威层。

**落笔**：`biz01abr` 第 34→**35** 版 9 项、93 第 50→**51** 版 7 项、94 v1.29→**v1.30** 2 项、`biz01cr` 一词同步。

**试点的三处硬伤**（都是"名与定义打脸"同族）：

1. **§5.2「枝梢之并」用旧义**——94 §1.3 定枝梢＝本次框架最深一级（此任务即⑥任务节点），原文却按旧义（枝梢＝⑤业务文档）命名"业务链"，且把覆盖范围说浅一级。深一层看，这句还**并了两件事**：叶那层核的是"X 的用例场景整段进入 Y"（93 §4.3 给组装步的覆盖形态），链那层核的是"⊆ 其业务链所能实现的范围"（端到端闭合），二者不同源。
2. **§1.2 把「任务节点」同时划给框架设计与概要设计**——根子是**按"哪几级归哪一步"分工**，而三形态是**同一棵树的深度切片**；⑤⑥在框架里已是树节点，只有⑦才是"展开项→树节点"的转变。
3. **`完整态` 之外的又一轮落后**；另补：§2 从未声明「输入语义属组装型」（两份同批试点都声明了）。

**权威层的实质发现**：94 §2.1.1 的条目前缀 `stfr-XXX` 是化石——91 §11（`@spr-`）与现行数据文件（`spr-001`…`spr-010`）都用 `spr`，**94 是唯一还写 stfr 的地方**，已改回 `spr`。连带一个**未解**：`biz01abr` 的 ID 串 `stfr2rbpl` 正是建在这个化石 token 上（`SKILL 指南ID` 字段仍带 stfr），而记档里链 ID 串写作 `spr2bpl`（现行 wft 目录名）——**同一段转换两个前缀**，改名牵动文件名／ID 字段／记忆，**未动，待人类一并裁**。

**判定留痕**（免得下轮重推）：

- **`需biz01修订` 不动**——值是退给**任务位**（93 §1.2「两半合占一个任务位」），不是退给某一半，两半共用一值成立；原先怀疑它分不清 abr／abp，推翻。
- **`落叶型` → `深化型`**（93 ×2 ＋ biz01cr ×1）——名里的"落叶"说的是旧模型（叶到这一步才落成树节点），94 §1.3 定"到概要叶已是树节点"，本步做的是补详细定义；93 §3.2 的描述早已改成"深化步"，名没换。与 `承接型`／`组装型` 的动作式命名对齐。
- **§6.3「判定动作」从 biz01abr 删去**——93 §3.6 定位是"X／Y 之外的读写接口"，应含只有读／写；该节列的是本任务自己的判据还注一句"已就地成文"（自指）。**biz01abp／biz01cr 仍带同形节，未同步**——它俩不在本轮审视范围，删法待定。
- **93 §3.5 只引 00-doc-conventions §十三 的"列定义"，未交代 Step 正文格式**——§13.4 要求每 Step 展开成 `| # | 操作 | 执行者 | 说明 |` 表、标题写 `### Step {N}`，EOS 线**无一份**照办（元流水线的指南才用那套）。已在 93 §1.3 引用表＋§3.5 两头说清：§十三 只取总览表的格式与编号约束，§13.4 不在本类文档使用。

**下一步**：`biz01abp`／`biz01cr` 按同一尺子深审（上一节记的两条挂账① ②③ 仍在）。

## 八、链级更名：角色履职 → 职责（2026-09-17）

人类指示「把角色履职更换为职责」（2026-09-17，起于 `biz01abr` 的标题行）。**替换单位是子串**：`角色履职` → `职责`，故「角色履职视角业务流程概要」→「**职责视角业务流程概要**」、「角色履职业务」（R 树根节点）→「**职责业务**」、「R 角色履职树」→「**R 职责树**」、「业务运行角色履职视角」→「**业务运行职责视角**」。**`履职` 单独出现时另判**（见下）。

**落笔（9 文件）**：`94` v1.31（16 处）、`91` v5.63（6）、`02` v1.2（5）、`04` v3.4（5）、`03` v1.2（2）、`01-pl4eos-spec` v2.4（1）、`biz01abr` 第 36 版（23）、`biz01abp` 第 5 版（22）、`biz01cr` 第 4 版（28）——共 108 处。94／91 的 PDF 已重生成；`02`／`03`／`04`／`01-pl4eos-spec` 各补一条变更记录行（沿用「历史变更记录保留旧词」口径）。

**人类同轮的三条裁决**：① **R 的指代不动**——94 §1.1 的「**R 树（职责视角，roles & responsibilities）**」中文随改、英文注解与 R／P／R0／Rn 记号一律不动（R 字母的词源仍在此）；② **93 号不动**——`r`＝角色履职视角 的记号定义连同 93 的其余 4 处使用一并留原样（**93 是唯一还写"角色履职"的在用规范**，其 §2.4 产品数据编号表会与 94 分家，挂账）；③ **履职改为职责**——但只在**术语位**（视角／序／单元：`91` 履职视角树→职责视角树 ×2、`biz01cr` 履职序→职责序、`biz01abr` 3 处），**普通动词位保留**（`91` ×3「在平台上**履职**的人」「谁在平台上**履职**」——换成"职责"不成话）。

**未动**：8 份 wft 指南（旧版将退役，其 `角色履职` 多在变更记录历史行，另 `wft01-biz`／`wft02-biz` 各有 1 处在体——「角色履职链」「最小履职单元」）、废弃档 `02-eos-business-summary-architecture`、`90-hold`、`.claude/memory` 记忆正文、**教材子项目 `50-program/01-系统工程研究`**（子项目隔离，主项目不碰）。

关联 [[eos-wft-retirement-biz-pilots-canonical]]、[[skill-guide-renaming-2026-09-15]]、[[chain-layer-name-rename-2026-08-28]]。


关联 [[94-eos-sys-dev-dbs-tree-authority]]、[[page-ui-four-levels]]、[[feedback-code-whitelist-2026-09-16]]、[[biz-chain-renumbering-2026-09-16]]、[[eos-design-landing-spec-93-refactor]]。
