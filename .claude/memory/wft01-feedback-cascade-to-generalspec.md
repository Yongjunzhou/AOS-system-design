---
name: wft01-feedback-cascade-to-generalspec
description: wft01 v4.2 反馈迭代重构中提取的三条通用模式已写入通用规范 v1.7
metadata:
  type: project
---

# wft01 v4.2 → 通用规范 v1.7 模式提取

2026-06-09，在 eos-wft01-or2str.md v4.2 的反馈迭代重构中，提取三条通用模式写入 01-generalspec-sysdev.md v1.7：

1. **§2.2.1 上层需求映射的完整性要求** — 架构节点应包含所承接需求的完整原文，使节点可独立审阅。映射变更时从原节点读出内容写入新节点，确保内容不丢失。

2. **§2.3.4 节点级迭代粒度** — 一条上层需求的不同下层架构节点可独立迭代互不阻塞。各节点附带独立反馈区，独立确认后即可进入下游。

3. **§3.2.4 映射变更的级联更新** — 反馈导致映射变更时，执行四项级联操作：源节点移出、目标节点追加、需求条目改签、状态按需更新。内容逐字写入不可缩写。

**Why:** wft01 v4.2 将迭代粒度从 OR 条目级改为 STR 节点级，表D 嵌入 02-*.md 管理区（文档状态嵌入模式），STR 节点自包含 OR 原文。这些实践具有通用性，不是 EOS 特化规则。

**How to apply:** 通用规范已覆盖，后续任何设计层级（OR→SR、SR→BA/SysReq、SysReq→PA）的反馈迭代均可引用 §2.3.4 和 §3.2.4 的模式。
