---
name: pending-biz01cr-rewrite
description: 开工简报：biz01cr 继续优化——现状实查（第 17 版／五章 20 节）、四轮已落、待办四组（六处待裁／本轮三个口／三处同族句／数据文件一轮）与可直接继承的尺子
metadata:
  node_type: memory
  type: pending
---

**人类 2026-09-23 定**：「我们新开一个对话，继续优化 biz01cr」。**本件即那一轮的开工简报**——新会话读本件即可开工，不必回看前几轮的挂账（那些是它们的记录）。前一轮（重写为五章）的简报已被本件取代。

**文件**：`20-pl4eos/10-pl4eos-subpl-sysdev/10-wfsysdev-4-eos/eos-biz01cr-rbpl2rbpd.md`；**本轮的 Y**＝`20-pl4eos/80-pl4eos-2-eosdata/04-eos-business-detailed.md`。

## 一、现状（2026-09-23 实查，非转述）

- **cr 第 17 版、613 行、五章 20 节、18 张表、3 张图**：一 目的与目标（§1.1~1.4）／二 输入文档结构及规则（**章下无节题**：图2-1 ＋ 表2-1 意义 ＋ 三条注）／三 输出文档结构及规则（§3.1 图3-1＋表3-1 意义／§3.2 表3-2 实现规则／§3.3 表3-3~表3-9 要素＋两注＋移交承诺）／四 输入到输出的过程及规则（§4.1 对应＋§4.2~4.5 四步＋§4.6 收束）／五 执行步骤及检验规则（§5.1 Step Start~§5.7 Step End）。
- **`04` v3.9**；**abr 第 102 版**（622 行）／**abp 第 41 版**（572 行）／**93 第 80 版**。
- **两条口径已由人类定稿**：§1.1 的目的（深化到**对信息系统界面与数据的具体要求**，使业务需求能在信息系统上可以实现）；交付物称「**写足对…的要求**」（依 91 §1.1「UI 页面由功能引擎配置生成」）。
- **已落笔的三个内容项**：⑤ 增「配置单元及配置信息组」（只引用不设计）／⑥ 增「表单字段」／两条新具名规则（支撑能力引用规则、表单字段规则）＋引擎能力对照补成两半。
- **提交**：`b8caf6ea`（五章重写）→ `66206bf0`（目的口径）→ `aa987caf`（称法对齐）→ `955d1073`（需求具体到页面类型），两仓已推。

## 二、标杆＝**abr 现在的样子**（第 102 版；abp 第 41 版同日同形，可作旁证）

以 abr 为准，别再引中间态。要点：**五章骨架**／第四章**一节一步 ＋ `#### 转换过程`（编号清单，每项点名规则）＋ `#### 转换规则`（具名规则，按首次被点到的次序排）**／第五章**一节一个 Step**、每节五件套（动作序／触发／产出与去向／人类参与点／失败处置）、Step End 自检＝**两级清单**、状态推进**不画图**（散文＋bullet）／第一章三条判据见 [[skill-guide-ch1-writing-criteria]]／表达：无 `——…——` 夹注、无不负责清单、同一判据不说两遍。尺子详见 [[skill-guide-ch4-process-and-rules]]。

## 三、已裁（人类已定，新会话不必再问）

1. **射程＝以 abr 为准**（[[feedback-abr-only-scope]] 的做法）：判据取 cr 自身前后相抵 ＋ 照 abr 作范本；不拿 93／94 当体检尺子，93／94 的口径只经由 abr 这个先例间接带入。
2. **cr ＋ `04` 同轮**；`02`／`03` 不在本轮（归数据文件一轮）。
3. **六、七两章化掉**：读写随读它／写它的那一步就地成文，§7 的增量标记与自检并入 Step End（已落）。
4. **治理内容从 cr 去掉**（§5.2 的治理覆盖检查与自检那行已删；§2 平台侧 R 树那条注**保留**，见 [[biz-chain-governance-removed]]）。
5. **目的口径**与**交付物称法**（见上），以及**「页面类型的具体定义」＝把需求具体到该节点那一类页面上**（不是写类型名、更不是页的方案；**表单字段也是需求**）。

## 四、待办

**甲、六处我自定的判断，人类未点头**（随时可撤）：

1. **第四章原 §4.3 的两张分型表未落**——「边界匹配检查」（边界内／边界扩展／退回上游／宿主误匹配四类）与「业务链关系检测」（确认／无新增／引擎判定变更／补充／过时五类）**只列档位名、无判据与处置**；判归属归上游（abr），cr 唯一对得上的「引擎判定变更」已并入支撑引擎权威值规则。故 §4.6 只留 修订转换／变更影响声明／影响面复查 三条。
2. **Step 2 与 Step 3 对调**——原序「Step 2 反馈处理 → Step 3 逐级写实」使「整体确认」落在产出之前（cr 自身相抵），改为 逐级写实 → 反馈处理 → 覆盖自查 → 写回。
3. **Step 仍五步**，未并成 abr 的四步（载入未并入生成步，仍独立成 §5.2）。
4. **§1.3 保七项**，仍留「写回与推进」（abr／abp 同款）。
5. **§1.2 仍留「形态的位置」与「人类门禁」两段**（[[skill-guide-ch1-writing-criteria]] 判据一说该删；abr／abp 都留着）。
6. **`04` 去「支持信息引用」**（91 §7.2 任务 4 有此项，cr 的要素表无；两边对齐时按 cr 侧收）。

**乙、本轮内容扩充留下的三个口**：

1. **「只引用不设计」这条界线**已落进 cr 正文（判据 91 §3.3「biz 路径不得新建、修改或详细设计 CU；配置信息组……biz 路径不得设计」）。若人类要的是 04 **真设计** CU 与配置信息组，须先反向修订 91，不在一轮内。
2. **三级同挂在 ⑤**（引擎 → 配置单元 → 配置信息组）。91 §七 的对称性有**两半**：明面用的是「biz 的输出文档 ↔ eng 的配置信息组」（同为 PL5 末级，故落 ⑤ 有据）；另一半是「**eng 的 CU（PL4）↔ biz 的 WBS（PL4）**」——严格按这一半，CU 该挂 ④ 那一级（P 树的 WBS业务）。
3. **91 把「引擎能力使用需求」归 wft03-biz**（cr 的**下游**，系统需求框架设计任务）——本轮回等于把它往上游挪了一格到 04。**91 §3.3 与 §1.2 那条注只报不改**，待人类定。

**丙、三处同族句未动**（只报）：`abr` 三处（§1.2、表1-1、三区输出）作「在说明级之上补各节点的详细定义」；`02` 的产出与消费行同族；**`94` §2.2 那条注**除旧口径外，另有「**菜单**、窗口标签页、表单标签页、功能操作项四级都挂在 R 树上」这一处独立的错（91 §1.1 与 `04` 都作**场景标签页**起头，菜单标签页不占树级，见 [[page-ui-four-levels]]）。abr／94 是权威文档，动须点头。

**丁、数据文件一轮**（[[pending-biz01abp-revision]] 的 S10）：`02` 的级名仍是旧族（角色职责／E2E职责／业务文档）；`03` 的树形与级名未动；`04` 的 §1.2 资产写回队列与 §3.3 关联资产（wft02-biz 时代遗留）、§2.1 索引表横线行比表头多一格、【待定】ID 前缀——均未动。

## 五、可直接继承的尺子（不必重推）

**过程／规则的判别句**（这句在讲"先做哪件再做哪件"＝过程，还是在讲"凭什么判、判出来哪一档"＝规则）；**规则名的形**（93 §3.4 已有的「X 规则」一脉，词取 91／94 的动词，名字写足限定，别处引名不引表号）；**清单体例**（节内用 `1. 2. 3.`，不用圈号）；**常量不占列**（整列同值的进引题或注）；**动词查归属**（一个动词在权威里已有主——如「编排」属场景与菜单引擎、「配置生成」属功能引擎——本任务再拿它作谓语就是替别人说话）；**「定义」是加工程度不是描述**（「定义 X」＝把要求加工到 X 这一粒度）；**页面由引擎按配置生成**（91 §1.1，故本任务只出要求）；**自足**（每节读完就能照着跑，不引别处）。

关联：[[skill-guide-ch4-process-and-rules]]、[[pending-biz01abr-ch5]]、[[pending-biz01abp-revision]]、[[feedback-abr-only-scope]]、[[eos-pilot-authority]]、[[biz-chain-governance-removed]]、[[skill-guide-ch1-writing-criteria]]、[[skill-guide-ch3-ch4-split]]、[[ch2-ch3-tabling-2026-09-18]]、[[cu-four-element-caliber]]、[[page-ui-four-levels]]、[[progress-93-skill-guide]]、[[chain-level-name-family]]。
