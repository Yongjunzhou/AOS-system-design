---
name: pending-biz01cr-rewrite
description: 挂账：biz01cr（R→职责视角业务流程定义）重写——新会话开工简报，含现状实查、标杆（abr 第 102／abp 第 41 的形）、逐章线索与开工前宜先裁的三项
metadata:
  type: pending
---

**人类 2026-09-23 定**：「我们新开一个对话，便于重写 biz01cr」。**本件即那一轮的开工简报**——新会话读本件即可开工，不必回看 abp／abr 那几轮的挂账（那些是它们的记录）。

**文件**：`20-pl4eos/10-pl4eos-subpl-sysdev/10-wfsysdev-4-eos/eos-biz01cr-rbpl2rbpd.md`（R→职责视角业务流程定义，biz 链第二个工位）。

## 一、现状（2026-09-23 实查，非转述）

- **第 13 版、410 行**；**七章**：一目的与目标／二输入／三输出／四过程规则／五执行与检验／**六 关联产品数据及规则（接口）**／**七 收尾**。
- **旧级名 51 处、新级名 0 处**（`角色职责`／`E2E职责`／`业务文档`；应换成 `职责级E2E业务`／`岗位级E2E业务`／`文档级E2E任务`）。
- **ch3 是旧形**：3.1 树形与语义／**3.2 节点类型与结构要点**／3.3 Y 侧规则／3.4 要素清单／3.5 移交承诺——**未表化**（abr／abp 已是「图3-1 ＋ 表3-1 意义 ＋ 表3-2 实现规则 ＋ 各级要素表」）。
- **ch4 是旧形**：4.1 深化与写实规则／4.2 要素生成判定／4.3 修订转换／**4.4 缺口规则**——**无步骤、无过程/规则两块**；且**缺口规则挂在 ch4**（93 §3.4 禁含：缺口的判定与处置归第五章的关口）。
- **ch5 是旧形**：5.1 设计过程（Step 序列，一张表）／5.2 检验规则／5.3 状态推进与退回／5.4 运行时特化参数——**不是「一节一个 Step」**。
- **六、七两章是旧骨架的残留**（abr／abp 已把它们化掉：读写在 ch5 各步就地成文、收尾并入 Step End）。

**一句话**：cr 落后 abr／abp 约两代。

## 二、标杆＝abr 与 abp **现在的样子**（不是旧样）

**直接照这两份（同日成形）**，别再引「abr 第 89 版」那类中间态：

- **五章骨架**：一 目的与目标／二 输入文档结构及规则／三 输出文档结构及规则／四 输入到输出的过程及规则／五 执行步骤及检验规则。
- **章首引题**（第四章）：一段普通文字，「一次转换依次走 N 步：…落在…」。
- **第四章＝一节一步 ＋ 两块**：`### 4.2 第一步 · 动作名`，其下 **`#### 转换过程`**（块首一句给"上游给什么"，中间**编号动作清单**，每项以「——使用**甲规则**」点名；块末一句给"本步交出什么"）＋ **`#### 转换规则`**（**具名规则**，按过程里首次被点到的次序排；每条以「**规则名**——判据出自…」引出，身体可以是表也可以是一段话）。尺子与体例详见 [[skill-guide-ch4-process-and-rules]]。
- **规则名的形**：取 93 §3.4 已有的「X 规则」一脉，词取 91／94 的动词，**名字写足限定**；别处**引名不引表号**。
- **清单体例**：节内编号清单用 `1. 2. 3.`——**不用圈号**（圈号在本类文档专指树级）。
- **第五章**：一节一个 Step（`### 5.x Step N · 动作名`），每节给全五件套——**动作序／触发／产出与去向／人类参与点／失败处置**；单节缺"人类参与点"时也要写明「本步无人类参与点」；**Step End 的自检清单是两级清单**（组名一条、组内每件事单独成条）。
- **Ch1 已有判据**：见 [[skill-guide-ch1-writing-criteria]]（§1.1 一行目的／§1.2 三样含形态的位置与人类门禁位置／§1.3 目标覆盖职责面且与后章各节产出对应）。
- **表达**：删「不负责清单」（93 §3.1／§4.1／§2.4 规则一三条同指）；同判据不说两遍；破折号夹注（`——…——`）拆掉。

## 三、开工前宜先裁的三项

1. **射程**——cr 这一轮的分析依据是什么？abr 侧有 [[feedback-abr-only-scope]]（「只以 abr 自身为据，93／94 也出局」）。**cr 是否同此、还是照 93／94 通检**？这决定后面所有"报什么、改什么"。（abp 那边的做法是照 93／94；abr 那边只收自身的相抵。）
2. **级名清扫是否连带**——cr 的 51 处旧级名与 `02`／`03`／`04` 的级名清扫是**同一件链级活**（[[chain-level-name-family]] 的未落项：「数据文件 02／03／04 与 91 其余处未扫」）。**cr 的 Y 是 `04-eos-business-detailed.md`**，故 cr 的 ch3 要素清单与 `04` §4.1 模板必须同一轮对齐；`04` 的模板本身也还是旧的。**只改 cr 不改 `04`，会立刻出现"指南引的 schema 之家与它不符"**（abp 这一侧的同病见 [[pending-biz01abp-revision]] 的 S10「数据文件一轮」）。
3. **六、七两章的去处**——「六 关联产品数据及规则（接口）」的内容按 abr／abp 的做法是**随读它的那一步就地成文**；「七 收尾」的 AI 标记约定与自检清单并入 Step End。**裁一下是照此化掉，还是保留章。**

## 四、cr 侧已知待办（从各处记忆收拢）

- **七章 → 五章**（上条）。
- **ch3 表化**：图3-1 ＋ 表3-1 意义 ＋ 表3-2 实现规则 ＋ 各级要素表 ＋ 表下注；要素清单与 `04` §4.1 模板互为对侧。
- **ch4 改「一节一步 ＋ 过程/规则」**；**缺口规则从 ch4 移出**（归第五章的关口）。
- **ch5 改「一节一个 Step」**；状态推进按 [[pending-biz01abr-ch5]] 第九轮后的写法（cr 的 Y=`04` 被 sw 链与配置链消费，**下游退回真实存在**，与 abp 不同）。
- **「治理内容从 biz 链去掉」待核 cr 同一对**——见 [[biz-chain-governance-removed]]（abr 四处已清，理由：治理预置节点无需求锚点、由人类直写 `02-*.md`、不经流水线）。**cr 的 5.2 检验规则里有一条「治理覆盖检查」**，是这条待核的落点。
- **cr 的 Y = `04` 的 schema**：cr 的 ch3 §3.4 要素清单要与 `04` §4.1 的节点模板对齐（同 `02`／`03` 那一轮）。

## 五、本轮（abp／abr 那十笔）留下的、可直接继承的尺子

不必重推，直接照用：**ch4 的过程/规则尺子 ＋ 具名规则 ＋ 清单体例**（[[skill-guide-ch4-process-and-rules]]）；**Step End 自检＝两级清单**；**去啰嗦三病**（同判据说两遍／破折号夹注／长句在列表项里未必无罪）；**不负责清单要删**；**状态推进不画图**（散文＋bullet，见 [[pending-biz01abr-ch5]] 的定案）。

关联：[[skill-guide-ch4-process-and-rules]]、[[pending-biz01abp-revision]]、[[pending-biz01abr-ch5]]、[[progress-93-skill-guide]]、[[chain-level-name-family]]、[[eos-pilot-authority]]、[[feedback-abr-only-scope]]、[[skill-guide-ch1-writing-criteria]]、[[biz-chain-governance-removed]]、[[eos-design-landing-spec-93-refactor]]。
