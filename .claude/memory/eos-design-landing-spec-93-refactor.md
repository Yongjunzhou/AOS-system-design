---
name: eos-design-landing-spec-93-refactor
description: 基于 20 号落地方法改造 EOS 设计线各任务 SKILL 指南——六章骨架 + 93 规范（现第 46 版）；biz 链 2026-09-16 重编号为 01a／01b／02（见 [[biz-chain-renumbering-2026-09-16]]），三试点随之改名；biz02 正文待重写
metadata:
  node_type: memory
  originSessionId: 7359a8bb-9109-4517-ad13-1640760aae6e
  modified: 2026-09-16T01:40:35.979Z
  type: project
---

2026-09-04 用户启动：以 20 号报告「设计与开发落地方法（Y=F(X)·由需求到方案逐对生成）」改造 EOS 设计线各 wft 人类方案及其 SKILL，先以 wft01-biz 推演、验证后铺开全链（biz/eng/nfr）。

**打磨对象规则（2026-09-04 用户纠偏，详见 [[eos-wft-retirement-biz-pilots-canonical]]）**：打磨对象 = 三试点 eos-biz01/02/03；`wft0?-biz-*`（v3.37/v9.16/v10.15）= 未落实 20 号的旧版、将退役——只作内容忠实度核对的单向内容源、不平行维护；试点必须自足——消灭「同现行版/引现行 eos-wft0x §」实质回指（改引 91/23/数据文件/93 或内化），文件头溯源注记可留。

**已收敛（对话定案）**——人类方案新骨架 = 六主章 + 收尾章：一 目的与目标 / 二 输入文档结构及规则 / 三 输出文档结构及规则 / 四 输出与输入转换规则 / 五 转换流程及检验规则 / 六 资产文档结构及规则（接口）〔+ AI 标记/自检/变更记录收尾〕。三条原则：①概念跟文档走、原理跟规则走——不设孤立概念/原理章，描述 X 的概念进 ch2、描述 Y 的进 ch3；②schema 全链单源（91 §11 + 数据文件自身 + doc-conventions），人类方案只引用 + 写本侧规则、不重复定义；③边界文档两侧面——生产它的任务 A.ch3（生产侧 + 传出承诺）、消费它的任务 B.ch2（消费侧 + 受理前提）互为接口契约对侧，并排核对即契约校验（20 号 §2.4 需求树的来历：B 的 X 树 = A 的 Y 树，无独立构建规则）。资产概念归资产文件自身、ch6 只作读写接口；运行时协议（Step Start/End/反馈/写回/生命周期）归 91 附录A，人类方案只引不重述。

**决策（2026-09-04 用户全同意）**：①新建 **[93-eos-skill-guide-spec.md](../../20-pl4eos/10-pl4eos-subpl-sysdev/10-wfsysdev-4-eos/93-eos-skill-guide-spec.md)**「设计与开发落地方案编制规范」为 wft 类人类方案静态编制规范之家（v0.1 骨架）；91 附录A 第二部静态布局面向 wft 类将迁出/改指 93，91 只留运行时 + 格式 token，面向 ort 类模板保留 91；②93 只管 wft（承接/分解类），ort00~03 预处理链维持 91 模板；③93 按方法级编制、不绑 biz/eng 名词，wft01-biz 做附录 B 实例。

**续·定案（2026-09-04）**：①判据归属分级按可复用面定家（方法级归 20 号报告/结构判据归 91/对齐判据家待裁/过程判据留本人类方案）——已入 93 §四.1；②**PL6 定位定案**——PL6 活动/正文条目 = 业务流程之叶（04 完整态定义成叶），PL5 输出文档 = 概要枝梢，02 概要不含叶、PL6 名称只以枝梢分解源判据展开项出现（理由：04 将 PL6 定义为方案条目、05 逐项转译 PL6→页面/正文内容、EOS「文档=概要末梢」自述）——试点 ch3 + 93 §三.3 已按此。试点文件 **eos-biz01-str2bph.md** v0.9-pilot 已建（六章骨架试写；str=相关方需求/20 号定名；相邻任务暂用现行名）；93 升 v0.3。

**续·biz 链三试点收官（2026-09-04）**：eos-biz02-bph2bpd（对① 阶段二·落叶）与 eos-biz03-bpd2srh（对② 阶段一·链尾移交）试点建成，与 biz01 成对验证——①两处接口契约均逐条成立（对内概要移交 biz01.ch3↔biz02.ch2；跨对完整态移交 biz02.ch3↔biz03.ch2）；②**ch2 输入两类语义**：承接型（X=上一对完整态：biz01 的 01、biz03 的 04）/ 落叶型（X=本对概要：biz02 的 02）；③覆盖逐对下传（02 承诺→04 实现→05 承接证明）；④链尾：biz 设计线止于 05 概要（功能表单枝梢+分解源判据），Y 完整态叶交开发线阶段二，契约对侧在开发线首 skill。**93 升 v0.4**：§三.2 输入两类语义、§三.3 概要—完整态边界、§4.2 两处契约已验证、§4.3 覆盖逐对下传+链尾移交均已定案回填；biz01/02/03 记为活实例。

**挂账/下一步**：①**02/04/05 数据文件节点模板重建**（现行 02 模板与 wft01/91「业务链-文档」模型脱节，停在旧「场景部件/信息载体」建模；试点 ch2/ch3 schema 引用依赖此）——试点落地的前置；②对齐判据之家待裁（91 §5.3+23 vs 整体入 23）；③93 回填 §三逐章细则/§六 工作流 + 附录 B（biz01 作示例）；④改造 91 附录A 静态部分改指 93 + 逐份改 wft 人类方案（biz 三份待试点落定后替换）+ 同步其 SKILL（单向派生）；⑤铺开 eng/nfr 链（wft01~05-eng、wft01/04-nfr 同骨架，注意 eng 有 srh2srd→pa 的第三对差异）；⑥本批提交（未提交：eos-biz01/02/03 三试点 + 93 v0.1→v0.4 + 记忆）。关联：[[chain-layer-name-rename-2026-08-28]]（EOS 设计线层名现状）、20 号报告（方法源，50-系统工程研究 教材子项目侧）。

**下一会话开工点 = 新开对话仔细审视 biz01~03 三试点（干净纸面；已提交 master ddbdf6b）**。审视简报：①三试点 vs 93 v0.4 六章骨架一致性——ch2 输入两类语义对位（biz01 承接型 X=01 / biz02 落叶型 X=02 概要 / biz03 承接型 X=04）、ch3 传出承诺成文化是否齐；②两处接口契约核对表逐条再验 + 抓三文件间说法漂移（引擎权威值唯一在 04、状态名、覆盖基线措辞、'05 也叫概要'命名混淆风险）；③术语同口径（枝梢/叶/分解源判据/预告/落叶/落地）；④对现行 wft01/02/03（v3.37/v9.16/v10.15）内容忠实度——试点压缩引用有无丢/歪判据；⑤与 91 §7/§10 + 数据文件模板的缺口（02 模板脱节为已知）；⑥各试点'待校准/待裁'批注收口（含对齐判据之家）；⑦命名残留定案（biz01~03 代号、str 定名、旧 wft 名/文件是否统一替换）。审视完回填 93 §三/§六 与附录 B。

**上午会话（2026-09-04）进展 + 下午开工点**：
- **三项裁决全同意并已回填提交**：A 判据回填=分级执行（试点引结构判据 + 自持过程判据；biz03 §四.2 出处列 6 处 wft03 引用消解，结构改指 91/能力 91 §4.4/SR-Q 归 20 号 §4.2 本对实例）；B **对齐判据之家定案 = 91 §5.3.1 判据 + 23 引用数据**（整体入 23 否决），91 附 **Q 族命名索引**（对齐 Q1~Q3 / 能力覆盖 Q1~Q4 / 22 分类 Q1~Q3+ 三族分家）；C1 PL6 叶只在完整态确认；C2 03 档型退役。
- **提交**：6b70b93（wft 退役规则记忆）→ 4aded91（批次1，试点 v0.10）→ bc48f8a（批次2，91 v5.53 §5.3.1 + 93 v0.5 + 试点 v0.11-pilot）。试点 eos-biz01/02/03 现 v0.11-pilot：自足（无现行版兜底）、契约表收编单栏（对侧对照引 93 §4.2）、判据落点已定。wft 退役规则独立记忆 [[eos-wft-retirement-biz-pilots-canonical]]。
- **批次3 打样（02 已改、未提交）**：`02-eos-business-summary-architecture.md` 已改 §3.1/§3.2 口径 + §4.1 新模板（业务链-文档模型：场景职责边界 + 业务链=文档序列 PL5 枝梢 + Bn 组装结论，PL6 只以枝梢展开项预告出现）+ bph-biz-GOV-001 演示态（含 t-M1/t-V1/t-K1 预告，标【打样·待审定】）。
- **下午开工点 = 铺全批次3**（按上午定的三条，用户认可格式后）：① 字段名逐字对齐试点 biz01 §3.5（schema 与人类方案同词）；② 术语**沿用现行「规范化的相关方需求」**——F15（01/全链更名"相关方需求"）未裁，留批次4 一次性扫，避免中途双名；③ **结构迁移不发明**：GOV-002..009 各节点保留既有真实内容映射进新字段，业务链只落确知项、细节留 `待设计/供人类确认`（GOV-001 = 唯一演示态，勿照其批量编 PL6 预告）。铺全含：§0.3 树画像补到"角色→场景业务→文档枝梢"层、0.1/0.2 统计、追溯表列名、02 版本/变更记录行。**其后**：03 归档（移 90-hold）+ 04/05 节点模板重建；93 回填 §三 逐章细则/§六 工作流 + 附录 A 骨架/附录 B（biz01 示例）仍未做。

**2026-09-15（严格按 94 v1.18 校订）**：93 → **v0.13**，试点 biz01 → **v0.14-pilot**、biz03 → **v0.13-pilot**（biz02 未动）。九项一次落齐：① §1.3 引用表 94 行随 §1.2 正名（"各树成立前提（三问三答）"→"**各产品数据树成立的理论依据**"）+ 下标 `i` → `n≥1`；② §1.4 单承接行措辞对齐；③ §三.0 过渡期"§五/§六"→ 94 现行 **§2.4 模型层 / §2.5 / §2.6**；④ §三.2 承接型 X 改「**上游最深可用原子（叶）**」（94 §1.1 明示 **01 不分形态**）；⑤ §三.3 **删「跨对承接允许枝梢与叶混入作为源」**（94 §1.2 依据三：定义域只取上游叶）+ 节点名去层码 + 立「**05 枝梢＝⑤ 用例场景功能 / 功能表单不属于系统需求树**」；⑥ §四.1 判据归属表新增「**树形/级语义判据 → 94 §1.2~§1.4**」「**分解终止标准 → 通用规范 §2.2.2**」两行，"概要/完整态、单承接"移出 20 号行；⑦ §4.3 链尾**重锚对③（06→07，落点【待裁】）**并单列 **biz 路径止点（05）≠ 设计线链尾**；⑧ 附录B「§〇 链上坐标」→ 第一章；⑨ 头注 + 变更记录。**要点**：两处口径（功能表单、承接源）是**按 94 直接判定**而非待裁——依据是 94 §1.4 承接表写死「04→05 落点＝⑤ 用例场景功能」、§2.5 把功能表单列为"旧平台对象（产物归属在配置方案收口）"，且 05 数据文件 §0.2/§0.3 与 91 §7.2/§7.3（v5.54/v5.58）早已同向校正；真正无家的一条是「04 输出文档（枝梢）↔ 功能表单」，须在 **94** 落笔（见下）。**仍未做**（照旧）：批次3 铺全、03 归档、04/05 节点模板重建、93 §三其余/§六/附录A/B 回填。

**同日新挂账**：① [[pending-91-align-to-94]]——人类定「91 要对齐 94，但**不是现在**」；已定位四处（§5.2 Rn 自相矛盾且落后 94 §2.2.1、§5.3 Pn 层级名/条数不符 §2.2.2、§3.1 SL 表行、§7.3 两树读法）；② **94 的「页面视角」补法草案待人类定**（拟 §1.1 补「第三个视角：页面视角」+ §2.5 补"功能表单挂本树、不挂系统需求树"，见 [[94-eos-sys-dev-dbs-tree-authority]] 收工盘点第 11 项）；③ 数据文件侧同口径残留未动（06 §0/§3.1/§4.1「srh-biz 架构末级（功能表单）」、05 §4.3 srh-eng 块「节点类型：功能表单」）；退役中的 wft0{1,2,3}-biz 三份按规则不改。

**同日续 v0.14 —— 原 §三.0 拆归其位**（人类定「3.0 的内容与其他节不在一个维度上」）：§三 七个子节全是"**人类方案第 N 章写什么**"的内容规则（一章一节、互斥穷尽），而 §3.0 管的是**跨章**的呈现要件（ch2/ch3 必给树图＋级语义表）＋**权威归属**（单源=94）＋**施行口径**（过渡期）三样，套不进本章概述的目录契约（"每章统一给：应含/禁含/规则来源"）。拆法：① 呈现要件 → **§2.3「通用必给要件：输入/输出文档的树形结构图」**（与 §2.1 骨架总览、§2.2 组织原理同族）；② 单源=94 + 过渡期 → **§4.1**（与「树与 schema 分工」注合流，同属 schema 单源族），两注互指。引用随迁 5 处（§1.3 引用表、§三.2、§三.3、头注两处、试点 biz01 §3.2「单源（93 §三.0）」→「93 §4.1」）。**可复用判据**：判断一节是否错位，不必先想"该放哪"——先试**"它符合本节的目录契约吗"**（套不进即维度不同）。**同日再定「正文不留日期·来历标记」**（人类原话：「（2026-09-04 定；2026-09-15 由原 §三.0 上移）等内容，不应该出现了」）——93 正文 15 处 + 头注内联变更史、三试点 8 处一并清掉，来历归 §变更记录；规则入 [[feedback-changelog-concise]]。**其后人类逐句通读 93 的表述层**（仍在进行），已修两处：v0.15「声明本任务在对/链上的坐标」→「在链上的位置——哪一对、哪一阶段」（§1.1/§2.1 表/§三.1 三处，隐喻首次出现未兑现）；v0.16 §1.4 术语表**就地定义 A／B**（A＝上游任务、B＝下游任务，随接缝而变）与 A.ch3／B.ch2 的章位——原表在 A/B 未定义时即用其缩写。**同类问题一类**：抽象词/缩写在**首次出现处不兑现**（答案在后文，或通篇没定义）；判据＝这个词第一次出现时能不能就地回答"它指什么/由哪几个量构成"。**续修（同日）**：v0.17 §2.3 重写（去自造词"通用必给要件"、目的由否定式改直陈）；v0.18「翻译／源语言／目标语言／文法」类比换 **Y=F(X)**（自变量／函数值／映射规则）——翻译保义·可回译·句对句，与"Y 造出新内容""N:1 单承接"不合，而函数自带宽该基数规则（连带试点 biz01）；v0.19→v0.20 **试点状态断言先降级、后按人类「不要提试点的内容」全部去除**——**规范文档只写规则，不写实例的进度／写法／是否可参照**（实例属另一层次且未定稿），§4.2 保留"核对方法 = 逐条映射"与"首条边界例外"，去掉试点核对结论；v0.20「对」首次出现改全称「**需求-方案对**」+ §1.4 术语表补「对」行。**教训：我在本轮 v0.14 里主动加过"三试点 ch2/ch3 是本条的首批落地处"——规范里不该出现实例，这条要记住。** 其后 v0.25 全文 `chN` 缩写改中文章名（91/94/20 号均不用该缩写、93 也从未定义），v0.27 **全文去括号**（正文约 182 → 6，只剩标题/章名后缀/版本标识/引用原文括注四类）——规则入 [[feedback-avoid-content-parentheses]]。**同日续**：三试点按 00-doc-conventions §8.9／§8.10 通篇改写——**正文括号 708 → 70**（biz01 283→24、biz02 209→23、biz03 216→23），余者皆为标题/章节后缀或版本标识；同期删历史（biz01 的"原枝梢+叶混入为源已作废"）与待办（biz03 的"94 §2.5 待收口""开发线对侧待成文"、biz01 的"待 §2.5 收口回填"）。版本 biz01 v0.18 / biz02 v0.16 / biz03 v0.16-pilot。（00-doc-conventions + CLAUDE.md 已在 69b12ad 提交并推送。）**同批未决**：附录B 的「§〇 链上坐标」——v0.13 已改"第一章（§1.1）"，但试点三份的**文件头前置引言块**（`# 标题`→`人类方案ID`→链上坐标/版本/定案注记/自足声明，然后才是「## 一、目的与目标」）很可能才是原指代，待人类定是否回退为「文件头前言」。

**同日续 · 93 v0.30：Step 条校正**（人类问「为什么转换流程及检验规则不允许包括 Step」）——原 §三.5 禁含把「Step Start/End」与反馈双轨、git 基线、写回脚本并列，会被读成"禁提 Step"，而 Step 1…N 恰恰是第五章要写的流程（三试点 Step 数各异，91 附录A 替不了）。改为「**动作序列就是 Step 序列**：一节一个 Step，标题『Step N · 动作名』，不另起平行编号」，禁含收窄为**会话协议**的复述；§2.2 组织原理同步。

**挂账（人类 2026-09-15 定「三个试点文档，后续要修订」）**：① **三试点 Step 对账**——现 §5.1 动作条数与 §5.4 的「Step Start/1~N/End」对不上（biz01 7 条动作 vs "1~5"、biz02 6 条、biz03 8 条），按 93 v0.30 应合成一套、§5.4 瘦身为只列特化参数；倾向以 §5.1 为准（它是新骨架重排过的主干，§5.4 数字抄自旧版），**待试点修订时一并办**。② 三试点正文括号改写已完成（708→70），**已提交并推送**（37ad24c）。

**下一会话开工点（人类 2026-09-15 定「新开一个对话，便于继续审视优化 93 文档」）**：带着**干净纸面**继续通读优化 93。已过进度——人类逐句审视已走完 **§1.1（链上坐标）／§1.4（A／B、术语表）／§2.1／§2.2／§2.3／§三.1（应含六条）／§三.2（定位与六条）／§三.3（注块）／§三.4／§三.5（Step 条）**，对应 93 由 v0.13 走到 **v0.30**。**尚未过**：**§四 跨文档一致规则（含 §4.1 判据归属表与两条注、§4.2 接口契约、§4.3 链尾）／§五 与既有文档体系的关系／§六 编制工作流与检查清单／附录A、附录B**。接手时先读 93 现状（v0.30，提交 37ad24c）与本文档，再让人类从 §四 起往下点。**注意**：人类读法一贯是「指出一处不聚焦／不好懂 → 我诊断＋改」，诊断要给**判据**（可复用的一句检查法）而非只改这一处。

**当前状态（2026-09-15 收工）**：93 **v0.35**、文件 **`93-eos-skill-guide-spec.md`**、标题《设计与开发 SKILL 指南编制规范—93 号规范》；三试点 v0.18／v0.16／v0.16-pilot；91 v5.61；00-doc-conventions v2.11；全部**已提交推送**（本地 master = origin/master = gitee/master = `082c67c`）。同日链级更名「人类方案 → SKILL 指南」见 [[skill-guide-renaming-2026-09-15]]。

**未办（挂账）**：

1. **94 §1.4 承接表 06→07 行标【待裁】**——06 系统功能需求树的叶 ⑥ IPO功能 落到平台构成树哪一级。93 §4.3 正文原有一句「本处随之待 94 收口」，2026-09-15 按 00-doc-conventions §8.10 删（待办归挂账），挂在此。
2. **07 两名并存**：20 号报告称「**平台方案概要**」，94 §1.1 名表矩阵称「**平台构成概要**」；93 随 94（§4.3 现在只引 94 §1.1）。是否在 94 §1.1 注一笔 20 号的称法，未定。
3. **93 §五／§六／附录 A／B** 尚未逐句过。
4. **15 个 SKILL.md 删变更记录后文件内再无版本标识**（它们原本不设头部版本行，版本号只活在变更记录里）——是否照 91／93／94 补一行版本行，未定。
5. **93 §4.4 已整节并入 §4.1**（作为节末「口径冲突」注），其两条重复指针删除；§四 现为三节。

**2026-09-15（本会话）· biz01 重构落地 v0.18 → v0.19-pilot**——依据 93 v0.35 + 94 v1.18。人类指示「以 93 号规范和 94 号文档为依据，重构 biz01」，先出方案→逐条裁决→执行。**四条裁决及理由**（人类未反对即采纳）：

1. **Step 数 = 五步**（设计材料加载／反馈处理／承接设计循环／覆盖与完备检查／写回与落账）。**纠正了此前挂账里的误判**：原记「§5.4 的 Step 1~5 抄自旧版、倾向以 §5.1 七条为准」——错。逐条对 91 附录A 后，§5.1 七条里**只有五条是任务特有动作**，另两条是被 91 明文收走的协议两端：「入口」= 91 §A.4 Step Start（其入口决策流程逐字就是受理判定→退回优先→反馈→路由四步；特化项 = §2.4 受理门槛表）、「人类确认」= 91 §A.3.2 Step End 三区反馈区。七减二 = 五，且**正好等于现行 `eos-wft01-biz-spr2bph/SKILL.md` 的 Step 1~5**。故 §5.4 的数字本来就对，错的是 §5.1。落法用 `#### 5.1.N Step N · 动作名`（H4）而非把 Step 提成 H3——避让 00-doc-conventions §2.3「编号一旦分配不因结构调整而改变」，5.2/5.3/5.4 与全文交叉引用原地不动；章首加「Step｜名称｜产出｜写资产」总览表，将来原样提取进 SKILL.md（91 §A.2.4 要求 SKILL 第三章有此表）。

2. **头注定案注记 = 逐条判死，不是"全部分散归位"**。biz01 七条的判定：③PL6 定位／④判据归属／⑤X 读法／⑥R／P 两树 —— **四条已在章内**（§3.3 引用块／§4.4+§6.3／§2.2／§3.2），删；**⑦P 完备机制 = 真残留**（§3.5 只有"候选节点建议"五字、§5.2 只有"补充材料回路"、§4.4 只有"缺口推断"，而"缺失时以候选节点挂 P 树标疑似态；人类裁决需要→转原始需求材料→ort00~03→str；不要→候选废弃"这条完整规则全文无第二处）→ **迁入 §4.4**；①②属命名、随命名轮。头里最终只剩：本文件是什么 / 命名约定 / 自足声明 / 版本行。**链上坐标与"概要的位置"移入 §1.1**（93 §3.1 应含）——原头注与 §1.1 重复。

3. **「需求簇」→「场景业务」（采 94 之名）。硬证据：全链 grep「簇」= 0**——ort00／ort01／ort02／ort03 四个 SKILL、91、94、01 数据文件**一次都没有**；「需求簇／建议簇」是 **biz01 v0.12-pilot 自造词**（变更记录里"需求簇=建议簇待复核"是出生证）。同一对象的链上定名：94 §2.0.2「切分产出的业务对象闭环就是相关方需求的**场景业务级**」「同一对象，链上称闭环、树里称场景业务」；94 §2.1.1 树 = 相关方角色 ① → 场景业务 ② → 需求条目 ③；ort01 命名约定「业务对象闭环片 = **主责角色 + 场景业务**」；ort03「**场景为锚**（主责角色 × 业务对象闭环）」「场景为边界上限，场景内不设中间单元」；91 §7.1「推断**场景业务**节点」。随此三处连带：①X 树按 94 §2.1.1 重画为**三级**，根＝相关方功能需求（01 含功能/非功能两棵树，本任务只消费功能树），**业务域降为 ① 相关方角色的属性、不是树位**；②「升格」动作取消——X 上这一级由 ort01 切出即成立，biz01 **没有重切 X 的权力**（93 §3.2：X 的分解决有权归谁、切得不对退回给谁重切），§2.3 改写为「切分权在 ort／本任务只复核边界；边界不通 → 标 `需退回重切` 回 ort 链」，并单列一条「**Y 侧的节点拆分与重切 X 不是一回事**」（原 §2.3 把两者混在一句里）；③01 物理文件确无场景业务级（结构 = 角色分片 → spr-NNN），如实保留"级待 01 模板重建落位、落位前按条目正文语义归拢标 `[待复核]`"的交代。

4. **命名与试点身份本轮不动**。量过成本面：「biz01」全仓 74 处但**只有 4 个非记忆、非试点文件**（三试点 + `04-eos-business-detailed.md`，而 04 已同时用 biz02/wft02-biz 两种写法）；真正的成本在**替换现行 wft 名**——91 §10.1 三路径全景图把每个 skill 按现行全名画死（下游还有 §10.3~§10.6 参数速查）、ort03-norm 有 6 处把移交目标写成 `wft01-biz-spr2bph`（含流程图 `├─ FR-BIZ → wft01-biz-spr2bph`）、15 个 SKILL.md 的 `name:` 与设计依据链接（**其中只有 wft01-biz 这 1 个有 7 条指向指南锚点的深链**，其余 10 个为 0——六章骨架重排过，换文件即断）。理由面：命名替换 = 把**整条现行 wft 链换成试点链**，而三试点里本轮只有 biz01 动，biz02/03 的内在结构（Step、级语义表、树权威、级名）都没同步——**目标态没成形前改名等于名字换了内容还没换**。故只做一件与命名相关的事：把「本文件是什么」从"93 骨架的试点改版——推演新写法"改为如实陈述（与现行并存、作改版候选、替换时点随链级命名统一）；头注 ①② 相应留到命名轮一次性扫。

**人类新裁决（本会话）· 全链不用 PL／SL 层级**——人类原话「不用 PL和SL层级了」。**biz01 已清 0**（原 15 处）。**波及面已量**：24 个文件约 526 处（91 有 77、`eos-wft02-biz` 127／`eos-wft03-biz` 100 为退役旧版按规则不动、02 数据文件 23、25 有 22、24 有 4、28 有 12、04 有 7、另两份试点 23+10、15 个 SKILL.md 亦在内）。**一处硬依赖**：02 数据文件 §4.1 模板的 `PL5 枝梢`／`PL6-A 活动预告`／`PL6-B 条目预告` 是**写进节点的实际内容**，biz01 去层码后两边将不同词——须与 02 模板同批扫（不阻塞本轮，biz01 的字段名本身不含层码）。同类：91 §五仍用 PL1~PL5／SL3~SL6（即 [[pending-91-align-to-94]] 挂账）。

**断链修复（本会话，3 文件 4 处）**：93 §4.2 的「**总表**」在 v0.20 已按人类定「规范不写实例」撤出，但 biz01 §3.6／biz02 §2.3／biz02 §3.6／biz03 §2.3 四处仍写「逐条对照（与契约校验汇总）见 93 §4.2 总表」——**链三节全断**。四处统一改指「**核对方法 = 93 §4.2 的逐条映射**」，不再假称有一张表。

**其余随改**（biz01 内）：§1.2 主要目的改引用块；§1.4 不负责栏补归属（X 切分调整 → ort 链；治理概要 → 人类渐进增补 + ort 补需求）；§3.2 R／P 树对齐 94 §2.2.1／§2.2.2——补根（角色履职业务／输出产品业务）、⑥ 正名「活动节点／正文条目」、加「**任务不占树位**」注、加 **Rn 与 R0 层级差**注（R0 少「角色职责」一级，治理节点 bph-biz-GOV-* 走 R0）、④ 名改「构件开发WBS（包）」、**树权威由「将落 23 树画像区」改指 94 §二**（93 §4.1 已定树权威 = 94，这是 v0.18 最实质的一处错误）；两张级语义表补 **「怎么得到」栏**（93 §2.3 四种来源：承袭／本任务产出／AI 建议+人类确认／目标组织级）；§2.2 级语义表同补；Q 族全名化为「对齐 Q1~Q3」（91 §5.3.1 有三族同名 Q，勿裸写）；Reuse／Add 改中文「复用／新建」；本文件内小节引用统一 §x.y 写法（对 93 的引用保留「93 §四.1」以随其本地风格）。版本 **v0.19-pilot**。

**本会话新增挂账**：① **biz02／biz03 同构待办**——Step 归并（各 7 条动作同型）、级语义表补「怎么得到」栏、树权威改指 94、级名（若涉簇）、去 PL／SL 层码；② **02 数据文件 §4.1 模板 + 打样节点去 PL／SL 层码**；③ **91 §五去 PL／SL 层码**（与 [[pending-91-align-to-94]] 合流）；④ biz02／biz03 尾章「已回填 93 §三.2/§4.2/§4.3，作试点记录」现**表述已不实**（93 里那些**试点结论**已于 v0.20 撤出，留下的只是**规则**）——待各自重构时改；⑤ 三试点其余文档侧口径：`04-eos-business-detailed.md` 用「biz01/02/03」新代号，与链级命名轮联动。

**2026-09-15（同会话续）· biz01 v0.19 → v0.20-pilot，随 94 v1.19 重画 §2.1.1**——人类说「相关方功能需求的树形结构变了」，据此重构。**94 v1.19 的新形状**（提交 fcba811）：`相关方功能需求 → ① 业务输出文档（根；所属主责角色／所属产品构件／所属E2E场景业务三属性）→ ② 任务节点 → ③ 系统用例场景级操作（叶，`stfr-XXX`）`——角色与场景由树级降为 ① 的**属性**（索引／分片轴），原注「条目止于场景级、场景内不设输出文档／开发任务节点」作废；该形状即 94 v1.13 从 §2.2 删掉的那对（输出文档→文档开发任务节点→用例场景类操作），改挂 01 树。连带：§1.4 承接表 01→02 行（上游叶改 ③、**落点改由 ① 的「所属E2E场景业务」属性定**）、§2.0.1（切分＝长出业务对象闭环、规范化＝长满 01 树）、§2.0.2（业务对象闭环在 01 树里是属性值、不再是树级）。**l 上轮 v0.19 的 §2.2（① 相关方角色／② 场景业务／③ 需求条目）整块作废。**

**biz01 v0.20 的改面**（八处）：①§2.2 X 树重画为上述三级 + 级语义表重写（① 的处置＝「落点与认亲坐标的来源」、② 的处置＝「枝梢任务类型的捕获来源」）；②§2.2 注 2「文档不是需求树的一级」**整条删除**——文档现在是 ①，正是树的一级；③§2.2 注 1 改「落位现状」（现行物理文件仍按角色分片挂条目、尚无 ①② 两级，读时按正文语义归拢标 `[待复核]`）；④§2.3 复核项改「① 三坐标齐备且唯一 + ③ 语义与三坐标一致」；⑤§1.1／§1.3／§1.5 承接落点改由属性定；⑥§3.2 补「**⑤ 与 X 的 ① 同指**，认亲坐标由 X 给出」——同一张文档三视图：X 侧需求承载级／R 侧履职对象／P 侧产品数据位；⑦§4.1 改写为「**落点由 X 给出**（94 §1.4）→ 同一 ① 下的叶共用场景属性、**必落同一场景业务——单承接由树形保证**、不靠逐条检查」+「① 整体分流」+「落点已定而 ① 内部语义跨界＝X 缺陷、退回重切」；⑧§4.2 生成项表补「P 认亲」行、文档清单改「**以 X 的 ① 为准**，不另立一份」、任务类型改「**读 ② 任务节点直接捕获**」；§5.2 单承接检查同改。术语统一：需求条目→条目、需求叶→叶（94 §2.1.1 用「条目」为叶的通称）。

**三条我做的推导（须人类复核，已写入 biz01 正文）**：① **① 的三属性即 Y 两棵树的坐标**——所属E2E场景业务→R 树 ④ 承接落点；所属产品构件→P 侧认亲落点；所属主责角色→21 业务域→P 产品类型路径；② **业务输出文档清单由 X 给出**（① 即文档），故 §4.2 的「明示／隐含／同类场景推断／资产线索推断」四来源收敛为「以 ① 为准 + 缺口走补充材料回路」；③ **任务类型不再全量推断**（读 ② 即可）。三条都直接读自 94，但 94 未逐句点出，属我的解释。

**由此挂账**：**① ort03（正文 + SKILL）未随**——它仍按「切出场景业务级 + 条目落位」设计，与 01 树新形状（① 业务输出文档／② 任务节点由谁写出）不一致，是 [[94-eos-sys-dev-dbs-tree-authority]] 待裁第 12 项的另一半；**② 01 数据文件树画像区**未随（且 01 至今无树画像区，见该记忆待裁第 10 项）；**③ `spr-`→`stfr-` 链级更名未动**（94 内已换、链上 112 处／22 文件），**biz01 本轮有意不写 ID 前缀**以回避该分叉；**④ `stfr` 的 F 覆盖不了 FR-BIZ／NFR／FR-ENG 三类**（94 待裁第 14 项）——biz01 §2.4 仍靠 `类型` 字段路由，未受影响但待分族定案。

**2026-09-16（本会话）· 立「93 §2.4 正文写法」+ biz01 去代号清引用**——人类逐轮收紧，成条后要求**记进 93**（并问「如何让 SKILL 指南符合这些要求」）：

- **93 v0.35 → v0.39**，新增 **§2.4 正文写法（跨章通用）**（插在「二、总骨架」，不动现有章号与外部引用）：①读者不必跳出当前句（引用不作主语谓语、同一权威源全文只挂一处、内容写全处撤出处）②不写元评论 ③不写来历与待办 ④**代号白名单**（正文只允许 X／Y／R 树／P 树 + 八行落法表；**保留原值**一类＝状态值／节点 ID／字段名与字段值）⑤文件头前言块四行、链上坐标进第一章 ⑥缩写与编号首次给全称。§6.2 成稿自检三问扩四问。
- **代号白名单的来历**（人类逐轮收）：先「01 含两棵树」的编号当名 → 再「同 ① 优先成批」的层级序号 → 最后「**可用的代号只有 X、Y，以及 R树和 P树，其他的通通不用**」。**要害是符号多重载**：`①` 同时是「树第几级」「需求-方案对第几号」「句内第一个」——同一文件内两种含义，读者须先判断是哪种。**「需求-方案对①②③」也去掉**，改按两端文档称呼「相关方需求 → 业务流程」。见 [[feedback-code-whitelist-2026-09-16]]。
- **biz01 v0.20 → v0.21-pilot**（约 127 处改动）：指向 93／94／91／20 号的引用 **33 → 20** 处（93／94 两类 17 → 8）；约 45 处代号换本名（文档写本名、资产写「21 相关方角色资产／23 输出产品架构／25 引擎资产」、规范首现给文题、任务改「阶段二任务／需求预处理链」、需求类型写全称、D／PCA→执行类／管理类、bph-biz→业务流程概要节点）；文件头删「与 `eos-wft01-biz-spr2bph` 并存／改版候选／替换时点」整句（溯源已在变更记录 v0.9 行）；§1.1 图与 §2.2／§3.2 树图保留 ①②③（93 §2.3 本就要求图给级位编号），图里 01／02／04／05 撤掉。
- **「迁移到 93」的人类决定：不搬，但能用到**——00-doc-conventions §八、§十三 均保持原位，由 93 出引用与口径：§1.3 引用表把 00-doc-conventions 的「角色」栏写实为**对 SKILL 指南为规范性**；§3.5 补「Step 布局总览表」条目引 §十三；§2.4 收口「其余各节直接适用，不必另行特化」。

**同日纠正一处我的误判（留作教训）**：我曾建议**退役 00-doc-conventions §十三**，理由「与 91 §A.2.4 列头打架、全库无人执行」——**错**。我只 grep 了 SKILL.md（11 份确实都无总览表），漏了指南本体：**§十三 的富列组被 15 份现行指南在用**（4 ort + 11 wft），91 §A.2.4 的精简列组反而只有 1 份工具 SKILL（`comformed-2-91spec`）在用。§十三 的适用行写「所有 Skill 文档」但举例 `wft01／wft02／ort01`——**全是指南名**。判据教训：**查一条规则死没死，要按它的适用行去找执行者，不能按自己的直觉找载体。**

**§十三 已修正并接入 93**（2026-09-16 人类授权"改"）：00-doc-conventions **v2.11 → v2.12**——①适用行去「所有 Skill 文档／§三 操作流程」的错指，改明**两类文档各自章位**（指南侧第五章、SKILL 侧第三章）；②列定义 **6 列 → 7 列**，补「**人类参与**」列（值形如 `无（AI 自动）` / `有——{形式}`，End 行恒为 `有`）；③「执行模式」取值口径去掉已废的「两链并行／仅某链」，改按实际取值 `统一／对话／人类／按路径／三路径分别／按批次`。93 v0.39 侧：§1.3 引用表把 §十三 并入「对 SKILL 指南为规范性」的适用行；§3.5 补「Step 布局总览表」条目（列定义引 §十三；SKILL 侧取列仍以 91 §A.2.4 为准）。

**biz01 v0.21 → v0.22-pilot**：§5.1 Step 序列改用全链统一列组（7 列 + Start／End 两行），与其余 15 份指南一致。**由此定下「新骨架指南用富列组」**。

**同日 · 头注改形 + biz01 指南ID 更名**：
- **头注只记两样**（93 §3.7 定稿，人类同日两度收窄：「变更记录，不再保留」→「只记录文档的开始日期」→「经历了多少版本」）：`**创建**：YYYY-MM-DD | **版本**：第 N 版`——**修订日期不写**，来历归 Git。93 §2.4 三条同步改口；93 骨架总览收尾章去掉「变更记录」项；biz01 §7.3 整节删除。**版本计数 ≠ 版本号**：biz01 从 v0.9 起算故为第 15 版（非第 23），93 从 v0.1 起算第 40 版；两套数并存的隐患见 [[feedback-changelog-concise]] 续二。
- **biz01 指南ID `biz01-str2bph` → `biz01-stfr2bph`**（人类 2026-09-16 定）——随 94 v1.19 的叶前缀 `stfr-`（相关方功能需求）；`str` 是 20 号旧称「相关方需求」，与本任务实际只消费**功能**需求树不符。**文件名 `eos-biz01-str2bph.md` 未动**（待定，见下）。
- **93 §2.2 组织原理与 biz01 文件头同句改写**：原「一份 SKILL 指南 = **一次 Y=F(X) 的完整说明**」**不准确**——量词挂错了对象：一个需求-方案对才是完整的一次 Y=F(X) 求值，一个任务只是那个对里的一步（biz01 只产出 Y 的概要）。改为「**一份 SKILL 指南说明本任务这一步的 X→Y**……**这一步不等于整个需求-方案对**」。附带去掉光秃秃的「规则 F」。

**待定（同日新增）**：① **文件名是否同改** `eos-biz01-str2bph.md` → `eos-biz01-stfr2bph.md`——现仅 biz02 §3 有一处链接指向它，改动成本小；但「文件不更名」是 2026-09-15 链级更名时的定案，是否沿用待定。② **计数式版本号**——「第 N 版」与 `v0.23-pilot` 两套数并存，是否把版本号直接做成计数（v1、v2…）。③ **00-doc-conventions §一 头部元数据**（文档版本／创建日期／修订日期 三件套）与本条冲突，未动（改它 = 该规则升为全项目）；它自己的头注也未改，保持自洽。

---

## 2026-09-16 第三会话：R／P 拆文档 + 任务位重编号 + 91 页面四级模型

**已提交**：`a7011403`（93 第 44 版去独立资产文档 + biz01 第 30 版）、`7aefef39`（本轮主体，11 文件 +1255/−460）。**两个都未推送**。

**落成五件**：

1. **91 v5.61 → v5.62**——页面层级首次与树级对齐：场景标签页↔④／窗口标签页↔⑤／表单标签页↔⑥／功能操作项↔⑦，菜单标签页只是浏览器标签页容器、不占树级。详见 [[page-ui-four-levels]]。
2. **94 v1.23 → v1.26**——业务流程拆三份文档（`02` R 概要／`03` P 概要／`04` R 方案，**04 恢复**）；承接回到 `04→05`／`06→07`；§2.2 补 R／P 适用范围（"把业务信息化"这类产品特有）与"详细定义的落点是界面"。详见 [[94-eos-sys-dev-dbs-tree-authority]] 末节。
3. **93 第 43 → 46 版**——v44 去独立资产文档（第六章「资产文档结构及规则」→「**关联产品数据及规则**」，21~28 全清，对齐判据的引用数据改指"被对齐的那棵树"）；v45 R／P 拆分的连带（组装型通则化、边界由两类增到三类、树图要件）；v46 删「两形态合并」、新增「**阶段一可以由两个并列任务分担同一份产品数据的两个视角，合占一个任务位，编号同号加字母**」。
4. **biz 链重编号 01a／01b／02**——见 [[biz-chain-renumbering-2026-09-16]]；三试点文件已 `git mv` 改名。
5. **数据文件**：新建 `02-eos-business-role-process.md`（R 概要，9 个治理节点自旧 02 迁入并更名 `bpd-biz-GOV-001~009`）、`03-eos-business-product-process.md`（P 概要，骨架＋P 侧模板＋迁移区）；`04-eos-business-detailed.md` 撤废弃、正名为「角色履职视角业务流程方案」v3.2；旧 `02-eos-business-summary-architecture.md` 挂废弃。

### 下一会话开工点

**重写 `eos-biz02-rbph2rbpd.md`**（文件已改名，正文还停在旧形状 `04→05` 承接型、活动节点／PL6／功能表单）。目标形状：

- **链上坐标**：需求-方案对「业务流程」的**阶段二**（同一任务位 01a／01b 之后的下一任务位）
- **输入语义**：**落叶型**（93 §3.2 第三类实例）——X = `02` 的概要，本任务把骨架落到叶定义
- **Y** = `04` 角色履职视角业务流程方案
- **两只手**：① 对**界面四级**逐级写实（这正是 91 §7.3 那七项任务的形状）② **补引擎类原始需求**——从 01a 搬来，在这儿才判得准（01a 时用例场景还只是名字级；91 §7.2 任务 8 也明说引擎能力对照归 wft03-biz）
- **对侧契约**：上游 = 01a 的传出承诺，下游 = 系统需求概要设计任务的受理前提
- 参照 `eos-biz01a-stfr2rbph.md`／`eos-biz01b-rbph2pbph.md` 两篇的形状

### 仍开着的

1. **01a 的覆盖检查缺口出口**——引擎类搬走后 §4.4 空了，覆盖检查若检出缺口就无处可去。要么它的缺口 = R 侧缺陷走 `需退回重切`，要么留一个"交人类裁决"的口。**这条要连 01a 一并解决。**
2. **`02` 的节点 ID 前缀**——R 侧与 P 侧两份若都用 `bpd-biz-*` 会撞号；旧规矩按形态分前缀（`bph-biz` 概要／`bpd-biz` 方案），要不要改成 `rbph-biz`／`pbph-biz`／`rbpd-biz`，随命名轮。
3. **链级清扫**——`01` 的追溯表、`91` §11.1 的文档全景表还指着旧名；`spr-` → `stfr-`（112 处）；`场景业务` 残留；91 的 PL 层码（压在 [[pending-91-align-to-94]] 下）。
4. **93 自身**——19 处内部小节引用、散句里的「需求-方案对②」「05」「06→07」未扫；§五／§六／附录 A／B 仍未逐句过。
5. **eng／nfr 两链**——R／P 拆分是 biz 特有，但"详细定义"那一层 eng 有对应形状，要跟着理；**这一轮没动，先走通 biz 链再谈**。

关联 [[94-eos-sys-dev-dbs-tree-authority]]、[[feedback-code-whitelist-2026-09-16]]、[[feedback-avoid-content-parentheses]]、[[pending-91-align-to-94]]、[[biz-chain-renumbering-2026-09-16]]、[[page-ui-four-levels]]。
