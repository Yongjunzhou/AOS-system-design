---
name: pending-91-align-to-94
description: 91 号规范的整篇反向修订（2026-09-22 人类启动）：三块整节重写、三块补正、两处自相抵已勘查成单；不再暂停，等范围点头
metadata: 
  node_type: memory
  type: project
  originSessionId: d9d470f3-bb34-43d0-9c31-85ebd491b29e
  modified: 2026-09-22T12:48:10.670Z
---

**人类 2026-09-22 定**：「91号规范是过时的，需要根据本文档等进行反向修订」。**此前 2026-09-15 的「不是现在」作废**——本项不再暂停。

**人类 2026-09-23 重申**（原话近似）「我已经多次说过，91 已经过时了，需要根据新的文档反向续订」。**做法固定为**：凡遇 91 与现行文档（94／93／三试点／01~04）相抵，**以新文档为准**，并**顺手把 91 改成新文档的样子**——不再引 91 作判准、不再报「某处与 91 相抵」等裁决。当日由这条规则查出的两处实例：① `94` §2.2「详细定义的落点是界面」那条注把「**菜单**」列在四级之首（应为**场景标签页**）；② 91 §1.1 的「菜单标签页…单击末级菜单 M4 系统新开一个浏览器标签页…由若干窗口标签页组成」与同节注「末级菜单 M4 对应一条场景业务，即树级 ④」相抵——人类口径为「**末级菜单与场景标签页一对一；场景标签页之上对应的是末级菜单之上的菜单**」。

改的方向＝**以试点为准**（[biz01abr](../../20-pl4eos/10-pl4eos-subpd-sysdev/10-wfsysdev-4-eos/eos-biz01abr-stfr2rbpl.md)／[biz01abp](../../20-pl4eos/10-pl4eos-subpd-sysdev/10-wfsysdev-4-eos/eos-biz01abp-rbpl2pbpl.md)／biz01cr）＋ 93（编制规范）＋ 94（树权威，v1.42）＋ 01~04 数据文件，见 [[eos-pilot-authority]]。

## 勘查结果（2026-09-22；三路并行只读，行号动笔时当次现核）

### 整节重写

**§七 业务系统之系统设计**——91 写的是三环串行 `wft01-biz → wft02-biz → wft03-biz`，现行是**两工位三指南**：工位一由 abr（相关方需求→R 树概要）与 abp（R 树概要→P 树概要）**并列合占**，工位二是 cr（→业务流程定义）。连带：①「P 组装执行上移 wft01」的定案作废（P 组装就是 abp 本体）；②引擎权威从 bpd-biz 改到 cr；③末级名 `PL5 输出文档`／`PL6-A 活动节点`／`PL6-B 正文条目` 全换成 R 七级／P 五级的本名（PL6 在 91 有 35 处，三指南 0 处）；④状态 `可以分解分配`／`可构件开发` 已被 `待确认方案→可以srb设计→在srb设计→已srb设计→待补充srb设计` 取代；⑤资产名 `bpl-biz／bpd-biz／srb-biz` 换成文档本名（三试点明说不设代号）；⑥**91 缺「业务流程定义」这一工位**（cr 就是它，无对应节）；⑦「工位／需求-方案对／环节」骨架要新引入（91 全节「工位」零命中）。

**§十一 产品文档结构、读写及反馈**——§11.4 的末级块式（`##### @{id}` ＋ 父节点／输入／输出／子条目状态／确认状态）与 01~04 实际的块式（`#### bpd-biz-{id}` ＋ 节点类型／块ID ＋ 块头字段表 ＋ 要素表）**没有一个字段对得上**，而 02/03/04 又都自述「schema 单源＝91 §11」；六层层级（业务域／项目类型／端到端业务／WBS／输出文档／构件）与 94 的 R 七级、P 五级相抵；ID 前缀 `@spr-`／`@bp-`／`@srf-`／`@prod-` 一套也不认；`## AI最近变更`／`## AI可以处理节点`／`## 自描述索引`／`## 业务域索引` 在 02/03/04 **一个都没有**（它们用「树画像／状态队列／节点索引／反馈区」那套），§11.9／§A.3.3 依赖这些分节与 `HEAD @上次 AI 运行` 的脚本在 02/03/04 上跑不起来。**§A.7 变更传播协议是这片唯一可整体保留的**。

**附录A 第二部分（§A.2／§A.3「Skill 文档怎么写」）**——91 的五章是「职责定位／领域概念基础／操作流程／**AI 标记约定**／**变更记录**」，现行是「目的与目标／输入文档结构及规则／输出文档结构及规则／输入到输出的过程及规则／执行步骤及检验规则」；且 91 要求第一章含「负责／不负责」，93 明文**禁含**。§A.3.1 的 `资产对齐：23=Q1` 例子随 23 退役失效；§A.4.3 的退回门禁还在看「人类标注 `[修改]`／`[同意]`」，与 §11.8 自己的单轨＋`确认状态` 由 AI 单方写入直接相抵；§A.5 的模式分化表整表只覆盖 ort 链、且仍在讲「双轨」（93 §1.2 已说 ort00~03 暂不适用该表）；§A.5.4 特化参数的「资产对齐级联」项试点不写。§A.1 阅读地图与 §A.8 速查仍画旧五章。

### 补正

- **§八 eng**：五 skill 名／序／状态与现行 `eos-wft01-eng`…`wft05-eng` 完全一致；只需改「设计角色＝业务配置人员」为三层分工（流水线开发者／AI／EOS 开发者）、处置 ⑤用例场景功能／⑥IPO 功能的末级表述（eng 指南两词零命中）、一处 R0 写成 R。
- **§九 nfr**：两 skill 名一致；补状态（`待确认方案`／`在NFR设计`／`待目标绑定`／`待补充NFR验证`）；核一处「**九种** vs **七种**页面类型」。
- **§十**：biz 部分（§10.1 图的 biz 段、§10.3 全表、§10.6 biz 参数表的列头／各 skill 名／状态／退回态／资产写回行）重写；eng／nfr 部分补正；§10.1 的 ort 链要补路径（在 `00-presysdev-4-eos/`）。
- **附录A 第三部分**：§A.4 换掉旧任务名示例；§A.5 删「双轨」「口令表」；§A.5.4 删「资产对齐级联」；§A.3.2 三区模板基本相符、只调命名。

### 两处 91 自己跟自己相抵（与下游无关，顺手清）

1. §十一 与 §A.3.3 都写「适用资产文档 **21~28**」，而 §11.1 清单与 §864／866 已剔掉 23、26，00-doc-conventions 那边写 **21~26**。
2. 读取定位机制：§A.3.3 R5 说靠 `read-node.sh` 脚本，§A.8／§11.9 说靠 grep——两套并存。
3. **顺带**：93 §3.5 写「总览表取列以 91 附录A §A.2.4 为准」，而 §A.2.4 给 4 列、§13.2 与试点是 7 列——93 那句要一起改指 §13.2。

## 早先定位的四处偏差（2026-09-15 审视 93 时清扫所得，仍在）

1. **§5.2 Rn 树自相矛盾 + 落后 94**——91 说「Rn 末级＝场景业务。沿 SL：SL3 角色类型→SL4 角色职责→SL5 场景业务→SL6 功能表单」，同节末尾又写「wft01-biz（**场景业务为枝、文档为末级**）」。
2. **§5.3 Pn 树层级名与条数对不上 94**——`PL2 E2E 项目业务`／`PL3 构件类型`／`PL4 WBS`／`PL5 业务输出文档` 对上 94 v1.42 的 产品级E2E业务／构件级E2E业务／WBS业务／文档级E2E任务。
3. **§3.1 四套视角表的 SL 行同病**——尾巴接「功能表单/主子表单」（功能表单不占树位、属页面视角）。
4. **§7.3 的两树读法**——与 94 给两棵树各自都设 ⑤ 的形状不同。

## 本轮已解掉的一处依赖

**「P 树一致性三类」已由人类 2026-09-22 裁定收掉**（abp 表4-5 整张撤、表4-6 顺号成表4-5）——所以 91 **§5.3.1 的「一致性三类」与 §7.1 任务清单第 7 行那半句**可随之处置（不再等）。改法随本轮一起定。详见 [[pending-biz01abp-revision]]。

## 边界

- **同批但另属**：PL/SL **层码**去留牵连 91 §3.1 两张表、wft 链、试点、数据文件——属链级批，可合并议、不必同批执行。
- **01 要不要拉齐**：§十一 的整节重写等于认 02/03/04 现在的结构为标准，而 01 还停旧链（角色分片 → spr-NNN、五个属性只剩「所属角色」、`AI可以处理节点` 里写着「待 wft01-biz 处理」）——**待人类点头**（另一条同源挂账见 [[pending-01-align-to-94]]）。
- 退役中的 `eos-wft0{1,2,3}-biz-*.md` 按 [[eos-wft-retirement-biz-pilots-canonical]] 不平行维护、**不改**。

关联 [[94-eos-sys-dev-dbs-tree-authority]]、[[eos-pilot-authority]]、[[pending-biz01abp-revision]]、[[pending-subpd-subpl-naming]]、[[progress-93-skill-guide]]。
