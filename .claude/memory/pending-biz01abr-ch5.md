---
name: pending-biz01abr-ch5
description: 挂账：biz01abr 收口四轮（2026-09-20）——第三轮六章七章并掉、Start/End 各成一节、93 骨架改五主章；第四轮补状态机第三路径、清 ch3→ch4 复述、补图1-1／图5-1，并以 abr 反推改 93 四条、94 五处改名
metadata:
  node_type: memory
  type: project
  originSessionId: aeb5791d-83c1-475f-b296-9d0e7b683e2a
  modified: 2026-09-20T07:39:32.290Z
---

**人类 2026-09-20 定，同日四轮**：

1. 第一轮——第五章重排为「一节一个 Step」＋ 检验就地成文 ＋ 缺口归第五章 ＋ 清除条件（提交 `471164ae`）。
2. 第二轮——「请修订你发现的问题，只是在修订之前，对每个问题都要再深入思考和分析下」：补退回落点、状态机三路径、状态写入分工、受理门槛判定取值、「业务链」改「文档序列」、引用改链接。
3. 第三轮——「应补充 step start 和 step end，这样，第七章就融合到 step end 中」＋「第六章和第七章就不需要存在了」：**abr 由七章改为五章**（提交 `b310b9ca`、`258030ce`）。
4. 第四轮——「继续优化 biz01abr，依据或反过来优化 93 号规范和 94 号文档」，中途把工作集收窄为「你只看 biz01abr、93 和 94 号文档，其他的文件不具备参考性」，末尾追加「给 1.2 节的那张图一个图编号和题目」与改名（提交 `7d0aa845`）。

**版本**：93 **第 70 版**、abr **第 75 版**、94 **v1.36**（本轮首次动）。两份 PDF 已重生成。

## 第四轮（2026-09-20）落了什么

**工作集三份**——abr、93、94。人类明令「其他的文件不具备参考性」，故族内比对（14 份 SKILL 指南普查、abp／cr 波及面、02 数据文件、`业务链` 残留）**全部作废**，不作论据。

**abr 八处**：

- **§5.1 状态机补第三条路径**（硬伤）——原图只有主线与下游退回两条，而 93 §3.5 要「主线、上游变更分流、下游退回**三条**」，且 abr 自己正文已声明写入 `待补充srb设计`。补 `待补充srb设计` 节点、两条版本递增自环、补全回边；三组用 `%%` 分行。mermaid 语法已用 `mermaid.parse()` 实机验过。
- **表3-2 ④** 撤「＋ 人类确认挂点：归拢条目、职责边界、收敛自查」（与 §4.2.1 表下注双写）。
- **表3-2 ⑤⑥⑦ 来历**由「本任务产出」改「**承袭**」——按 93 §3.3 四来源（形态边界注明写三级由相关方需求直接给出）。**改后「整列常量」的毛病自己没了**（①②③④ 本任务产出／⑤⑥⑦ 承袭），故 93 §2.3「逐行取值」一个字没动。
- **§4.2.1 末段**删对构建规则五步的复述、**§5.4 第 1 件**删对表4-2 判定与本比对的复述，均改引名。
- **表4-4** 补「边界外」一行——原表只有边界内／边界扩展，表头却写着「结果｜判定｜处置」，漏的恰是最该退的那一路。
- **表4-3 判据列**三处「语义与业务逻辑分析」换掉（自造词、判据空转；同表处置列的「复用 → 改进 → 新增」才是 93 §3.4 的成词）；§1.3 条 2／3、§5.7 自检清单同批清。
- **§2.1 注二**删重复的「本任务消费其中三个属性」（§4.1 注已有）。
- **补两个图号**：§1.2 链图 → `图1-1：业务设计链与本任务的位置`；§5.1 状态机 → `图5-1：节点状态机`。

**93 四条**（abr 反推）：

- §3.4 删「单承接，可 N:1、不可 1:N」，改引 94 §1.2~§1.4——原句重述了 §4.1 判据归属表划归 94 的树级承接规则正文，**规范自己撞自己**。
- §3.4 **禁含**补「第三章已给的判据本体与构建规则只引名，不复述本体」——与 §3.5 的跨章禁含对仗；abr 那三处复述的根因就是这个空档。
- §3.3 把「收敛自查」从「按级并进该表」里摘出单列——它管整棵树，落不进任何一行。
- §3.5 给 **Step Start／Step End 的标题体例**——原规范只规定了 Step 1…N，Start／End 是空档。

**94 五处**：§1.1 清单行与注、§2.2 注，改名「产品开发视角业务流程概要」→「**产品视角业务流程概要**」。93 侧 0 处。94 由此 v1.35 → v1.36。

**两条撤回**（深挖后自己推翻第一遍判断，见 [[feedback-deepen-before-fix]]）：

- 表3-1 ⑤ 的正式/轻量判据与 §4.2.4 **不是双写**——93 §3.3 要它写在意义表、§3.4 要第四章有「类型与要素生成判定」，两头点名，是分工。§4.2.4 真正的欠缺是没写「从 X 的什么推出」，另案。
- 「业务输出的产品数据」**是 94 §2.2 的成词**（94:203「⑤ 业务文档＝业务输出的产品数据」），非 abr 自造。

## 前三轮已落（要点）

**第三轮**——abr 七章 → 五章：`## 五、执行步骤及检验规则` 下为 章首引题 → 表5-1 → `### 5.1 Step Start`（受理门槛表＋状态推进与退回）→ `### 5.2~5.6 Step 1~5` → `### 5.7 Step End · 运行结束输出`（三区模板实例＋增量标记＋自检清单）；原「六、关联产品数据及规则（接口）」与「七、收尾」两章撤销。接口读写并入 Step 1／Step 5；章首引题的写法定死为「**只写表上看不出来的整章级事实**」；明写「一次受理一张业务文档」；93 骨架同步改五主章（§3.6／§3.7 撤销）。§1.2 链图两个节点正名（`业务流程配置方案`→`业务配置方案`、`业务流程实例`→`业务实例`）。

**前两轮**——退回有落点（Step 3 触发认领，门禁在 Step Start）；状态机三条路径齐、画法改 mermaid `stateDiagram-v2`；§5.1 声明写入分工（本任务写四态、下游写两态）；表5-2 一名三写收为「受理门槛（对 X）」、判定取值改 91 的成词「软提示」；「业务链／文档秩序」全改「文档序列」；13 处跨文档引用改 Markdown 链接。

## 仍开着的口

1. **§3.3 节题撞车 ＋ §3.2 前置句否定式**（第四轮新发现，已给建议、等裁）——① §3.3 节题「Y 树各级节点**属性**」与同节正文的「**要素**」自撞，「属性」在本链是专词（业务文档的五个坐标）；上一轮已把 §4.2.4 的「属性」改成「实现方式」、理由正是这个词撞车，**§3.3 节题没跟上**。② §3.2 前置句「各级只写到结构判定为止……**不写实现内容**」里的「实现」与表3-2 ⑤ 的「业务文档**实现**方式」同词反向，读者判不出 ⑤ 属哪边；且否定式边界声明违 93 §2.4 规则二／规则九。③ 第三处小项：「哪些留给下一个任务位补」在 §3.2 前置句、表3-2 ⑤⑥⑦、§4.2.3 注里说了**四遍**，建议只留 ⑤⑥⑦ 三格。
2. **改名残留——「产品开发视角」视角名**（人类 2026-09-20 定「暂时不动」）——三份内约 20 处仍是裸的「产品开发视角」。**裂口在**：94 §1.1 有条注叫「**名称自带形态**」（同一份产品数据按停在哪一级各占一行），要求「产品视角业务流程概要」前面那截恒定；而**不带形态后缀的本名**「产品开发视角业务流程」仍在 94:128、93:118（规则四白名单举例）、93 §1.2、93 §3.2 四处未改——同一份产品数据现在两个名字。
3. **§1.2 链图三处待裁**（第三轮报出，未动）——① 图上的边 `业务流程定义 →业务配置方案` 跳过整个方案空间（94 §1.1 的 05／06／07／平台构成概要全未出现，abr 全篇亦无「系统需求」「平台构成」「方案空间」字样）；② `业务信息系统` 不是产品数据；③ 段名「业务系统设计」「业务系统配置」——**连同「业务信息系统」「业务配置方案」「业务实例」五个词，在 93 与 94 里全部为 0**，全在 abr §1.2 一段说明与图1-1 里。**等一句话**：图是补齐 94 的方案空间，还是只画到本任务下游为止。
4. **94 与 93 相抵**——93 §4.3 写「链末份产品数据是平台构成框架，见 94 §1.1。……设计线的产品数据链止于框架」，而它引的 94 §1.1 在平台构成框架之后还有平台构成概要、配置方案、前后端组件方案三行，94 并为后两者各留 §2.5／§2.6 一节。**一句「见 94 §1.1」引不出它要的结论。**
5. **94 的建议（未落，等一句话）**——§2.2 补「文档序列不占树位」：该 E2E职责处理这批业务文档的序随 ④ E2E职责 携带，不另立一级；同一批文档在 P 树里另按构件归组，序记在 ④ 构件开发WBS（包）。
6. **超出三份工作集、第四轮不予判定**——① 表5-1 的列定义与编号约束（93 §3.5 外派给 00-doc-conventions §13.2／§13.3，含「名称 2~6 字」与「首段应为总览表」两条与现状相抵）；② `[待下游推断]`／`供人类确认` 两枚标记（abr §5.7 把定义源指给 91 §A.3.1）。
7. **abp／cr 跟随面**（第三轮报出，未扫）——七章 → 五章、章首引题、表号、表5-1 体例、状态机补 `待补充srb设计`、受理门槛首行（abp／cr 把 Y 侧退回态当硬退出，abr 那一处是对的，可作定尺）、判定词「软限制」→「软提示」、「业务链」各残留 1 处。

## 状态

第四轮已提交：`7d0aa845`（abr 第 73~75 版 ＋ 93 第 70 版 ＋ 94 v1.36，5 个文件含两份 PDF）。**未推**——master 领先 `origin/master`。`100-subprojects/05-投资工程研究/` 的并行改动（含其 `claude-memory/` 两份与两个未跟踪的工作目录）按 §九 提交边界不并入。**91 未动、00-doc-conventions 未动。**

关联 [[skill-guide-ch4-ch5-boundary]]、[[skill-guide-ch3-ch4-split]]、[[ch4-ch5-rename-and-abr-ch4-rebuild]]、[[ch2-structure-only-verify-in-ch5]]、[[gap-taxonomy-by-anchor]]、[[biz-chain-governance-removed]]、[[eos-design-landing-spec-93-refactor]]、[[91-appendix-a-internal-conflicts]]、[[feedback-deepen-before-fix]]、[[feedback-work-style]]、[[feedback-code-whitelist-2026-09-16]]。
