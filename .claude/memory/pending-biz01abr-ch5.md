---
name: pending-biz01abr-ch5
description: 挂账：biz01abr 第七、八轮（2026-09-21）——第五章重构为六节、写回随确认落地；新增承接记录（X 上回写承接节点＋方式＋理由）；依据只限 abr＋93＋94；下一轮主攻「说明」的落点与 abr 自有的前后相抵
metadata: 
  node_type: memory
  type: project
  originSessionId: 33aa7253-51b9-4161-a221-024938a324df
  modified: 2026-09-21T00:00:00.000Z
---

**人类 2026-09-21 定，第七、八轮**（前六轮均已提交）：

第七轮两条指令——① **第五章步序重构**：Step Start 与 Step 1 不变；Step 2 ＝ 基于对话确认 R 树方案；Step 3 ＝ E2E职责检查并补充需求（原 Step 2）；Step 4 ＝ 基于对话确认补充需求；Step End ＝ 给出本轮任务成果总结。**Step 2 与 Step 4 一旦确认即直接写回产品数据**。② **依据只限 biz01abr、93、94 三份**——见 [[feedback-basis-abr-93-94]]。

第八轮一条指令——**承接记录**：Step 2 与 Step 3 要把「X 的业务文档被 Y 的哪个节点承接」记到 X 上，并给出可以承接的理由分析；归属按「第四章放方法、第五章放过程」；方式取复用／改进／新增三档。

**版本**：abr **第 85 版**（84 版＝第五章重构、85 版＝承接记录）、93 **第 74 版**；PDF 已重生成。提交 `69847c55`（重构）＋ `dd5171fd`（承接记录）。

## 第八轮落了什么（同日）

人类定：**Step 2 与 Step 3 要把「X 的业务文档被 Y 的哪个节点承接」记到 X 上，并给出可以承接的理由分析**；归属问「第四章放方法，第五章放过程」——照此办；三档取**复用／改进／新增**；引用写法人类提 `ID@Y`，我改为 `ID@文档本名`。

**第四章 §4.1 新增承接记录**（判据与构成）：承接节点 ＝ 该业务文档所属的 E2E职责节点（**Y 的节点即一个 E2E职责**，业务文档只是它下面的一级——02 的 §3.1 明写）；写 Y 的块ID ＋ `@` 文档本名，如 `bpd-biz-GOV-001@职责视角业务流程概要`；承接方式三档逐档定义在业务文档级上（复用＝已有文档且语义被覆盖／改进＝已有文档但正文特征变或边界扩展／新增＝原先没有）；可承接理由 ＝ 业务对象闭环 ＋ 同源判据的成文。

**第五章**：Step 2 写回加「**也写的是 X**」（三项回写相关方需求中该业务文档名下的条目，同文档同值）；Step 3 加「**承接记录补覆盖面**」（末级整段进入 ＋ 文档序列兑现；核不过的标缺口）；Step 4 的写回加「承接记录随改动而改」；**表5-1 的 Step 2／Step 3 写资产列补「相关方需求」**；自检清单三组同步。

## 承接记录带出的、仍待裁决的六件

1. **记在 X 的哪一级**——X 现在的物理形态是角色分片 → `spr-NNN`，**没有业务文档这一级**（`20-pl4eos/80-pl4eos-2-eosdata/01-eos-specified-requirements.md` 的 `## 系统用户 {#user}` 下直接挂条目），所以记录按条目逐条写、同一业务文档的多条重复同一个承接节点。挂到业务文档上要等 01 按 94 §2.1.1 重建（即 [[pending-01-align-to-94]]）。
2. **X 上已有 `下游映射` 字段、全空**——spr-001～spr-010 每个条目块都有一行 `**下游映射**：`。这条记录的第一落点就是它；但该字段的格式归 01 数据文件自己（依据外），本轮未碰。
3. **`@` 的位置与前缀 `@` 相撞**——现有数据文件里 `@` 一律作**节点 ID 前缀**（`@engine-entity`、`@node-ncr`、`@nfr-…`，全库 300＋ 处）；承接记录用的是**后缀**（`bpd-biz-GOV-001@职责视角业务流程概要`）。将来 grep `@` 会同时捞到两类。
4. **X 上现在有两类本任务写的字**——条目状态（`待处理` / `需退回重切`）与承接记录；两者可能互相矛盾（记录已写、状态仍是 `待处理`）。受理门槛要不要加一条核对，待定。
5. **表4-3 的 ⑤ 行仍是两档**（并入／新建），记录取三档——两者不冲（复用与改进都落在「并入」之下），但要不要把 ⑤ 也收成三档、让全章一套话，待定。同理 表4-2 的 ④ 是「合并或新建」。
6. **三档与 §4.3 表4-5 的词典并存**——表4-5 是「确认／无新增、引擎判定变更、补充、过时」四档，属**文档在序列里**的关系轴；三档属**节点与已有节点**的关系轴。两轴分立成立，但要不要合一，待定。

## 第七轮落了什么

**abr**：第五章自五节改为六节。写回从 Step End 前移到两次确认上——**表5-3「确认后写回的四种情形」移入 Step 2**，并补一档「已被下游消费过的节点本次被修订」（`可以srb设计`／`在srb设计` 维持原状态、版本递增；`已srb设计` 写 `待补充srb设计`），把原先只活在图5-1 上的 `待补充srb设计` 写入落点补齐；**要素机械检查随写回移入 Step 2**；覆盖检查成 Step 3、复跑归 Step 4；**特化参数留 Step 2**（操作条目去掉「补充原始需求材料候选」），Step 4 自带操作条目与复跑规则；Step End 改为只汇总（三区输出 ＋ 收尾 ＋ 增量标记 ＋ 自检清单，自检重分八组）；引题、表5-1、Step Start 的状态指向句同步；**第一章 §1.2 门禁处数两处→三处**（Step Start 选定、Step 2 确认方案、Step 4 裁决缺口）。

**93 §3.5** 按 abr 对齐两处：Step End 允许「写回已随对话确认就地落盘、End 只作本轮成果总结」；特化参数按讨论确认的处数分写（各写它那一处的操作条目）。

**人类未逐条裁、落笔为随重构不得已的四项**（随提交一并落了，随时可撤）：表5-1 Start 行「写资产」由「—」改「相关方需求」（§5.1 明言在 X 侧写 `需退回重切`、表5-2 退回上游行的动作也在写；链上先例 wft01-biz 同把 X 侧推进写进写资产列）；Start 行「产出」由「受理结论 + 未确认清单」改「受理结论 + 候选清单」；Step Start 状态指向句由「Step End 把本轮结果写回」改「Step 2 在确认后写回」；表5-3 首列「本轮新生成的方案」改「本轮生成或修订的方案」（含由 `需biz01修订` 转来的）。

## 仍开着的口

1. **第六轮报告里以 91 为锚的条目——按新依据一律搁置**（91 与 00-doc-conventions 都不算判据）：增量标记那句把 `[待下游推断]`／`供人类确认` 挂在 §A.3.1（该节只定义四枚主标记 ＋ `[推断]`）；`[需确认]`／`[需重设计]` 的双指针各挂一半；三区「下游 →」与 §1.4「同一任务位」相抵；补充材料文件名 `YYYYMMDD-biz01abr-批次ID-文档序列类…`（链上统一为 `<YYYYMMDD>-<对象名>补充原始需求材料.md`）；补充材料回路不写落点与登记。**abp／cr 与 8 份 wft 指南不在依据内，不再作对照**。
2. **「说明」的落点**（最重，超出 abr＋93／94 工作集）——94 §1.3 用「各级只到说明」定义概要，abr 表3-3~3-9 逐级要「说明」，而 02 与 03 两份数据文件的节点模板都没有说明字段。改它要先定说明落在树画像区还是节点块，牵 abp／cr 两份模板。
3. **abr 自己仍有的前后相抵**（按 [[eos-pilot-authority]] 仍是 abr 的 bug）：§5.2「读的是 X」冒号后混进 Y 侧的存量方案节点、且漏「未确认的存量方案节点」；§5.2「判的是归属，不是内容」与 §5.1「按条目正文语义归拢」相抵、又与第二章末那条注重复；表5-2 第 3 行文档级条件配条目级动作；「合理空置」无判据（链上写法带「原因」二字）；**「E2E职责检查并补充需求」不动作领起**（93 §3.5 名体例要求动作领起）；§1.2 链图三处待裁（跳过整个方案空间／`业务信息系统` 不是产品数据／五个词在 93 与 94 里零出现）。
4. **93 §4.3 链尾与 94 §1.1 相抵**——93 写「设计线的产品数据链止于框架」，94 §1.1 之后还有平台构成概要、配置方案、前后端组件方案三行；abr 对链尾无表述，本条裁不了。
5. **跟随面**——abp／cr 仍是七章结构，未随 abr 的五章、章首引题、表号、Step 名与写回位置。
6. **01 号相关方需求按 94 §2.1.1 重建**——见 [[pending-01-align-to-94]]。

关联 [[feedback-basis-abr-93-94]]、[[eos-pilot-authority]]、[[ch4-ch5-rename-and-abr-ch4-rebuild]]、[[ch2-structure-only-verify-in-ch5]]、[[ch3-y-tree-tables-and-rules]]、[[skill-guide-ch3-ch4-split]]、[[skill-guide-ch2-ch3-tabling-2026-09-18]]、[[skill-guide-ch1-writing-criteria]]、[[chain-form-three-tiers-naming]]、[[feedback-code-whitelist-2026-09-16]]、[[feedback-work-style]]、[[eos-design-landing-spec-93-refactor]]。
