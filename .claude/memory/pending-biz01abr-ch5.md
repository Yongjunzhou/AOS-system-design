---
name: pending-biz01abr-ch5
description: 挂账：biz01abr 第五章三轮收口（2026-09-20）——第三步把六章七章并掉、Start/End 各成一节；93 骨架由「六主章＋收尾章」改为五主章
metadata:
  node_type: memory
  type: project
  originSessionId: aeb5791d-83c1-475f-b296-9d0e7b683e2a
  modified: 2026-09-20T06:27:35.905Z
---

**人类 2026-09-20 定，同日三轮**：

1. 第一轮——第五章重排为「一节一个 Step」＋ 检验就地成文 ＋ 缺口归第五章 ＋ 清除条件（已提交 `471164ae`）。
2. 第二轮——「请修订你发现的问题，只是在修订之前，对每个问题都要再深入思考和分析下」：补退回落点、状态机三路径、状态写入分工、受理门槛判定取值、「业务链」改「文档序列」、引用改链接。
3. 第三轮——「应补充 step start 和 step end，这样，第七章就融合到 step end 中」＋「第六章和第七章就不需要存在了」：**abr 由七章改为五章**。

93 现为**第 68 版**、abr **第 68 版**（93 的 PDF 已重生成）。**94 未动**——其注属树口径，按 [[feedback-work-style]] 只出建议（建议原文见下）。

## 第三轮落了什么

- **abr 七章 → 五章**：`## 五、执行步骤及检验规则` 下为 表5-1（章首第一段，前面不再有引题句）→ `### 5.1 Step Start`（受理门槛表 ＋ 状态推进与退回）→ `### 5.2~5.6 Step 1~5`（编号未动）→ `### 5.7 Step End · 运行结束输出`（增量标记 ＋ 自检清单）。原「六、关联产品数据及规则（接口）」与「七、收尾」两章撤销。
- **接口章的归属**——读的写在 Step 1（「读的是 X…唯一的外部输入；R 树即资产、就在本任务的产出里，不另读资产文档」），写的写在 Step 5（「写的是 Y…不另立一册」）；表6-1 随之撤销。
- **收尾章的归属**——增量标记与自检清单进 Step End；「不设变更记录」的细则上移进 §2.4 规则三（原「见 §3.7」的指针随之取消）。
- **Step End 补上三区模板实例**（人类追问「那 step start 呢，要不要补」时查出的缺口）——91 §A.3.2 要求「各 Skill 的 Step End 实例化时填充第一区的状态变量和结果摘要行」，而 abr 改版时只留了「按 §A.3.1／§A.3.2 执行」一句；现按旧版 wft02-biz §5.8 的形补齐（`当前状态` ＋ 本轮 R 树／覆盖检查／补充材料三行摘要 ＋ A/B/C ＋ 下一步两行）。**Step Start 一侧不缺**——它的特化本来就只有受理门槛与状态推进两样，且都已在节内；91 §A.4.5 要求各 Skill 特化的四项（输入对象类型、输入合法状态值、退回节点状态值、特有校验项）逐项对上，无缺项。
- **章首补回一段引题**（人类 2026-09-20：「表5-1之前是否应该有一段内容，既是对第五章的概要，也是对表5-1的说明」→「应该有吧」）——abr 现章首为：**一段引题 → 表题 → 表5-1 → 5.1 Step Start**。引题的写法定死为**只写表上看不出来的整章级事实**：一次运行从哪起收于哪、哪几步并非每轮都走、本任务的检验分布在哪几处；**不逐行复述表、不解释列义**（列义归 00-doc-conventions §13.2，93 §3.5 明令「不各写一套列」）。这三样是这一位置唯三非复述、非元评论的内容；旧版 abp／cr／wft02-biz 表前那几句（「两端由 91 承载，下表一并列出」「受理判定按入口检验表特化；列定义引 …§十三」）正是 93 §2.4 要清的两类毛病，不作样本。
- **明写输入单位＝一张业务文档**（人类 2026-09-20：「每次只输入一个业务文档」）——此前只在各处隐含（§5.4 说「本次这批条目**所在的**业务文档」、表5-2 的数量限也是按张计），没有一句正面声明，93 §3.5 要的「**一次多少**」因此是可推不可读。现于 §5.1 受理门槛表后加一句「**本任务一次受理一张业务文档**：该文档下的全部 `待处理` 条目一次读完、一并组装，不跨文档成批」，表5-2 补一行「本次的 `待处理` 条目跨了多张业务文档 → 软提示 → 输出文档分布，本次只处理其中一张，其余下一轮重试」；§5.2 的读句改「这一张业务文档下全部处于 `待处理` 的条目」，§5.4 的组装范围改「本次这一张业务文档」。**未进章首引题**——它是受理侧的事实，与 §5.1 那句重复，只留一处。
- **Step Start／End 的节标题写法**已与旧版对齐：`### Step Start`（不带名称，91 §A.2.4 与 wft02-biz 都如此）／`### Step End · 运行结束输出`（带名称）。表5-1 的名称列照 00-doc-conventions §13.2 给 Start 填「前置校验与路由」，与节标题不必同名。
- **93 骨架改五主章**：§2.1 的骨架表删「六 关联产品数据及规则」与「收尾」两行、章序逻辑改写；§3.5 的应含改为「总览表 / Step Start / Step 1…N / 两处检验 / Step End / 运行时特化参数」六条，明写「读写的旁支落进跑它的那一节，不另设接口章」与「Start 与 End 各成一节，固定机制只引不重述」；§3.6、§3.7 两节撤销；§1.1、§2.3、§2.4 规则三、§3.2 禁含、§4.1 表两行、§5.1 映射表、§6.1 步骤清单同步。
- **顺带解掉一笔挂账**：abr 章首不再有引题句，00-doc-conventions §13.2「操作流程章的首段应为总览表」与 93 表号通则不再相抵，该处**不必改**。

## 前两轮已落（要点）

退回有落点（Step 3 触发认领，依据旧版 wft02-biz「退回修订走 Step 3~5、门禁在 Step Start」）；状态机三条路径齐、画法改 mermaid `stateDiagram-v2`；§5.1 声明写入分工（本任务写四态、下游写两态）、删 §5.6 的「推进为 `可以srb设计`」；表5-2 一名三写收为「受理门槛（对 X）」、判定取值改 91 的成词「软提示」；「业务链／文档秩序」全部改「文档序列」、缺口族改「文档序列类」；§5.5 覆盖检查改两面；13 处跨文档引用改 Markdown 链接（00-doc-conventions §4.2）。

## 仍开着的口

1. **表5-1「名称」列的长度**——00-doc-conventions §13.2 定「2~6 字」，而三试点的 Start（前置校验与路由 7 字）与第 4 步（abr 11／abp 11／cr 14 字）都超；93 §3.4 要求「动作名取该步的产出名…读者从标题要能看出这一步产出什么」，复合步骤必然两段，**两规则相抵**。生态分布：wft01/02 的短名守规矩，wft03 与三试点普遍超。**建议放松 §13.2**（如「2~6 字；并列两段不超过 10 字」）而不是砍名字；§13.2 不在授权范围，未动。
2. **abp／cr 要跟着扫**——上两轮的七样（缺口归第五章＋一节一个 Step＋检验就地成文／退回落点／状态机三路径／状态写入分工／判定取值／文档序列用词／引用改链接）之外，本轮又添**骨架改五章**：补 Step Start 与 Step End 两节、把接口章与收尾章并掉，并给 Step End 补三区模板实例。
3. **cr 的治理内容**见 [[biz-chain-governance-removed]]；**abp／cr 的受理门槛表首行**把 Y 侧退回态当 X 侧不可处理项、判成硬退出，与 91 §A.4.3 的退回门禁相抵；**abr 这一处是对的**（表里只列输入侧状态），可作定尺样本。
4. **91 两处**——§A.5 说反馈特化参数「由各 Skill 在第三章 Step 2 中定义」（对 SKILL.md 正确；对 SKILL 指南由 93 §3.5 归第五章）；§5.4 的治理预置口径见 [[biz-chain-governance-removed]]；§A.6 与 §A.6.3 的矛盾见 [[91-appendix-a-internal-conflicts]]。
5. **00-doc-conventions 第十三章三处**——① **§13.2「操作流程章的首段应为以下格式的总览表」**：与 93 §3.5 新定的「章首先给一段引题、随后总览表」**正面相抵**（abr 现按 93 落）。建议 §13.2 改为「操作流程章**先给一段引题，随后即为以下格式的总览表**；引题只写本章一次运行的概貌，不逐行复述表、不解释各列」——**通用规范，未动**。② §13.3 规则 6「运行结束输出（Step End）可独立成章，不必作为普通 Step 小节展开」是许可式措辞，与现在的「End 成一节」不冲突，但口气像在鼓励独立成章。③ §13.2 的「名称 2~6 字」见第 1 条。
6. **94 的建议（未落，等一句话）**——在 §2.2 注块「干枝由同一批属性落地」之后补一条：**「文档序列不占树位」——该 E2E职责处理这批业务文档的序，随 ④ E2E职责 携带，不另立一级；同一批文档在 P 树里另按构件归组，序记在 ④ 构件开发WBS（包）。**

## 状态

本文件所记的三处改动（abr／93／记忆）已提交到 master，**未推**（远端落后若干条）。**94 仍是 v1.35 未动**。`100-subprojects/05-投资工程研究/` 的并行改动按 §九 提交边界不并入。

关联 [[skill-guide-ch4-ch5-boundary]]、[[skill-guide-ch3-ch4-split]]、[[ch4-ch5-rename-and-abr-ch4-rebuild]]、[[ch2-structure-only-verify-in-ch5]]、[[gap-taxonomy-by-anchor]]、[[biz-chain-governance-removed]]、[[eos-design-landing-spec-93-refactor]]、[[91-appendix-a-internal-conflicts]]、[[feedback-deepen-before-fix]]、[[feedback-work-style]]。
