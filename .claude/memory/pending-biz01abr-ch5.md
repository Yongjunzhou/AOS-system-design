---
name: pending-biz01abr-ch5
description: 挂账：biz01abr 第七、八、九轮（2026-09-21）＋ 第 96~99 版（2026-09-23）——第五章重构为六节、写回随确认落地；新增承接记录；第九轮删净 Y 侧退回（图5-1 整张删）；第 96 版第四章按 abp 风格重构、第 97 版第五章对上 ch4 并收 abr 自己的五条相抵；**状态机图定案：不画**
metadata: 
  node_type: memory
  type: project
  originSessionId: 33aa7253-51b9-4161-a221-024938a324df
  modified: 2026-09-22T13:50:04.042Z
---

**人类 2026-09-21 定，第七、八轮**（前六轮均已提交）：

第七轮两条指令——① **第五章步序重构**：Step Start 与 Step 1 不变；Step 2 ＝ 基于对话确认 R 树方案；Step 3 ＝ E2E职责检查并补充需求（原 Step 2）；Step 4 ＝ 基于对话确认补充需求；Step End ＝ 给出本轮任务成果总结。**Step 2 与 Step 4 一旦确认即直接写回产品数据**。② **依据只限 biz01abr、93、94 三份**（当日末再收窄为「不参考其他文档」，见 [[feedback-abr-only-scope]]）。

第八轮一条指令——**承接记录**：Step 2 与 Step 3 要把「X 的业务文档被 Y 的哪个节点承接」记到 X 上，并给出可以承接的理由分析；归属按「第四章放方法、第五章放过程」；方式取复用／改进／新增三档。

**当轮版本**：abr **第 88 版**（84＝第五章重构、85～87＝承接记录三稿、88＝删净 Y 侧退回 ＋ 收缩图5-1）、93 **第 74 版**；PDF 已重生成。

## 第 96~97 版（2026-09-23）：第四章按 abp 风格重构 ＋ 第五章对上 ch4

**人类两条指令**：① 「以 abp 的第四章风格重构 abr 的第四章内容」（见 [[skill-guide-ch4-process-and-rules]]，第 96 版）；② 问「abr 的第五章要不要重写？」→ 裁「**只做 1~8（对上 ch4 ＋ 收自己的相抵），不引 93 §3.5 当判据**」（第 97 版）。**射程**：①是直接指令、越过 [[feedback-abr-only-scope]]；②人类明说只收 abr 自己的相抵，故本轮**不拿 93 §3.5 当判据**。

**第 97 版落了八条**：

1. **§5.2 的四步改成第四章四步的编号清单**，每项点名规则名（干枝落位规则／岗位级E2E业务的类型与边界规则／边界匹配规则；岗位级E2E业务聚类规则；末三级落位规则／任务类型与名称规则；文档序列关系检测规则／实现方式三项规则）；**删「这四个步骤都按并入处理」**——与 abp 已撤的 §4.3 包装节同一副旧框架（"存在两种模式"）。
2. **§5.6 自检的「生成」项改成按第四章四步的规则逐条核**（原先用「边界匹配／关系检测／变更影响声明」这类描述名）。
3. **§5.3 的「也写的是 X」改引**「**承接记录规则**」（原把六项名称抄了一遍）。**顺带答了上一轮的待裁**：承接记录规则留在第四章是对的，正因为第五章要引它。
4. **§5.2 的读取句拆成「X 这一处 / Y 这一处」**——原先把它混在"读取的是相关方需求"之下，且**漏了「未确认的存量方案节点」**。
5. **删 §5.2 的「判的是归属，不是内容」**——与 §5.1「按条目正文语义归拢」相抵，且与第二章末那条注（同一个文档可对应多个相关方需求）重复。
6. **表5-2 第 3 行的动作改回文档级**——原「本次其余条目照常，该文档的条目挂 `[需确认]`」是文档级条件配条目级动作；改「其余照常，该文档挂 `[需确认]`」（与 abp 同款行齐平）。
7. **「合理空置」补判据**——§5.3 的要素机械检查改「要素非空，或属**合理空置**——还没到产生它的时点，即完备性检查结果与补充材料引用由后面的 Step 产生」。
8. **Step 3 改名动作领起**——「岗位级E2E业务检查并补充需求」→「**检查岗位级E2E业务并补充需求**」（表5-1 与 §5.4 节题两处）——判据是 abr 自己其余四个 Step 名都动作领起，只有这一个不是（内部相抵，不引 93）。

**另补**：§5.2 补「**本步无人类参与点**」。

**第 98 版两处**（人类 2026-09-23 同轮追加，起因在 abp 侧——见 [[pending-biz01abp-revision]] 的「P 树归位记录」）：

1. **表3-7（文档级E2E任务 的要素）加一行**——「**P 树归位记录** | 本张文档在 P 树上被归位到的节点 ID，以及该节点当前的状态值；尚未归位的为空」。**为什么先在 abr 立**：`02` 是 abr 的 Y，它的 schema 由 abr 第三章声明；abp 只是写这个字段的人。
2. **§5.3 的「合理空置」判据扩一条**——**P 树归位记录由下游的产品视角业务流程任务在归位时写入**。不扩这一条，abr 的要素机械检查会把这个空字段当缺陷。

**第 99 版一处**（人类 2026-09-23 同轮追加）：**两个子块标题改「转换过程／转换规则」**（原「本步的过程／本步的规则」）——与 abp 同步，理由与体例见 [[skill-guide-ch4-process-and-rules]] 第 5 条。

### 状态机图的定案（同轮，人类裁）

**不画状态机图**。裁定链：abr 的图5-1 于第八、九轮按「要收缩」删过（理由「凡下游的验收行为、状态机的转移边，都是复述，执行者不必知道；**生存周期本身又归 91 §A.6**」，删后 abr 只画三张图、全是本任务自己的内容）；2026-09-23 我在 abp §5.1 又加了一张同类图（引 93 §3.5「状态机图与写入分工并给」），**人类当场裁定撤掉 abp 那张**。**结论**：两个试点的状态推进都用散文＋bullet 写，不画图；**93 §3.5 的「状态机图与写入分工并给」应改「写入分工并给」**——这一条 93 侧未落（与它同时开着的还有第九轮那条「§5.1『上游变更…按状态分流』那句本身与 91 §A.6 重复，要不要一并收」）。



## 第九轮落了什么（同日）：删净 Y 侧退回

**人类三次给定性，逐层收窄**：① 问「后续任务退回的 Y，本任务如何处理」；② 「本任务对退回的 Y 可以不做任何处理，只是人家拒收、改其状态值，对本任务没有影响」；③ 「在 biz01abr 中无需体现退回 Y 的任何内容——对读者反而是噪声」。

**定案**：**Y 侧退回（下游拒收）在 abr 里整体删除**。理由三条——`需biz01修订` 这个名字自带「等本任务来改」；abr 已有「Start 按状态路由」这个通用机制；节点生命周期归 91 §A.6（abr 自己就这么引）。凡下游的验收行为、状态机的转移边，都是复述，执行者不必知道。

**删了十二处**：表1-1 两个下游行的「检出缺陷退回 `需biz01修订` + 缺失说明」；§1.4「退回是反向迭代」整条注；表5-1 Start 行「退回路由」→「按状态路由」；§5.1 引题「三类入口」→「入口」、尾句「状态推进与退回」→「状态推进」；§5.1「捡起未确认的方案」→「捡起存量节点」（首句同步泛化）；表5-2 末行「＋无退回」→「＋无入口检出的存量节点」；§5.1 段名「两种退回，方向相反」→「退回上游」并删 Y 侧半句；图5-1 三条退回边与 `需biz01修订` 节点；§5.2 首句「有下游退回的节点待修订」→「入口检出待本任务处理的存量节点」；§5.3「（含由 `需biz01修订` 的节点修订后转来的）」；三区模板「或 `需biz01修订` 的节点重新运行」；自检清单受理组那句。

**保留**：X 侧的 `需退回重切`、退回上游、退回门禁（表5-2 两行 ＋ §5.2 末「按受理门槛的退回处置」）——那是本任务自己的动作；§1.4「本任务传出的概要有两个下游」注。

**连带处置**（不在人类逐条点名的范围内）：把「捡起未确认的方案」泛化为「捡起存量节点」，§5.2／表5-2／自检同步用「存量节点」——**为的是把「`需biz01修订` 的节点由本任务认领」留在 abr 手里，而不点退回二字**。人类说撤即撤（撤了就等于把认领整体推给 91 §A.6，节点会无人认领）。

**裁前报过、裁后撤回两条**：表5-3 第五段与图5-1「相抵」——深读不成立（第五段按「原为 `可以srb设计`／`在srb设计`／`已srb设计`」分档，退回节点当前状态 `需biz01修订` 不在档内，自然落到第一行）；「主语落空／组单／分诊三出口」——一经定性为「拒收」即整体消失。

**图5-1 整张删（人类：「要收缩」）**——半缩不成立：若只留本任务写的转移，`待补充srb设计` 便没有本任务侧的入边，图会散；而图里每条边（下游锁定/完成、上游变更分流的版本递增、待补充回路）在 §5.1「上游变更…按状态分流」句与表5-3 第五段里都已有可读的落点，生存周期本身又归 91 §A.6。删后「状态推进」句的冒号改句号，与分流句各成一段。abr 现在只画三张图（1-1 链位／2-1 X 树／3-1 R 树），全是本任务自己的内容。**未动**：§5.1「上游变更…按状态分流」那句（表5-3 第五段的「另按上游变更分流」要引它），它本身与 91 §A.6 重复，要不要一并收，人类未定。

## 第八轮落了什么（同日）

人类定：**Step 2 与 Step 3 要把「X 的业务文档被 Y 的哪个业务文档承接」记到 X 上**。口径四轮收窄：「Y 的哪个节点」＋「给出可以承接的理由分析」→「只需记录被 Y 的哪个**业务文档**承接就 OK」→「**我们处理的就是 X 中的某个业务文档及其下级节点，因此 Y 就是以业务文档节点承接 X 就 OK**」→ 认可「记录改用 X 已有的追溯关系体例 ＋ 给表3-7 补追溯键」。归属按「第四章放方法、第五章放过程」。**一次运行的处理单位＝一张业务文档及其下级，故一轮只产出一条记录。**

**第三章**：**表3-7（业务文档）补「内联稳定标签——下游逐级对上的追溯键」**，与表3-8／表3-9 齐平；自检清单的 Y 侧要素同步。

**第四章 §4.1 新增承接记录**（判据与构成）：按 **X 已有的「追溯关系」体例写六项**——**源节点**＝本条所属业务文档名下的条目；**关系类型**＝承接；**目标节点**＝Y 的业务文档（写它的内联稳定标签）；**目标文档**＝职责视角业务流程概要；**状态**＝随覆盖检查落；**说明**＝可承接理由一句话（业务对象闭环 ＋ 同源判据；完整成文落该 E2E职责节点的「推理依据与补全说明」）。

**第五章**：Step 2 写回加「**也写的是 X**」（六项写在该业务文档名下的条目上，同文档同值）；Step 3 加「**承接记录复核**」（把结论落进记录的状态——核过的写已承接，核不过的写缺口）；Step 4 的写回加「换了目标节点或换了状态的，记录随之改」；**表5-1 的 Step 2／Step 3 写资产列补「相关方需求」**；自检清单三组同步。

**三稿的来去**（85→86→87）：① 承接那一头先定为 **E2E职责节点（bpd-biz）**，86 版降到 **业务文档节点**；② 记录先含**承接方式三档**（复用／改进／新增）与**可承接理由**两项，86 版撤掉（三档留回表4-3 原处，理由改落 Y 侧）；③ 写法先自造 `名称@块ID@文档本名`，**87 版改用 X 已有的追溯关系六项**——「目标文档」在那张表里本来就是一列，自造的后缀是重复造轮子。

**提交**：`69847c55`（第五章重构）→ `dd5171fd`（85）→ `856b4e30`（86）→ `2e330b2b`（87）。

## 现场查证：X 的业务文档这一级

**树上有、文件里没有**——94 §2.1.1（abr 图2-1 所引）把 X 的树定为 业务文档 → 任务节点 → 用例场景、业务文档带五个属性；而 `20-pl4eos/80-pl4eos-2-eosdata/01-eos-specified-requirements.md`（v3.2，2026-08-27）的物理形态是 `## 系统用户 {#user}` 角色分片 → `#### spr-NNN` 条目块，**中间没有业务文档块**，条目字段里五个属性**只有「所属角色」一个**，`## AI可以处理节点` 那节还写着「待 **wft01-biz** 处理」。abr §5.1 自己就写着「X 的物理文件以角色分片直接挂着条目，本步先按条目正文语义把它们**归拢**到所属业务文档」，`[待复核]` 正是为此设的。**这份文件整体还停在旧链**，即 [[pending-01-align-to-94]]。

**X 上早有一个装这条记录的正规位置**——`## 追溯关系` 表（列：源节点｜关系类型｜目标节点｜目标文档｜状态｜说明），现有两行 `spr-* → allocated_to → bpl-biz-*｜02-eos-business-summary-architecture.md`（待填充）与 `spr-* → verified_by → TRACE-*｜08`。**两行都还是旧名**（`bpl-biz-*`、`02-eos-business-summary-architecture.md`）且从未被填过。

## 承接记录仍待裁决的四件

1. **记在 X 的哪一级**——X 现无业务文档块，记录按条目逐条写、同一业务文档的多条重复同一套值。挂到业务文档上要等 01 按 94 §2.1.1 重建（[[pending-01-align-to-94]]）。
2. **承载处留一处还是两处**——X 上现在有两处能装：条目块里的 `**下游映射**：` 字段（spr-001～spr-010 全空）与 `## 追溯关系` 表。abr 现在按追溯关系的体例说，但没说写进哪一处。
3. **关系类型的取值语言**——01 现有两行用英文蛇形（`allocated_to`／`verified_by`）；承接这一条要不要也定一个英文词，待定。
4. **X 上两类本任务写的字可能矛盾**——条目状态（`待处理`／`需退回重切`）与承接记录；记录已写而状态仍是 `待处理` 的情形，受理门槛要不要加一条核对，待定。

## 第七轮落了什么

**abr**：第五章自五节改为六节。写回从 Step End 前移到两次确认上——**表5-3「确认后写回的四种情形」移入 Step 2**，并补一档「已被下游消费过的节点本次被修订」（`可以srb设计`／`在srb设计` 维持原状态、版本递增；`已srb设计` 写 `待补充srb设计`），把原先只活在图5-1 上的 `待补充srb设计` 写入落点补齐；**要素机械检查随写回移入 Step 2**；覆盖检查成 Step 3、复跑归 Step 4；**特化参数留 Step 2**（操作条目去掉「补充原始需求材料候选」），Step 4 自带操作条目与复跑规则；Step End 改为只汇总（三区输出 ＋ 收尾 ＋ 增量标记 ＋ 自检清单，自检重分八组）；引题、表5-1、Step Start 的状态指向句同步；**第一章 §1.2 门禁处数两处→三处**（Step Start 选定、Step 2 确认方案、Step 4 裁决缺口）。

**93 §3.5** 按 abr 对齐两处：Step End 允许「写回已随对话确认就地落盘、End 只作本轮成果总结」；特化参数按讨论确认的处数分写（各写它那一处的操作条目）。

**人类未逐条裁、落笔为随重构不得已的四项**（随提交一并落了，随时可撤）：表5-1 Start 行「写资产」由「—」改「相关方需求」（§5.1 明言在 X 侧写 `需退回重切`、表5-2 退回上游行的动作也在写；链上先例 wft01-biz 同把 X 侧推进写进写资产列）；Start 行「产出」由「受理结论 + 未确认清单」改「受理结论 + 候选清单」；Step Start 状态指向句由「Step End 把本轮结果写回」改「Step 2 在确认后写回」；表5-3 首列「本轮新生成的方案」改「本轮生成或修订的方案」（含由 `需biz01修订` 转来的）。

## 仍开着的口

> **射程**：按 [[feedback-abr-only-scope]]（2026-09-21 收窄为「不参考其他文档」），下面**只有第 3 项**落在下一轮的射程里；1／2／4／5／6 都要看别处才判得了，记下不判。

1. **第六轮报告里以 91 为锚的条目——按新依据一律搁置**（91 与 00-doc-conventions 都不算判据）：增量标记那句把 `[待下游推断]`／`供人类确认` 挂在 §A.3.1（该节只定义四枚主标记 ＋ `[推断]`）；`[需确认]`／`[需重设计]` 的双指针各挂一半；三区「下游 →」与 §1.4「同一任务位」相抵；补充材料文件名 `YYYYMMDD-biz01abr-批次ID-文档序列类…`（链上统一为 `<YYYYMMDD>-<对象名>补充原始需求材料.md`）；补充材料回路不写落点与登记。**abp／cr 与 8 份 wft 指南不在依据内，不再作对照**。
2. **「说明」的落点**（最重，超出 abr＋93／94 工作集）——94 §1.3 用「各级只到说明」定义概要，abr 表3-3~3-9 逐级要「说明」，而 02 与 03 两份数据文件的节点模板都没有说明字段。改它要先定说明落在树画像区还是节点块，牵 abp／cr 两份模板。
3. **abr 自己仍有的前后相抵**（按 [[eos-pilot-authority]] 仍是 abr 的 bug）——**前五条已于第 97 版落**（见上「第 97 版落了八条」的第 4～8 条）：§5.2「读的是 X」冒号后混进 Y 侧的存量方案节点、且漏「未确认的存量方案节点」；§5.2「判的是归属，不是内容」与 §5.1「按条目正文语义归拢」相抵、又与第二章末那条注重复；表5-2 第 3 行文档级条件配条目级动作；「合理空置」无判据；「岗位级E2E业务检查并补充需求」不动作领起。**余一条未动**：§1.2 链图三处待裁（跳过整个方案空间／`业务信息系统` 不是产品数据／五个词在 93 与 94 里零出现）——后两条按射程只报内部那一面（同一图内三段的体例不一、与 §1.4 衔接表对不上）。
4. **93 §4.3 链尾与 94 §1.1 相抵**——93 写「设计线的产品数据链止于框架」，94 §1.1 之后还有平台构成概要、配置方案、前后端组件方案三行；abr 对链尾无表述，本条裁不了。
5. **跟随面**——abp／cr 仍是七章结构，未随 abr 的五章、章首引题、表号、Step 名与写回位置。
6. **01 号相关方需求按 94 §2.1.1 重建**——见 [[pending-01-align-to-94]]。

关联 [[feedback-abr-only-scope]]、[[eos-pilot-authority]]、[[ch4-ch5-rename-and-abr-ch4-rebuild]]、[[ch2-structure-only-verify-in-ch5]]、[[ch3-y-tree-tables-and-rules]]、[[skill-guide-ch3-ch4-split]]、[[skill-guide-ch2-ch3-tabling-2026-09-18]]、[[skill-guide-ch1-writing-criteria]]、[[chain-form-three-tiers-naming]]、[[feedback-code-whitelist-2026-09-16]]、[[feedback-work-style]]、[[eos-design-landing-spec-93-refactor]]。

**第 100 版四处**（人类 2026-09-23 同轮追加：「你查 abr 中类似的情况并修订之」——承接 abp 第 37／38 版的去啰嗦）：扫法同上（字数 ≥150 或破折号 ≥2，排除引题的单破折号与两条独立引题同行）。**修四处**：① **§1.2「两个任务跑的是同一批…」**——「干枝——…那几级：」引题双标记改「，也就是…那几级：」，「末三级——…——是同一批」**双破折号夹注**改「最下的三级是同一批，也就是**末三级**：…」；② **§1.2「这棵树最下的三级…」**——开头的**双破折号夹注**改「已经由上游给出，也就是…：」，同段「**叶**——也就是用例场景那一级——的详细定义」改「用例场景那一级是**叶**，它的详细定义…」；该段是全篇最重的两处夹注重叠处；③ **§3.3 引题**——「除块头的三个字段——…——外」改两句（与 abp 同形）；④ **§4.5 实现方式三项规则**末句「以 X 的文档级E2E任务为准——本任务不另立清单」改逗号。**判为无病不改**：§5.2／§5.3／§5.4 各长句（均为枚举或引题体例）、§1.3 目标5、§5.6 自检与增量标记（列表体例）。
