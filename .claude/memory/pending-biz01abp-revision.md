---
name: pending-biz01abp-revision
description: 挂账：biz01abp 第 22~30 版已落；第 31~34 版＝第二~四章定点修订九条 ＋ 第四章「过程／规则」两次归位 ＋ 第五章按二三四章重写 ＋ 文档级E2E任务加「P 树归位记录」 ＋ 子块标题改名[第 31~36 版] ＋ §5.1 选定段去啰嗦[第 37 版，见文末]（第 31~36 版提交 `c32df554`；第 37 版提交 `12f72e60`）；余项＝七条待裁 ＋ 数据文件级名清扫一轮
metadata: 
  node_type: memory
  type: pending
  originSessionId: d9d470f3-bb34-43d0-9c31-85ebd491b29e
  modified: 2026-09-22T13:49:39.167Z
---

**人类 2026-09-22 定**：「我们继续修订 biz01abp」。第 19（`a323be03`）、20（`7ad67d6e`）、21（`2a0ba792`）三版已落；**第 22 版＝表3-2 逐行定案一轮**（`7b0b61c4`，连带 `28a0a8b7` 95 迁移、`1f4f948e` 记忆）；**第 23~26 版＝第一~三章打磨与反向修订**（`ee84a61f`＋`3033b6e8`，连带 `60bac879`）；**第 27 版＝第四章重构，提交 `3fd4e107`**（记忆 `2936ce0b`）；**第 28~30 版＝第五章重写、第四章各步提级、§4.5 整节删，一次提交 `a7fab8c5`**（93 第 80 版 `98a64f45`、本目录 PDF 删除 `e5deaac4`）。

## 本轮：第二~四章定点修订（2026-09-23，第 31~37 版，提交 `c32df554`＋`4193501c`）

**人类 2026-09-23 定**：「我们继续优化 biz01abp」「重点是第二、三、四章」。做法＝逐条对 93 §3.2／§3.3／§3.4 的应含禁含 ＋ 94 树权威 ＋ abr（标杆）＋ `03` 数据文件（第三章引的 schema 之家）对读，报十条；人类裁 S8「改 abp」、S9「改 abp」，其余七条按「可直接落」落。

**九条已落**（行号指改前）：

1. **表2-1 的 ⑤ 行补齐**（原 130 行）——93 §2.3 定「节点意义即 94 树里的节点意义」，第三章表3-1 的 ⑤ 上一轮正是照这条补的，第二章漏了：补 94 表2-5 ⑤ 的「带五个属性…对应一个窗口标签页」与「它的类型分正式与轻量，判据是…不是计划清单」两截。**注**：表3-1 的 ⑤ 不加 正式/轻量——94 表2-6 的 ⑤（P 树那一行）本就没有，只在表2-5（R 树那一行）有，**两侧不对称是对的**（封面模式的实体在 `02`，P 树带同名视图）。
2. **五属性注两处动**（原 144 行）——位置由「表2-2 之后」上移到「表2-1 之后」（93 §3.2：这类结构规则「作**意义表**的注」），文字删去与 ⑤ 行重复的五个名（改成「它们随文档级E2E任务一起携带，不各占一级，而是…」）。
3. **表2-2 的 ②③ 改「不涉及」**（原 138／139 行）——② 与 ③ 本任务取不到任何值（取批按构件级E2E业务走），原写「只读」与 ④ 真读文档序列的「只读」不是一个意思；改后「只读＝有东西要读」「不涉及＝不取任何值」一词一义。
4. **第三章补 Y 的 schema 出处句**（原 152 行前）——「它的结构、字段与读写约定，出处是 91 号规范 §11 与产品视角业务流程概要数据文件自身。下图是这棵树的展开。」与第二章同体例；93 §3.3 的应含头一条（节点类型判据）就带着「引用 schema 出处」，cr 也这么写。
5. **表3-2 ① 的「完备对账」换掉**（原 187 行）——该词全仓只此一处（91／93／94 与另两试点皆无），与上一轮拿掉 abr 的「语义与业务逻辑分析」同一条判据；改成「在本树里作认亲与存量方案的检索域」（§5.2 的读取范围确实写「同一产品级E2E业务／**同域**的候选存量方案节点」）。
6. **§4.4 两条注去复述**（原 340／342 行）——93 §3.4 禁含「第三章已给的判据本体与构建规则只引名，不复述本体」：注一删「节点名取自组内文档的名称与用途的共同特征」（＝表3-2 ④）与「说明写的是这个WBS业务收哪些文档级E2E任务」（＝表3-6 说明行）；注二删「该WBS业务的『输入』要素写…哪一部分正文」（＝表3-6 输入行）。两注各留本步的判定与动作，并补施动者（93 规则八）。
7. **表4-3 判据格与 §4.3 两条注**（原 314／316／320 行）——判据格删「——两树上同指一张文档」（＝表3-7 内联稳定标签行的本体）；「说明与标签随同名视图带过来」整段删（其来历半截＝表3-2 ⑤ 的承袭），其禁令半截并入「文档级E2E任务不合并」成一条（「两张不同的文档级E2E任务不并成一张，说明与内联稳定标签也不另拟、不增减」）；「不重排」三字**未留**（与 §4.4 的排序位相抵）。
8. **§4.1 注二改口**（原 274 行）——原说所属岗位级E2E业务与本任务无关，与表2-2 的 ④（「它的文档序列是本任务排 WBS业务内序位的来源」）及表4-3／§4.4 两处实用相抵；改「另两个不落 P 树上的任何一级——所属主责角色是 R 树那一面的坐标，所属岗位级E2E业务是文档序列的宿主，本任务只取它的文档序列」。
9. **§4.2 节题的范围状语**（原 278／288 行）——94 表1-4 定「干枝＝非叶节点」，本树非叶是 ①②③**④** 四级，本步只生成前三，第四级在第三步；节题改「由属性生成 Y 的干枝**到构件级E2E业务为止**」、表题同随；**连带四处同名引用**（ch1 目标1 题名、ch5 表5-1 与 §5.2 的两处枚举）。

**S8（人类裁「改 abp」）**：§4.4 节题加「本级的」→「按**本级的**节点意义归组 WBS业务」。病＝93 §2.4 第十二条的例句原话正是「「按节点意义定 WBS 业务」要写成「按本级的节点意义定 WBS 业务」」，abp 换了动词、缺的限定照旧。**连带三处同名引用**（ch1 目标2 题名、ch5 表5-1 与 §5.2）。**93 未动**（另一条路是改 93 的例句，人类未取）——故 93 第十二条第三形态「用『其』『该』『本级』顶替限定」与它自己例句用「本级的」之间的**轻微自相抵仍留着**，作为 93 自身轮次的项。

**S9（人类裁「改 abp」）**：第四章把「分几步」那句找回来——补在 §4.1 末、§4.2 之前：「**一次转换依次走三步**：由属性生成 Y 的干枝，落在业务域、产品级E2E业务与构件级E2E业务；生成文档级E2E任务节点，落在叶上；按本级的节点意义归组 WBS业务，落在 WBS业务这一级。」病＝93 §3.4 应含第 1 条要「转换过程：…分几步、每步做什么、每步在 Y 的哪一级上落」；上一轮按规则十一（不汇总若干节合起来的样子）把包装节引题删了。**人类裁定 93 应含胜**，故 abp 补给、93 不动。**abr 那边仍留着包装节与引题「一次转换依次走四步。」**——留给 abr 提级那一轮。

**顺手补三处无主句**（93 规则八，紧邻已改的注）：§4.2「扩展的节点，本任务按本批把这一句扩写」；§4.3「还没有位的，本任务新增一位」；§4.4「移除节点是架构决策，本任务标 `[需裁决]` 交人类」。**表身单元格仍不动**（判定｜处置短式）。

### 同轮续：第四章「过程／规则」按尺子归位（第 32 版）

**人类 2026-09-23 定**：「我们优化第四章，重点是内容的结构：**过程应该是过程的样子，规则应该是规则的样子**」（接着上一句「重点是第二、三、四章」而来）。第 29 版立下「每步含过程与规则两块」的形，但**内容没按块归位**。

**这把尺子是本轮的主要产出**（拟上收 93 §3.4，本轮只用于 abp）：

- **过程＝这条路**：上游给什么 → 按什么序做哪几件动作 → 每件落在 Y 的哪一级 → 本步交出什么。**只说做什么、按什么序，不说凭什么判、分几档**。
- **规则＝这把尺子**：判什么（判据出处）→ 跟谁比（比对范围）→ 分几档（档位）→ 每档怎么处置。**从 X 推出 Y 各要素的判定也归这里**（93 §3.4 应含第 3 条）。
- 判别句子的口诀：**这句话在讲「先做哪件、再做哪件」（过程），还是在讲「凭什么判、判出来是哪一档」（规则）**。

**诊出的两病**：① 三处「过程」都在讲判定——§4.2 过程原写「比出这一级是引用、扩展还是新建」（＝表4-2 的判定＋档位名）、§4.3 过程原写「挂在哪个构件级E2E业务之下，由…属性定」（＝表4-2 ③ 已说）、§4.4 过程原写「每张文档归进哪个WBS业务、谁先谁后」（＝列判定对象），且三处都只有一句、缺「上游给什么／交出什么」，读起来是残句；② 「规则」里混着动作序——§4.3 的「占位兑现」是「先比占位（动作序）＋三去向（处置）」两事装一条。

**落法**：

1. **章首补一段引题**（第 255 行）——「一次转换依次走三步：…」由 §4.1 末**上移**成第四章的引题，与第五章的引题（「一次运行起于 Step Start，依次走 Step 1~4 的设计动作，收于 Step End。」）同形。这样「过程」在章与步两级各有一处，§4.1 也回到纯「两端对应 ＋ 认亲」。
2. **三处过程重写**，统一成三段式（上游给什么／按什么序／交出什么）并删去判据与档位名：§4.2「…每落一级，本任务都拿已有的同级节点比一次，据此定下这一级的位置；上一级落定之后，下一级才有它所在的父节点。三级落完，本批的干枝就位。」§4.3「上一步把本批的干枝落定之后，本步把本批的文档级E2E任务逐张挂到它的构件级E2E业务之下。每张先与该构件之下的存量占位对一次，再落到它在该构件之下的那个位。本批全部落完之后，文档级E2E任务这一级就整批就位。」§4.4「整批文档落到构件之下以后，本步把这批文档当一个整体一次判完：先定每张文档归进哪个WBS业务，再定WBS业务与WBS业务之间的先后，最后定每个WBS业务内各文档的序位与输入。判完，WBS业务这一级就整批就位。」三处收句同形。
3. **三个「规则」块的开头统一为判据出处句**：§4.2「落位判定分三档，判据出自 91 号规范 §5.3.1。」（原有）、§4.3「落位的判据取内联稳定标签与名称级比对，即表3-7 与 91 号规范 §5.3.1 里的定义。」（新补，原先没有）、§4.4「归组的判据取 WBS业务这一级的节点意义，即 94 号文档 §2.2.2 表2-6 里 WBS业务一行的意义。」（原有）。**§4.3 那句起初写成「分三种情形」，因表4-3 只有一行（三情形在处置列）而改掉**。
4. **§4.2 两条注并成一条**——「节点的说明怎么来」与「需扩展时改节点的边界」都在讲说明／边界，重叠；并成「**干枝各级的说明与边界怎么定**」。
5. **§4.3「占位兑现」改成「占位的去向」**并瘦身——动作序那半（先比占位）移进过程块，规则块只留本表没说的：不与任何占位同指的**各占位不动**（保留），并把兑现支指回表4-3（原「另两条去向」的计数是错的：91 §5.3.1 共三条去向，原文已把两条写在自己句里）。
6. **§4.3 过程删「X 的文档级E2E任务整段搬入 Y」**——那是 94 §1.4／§2.2 的树级关系，属「只有撤掉才不越界」一类（abr 表4-1 注二撤、[[skill-guide-ch3-ch4-split]] §二）；来历由表3-2 ⑤ 的承袭承载。
7. **章首引题的写法**：不带粗体、成一段普通文字，与第五章引题同形（第五章引题无粗体）。

**留下的两处**（报出未动）：① **ch5 §5.2 那句「第二项里每张文档先与本构件级E2E业务之下的存量占位对一次，判定与处置按表4-3」与 §4.3 过程近同**——该重复在我这轮之前就有（原先在 §4.3 规则里），ch5 不在「优化第四章」的射程内，留下一轮；② **4.1 里的「认亲」是判定动作（93 §3.4 应含第 5 条对齐规则）却没有「本步的规则」这类外壳**——4.1 不是一步，故不设过程／规则对，维持现状。

### 同轮续：第四章每步改「动作清单 ＋ 具名规则」（第 33 版）

**人类 2026-09-23 定**：「每一步应是这样的风格：①做什么，使用xx规则 ②做什么，随用yy规则；xx规则：…yy规则：…」——即**过程＝编号动作清单，每项点名它要用的规则；规则＝具名规则块，排在过程之后**。这一改的直接收益是**规则有了名字**：别处引用从「按表4-3」变成「按**末级落位规则**」——表号重编不破，且正合 93 §3.4 禁含要的「只引名，不复述本体」（原先没名可引，只能引表号）。

**我答「你是怎样理解的」后定的四处**（人类先要理解、再点头）：

1. **动作清单不用圈号 ①②③**——本文件圈号专指树级（⑤＝文档级E2E任务），节内清单若同用会撞；改用 `1. 2. 3.`（与「主要目标」同体例）。
2. **动作与规则是"点名"不是一一对应**——允许一项挂多条规则、多项挂同一条（§4.2 的三项落位共用干枝落位规则）。
3. **过程块保留三段式，中间那段换成清单**——块首一句给"上游给什么"（施动者在此给一次）、中间编号清单、块末一句给"本步交出什么"。清单因此是齐的（每项都是"动作＋规则名"），前提与产出不当凑数项。
4. **规则块按过程里首次被点到的次序排**；每条规则自带判据出处、判据、档位、处置——**上一轮我补的三处「规则引题句」（判据出自…）被吸收**，出处归到每条规则自己的开头。

**两小问由我定**（人类授权）：规则名引题用「**——**」不用「：」（全篇体例，且冒号接表会断）；动作项**动词起首**、施动者由块首那句给一次。

**九条规则名**（贴 93 §3.4 已有的「X 规则」形，名字取 91／94 的动词，不自造）：§4.2 **干枝落位规则**／**干枝各级的说明与边界规则**；§4.3 **末级落位规则**／**认亲规则**／**占位去向规则**；§4.4 **WBS归组规则**／**WBS业务的名与说明规则**／**WBS业务的序位与输入规则**／**WBS归组的例外处置规则**。名字一律写足限定（93 §2.4 第十二条），故「WBS归组的例外处置规则」偏长也不省。

**连带三处**：

- **§4.1 的「认亲」整段移进 §4.3 的规则块**（成「认亲规则」）——它本就是判定动作（93 §3.4 应含第 5 条对齐规则），却挂在没有规则外壳的 4.1 底下（这正是上一轮报出未动的第二处）；移后 4.1 回到纯「两端对应（表4-1 ＋ 两注）」。
- **§4.2 的「同义的节点也算已有」并进「干枝落位规则」的开头**——表4-2 的判定列本就写着"说明覆盖不覆盖"，同名/说明两步比对是它的展开。
- **§4.3 的「文档级E2E任务不合并」并进「末级落位规则」的开头**（原为表下一条独立粗体注）。

**留下的两处**（报出未动）：① **ch5 §5.2 那句「第二项里每张文档先与本构件级E2E业务之下的存量占位对一次，判定与处置按表4-3」与 §4.3 过程近同**——该重复在我这轮之前就有（原先在 §4.3 规则里），ch5 不在「优化第四章」的射程内，留下一轮；② **ch5／ch3 里的表号引用（按表4-3 等）未改按规则名引**——规则名已就位，改用名字更稳，但同属 ch5／ch3 的活；这条与上一条同源，可一并办。

### 同轮续：第五章按二、三、四章重写（第 34 版）

**人类 2026-09-23 定**：「对于 biz01abp，基于第二、三、四章的内容，重写第五章」（与第 28 版同一句指令，故同为**审计＋对齐**：二三四章这两轮大改之后，第五章要对上新的它们）。

**诊出四处失配**：

1. **§5.2 在重述第四章的过程与表**——「第二项里每张文档先与本构件级E2E业务之下的存量占位对一次，判定与处置**按表4-3**」（＝ch4 §4.3 的过程 ＋ 表号）、「WBS 归组待文档级E2E任务落位之后整批一起判，范围含存量的文档」（＝ch4 §4.4 的过程）、「本批与任何已有WBS业务都不属同一段业务的，本任务新建该WBS业务，并另出一份新建WBS业务方案，列出该级应增补的文档级E2E任务」（＝表4-4 的新建行）、「P 坐标由本任务逐文档标定，认亲本体唯一」（＝表3-6 的认亲行＋认亲规则）。93 §3.5 明写「跨章的判据（X→Y 的判定规则）**只引第四章的规则名，不重述本体**」——而 ch5 当时还引着**表号**，规则名一轮落定后这处成了双料违规。
2. **§5.1 的状态推进只有一段散文**——缺**状态机图**（93 §3.5：「**状态机图与写入分工并给**：主线、上游变更分流、下游退回三条路径与 91 附录A §A.6 的状态机一一对应」），也缺三条路径的分述。
3. **§5.6 自检用的名字与第四章对不上**——「已有文档的**过时判定**」vs ch4 规则里的「**已有文档被归出去**」（一处一名，93 规则七）。
4. **§5.4 的「输入闭合」在有面 bullet 与正文段里各说一遍**。

**落法**：

1. **§5.2 的三项改成第四章三步的编号清单**，每项点名规则名（干枝落位规则／干枝各级的说明与边界规则；末级落位规则／认亲规则／占位去向规则；WBS归组规则／WBS业务的名与说明规则／WBS业务的序位与输入规则／WBS归组的例外处置规则）；重述句删净，只留 ch5 自己那层的两条事实（本批同属一个构件级E2E业务、干枝一次定出；本步另出的两样东西）。**另补「本步无人类参与点」**——93 §3.5 要每节给全「动作序、触发、产出与去向、人类参与点、失败处置」五件套，§5.2 原先缺这一件。
2. **§5.1 的状态推进从一段散文扩成「写入分工 ＋ 上游变更分流 bullet」**：主线 `待确认方案 ──人类确认──→ 可以srb设计`；上游变更分流只落得下一支（本次没处理的→保持原状；已经标定 P 坐标的→版本递增，增量处理，状态维持 `可以srb设计`）。**顺带改掉一处硬错**：§5.1 原写「本任务往 Y 里写 `待确认方案`／`可以srb设计`／`待补充srb设计` 三个状态」，与 §5.6 三区输出的「当前状态：<待确认方案 / 可以srb设计>」相抵，也与本侧无下游的事实相抵——改两态。

   > **这一处起初做成 mermaid 图5-1（状态机），人类 2026-09-23 当场裁定撤掉**——abr 的同一张图在第八、九轮已按「要收缩」删过，理由是「凡下游的验收行为、状态机的转移边，都是复述，执行者不必知道；生存周期本身又归 91 §A.6」，删后 abr 只画三张图、全是本任务自己的内容。按 [[eos-pilot-authority]]（abr 为权威）**改成与 abr 同形的散文＋bullet，不画图**；**连带 93 §3.5 的「状态机图与写入分工并给」应改「写入分工并给」**（见 [[pending-biz01abr-ch5]]）。abp 的版本号仍是第 34 版——加图与撤图都在这一未提交版内。
3. **§5.6 自检的「生成」项改成按第四章三条规则逐条核**，名字与 ch4 对齐（「已有文档被归出去」）。
4. **§5.4 的「输入闭合」判据只留在正文段**，有面 bullet 只留锚点。

**连带待办**：`03` 的 §1.1 状态取值表要随之改（它把 `需biz01修订` 注成「下游检出缺陷退回」、把 `在srb设计`／`已srb设计` 注成「由下游写入」，而本侧无下游）——归 **S10 数据文件一轮**，本轮未动 `03`。

### 同轮续：文档级E2E任务加「P 树归位记录」（第 35 版）

**人类 2026-09-23 定**：「应该记录职责视角业务流程概要中的文档级E2E任务节点被归位的产品视角业务流程概要中的节点ID，以及前者的状态值」，并给出用途「**便于追踪和选择**」。

**读法的裁定**：「以及前者的状态值」有两读——（一）记 P 侧那个节点的状态值；（二）记 X 自己的文档级E2E任务节点的状态值。**「便于追踪和选择」定了（一）**：**选择**要判「哪些文档还没在 P 树上就位」，看的是 P 侧节点的状态（`待确认方案`／`可以srb设计`）；而 X 自己的状态是 abr 的账，abp 只写 `需biz01修订` 这一种（表5-2 两行），让 abp 往 X 的文档节点上写状态会越界。**与 abr 的承接记录同构**——那条的「状态」列记的也是收下它的那一侧的进度（核过的写已承接、核不过的写缺口）。

**这条记录带来的实利**（也是它值得加的理由）：abp §5.1 的批次判据「尚未在 P 树上标定 P 坐标」原是**跨两棵树**才判得出的；有了 X 侧这条记录，**扫 X 一处即知**——没有归位记录的就是尚未归位的（**选择**）；顺着记录里的节点 ID 直达 P 树那一侧（**追踪**）。

**落点六处**（顺序有讲究——`02` 是 abr 的 Y，要素得先在 abr 的表里立）：

1. **abr 表3-7（文档级E2E任务 的要素）加一行**——「**P 树归位记录** | 本张文档在 P 树上被归位到的节点 ID，以及该节点当前的状态值；尚未归位的为空」（abr 第 98 版）。
2. **`02` §4.1 节点模板**——要素表加同一行；逐文档项（「业务文档实现方式与任务、叶」那段列表）加「P 树归位记录：{P 侧节点 ID}（状态：{…}）」；**并改标注纪律**——原写「本文件不写 P 侧内容（产品类型／构件／WBS／认亲）」，这条回指是 P 侧派生的事实，补一句例外与用途（给 X 侧就近追踪与选择用）。
3. **abp 第三章**——表3-7 下加注「**P 树归位记录写在 X 侧**」。
4. **abp 第四章认亲规则**——改「归属的结果落成两项」：本树同名视图上的 P 坐标 ＋ X 侧的 P 树归位记录。
5. **abp 第五章四处**——§5.3 写回加「**也写的是 X**」（每次写回一并刷新）；§5.3 特化参数的操作条目补一项；表5-1 Step 2 的写资产列补「职责视角业务流程概要」；§5.6 自检的「确认与写回」补一句；§5.1 的批次判据补「就地判据落在 X 侧」。
6. **abr 第五章一处**——§5.3 的「合理空置」判据扩一条：**P 树归位记录由下游的产品视角业务流程任务在归位时写入**（否则 abr 的要素机械检查会把这个空字段当缺陷）。

**未落**：`02` 的级名仍是旧族（E2E职责／角色职责／业务文档），本轮只加了字段——归 S10 数据文件一轮。

### 同轮续：两个子块标题改名（第 36 版）

**人类 2026-09-23 定**：「本步的过程 → **转换过程**，本步的规则 → **转换规则**」（两试点同步：abp 6 处／abr 8 处）。**这是把第 29 版的裁定翻回**——那一版明写「`##### 本步的过程`／`##### 本步的规则` → `####`（**「本步的」保留**——它是限定，不是 93 第十二条要打的「其／该／本级」那类顶替，去掉了反而落不下「哪一步」）」。**翻回的两条理由**：① 与章题同族（第四章就叫「输入到输出的过程及规则」），且与 93 自己的词对齐（93 §3.4 的应含项叫「**转换过程**」、§6.1 的操作叫「**转换规则**」）；② 那个限定改由**节题**承担——`### 4.2 第一步 · …` 就压在两个子块头上。理由与体例记进 [[skill-guide-ch4-process-and-rules]] 第 5 条。



**报告里列出、人类未裁、也未落的一条**（留作 93 自身轮次）：93 §3.3 应含末句「**类型判据与形态身份也不受此限**——正式／轻量、执行类／管理类的判据仍写在本表那一行」，按字面读要求第三章的意义表给 正式/轻量；而 94 表2-6 的 ⑤（P 树那一行）没有它。**本轮取的口径**＝「94 树那一行有的照写、那一行没有的不补」（abp ch3 不加、ch2 加），理据是 93 §3.3 那句本意是「不许因『同指只作指认』把 94 已有的判据丢掉」而非「给本没有它的树补判据」。**建议 93 那半句改写清楚**，否则下一个人会照字面给 P 树 ⑤ 补判据。

**报出、人类未裁的第十条（S10）：数据文件一轮**。`03` 与第三章对不上，而 `03` 正是 ch3 引的 schema 之家（93 §4.1：结构只有一个权威家）。**这不是 abp 的账，也不是写错**——是 2026-09-21 级名定案（`[[chain-level-name-family]]`）的**未落项**，那里明写「数据文件 02／03／04 与 91 其余处未扫」「这是一次 schema 改名——字段名进了 91 §11 与 02／03／04 数据文件的口径，那几处尚未跟」。**但半改比不改更乱**：`03` 的级名一换成 94 那一族，`02` 不改就出现「abp 第二章引的 X（02）用旧名、第三章引的 Y（03）用新名」；实查 `02` 现有 **36 处**旧名（角色职责／E2E职责／业务文档），`94` 里这三个名一处都没有。

建议单列一轮**「数据文件级名与结构对齐」**，次序：

1. **`02`＋`04`＋`cr` 的级名与属性名清扫**（三级更名 ＋ 三个属性名）——`02`／`04` 是同一棵树的两态，`cr` 读 `02`，必须同轮。
2. **`03` 的树形与级名**——七级改五级；§0.3 图、§0.1／§0.2／§2.1 的列名、§3.1 节点口径（去「数据依赖闭合／数据依赖链」，改 94 表2-6 ④ 的原话）、§3.3「末三级同名视图」改「末级」。
3. **`03` §4.1 节点模板对齐 abp 表3-6／表3-7**——删「P 树一致性结论」与「缺口推断」两行；补 名称／说明／在构件级E2E业务内的序位；逐文档认亲项「数据依赖：{前序文档 · 依赖什么数据}」→「输入：{来自本构件级E2E业务内哪张文档的哪一部分正文}」，占位项「兑现时按数据依赖链复核」→「按WBS业务这一级的节点意义复核」（照 91 §5.3.1 现文）。**保住**「业务文档视图（逐文档）：与 `02` 同名、同一次序」——abp 的「序位承袭 X 的文档序列」正是依它定的。
4. **`03` §1.1 状态取值表 ＋ abp §5.1 状态推进（人类需裁）**——现文 `03` 把 `需biz01修订` 注成「下游检出缺陷退回」、把 `在srb设计`／`已srb设计` 注成「由下游写入」，而本侧无下游；abp §5.1 也顺着声明写 `待补充srb设计`，但该状态在 ch5 里没有写入场合。**建议的裁法**（本轮已按 91 §A.6 想透、未落）：P 树节点主线只两态 `待确认方案 ──人类确认──→ 可以srb设计` ＋ `已废弃`，占位记 `占位` 不进状态机；三条路径的落地＝**上游变更分流只落「可推进态→可推进态（版本递增）」一支**、**下游退回无**（`在srb设计`／`已srb设计`／`待补充srb设计` 在 P 树上不出现，本任务对 X 写的 `需biz01修订` 落在 `02` 上）。理据：§A.6 三条分支都以「下游有没有消费过」为分流键，而 `03` 的 P 坐标是随末级同名视图进入下一对的、`03` 自己不被单独承接，三个下游态**在 P 树上没有对象**。**93 §3.5 要求「缺一条路径的图会把合法的流转挡掉」，故须写明为无并给理由，不能略过**；abp §1.4 的「下游｜无」已起头，ch5 补状态面的后果。**写法定案（2026-09-23 人类裁）＝散文＋bullet，不画状态机图**——见上第 34 版那处的注与 [[pending-biz01abr-ch5]]。
5. **`91` 其余处**——§5.1 视角总表两行、§5.3 的 Pn 链、§11.1 文件清单（02／03／04 旧名）、§11.5 ID 前缀表（`@doc-` 还注着「P 树 ⑤ 业务文档」），另加 `@wbs-` 一类 WBS业务 层的前缀。abp 二三章不直接引它，可另列。
6. **`03` 的【待定】节点类型与 ID 前缀**——abp ch3 §3.3 只写「块ID、节点类型、当前状态」不给值，与 `03` 的【待定】一致，**abp 不用动**；值等 91 §11.5 那一项定。

## 本轮：第五章重写（2026-09-22，第 28 版）

**人类定**：「以第二、三、四章为依据，重写第五章，布局和格式可以参考 biz01abr 的第五章」。做的是**审计＋对齐**，不是推倒重来——ch5 第 12 版立下的骨架（五章、表号、写回随两次确认就地落地、Step End 只汇总）一直随 ch1~4 逐轮同步，故本轮净改 19 增 21 删。

**逐条落**：

1. **表5-1 Step 1 行**——「查占位兑现」原单列成第 5 项，而 ch4 把占位兑现放在**第二步**（表4-3 的判定列）之内；删该项，四项名改为照抄 ch4 §4.2 四个节题（末项原「标定它的 P 坐标」→「标定文档级E2E任务的 P 坐标」）；产出列补 **新建WBS业务方案**。
2. **§5.2 删「占位兑现检查」整段**——同上，改为四项段里的一句「第二项里每张文档先与本构件级E2E业务之下的存量占位对一次，判定与处置按表4-3」。
3. **新建WBS业务方案有了落点**（原 ch4 表4-4 产出、ch5 全无着落）——Step 1 产出 → 随 P 树方案一并交 Step 2 确认（照 abr 的「岗位级E2E业务聚类方案」先例）→ 其列出的应增补文档级E2E任务**计入 §5.3 应有清单比对的应有清单** → 缺口在 Step 4 落占位。连带改 表5-1／§5.2 产出去向／§5.3 未确认清单／§5.4／自检两行。
4. **§5.4 第二面的名与锚**——「WBS业务这一面」→「**产品数据这一面**」（两条判据一个锚构件级E2E业务、一个锚本WBS业务，原名与前一锚相抵）；两条判据各写明锚点；「按本级的节点意义核」→「按WBS业务这一级的节点意义核」。核 91 §5.3.1 原文确认原来的两条判据表述无误（「应有的产品数据在本 WBS 业务里缺席」正是两锚并用）。
5. **§5.2 失败处置删一句**——「本批中某张文档整体归位不到的，该文档退出本批」在 ch4 无对应支（落位由属性定、判不出走表4-5 的 `[需裁决]`）。
6. **§5.6 增量标记四枚→三枚**——`[占位]` 撤（ch2~4 与 `03` 都只以**当前状态 `占位`** 认它，无此括注标记）。
7. **格式随 abr**：章首引题「起于…收于」；§5.2 去掉「占位兑现检查」这一多出的粗体引子（abr 的 §5.2 只有「读取需求」一个引子）；§5.3 写回段改为 abr 的「**写的是 Y**——」体例；§5.1 退回上游去掉两个破折号夹的三项枚举（照 abr 的短式，三项在表5-2 里已有）。
8. **表5-2 三处**——第 2、7 行的「该节点」写全为「该岗位级E2E业务节点」。
9. **删一句分工声明**——§5.2 读取输入里的「文档承载什么、怎么开发，是上游的事；本任务判的是它作为产品数据落在哪个构件、哪个 WBS业务、哪个序位」：删掉本句该段仍完整（93 §2.4 规则一的判据），且「判的是落位」与 ch3 表3-2 的「由属性直接确定」相抵。

**10. 第三章两处**（同日，随人类问「第三章有关文档级E2E任务节点是如何实现的」顺手修）——① **表3-2 ⑤ 的构建规则**原写「本项挂在「所属构件级E2E业务」之下」，与图3-1（⑤ 画在 ④ 之下）、表3-6 的认亲（挂在 WBS业务节点上）、表3-7 的「在WBS业务内的序位／输入」、94 图2-5 全线相抵——**那是把 ④ 那一行的句子抄过来抄错了格**（93 §3.3 说「构建规则」一格管的是**直接父节点**即父节点归组，④ 的父是 ③、⑤ 的父只能是它所在的 ④）→ 改「**本项挂在 WBS业务之下**」；② **表3-1 ⑤ 的意义行补**「在 P 树上是**叶节点**，也就是概要的末级」（94 表2-6 ⑤ 有这句、ch2 表2-1 的 ⑥⑦ 对 R 侧也各写了枝梢／叶）。**第四章不动**：⑤ 先由「所属构件级E2E业务」属性定到该构件、再随 WBS 归组落进 ④，那是 §4.2.2→§4.2.3 的过程，ch4 §4.2 引题「WBS业务由落位的结果整批聚类而定」已交代。

**11. 表4-5「P 树一致性三类」整张撤——人类 2026-09-22 定「收掉它」**（第 28 版内）。**缘起**：人类问「在 R 树上，文档级E2E任务已经被确定了，在 P 上直接归位就可以了，存在这三类不一致性问题吗」——核后答「基本不存在，只剩一条变形的」。**理由**：三类是「判归属」时代的判据——那时要对着 23 号资产那份**构件产品数据清单**逐文档判 Q1/Q2/Q3，判就有判错的可能，故配三类兜底；23 已取消、①②③ 已改「由属性直接确定、归属不判」（第 22 版裁定），**判的动作没了**——**误引用**降为 X 侧属性缺陷（走退回上游）、**归属冲突**的前提（两名争一位）随「位置由内联稳定标签认」而消失、只剩**误新建**变形为「名称级比对的同名判定」。**落法**：① 表4-5 整张删（§4.2.4 只剩「标定的落点」）；② **「同义也算已有」并进表4-2**（新注：落位先按名称比对，名对不上再看说明——同父节点下已有节点的说明覆盖本批文档的用途或产品数据类别的，仍是同一个，引用它、不另起节点；同一父节点下不设两个同义的节点）；③ **表5-2 补一行退回上游**（「所属产品级E2E业务」或「所属构件级E2E业务」与它承载的产品数据不是同一类）；④ 表4-6 顺号成 **表4-5**；⑤ 清掉全篇 13 处「P 树一致性结论」（ch1 §1.3 目标 4、表3-6 的要素行、ch4 §4.2 引题、ch5 表5-1 与 §5.2／§5.3 四处／§5.6 增量标记与自检两行）。**未动**：`03` 数据文件 §4.1 模板里那行「P 树一致性结论 | 三类检查」（随 `03` 整备一起改）。**连带解锁**：91 §5.3.1 的「一致性三类」与 §7.1 任务清单第 7 行那半句可随 91 反向修订处理，见 [[pending-91-align-to-94]]。

**12. 第四步「标定文档级E2E任务的 P 坐标」整节删**（人类问「这一步还需要吗」）——它是**空步**：P 坐标三项由第一步（①②③ 三级，属性定）与第三步（④，聚类定）产生，三项的档位也由那两步的判定给出；第四步剩下的只有「写下来」，而「写回」按 93 §3.4 的禁含归第五章，且 ch5 §5.3 的写回句早已写了这两个落点（同名视图上的 P 坐标 ＋ WBS业务节点的认亲清单）。**落法**：① §4.2.4 整节删，§4.2 由四步改**三步**（引题、§4.3 的「三步都按并入处理」、ch5 表5-1 Step 1 职责、§5.2 的「依次走三项／三项走完／三项都按并入」同步）；② 该节里那半句「**两处同源、一动同改**」搬到**表3-6 表下成注**（「**P 坐标两处都写**——每个WBS业务节点的认亲要素，与它名下每张文档级E2E任务的 P 坐标，是同一件事的两个落点：两处同源，本WBS业务的边界或序位一动，两处同改」）——判据用第 27 版立的分界尺子「X 拿掉还成立、且不涉及『从 X 取什么』→ 第三章」。**标定这个动作现在只落 ch5 §5.3 的写回**。**表号不受影响**（第四章仍是 表4-1…表4-5 连号）。

**13. §4.2.3 与第三章对 WBS业务 的实现规则对齐**（人类问「4.2.3 第三步与第三章中关于 WBS业务节点的实现规则是否有不一致的地方」）——**两处**：

**实质一处：「WBS业务的边界」在第三章没有载体。** 表3-2 ④ 与表3-6 里「边界」都不是要素，能当边界的只有表3-6 那句**说明**（「一句话说清本WBS业务收哪些文档级E2E任务」）；而第四章有三处建在「边界」上（表4-4 的扩展档、§4.3 的边界匹配与「超出当前边界」）。②③ 两级是靠表4-2 下那句「需扩展时改其边界——只把该级自身的说明随本批扩写」接上的，④ 这一档没接。**接不上的后果是硬的**：只把新文档归进去、重排序位而不动说明，表3-2 下的收敛自查第一条「父节点语义覆盖子节点语义之并」当场不成立。**落法**：表4-4 下补一注——「**WBS业务的边界落在它的说明上**——「收哪些文档级E2E任务」那句就是边界；扩展边界，就是把这句随本批扩写。节点名仍按表3-2 取自组内文档的名称与用途的共同特征，共同特征没变就不改名。」

**用词一处：P 侧这个动作两章一个叫「聚类」一个叫「归组」**，§4.2 引题里两个词隔一句同现（「WBS业务由落位的结果整批**聚类**而定。……WBS **归组**的判据取自 94 号文档」）。**查权威定案**：**P 侧用「归组」**（94 §2.2 那行「同一批文档在 P 树里另按构件**归组**，序记在 ④ WBS业务」；91 通篇「WBS 归组」——§5.3.1、§7.1、§10.6 等）；**R 侧用「聚类」**（94 给 ③ 职责级E2E业务、abr 的「岗位级E2E业务聚类方案」）。**落法**：abp 四处 P 侧「聚类」改「归组」（表3-2 ④ 的构建规则、表4-1 下注、§4.2 引题、§4.2.3 引题）；① 业务域的「逻辑聚类」与 R 侧 ③ 的「聚类」保留。

**14. §4.3「并入已有 Y」三处收口**（人类问「这节是什么目的」）——**目的**＝93 §3.4 点名的「**修订转换规则**（复用→改进→新增 的分流与关系检测）＋**影响面复查**」，第四章四块应含内容里的一块；ch1 §1.3 那次删「三项复查」正是把职责落到这里。**收口三处**：① **删「边界匹配」整项**——它自称「判据同表4-4」，而表4-4 三档本来就是对着已有 WBS业务判的（引用／扩展／新建），末尾「不同段退回另择」也已被「新建」那档包住；它独有的一句「判不出的标 `[需裁决]`」并入表4-4 下的注。② **修「过时」的判定**——原写「本WBS业务内已有文档**本次无覆盖**，且本次归位后不再落在本范围内」，而本批的定义是「**尚未**在 P 树上标定 P 坐标的文档」，于是每一张存量文档都「本次无覆盖」、这一档会对全部存量成立 → 去掉前半句。③ **改 §4.3 引题**——原「三步都按并入处理」把「并入」写成了一个分支，而表4-2 的比对范围本就是「同域已有的／同一产品级下的已有」节点，三张表一直在对着已有节点判、没有两种模式 → 改「走下面两项」；ch5 §5.2 同步改「另做并入的两项——文档关系检测与变更影响声明」，§5.6 自检的「边界匹配／关系检测／变更影响声明」去「边界匹配」。**留着没动**：表4-5 的「复用／补充」与表4-3 的处置仍有重叠面——留，因为它是 93 点名要的分流，且切入角度不同（表4-3 判「这张文档有没有位」，表4-5 判「它与段内已有文档是什么关系」）。

**15. 段要排序（人类裁定）＋ §4.3 再收＋输入范围放到构件级**（同日，人类两条：「一个构件级E2E业务下的WBS业务是需要排序的」「4.3节，我没看出来它的价值」）——

**一、段排序**（据此推翻我上一条的判断——全书原来只有「在WBS业务内的序位」，段自己在构件之下的位次没有落点）。**落法**：① 表3-6 加要素「**在构件级E2E业务内的序位**——本WBS业务在构件之产品数据序列上占第几段」；② §4.2.3 的「序位与输入怎么来」加段间通则「各WBS业务之间的先后，按它在本构件级E2E业务的产品数据序列上占的那一段定——**上一段的产出是下一段的输入**」；③ 表4-4 的「新建WBS业务」档补「排入它在构件级E2E业务内的序位」；④ 连带改 ch1 主要目的（「按它在本构件级E2E业务的产品数据序列上占的位置排出开发次序」）、§1.3 目标 2、§5.6 自检的 Y 侧要素行。

**二、§4.3 再收**——撤**表4-5「WBS业务内文档关系检测」整表**（四行里「复用／补充／位次变更」是表4-3 三支的重说，只有「过时」是新的）→ §4.3 只剩两条：**过时判定**（本WBS业务内的已有文档、本次归位后不再落在该范围内的 → 标 `[需裁决]`）与**变更影响声明与影响面复查**；ch5 §5.2／§5.6 自检同步改「过时判定」；第四章表号仍是 表4-1…表4-4 连号（删的是末一张）。

**三、输入范围放到构件级（是上面第 ② 条逼出来的）**——「上一段的产出是下一段的输入」一写，表3-6／表3-7 的「输入」原来锁在「本WBS业务内」就当场相抵，于是：**表3-6 的「输入」**改为「本WBS业务内各文档的次序，以及每张文档的输入来自哪张文档的哪一部分正文」（后半句去限定）；**表3-7 的「输入」**改为「本张文档的输入来自**本构件级E2E业务内**哪张文档的哪一部分正文；本WBS业务的首张，其输入落在前一个WBS业务里」；§4.2.3 的同一句同步；§5.4 的判据名**「段内输入闭合」→「输入闭合」**、锚改为本构件级E2E业务、「每张文档的输入都能在它之前的文档里找到来源」（§5.6 自检同步）。**注**：这个判据名是第 22 版定的，本轮改了名与范围；**待核**＝91 §5.3.1 的缺口推断行与 `03` 的模板是否也要跟（`03` 随整备）。

**16. §4.3「并入已有 Y」整节撤**（人类：「关于4.3节，你好好分析下它的价值以及必要性」）——**分析结论：两条规则都有用、都必要；凑成一节不必要。** ① **过时判定**必要——它是全任务唯一一条「往下撤」的通道（第三步把本批与存量文档一起重判归组，重判可能让早先落定的文档不再落在原段里；删节点是架构决策，只能标 `[需裁决]`）；② **变更影响与影响面复查**必要——93 §3.4 点名把「影响面复查」列进第四章应含内容，且「本任务这一侧复查什么」是本节独有。**但作为一节不必要**：三条理由——(a) 按触发条件凑桶（两条唯一共同点是「Y 里已有东西时才会发生」，一个属第三步、一个横跨三步），不按读者做事的位置分家；(b) **成立前提已被拆掉**——原引题「三步都按并入处理」在说存在两种模式（首次建树／并入），而表4-2 的比对范围本就是「已有节点」，三张表一直对着已有节点判，没有两种模式；(c) 它已空过一次（边界匹配与关系检测表今天先撤）。**落法**：撤节——**过时判定**收进 §4.2.3 表4-4 下成第四条注「**已有文档被归出去**——本WBS业务内的已有文档，本次归位后不再落在该范围内的，标 `[需裁决]` 交人类——移除节点是架构决策，本任务不自行删」；**变更影响与影响面复查**降为 §4.2 末尾一段（无小节标题）。连带：ch5 §5.2 那句改「另作已有文档的过时判定与变更影响声明」、§5.2 首段的「按并入已有 Y 的方式处理」删、§5.6 自检改「已有文档的过时判定与变更影响声明已执行」。**第四章自此两节**：4.1 X 与 Y 的对应／4.2 转换过程（4.2.1~4.2.3 ＋ 末尾的变更影响段）。

**17. 表达方式的整轮清扫 ＋ 规则上收 93**（人类两条：「有些标题或句子是不是应该补充主语或定语」「你把这个规则落在93规范中」）——

**病名与来历**：这两轮**打补丁**时落下的电报体——① 条件小句连用、句子里没有施动者（「…的，标…」「…的，把…」）；② 破折号与分号把三四件事硬串一句；③ 「该范围」「这句」「后者」「段属」指代落不下；④ 小标题式压缩引子（「名随组内文档的共同特征」「两级序位怎么排」）。**其中 ② ③ 与「无主语的公式单独成句」本来就在 93 §2.4 规则八里**（判据「这句念一遍，能说出是谁在做什么吗」），**缺的是「限定」这一条**——故上收为 93 **§2.4 第十二条「标题与引子写足限定」**（判据：把这一行单独抄出来给别人看，他能不能说出这是哪一级、哪个节点、哪份文档上的事；三种常见形态＝动作没有对象／对象没有所属／用「其」「该」「本级」顶替限定）；93 → **第 80 版**，PDF 已用 md2pdf 重生成。

**abp 侧改动（约 21 处）**：
- **八条注的去电报体**（人类点五处 ＋ 同病三处）：表4-4 的四条注（边界／名／两级序位—拆成两条「序位」「输入要素」／已有文档被归出去）与 §4.2 末的变更影响段，另加表4-2 的「同义的节点也算已有」「文档级E2E任务不合并」「占位兑现」——一律改成**一句一个施动者**（「本任务…」）、分号拆句、「该范围／后者／段属」换成写明（「该WBS业务范围」「归进哪一段」）。
- **六处引子补定语**：「两级序位怎么排」→「**本WBS业务的**两级序位怎么排」、「输入要素写什么」→「**本WBS业务的**输入要素写什么」、「边界记在它的说明里」→「**WBS业务的**边界记在说明里」、「名取自组内文档的共同特征」→「**WBS业务的节点名**取自组内文档的共同特征」、「已有文档被归出去时」→「**本WBS业务**已有文档被归出去时」、「存量节点」→「**待处理的**存量节点」；另**§4.2.3 节题**加定语「按**本级的**节点意义定 WBS业务」、「说明怎么来」→「**节点的**说明怎么来」、「需扩展时改其边界」→「需扩展时改**节点的**边界」。
- **九处无主句补施动者**：§5.1「受理门槛如下，**本任务**逐条判」；§5.2 触发「**本步**就走」、读取输入（原本**整句无谓语**——「本批全部文档级E2E任务，各项上的…」，改为「**本任务读的是**本批全部文档级E2E任务：…」）、失败处置「**本任务**标 `[需确认]`」；§5.3 未确认清单「**本任务**把…一并列出」、逐条讨论「讨论中提出修改的，**本任务**按讨论结果把方案改在 Y 上」、机械检查「**本任务**逐文档检查…**本任务**就地补齐」；§5.4 触发「Step 1 走完之后**本步**就执行」；§5.5「**本任务**逐条判」；§5.6 收尾「**本任务**更新…，并提交基线」。ch1 §1.1 主要目的补主语「**本任务**把…」。
- **没动**：表4-2／表4-3／表4-4／表5-2 等**表身单元格**——表按惯例是「判定｜处置」短式，93 那几条正文写法不往表格里套。

**18. 全文扫描第二轮**（同日，人类「你再根据这个规则，仔细扫描并修订下文档全文」）——按新立的 93 第十二条＋规则八把 ch1~ch5 通篇过一遍，另修 **20 处**：

- **ch1**：「需要人类介入」的三条各补施动者（**人类**从候选清单选定／**人类**讨论确认／**人类**裁决缺口）；目标 2 补判据句「判据取 WBS业务这一级的节点意义」并把它自己的「该构件」写明为「本构件级E2E业务」；目标 3 的两个「同指的」写明「与某个占位同指的／不与任何占位同指的」；目标 4「每张逐一标定」→「**本任务**每张逐一标定」；目标 5、6 的动作用「本任务」补主语。
- **ch2**：无（首轮已清）。
- **ch3**：表3-2 ⑤「**占位节点例外**」→「**占位节点是例外**」（缺谓语）；表3-7 注补「**本任务**按表3-7 把要素补齐」。
- **ch4**：**表4-2 表题**「干枝由属性落地」→「**Y 的**干枝由属性落地」、**4.2.1 节题**「由属性生成干枝」→「由属性生成 **Y 的**干枝」（**ch5 表5-1 与 §5.2 的两处同名引用同步**）；§4.2.3 引题「归进哪一段、段与段的先后」→「归进哪个WBS业务、WBS业务之间的先后」；边界注「段内文档」→「该WBS业务内的文档」；序位注「上一段的产出是下一段的输入」→「**前一个WBS业务**的产出是**后一个WBS业务**的输入」；归出去注「归进哪一段判不出的」→「归进哪个WBS业务判不出的」。
- **ch5**：§5.2 三项段补三处施动者（新建该WBS业务／P 坐标由本任务标定／另作过时判定）；收敛段三处（调树、标 `[需裁决]`、按受理门槛退回）；§5.3 写回首句、§5.3 与 §5.5 两处失败处置；§5.4 的影响面复查句与两处缺口动作；§5.1「退回上游」句。

**扫描方法（可复用）**：无主句用两个正则捞——`的，[把标记生另按交更确逐再就地]` 与 `——[把标记生另按交更确判逐]`；缺定语则逐条把引子单独读，问「这是哪一级、哪个节点、哪份文档上的事」。**表身单元格仍按「判定｜处置」短式不动**（93 那几条正文写法不往表格里套）。

**19. 删净本目录的 PDF**（同日，人类「把 10-wfsysdev-4-eos 目录中的所有 PDF 文档给删除了」）——六个：`91-eos-biz-eng-spec.pdf`、`93-eos-skill-guide-spec.pdf`（含本轮重生成的那版）、`94-eos-sys-dev-dbs.pdf`、`eos-biz01abr-stfr2rbpl.pdf`、`eos-wft01-biz-spr2bpl.pdf`、`eos-wft02-biz-bpl2bpd.pdf`（共约 9.3MB；子目录内没有）。六份原本都被 git 跟踪、`.gitignore` 里也无 PDF 规则，故未提交前可用 `git restore` 取回。**PDF 是 md 的派生物**，md2pdf 随时可再生（约定见 `CLAUDE.md` §七）。

**20. §4.2.3 重构（含标题）＋ 变更影响另立 4.2.4**（人类「那还是需要你重构下4.2.3，包括标题」）——**病有两处**：表下**挂了六条注**（名、边界、序位、输入、归出去，外加一条本不属本步的变更影响），不按事分家；**标题**「按本级的节点意义定 WBS业务」里的「本级」在标题上落不下——单独抄出来不知道指哪一级。**落法**：① **标题**改「第三步 · **按节点意义归组 WBS业务**」——「归组」是 P 侧动作的本名（94 §2.2「在 P 树里另按构件**归组**」、91 通篇「WBS 归组」），「节点意义」由同一标题里的对象 WBS业务锚定；ch1 §1.3 目标 2、ch5 表5-1 与 §5.2 的三处同名引用同步。② **引题**判据句写全：「判据取 **WBS业务这一级的**节点意义」。③ **六条注并成三条**、一条一件事——「**WBS业务的名与说明怎么来**」（名取自组内共同特征＋说明即边界、超出时扩写）、「**WBS业务的两级序位与输入怎么排**」（段间先后＋段内序位＋输入要素：两级序位与输入本是同一件事的不同落点）、「**判不出与归出去的两种情形**」（归组判不出→`[需裁决]`；输入判不出→`[推断]`；已有文档被归出去→`[需裁决]`）。④ **变更影响与影响面复查另立 `#### 4.2.4`**——原先是 §4.2 末尾一段，排在 §4.2.3 之下、看着像第三条注 ✗；它是横跨三步的收束规则，归属在 §4.2 这一层，立成 4.2.4（标题不带「第 N 步」，因它不是转换的一步）；93 §3.4 点名的「影响面复查」由此有了可见的家。**表3-2 ④ 的「本级」保留**——该表有「级」列，「本级」在同一行里落得下，与标题那种没有任何上下文的情形不同。

**21. 第四章每步改「过程／规则」两块**（人类「第四章的标题是过程与规则，则每一步应该包括过程和规则两个部分」）——每个 4.2.x 下加两个 H5：**`##### 本步的过程`**（这一步按什么次序做什么、落在哪一级；含判据）与 **`##### 本步的规则`**（承它的表与表下注）。落法：**4.2.1** 的过程写全三级落位次序（业务域 → 产品级E2E业务 → 构件级E2E业务）；**4.2.2** 的过程只留「整段搬入 ＋ 由属性定挂在哪个构件级E2E业务下」，「判三档（并入已有的一位／新增一位／兑现占位）」归规则（表4-3）；**4.2.3** 的过程写「一次判完这一批：归哪个WBS业务 ＋ WBS业务之间谁先谁后」，判据句（取 WBS业务这一级的节点意义）与「WBS业务不来自属性」那句随行进过程。**顺带删掉 §4.2.1 末尾那条引用块「三处确认都落在 WBS业务上」**——它是编排的汇总（93 规则十一），且「人类从候选清单里选定」是 ch1 §1.2 与 ch5 §5.1 已说过的事实（规则十 一处一说）。**4.2.4 不设过程／规则**（它不是转换的一步）。

**22. 第四章各步提级 ＋ 包装节撤**（人类「把4.2.1等提级，你评估下」；第 29 版）——**评估结论：该提**。四条依据：① **93 §3.4 明写「一节一步，标题写「第 N 步 · 动作名」」**——提级前四步是 `#### 子节`，挂在「`### 4.2 转换过程`」这个包装节之下，「一节一步」落成了「一节一子节」；② **包装节的唯一内容违 93 规则十一**——§4.2 引题「一次转换依次走三步：…三级由属性值逐级落位，文档级E2E任务整段搬入，WBS业务由落位的结果整批归组而定」正是「汇总若干节合起来的样子」；③ **章内级深齐平**——ch4 原是全篇唯一用到 H4／H5 的章（ch1／ch3／ch5 都止于 `###`），提级后全篇同为「章 → 节」，最深只到步内的两块；④ **与第五章同形**——ch5 是「一节一个 Step」，ch4 提级后是「一节一步」。
>
> **落法**：① 删「`### 4.2 转换过程`」题与引题整块；② `#### 4.2.1~4.2.4` → `### 4.2 第一步…`／`### 4.3 第二步…`／`### 4.4 第三步…`／`### 4.5 变更影响与影响面复查`（4.5 不带「第 N 步」——不是转换的一步）；③ `##### 本步的过程`／`##### 本步的规则` → `####`（**「本步的」保留**——它是限定，不是 93 第十二条要打的「其／该／本级」那类顶替，去掉了反而落不下「哪一步」）；④ **引题里的两处判据出处按规则归位**：91 §5.3.1（引用／扩展／新建三档）落进 §4.2 的规则、94 §2.2.2 表2-6 的 WBS业务一行落进 §4.4 的规则（93 §3.4 的判据来源表原就指定这两家）；⑤ **§4.4 过程中的一句复述删**——「WBS业务不来自属性，它是构件级E2E业务中的一段业务，由该构件级E2E业务下的文档级E2E任务归组而成」是 ch3 表3-1 ④ 的原文，按 93 §3.4 禁含（第三章已给的判据本体只引名、不复述本体）只留判据句与出处。表号不动（表4-1~表4-4 仍按出现次序连号），ch5 引「表4-3」不受影响。
>
> **abr 那边不动**：abr 有同一个包装节，其引题「一次转换依次走四步。」按规则十一同病、也该随提级一并处理——但改 abr 要人类点头（[[feedback-abr-only-scope]]、[[eos-pilot-authority]]），**留作 abr 自身轮次的项**。

**23. §4.5「变更影响与影响面复查」整节删**（人类「4.5应该给删除了，你分析下」；第 30 版）——**分析结论：该删**，五条依据：

> ① **两句都不是第四章的内容**。第四章的定位是 X→Y 的映射与判定（93 §3.4「定位」）。4.5 只有两句：**变更影响声明的落账**（判类型、写进节点末尾）是一次**写回动作**——93 §3.4 禁含「一次运行的 Step 序列属第五章」，本链的尺子也是「写回归第五章」；**影响面复查**是一次**检验**——本链两条尺子（[ch2-structure-only-verify-in-ch5]「检验一律归第五章」、[skill-guide-ch4-ch5-boundary]「检查与处置归第五章」）都归第五章。两句里没有一句判哪一级、判引用／扩展／新建、判归属。
>
> ② **两句在第五章都有现成落点，删了不丢内容**。变更影响声明：ch5 §5.2 已写该动作（「另作已有文档的过时判定与变更影响声明」）、§5.6 自检已有一行；缺的只有 91 §A.7.2／§A.7.3 两处指针。影响面复查：ch5 §5.4 已写触发与范围（「本次改动了已有干枝的，另按影响面复查其下已归位的部分」），§5.5 的复跑与级联处置也已写。
>
> ③ **它的存在造成同一件事四处说且触发口径相抵**：「本次改动了已有枝的」就复查（§5.4）与「判为修订型或结构型才复查」（4.5）不是同一个条件——按 91 §A.7.2，**增量型**改动「已有下游不受影响」，本不该触发复查，§5.4 的旧口径**触发过宽**；另两处是 §5.3 的级联触发条件与 §5.5 的复跑。留 ch4 一处，93 规则十（一处一说）就永远清不掉。
>
> ④ **它是撤 §4.3「并入已有 Y」时挑剩的残件**（第 27 版撤两件、留这一件）。留它的唯一理由是 93 §3.4 的应含清单点过「影响面复查」这个名——而 93 §3.5 同时说「本任务自己的检验落到跑它的那一节」，两处并存正是 §3.4 与 §3.5 的边界没划清。
>
> ⑤ **93 §3.4 那条对 abp 本就不适用**。该条原文是「对 Y 枝梢做 Extend/Split/Merge 改进时，**对既有承接的复查规则**」——讲的是**承接关系**的复查。**abp 全篇无承接**（`承接` 只出现三次，两次在 94 抄来的级语义正文里、一次在 ch1 §1.4 说「不进承接链」；abp 的两树关系是**认亲**，10 处）。组装型任务不设承接，与同节第 2 条「组装型的…不是承接，整项不设」同理。abr 有承接（21 处），那条对 abr 适用——**故 abr 的同一段保留**。

> **落法**：① §4.5 整节删（第三章与第五章之间不再有第五小节，ch4 自此四节：4.1 对应 ＋ 4.2／4.3／4.4 三步）；② 91 的两处指针搬进 ch5 §5.2——「该构件级E2E业务在 Y 上已有节点的，本任务另作已有文档的过时判定与变更影响声明。**变更影响声明按 91 §A.7.2 判变更类型，按 §A.7.3 的格式写入节点末尾**」；③ ch5 §5.4 的触发改口径——「本次改动了已有干枝的」→「**本次改动判为修订型或结构型的**」（消掉与 91 §A.7.2 相抵的过宽触发），范围写明「被改动干枝之下已归位的部分」，处置留「失配的按整批重归位」；④ **「或回退」三字不搬**——abp 无 Y 侧退回路径（本侧无下游，第九轮起 abr 也删净了 Y 侧退回），X 侧缺陷走受理门槛的退回上游、不由复查触发，故「回退」在这边没有落点。
>
> **留给人类的一处**：删掉 4.5 后，**93 §3.4 应含清单第 5 条「影响面复查」在第四章没有落点**。建议的裁法是给那条补一句非适用/落点说明（照同节第 2 条「组装型…整项不设」的先例，改成「复查的触发与处置归第五章跑它的那一节；本条只对承接型与深化型适用」）。**它牵连 abr**——abr §4.3 里挂着同款一段（line 381），一旦定，两边一起照裁。

**留着的、要人类裁的**（本轮唯一实质缺口）：**§5.1 状态推进缺「上游变更分流」一条路径**。93 §3.5 要求主线／上游变更分流／下游退回三条并给；abp 的下游退回本就该缺（`03` 无下游），但上游变更分流有——且 §5.1 声明写入 `待补充srb设计`，而表5-3 只覆盖 `待确认方案`／`可以srb设计` 两种，**这个状态在 ch5 里没有写入场合**。要裁的是：`03` 的节点是 `02` 侧实体的同名视图、状态随实体（第 26 版记下的「留待核」），而 `03` 自己声明「`在srb设计`/`已srb设计` 由下游写入」却无下游——**这两句要先定**，ch5 才好照 abr 的三条 bullet 写（写「可以srb设计／在srb设计／已srb设计」三条，还是只写本任务受理的「可以srb设计／待补充srb设计」两条）。

## 本轮：第四章重构（2026-09-22，第 27 版，提交 `3fd4e107`）

**人类定**：「基于第二章和第三章调整与重构第四章」＋「第四章现有的内容，有一些超出了和第三章内容含义的边界」。判据＝93 §3.4 的应含与禁含 ＋ 现已定稿的第二、三章。

**立住的分界尺子**（两把合用）：**X 拿掉还成立，且不涉及「从 X 取什么」→ 第三章**。落到表上——第三章表3-2 ＝ 来历（承袭／本任务产出）＋构建规则（挂在谁之下）＋形态边界；第四章 ＝ 从 X 的哪个字段生成哪个要素（93 §3.4「类型与要素生成判定」）＋判据＋比对范围＋判定档位＋处置；两章对 94 的判据本体与 91 §5.3.1 的判据定义**都只引名**。

**越界双向收口**（A 组＝第四章删，B 组＝第三章删）

- A1 §4.2.2「名称不另起…本文件只带同名视图」→ 归第三章 §3.1 注（已有一份）
- A2 §4.2.2「占位有名与坐标、没有方案」「占位是一份具体文档的主张」→ 归第三章表3-7 注与 91 §5.3.1
- A3 §4.2.3 与表4-4 的三支归组 → 与第三章表3-2 ④ 的构建规则同一件事；规则留第四章，第三章只留归组事实
- A4 §4.2.4 首句（P 坐标由哪三项组成、各带档位）→ 第三章 §3.2 引题已立
- A5 §4.2.1 末句「按受理门槛退回」→ 归第五章表5-2 同名行
- B1 表3-2 ④ 后半段（三支归组＋时序＋以占位落树）→ 第四章表4-4 与第五章
- B2 表3-2 ⑤「与占位逐项语义匹配」→ 第四章表4-3
- B3 表3-2 ①「这一级的档位只有引用与新建两档」→ 第四章表4-2 ① 判定列

**九条已查实的 ch4 账逐条落**：① 表号重编（原 §4.2.2 挂表4-4／§4.2.3 挂表4-3）→ 现 表4-1…4-6 按出场次序；② §4.2.3 复述 94 表2-6 ④ 本体 → 改「判据取本级的节点意义」，表4-4 判定列同改；③ §4.2 引题出处 → 三档与一致性三类仍指 91 §5.3.1，WBS 归组另指 94；④「层级判定档位」→「判定档位」，三档名统一为**引用／扩展／新建**（表4-4 首行「并入已有WBS业务」→「引用已有WBS业务」）；⑤＝B2；⑥ 表4-1 的注 2、注 3 删（X ②③④／⑥⑦ 无对应，与表2-2 一处一说）、X 列括注「（本次的输入，带五个属性）」去掉；⑦「段」按上一轮已裁（保留「一段业务」、④ 的「定段」、判据名「段内输入闭合」）——只改「段内序位」→「WBS业务内序位」、§4.2.3 的「段的边界」删；⑧ §4.3 仍三项；⑨ 四个节题体例保住。

**四条自答的裁决点**（人类：「有些需要我裁决的事项，其实你再深入思考下就有答案了」）

1. **④ 的「序位」与「输入」**——序位**承袭** X 的文档序列、不脑补；「依赖什么数据」由本任务按各文档的说明判、判不出标 `[推断]`。依据：§5.1 明写 R 树的组装权含文档序列、X 的物理文件按岗位级E2E业务分片，**`03` 模板的「业务文档视图（逐文档）」写着「与 `02` 同名、同一次序」**。连带改第二章表2-2 的 ④ 行与 §5.2 读取输入。
2. **要素生成判定按步分置**，不另立一节（abr 先例：原四段规则全部并入六步）。
3. **P 坐标两处都写**（第三章表3-6 的认亲 ＋ 表3-7 的 P 坐标），§4.2.4 立「两处同源、一动同改」；连带改 §5.2 的写回句。
4. **表4-3 与表4-6 的分工**（abr 先例：表4-3 的「已有」支只给去向）＋「复用该文档」→「该同名视图不动」——**Y 不持实体**。

另两条同轮定：**§4.3 的「边界匹配检查」不再另立表**，降为表4-4 在并入模式下的读法（一句引名＋三个去向）；**「认亲」的定义落在第四章 §4.1**，第三章 §3.1 注里的旧定义随之删。

**顺带清掉**：§5.2「段的边界要看得见整批文档…逐张判正是误新建的来路」（安置的理由＋元评论）；§5.6 自检「边界取在依赖闭合处」（退役件同族词）；§1.3 目标2 的判据句；「直接引用」余一处（§1.3 目标1）。

**拟补未补**：§1.3 目标3「兑现占位」仍是以规则的口吻写的（93 §3.1 要求主要目标写结果）——留给下一轮或一并收。

## 本会话已做（2026-09-22；已提交 `ee84a61f`＋`3033b6e8`）

**abp 第二、三章 → 第 23 版**：章首认定句与 schema 出处句；**表2-2 补 ⑥⑦ 两行**并换列名（原只列 ②③④，与表4-1 记 `—` 的五级不合）；五属性注去与 §4.1 重复的消费句；表2-1 ③ 空格。§3.1 引题与注1 瘦身（认亲定义归第四章）；**删「平台侧的 P 树单分支」整条**（含白名单外的代号 `P0 树`；abr 已删同段）；表3-1 ② 错字「成为」→「称为」、⑤ 行去与引题／图重复的深度句；§3.2 引题**就地兑现「P 坐标」并引入「结构判定」**；表3-2 ②③ 位次改正（原「第二位／第三位」错一位）、④ 补第三项并把两档说全、⑤ 形态边界写全（原漏序位与内联稳定标签）；表3-6／3-7 措辞；占位注「按本表」→「表3-7」；移交承诺下长注四件事收成一句。

**abp 第一章 → 第 24 版**：主要目的末句去非术语「序列文档」；§1.2 补状态推进值 `可以srb设计`；目标1 改「由属性直接确定」（原「判定它落在这一级的哪个节点」指代落不下来）；目标2 补主语；目标5 补判定档位、删与「P 树无场景级」相抵的「可被多个场景引用」；§1.2 的 X 名改「相关方功能需求」。

**abp 第三章：删「移交承诺」整块（第 26 版）**——人类 2026-09-22 问「P 树不再向下传递使用，移交承诺还需要吗」。三条证据定案 `03` 无下游：① 94 §1.4「本链上的承接只有两处：`04→05`、`06→07`」，表1-6 把 `02`↔`03` 定为**同一对象的两面**（非承接）；② 91 §7.3 的 wft03-biz 输入是 **`bpd-biz` 节点**（04），不是本文件；③ 全仓检索：「产品视角业务流程概要」只被 93／94／abp／abr／cr／03 提到，05／06／07 与各 wft 指南零引用。**判据**＝93 §4.2 把移交承诺定义为**接口契约的 A 侧**（与 B 的受理前提互为对侧）——对侧不存在则无对象；四条内容三处各有落点（`可以srb设计`／要素齐备／覆盖结论→§5.3 表5-3＋§5.6 自检；实体归属→§3.1 注）。**连带修三处替不存在下游说话的句子**：§1.4 表下注「P 坐标…由那一对取用」→「就落在本文件里，随本文件交付，不进承接链」；表3-7 内联稳定标签「下一个需求-方案对逐级对上」→「与 X 侧文档级E2E任务对上——两树上同指一张文档」（§4.2.2 的实际用法）；§5.6 三区输出「下游 → …」→「无——本侧止于概要，交付物不进承接链」。表下那句「与职责视角业务流程任务的移交承诺**互为对侧**」也删——abp 与 abr 是**并列**，不是 93 §4.2 的相邻 A／B。**abr 的移交承诺保留**（它有两个真下游）。**顺带看到、未动**：`可以srb设计` 这一状态值是给下游认领用的，而 `03` 无下游——现文靠「节点是 `02` 侧实体的同名视图、状态随实体」自洽，要不要在 91 §A.6 层面另说，留待核。

**§1.3 主要目标：abp 十项 → 六项（第 25 版），abr 十项 → 八项（第 95 版）**——人类 2026-09-22 定「主要目标中的 5、6、7、8 是画蛇添足」。判据＝93 §3.1 的目标要与**后章各节的产出**对应：**无独立产出**的四条（并入时的三项复查、调树收敛、属性齐备保证，以及「标定 P 坐标」与前一条同指）整条去掉，其中 P 树一致性结论是 §4.2.4 的真产出，并进前一条「归位与 P 坐标」。删掉的职责各有落处、未丢信息：三项复查→§4.3、调树收敛→表3-2 表下的收敛自查＋§5.2、属性齐备→ch2 表2-1 后的五属性注＋§5.1 表5-2。abr 同尺子删的是 7（调树收敛）、8（三项复查）两条——它的 5、6（实现方式、承接记录）是真产出，不动。

**abr 第二、三章 → 第 93 版**：X 认定句与下一句合并 ＋ 补 schema 出处句；两条注改写（「亦或」别字、主语跳）；§3.1／§3.2／§3.3 三处引题；表3-2 ④；**移交承诺第 2 条「属性」→「要素」**（表列名与 §3.3 引题都作「要素」，内部相抵）。

**abr 第一章 → 第 94 版**：主要目的与 §1.2 的 X 名对齐新 ch2 认定句；补状态推进值；「相关方需求里给的就是这三级」→「相关方功能需求」。

**格式清扫**：链级更名留下的「中文＋空格＋节点名」共 **49 处**清零——abr 41／abp 4（两章内）＋ **94 4 处**；**94 表2-6 ② 错字「成为」→「称为」**（94 → v1.41）。**更正**：此前报告称「91／03／cr／8 份 wft 亦有」是未经核对的推说，实测只有 abr 有。`§一 定义` 那类空格是正规排版，不动。

**作废一条旧账**：abr 表3-2 ⑤⑥⑦ 的「承袭」**不是漏改**——`7d0aa845`（2026-09-20）有意由「本任务产出」翻回，理由写在提交信息里（93 §3.3 四来源、改后该列不再整列常量）。见 [[ch3-y-tree-tables-and-rules]] 的更正。

## 反向修订三份权威（2026-09-22，人类授权「反向修订 93、94、91」；提交 `ee84a61f`＋`3033b6e8`）

**94（树权威）→ v1.42，五处**——把 P 树收到 ⑤，与人类第 20 版的裁定对齐（该版提交信息自称「与 91 §5.3 的 Pn 树口径一致」，91 §5.3 明写 PL1~PL5、末级＝业务输出文档）：① §2.2.2 图2-5 删 ⑥⑦ 两行、表2-6 删 ⑥⑦ 两行，⑤ 行改「见 表2-3。在 P 树上是**叶节点**，即概要的末级」；② §1.4 表1-6「末三级同名同批」→「**文档级E2E任务同名同批**」；③ §1.4 的「`01 → 02` 与 `02 → 03` 两处…两侧的 ⑤⑥⑦ 都是同一批」拆开——前者 ⑤⑥⑦ 同批，后者同指的只有 ⑤；④ §2.1.1 注改「与 Rn 树的 ⑤⑥⑦、**Pn 树的 ⑤** 是同一批节点」；⑤ §2.2 引注「从 `01` 到 `02`…三级的节点整段搬入」「从 `02` 到 `03` **只搬入 ⑤ 这一级**」＋ §1.2 末句同步。

**93（编制规范）→ 第 79 版，四处**：① §2.3 那条「与上游同指的节点——**意义**只写『同 X 树的同名节点』」收窄为「**树图下的注只作指认**」，并补「**意义表不受此限**：同指的级照写全文」——消掉它与下一条「节点意义即 94 树里的节点意义」的自撞（本条原本只针对 §3.1 的注，落笔时扩错了格子）；② §3.3「移交承诺」补条件——「**本侧止于概要、链上无下游任务的不设**」；③ §4.1 判据归属表去「WBS 归组」，改注「WBS 归组的判据**不在此列**——取 WBS业务这一级的节点意义，家 94 §2.2.2 表2-6 ④」；④ **组装型的定义被 P 树收 ⑤ 逼着改**：原「X 的最深一级就是本案 Y 的最深一级」对 abp 已不成立（X 的叶＝⑦用例场景、Y 的叶＝⑤文档级E2E任务），改为「**本案 Y 上最深的若干级整段取自 X 上的同指节点**」；§3.2 的三类说明、§3.5 的组装型式子「X 的末级节点整段进入 Y、一个不少、同名同级」→「X 与 Y 同指的那几级…同名同批」、§3.3 枝梢双向、§4.2 的两处「末三级」同步。

**91（领域权威）→ v5.67，三处**：① §5.3.1 判据之家删「**PL4 WBS 归组**（数据依赖链 + 完成耦合硬判据）」，补「**WBS 归组的判据不在此列**——取的是 WBS业务这一级的节点意义，家 94 §2.2.2 表2-6 ④」；删掉后「判据族统一定义于此」不再 over-claim（剩下的对齐 Q1~Q3／一致性三类／缺口推断都有本体）；② §5.3.1 占位段「兑现的时候再按**数据依赖链**复核」→「再按 WBS业务这一级的**节点意义**复核」；③ §七 任务清单第 7 行「PL4 WBS 归组（**数据依赖链硬判据**）」→「（按该级的节点意义）」。**「数据依赖链／完成耦合」在正文里由此清零**（余下只在记忆的历史记录里）。

**PDF**：91／93／94／abr 四份按 `md2pdf` 重生成。

## 余项（仍开着）

1. **`03` 数据文件**旧级名模板与「数据依赖」同族词——按人类原话随 `03` 那一轮整备一起改（`03` 另有旧级名、还画着 ⑥⑦、树画像与要素行都与 abp 对不上）。
2. **「段」的收法**——`段内序位`／`段的边界`／`定段`／`段内输入闭合` 四处的去留（人类未裁）。
3. **「输入」一词两义**（任务侧的 X vs 某张文档的上游）——已核语境不混；要零重叠可改要素名为「上游／输入来源」。
4. **④ 的占位落点与 §5.4 的关系**——④ 只写「以占位落点」，落哪个 Step 未写；与 §5.4 的应有清单比对未收口。
5. **94 表2-6 ④ 的意义**要不要随改——它现在是 WBS 归组的**唯一活依据**，表述与退役件同源。
6. **abr 表3-2 ④ 的「边界四标准」**（完整性／一致性／紧凑性／粒度匹配）全仓只源自退役件 §3.2.2，R 侧还没找到活依据。
7. **`可以srb设计` 这一状态值**：它是给下游认领用的，而 `03` 无下游——现靠「节点是 `02` 侧实体的同名视图、状态随实体」隐式自洽，要不要在 91 §A.6 层面点明。

## 第 22 版：表3-2 逐行定案（人类逐条点头，非一次给）

| 级 | 定案 |
|---|---|
| ① | 去 R 树对照句（「R／P 两棵树各有一份节点，同值、不共享」——表3-2 只写本任务这一侧的动作；该事实在 §4.1 讲两树对应处已立）；构建规则＝**由属性直接确定**（名取「所属业务域」属性值原文、挂根下）；不设扩展一档 |
| ②③ | **由属性直接确定**——名取属性值原文、挂父级下；**归属不判、不另立聚类原则**（属性已给）；**删「判定锚点」整格**（其来源是待退役件，且属性已给归属）；删「属性给值」（②③ 行内与"名取属性值原文"重复） |
| ④ | **待文档级E2E任务落位之后才定**（人类定：WBS业务节点的实现，须在文档级E2E任务节点实现之后）；切段判据＝**本级的节点意义**（94 表2-6 ④：相互关联、耦合度较高、与最后一份同时完成）；归得进／归不进两支，归不进者出新建WBS业务方案 ＋ 列出该节点应增补的文档级E2E任务、以**占位**落树；「数据依赖链／完成耦合」两个外来名退出 |
| ⑤ | 与「所属构件级E2E业务」之下的**所有占位**逐项做语义匹配——同指的替换该占位，不同指的新建；**占位节点例外**（由本任务产出，非承袭）；写到结构判定为止 |
| 表下 | 收敛自查去末句（「新文档归不进现有WBS业务，才上溯建更高层」在 P 侧无活干、且与 ④ 相抵） |

**连带**：§4.2 第二／三步**对调**（生成文档级E2E任务节点 → 按节点意义定 WBS业务）；表4-3 表题与判定列改写；§4.2 引题、§5.2 四项、表5-1 Step 1 行同步；§4.3 边界匹配检查（3 处）与文档关系检测（2 处）改描述句；§5.4 与自检清单的判据名改「段内输入闭合」；§5.2 占位兑现检查的范围由「该WBS业务内」改**构件级**（与 91 §5.3.1／§5.2 对齐）。

**术语扫平 19 处**：甲族（要素名「数据依赖」→「输入」，10 处；对齐 91 §2.2 的"输入文档"）；乙族（判据名去名、只留句子，9 处）。**「数据依赖链／完成耦合／依赖链」三词在 abp 清零。**

## 本轮被否掉的四路（免得重复提）

1. **把待退役件当依据**——`eos-wft01-biz-spr2bpl.md` §3.3.3 是 21 版本体的唯一活出处，人类定「退役的文档怎么能够作为依据」→ 不能。
2. **「设计意图」**——出自 95 报告 §3.6；人类定「95 报告只是我学习用的」→ 不作项目依据，该词当场撤回。
3. **「段」代称 WBS业务节点**——不合适（自造同义词）；只保留「一段业务」（94 原话）、④ 的"定段"与判据名"段内输入闭合"。
4. **「数据依赖链」当切段判据**——它是**关系**（沿整条链处处成立，切不出界；退役件自己也还得再加"按业务环节切段"），两条并用犯 94 §1.2 表1-3 **条件一（判据单维）**。归位：**关系是证据，节点的意义是判据，性质是检验**。

## 95 号定位（2026-09-22 人类定）

**只是学习材料，不作项目依据**；已落进 94／20 号的结论才有效力。两份（md ＋ pdf）已迁 `100-subprojects/00-topic-research/`。详见 [[95-product-data-itemization-tree-allocation-report]]。

## 待裁 / 挂账（七条）

0. **§5.1 状态推进的上游变更分流路径**——**ch5 侧已于第 34 版落定**（写入分工 ＋ 上游变更分流 bullet，散文体，不画图）；**余 `03` 侧未改**：`03` §1.1 的状态取值表仍把 `需biz01修订` 注成"下游检出缺陷退回"、把 `在srb设计`／`已srb设计` 注成"由下游写入"，而本侧无下游。**这一半归 S10 数据文件一轮**，与 `03` 的级名、结构一起办。

1. **94 表2-6 ④ 的意义**——现在是 P 侧 ④ 与 WBS 归组的唯一活依据；其表述与退役件同源，要不要随改。
2. **91 §5.3.1** 那句「判据族统一定义于此」——节内只有缺口推断与占位有本体，三档条件、WBS 归组、一致性三类条件都没有；abp 已不再引「WBS 归组」那一支，这句的收法待定。
3. **abr 同病**——其表3-2 ④ 的「边界四标准（完整性／一致性／紧凑性／粒度匹配）」全仓只源自退役件 §3.2.2，无活依据；P 侧靠 94 表2-6 ④ 找到了活依据，R 侧还没找到。**abr 的第四章另有一处待过**：表4-2 ④ 与表4-4 边界匹配检查的重复面比 abp 轻，但也未按本轮的尺子重量一遍。
4. **`03` 数据文件**——6 处同族词（模板「数据依赖：{前序文档 · 依赖什么数据}」、要素行「本包的…按数据依赖链排定」等）；另有旧级名、还画着 ⑥⑦、树画像与要素行都与 abp 对不上。**本轮的「序位承袭 X 的文档序列」正是依它的「业务文档视图（逐文档）：与 `02` 同名、同一次序」定的**，改它时要一并保住这条。
5. **「输入」一词两义**（任务侧的 X vs 某张文档的上游）——已核语境不混；要零重叠可改要素名为「上游／输入来源」。
6. **④ 的占位落点与 §5.4 的关系**——④ 现只写「另出新建WBS业务方案、列出该级应增补的文档级E2E任务」，落在哪个 Step 未写（按现文是 Step 4 裁决之后）；与 §5.4 应有清单比对（那里锚在构件级E2E业务，本条锚在WBS业务）未收口。

**已关**：①「段」的收法（第 27 版按上一轮裁定收口：保留「一段业务」「定段」「段内输入闭合」，改「段内序位」与「段的边界」）；②§3.1 平台侧 P 树单分支注（第 23~26 版已删）。

## 仍搁置（更早遗留）

- 表4-3 ⑤ 行与其下立规句仍在「文档／文档级E2E任务」间来回（P 侧只剩这一级带文档，代价已低）。
- 「说明」的落点无家（`03` 节点模板无说明字段）；`03` §4.1 模板与 abp 表3-6／3-7 的要素行不对应。
- ⑥⑦ 是否也收成「复用／新增」两档（见 [[biz01abp-business-document-reuse-or-add]]）。
- 「产品视角」更名外围：91 三处、8 份 wft 指南仍叫「输出产品视角」。
- 意义的一次源方向（见 [[pending-meaning-authority-direction]]）。
- 上轮三处「我判了、等点头」仍按现状：承接记录整项不设、认亲本体唯一与归属冲突合一、Step 3 名「检查覆盖并补充需求」。

关联 [[pending-biz01abp-rewrite]]、[[biz01abp-business-document-reuse-or-add]]、[[95-product-data-itemization-tree-allocation-report]]、[[eos-pilot-authority]]、[[progress-93-skill-guide]]、[[feedback-work-style]]、[[pending-meaning-authority-direction]]。

## 第 37 版：§5.1「选定本次处理对象」去啰嗦（2026-09-23）

**人类 2026-09-23 定**：「这一段有点啰嗦」（引原文）。**病两处**：① **同一个判据说了两遍**——「尚未在 P 树上**标定 P 坐标**」与「没有 **P 树归位记录**的即属本批」是一件事（后者是前者的就地判据），并列着写像两条要求；② **三处破折号夹注**把四件事串成一句（批次定义、上限、判据、清单），读着是一团。**落法**：判据只留一次（「尚未在 P 树上归位」＋「判据落在 X 侧：没有 P 树归位记录的即属本批」）；破折号改句号，分成两段（批次／清单）；顺带删「每项文档级E2E任务是该节点文档序列里的一个条目」（该事实在第二章表2-2 ④ 与 §5.2 的读取句里都有）。**教训**：上一轮我把「就地判据」那半句**追加上去**时，没回头看它与前一分句重复——**追加句子前先读一遍它要接的那句**。

## 第 38 版：全篇扫「啰嗦段落」（2026-09-23）

**人类 2026-09-23 定**：「你仔细看看本文档中还有没有类似的段落」（承接第 37 版那次去啰嗦）。**扫法**（可复用）：用 node 逐行量两个量——**正文段的字数**（去空白）与**破折号个数**，正文非表非标题行、字数 ≥150 或破折号 ≥2 的挑出来读；引题用的单破折号（`**引题**——`）与两条独立引题同行（`**产出去向**——…。**人类参与点在本节**——…`）不算病。**判据**是第 37 版那两条：①同一判据/事实说两遍 ②破折号夹注把几件事串成一句（含 `——…——` 这种双破折号夹注）。

**abp 扫出同病三段、已修**：

1. **§1.1 主要目的**——`覆盖检查——齐不齐、接不接得上——缺什么就补什么` 是双破折号夹注；改「：…齐不齐、…接不接得上，缺什么就补什么」。
2. **§1.2「这棵树最下的一级」段**——`——文档级E2E任务——` 双破折号夹注，改成「也就是文档级E2E任务」；同段「不做**定义**——页面层级的写实…」是应作冒号的两句，改冒号。
3. **§5.4「应有清单比对」段**——**318 字一坨** ＋ 三处破折号；拆成三段（应有清单比对／输入闭合／缺口成候选），破折号全改冒号或句号。

**扫出但判为无病**（不改）：§1.3 目标5（157 字、三处「；」——是四项并列的目标陈述，属列表体例）；§1.4 的自检「生成」项（224 字、五处「；」——自检清单的分组项，同为列表体例）；「增量标记」那句（231 字——是标记定义句，缩写与全称并列没法再压）。

**abr 同病两段（报出未动）**：第一章 §1.2 的「两个任务跑的是同一批…」（120 字、两处夹注结构）与「这棵树最下的三级…」（228 字、**两处双破折号夹注**，是全篇最重的一处）。abp 的对应两段本轮已清，abr 的留着——待人类点头。

## 第 39 版：删「存量节点」——那是 abr 的词，abp 无下游故不成立（2026-09-23）

**人类 2026-09-23 两问逼出来的**：先问「这里的存量节点是什么，有没有，你好好分析下」（指 §5.2 首句），我答「上半有（`待确认方案` 的遗留方案）、下半无（`需biz01修订` 的退回节点）」；人类随即纠正——「**好像没有存量节点啊，每次新进的文档级任务都被处理了，最多是没有被人类确认**」。**这一纠正对，且比我的分析深一层。**

**词源**：「存量节点」是 **abr 第九轮**（删净 Y 侧退回）把「捡起未确认的方案」泛化来的，为的是让 `需biz01修订` 的退回节点仍由本任务认领而**不点「退回」二字**。所以它在 abr 的实指是**两类**：`待确认方案` 的遗留方案 ＋ `需biz01修订` 的退回节点。**abr 有下游（cr 与 abp），下半真实存在，且那类节点确实要重新处理（修订）**——故 abr 的「先于新输入处理」站得住。

**abp 错在哪**：abp **无下游**，`需biz01修订` 只作为**本任务往 X 侧写的退回标记**出现（表5-2 四行），从不落在 Y 的节点上——下半是空的。**更要害的是：连上半也不是「待处理」**。上一轮未确认的方案**已经被处理过了**（Step 1 生成过、Step 2 讨论过），差的只是**人类确认**这一下——所以它是**已处理、未确认**，归 **Step 2 的未确认清单**（§5.3 本来就有：「本步要讨论确认两样东西：Step 1 刚生成的方案，以及状态停在 `待确认方案` 的遗留节点」），**不是 Step 1 的处理对象**。**Step 1 的处理对象只有一个**：本批新汇入的文档级E2E任务。**所以「存量节点」这一支不只是词含糊，而是把一件事归错了步** ✅。

**落法五处**：① **§5.2 首句**——「有新输入汇入、或者入口检出待本任务处理的存量节点时，本步就走；两者都没有的时候，本步不走。存量节点先于新输入处理。」→「有新输入汇入时，本步就走；没有的时候，本步不走。」（Step 1 只收新输入，删掉错收的第二支与自编的先后次序）；② **§5.1 定义段**——「**待处理的存量节点**——…由入口检出，与本次选定的文档级E2E任务一并作为处理对象。**其中**状态为 `待确认方案` 的…」→「**上一轮未确认的遗留方案**——入口还检出状态停在 `待确认方案` 的方案节点…**它们不再作为处理对象**，只在 Step 2 与本轮新生成的方案一并交人类讨论确认。」（循环定义 ＋ 空预设的「其中」 ＋ 错标处理对象，三病一起收）；③ **表5-1 Start 行**「检出存量节点」→「检出上一轮未确认的方案」；④ **表5-2 末行**「无待处理的文档级E2E任务 ＋ 无入口检出的存量节点」→「…＋ 无上一轮未确认的方案」（**它会挡住一次只该走 Step 2 的运行**——这是那支归错步的代价）；⑤ **§5.6 自检**「入口检出的存量节点已并作本次处理对象」→「入口检出的上一轮未确认方案已并入 Step 2 的未确认清单」。**另去**「由入口检出」（93 §3.5 禁含：入口的检测与优先序由 91 附录A 单源规定）与 §5.6 载入项里「未确认的存量方案节点」的旧措辞。

**abr 不动**（那半边有实指）。**留下的连带**：§5.4 的触发写「Step 1 走完之后本步就执行」——若一次运行只走 Step 2（无新输入、只确认遗留），覆盖检查还跑不跑、跑的范围是什么，与新的 Step 1 触发拼起来看是空集，待定。

## 第 40 版：删两试点的「另外两类缺口不在本任务」注（2026-09-23）

**人类 2026-09-23 问**：「另外两类缺口不在本任务…这两段是有有必要保留」→ 裁「**删**」。**两试点同删**（abp 第 40 版删「文档序列类＋引擎类」那两条、abr 第 101 版删镜像的「产品数据类＋引擎类」那两条）。

**它是什么**：一张**不负责清单**——列出本任务不做的另两类缺口、它们锚在哪、归谁。**三条 93 判据同指一删**：

1. **93 §3.1**：「**不设"负责／不负责"节**——…不负责是负责的补集，**不可穷举**…本任务不做的事**各归其位，散布在它实际发生的地方**」——那两条理由逐字适用（只列两类，凭什么就这两类）。
2. **93 §4.1 的「缺口三族各归一处」注**：已指定另两族「**就地成文于（其归属任务的）第五章的关口**」——**那两族的家不在 abp**，abp 提它们＝替别人说半句（只报"别处有这件事"，不给判据本体）。
3. **93 §2.4 规则一**：「**分工声明不算引用**…判据：**删掉那句分工声明，本节是否仍完整可读——仍可读，就该删**」——删掉，§5.4 照跑。

**删了不丢东西**：93 §3.5 要求关口写的是「**哪一类缺口归本任务**、锚在哪一级、候选去留谁裁、走什么回路」——是**正面陈述**，abp §5.4 正面那半本就齐（「检出缺口即缺了应有的**产品数据**」——**判据自带边界**；「记 `[产品数据缺口]`，生成产品数据类补充材料候选，去留交 Step 4 裁决」）。**安全性靠判据句里的"产品数据"四个字，不靠那条注**。

**同类还开着的**（本轮报出未动）：abp §5.1 的「**R 树的组装权在职责视角业务流程任务的手里：…本任务不重复长 R 树的干枝**」与 abr §5.1 的镜像句——同为分工声明，但它是**表5-2 那些"退回上游"行的理由**（X 的干枝有缺陷时本任务不能自己修，只能退），故疑似载重；两者去留待裁。
