---
name: pending-biz01abp-rewrite
description: 挂账：新开对话的重写任务——以 biz01abr 为参考依据、按五章框架重写 biz01abp（X 的粒度＝R 树上的业务文档，Y＝产品开发视角业务流程概要，P 树侧判定归 abp）；含现场事实、该随与不该随的边界、已裁四项
metadata:
  type: project
---

**人类 2026-09-21 定**（开新对话前）：「我们新开一个对话，以 biz01abr 为参考依据，重写 biz01bp，即输入 X 为职责视角业务流程概要中业务文档，输出 Y 为产品视角业务流程概要。」

## 现场事实（写前先核，勿凭记忆）

| 项 | 值 |
|----|----|
| 目标文件 | `20-pl4eos/10-pl4eos-subpl-sysdev/10-wfsysdev-4-eos/eos-biz01abp-rbpl2pbpl.md` |
| 目标现状 | **第 11 版、七个主章**（一目的／二输入／三输出／四转换／五执行／六关联产品数据及规则（接口）／七收尾）；SKILL 指南ID `biz01abp-rbpl2pbpl` |
| 参考文件 | `eos-biz01abr-stfr2rbpl.md`，**第 89 版、五章**（七章的第六章与第七章已并入第五章） |
| 规范 | 93 第 75 版（§2.4 已补到**十一条**）、94、91 |
| 已提交 | `61a69018`（93 v75 ＋ abr v89）、`28ff50ae`（abr 再改）、`0ded0e48`（记忆）；github 与 gitee 两仓已推 |

## 权威：P 树在 94 §2.2

- **P 树干枝由同一批属性落地**：业务文档的五个坐标 → P 树 ① 业务域／② **产品类型**／③ **产品构件类型**（②③ 是父子，构件是类型的子节点，两级都要给且挂同一类型之下）。
- **⑤ ⑥ ⑦ 与 R 树同名同批**：⑤ 业务文档、⑥ 任务节点（＝枝梢）、⑦ 用例场景（＝叶）。**从 `02` 到 `03` 不做映射、不做分配**，三级整段搬入；本任务的活是**长干枝 ＋ 逐级判定三级的落位**。
- **末三级实体由 `02`（职责视角业务流程概要）持有，`03` 带同名视图**；两树经**认亲**互指，认亲＝逐文档标定 P 坐标。
- **文档序列不占树位**：同一批文档在 P 树里另按构件归组，序记在 **④ 构件开发WBS（包）**。
- R↔P 对应同见 94 §1.4（同一批业务文档的两面：R 树问"该角色按什么序处理它"，P 树问"它属哪个构件、怎么生成"）。

## 该随 abr 的（形与写法）

五章骨架；章首引题（**只写一次运行的起止**，不汇总"哪几步并非每轮都走"）；**表5-1 Step 布局总览**及其列；表号体例（`表N-M` 带题）；Step 名体例（动作领起、以对象收尾）；**写回随两次对话确认就地落地、Step End 只汇总**；93 §2.4 **十一条**全部，尤以第十一条「不写编排的说明」与本轮立的判据。

## 不该照抄 abr 的（内容须按 P 树重推）

三、四章（P 树的级语义、判定动作、干枝落位表全不同）；受理门槛的具体条件（对 X 的性质不同——abr 的 X 是相关方需求条目，本任务的是 R 树节点）；缺口类别与归属；承接记录（若 P 树侧无对应需求来源，此项可能整项不设——**须先判**）。

**认亲与 P 树侧判定归本任务**，不另立他处。

## 已裁四项（人类 2026-09-21 裁，先问后写已办）

1. **名字＝`biz01abp`**（"名字已久"）——人类先前写作 `biz01bp` 系简写，**不更名**，SKILL 指南ID 仍 `biz01abp-rbpl2pbpl`。
2. **X 的粒度＝R 树上的业务文档**——输入单位是业务文档这一级（⑥ 任务节点、⑦ 用例场景随带，照 94「三级整段搬入」）。**按此推第五章步序**：与 abr 的单件输入同构，一次运行处理一张业务文档。
3. **按五章框架重写**——骨架随 abr 的五章；章内容按 P 树重推，不是把七章原文搬进新壳。
4. **P 树侧判定在 abp 中**——认亲与 P 坐标系判定都落在本任务，不外推。

关联 [[skill-guide-rule-no-layout-explanation]]、[[term-chuanchu-chengnuo-renamed-shouyi]]、[[feedback-93-principles-over-terms]]、[[pending-abr-cleanup-open]]、[[pending-biz01abr-ch5]]、[[eos-pilot-authority]]、[[biz-chain-renumbering-2026-09-16]]。
