---
name: wft01-biz-section31-discussion
description: 2026-06-29 §3.1 STR-F 架构说明讨论——议题1~3已落账，议题4无需操作，STR-F节点完整要素结构待讨论
metadata:
  type: project
---

## 本轮已完成（2026-06-29）

### 议题1：架构图平级化执行力
- 图：文档业务指标从树形缩进 `│   └──` → 平级 `├──`，通过 `（度量对象 → 场景部件 X）` 注释表达度量关系
- 文 L224：「嵌套…不在 L5 平级排列」→「平级排列，通过`度量对象`字段引用」
- 消除了 v1.54 日志说平级化但图/文仍嵌套的不一致

### 议题2：L5 层级定义歧义
- 废弃 `场景部件 : 输出文档 = 1 : 1` 等式
- 改为自然语言：「每个场景部件对应一个业务输出文档或一个业务管理指标」（后又改为「对应一个业务产出物」）

### 议题3：引入「业务产出物」伞概念
- 伞概念：**业务产出物** = 业务输出文档 + 文档业务指标 + 场景业务指标
- 全文约 20 处广义「输出文档」→「业务产出物」
- 狭义「业务输出文档」保持不变
- 涉及：§一概述/职责/目的、§3.1定义/图/要素、§3.2提取规则/引擎判定表/标注、§3.4关系检测锚点/表行、§3.5分流规则/要素提取、§四覆盖自查/增量标记表

### 议题4：场景引擎职责边界声明
- 确认无需恢复 v1.51 被砍掉的职责边界声明
- §3.1「场景引擎无需判定」+ §3.2 两步法仅覆盖部件级 + Phase B/C 操作指令 已自足

### 额外：两段独立段落→字段填写指引
- 删除「平台治理类角色」独立段落 → 核心认知吸收进 `治理对象类型` 字段说明（治理角色也产业务输出文档，走同一套流程，多为内置CAX）
- 删除「菜单引擎与 wft01-biz 边界」独立段落 → 职责边界吸收进 `菜单可见角色` 字段说明（wft01仅传递，菜单设计由wft02 A2完成）

## 待讨论（下一轮对话主题）

### STR-F 节点完整要素（§3.1 L255-263）
当前7行平列，性质不同却被压平为列表：
- 节点前导字段（定义/业务域/产品类型/用户角色/视角类型/功能说明）——来源不同、用途不同，无法归入统一概念组
- 条件属性（治理对象类型/菜单可见角色）
- 场景部件清单（节点主体内容）
- 过程留痕（推理依据/覆盖自查/承接OR末级）
- 下游占位（符合性分析/设计指导）
- 版本管理（STR-F版本号/变更影响声明）

讨论中尝试过分组方案但"节点身份"等标签被否定——这些字段不构成概念组，只是都写在节点头上。用户认为这一节不易理解，需在新对话中继续讨论其结构和表述方式。

### §一·职责定位 未动的剩余项
- 搬迁/恢复操作完成后 §一 恢复原状，未做调整

**Why:** 2026-06-29 从 §3.1 四个议题出发，顺藤摸瓜触及了要素模型、术语体系、段落性质分类等深层问题。议题1~3+额外修改已落账，STR-F完整要素的结构问题需要新对话继续。

**How to apply:** 新对话中以本轮修改后的文件为基线，从「STR-F 节点完整要素」一节开始继续讨论。
