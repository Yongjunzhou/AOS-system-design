---
name: 94-eos-sys-dev-dbs-tree-authority
description: 94 号文档（EOS 系统设计产品数据分解结构）为全链树形/级语义唯一权威；v1.23 定形——Rn／Pn 末三级为「业务文档 → 任务节点（枝梢）→ 用例场景（叶）」，E2E职责为 ④，业务文档 ⑤ 带三坐标；业务流程这一对两形态合并在一份文档（04 含概要、02 退役）；含四棵树的形状、已定口径与仍开着的分叉
metadata:
  node_type: memory
  type: project
  originSessionId: c697d226-794d-420d-b7f2-342928ba7e80
  modified: 2026-09-16T03:0:00.000Z
---

**94-eos-sys-dev-dbs.md = 全链树形/级语义唯一权威**（人类定 2026-09-09；文件在 `20-pl4eos/10-pl4eos-subpl-sysdev/10-wfsysdev-4-eos/`；旧稿 `94-产品数据树结构.md` 已退役）。其余文档（数据文件「树画像」区、23、SKILL 指南 ch2/ch3 的任务视图、91 的树描述）朝 94 对齐。

**最新状态：v1.23（2026-09-16，提交 `0df7056d`，已推 origin 与 gitee）**，与 93 第 43 版、biz01 第 29 版同一批提交。前一版 v1.22 的树形已作废。

## 〇、v1.23 定形的形状（2026-09-16）

```
Rn：角色履职业务 → 业务域① → 相关方角色② → 角色职责③ → E2E职责④
        → 业务文档⑤（业务输出的产品数据，对应本文档开发任务）
            → 任务节点⑥（文档开发任务的多个步骤，**枝梢**）
                → 用例场景⑦（**叶**）

Pn：输出产品业务 → 业务域① → 产品类型② → 产品构件类型③ → 构件开发WBS（包）④
        → 业务文档⑤ → 任务节点⑥（枝梢）→ 用例场景⑦（叶）

R0：角色履职业务 → 业务域①（流程与IT，单域） → 相关方角色② → E2E职责③
        → 业务文档④ → 任务节点⑤ → 用例场景⑥          〔相对 Rn 少「角色职责」一级〕

P0：输出产品业务 → 业务域① → 产品类型② → 构件③（引擎） → 配置单元④（CU） → 配置信息组⑤（末级，无叶）
```

**§2.1.1 相关方功能需求树**（X 树，v1.19 重画、v1.23 改名）＝ **① 业务文档**（根；属性＝所属主责角色 × 所属产品构件 × 所属E2E职责）→ **② 任务节点** → **③ 用例场景**（叶，`stfr-XXX`）。**X 的 ①②③ 就是 R／P 树的 ⑤⑥⑦**——同名同批、整段搬入，01 → 业务流程**不做映射、不做分配、不做承接**（叶两侧同一批）。

### 名字（2026-09-16 人类逐条定，两侧同名）

| 旧 | 新 |
|---|---|
| 场景业务 | **E2E职责**（④；R0 里是 ③） |
| 业务输出文档 | **业务文档** |
| 活动节点 | **任务节点**（与 X 的 ② 同名，此级两侧合一） |
| 系统用例场景级操作／用例场景类操作 | **用例场景**（X ③ 与 Y ⑦ 同名） |
| 所属E2E场景业务（属性） | **所属E2E职责** |

### 「业务流程」这一对是特例

**两个形态合并在一份文档里**——04 业务流程方案**含**概要，**02 业务流程概要退役**（`02-eos-business-summary-architecture.md`，它上面住着治理预置节点 bpd-biz-GOV-*，迁往何处未定）。理由：这一步的叶由相关方需求直接给出，没有待落之叶。其余各对仍分两个任务。
- 94 §1.1 例外注由两处改三处；§1.1 清单表删 02 行。
- §1.3 枝梢＝**概要的最深一级**（对本案即 ⑥）、叶＝完整态的末级（⑦）——「枝梢是叶的上一级」在本案成立。
- §1.4 承接表**两行**（04→05 落系统功能需求树枝梢 ⑤ 用例场景功能／06→07 落点【待裁】）；「01 → 业务流程 也不是承接」单列一条；两形态举例只剩（05↔06）。
- §1.2 依据三收窄：真正用到 N:1 分配的只有 04→05 与 06→07。

### 资产

**产品数据即资产；R 树与 P 树就是资产**（2026-09-16 人类三句：产品数据即资产／2x 编号的资产没有啦／R 树和 P 树就是资产）。所以 **21 相关方角色资产、23 输出产品架构、25 引擎资产都不存在了**——它们的内容本来就是两棵树里的层。任务只读 X、只写 Y；「与 23 对齐三档」改为「在 P 树里三档判定（引用／扩展／新建）」。

## 一、其他已定口径（仍有效）

- **§2.0 加工链**：五态（原始／文本化／切分／澄清／规范化），只有切分与规范化动结构；「业务对象闭环」＝链上切分单位＝R 树的 `E2E职责` 级＝01 树 ① 上的属性值。
- **§2.1 相关方需求**：不分形态（一产出即完整态）；§2.1.2 非功能树＝一级类目→分类维度→具体需求。
- **§2.3**：根名 `SR-F 系统功能需求树`／`SR-N 系统非功能需求树`；§2.3.1 ③ 按**系统职责**聚类。
- **§2.4～§2.6 未动**（平台构成＝企业信息系统→信息系统→模型→前后端组件；配置方案与前后端组件方案【待定义】）。※曾自行改 §2.4 并删 §2.5/§2.6，**被人类退回**，勿再自行改。
- **94 全文已去 PL／SL 层码**（v1.18）；变更记录整节删除（版本号在文档头）。
- **ID 前缀** `stfr`＝相关方功能需求（前身 `spr`／`OR`）。

## 二、仍开着的

1. **02 退役的实体活**——02 那份文件与它上面的 9 个治理预置节点 bpd-biz-GOV-001~009 搬去 04？（本轮只改了 94／93／biz01 的口径，文件未动。）**biz02（`bph2bpd`）也随之没有对象**，人类定「先暂时保留」。
2. **04 数据文件模板**——`04-eos-business-detailed.md` §4.1 里「来源 业务概要（02）」这一行、⑤ 被称枝梢、旧 PL6-A/B 叶定义，都待改。
3. **旧 PL6-B 那批条目**（指标／看板／风险／问题）在新形状里没有树位——是文档正文还是别的，未定。
4. **`spr-` → `stfr-` 链级更名**——94 内已换，链上仍 `spr-`：112 处／22 文件（01 数据文件 30 处含 `spr-SRC-*`、ort03 正文 16 + SKILL 4、91 共 4 等）。生产者是 ort03。
5. **`stfr` 的 F 覆盖不了 01 的全部条目**——同一 ID 序列挂着 FR-BIZ／NFR／FR-ENG 三类，现靠 `类型` 字段路由；分族未决。
6. **01 树新增的 ① ② 由谁写出**——归给 ort03，但 01 现行文件以角色分片直接挂条目、无这两级；**ort03（正文 + SKILL）未随**。
7. **「页面视角」在 94 无家**——「04 的输出文档 ↔ 功能表单」既非承接、也不在树间对应表（该表限定不涉视角转换）；功能表单已被 §2.5 指为"旧平台对象、产物归属在配置方案收口"，而 §2.5 仍【待定义】。
8. **§2.4 平台构成**的方向（"模型"层无据；③ 两类还是三类；§2.5／§2.6 是否独立产品数据）。
9. **§2.3.1 未收「功能表单不属于 SR 树」判据**（05 §0.2 与 91 §7.3 有）；06 仍写「srh-biz 末级＝功能表单」。
10. **`SR-N` 分叉**——94 用 SR-N，通用规范全篇 `SR-NFR`（且 `SR-NFR-001` 是节点 ID 前缀）。
11. **PL／SL 层码链级未清**——94 已清；91 §3.1 两张层级表、wft 链、试点、数据文件仍用。人类 2026-09-15 定「91 要对齐 94，但**不是现在**」，见 [[pending-91-align-to-94]]。
12. **95 报告**的"落点"注仍逐字引 §1.2 旧标题。

> **改动 94 的纪律**（2026-09-10 人类："你越界了"）：94 的**树形／口径／结构／分类**改动**只出建议、不落笔**；落笔须人类针对该条的明确授权。见 [[feedback-work-style]]。

关联 [[feedback-work-style]]、[[eos-design-landing-spec-93-refactor]]、[[page-view-vs-classification-view-correspondence]]、[[tree-level-vs-expansion-item]]、[[business-def-vs-engine-ipo-product-split]]、[[pending-91-align-to-94]]。
