---
name: 94-eos-sys-dev-dbs-tree-authority
description: 94 号文档（EOS 系统设计产品数据分解结构）为全链树形/级语义唯一权威；v1.22 业务树重排为 ⑤ 业务输出文档（枝梢；本级即文档开发任务）→ ⑥ 活动节点 → ⑦ 系统用例场景级操作（叶），正文条目去掉，枝梢定义改「概要的最深一级」；v1.19 重画 §2.1.1、前缀 stfr；含树形状、已解决项与仍在的分叉
metadata: 
  node_type: memory
  type: project
  originSessionId: c697d226-794d-420d-b7f2-342928ba7e80
  modified: 2026-09-16T02:03:41.678Z
---

**94-eos-sys-dev-dbs.md = 全链树形/级语义唯一权威**（人类定 2026-09-09；文件在 `20-pl4eos/10-pl4eos-subpl-sysdev/10-wfsysdev-4-eos/`；旧稿 `94-产品数据树结构.md` 已退役）。其余文档（数据文件「树画像」区、23、wft 人类方案 ch2/ch3 任务视图、91 的树描述）朝 94 对齐。

**最新状态：v1.22（2026-09-16，提交 `86bc8b2`，已推 origin 与 gitee）**；同日中间态 v1.20／v1.21；v1.19（2026-09-15 改 §2.1.1 树形 + 节内 ID 前缀改 `stfr`）；v1.18 提交于 `9b97e39`（2026-09-10）。

## 〇、v1.22 变动（2026-09-16，本会话）

人类授权「94 号文档也需要同步更改」，业务树（Rn／Pn／R0）末三级一次重排落笔。**定稿形状**：

```
场景业务 ④（枝干）
└── 业务输出文档 ⑤（枝梢；本级即文档开发任务）
    └── 活动节点 ⑥
        └── 系统用例场景级操作 ⑦（叶）
```

1. **⑤ 名保留「业务输出文档」**（人类：「业务输出文档没有问题啊」「⑤是文档，但更是文档开发任务」）——**节点名取物件，这一层的角色名取它在树里的身份**。同一物件在三条树上同名（X① 业务输出文档、P⑤ 业务输出文档）。意义写「待开发的那份；本级即文档开发任务」。**§2.1.1 的「① 与 ⑤ 同指」注重写**为「① 到方案侧成为 ⑤ 的文档开发任务」。
2. **⑥ 只留「活动节点」**——原与活动节点并列的**「正文条目」去掉**（人类：「首先，正文条目给去掉」）。文档正文不占树位，由活动节点承载。
3. **⑦ 新增为叶**：人类先写「用例场景类操作」，后定为「**统一成：系统用例场景级操作**」——与 X 树 ③ **同名**。⑦ 挂在 ⑥ 活动节点之下。
4. **§1.3 枝梢定义改口**：「叶的**上一级**节点——概要的最深节点」→「**概要的最深一级**节点」——加了 ⑦ 之后「上一级」不再成立；原定义两个维度（相对位置／形态归属）本靠巧合重合。
5. **撤「「任务」不占树位」注**——任务已升为 ⑤ 的层角色。

biz01 同步至**第 29 版**。提交 `86bc8b2`，已推 origin 与 gitee。

**仍未定（下次开工）**：① **⑦ 的意义空着**（现只写"是叶"）；② **两棵树现已逐级同构**（业务输出文档／任务节点↔活动节点／系统用例场景级操作）——中间那级是否也要统一名、同构是否有意；③ **§1.4 表下要补一句**：两侧叶同名是同一批实例还是两批（若同一批，01→02 落点为何是 ④ 而非 ⑦）；④ **三独立判据**随正文条目失去对象，留待用还是作废；⑤ **P0 树（§2.2.4）未动**（其 ⑤ 是配置信息组，非文档）；⑥ 91 号规范 A 类过时（PL6-A/B、正文条目、枝梢定义）+ B 类去层码，见 [[pending-91-align-to-94]]。

> **改动 94 的纪律**（2026-09-10 人类："你越界了"）：94 是权威文档，其**树形／口径／结构／分类**改动**只出建议、不落笔**；落笔须人类针对该条的明确授权。见 [[feedback-work-style]]。

## 一、全文结构（v1.18，两章）

**第一章「一、产品数据及其形态」**
- **§1.1 EOS 的产品数据清单**——清单表（产品数据 × 文档 × 空间 × 本文）＋ 业务树名矩阵 ＋ 两个空间（业务／方案，成对相对而言）
- **§1.2 各产品数据树成立的理论依据**（v1.18 正名，原「本文各树凭什么成立：三个前提」）——正文与三条标签、§1.4 引用一律用「依据」（**依据一** 条目化＝29148 良构需求 ／ **依据二** 树形化＝Simon 层级 + WBS 100% 规则，成树三条件＝判据单维 + 条目单归属 + 分组完备互斥 ／ **依据三** N:1 分配＝公理化设计 FR↔DP；定义域取需求叶、值域取方案枝梢、映射必须是函数）
- **§1.3 产品数据的两个形态**——叶＝末级／枝梢＝叶上一级／概要＝根→枝梢／完整态＝根→叶；叶只在完整态产出；叶有判据
- **§1.4 形态之间的承接与已定的树间对应**——承接规则「上游最深可用原子（叶）→ 下游能负责该语义的节点，枝干或枝梢、以语义定级」；可 N:1 不可 1:N；承接表**三行**（01→02 落枝干场景业务／04→05 落枝梢／06→07 落点【待裁】）；树间对应表四行（含「功能树↔非功能树共用 ① 信息系统 根」——v1.18 由张冠李戴处改挂）

**第二章「二、各产品数据的分解结构」**＝§2.0 原始需求（入口加工链）／§2.1 相关方需求（§2.1.1 功能／§2.1.2 非功能）／§2.2 业务流程四节／§2.3 系统需求（§2.3.1 功能／§2.3.2 非功能）／§2.4 平台构成／§2.5 配置方案【待定义】／§2.6 前后端组件方案【待定义】。

## 二、§2.2 四节的树形状（v1.18，**节序 Rn／Pn／R0／P0**）

R 树＝角色履职视角（↔SL）、P 树＝产品开发视角（↔PL）；下标 `0`＝平台本身、`n≥1`＝各业务。

| 节 | 标题 | 树的形状 |
|---|---|---|
| §2.2.1 | 业务运行角色履职业务流程（**Rn** 树） | 角色履职业务 → 业务域① → 相关方角色② → 角色职责③ → 场景业务④ → **业务输出文档⑤（枝梢；本级即文档开发任务）→ 活动节点⑥ → 系统用例场景级操作⑦（叶）** |
| §2.2.2 | 产品开发业务流程（**Pn** 树，引擎平台之外的产品） | 输出产品业务 → 业务域① → 产品类型② → 产品构件类型③ → 构件开发WBS④ → **业务输出文档⑤（同级即文档开发任务）→ 活动节点⑥ → 系统用例场景级操作⑦** |
| §2.2.3 | 业务配置角色履职业务流程（**R0** 树） | 角色履职业务 → 业务域①（流程与IT，单域） → 相关方角色②（业务配置角色：业务配置人员／平台治理角色） → 场景业务③ → **业务输出文档④ → 活动节点⑤ → 系统用例场景级操作⑥** |
| §2.2.4 | 引擎平台开发业务流程（**P0** 树） | 输出产品业务（单分支——只有配置单元） → 业务域① → 产品类型② → 构件③（引擎） → 配置单元④（CU） → 配置信息组⑤（**v1.22 未动**） |

- **节序 2026-09-10 互换**（原 P0 在 §2.2.3、R0 在 §2.2.4）——同一产品索引内 R 前 P 后。
- **R0 相对 Rn 少一级**：「角色职责」（SL4）——「相关方角色」之下直接是场景业务。§2.2.3 原有的两条防御性注已删（信息在 §1.1 矩阵、树行内联与 91 §5.4）。
- **P0 树 2026-09-10 收为五级**——删原 ④WBS 与 ⑦叶（对齐 91 §3.1「P0 无 WBS、CU 直占 PL4、配置信息组＝末级、无 PL6」）；原【待裁】注随之删。※P0 的 ⑤ 该算「叶」还是「枝梢」未明说（wft01-eng 称"业务流程概要末级节点"＝枝梢，91 称"末级"＝叶），94 未标。
- **两棵树**：Rn 与 Pn 独立，在输出文档处认亲互指；R0 与 P0 是同一产品（平台本身）的两棵视角树，bph-eng 是交汇点（此说明在 91 §5.1/§5.4，94 不载）。

**去噪与去码（v1.16 / v1.18）**：正文不留跨文档关系句／来历说明／跨节关系句／内联节号（保留清单表「本文」列＝索引、依据指针）；**v1.18 起 94 全文去 PL／SL 层码**（PL5／PL6-A／PL6-B／L1~L7），改用节点名与各树自己的 ①②③；**七棵树的「枝梢／叶」标注一律不标**（级语义由 §1.3 定义）。R↔SL、P↔PL 的**视角名别名**保留在 §1.1（是别名，不是层码）。

## 三、其他已定的口径

- **§2.0 加工链**：五态（原始／文本化／切分／澄清／规范化），只有切分与规范化动结构；「业务对象闭环」＝链上切分单位＝两树共用的 `场景业务` 级。
- **§2.1**：01 相关方需求不分形态；**§2.1.1 相关方功能需求树 v1.19 重画**（人类 2026-09-15 定）＝ **① 业务输出文档（**根**，属性＝所属主责角色／所属产品构件／所属E2E场景业务）→ ② 任务节点 → ③ 系统用例场景级操作（叶，`stfr-XXX`）**——角色与场景由树级降为 ① 的属性（索引／分片轴），原注「条目止于场景级、场景内不设输出文档／开发任务节点」作废；该形状即 94 v1.13 从 §2.2 删掉的那对（输出文档→文档开发任务节点→用例场景类操作），改挂 01 树。**连带改**：§1.4 承接表 01→02 行（上游叶改「③ 系统用例场景级操作」、落点改由 ① 的「所属E2E场景业务」属性定）、§2.0.1 表格与散文（切分＝长出业务对象闭环而非「第一级」；规范化＝长满 01 树）、§2.0.2（业务对象闭环在 01 树里是属性值，不再是树级）。**ID 前缀**：人类定 `stfr`＝相关方功能需求（前身 `spr`＝Specified (Stakeholder) Requirements，前身 `OR`）；94 内已换（仅 §2.1.1 两处）。§2.1.2 相关方非功能需求树＝一级类目→**分类维度**→具体需求（v1.18 由「非功能主题」正名，随 22 资产）。
- **§2.3**：两树根名 v1.18 加前缀 **`SR-F 系统功能需求树`／`SR-N 系统非功能需求树`**；§2.3.1 ③ 按**系统职责**聚类；§2.3.2 ④ SysReq-NFR 末级。
- **§2.4～§2.6**：**未动**（平台构成＝企业信息系统→信息系统→**模型**→前后端组件；§2.5 配置方案、§2.6 前后端组件方案【待定义】）。※AI 曾自行把 §2.4 收成一树两支并删 §2.5/§2.6，**被人类退回**，勿再自行改。
- **变更记录**：v1.18 起整节删除（00-doc-conventions「历史」为可选、历史版本由 Git 管理、文档内只留"轻量"变更记录）。版本号仍在文档头。

## 四、仍开着的（2026-09-10 收工时盘点）

1. **§2.4 平台构成**——需人类定方向：「模型」层无据（全仓独有；来历＝94 首版那棵被劈开的树，上半支演化成 P0）；③ 是两类（前端/后端）还是三类（+平台服务，91 §4/§8.5/§10.6 与 wft05-eng 均三类）；§2.5/§2.6 是独立产品数据还是 §2.4 的两支。
2. **§2.3.1 未收「功能表单不属于 SR 树」判据**（05 §0.2 与 91 §7.3 有）；06 至今仍写「srh-biz 末级＝功能表单」。
3. **PL/SL 层码链级未清**——94 已清；91 §3.1 的两张层级表（四套视角 ML/SL/PL/EL）、wft 链 16 份、三试点、数据文件仍用。**91 的偏差不止层码**——§5.2/§5.3/§3.1/§7.3 对 Rn／Pn 的层级描述也与 94 §2.2 对不上（§5.2 还自相矛盾）。人类 2026-09-15 定「91 要对齐 94，但**不是现在**」→ 详见 [[pending-91-align-to-94]]。
4. **`SR-N` 分叉**——94 用 SR-N，通用规范全篇 `SR-NFR`（675 处，且 `SR-NFR-001` 是节点 ID 前缀）。要收敛须链级更名。
5. **91 §7.3「biz 路径不在 07 重复定义」 vs 01-pl4eos-spec §4.6「07 覆盖配置方案组件」**——两者对不上。
6. **95 报告**的"落点"注仍逐字引 §1.2 旧标题「本文各树凭什么成立：三个前提」。
7. **§2.1.2 与 bph-nfr 的关系未交代**——bph-nfr 落 02、归组锚点是四元组，形体与 §2.1.2 不同（AI 判断两者本不冲突，属推断，未落笔）。
8. **变更记录是否推广到 91/05/06/07**（它们都还带版本史节）。
9. **§2.3.1 的叶（⑥ IPO功能）由平台构成树哪一级承接**【待裁】（表内已记）。
10. **数据文件缺口**：06 无完整态叶树；01/04 无树画像区。
11. **「页面视角」在 94 无家**（2026-09-15 审视 93 时检出）——「04 的**输出文档（枝梢）** ↔ 功能表单」既不是承接（§1.4 承接源只取上游叶），也不在 §1.4「其余已定树间对应」表（该表限定"**不涉及视角转换**"）；功能表单已被 §2.5 指为"旧平台对象、产物归属在配置方案收口"，而 §2.5 仍【待定义】。AI 拟了两处补法草案（§1.1 补「**第三个视角：页面视角**」+ §2.5 补"功能表单挂本树、不挂系统需求树"），**待人类定**。这正是 93 v0.13 与试点 biz03 里"04 输出文档↔功能表单待 94 §2.5 收口后回填"所指。

12. **01 树新增的两级（① 业务输出文档 / ② 任务节点）由谁写出**——94 v1.19 把「长满 01 树」归给 ort03，但 01 现行物理文件以角色分片直接挂条目、没有这两级。**连锁**：ort03（正文+SKILL）按「切出场景业务级 + 条目落位」设计，**未随**。※**biz01 已随（2026-09-15，v0.20-pilot）**——§2.2 X 树改新形状、主责角色/产品构件/E2E场景业务降为 ① 属性、§2.3 复核项改「三坐标齐备唯一 + 条目语义一致」、§4.1 承接改「落点由 ① 属性定、① 整体分流、单承接由树形保证」、§4.2 文档清单改以 X 的 ① 为准且任务类型改读 ②；原 §2.2 注 1「场景业务级待 01 模板重建落位」已废。**仍待**：ort03 侧、01 数据文件树画像。
13. **`spr-` → `stfr-` 链级更名**——94 内只 §2.1.1 两处已换，链上仍是 `spr-`：112 处 / 22 文件（01 数据文件 30 处，含派生家族 `spr-SRC-*`；ort03 正文 16 + SKILL 4；91 4；06 术语表、通用规范、02/08/22 等）。**无脚本引用**（`.sh/.js/.py` 零命中）。生产者是 ort03（「跨源文件统一分配 `@spr-{seq}` 节点 ID 与 `spr-{seq}` 业务编号」）。2026-09-15 人类说「那你改吧」，语境指向 94 内部两处矛盾（§1.4／§2.0），**链级更名未获明确指令、未动**。
14. **`stfr` 的 F 覆盖不了 01 的全部条目**——同一条 ID 序列上挂着 FR-BIZ / NFR / FR-ENG 三类，路由现靠 `类型` 字段（biz01 §2.4 受理门槛）。分族（`stfr` / `stnfr` / 引擎侧另定）还是 `stfr` 只当 §2.1.1 这棵功能树的叶前缀、其余用通名——**未决**；分族则还牵出「各族序号自 001 起还是共用号池」。

关联 [[feedback-work-style]]、[[eos-design-landing-spec-93-refactor]]、[[page-view-vs-classification-view-correspondence]]、[[tree-level-vs-expansion-item]]、[[business-def-vs-engine-ipo-product-split]]、[[pending-91-align-to-94]]。
