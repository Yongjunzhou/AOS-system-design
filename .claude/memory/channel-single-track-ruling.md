---
name: channel-single-track-ruling
description: 定案：反馈通道单轨（2026-09-20，全链）——线下标注轨退役、确认收进轮内对话；91 已落，跟随面未扫
metadata: 
  node_type: memory
  type: project
  originSessionId: 8f162f09-b5c9-4567-9503-dd7687a36c02
  modified: 2026-09-20T08:57:22.918Z
---

**人类 2026-09-20 定**：人类对 AI 产出的方案给反馈，通道**只有对话一条**，全链一致。原 [91 §A.5](../../20-pl4eos/10-pl4eos-subpl-sysdev/10-wfsysdev-4-eos/91-eos-biz-eng-spec.md) 的「线下文档修订」轨——人类在 `确认状态` 字段标注 `[同意]`/`[修改]`/`[驳回]`、或自由文本编辑方案内容，经 git diff 检出——**退役**。这是把 2026-08-19 的「反馈双轨化」（那次是 wft01/02/03 链级统一到双轨，91 §A.5 随之 v5.20）**翻回来**，翻转本身已经人类确认。

**Why**：确认动作搬进了轮内（一轮从选料走到入库），就没有事后标注的位置。双轨存在的理由是"人类可能不在会话里"，新序假定人类就在会话里。判据：**这条通道承载的是"确认"还是"回流"**——确认（人类对 AI 方案表态）可以在会话里一次做完；ort02 那种"持问题清单线下向相关方澄清"是**回流**，性质不同（但仍已人类裁定一并退役，见下）。

**91 已落（v5.64，提交 `5a1d1775`）**：
- §11.8 整节重写——撤「结构化反馈（人类在字段里标注）」「非结构化反馈（自由文本 + git diff 兜底）」；**`确认状态` 字段改由 AI 单方写入**——取值表六行（`(空)`／`[同意]`／`[修改]`／`[驳回]`／`[已处理]`／`[需裁决]`）**一字未删**，语义从"人的指令"滑到"AI 的记录"。
- §11.10 由「AI **反馈检测**规则」**改主题为**「AI **变更感知**规则」，**节号不动**——判定分支改为"上游下发的新版本／非预期修改／排版"。HEAD 锚点机制 R14~R16 照旧。
- §11.7 的 `待反馈处理` 分节**条件**改为「节点停在本 Skill 待确认态」；§11.11 同步更新清单改「处理一处反馈」。
- §A.5 双轨改**单轨**；§A.5.1 流程起点改「产出方案，列出未确认的方案」；§A.5.4 确认状态字段收窄；特化参数落点改「讨论确认那一步」。
- §A.4.1／§A.4.2／§A.4.4——入口② 由「有待处理反馈？含人类标注但无 已处理」改「**有待确认的方案？**节点停在待确认态」；优先序由 `退回优先 → 反馈处理 → 新输入路由` 改 **`退回优先 → 新输入路由 → 反馈处理`**。
- §A.3.3 步骤零 R0a 改「检出**本任务自身之外**的修改」。

**未扫（下一步）**：
1. **8 份 wft 指南 ＋ 各自的 SKILL.md ＋ abp／cr**（约 22 文件）的「反馈双轨」段、变更感知段、`先反馈后新增` 表述。
2. **八处已失效的锚点**——`[§11.10](91-eos-biz-eng-spec.md#1110-ai-反馈检测规则)`，节号在、标题不在（节号稳定但标题仍是定位的一部分；以后引权威源宜指向 §号）。
3. **`待反馈处理` 分节名保留未改**——改名会牵脚本、各 SKILL 与产品数据文件里的字面量，属[[pending-subpd-subpl-naming]]那类"证据冲突不能擅自扫"；要改须一次性全扫。
4. **§A.5 模式分化表 ort 两行**仍写「双轨」与线下通道——人类明说"OR 业务链也去掉线下那条轨道，只是现在不用管"。它与新的单轨表述**明着相抵**，是 91 目前唯一一处自相抵。

**OR 链**：人类 2026-09-20 明定"也去掉线下那条轨道，只是现在不用管，后续我们再讨论优化"。

关联 [[pending-biz01abr-ch5]]、[[skill-guide-ch4-ch5-boundary]]、[[wft01-biz-review-ort02-benchmark]]（2026-08-19 双轨化那次的反向）、[[feedback-work-style]]。
